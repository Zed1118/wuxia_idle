# Codex 视觉验收派单 · 半手动战斗 P0 步骤5 全闭环

**commit：** 37a88d23 · **日期：** 2026-06-13
**验收包：** `build/macos/Build/Products/Debug/wuxia_idle.app`（`./tool/build_acceptance.sh` 预编，VISUAL_ROUTE=hub）
**分辨率固定 1280×720**（沿派单惯例，避免窄屏裁切）。

## 验收范围说明（先读）

步骤5 新东西有三块：① manualStep 首通 UI ② autoReplay 动画 ③ 全局开关切手动。
其中 **② autoReplay 动画 / ③ 全局开关行为** 是**时序动画 + 行为验证**，无静态 route，
**Codex 不验**（由用户真玩，剧本另出）。

**本派单 Codex 只验 ① manualStep 首通 UI**（静态/交互截图）——`battle_manual_step` 路由。
此 UI 自步骤3c 起未变（步骤4/5 只接了 host glue，没动 BattleScreen 单步 UI），
步骤3c 派单写了但未回收，本次合并刷新 commit 一并验收。

## Codex 验收点（路由 `battle_manual_step` · 逐项截图 + PASS/FAIL）

hub 总入口 → 点 **`battle_manual_step`**（scenarioChargeBreak 满配主控 + seed=3 确定性）。

⚠️ 本路由是**交互验收**（非开屏即终态）：进屏后需点击驱动。

1. **入口/初始态**：进屏底部指令台右侧应是实心绛金「**下一步**」按钮（非「快进」）。
   开屏不自动播（画面冻结在 seed 态，无人出手）。
2. **行动顺序条**：点 1~2 次「下一步」推进到 tick 边界填队列后，顶部出现
   「**本回合行动顺序：甲 ▸ 乙 ▸ …**」条，队首角色高亮（绛金描边）。
   截图验：顺序条存在、角色名可读、队首高亮明显。
3. **强制停顿逐步**：继续点「下一步」，每点一次只结算一个 actor（飘字/弹道逐个出），
   行动顺序条随队列缩短、队首前移；队列空后再点 = 新回合边界。验「一步一 actor」节奏。
4. **目标 picker（立即弹）**：选主控（左下头像），点其一个**单体技**（强力/破招/大招）。
   应**立即弹**「选择目标」弹窗，列存活敌人（带血量），无死亡敌人。
   截图验：弹窗水墨样式、敌人项可点、标题「选择目标」。
5. **选目标下发**：picker 里点一名敌人 → 弹窗关闭，该技能按钮盖「待发」印。
   再点「下一步」推进到该 actor 行动时，应攻击所选目标（飘字落在该敌身上）。

## 关注的视觉质量

- 「下一步」按钮与快进区别明显（实心 vs 描边），点击区够大（96×52）。
- 行动顺序条不与顶部危险条（敌蓄力时）打架/重叠，两条都在时排版正常。
- picker 弹窗在 1280×720 居中、不溢出、底色 panel 与战场背景对比够。

## 已知非 bug

- seed=3 是固定确定性场景；蓄力敌人在场时危险条 + 行动顺序条同屏属正常。
- 鼠标合成若不稳（历史 charge_break 验收遇过），改人工点击；FAIL 前先确认是合成问题非 UI 问题。

## 回收

截图 + 逐项 PASS/FAIL 写 closeout `docs/codex_step5_manual_step_ui_<date>_closeout.md`；
误判项注明测试已覆盖（battle_manual_step_ui_test 8 测）。真玩手感（节奏顺不顺 /
picker 打断感）单列「真玩拍板」不计入视觉 FAIL。

---

## 附：autoReplay 动画 / 全局开关 = 真玩剧本（用户，不走 Codex）

无静态 route，须真实游戏（`flutter run -d macos` **不带 VISUAL_ROUTE**，玩真实存档）验：

- **manualStep 真实首通手感**：进任一未通关关卡 → 验强制手动、节奏顺不顺、picker 打断感。
- **autoReplay 动画**：手动首通一关 → 重打同关 → 应自动播放动画重演（确定性复刻同样打赢），
  验动画顺不顺、有没有跳帧/漏招、节奏可不可忍。
- **全局开关切手动**：设置面板关「自动战斗」总开关 → 重打已通关关卡应回到手动单步，
  验开关确实生效。
- **覆盖全 5 类战斗**：主线/塔/心魔/轻功。**群战** autoReplay 已知降级 autoFallback
  （formation 玩家输入未入 seed+ops），手动首通仍录制——验群战重打走的是现有自动战斗（无害）。
