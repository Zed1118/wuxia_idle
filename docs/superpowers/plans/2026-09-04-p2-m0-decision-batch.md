# P2 M0 决策打包计划（2026-09-04）

## 结果合同

- 单一目标：把 M0 现有 22 条开放登记压缩成一份可一次确认、可逐项追溯的决策包。
- 固定分母：20 条 `tuning`、1 条 `deferred_pending_matrix`、1 条 `partially_reopened`。
- 实时基线：4 条 tuning 已有人类选择，16 条仍无选择；心魔 AI 缺四列矩阵；事实性失败展示已集成但登记未对账。
- 本批预期增量：完成 22/22 分类与事实对账，把真正需要的新选择压缩为 1 个推荐批次。
- 不计进度：文档、候选方案、测试数量、READY 均不代表 M0 正式关闭。
- 成本上限：90 分钟；不改生产代码、数值、玩法、GDD 或 `PROGRESS.md`，不 merge、不 push。

## 执行与验收

1. 以当前 `main`、决策台账、任务台账和已合入提交为事实源，区分“已决定”“待证据”“待选择”。
2. 对已完成但登记滞后的事实性失败展示、姿态生产接线做台账对账。
3. 补齐七心魔 canon / narrative / behavior / test 四列候选矩阵，但保持现有 AI，不冒充逐名签字。
4. 将 16 条未选择 tuning 分成 14 条“保持当前基线”和 2 条“先测量后冻结”，给出一条可直接批准的推荐回复。
5. 解析 YAML、核对固定分母、检查 diff 仅含治理文档，并在独立分支提交 READY 候选。
