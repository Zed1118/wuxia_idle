# 自动与点选战斗分工说明计划

> 上游稳定点：`codex/battle-effect-lifecycle@5820da95`
> 分支：`codex/battle-intervention-value`

## 1. 目标

让顶栏战斗方式标识不仅说“自动战斗 / 可点选”，还明确两种方式的职责与点选代价：托管按轮转谱行动；可点选仍自动推进，但点技能会预支该角色下一次行动并重新回势。

## 2. 方案

- 保持现有 30px 模式胶囊与双视口布局不变。
- 用桌面端 hover/focus tooltip 承载解释，不增加教程弹窗或常驻 HUD 密度。
- 文案集中进入 `UiStrings`，不在 widget 内硬编码中文。

## 3. 验收

- [x] 纯自动模式提示托管轮转语义。
- [x] 可点选模式提示持续自动、预支行动与重新回势语义。
- [x] 胶囊可访问性 label/value 不回归。
- [x] 战斗模块、analyze 与双视口实机 UI 通过，冻结 `[READY]`。

## 4. 当前恢复点

- **状态**：可冻结。
- **最后完成**：模式胶囊接入 hover/focus 提示与去重后的无障碍语义；常驻尺寸、布局和视觉不变。
- **下一步**：提交 `[READY]` 稳定点，继续审计代表性战斗节奏。
- **已跑验证**：模式提示 2/2、指令台组合 41/41；战斗模块 734/734；`flutter analyze --no-pub` 零问题；`battle_tap_preview` 1280×720、1440×900 实机截图无溢出、遮挡或顶栏挤压。
- **阻塞项**：无。
