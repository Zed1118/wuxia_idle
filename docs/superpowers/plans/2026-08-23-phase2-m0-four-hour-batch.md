# 二阶段 M0/M1 四小时高速执行批

## 目标

以 `e292d3a069fbc0e129dd74fafc1ebb3746f53557` 为冻结基线，在不直写 `main`、不偷偷冻结 `PROPOSED` 产品决策的前提下，完成二阶段 M0 底账与可并行的 M1 纯领域候选切片。

## 安全边界

- 所有执行端使用独立 worktree/分支，不修改主 worktree。
- 禁止部署、发布、推送、强制重写历史、破坏性删除和依赖升级。
- 连续三次验证失败、文件越界、公共契约冲突、需用户拍板或时限到期时，更新恢复点后停止。
- Pi/Qoder 输出均视为待审候选；只有 `[READY]` tip + clean worktree 才进入主审。

## 任务切片

1. 主调度：维护 decision/task registry、资源锁、状态和恢复点。
2. Pi + DeepSeek V4 Flash：只读定位换波冷却、远征并发、主线掌门指针与心魔惩罚的真实证据。
3. Qoder + Qwen3.8-Max：建立当前性能、确定性、headless 与生产路由的测试基线地图。
4. Codex geometry：新文件实现六几何纯领域候选与边界测试，不接生产。
5. Codex posture：新文件实现单一“累计姿态伤害”候选与状态机测试，不接生产。
6. Codex gap audit：只读独立复核未决项是否会阻断后续路径。
7. Codex status：在 gap audit 完成后复用执行槽，新文件实现状态固定拍与叠加/刷新候选，不接生产。

## 验收 checklist

- [ ] 生产接线证据：M0 证据包指明真实入口/消费方；M1 候选明确尚未接线。
- [ ] targeted test：实现分支附命令、通过数与失败证据。
- [ ] 红线影响：不改数值硬红线、三系锁死、在线=离线或反主流边界。
- [ ] 残留风险：未接线、未视觉验收、公共 API 尚未 G1 冻结等必须显式列出。
- [ ] 外部补丁文件白名单、工作区干净和 `[READY]` tip 均达标。
- [ ] 集成前独立审真实 diff，不以执行端自报为验收。

## 当前恢复点

- 状态：首批已完成并冻结恢复点；生产接线等待 G0/G1。
- 最后完成：冻结主仓基线，建立 13 个二阶段隔离 worktree，完成 Pi 证据包与 C02–C10 九个纯领域候选的主审/独立复核；临时整合态 77/77 targeted 与 18 文件 analyze 通过。
- 下一步：Qoder 基线包已主审收尾；补齐最终恢复点并将协调文档并入候选整合分支，不合入 `main`。
- 已跑验证：主 worktree clean；`main == origin/main == e292d3a`；Pi/Qoder 连通与派单已在前置复检通过。
- 阻塞：五类产品 `PROPOSED` 决策与 6 类已否方向重开冲突等用户回来后拍板；不阻塞本批 M0/M1 候选切片。
