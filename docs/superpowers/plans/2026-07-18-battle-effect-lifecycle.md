# 战斗特效生命周期验收计划

> 上游稳定点：`codex/battle-terminal-danger-clear@b97ecfc2`
> 分支：`codex/battle-effect-lifecycle`

## 1. 目标

冻结同施放同位特效合流后的完整生命周期：重复命中应从头续播，原始 520ms 截止点不得误删；最后一次续播完成后必须移除并释放，避免长时间挂机积累活跃贴片。

## 2. 方案

- 直接驱动生产 `BattlePlaybackController.playAction`，不另造测试实现。
- 在首次特效接近结束时重复同一动作，跨过首次截止点确认实例仍在。
- 再跨过续播截止点确认全部实例退场；同时保留现有同组数量与跨 tick 分组断言。

## 3. 验收

- [x] 同组特效数量保持为一组流派贴片加一组暴击贴片。
- [x] 跨过首次 520ms 截止点后仍处于续播态。
- [x] 跨过末次 520ms 截止点后活跃特效归零。
- [x] 战斗模块与 analyze 通过，冻结 `[READY]`。

## 4. 当前恢复点

- **状态**：可冻结。
- **最后完成**：生产 controller 的同组续播、旧截止点保活和新截止点退场已形成临界时间契约。
- **下一步**：提交 `[READY]` 稳定点，转入战斗方式与手动干预收益审计。
- **已跑验证**：`battle_playback_controller_test.dart` 20/20；战斗模块 732/732；`flutter analyze --no-pub` 零问题。该切片不改 UI 像素，继承上游双视口实机验收结果。
- **阻塞项**：无。
