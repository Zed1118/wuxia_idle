# 2026-07-19 审查报告落地 · 三目标序列计划

## 目标与分支

- 基点：`main@db9151d1`（建立时与 `origin/main` 一致）。
- 分支：`codex/audit-fix-20260719`。
- worktree：`.worktrees/audit-fix-20260719`。
- 执行纪律：目标 1 完成后冻结为 `[READY]`，等待主会话终审；未获续跑指令前不进入目标 2。目标 2、3 同理递进。
- 禁区：不碰 `PROGRESS.md`、`pubspec.yaml`、`character_avatar.dart`、`saveVersion`、结算公式层；发现必须触碰即停为 `[BLOCKED]`。

## 目标 1 · 玩家误导热修

### 验收标准

- `UiStrings.glossarySchool` 与生产 `SchoolCounterMatrix` 的三向克制一致；新增从生产矩阵读取方向的同源语义测试，并留破坏证红。
- 实测 `DispelService.dispel` 后，dialog 如实展示：永久内力不变、增加 6 小时内息紊乱且累计上限 12 小时、旧主修修炼度保留一半且可能回层；换入主修（原辅修）修炼度不动。新增同源语义测试并留破坏证红。
- `dispersionInternalForcePenalty` 经 `rg` 实测除解析/字段定义外，唯一生产消费方是旧 dialog 预览，当前结算无消费方。本目标只移除误导性展示，不删除字段、不改 `numbers.yaml`；字段后续处置交主会话另行拍板。
- `GDD.md` 发布边界改为绝对层 17（Lv170）；相邻路线段按现有生产/测试真相源同步为 40 关主线首通 Lv57、全内容参考路线 Lv91，并明确当前内容不要求升满发布 cap。此范围足够且无歧义，无待拍表述。
- `data/stages.yaml` 仅更新头注。实测命令：

  ```sh
  awk '/^[[:space:]]+stageType: / {count[$2]++} END {for (type in count) print type, count[type]; print "total", count["mainline"]+count["innerDemon"]+count["lightFoot"]+count["massBattle"]}' data/stages.yaml
  ```

  输出：`mainline 40`、`innerDemon 7`、`lightFoot 5`、`massBattle 5`、`total 57`。
- targeted 覆盖 strings/glossary、dispel、technique panel 与邻近 `battle_log`；`flutter analyze --no-pub` 0 issue；改动 Dart 全部 format；`git diff --check` 通过。
- §8.2 四证据齐全：生产接线、targeted 命令与通过数、红线影响、残留风险均写入本文件恢复点。

### 切片

1. 新增两条同源语义回归，先在旧生产代码上观察 RED，记录失败摘要。
2. 最小化订正 `strings.dart` 与 `dispel_dialog.dart`，同步既有 widget 测试，跑 GREEN。
3. 仅订正 `GDD.md` 指定段与 `stages.yaml` 头注；GDD 变更用 `[GDD]` 中文动宾提交。
4. 跑完整 targeted、analyze、格式/diff/禁区检查，回填 §8.2 证据并以 `[READY]` tip 冻结。

### 破坏证红记录

- 命令：`flutter test --no-pub test/shared/player_guidance_semantic_sync_test.dart`。
- 克制 RED：旧 `glossarySchool` 实际为“刚猛克灵巧…”，测试从生产
  `SchoolCounterMatrix.multiplierFor` 推导出期望“刚猛克阴柔”，失败于
  `does not contain '刚猛克阴柔'`。
- 散功 RED：真实 `DispelService.dispel` 返回 `internalForceBefore=5000`、
  `internalForceAfter=5000`，旧 `UiStrings.dispelCostInternalForce` 却产生
  `内力 5000 → 5000`，失败于期望 `永久内力 5000（不变）`。
- GREEN：同源语义测试 2 条 + `technique_panel_screen_test.dart` 18 条，合计
  `20/20` 通过。真实结算另断言紊乱 `9h + 6h` clamp 到 `12h`、旧主修
  `800→400`、换入主修原辅修进度 `275` 不变。

## 目标 2 · 存档安全

### 基点同步

- 目标 1 已由主会话合并为 `6a441286`；2026-07-19 续跑时确认目标 1 tip
  `2564131b` 已在 main，worktree 由 `2564131b` fast-forward 到
  `main@4d434dc0`。同步包含 K1 将 `EquipmentDisposalDef` 从
  `numbers_config.dart` 抽出的变更，目标 3 不再基于旧结构开工。

### 根因与修法判定

1. **前向版本降写**：`IsarSetup._ensureSaveData` 仅判不等，导致 `0.99.0`
   同样进入 `_migrateSaveData`；尾部无条件把 `saveVersion` 写成 `0.37.0`。
   修法是在唯一入口使用既有 `_compareVersion`：高于当前时抛专用异常，禁止进入
   migration transaction；`init` 捕获后关闭刚打开的实例并清空全局句柄，避免 caller
   捕获异常后误用。`listSlots/_readSummary` 同样拒绝未来版本，使存档选择屏既有
   `AsyncValue.error → ErrorFallback` 明确呈现失败。
2. **schema 打开前备份评估**：现有 `SaveManagementService.createBackup` 依赖已打开的
   `Isar` 与当前 `SaveData`，`SaveRestoreDatabaseOps` 也只负责候选校验/关闭，无法在
   未打开 schema 时读取版本；直接复用会形成循环。若要严格“识别未来版本前绝不
   打开原库”，只能为所有既有档先做 raw file preflight 副本，或新增外置版本 manifest，
   均超出本单点修并需恢复/清理 spec。目标 2 采用应用层零写 fail-closed；不自行扩架构。
3. **心跳旧对象回写**：`touchOnlineNow` 在事务外读完整 `SaveData`，等待事务期间其他
   写入可被 stale `put` 覆盖。修法：事务开始后重读，仅改 `lastOnlineAt` 再 put。
4. **主菜单四任务竞态**：并发本身是既有首帧行为；实际跨 collection 冲突点是
   `unlockJianghuJourneyOnOpen` 在事务外读旧 `SaveData` 后完整 put，可能覆盖离线结算
   刚写的累计字段。为避免串行 await 离线 recap dialog 改变启动体验，只把远行解锁
   改为 transaction 内重读并最小改 `jianghuJourneyUnlocked`；Isar 写事务串行化消除窗口，
   `main_menu_startup_gate.dart` 保持四任务对外时序不变并补安全注释。
5. **周目跨槽残留**：`SelectedChallengeCycle` 的 keepAlive family 仅以 chapterKey
   为 key，切槽 invalidate `isarProvider` 不会清它。修法将 family key 扩为
   `(slotId, chapterKey)`，生产 wrapper 统一绑定 `IsarSetup.currentSlotId`；导航往返仍
   keepAlive，同章不同槽天然隔离，不改战斗语义。

### 切片与验收

1. 写真 Isar `0.99.0` 前向档回归：拒绝迁移、实例关闭、代表字段零改写、
   `SaveSelectScreen` 命中既有 `ErrorFallback`；旧实现破坏证红。
2. 给 touch/journey 加仅测试用 barrier seam，确定性注入“事务前有并发写”；旧实现
   观察字段被覆盖 RED，再移至事务内重读得 GREEN。
3. 将 cycle provider key 绑槽；测试 slot1 选择不出现在 slot2，且 slot1 导航 keepAlive
   语义仍保留；旧单 key 测先作行为 RED 记录。
4. 跑 save/restore、isar_setup、startup/journey、cycle provider/control targeted；
   `flutter analyze --no-pub` 0 issue；零 saveVersion bump。

### 破坏证红与修复证据

- 前向版本 RED：真 Isar `saveVersion=0.99.0` 调 `IsarSetup.init` 实际正常返回
  `null`，未抛 `UnsupportedSaveVersionException`；证明旧入口把未来档送入迁移。
- touch RED：barrier 停在旧实现“事务外读取完成、writeTxn 未开始”之间，并发把
  `slotName` 写为“并发改名保留”；旧 touch 恢复后完整 put 使实际值回到 `null`。
- startup/journey RED：barrier 期间模拟离线任务把 `totalPassiveExperience` 写为
  `321`；旧远行解锁旧快照 put 后实际变成 `0`。
- cycle RED：slot1 `ch1` 选第 2 周目后切 `currentSlotId=2`，旧单 key provider
  实际仍返回 `2`，未回 `null`。
- GREEN：四组核心回归 + cycle control 共 `27/27`；扩展到全部 IsarSetup
  migration/slots、save/restore、save-slot startup、online presence、expedition startup、
  journey、cycle provider/control、settings restore 后 `76/76`。
- UI 测试边界：真 Isar IO 直接放进 `testWidgets` 会触发本项目既有 fake-async
  死锁；故真 Isar 测负责证明 `init/listSlots` 都抛同一前向异常，widget 测用该异常的
  `AsyncValue.error` 覆盖 provider，证明 `SaveSelectScreen → ErrorFallback` 分支命中。

## 目标 3 · 远行/断魂庄数值回填（进行中）

### 基点与方案

- 目标 2 已由主会话合并为 `85f173ff`；续跑时 worktree 从目标 2 `[READY]`
  快进合并到 `main@0d0a6e6d`，同时纳入 C3 群战递补表现层修复。合并零冲突；
  `character_avatar.dart` 的变化只来自 main 基线，本目标不触碰。
- 远行 schema 落在既有 `data/expeditions.yaml`：新增普通/险关的确定性敌队池，
  每个敌人继续复用 `EnemyDef` schema；新增由 YAML 提供的逐节点 HP/攻击增长率与
  封顶值。`ExpeditionConfig` 负责按 `nodeSeed` 选队、按 `node.index` 计算并 clamp，
  `ExpeditionCombatRunner` 只消费配置，不保留中文敌名、基础数值或成长公式。
- 敌队 `skillIds` 只引用生产 `skills.yaml`；加载期补同源引用完整性校验，并要求
  普通/险关池均非空、全池覆盖刚猛/灵巧/阴柔，防配置退化回单流派空招式。
- 断魂庄以生产 `GauntletBattleRunner` + `test/support/progression_battle_probe.dart`
  的真实装备/心法建队路径新增 `test/tools` 三档诊断：入门=Lv100（三流·熟练）、
  推荐=Lv140（二流·启蒙）、当前满配=Lv170（二流·熟练）；每档固定多 seed 连跑三关，
  保留关间 HP/真气/冷却/倒下状态，不注入补给，输出单关及整庄胜率。

### 切片与验收

1. 先加远行 schema/生产接线语义测试与 runner 源码 guard，在旧实现留 RED；实现
   YAML 敌池、曲线、加载期技能引用校验，跑 expedition targeted 后提交 `[schema]`。
2. 新增断魂庄三档胜率探针，先记录现占位数值基线；只调
   `data/boss_gauntlets.yaml` 的 `baseHp/baseAttack/baseSpeed`，迭代到入门不无脑过、
   推荐有稳定通关空间、满配仍实际交战，提交 `[balance]`。
3. 跑 expedition/gauntlet/balance targeted、配置红线、runner grep、
   `flutter analyze --no-pub`；确认禁区与 `progression_release_budget_test` 零 diff。
4. 回填胜率表、RED→GREEN、§8.2 四证据与五项恢复点，树净后提交 `[READY]`。

### 明确边界 / 待拍

- `[BLOCKED]` 三选一“专属具名装备”需要新装备定义与 lore 内容批，本目标只保留现有
  三件占位引用；是否立项需主会话另拍。
- `[BLOCKED]` `iconPath` 依赖 MJ 产图，本目标保持空占位；是否补图需美术批另拍。
- `progression_release_budget_test`、`saveVersion`、`numbers.yaml`、结算公式层均零触碰。

### 远行 schema 破坏证红

- RED 命令：`flutter test --no-pub test/features/expedition/expedition_config_validation_test.dart test/features/expedition/expedition_config_load_test.dart test/features/expedition/expedition_combat_runner_test.dart`。
- 旧 `ExpeditionConfig` 编译期缺少 `normalEnemyTeams` / `eliteEnemyTeams` /
  `enemiesForNode`，证明生产 schema 尚未承载敌池与深度曲线；可独立编译的 runner guard
  同时失败于中文注释/敌名、`EnemyDef(` 和 `300 + depth * 15` 等 Dart 数值公式。
- 首轮 GREEN：上述三文件 `15/15`；生产 YAML 加载、曲线 cap、稳定 seed、三流派技能
  引用、真 Isar 派遣后前五节点战斗均通过。
- 扩展 GREEN：`flutter test --no-pub test/features/expedition` 为 `69/69`；六个改动
  Dart/测试文件定向 `flutter analyze --no-pub ...` 为 0 issue；runner PCRE grep 对
  中文、数值字面量、`EnemyDef(` 零命中；`git diff --check` 通过。

### 断魂庄三档校准证据

- 探针口径：固定 `50` seeds；三人分别刚猛/灵巧/阴柔；入门档 Lv100（三流·熟练、
  欠配）、推荐档 Lv150（二流·启蒙、标准配装）、满配档 Lv170（二流·熟练、近满配）；
  生产 `GauntletBattleRunner` 真跑三关，关间经 `GauntletController` 白名单继承
  HP/真气/冷却/倒下状态；不使用补给，故读数是保守自动战斗基线。
- 旧占位 RED：入门/推荐/满配整庄胜率 `98% / 100% / 100%`，入门档明显无脑通过；
  平均三关累计 ticks `130.7 / 54.9 / 40.4`。
- 校准后结果：

  | 档位 | 第一关累计胜率 | 第二关累计胜率 | 整庄胜率 | 最终相位到达率 | 平均累计 ticks |
  |---|---:|---:|---:|---:|---:|
  | 入门 Lv100 | 96% | 2% | 2% | 2% | 61.6 |
  | 推荐 Lv150 | 100% | 100% | 50% | 50% | 101.1 |
  | 满配 Lv170 | 100% | 100% | 100% | 100% | 53.2 |

- 判读：入门档在第二关护法血墙形成真实门槛；推荐档稳定到达庄主且一半通关；满配
  稳定通关但所有固定 seed 均把闻九针推进最终阶段，并非跳过相位机制。三档 bracket
  以测试硬守 `入门≤35% / 推荐40%~90% / 满配≥80% / 满配最终相位≥80%`。
- 数值 RED：broken loader 把庄主 `baseHp` 改为 `60001`、敌人 `baseAttack` 改为
  `2001` 时，旧加载器均错误接受；新增加载期 fail-fast 后两条转 GREEN。最终最高
  敌 HP `42000 < 60000 < 1M`、最高敌攻击 `1100 ≤ 2000`；三档玩家 HP/内力/单件
  装备攻击与全部引用招式倍率由同源 `numbers.combat.redLines` 逐项断言。
- 扩展 GREEN：boss_gauntlet 全目录 + 三档探针 + 联合经济模型/探针 + gauntlet
  item/wave-b/skill-source 红线合计 `165/165`；八个改动 Dart/测试文件定向 analyze
  0 issue。联合经济模型的旧占位胜率已同源回填 `0.02 / 0.50 / 1.00`。

## 当前恢复点

- 状态：目标 1/2 已终审合并；目标 3 已同步 `main@0d0a6e6d`，正在远行 schema RED 阶段。
- 最后完成：核清远行旧 runner 以 Dart 单敌占位合成且绕过 `ExpeditionConfig`；确认
  断魂庄三队机制已全由 YAML→`EnemyDef`→生产 runner 接线，当前仅基础三数仍标 TODO。
- 下一步：写远行 schema/接线失败测试，留破坏证红后实现 `[schema]` 切片。
- 已跑验证：
  - 预热：`flutter pub get` exit 0 且 `pubspec.lock` 零 diff；build_runner exit 0（128 outputs）。
  - 破坏证红：见上文两条 RED 摘要；修复后同源语义 + technique panel 首轮 `20/20`。
  - 文档真相源：release cap + progression budget `7/7`；GameRepository production counts 定向 `1/1`；stageType 命令输出 40/7/5/5、总计 57。
  - 最终 targeted：`flutter test --no-pub test/shared/player_guidance_semantic_sync_test.dart test/features/help/glossary_topic_label_test.dart test/shared/widgets/glossary_tip_test.dart test/features/dispel/application/dispel_service_test.dart test/features/dispel/application/dispel_persist_test.dart test/features/technique_panel/presentation/technique_panel_screen_test.dart test/combat/battle_log_test.dart`，`65/65` 通过。
  - 静态检查：`flutter analyze --no-pub`，`No issues found`（90.0s）。
- 目标 2 最终：核心 GREEN `27/27`；最终扩展 targeted `76/76`；
  `flutter analyze --no-pub` 为 `No issues found`（11.3s）；全部改动 Dart format 0 changed。
- 阻塞项：实现阻塞无；具名装备/lore 与 `iconPath` 分别待内容批、美术批拍板，按目标
  边界保持占位，不阻塞数值回填。

## §8.2 交付证据

- **生产接线证据**：流派短释义由 `HelpTopic.school.shortText → UiStrings.glossarySchool` 真实帮助入口消费，方向由 production `NumbersConfig.schoolCounter` 守卫；散功 dialog 仍由 `TechniquePanelScreen` 的既有换主修确认路径打开，确认后真实调用 `DispelService.dispel`，展示值与同一 production config/service 语义对照；GDD 数字由 production release cap 与 progression tests 复核；stages 头注由全部生产 `stageType` 条目重算。
- **targeted 结果**：最终直接相关 + 邻近 `battle_log` 共 `65/65`；文档真相源定向另 `8/8`；两条新增同源语义测试均有有效 RED→GREEN 记录。
- **红线影响说明**：零数值表/结算公式/schema/saveVersion 改动，零三系锁死与在线=离线影响；`stages.yaml` 只改注释。中文仍只在合法集中 sink `UiStrings`，presentation 未新增中文；未新增 Dart 领域数值常量。`dispersionInternalForcePenalty` 的旧配置字段无结算消费方，按授权保留不删。
- **残留风险**：按本单要求未跑全量、Windows 实机或视觉 smoke；dialog 新增两行后由 widget 渲染与文字查找覆盖，未做常规桌面截图。本目标不改数据库/存档，无迁移风险。无效的 `dispersionInternalForcePenalty` 配置字段仍存在，后续是否废弃需主会话单独拍板。

## 目标 2 · §8.2 交付证据

- **生产接线证据**：`SaveSlotStartupService.openSlot → IsarSetup.switchSlot/init → _ensureSaveData`
  是真实开档迁移入口；`slotListProvider → IsarSetup.listSlots/_readSummary → SaveSelectScreen`
  把未来版本异常送入既有 `ErrorFallback`。`touchOnlineNow` 仍由 app lifecycle、
  `OnlinePresenceController` 与切槽 flush 消费；远行解锁仍由 `MainMenuStartupGate →
  maybeUnlockJianghuJourney` 消费；五处选关 presentation 统一经 current-slot wrapper 读取
  `(slotId, chapterKey)` provider。
- **targeted 结果**：冻结前命令覆盖 16 文件：全部 IsarSetup migration/slots、
  save-management/file-ops/settings restore、save-slot startup、online presence/lifecycle、
  expedition startup/journey、cycle provider/control，共 `76/76`；`flutter analyze --no-pub`
  0 issue。四条新增竞态/前向回归均有上文 RED→GREEN 记录。
- **红线影响说明**：`_currentSaveVersion` 仍为 `0.37.0`，零 bump；未改 schema、
  `numbers.yaml`、收益/结算公式、三系锁死或反主流机制。心跳和离线结算仍使用相同
  `now` 与收益窗口，只把读取移进既有事务；主菜单四任务保持并发，不改变 dialog/启动
  时序；周目状态仍只在会话内 keepAlive、不落盘。
- **残留风险**：应用层保证未来档零字段写入并关闭句柄，但 Isar 必须先打开数据库才能
  读取库内 `saveVersion`，故不承诺 schema engine 在拒绝前绝无内部元数据触碰；严格
  pre-schema 保护需 raw-copy/外置 manifest 新 spec。并发测试用 barrier 确定性覆盖目标
  interleaving，未做随机压力测试。真 Isar IO 与 widget fake-async 分测，UI 分支由同一
  异常的 `AsyncValue.error` 覆盖。按本单节奏未跑全量或 Windows 实机。
