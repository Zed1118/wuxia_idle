# 战斗插队 AP 归零门控计划

> 上游稳定点：`codex/battle-tempo-diagnostics@c4737511`
> 分支：`codex/battle-intervention-ap-gate`

## 1. 目标

修复同一角色插队后 AP 已归零仍可再次借满并连续出招的问题，使“预支这一拍、随后等行动条重新积累”的既定规则真正成立；保留不同角色 A→B→C 各自连续插队的已拍板语义。

## 2. 范围

- domain：`interveneNow` 拒绝 AP 已归零的角色，不改伤害、真气、冷却、速度或行动条增长公式。
- presentation：指令台把 AP 归零角色的技能显示为短暂不可用，并给出既有水墨语气状态，不让点击静默失败。
- tests：覆盖同角色第二招被拒、行动条重新积累后可再借、不同角色仍可连续插队、按钮状态与生产回调门控。
- visual：复用 `battle_tap_live`，在 1280×720 与 1440×900 验证布局和不可用态。

## 3. 验收

- [x] 同角色首次插队后 AP=0，第二次插队不产生日志、不消耗真气/CD。
- [x] 下一 tick 积累出正 AP 后可再次插队。
- [x] 不同角色可在同一 tick 边界依次插队。
- [x] UI 明确显示“回势中”状态，禁用与 strategy 一致。
- [x] targeted、完整战斗模块 666 项、诊断探针、analyze 通过。
- [x] 双视口真实 macOS 视觉 smoke 无溢出、异常或错误日志。
- [x] 工作树仅含本切片文件，冻结 `[READY]`。

## 4. 当前恢复点

- **状态**：实现与验证完成，待整理 diff、提交并冻结。
- **最后完成**：strategy/UI 双门控与“回势中”状态接线；修复前测试证明同角色可连续两招，修复后同角色被拒、回 AP 后恢复、不同角色连插均通过。
- **下一步**：格式化报告输出，检查 diff/清洁度，提交 `[READY]`。
- **已跑验证**：targeted 48/48；完整 `test/features/battle` 666/666；诊断 1/1；`flutter analyze` 0 issue；`battle_tap_live` 1280×720 实际单步+群攻后六招显示“回势中”，1440×900 初始态布局通过，日志无 overflow/exception。
- **阻塞项**：无。
