# Phase 2 受托桌面实测（2026-09-05）

## 结果合同

- 用户授权：本任务明确要求“真人目检由你去负责做，尽快疏通阻塞”。Codex 负责实际 macOS GUI 操作、可见结果检查、证据和缺陷处置；验收者如实标注 Codex，不冒充自然人，也不把委托等同于所有产品判据或 Windows 的豁免。
- 基线：`79adb840be807e0ef6af048b2f9deef0732bd0ab`，main 与 origin/main 一致、clean；上一批 exact-SHA CI `33936690271` 已成功。重新从该版本构建 macOS Profile 包，从复制的生产根应用启动，不用 visual-route 替代正式路径。
- 分支：`codex/p2-delegated-desktop-acceptance-20260905`；复用 `/Users/a10506/.codex/worktrees/a0d7/挂机武侠`，不新增持久 worktree、不清理历史候选。
- 单一目标：完成 M2–M6 集中验收表的受托实测并收敛真实阻塞。固定检查分母 27：预检 3、M2 3、M3 5、M4 5、M5 6、M6 5。汇总行不重复计数。
- 预期增量：由本候选 0/27 已检查推进到逐项有实测结果或具体阻塞；检查完成数与 PASS 数分开，NOT_RUN 不计完成，BLOCKED 不冒充通过。正式 M0–M9 仍按 1/10 起算，主线目录 105/105、塔 0/49 不因视觉检查增加。
- 首轮成本上限：以 11:15 CST 开始的 60 分钟实测窗口为首轮检查点（含构建和存档安全准备）；约 25%/50%/80% 时看实际可见链路进展。不为纯验收重复本地全量；发现缺陷先最短复现和定向验证，若需跨切面修复另报成本与边界。
- 非目标：塔迁层、M8/M9、数值/奖励/经济/schema/saveVersion 变更、伪造真人签字、Windows 代签、删除或覆盖既有存档、未授权 main 合并/push。

## 执行顺序与门禁

1. 对照集中验收单和权威 M 段，冻结 SHA、Profile AOT 和分发包校验值。
2. 先保护现有存档；只在确认空槽或明确可用的独立测试副本上进行会写存档的操作，不猜测现有存档可丢弃。
3. 通过原生 GUI 实走启动、存档、主菜单/Hub、地图、第一章战斗/结果/返回；以生产入口为主，常规桌面视口取证。
4. 逐项扩展到合法可达的武器和六模式；不可达要记录缺少的合法前置，不以 debug fixture 或静态代码替代。
5. 截图与 AX 状态只能证明实际呈现和操作结果；连续输入手感、声音听感、长时性能、Windows 要单列证据能力边界。
6. 遇到客观 UI 缺陷，保留截图、操作序列、版本和最短复现；修复遵守既有 token/语义，不改产品判据求绿。
7. 交付检查数、通过数、失败/阻塞、截图索引、候选 clean 状态与最多三类关键阻塞。台账不得把本轮 AI 实测直接写成未发生的真人经历。

## 当前恢复点

- 状态：`LOCAL_FIXES_VERIFIED_ACCEPTANCE_INCOMPLETE`，12:03 检查点；正式里程碑仍 1/10。第一轮不扩张施工，转证据收口。
- 已核对：main 精确 SHA/clean；27 项清单；生产根 `main.dart → SplashScreen → SaveSelectScreen`；存档仅在选槽后开档与维护。CodeGraph 在本任务工作树未初始化，未擅自初始化。
- 已执行：冻结 main Profile 构建成功，AOT SHA256 `f5d5f4abacc8557c1270c0ff7a7792166660f217348a80fe066845a87ff727c7`。原包实际启动至选档页；三个旧档均非空，冷备份校验通过。
- 隔离：复制冻结包，只将 bundle ID 改为 `com.pen.wuxia.acceptance20260905` 并以原五项 entitlement 重新本地签名。AOT 与原包逐字节一致；该包并非原分发 archive 的相同字节，报告必须区分。所有试玩写入独立容器。
- 实走：新档创建、1280×720 设置与滚动、三 Hub 下钻/返回、地图新档锁定态、塔第一层首通结算/返回；旧档副本的归来结算、轻功首关叙事/战斗/胜利/返回、守城选人/布阵/战败返回、断魂庄入场/战败/离庄。主线已验证首关移动/普攻/技能/闪避、暂停和失败重试，尚不等于 G2 指定两关通过。
- 存档编号：第一次把旧 slot1 副本放到空 slot2 时，仅在选档摘要即发现进度不匹配，未进入该槽。定位为进度按 slotId 查找，已将副本移出容器；新建测试 slot1 也可恢复地留存后，旧 slot1 按原编号复制并确认 47 关/塔 7 层。原用户文件始终不覆盖，测试状态与原始备份分别保留。
- 修复：实机发现地图深底深字、六个浅纸详情浅字、空技能槽显示倒地。三类断言先 RED（35 PASS/3 FAIL），修复仅改展示色 token 和空槽状态行；不改技能禁用、键位、数值、判定、存档或门槛。地图/详情/技能印定向 97 PASS，补充真实战斗屏及禁用语义 39 PASS；根 analyze 在补齐嵌套 probe 离线依赖后无问题，锁文件无变动。
- 后续：冻结修复候选并构建 Profile，在同一独立容器重新进入生产路径复验三类缺陷；继续合法可达项目。27 行最终表逐项区分 PASS/PARTIAL/FAIL/BLOCKED/NOT_RUN，不因创建包或本地测试改成真人签字。
- 第二轮实机：`86467861` 的六详情、地图标题、空技能印已复验；六模式均走完至少一次胜利/败局/召回闭环；黑风岭自动可见胜利，风雨渡口另关闭自动战斗并合法装备缠丝索完成手动胜利。五武器手感、Boss 应对窗口、声音和 Windows 均未冒充通过。
- 新发现：塔败局写入后列表仍显示旧尝试/失败次数；真实 Isar 测试已确认记录为 `1/1` 但现有页面未更新（有效 RED）。修复只在落库成功且宿主仍 mounted 后失效进度 provider；相关四文件回归 28 PASS，待冻结新版本实机复验。另记录战场边缘敌人裁切/遮挡，未扩张渲染布局施工。
- 存档安全复核：首次原包选档页访问后三个原档文件哈希发生变化，不能再称逐字节未动；对冷备份与当前原档的独立副本比较全部 26 个当前 collection，三个槽所有业务记录一致（3 PASS）。没有覆盖恢复任何用户原档。试玩状态均另存可恢复副本。
- 最终代码候选：`e3a983eaf344dda003adefcc83ebce66cbb49185`，Profile 构建成功；隔离副本 AOT 与冻结包逐字节一致，签名验证通过。真实塔第 8 层败局关闭提示后，原页面由 59 次尝试/2 次失败即时变为 60/3，最高第 7 层不变。地图标题、塔详情和空技能槽同时复验。根 analyze 最终无问题。
- 27 项结果与版本、原始证据索引、存档异常说明见 [`phase2_delegated_desktop_acceptance_2026-09-05.md`](../../audit/phase2_delegated_desktop_acceptance_2026-09-05.md)。9 PASS、10 PARTIAL、2 REWORK、3 BLOCKED、3 NOT_RUN 为跨版本受托检查表，不是最终 SHA 整体通过率；未改写真人验收模板或正式进度。
