# 二阶段阻塞验证

- 目标：冻结 e07a6a9f0 的真实生产代码，主动验证当前全部阻塞；主 WIP 为 M2 黑风岭与合法新档连续五关的输入、终局、持久结算链。
- 分支：codex/p2-blocker-verification-20260906。
- 实时基线：HEAD/origin/main = e07a6a9f0，开局 clean；上一轮同代码 6066 测试、精确 SHA CI 成功。正式 M0–M9 = 1/10，仅 M1 关闭。
- 预算：首轮最多 90 分钟墙钟；45 分钟检查证据增量。主成本为墙钟，不声称精确 token 用量。优先复用已有运行时与测试，不安装大型 VM。
- 数据边界：只用临时 Isar 或已关闭的隔离冷备份；原生产与用户正在玩的 humanfix 存档不写。冻结数值、设计、schema，不新增塔层。

## 验收标准与顺序

1. 汇总权威 M0–M9 阻塞，每项绑定可执行测试、生产消费路径、结果和证据限制。
2. M2：真实新档创建服务与关卡入口、输入事件、敌方行为、终局、下一关、落库；禁止伪造胜利、抬血或预写通关来证明五连关。
3. 新交互：鼠标长按两秒、松开继续、单击取消、Q/R 共存、焦点恢复及双视口边界；区分 widget 注入与原生输入。
4. 一条辅助只读验证：五武器、六模式/导航/持久化、音频、M0/M7 内容缺口；检查真实断言，不以名字充当覆盖。
5. 可行时执行当前 SHA Windows 构建/校验及性能/长时实验；物理设备、真人主观结论不代签。
6. 若定位实现缺陷，先记录复现和 RED，再做范围内最小修复、定向复验；相关验证、生产路径、红线影响、残留风险全部入报告。
7. 提交恢复点并受控整合验证记录，保持正式 Gate 分母不变；仅证据充分的行可改变状态。

UI 验证使用 1280×720、1440×900；关注键鼠、focus、语义和恢复。测试、截图不冒充真人验收；历史结果不冒充新 SHA。仓库 CLAUDE.md §8.2 四项交付证据适用。

## 当前恢复点

- 状态：本批阻塞核验与首胜修复 READY；正式门 1/10、音频/性能/自动战斗等缺陷仍开放。
- 已完成：真实新档首胜 Next 缺陷有效 RED→GREEN；合法学徒冷副本连续五关真实 victory、章末叙事/返回与重开 Isar 全部通过，五关 cleared、五条 journal closed；没有抬血、伪造胜利或预写进度。
- 验证：新增回归与相邻 4 文件 13 项通过；analyze 0 issue；持锁全量 6067/6067，844.72 秒；代码精确 SHA 4ed04ae42 的 CI 34005471171 全绿，Windows 34004669034 原生窗口启动与 17.06 秒进程观察成功。
- 补充实测：合法五武器实装 5/5；辅助 64/64，30 MP3 全解码；正常菜单冷副本启动和轻功败局返回。双视口带声/静音 4 个 60 秒 Profile 加两组 600 秒持续运行全部帧门红；带声十分钟 RSS 约 2.42 GiB，静音 RSS 门通过。错误缓存的音频前提已用只读 VM 对象核对并更正标签，以全新隔离包补齐带声实测。
- 已复现未修：暗器自动战斗 300 秒停滞，停步+精准瞄准对照 30.8 秒/40 杀胜；减少闪光开关无效；原生音频 continuation 告警与明显内存增长；塔 typed 候选错误目标（非当前 legacy 生产塔早胜）。正式真人、物理 Windows、两专属音效、M0 接线、M7 49 层/5 接缝、72h 仍开放。
- 数据：原生产三档 SHA-256 前后相同；用户 humanfix 档不写；新包与测试均独立容器或临时冷副本。数值、战斗规则、schema 不动。
- 收口：主动测试约 72 分钟；只做文档归档、受控集成和已有 CI 等待，不扩展施工。报告 `docs/audit/phase2_blocker_verification_2026-09-06.md`，外部证据 `/Users/a10506/Documents/Codex/2026-09-06/wuxia-blocker-tests/`。最终 Git/CI 在外部 integration-final.json、main-ci-final.json 冻结。
- 下批顺序：同一 M2 主 WIP 修复自动战斗近身停滞；再做原生音频/内存/性能、减少闪光；不先扩塔。无新的产品决策待批。
