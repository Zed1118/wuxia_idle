# 二阶段阻塞验证

- 目标：冻结 e07a6a9f0 的真实生产代码，主动验证当前全部阻塞；主 WIP 为 M2 黑风岭与合法新档连续五关的输入、终局、持久结算链。
- 分支：codex/p2-blocker-verification-20260906。
- 实时基线：HEAD/origin/main = e07a6a9f0，开局 clean；上一轮同代码 6066 测试、精确 SHA CI 成功。正式 M0–M9 = 1/10，仅 M1 关闭。
- 预算：首轮最多 90 分钟墙钟；45 分钟检查证据增量。主成本为墙钟，不声称精确 token 用量。优先复用已有运行时与测试，不安装大型 VM。
- 数据边界：只用临时 Isar 或已关闭的隔离冷备份；原生产与用户正在玩的 humanfix 存档不写。冻结数值、设计、schema，不新增塔层。

## 验收标准与顺序

1. 汇总权威 M0–M9 阻塞，每项绑定可执行测试、生产消费路径、结果和证据限制。
2. M2：真实新档创建服务与关卡入口、输入事件、敌方行为、终局、下一关、落库；禁止伪造胜利、抬血或预写通关来证明五连关。
3. 新交互：鼠标长按两秒、松开继续、单击取消、Q/R 共存、焦点恢复及双视口边界；区分 widget 注入与原生输入。
4. 一条辅助只读验证：五武器、六模式/导航/持久化、音频、M0/M7 内容缺口；检查真实断言，不以名字充当覆盖。
5. 可行时执行当前 SHA Windows 构建/校验及性能/长时实验；物理设备、真人主观结论不代签。
6. 若定位实现缺陷，先记录复现和 RED，再做范围内最小修复、定向复验；相关验证、生产路径、红线影响、残留风险全部入报告。
7. 提交恢复点并受控整合验证记录，保持正式 Gate 分母不变；仅证据充分的行可改变状态。

UI 验证使用 1280×720、1440×900；关注键鼠、focus、语义和恢复。测试、截图不冒充真人验收；历史结果不冒充新 SHA。仓库 CLAUDE.md §8.2 四项交付证据适用。

## 当前恢复点

- 状态：WIP。
- 已完成：辅助 M0/M3–M9 阻塞矩阵、64 项定向验证、30 MP3 完整解码；减少闪光设置未消费和塔候选目标约束缺口均有真实反例。生产数值、原始存档不改。
- 主 WIP：真实新档首胜后 Next 导航复现失败，定位 StageListScreen 局部 LayoutBuilder context 被进度 invalidate 卸载；改为捕获页面根 context。生产 Onboarding→第一关真实键盘战斗→结算→第二关的新回归有效 RED→GREEN；单次只读复核通过，修正测试早期资源清理。
- 验证：最终相邻 4 文件 13 项通过；analyze lib/test/tool 0 issue。首轮联合失败来自未固定随机源触发真实奇遇，已固定 DefaultRng 种子并保留失败日志，不修改业务概率。外部合法冷备份五关试验已经产生五次真实 victory（25/15/40/3/2 击杀），章末叙事/最终 receipt 关闭仍在继续检验，尚不宣称完整通过。
- Windows：e07a6a9f0 的 34003842680 构建成功；新增正式 exe 启动/窗口存活 15 秒工作流，必须在当前修复 SHA 远端实际执行后判定。CI 运行不替代实体设备与真人。
- 下一步：五关章末闭环与重开存档、最终全量及新包、原生双视口密度/长时采样、当前 SHA Windows 启动实验。
- 阻塞：正式 G2/主观手感与物理 Windows、专属音频、减少闪光、M0 接线及 M7 迁移仍开放；正式 1/10 不变。
