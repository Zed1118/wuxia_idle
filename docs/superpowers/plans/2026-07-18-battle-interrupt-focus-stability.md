# 蓄力破招焦点稳定计划

> 上游稳定点：`codex/battle-break-focus@e25d2568`
> 分支：`codex/battle-ready-interrupt-focus`

## 1. 目标

敌人蓄力时，若玩家当前手选角色已有可立即下发的破招技，保持该焦点；只在当前角色无法破招时，才临时切到队内可破招者。

## 2. 方案

- 蓄力分支先检查存活手选角色是否存在可下发 `canInterrupt` 招式。
- 手选角色不可破招时，再按队伍顺序选首个可破招者。
- 用 `any` 扫描角色全部招式，不依赖「第一个破招技」的列表顺序。
- 不改手选基线、破招结算、AI 与技能状态。

## 3. 验收

- [x] 手选角色已可破招：蓄力出现后保持手选焦点。
- [x] 手选角色不可破招：仍自动切到可破招队友。
- [x] 同角色前一破招技阻塞、后一破招技可用：仍判定可破招。
- [x] 破绽焦点与阵亡回落不回归。
- [x] 完整战斗模块、analyze 与双视口通过。
- [ ] 工作树清洁，冻结 `[READY]`。

## 4. 当前恢复点

- **状态**：实现与验证完成，待冻结提交。
- **最后完成**：蓄力焦点先保留可破招的手选角色，再扫描队友；单角色使用全招式 `any` 判定。
- **下一步**：检查 diff 与工作树，冻结 `[READY]` 稳定点。
- **已跑验证**：手选保留用例先红后绿；指令台 34/34；完整战斗模块通过；`flutter analyze` 0 问题；指令案台既有 1280×720 / 1440×900 双视口回归通过。
- **阻塞项**：无。
