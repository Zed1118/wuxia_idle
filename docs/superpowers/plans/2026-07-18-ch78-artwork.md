# Ch7「北望」+ Ch8「出塞」22 图补齐计划

## 目标

- 按 `test/fixtures/known_missing_assets.txt` 的 22 个精确路径补齐 10 张透明敌人立绘、2 张章节封面和 10 张剧情阅读背景。
- 美术与现有水墨克制资产一致，灰衣人三次出场形象同源渐显，`huiyiren_final` 才露真容。
- 资产入库后清空 allowlist 的 22 条路径，不改任何数值、schema、saveVersion、文案或设计文档。

## 分支

- 基点：`main@5e3feb0d`
- 分支：`codex/ch78-artwork`
- worktree：`/Users/a10506/Desktop/Projects/挂机武侠/.worktrees/ch78-artwork`

## 验收标准

- [ ] 生产接线：22 个精确路径全部存在，allowlist 对应行全删，生产 `Image.asset` 引用自动命中。
- [ ] 10 张立绘均为 `1024×1536` RGBA，四角 alpha=0，全身不裁切，脚底站位可用。
- [ ] 2 张封面与 10 张阅读背景均为 `1456×816`，前者对齐 chapter 01..06，后者保留文字可读性。
- [ ] 使用内置 `image_gen` 逐张生成；透明立绘先生成纯色抠像源，再本地去背并验 alpha。
- [ ] 运行 `python3 tool/convert_assets_webp.py`，只保留有体积收益的转换，记录 22 图总体积。
- [ ] `flutter test --no-pub test/tools/asset_audit_test.dart test/data/` 通过，并按派单在批末跑一次全量测试。
- [ ] `flutter analyze --no-pub` 为 0 issues。
- [ ] `chapter_list`、Ch7/Ch8 各至少 2 个 battle route、至少 2 个 narrative route 在 1280×720 与 1440×900 完成视觉 smoke，截图只放 `/tmp` 或 `build/visual_acceptance/`。
- [ ] 红线检查：零触及数值硬红线、三系锁死、在线=离线、反主流机制、Dart 中文/数值硬编码。
- [ ] 交付说明列出未覆盖视口、未目检项、体积与风格风险。
- [ ] 所有改动已 commit，工作树干净，tip 以 `[READY]` 开头。

## 任务切片

1. 预热 worktree，读取项目真相源、已否任务清单与形象权威 YAML，完成 analyze 基线。
2. 建立现有立绘/封面/阅读背景参照板，整理 22 张图的生成提示词与三次灰衣人不变特征。
3. 生成 Ch7 的 5 张立绘、1 张封面、5 张阅读背景，尺寸/透明/站位自检后小切片 commit。
4. 生成 Ch8 的 5 张立绘、1 张封面、5 张阅读背景，尺寸/透明/站位自检后小切片 commit。
5. 运行转码工具，清理 allowlist，完成资产审计、targeted test、analyze 与全量测试。
6. 运行 `--visual-route=` 双视口验收，核对截图不入库，更新恢复点与交付四证据，打 `[READY]` tip。

## 当前恢复点

- 状态：进行中——Ch7 的 11 张资产已入库并完成首轮目检，待提交稳定切片。
- 最后完成：生成 Ch7 的 5 张透明立绘、1 张封面和 5 张阅读背景；立绘均为 1024×1536 RGBA 且四角 alpha=0，场景均为 1456×816 RGB。
- 下一步：提交 Ch7 切片，再以 `huiyiren_beijing.png` 为身份参照生成 Ch8 的沙夜/终局递进形象与其余资产。
- 已跑验证：基线 `flutter analyze --no-pub` 0 issues；Ch7 5 张立绘 Pillow 实测尺寸/模式/四角 alpha 合规；场景 `file` 实测尺寸合规；两张对照板已目检。
- 阻塞项：无。
