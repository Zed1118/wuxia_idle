# P2 M2 Batch18：显式运行时延续边界

## 目标

从 Batch17 READY `87ee892189dd1c4c1d131e4a06df649f34328769` 出发，并行闭合三个无需新产品决策且 owned files 不重叠的 host-neutral 合同：显式败北目标投影映射、主线下一关 run/听剑占用延续、攻击令牌租约批次回执。本批不接 production host，不晋升 Ch1 candidate，不实现 checkpoint/anchor projector、ActionTimeline 推断、durable coordinator/store/schema/CAS/outbox 或任何 tuning/Profile/G2/真人验收。

## 并行任务

1. R22 / Pi + DeepSeek V4 Flash high：把 caller 按 encounter entry 明示的既有 Target/Commander defeat projection，经 R07 exact binding 映射到 R13 event source；拒绝任何字符串/role/actor 推断。
2. R23 / Qoder CLI + exact Qwen3.8-Max high：组合 R19 release、R01 `proceedToNext` 与 R15 optional reacquire，保持 run/participant 身份与 owner-bound prepared commit。
3. R24 / Codex high + Pi 最终只读审查：为 R12b Session 成功提交的 caller-declared lease mutation 发布 immutable before/mutations/after receipt；失败保留旧 receipt。

## 硬边界

- R22 仅闭合 Target/Commander defeat payload；不构造 projection，不处理 pursue/checkpoint/anchor/marker/time/defend，不修改 candidate fixture 或 production data。
- R23 不决定 settlement/session identity、下一关资格来源、shared occupancy、reward/claim/grant、persistence/UI。
- R24 不从 ActionTimeline、hit、defeat、cooldown、cancel/interrupt 推断 mutation/action ID，不定义 capacity/budget/default/tuning。
- candidate promotion、objective 全流程 executable、production route/host、durable store/schema/CAS/outbox、Profile/G2/真人验收继续 Gate。

## 验收

- 三来源在独立 worktree 完成 TDD、精确外部模型审查、targeted/analyze/format/path/diff/status 与独立终审后追加各自 READY。
- 主控逐项核验 actual diff、source→integration stable patch-id、owned blobs、failure zero-publication 与 Gate 诚实性。
- 集成态运行去重联合 targeted、changed-Dart scoped analyze、format、YAML/diff/path/main refs、full suite 与独立终审；清零后只追加 Batch18 READY。

## 当前恢复点

- [x] Batch17 READY 已冻结，full 5079/5079、独立终审 P0/P1/P2=0，main/origin main 未修改。
- [x] Batch18 独立 integration worktree 已从 Batch17 READY 创建。
- [x] integration 已完成 `flutter pub get`、build_runner 126 outputs（63 个 `.g.dart`）与 Batch17 `libisar.dylib` 恢复（SHA-256 `f22f60782156ff3205c4ef72ff157337640604a8a0c4c416555a2432c764742d`）；工作树保持 clean。
- [ ] R22/R23/R24 各自在 source worktree 恢复环境并完成并行来源。
- [ ] 联合/full 验证、独立终审与 Batch18 READY。
