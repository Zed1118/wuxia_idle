# N13 · 二阶段拍板菜单可恢复计划

## 结果合同

- 唯一目标：交付 `docs/dispatch/pool/phase2_decision_menu.md`，让用户按去重解锁力用最少决策解锁最多任务组。
- 分支/基线：`codex/p2-leftover-pool-20260826` / `f1e645f0`。
- 固定分母：N12 `39` 条 / `236` 组；红 `31` 条 / `210` 组，绿+黄 `26` 组。
- 主阻塞：重复任务 ID、已签冻结合同和真待拍节点必须拆分，不能从生产实况反推签字。
- 预期增量：形成一份 150 行内、可用 N12 重算的签字菜单；不晋升 M0–M9 任何 Gate。
- 成本上限：单文档编译/全量 Gate 一轮；无可靠 token 计量，墙钟超过 90 分钟且守恒未闭合时停线重评。
- 非目标：不修改任何 `lib/`、数值、schema、玩法、用户决策或 main。

## 验收标准

- [ ] 产物不超过 150 行，S/G/E 依赖每项有判据。
- [ ] 每个待拍节点含决策 ID、大白话问题、状态行号、原文选项、证据、三个解锁数和 §20 ID。
- [ ] 方案无 ID/无选项时使用指定原文，不新建任何决策 ID 或选项。
- [ ] 去重 S/G 总解锁集 `≤ 236`，并写明与 236 的差额来源。
- [ ] 零成本节把“实况”和“已签”分开，仅采信 N7 原始签字证据。
- [ ] 生产接线证据：不适用（本任务仅交付 dispatch 文档）；改为 N12/方案/N7 三源可追溯。
- [ ] 定向验证：行数、禁代拍词、ID 来源、数字守恒和禁区 diff 均由命令实测。
- [ ] 红线影响：零数值/三系/在线离线/反主流/文案硬编码变更。
- [ ] UI/UX 加码：不适用，本任务不修改窗口或交互。
- [ ] 外部审查：不适用，未导入外部问题清单。
- [ ] 禁区五文件、`lib/`、`test/` 零改动；tip 以 `[READY]` 开头且工作树干净。
- [ ] `/Users/a10506/.claude/skills/afk/scripts/gate.sh` 以全量模式 PASS。
- [ ] 残留风险只限于用户尚未拍板的 11 个问题；本文档不代为作答。

## 任务切片

1. 现取 N12/N7/方案行号与签字证据。
2. 建立依赖分类、红行签字单元和去重组集。
3. 按总解锁力写菜单与零成本节。
4. 运行静态自检、禁区检查和全量 Gate。
5. 冻结 `[READY]` 恢复点。

## 当前恢复点

- 状态：READY 候选，菜单和依赖图已落盘，静态校验已通过；冻结 commit 后的全量 Gate PASS 是最后条件。
- 最后完成：N7 存在性与原始签字列已核对；S/G 去重集守恒为 `210/236`；待拍集现取为直接 `62` + 传递 `18` = 可达 `80` 组。
- 下一步：冻结 `[READY]` commit，运行全量 `gate.sh`；终态 PASS 在交付信息记录，不为回填日志再改动冻结 tip。
- 已跑验证：菜单 `67` 行；禁代拍词 `0` 命中；待拍 `11` 行的算术为 `62+18=80`；N12 红行实测 `31/210`、非红 `8/26`、G2 毛可达 `12/103`；`git diff --check` 通过；禁区、`lib/`、`test/` 无 diff。
- 阻塞项：无。
