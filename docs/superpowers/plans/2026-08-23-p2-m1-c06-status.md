# P2-M1-C06：状态固定拍候选

## 任务元数据

- taskId: P2-M1-C06-STATUS
- milestone: M1 / C 状态固定 tick
- owner: Codex
- branch: `codex/phase2-m1-status-20260823`
- worktree: `/Users/a10506/Desktop/Projects/挂机武侠-phase2-status`

## 目标与非目标

目标：提供纯 Dart 的 `slow/root/internalInjury/poison` timed status 候选，验证固定 tick、伤害调度、同源刷新、显式叠层上限、确定性排序和参数校验。

非目标：不接 reducer、战斗模型、数据 loader、UI、Boss 转换、存档或生产入口；不决定最终数值和 G1 公共 API。

## 白名单

- `lib/features/battle/domain/phase0a/status_effects.dart`
- `test/features/battle/domain/phase0a/status_effects_test.dart`
- 本计划文件

禁止修改其他文件。

## 冻结合同

- 状态类型只允许 slow/root/internalInjury/poison。
- `slow` 只暴露移动倍率；`root` 禁止常规移动但允许攻击/防御。
- 固定逻辑 tick 递减；毒/内伤可按调用方注入的 cadence 与 damage 调度。
- 同来源默认刷新；只有显式 `stackLimit > 1` 才允许叠层。
- duration、interval、stackLimit 非法时拒绝；所有数值由调用方注入。
- 输出按绝对逻辑 tick、稳定 type/source 排序，批量推进与逐 tick 推进结果一致。

## 验收与验证

- TDD 覆盖非法参数、刷新、叠层、固定 tick 伤害/过期、root 合同和 slow 合同。
- 命令：`dart format lib/features/battle/domain/phase0a/status_effects.dart test/features/battle/domain/phase0a/status_effects_test.dart docs/superpowers/plans/2026-08-23-p2-m1-c06-status.md`
- 命令：`flutter test --no-pub test/features/battle/domain/phase0a/status_effects_test.dart`
- 未接生产；API 待 G1 主审确认。

## 当前恢复点

- 状态：独立复核与主审边界修正完成，定向测试 12/12 与 analyze 通过。
- 复核修正：同源刷新采纳新 spec；`active` 返回 detached snapshot；到期拍先结算伤害再移除；叠层减速按层生效；伤害事件携带绝对逻辑 tick，批量与逐 tick 排序一致。
- 验证：`flutter pub get`、format、analyze 通过；定向测试 12/12 通过。生产接线和最终 schema 留给 G1。
- 风险：`TimedStatusInstance` 为可变快照类型，未来接 reducer 时仍需由公共模型决定复制/持久化语义；ledger 内部状态不会由 `active` 快照泄漏。
