# P2 M5 Batch1：心魔修炼度惩罚收口

## 目标

把已冻结的 `INNER-DEMON-CULTIVATION-01` 从决策层落实到生产代码：心魔失败不再扣除主修招式修炼度，只保留有上限的内息紊乱，并让战败摘要只陈述真实发生的后果。

## 基线与边界

- 基线：`a0f3ee5b96a2d125f0df0cb9f44a84910c0d0655`（P2 M2 Batch20 READY）。
- 不修改 `main` 或 `origin/main`。
- 不复用 C17A/C17B；这是独立的 M5 迁移批次。
- 不处理七心魔 AI 矩阵、数值定标、画像、生产 host、持久化或 G2。
- 两个源任务文件所有权不重叠；集成层只合并已审核补丁并回写登记。

## 源任务

### R01：规则与配置迁移（Pi + DeepSeek V4 Flash）

1. 删除 `failure_penalty.main_cultivation_multiplier` 的生产配置与类型入口。
2. 对退役键提供显式拒绝，防止旧惩罚静默回归。
3. 心魔失败只应用受上限约束的内息紊乱；主修修炼度、境界、永久内力、装备和伤势均不变。
4. 保留结果快照中的前后值证据，但不得写回修炼度。
5. 用定向测试覆盖非整值进度、上限、重复失败和旧配置拒绝。
6. 同步移除所有测试夹具对退役 `failurePenalty` 构造参数的依赖。

### R02：战败摘要对齐（Qoder CLI + Qwen3.8-Max）

1. 心魔战败摘要显示心魔名与“内息紊乱”。
2. 不显示未变化的内力区间，也不显示“修炼度回退”。
3. 普通 Boss 战败的伤势、内力和修炼度摘要保持不变。
4. 只有确认无调用后才删除退役字符串。

## 集成与验收

1. 主控逐文件复核两个补丁和测试意图。
2. 运行规则层、数据层、战斗结算和主线摘要联合定向测试。
3. 运行变更范围 `dart analyze`、完整 `flutter test` 和仓库既有检查。
4. 由未参与实现的 Codex agent 做独立 P0/P1/P2 审查。
5. 回写任务/决策登记、审计、真实提交号和测试计数，最后生成唯一 READY 恢复点。

## §8.2 交付 Gate

- [ ] 生产接线：`numbers.yaml` → `InnerDemonDef` → `BattleResolutionService` → `InnerDemonService` 的旧惩罚入口已完整移除。
- [ ] 定向验证：规则、数据、结算和摘要联合测试记录真实命令与通过数。
- [ ] 红线说明：不碰数值硬上限、三系锁死、在线离线等价和反主流禁区；不在 Dart 新增数值或中文文案。
- [ ] UI 验证：普通 Boss 摘要不变；心魔摘要在常规桌面视口完成 smoke，或如实记录环境阻塞与残留风险。
- [ ] 残留风险：未覆盖的视觉、配置迁移、性能、存档和调参风险显式列出。
- [ ] 外审结论完成证伪/复核，不把模型输出直接当验收。
- [ ] `flutter analyze`、联合定向测试、完整 `flutter test --no-pub`、仓库检查全部通过。

## 当前恢复点

- 状态：源任务并行实现中。
- 最后完成：登记提交 `f1dc0c9e`；所有权/基线冻结提交 `c0f72b8e`；R01/R02 worktree 已从 `f1dc0c9e` 创建。
- 下一步：等待两个 READY 源补丁，主控逐 diff 审查后以稳定提交身份集成。
- 已跑验证：两份 registry 均可被 Ruby YAML 解析；登记 diff 通过 `git diff --check`。
- 阻塞项：无。M2 结算身份、真实 lease 生命周期等架构 Gate 与本批无关，继续保持未决。

## 停止条件

- 发现心魔失败仍依赖未冻结的 AI、数值或宿主语义。
- 普通 Boss 战败摘要出现行为变化且无法在批次内隔离。
- 需要修改存档 schema、数据迁移或生产配置。
- 任一定向/完整测试失败且根因不属于本批次。
