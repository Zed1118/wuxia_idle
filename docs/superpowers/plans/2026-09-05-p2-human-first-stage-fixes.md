# M2 真人首关阻塞修复

- 授权：用户要求按优先级处理全部剩余任务，并于 2026-09-05 实际试玩反馈。分支 `codex/p2-human-first-stage-fixes-20260905`，起点 `ce29304e77956b9cffd6452e4c1f486d66634cd6`，开局 clean。
- 单一结果：处理本轮真人试玩阻塞并交付可复验的隔离包：底边不遮挡、首关出口条件可见、鼠标两秒开启持续普攻、第一章两位 Boss 主动接敌且出手可见。
- 固定门：M2 仍为主 WIP；正式 M0–M9 仍 1/10。真人已报告移动顺畅、普攻和技能正常，并提供山门之外胜利/下一关截图；不再把该档首关记为不能获胜。
- 缺陷：普通移动/闪避未受已配置 arena 限制，导致玩家越出相机投影、被 HUD 遮挡；首关实际要求清除 25 名敌人且到达 x=520 出口，缺少对应画面指引。十几秒间是否移动仍待反馈，不虚构结算延迟根因。
- 新增授权：用户要求鼠标左键长按两秒激活自动普攻，单击取消；随后反馈青衫汉子等 Boss 站在远处不接敌。普通单击取消自动普攻，但 Q 聚怪选点保留；松开后持续攻击射程内最近活敌，不自行追赶，失焦/暂停/终局退出自动普攻。
- 边界：沿用既有 arena 和 checkpoint 数字，不改伤害、血量、奖励、存档结构、首关结束条件或冻结的群战调优决定。第一章两位 Boss 只改错误的 AI profile 引用，继续保留 token、身份、招式和预算。原用户存档保持隔离，旧试玩进度先保全再换包。
- 验证：定向 RED→GREEN；普通输入/闪避、三种控制入口一致性、checkpoint 前后及其他 typed objective 回归；1280×720 与 1440×900 画面；scoped analyze；共享锁下最终全量。进入受控集成、工作树 clean 后仍保留真人复验门。
- 成本：墙钟 45 分钟检查点，90 分钟无证据门变化重评；不按提交数或测试数计进度。
- 分工：依据 CLAUDE §8.0，一个有界子任务顺序处理边界/鼠标；另一个只处理 Boss 接敌与动作反馈。主窗口处理出口提示、实际 diff 复核、集成与验收。共享分支，子任务不自行提交或登记完成。

## 恢复点

2026-09-05 20:40：四类修复在当前分支。边界 4 项 RED→GREEN，相邻 118/118；鼠标 11 项 RED→GREEN，相邻 64/64；出口真实 Host 20/20 与清场后等待/移动过线/胜利结算、双视口 1/1；两位 Boss 接敌原数据 3 pass/2 fail，修复 5/5，相邻 24/24。敌方动作反馈后续正在补真实事件→画面回归。局部测试计数有重叠，不相加作总分母。

批末持锁全量已开始。完整 analyze 发现一条测试无用 import，已删除；最终 analyze、构建和原生复验待完成。旧冻结包 Windows run33964735494 与资料 CI run33964881323 均 success，产物哈希已核验，旧 heartbeat 已暂停；这两个结果不证明本批代码 CI。

剩余风险：另外 11 位使用驻守画像的 Boss 只是静态候选，未在此批统一修改。用户所述十几秒是否继续右移未获回答；已证实首关规则没有清场后计时器，真实出口条件需到达既有 x=520。图片与绿色测试不代签新包真人手感或 Windows。

证据根目录：`/Users/a10506/Documents/Codex/2026-09-05/wuxia-priority-acceptance/`；当前验证日志位于 `build/human-fixes-evidence/`、`build/arena-bounds-evidence/`、`build/pointer-hold-evidence/`，交付时归档外部证据包。
