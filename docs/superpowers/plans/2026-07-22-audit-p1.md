# 2026-07-22 外部审查 P1 triage+落地批（恢复点）

- worktree: `.worktrees/audit-p1`，分支 `kimi/audit-p1-20260722`，基线 main 5396706e
- 输入：`~/Desktop/挂机武侠_最新全量审查报告_2026-07-21.md`（07-21 基线，行号已现 grep 复核）
- 范围：报告 5.1 / 5.2-5.4 / 5.5 / 5.7 / 6.1（仅头部口径）。5.6、6.2-6.5、七 不在本批。

## Triage 结论（均已现 grep/读码复核）

| 项 | 结论 | 证据 |
|---|---|---|
| 5.1 占用契约 5 消费者 | **证实** | `CharacterOccupancyService` 全 lib 仅 expedition_service.dart:63/69、expedition_providers.dart:75、gauntlet_service.dart:145/151、gauntlet_providers.dart:254 消费；lineup_service.dart:113 只查闭关字段；stage_battle_setup.dart:177 直读 activeCharacterIds；equipment_service.dart:48 equip / :128 unequip 无保留校验；equipment_disposal_service.dart:181 `_guard` 无保留校验；technique_learn_flow_service.dart:54 learn 无占用校验 |
| 5.2 战败持久化 | **证实 → [BLOCKED]** | `ExpeditionRun`（expedition_run.dart:13-36）无状态字段；持久化战败态需加 schema 字段，触红线停手待拍。5.3 修复（召回前先 settle 并传 defeated）可经确定性重战兑现战败伤势，缓解「召回不落伤」 |
| 5.3 召回不先结算 | **证实** | recall（expedition_service.dart:321）无 settle；UI 直调（expedition_overview_screen.dart:392） |
| 5.4 并发召回重复发奖 | **证实** | recall run 读在 txn 外（:325）、txn 内无重读（:354）；召回按钮无 `_submitting`（dispatch 有，:66-84） |
| 5.5 saveDataId 混用 | **证实** | 主线进度行 `saveDataId=IsarSetup.currentSlotId`（mainline_providers.dart:17、stage_entry_flow.dart:209，值 1-3）；expedition 存/查 `save.id=0`（expedition_service.dart:95/358）；gauntlet 查 `save.id=0`（gauntlet_service.dart:472/580）→ 生产永远查空、层锁门禁 clearedSet 空。expedition_recall_test.dart:211 固化 saveDataId=0 |
| 5.7 永久进度无行为 | **证实** | `baicaoMaxDepth`（save_data.dart:123）、`grantedTicketMilestoneIds`（:135）全仓 grep 仅命中定义+默认值测试，生产零读写。规格：design §6.4（塔 10/20/30 层一次性断魂帖+旧档补发、F1 防重体例）、§3.3（最深节点展示用） |
| 6.1 GDD 头部口径 | **证实（头部）** | GDD.md:8 v1.22 称 cap 层10/Lv100、30 关、实测 Lv44/85；实测 numbers.yaml `release_cap.max_absolute_realm_level=28`（一流·登峰）、stages.yaml 60 主线关（`stageType: mainline` ×60，Ch1-12）、正文最新口径 GDD.md:566（60 关首通 Lv80、路线 Lv103，2026-07-21 重校）。正文其余冲突（cap24/40关/250招等）属 6.4 范围，本批不动 |

## 实施计划（小切片 commit）

1. [GDD] 头部加 v1.23 订正口径，v1.21 迁 `docs/_archive/GDD_CHANGELOG.md`（遵 GDD 自身维护规则留最近 2 版）
2. 5.5：expedition/gauntlet 查 mainlineProgress 改 `IsarSetup.currentSlotId`（沿 ascend_service.dart:341 先例）；不改 `run.saveDataId` 存储（进 seed，改了会在途 run 节点重roll）；更新固化测试
3. 5.3+5.4：UI 召回前 `settleToNow` 并把 defeated 传给 recall；recall txn 内重读 run 防并发重复发奖；召回按钮加防重
4. 5.7：recall 事务写 `baicaoMaxDepth`；塔 10/20/30 层首通发断魂帖+`grantedTicketMilestoneIds` 防重+旧档 lazy 补发
5. 5.1：5 消费者接 `CharacterOccupancyService`（编成禁换上/战斗过滤/装备穿卸移/出售分解/心法学习）
6. analyze + 全量测试（基线 4626）+ draft PR

## 进度

- [x] 环境：flutter pub get OK；build_runner 重生 .g.dart（equipmentCatalogEntry 缺失）
- [x] triage 全部完成（结论见上表）
- [x] 切片1 [GDD] e776ba8e 头部 v1.23（层28/12章60关/Lv80·Lv103，v1.21 迁归档）
- [x] 切片2 45354a49 5.5 层锁门禁改按槽号查（expedition+gauntlet 3 处查询、3 处固化测试改 currentSlotId）→ targeted 247 过
- [x] 切片3 cdaf2f8a 5.3+5.4 召回前 settleToNow 并传 defeated、recall txn 重读 cursor 守卫（+2 测）、UI 防重 → 88 过
- [x] 切片4 70682912 5.7 recall 写 baicaoMaxDepth（max 单调）、塔 10/20/30 断魂帖+getOrCreate 懒补发（+6 测）→ 103 过
- [x] 切片5 d761a284 5.1 五消费者接占用（+9 测：lineup 3、battle 1、equip 3、disposal 3→实 3、learn 1）→ 577 过，analyze 0
- [x] 全量 4644 pass/0 fail（基线 4626+18）、analyze 0、draft PR #58（Zed1118/wuxia_idle）
