# 行动队列期间即时干预门控计划

> 上游稳定点：`codex/battle-restart-vfx-reset@4a53d66d`
> 分支：`codex/battle-intervention-queue-guard`

## 1. 目标

让战斗 UI 对“技能当前可下发”的承诺与引擎完全一致：当单步/验收模式停在非空 `actorQueue` 的拍内结算态时，不显示可用按钮、不弹破绽提示、不进入选目标态，也不把必然被引擎拒绝的命令交给 notifier。

## 2. 方案

- 在战斗技能工具层补齐含 `BattleState` 的即时干预总门控，统一 finished、actorQueue 与角色技能资源条件。
- 战斗屏点击入口、焦点推荐、破绽提示和技能按钮全部复用同一口径。
- 队列结算中复用现有“回势中”视觉，不新增平行提示或数值系统。
- 保留 `canInterveneWithSkill` 作为只描述角色资源/控制态的低层函数。

## 3. 验收

- [x] `actorQueue` 非空时按钮语义禁用，点击不下发。
- [x] 队列非空时破招/破绽推荐不误报可操作角色。
- [x] 队列清空后既有即时干预行为不回归。
- [x] 完整战斗模块、analyze 与双视口 UI 通过，冻结 `[READY]`。

## 4. 当前恢复点

- **状态**：已完成，待冻结稳定点。
- **最后完成**：新增 `canInterveneNow` 战斗快照总门控，点击入口、按钮、焦点推荐与破绽提示统一复用；拍内队列态显示既有“回势中”灰态。
- **下一步**：提交 `[READY]`，继续审计代表性战斗节奏与历史战报稳定性。
- **已跑验证**：局部 UI 62/62；完整 `test/features/battle` 724/724；`flutter analyze --no-pub` 通过。1280×720 / 1440×900 双视口验收包含在完整战斗回归中。
- **阻塞项**：无。
