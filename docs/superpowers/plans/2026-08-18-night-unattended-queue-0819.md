# 夜间挂机批 RP0 · 2026-08-18 夜 → 08-19 06:00

> 授权:项目主人 2026-08-18 22 时睡前授权「启动挂机工作流,推进至明早 6 点,
> 遇测试目检任务顺延到最后」。
> 基线:main `bb0f664d`(0C 批终态,CI run `32143146897` success,5167/0)。
> 协议:CLAUDE.md §8.0 可恢复任务协议(分支+RP0+验证+破坏证红+merge --no-ff+收账)。
> 旧 `.nightshift/` 调度体系已失效(conf 指向搬迁前旧路径、任务停在 2026-05 T23/T24),
> 本夜由当前会话直接作执行端,纪律等价。

## 1. 串行队列(每批独立分支)

| # | 批 | 性质 | 分支 | 通过条件 |
|---|---|---|---|---|
| N1 | 旧 3v3 拆除范围审计 | 只读审计·零代码 | `audit/legacy-3v3-removal-scope-0818` | 报告落 `docs/audit/`:旧 3v3 屏/路由/测试/yaml/文档引用逐层清点 + 拆除影响面 + §7.4 ADR 三路线事实表;analyze/test 计数守恒 5167 |
| N2 | 死链残余清零 | 机械修复·纯 md | `fix/doclink-residue-0818` | 重扫取新基线 → 逐条处置(B 标注否决/D 实证已删)→ 重扫守恒;触及 GDD/数值/schema 的条目只登记待拍不修 |
| N3 | 长寿文档漂移审计(可空) | 只读审计 | `audit/living-doc-drift-0818` | PROGRESS ≤100 行纪律/BACKLOG ≤80 行/GDD 与 08-18 拍板方向(战斗终态=0A ARPG)的 drift 清单;只报告不改 |

## 2. 顺延清单(夜间禁碰,留早晨)

1. 一切需真人目检/试玩判读:6 人主观 Gate、Windows 实机、暂停横幅真机观感、
   视觉路由拍图类验收;
2. 一切拍板项:§7.4 存量路线 ADR(N1 只备事实)、Phase 1 spec 立项、
   122 关/塔 49/旧 3v3 去留终裁;
3. 0B `MANUAL_RIG_PENDING`:33–55 人日人工拆件补画,非夜间可执行项;
4. 数值/GDD/schema/玩家可见 UI 改动(🔴 决策级)。

## 3. 批内纪律(沿 0C 批体例)

- 每批先 RP 冻结再动手;纯文档批守 §8.0 v1.29 免 Flutter 全量,但 N1/N3 若
  零 `.dart` 改动须留痕「计数应保持 5167 待 CI 核」;
- commit message 中文动宾;写完 dart 必 `dart format`(本夜预期零 dart);
- merge --no-ff → PROGRESS 登记(≤100 行纪律)→ push → CI 盯守显式读
  conclusion(不信退出码);
- 破坏证红仅对代码批适用;本夜三批预期均为纯文档/审计,若途中出现必须动
  代码的分支,停下记入顺延清单,不夜间代拍。

## 4. 恢复点

- RP0 落盘 commit 后:队列未开跑,重读本档即可续;
- N1 中途断:报告草稿在 worktree 分支,按 §1 通过条件续写;
- N2 中途断:扫描器重跑取基线,处置进度以报告「已处置条目表」为准;
- 任一批 merge 后断:跳过该批续下一批,PROGRESS/CI 以 git log 为准。

## 5. 验收勾选

- [x] RP0 落盘并 commit(`5d3deabf`)
- [x] N1 报告合入 + CI 绿(merge `92ebedac`;终态 tip CI `32149125730` success 5167)
- [x] N2 清零合入 + CI 绿(merge `3f4228bf`;C 类 6 修,残余 65 全归类 B/D 接受)
- [x] N3 报告合入(merge `86c9004f`;D1/D2 待拍 · D4 v2 方案未入库留指认)
- [x] 早晨收账:PROGRESS 收账条 + 顺延目检项清单呈用户(夜批总报已呈;中间三个 cancelled run 系连推 concurrency 取消非失败;本勾选 commit 的 CI 待盯)
