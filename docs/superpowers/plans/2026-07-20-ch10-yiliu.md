# Ch10「中州」一流拐点主线章 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development 或 superpowers:executing-plans 逐 task 实装。步骤用 `- [ ]` 追踪。

**Goal:** 实装主线第 10 章「中州」（一流 yiLiu 阶首章）——5 关 + 6 一流敌招 + 1 末 Boss 真解 + ~13 篇叙事，发布上限 cap 17→24，全 reconcile 站点绿。

**Architecture:** 加章 = 数值层(yaml) + 红线层 + 叙事层 + reconcile 级联同步。沿 Ch7-9 成功先例；**先 Phase-0 grep 盘点所有 reconcile 站点现值再动手**（禁打地鼠·memory `feedback_wuxia_add_mainline_chapter_reconcile`）。装备零新增（复用 12 件 liQi）。

**Tech Stack:** Flutter Desktop / Dart / Isar / YAML 配置 / flutter test 红线守卫测。

**Spec:** `docs/spec/2026-07-20-ch10-yiliu-design.md`

---

## 执行者必读约束

- **环境预热**（fresh worktree 首次）：`flutter pub get` → 从主仓 `cp` 完整 `libisar.dylib` → `dart run build_runner build --delete-conflicting-outputs`（`.g.dart` gitignored，不跑则全量编译红）→ 冒烟一个测。见 memory `feedback_fresh_worktree_libisar_dylib` / `feedback_wuxia_pen_build_runner`。
- **命令均在 worktree 根执行**（cwd = `.claude/worktrees/ch10-yiliu`）；中文路径 cd 坑 → git 用 `git -C`。
- **YAML key**：`skills.yaml` 用 camelCase（`powerMultiplier`/`qiDelta`），其余 snake_case。
- **改 dart 后必 `dart format`**（CI format gate 先于测试）。
- **reconcile 现值必 grep 实测再改，禁猜**（anti_hallucination）；progression Lv 快照**逐值 fail-fast 迭代**（测停首个错值→改→单文件重跑 ~5s）。
- **破坏证红在 commit 后做**（memory `feedback_break_red_after_commit`）；测试与 commit 用 `&&`。
- **测试节奏**：跨切面（yaml/schema/reconcile）→ 批末全量 `flutter test --no-pub`（并发）；自包含小改 targeted。

---

## File Structure

| 文件 | 动作 | 责任 |
|------|------|------|
| `data/skills.yaml` | modify | +6 一流门派绝学敌招 + 1 末 Boss 真解 |
| `data/stages.yaml` | modify | +stage_10_01..05 |
| `data/numbers.yaml` | modify | `max_absolute_realm_level` 17→24 |
| `data/narratives/chapters/chapter_10.yaml` | create | 章首尾 prologue+epilogue |
| `data/narratives/stages/stage_10_*.yaml` | create ×12 | 10 opening/victory + 2 defeat |
| `test/**` (~26 站点) | modify | reconcile count/boss/Lv/skill/cap 同步 |
| `lib/**/chapter_list_screen.dart` 等 | modify | 生产可见性（章列表/菜单/strings） |
| `GDD.md` | modify | §8.1 章表 + 招式池 |

---

## Task 0: 环境预热 + Phase-0 站点盘点

**Files:** 无改动（只读盘点 + 环境）

- [ ] **Step 1: 环境预热**

```bash
cd .claude/worktrees/ch10-yiliu   # 若尚未在此
flutter pub get
cp ../../../.dart_tool/.. ; MAINREPO="/Users/a10506/Desktop/Projects/挂机武侠"
find "$MAINREPO" -name libisar.dylib -maxdepth 4 2>/dev/null | head -1   # 定位主仓 dylib
# 将完整 dylib cp 到 worktree 对应位置（避免截断，见 memory）
dart run build_runner build --delete-conflicting-outputs
```
Expected: build_runner ~72 outputs，无编译错。

- [ ] **Step 2: Phase-0 grep 全 reconcile 站点现值**（记录每个数字，喂给后续 task）

```bash
# 主线 count（每章5关，加 Ch10 → +5）
grep -rn "mainlineCount\|45\b" test/ | grep -iE "mainline|stage.*45|hasLength\(45" | head
grep -rn "hasLength(4[0-9])\|4[0-9] 关\|1, 9\]\|\[1\.\.9\]\|<= *9\b" test/ lib/ | head -40
# boss 计数 / catalog
grep -rn "isBoss\|图鉴\|catalog\|33\b\|20 " test/features/*boss* test/**/*boss* 2>/dev/null | head
# skill 计数（加真解 +1）
grep -rn "hasLength([0-9]\{3\})\|[0-9]\{3\} 招" test/ | grep -iE "skill|genericIds" | head
# cap 站点（17→24）
grep -rn "maxAbsoluteRealmLevel\|release_cap\|17\b\|停在.*层\|Lv100" test/ lib/ | grep -iE "cap|realm|release" | head -30
# TechniqueTier 一流前缀（敌招命名）
grep -rn "enum TechniqueTier\|menpai\|juexue\|mingjia" lib/core/domain/enums.dart data/skills.yaml | head
# Lv 快照锚点
grep -rn "progression_release_budget\|idle_horizon\|首通\|全内容\|Lv9[0-9]" test/tools/ test/**/*progression* 2>/dev/null | head
```

- [ ] **Step 3: 把现值填入本 plan 的「Reconcile 现值表」**（Task 7-11 引用）。不 commit（纯盘点）。

---

## Task 1: 一流门派绝学敌招（复用·零新增·2026-07-20 Phase-0 证伪）

**Phase-0 证实**：`skill_<school>_menpai_basic/skill/ult`（3 派 × 3）**已存在**（skills.yaml:438-549·刚猛落雷掌500/千斤坠2400/怒龙翻3000·灵巧燕回身/追风刺·阴柔一组·`tech_<school>_menpai` parent 已存在·cap=3000）。**spec §4.1 预估「新增 6 招」证伪 → 直接复用**（memory `zero_ref_asset` / 反例）。

- [x] Ch10 敌人 skillIds 直接引用现有 `skill_<school>_menpai_basic` + `skill_<school>_menpai_skill`（按关卡 school 配）。**无新增、无 commit**。skill 计数不 +6，仅真解 +1（Task 2）。

---

## Task 2: 末 Boss 一流真解 ×1

**Files:** Modify `data/skills.yaml`

- [ ] **Step 1: 追加真解**（沿 `skill_chen_sha_yi_jue` 模板）：`id: skill_<真解名>`，`type:powerSkill / targetType:single / powerMultiplier:3600 / qiDelta:-38 / cooldownTurns:4 / requiresManualTrigger:false / parentTechniqueDefId:null / source:mainline_drop / style:yinRou / tier:4 / proficiency.effects{shuLian/jingTong/huaJing: {damage_pct:0.08}}`。真解名待叙事定（Task 6 前占位一致 id）。
- [ ] **Step 2: 验证** `flutter analyze` 0。
- [ ] **Step 3: Commit** `git commit -am "[balance] 新增 Ch10 末 Boss 一流真解（yinRou·mult3600·沿沉沙一诀模板）"`

---

## Task 3: stage_10_01..05 五关

**Files:** Modify `data/stages.yaml`（在 stage_09_05 后、心魔境前插入；正向从 `- id:` 定位，memory `feedback_stages_yaml_edit_direction`）

- [ ] **Step 1: 追加 5 关**，字段照 Ch9 模板（`stageType:mainline / chapterIndex:10 / requiredRealm:yiLiu / prevStageId 单链 10_01←…←10_05`）。逐关：

| 关 | realmLayer | school | HP | 攻 | 速 | diffMult | isBoss | drop(复用 liQi) |
|----|-----------|--------|-----|----|----|---------|--------|------------------|
| 10_01 | qiMeng | gangMeng | 18000 | 720 | 250 | 10.2 | 否 | weapon_liqi_long_quan(1.0)+armor_liqi_xuan_tie_jia(0.3) |
| 10_02 | qiMeng | lingQiao | 19000 | 750 | 285 | 10.4 | 否 | weapon_liqi_pan_long_dao(1.0)+accessory_liqi_fei_yu_pei(0.3) |
| 10_03 | ruMen | yinRou | 20500 | 780 | 280 | 10.6 | 否(章中考验) | weapon_liqi_lian_zi_bian(1.0)+accessory_liqi_hu_xin_jing(0.3) |
| 10_04 | ruMen | gangMeng | 26000 | 900 | 255 | 10.8 | 是(纯 stat·**无 bossPhases**) | weapon_liqi_hu_tou_gou(1.0)+armor_liqi_qing_luo_shan(0.4) |
| 10_05 | shuLian | yinRou | 32000 | 950 | 305 | 11.2 | 是(末 Boss) | weapon_liqi_li_hua_zhen(1.0)+armor_liqi_tie_ye_jia(0.5)+accessory_liqi_jin_chuang_gao(0.3) |

- 各关敌 `skillIds` 用 Task 1 的一流门派绝学 basic+skill（按 school 配）。
- 各关 `item_xinxuejiejing` quantity 沿 Ch9 续进 [28,34]→[32,40]；`item_silver` [300,380]→[380,480]；10_05 加 `item_jingyandan_large [1,1] 0.30`。
- **10_05 末 Boss**：`dropSkillManualId: skill_<真解>` + 敌 `chargeSkillId: skill_<真解>`（双用）+ `bossPhases`：`[{hpThresholdPct:1.0},{hpThresholdPct:0.5, aiMode:aggressive, onEnterMechanic:chargeCounter, titleKey:bossPhase_desperate}]`；敌 `skillIds` 含该真解。配 `narrativeDefeatId` 于 10_04/10_05。
- `baseExpReward` 先占位 [72,72,76,108,140]（Task 9 按 progression 校准）。
- `biome`/`sceneBackgroundPath`：10_01 frontier→10_02..05 mountainForest（`assets/scenes/battle_mountain_forest.png` 若存在，否则复用现有 + Task 8 allowlist）。`iconPath: assets/enemies/zhongzhou_<name>.png`（图待 codex，errorBuilder allowlist 兜底）。

- [ ] **Step 2: 验证加载** `flutter test --no-pub test/data/game_repository_test.dart`（会因 count 红 → Task 7；本步确认无 yaml 解析/红线 fail-fast 崩，尤其 prevStageId 单链 + realmLayer ≤ cap + 敌 skill-school 一致）。
- [ ] **Step 3: Commit** `git commit -am "新增 Ch10「中州」5 关（stage_10_01..05·一流·末 Boss 真解双用两相位）"`

---

## Task 4: 发布上限 cap 17→24

**Files:** Modify `data/numbers.yaml:206`

- [ ] **Step 1:** `max_absolute_realm_level: 17` → `24`。
- [ ] **Step 2: 验证** `flutter test --no-pub test/data/progression_release_cap_test.dart`（区间断言随之调，见 Task 11）。
- [ ] **Step 3: Commit** `git commit -am "[schema] 抬发布上限 17→24（一流熟练·配 Ch10 一流三章）"`

---

## Task 5: 红线层（真解白名单 + 机制成对 + 加载校验）

**Files:** Modify `test/**/wave_b_content_redline_test.dart`（真解白名单）；确认 `stages.yaml` 机制成对

- [ ] **Step 1:** `standaloneBossManualIds` 白名单登记 Task 2 真解 id（排除出 2/2/2 流派配平池）。grep 定位：`grep -rn "standaloneBossManualIds" test/`。
- [ ] **Step 2:** 确认 10_05 `chargeSkillId` ⟺ bossPhase `onEnterMechanic:chargeCounter` 成对；10_04 无 bossPhases（`readable_tempo` missingBossMechanic / chargeRows 校验）。
- [ ] **Step 3: 验证** `flutter test --no-pub test/**/wave_b_content_redline_test.dart test/**/readable_tempo*`（定位后跑）。
- [ ] **Step 4: Commit** `git commit -am "Ch10 真解白名单登记 + 末 Boss 机制成对校验"`

---

## Task 6: 叙事 ~13 篇（chapter_10 + 12 stage narrative）

**Files:** Create `data/narratives/chapters/chapter_10.yaml` + `data/narratives/stages/stage_10_0{1..5}_{opening,victory}.yaml` + `stage_10_0{4,5}_defeat.yaml`

- [ ] **Step 1: chapter_10.yaml**（prologue+epilogue，第三人称「他」）——承 Ch9「边塞三程到此为止，江湖才刚往深处去」；文化弧：碛北南下入中州、循路→开路顿悟、师父遗言**第 3 处回响**（章末兑现）、末 Boss 留文化承载遗物 hook Ch11。Tier 风格词「通达/洞明/练达/举重若轻」。
- [ ] **Step 2: 10 段 stage opening/victory**（第二人称「你」）+ 2 段 defeat（10_04/10_05）。地名/人物按 §3 骨架创作；末 Boss = 守道名宿（循前人规矩，与主角开路互镜）。
- [ ] **Step 3: 体例验证**（grep 0 命中）：

```bash
grep -rlE "legendary|epic|史诗|神器|传说级|无敌|最强|究极|霸气|逆天|血溅|刀光剑影|master class" data/narratives/**/*10_* data/narratives/chapters/chapter_10.yaml
# 现代词抽查 + 字数
```
Expected: 黑名单 0 命中；总字数 ~5-6k。
- [ ] **Step 4: 验证** `flutter test --no-pub test/**/mainline_narrative_completeness*`（count 1→10 会红 → Task 7）+ `asset_audit`。
- [ ] **Step 5: Commit** `git commit -am "[content] Ch10「中州」叙事 13 篇（章首尾+10 段 stage+2 defeat）"`

---

## Task 7: Reconcile — count 站点（N→N+5，45→50）

**Files:** Modify（Task 0 grep 定位现值）：`test/tools/progression_playtest_diagnostic*`（+ CSV evidence `UPDATE_PROGRESSION_PLAYTEST_EVIDENCE=1` 重生）/ `test/**/game_repository_test`（≥3 处：mainlineCount / 主线关红线含 Boss / R3 prevStageId 单链）/ `mainline_narrative_completeness`（count + 章循环 1→10）/ `balance_simulator`（`>=` 不破·硬编码改）/ `readable_tempo`（名 + 章 ratchet + **终章门槛钉 `stage_10_05`**）

- [ ] **Step 1:** 逐站点按现值 +5（45→50 等）；语义化优先（用 `>=`/`.length` 不硬编）。
- [ ] **Step 2:** CSV evidence 重生：`UPDATE_PROGRESSION_PLAYTEST_EVIDENCE=1 flutter test --no-pub test/tools/progression_playtest_diagnostic*`
- [ ] **Step 3: 验证** 上述文件全绿。
- [ ] **Step 4: Commit** `git commit -am "Ch10 reconcile：主线 count 45→50 + 终章门槛钉 stage_10_05"`

---

## Task 8: Reconcile — boss 计数 + catalog + 生产可见性

**Files:** `test/**/stages_boss_enemy*`（isBoss +2）/ `boss_memory_providers*`（图鉴）；`lib/**/chapter_list_screen.dart`（`_chapters` `[1..10]`）+ 章卡 widget 测（viewport 扩容 800×2000）/ `lib/**/main_menu*`+`status_summary*`（章循环 `<=10`）/ `lib/shared/strings.dart` + `UiStrings.chapterTitle/Hint`（switch +「十章」）/ `boss_memory_key`（group index 偏移·持久化不重排旧值）

- [ ] **Step 1:** 逐站点同步（Task 0 grep 现值）。boss_memory_key 新章 chNum 与心魔/轻功/群战撞 → 按现有偏移规则加，旧持久化值不重排。
- [ ] **Step 2: 验证** targeted 上述测 + `flutter analyze` 0 + `dart format`（改了 dart）。
- [ ] **Step 3: Commit** `git commit -am "Ch10 reconcile：boss 计数/图鉴 + 章列表/菜单/strings 生产可见性（第十章可达）"`

---

## Task 9: Reconcile — progression 逐值实测（Lv 快照）

**Files:** `test/tools/progression_release_budget*` + `progression_idle_horizon_simulation*`（首通/全内容 Lv + cumExp/缺口/里程碑全位移）

- [ ] **Step 1:** 跑测取 fail-fast 首个错值 → 按实测改 → 单文件重跑（~5s）→ 迭代至绿。**禁猜**（逐值实测）。同步回填 stage `baseExpReward`（Task 3 占位）使曲线自洽。
- [ ] **Step 2: 验证** 两文件全绿。
- [ ] **Step 3: Commit** `git commit -am "[balance] Ch10 reconcile：progression Lv 快照逐值实测（首通/全内容 Lv 位移）"`

---

## Task 10: Reconcile — skill 计数 3 处 + tier 红线

**Files:** `test/**/game_repository_test`（skillDefs 总池）/ `skill_count_contract_test`（genericIds hasLength + **交叉核对 GDD「N 招」**）/ `skill_qi_redline_test`（skillDefs hasLength）/ `enhancement_material_supply_test`（结晶总量）/ `mainline_stage_curve*`（章→境界映射 cap-agnostic，按 `requiredRealm.index` 不硬编）

- [ ] **Step 1:** skill 总数 +7（6 敌招 + 1 真解）三处同步；结晶总量 +Σ新关；tier 红线改语义化。
- [ ] **Step 2: 验证** targeted 全绿。
- [ ] **Step 3: Commit** `git commit -am "Ch10 reconcile：skill 计数 +7 三处 + 结晶供给 + tier 红线 cap-agnostic"`

---

## Task 11: Reconcile — cap 17→24 抬升 4 站点

**Files:**（memory `feedback_wuxia_release_cap_raise_reconcile`）① `wave_b_content_redline`+`skill_source_redline`（`canEquipAtRealm(releaseTier)` 重算 + 补 `&& !s.mountDeferred`）② `inner_demon_r5_redline` R5.3（改 cap-agnostic 读 `maxAbsoluteRealmLevel`）③ stale 文案 de-drift（`overflow_layer_jump_probe`/seclusion/game_repository「Lv100/停在层10」label）④ 确认不破（dynamic 读 cap 的、`progression_gate_service` 自建 fixture）

- [ ] **Step 1:** 逐项按 memory 改法处理（Task 0 grep 定位）。
- [ ] **Step 2: 验证** targeted 上述测全绿。
- [ ] **Step 3: Commit** `git commit -am "Ch10 reconcile：cap 17→24 抬升 4 站点（挂载完备/升层门禁/stale 文案）"`

---

## Task 12: GDD §8.1 同步

**Files:** Modify `GDD.md`（§8.1 章表 + 招式池「N 招」，与 `skill_count_contract` 交叉一致）

- [ ] **Step 1:** §8.1 加 Ch10「中州」行 + 招式池总数 +7 + 发布边界层更新（一流熟练）。
- [ ] **Step 2: Commit** `git commit -am "[GDD] §8.1 同步 Ch10「中州」章表 + 招式池 +7"`

---

## Task 13: 批末验收 + 破坏证红

**Files:** 无（验证）

- [ ] **Step 1: 全绿门禁**（跨切面 → 全量）：
```bash
flutter analyze                    # Expected: No issues found
dart format --output=none --set-exit-if-changed .   # Expected: EXIT 0
flutter test --no-pub              # Expected: 全量 pass 0 fail（基线 4586 + Ch10 新增）
```
- [ ] **Step 2: 红线专测**：`full_build_damage_redline`（不进百万）/ `balance_simulator`（末 Boss 血线 ~0.94-0.96·跨阶战败风险）/ `skill_qi_redline` / `wave_b` 配平 全绿。
- [ ] **Step 3: 破坏证红**（commit 后做）：临时改真解 mult=9000 → `skill_qi_redline`/`encounter_red_lines` RED EXIT=1 → `git checkout -- data/skills.yaml` 还原 → 重跑绿。改 count 断言证辨别力同理。
- [ ] **Step 4: 更新 PROGRESS.md 顶段**（四态：已完成/已验证/已知风险/下批建议）+ commit。
- [ ] **Step 5: push + draft PR**（`gh pr create --draft`），PR body 列 spec/plan 链接 + reconcile 站点 + 已知风险（11 图待 codex / 数值 balance 剖面）。

---

## Reconcile 现值表（Task 0 填，Task 7-11 引用）

| 站点 | 现值 | 目标值 |
|------|------|--------|
| mainlineCount | _(grep)_ | +5 |
| 主线关总数 hasLength | _(grep)_ | +5 |
| isBoss 敌总数 | _(grep)_ | +2 |
| 图鉴 catalog | _(grep)_ | +2 |
| skillDefs 总池 | _(grep)_ | +7 |
| skill genericIds hasLength | _(grep)_ | +7 |
| 章循环上界 `<=N` / `[1..N]` | 9 | 10 |
| 首通 Lv / 全内容 Lv | _(grep)_ | 逐值实测 |
| max_absolute_realm_level | 17 | 24 |
| TechniqueTier 一流前缀 | _(grep)_ | — |

---

## Self-Review

- **Spec coverage:** spec §2 文化弧→Task 6；§3 五关→Task 3；§4.1 敌招→Task 1；§4.2 真解→Task 2；§4.3 装备复用→Task 3 drop；§4.4 叙事→Task 6；§5 cap→Task 4；§6 reconcile→Task 7-12；§7 验收→Task 13。全覆盖。
- **Placeholder:** reconcile 现值标「_(grep)_」非偷懒——加章现值必执行时实测（memory 铁律），已给定位命令 + 改法规则 + 目标增量；真解名/地名待 Task 6 叙事定，id 全程一致 `skill_<真解>`。
- **Type consistency:** stage id `stage_10_0N`、真解 id、`skill_<school>_<一流前缀>_<type>` 命名全程一致；cap 字段 `max_absolute_realm_level` 一致。

## Ch10-12 说明

本 plan 仅 Ch10。Ch11-12 复用敌招/真解命名体系 + 本 plan 结构，另起 spec/plan（一流三章共 cap 24）。
