# 零引用逐条复核恢复点

- 目标：完成 B2-1 基线重锚、B2-2 四层审计；主体 READY 后再评估 B2-3。
- 分支：`codex/b2-unused-keys-review-20260917`。
- 派单基线：`c64b165938d948a24ab64c7c71de950eea466758`。
- 开始时间：2026-09-17 21:49:31 +08:00。
- 写入范围：仅派单六个白名单路径；配置、生产代码、测试和 B-2 原表均只读。
- 验收：基线保护通过；逐条四层命令可复跑；汇总等于明细；证据片段与源行一致；差异仅白名单；中文提交、干净工作区。
- 本单不运行 Flutter、GUI 或真实存档操作；不 push、merge、rebase、revert。

## 当前恢复点

- 状态：B2-1、B2-2、B2-3 审计产出 READY；最终收据绑定 BLOCKED。
- 最后完成：主体 87 路径／202 叶子的四层报告；建议为 0／42／22／23 路径，0／53／106／43 叶子；误判实际消费为 0。
- 下一步：生成绑定审计提交的事实收据并封装提交；协调者需确认外置收据或允许审计提交与封装 tip 分离，方可解除最终绑定阻塞。
- 已跑验证：新基线保护通过；1796 / 847 / 497；四类 234 / 0 / 202 / 1360；零引用 87 归一路径、51 末段；原表集合差异为空；grep 8/8、生产双向 3/3。
- 阻塞：收据规范要求最终提交自包含自身 SHA 与完整补丁摘要，形成自引用；已请求交付方式澄清，审计可继续。

## 分工

- 主窗口：生成报告、独立核对、集成与收据。
- 基线复核：唯一写扫描器，逐条重定位排除证据。
- 合同复核：只读核对设计合同与实际消费。
- 历史复核：只读核对加入意图与历史消费。

## B2-2 收口验证

- B2-1 检查点：`0411a2ed2`（READY）。
- `python3 tools/audit/numbers_unused_keys_review.py --write` 实跑 519 条去重证据命令。
- `python3 tools/audit/numbers_unused_keys_review.py --check`：四层复跑与报告逐字节一致。
- 主体报告 SHA-256：`28c1dd9af8155eacbb893961d6cfd7b1e7a66fa4583c1c7c8c1f51ff92f0bd4a`。
- 从 Markdown 独立重计 87 行／202 叶子，与当前 YAML 和原表 key 集合一致；十四组所有引用行均与源码原文一致。
- 根窗口独立零字面 grep 8/8；loader 抽查 3/3；内存重复键校验 2/2 拒绝，合法数组同名保留 2 叶。
- 新基线保护通过；旧基线保护失败且自动判零降至 0，保守条件未放宽。
- `git diff --check`：退出 0；变更和未跟踪路径均在白名单内。
- 原表删除建议重分类 57 路径／169 叶子；没有获准删除字段。
- Flutter test / analyze / format 均 NOT_RUN；未进行正式 Gate 或真人验收。

## B2-3 收口验证

- B2-2 READY 检查点：`c579d0766159d4bf875295e1b573bad074750a97`。仅在此提交后开始弹性尾。
- 筛选池 28 路径／72 叶子，挑选三类装备上界 3 路径／21 叶子完成同样四层复核，均建议保留有合同；其余 25 路径／51 叶子未扩审。
- 正式四类判定与主体建议计数不变；弹性尾不晋升正式零引用。
- `python3 tools/audit/numbers_unused_keys_review.py --include-pool --write`：实跑 530 条去重命令。
- `python3 tools/audit/numbers_unused_keys_review.py --include-pool --check`：逐字节一致；独立解析附录行数和叶子数通过。
- 最终审计报告 SHA-256：`5b1d0bc8229b6f15fc1b76a7dfa4ea88322c517050e014dbd673b3ef94f94a0b`。
- 完整证据回放发现被引用文档自身的代码围栏会影响 Markdown 显示，已改用比原文更长的围栏；原文未删改，计数与建议不变。
- 修正后再次完整四层复生成，逐字节一致；根窗口从最终报告提取并重跑全部 367 条 rg/sed，命中数、退出码与完整原文 367/367 一致。总证据记录 530 条。
- `git diff --check` 通过；不修改任何白名单外文件。

## 收据绑定阻塞

- 来源：`/Users/a10506/.claude/skills/afk/scripts/receipt.schema.md` 的字段约束要求 head_sha 为“执行端收工 tip”，并对完整补丁求摘要；生成顺序第 5 条又要求在收据生成后提交它以保持清洁。
- 若把收据提交到其声明的最终 tip，提交 SHA 与完整补丁摘要都包含收据自身，形成无法按正常提交步骤求出的自引用；不得伪填最终 tip 或声称 Gate 通过。
- 已向用户请求选择可核验交付方式，尚未取得答复。保留两层事实：先冻结 READY 审计提交，收据如实绑定该提交；随后只提交收据形成 BLOCKED 封装 tip。这提供可复核证据，但明确尚未满足“head_sha 等于最终 tip”的整单约束。
- 最终仓库外摘要记录审计提交、封装提交、耗时和上述偏差。解除需要协调者允许外置收据，或明确接受收据绑定审计提交；无需修改配置、生产代码或测试。
- 派单要求 Flutter 三项 NOT_RUN；现 Gate 的 --skip-full 仍对撞 analyze/format，须由协调者按派单独立复跑后处理，当前不声称整单 Gate PASS。
