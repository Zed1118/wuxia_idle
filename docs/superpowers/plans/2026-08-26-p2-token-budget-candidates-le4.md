# P2 攻击令牌总预算 ≤4 候选重跑计划

## 目标与边界

- 目标：用现有 TOKEN 台架穷举四类总预算 2–4 的全部合规组合，产出可与原 A/B/C 横向对照的实测候选表与推荐。
- 非目标：不替用户选择，不修改或落地生产配置，不触碰 registry、姿态、`defenseBreak` 或项目禁区。
- 分支：`codex/p2-token-candidate-rerun-20260826`；基线：`03275d2c`。

## 验收标准

- [x] 原 A/B/C 既有用例与断言不修改，新代码复现原表三行。
- [x] 真实生产链使用同一 `AttackTokenDirector`，且已核对零预算的移动/攻击语义。
- [x] 穷举结果固定为非负整数解：总和 2/3/4 各 10/20/35 组，合计 65 组。
- [x] 报告 `docs/spec/phase2_token_budget_candidates_le4_20260826.md` 不超过 120 行，包含全表、A 基准、实测语义、A/B/C 复现与单行推荐。
- [ ] 定向、全量 reporter + `[E]`、analyze 已通过；全仓 format 因基线 8 个探针文件不洁而失败。
- [x] 仅候选证据/台架/恢复计划改动，中文动宾 `[BLOCKED]` tip，工作树 clean。

## 任务切片与当前恢复点

1. 读懂原台架与生产消费链，原口径复现 A/B/C。
2. 增加零预算生产闸语义实测与 65 组穷举。
3. 采集数字，写 ≤120 行报告。
4. 完成全部验证、diff/禁区复核、提交与 clean 确认。

**当前恢复点**：BLOCKED；候选与证据已完成，A/B/C 精确复现，65 组已实测；唯一未关闭门为 `dart format . → 0 changed`，当前 SDK 对基线 8 个 `tools/phase0minus_probe/**` 文件报告格式变更。越界变更未保留。
