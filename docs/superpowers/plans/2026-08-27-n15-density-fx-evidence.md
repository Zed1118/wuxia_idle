# N15 密度视效证据计划

## 结果合同

- 单一目标：机器判定 N15 矩阵格 1–4，全 PASS 才 `[READY]`，任一无法判定或 FAIL 即 `[BLOCKED]`。
- 固定分母：格 1 真实敌人数、格 2 攻击令牌序列、格 3 逐 tick 领域状态、格 4 塔 14/群战 18/24 密度。
- 非目标：格 5–7、视觉截图、任何 `lib/`/生产配置改动、调优、合并或推送。
- 基线：`5440f931`，分支 `codex/p2-density-fx-evidence-20260827`，开工时工作树 clean。
- 成本边界：按真实墙钟记录；约 90 分钟无 Gate 变化则停止扩展并重评。

## 验收标准

- [x] 复核 N2 审计 L55 与方案 L800–802/L1420–1428。
- [x] 读 `docs/spec/rejected_task_registry.md` 并把 CLAUDE.md §8.2 落入清单。
- [x] 从现有源码定位真实设置项、provider 与消费点，不猜字段。
- [ ] 格 1–3：同 seed/关卡/角色快照，唯一差异为高/低特效，计数/令牌/稳定摘要全等。
- [ ] 格 4：从独立生产配置读密度目标，与生产运行实体计数比对。
- [x] 同一 targeted 命令连跑两次结果一致。
- [x] `gate.sh` 全量模式通过；检查 diff/禁区/skip/报告行数。
- [x] 工作树 clean，tip 为 `[READY]` 或 `[BLOCKED]` 中文动宾提交。

## 生产路径与红线

- 生产设置实况：`GameplaySettings.reduceFlashing` 只被设置面板与 `SharedPreferences` 消费，战斗/VFX/领域层零消费；无高/低特效密度值或 provider。
- 密度配置实况：塔 14 层 `enemyTeam` 为 3；群战逐波总量为 10/17/19/24/26，单波上限 7；无 active=14/18/24 生产目标配置。
- 红线影响：不改数值、结算、三系锁、在线=离线、文案或任何生产路径；只增测试与审计文档。

## 当前恢复点

- 状态：`[BLOCKED]`，固定分母 0/4 PASS。
- 最后完成：发布 `[BLOCKED]` 候选；2 条可重放阻塞事实测试通过，格 1–4 各有 1 条显式 `skip:` 且与报告 FAIL 一一对应。
- 下一步：交由协调者评审；本分支不再扩展、不合并、不推送。
- 已跑验证：targeted 连跑两次均 `2 PASS / 4 SKIP / 0 FAIL`；完整日志 `cmp` 退出 0，SHA-256 均为 `7ff3fc9d4416f90f91aa2007f66f47734bc354928c7c215f9d5ccc3b5d485b21`；单文件 analyze 0；`git diff --check` 通过；全量 gate PASS（`5613 PASS / 4 SKIP`，analyze 0，format 0 changed）。
- 阻塞项：必须新增并接线生产特效密度设置，且必须新增/冻结 14/18/24 密度生产配置；均需改 `lib/`/配置，超出 N15 权限。
