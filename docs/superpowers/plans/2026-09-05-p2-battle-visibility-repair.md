# Phase 2 战场可读性返工

## 结果合同

- 授权：用户“按照建议修复”；延续同一受控候选分支，不创建新 worktree，不合并/push main。
- 实时基线：候选 `b13a0094` clean；main/origin/main `79adb840` clean。上一轮四项修复已在候选内，不重做。
- 主 WIP：M4 客观画面缺陷返工；本轮固定两个缺陷门：人物与 HUD/视口边界冲突、终局 Q/R 误报倒地。预期本地修复 0/2→2/2，正式 M0–M9 仍 1/10，不将局部完成当正式放行。
- 13:00 CST 起首轮 60 分钟检查点；主成本为墙钟时间。25%/50%/80% 看有效 RED、真实修复与 GUI 复验进展；80% 后优先收口，无足够余量不扩展五武器/Boss/长时验收。
- 保持领域坐标、75% camera、攻击距离、战斗结果、奖励与 schema/saveVersion 不变。只调整表现投影留白、视野外人物绘制/选取一致性和技能印终局显示；不新增 Boss 预兆图标或玩法。
- 存档：只启动同 AOT 的独立 bundle ID 副本，沿用受保护测试容器，不打开/覆盖原用户存档。

## 验收 checklist

1. 新测试先有效 RED：双常规视口的完整人物边界与 HUD 不重叠；真实战斗胜利时玩家存活、Q/R 不显示倒地且键鼠仍禁用。
2. 生产接线：共享 `Phase0aBattleScreen → Phase0aStage` 被各真实 Host 消费，演员绘制、鼠标选敌与屏外威胁规则一致；不独立挪动人物脱离 VFX/领域坐标。
3. 定向与相邻测试、analyze、format、diff check；仅表现层不无脑全仓。冻结 exact SHA 构建 Profile。
4. 独立容器从生产根进入轻功、守城、断魂庄及主线胜利；1280×720 与 1440×900 实机 smoke。未完成的尺寸/路径不得借 widget 测试代签。
5. 红线：无战斗数值、三系、在线离线规则或用户存档变化；不散写中文文案/业务常量，不加高频日志。
6. 收口：版本化证据与残留风险、候选 clean 恢复点；在全部约定验证通过前不标 READY，正式进度仍 10%。

## 当前恢复点

- 状态：本地两个缺陷 2/2 已验证，写完待评；正式 M4 未关闭。冻结实机代码 `8c50f0f99cb7eac1955dd0d1cead099ac62490e4`。有效 RED 为双视口越界与胜利存活误报共 3 FAIL；249 项扩展回归通过，包含整目录 presentation 与主线/塔/断魂庄真实接线、G2 生产验收测试。
- 增强检查：25 个 resident 人物图片仍逐一核对解码上限（含屏外）；层级按实际像素滞回检查；鼠标测试使用镜头内目标及真实可见位置。浮点往返断言改为双轴 1e-9 容差，并登记测试契约迁移。
- Mutation：移除屏外鼠标选取保护得到精确 1 FAIL（隐藏敌人出现选中标记）；已还原，增强的定向检查 15 PASS。最终 analyze/format/契约 Gate 通过，Profile 构建成功。
- 实机：轻功、守城、断魂庄的边缘人物与底部 HUD 分离；主线 05 胜利，祖师 6070/6070，Q/R 无倒地误报。1280×720 及目标 1440×900 的窗口扩展均实走，后者缺精确原生尺寸读回，不能冒充正式尺寸签字。近身人物重叠、完整手感/听感/Windows 仍开放，详 `docs/audit/phase2_battle_visibility_repair_2026-09-05.md`。
- 收口约 13:21，墙钟约 21/60 分钟；应用已退出，原三档哈希不变。只冻结当前候选，不扩张新内容；待授权合入 main 并执行集成全量/exact-SHA CI，formal 仍 1/10。
