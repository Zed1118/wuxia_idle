# P2 M2 Batch15：结构构造与主线运行时合同

## 目标

从 Batch14 READY `7bc31c5f5463aac26e127912576350487ac0a8d3` 出发，并行补齐三个互不重叠、无需新产品决策的证据或应用合同：五关 candidate-only runtime 构造矩阵、参与者政策到连续主线 run 的 admission 组合、随行听剑单关占用的不可变生命周期。本批不切 production host，不晋升 candidate/tuning，不执行 Profile 或真人验收。

## 并行任务

1. V01 / Qoder CLI + Qwen3.8-Max high：只证明现有五关 candidate assignment 可经 loader、typed migrated route、runtime plan 与 Batch14 显式 assembler seam 完成结构构造。objective defeat projection 使用逐 actor 显式空声明，不把 construction 冒充 objective 可执行性。
2. R14 / Codex high：组合既有 `MainlineParticipationPolicy.resolveParticipant` 与 `MainlineRun.begin`，返回保留原 request、selection 与 run 的不可变 admission；所有 leader/eligibility/run/stage/loadout facts 均由 caller 显式提供。
3. R15 / Pi + DeepSeek V4 Flash high：实现 owner-bound immutable predecessor → prepared successor 的单关 occupancy runtime；acquire 只消费 choice/blocking status，release 只消费 R02 冻结的四种 reason，空 choice 为 revision 不变的显式 no-op。

## 硬边界

- V01 不修改 candidate fixture，不推断 objective identity，不运行数值、性能或玩法验收，不使用 production 标签。
- R14 不查询当前掌门、占用、伤势或装配，不接 host/save/UI/persistence，不创建第二持久装配方案。
- R15 不查询四类活动，不实现成长比例、cap、grant 或 durable claim；release 与 proficiency claim 完全分离。
- production host/stage route、candidate/tuning 晋升、Mac/Windows Profile 与真人试玩继续保持 Gate。
- R12b 的显式 transactional session plumbing 已完成只读预检；因它修改 combat session 且需要单独验证 fork/outer-flow rollback，放入下一独立切片，不与本批三项混写。

## 集成与验收

- 三项在共同 READY 基线的独立 branch/worktree 内小切片实现；V01/R15 分别记录实际 Qoder/Pi 版本、精确模型与只读设计/最终 diff 审查。
- 主控逐项核对 actual diff、中文动宾提交、owned files、source→integration stable patch-id、生产隔离与 Gate 诚实性。
- 集成态执行去重联合 targeted、变更 Dart scoped analyze、format、YAML/Markdown/diff/path/main refs 快速闸门、full Flutter test 与独立终审；P0/P1/P2 清零后追加 Batch15 READY。

## 当前恢复点

- [x] Batch14 READY `7bc31c5f` 已冻结，main/origin main 保持 `e292d3a0`。
- [x] 完成 Batch15 候选盘点与 R12b 只读 API 预检。
- [x] 创建三来源 worktree 与一集成 worktree；四者完成 lockfile pub get、build_runner 126 outputs、63 个 `.g.dart` 与 dylib SHA 对齐。
- [x] 登记 V01 / R14 / R15 与 Batch15 integration 的文件所有权、依赖和 Gate。
- [x] R14 完成修后 READY、独立复审、稳定 patch-id 集成与主控 54/54 复跑。
- [x] R15 完成 Pi/DeepSeek 两轮只读审查、来源 65/65 与 Codex 独立终审，五个非空提交已集成。
- [x] V01 完成 Qoder/Qwen3.8-Max 两轮只读审查、两项 P2 修正与 51/51 复验，四个非空提交已按 stable patch-id 集成。
- [x] 三来源实现、外部 CLI 证据、独立复审与 READY 全部完成。
- [ ] 主控集成、联合/full 验证、独立终审与 Batch15 READY。
