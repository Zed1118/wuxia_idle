# Stage preview production encounter parity

- Goal: make the existing chapter stage subtitle describe the same production encounter selected by the mainline battle host. Primary gate remains M2; formal M0–M9 stays 1/10.
- Baseline: clean main/remote/worktree `40f60bde933c33c8fbead6534e14d1931c27c714`, exact CI success. Black Wind Ridge shows legacy 3 waves/9 enemies instead of the typed 40-entry encounter; Ferry shows 6 instead of 2.
- Branch: `codex/stage-preview-runtime-20260905`.
- Scope: existing stage-row enemy count only. Reuse repository catalog lookup and localized count formatter; preserve locked hints and legacy fallback. No numeric tuning, save/schema, routing, battle, objective, or interaction changes. Do not pretend spawn entries constitute a fixed wave count.
- Budget: about 45 minutes, wall clock; one prerequisite for truthful M2 desktop acceptance, no formal gate increment. No new main merge/push is included in this local follow-up; prior explicit merge authorization covered the test-hardening patch.
- Steps: reproduce subtitle mismatch with production-data widget coverage; apply minimal consumer correction; targeted/adjacent tests and analyze; 1280×720 and 1440×900 production-root isolated visual smoke; review diff and commit a clean READY candidate.
- Checklist (§8.2): [x] real StageListScreen consumer uses repository combat encounter lookup shared with runtime binding adapter; [x] targeted tests with failure before correction and success after; [x] no hardcoded content/counts, no numeric or realm/online/offline rule change; [x] desktop viewport captures and residual limitations recorded. No interactive control is changed.
- Recovery point: freeze this three-file candidate with a `[READY]` commit and verify a clean worktree. Rejected task registry read; this repairs an existing subtitle, not a new prebattle advice panel or prohibited preparation feature.

## Verification and handoff

- Real production chapter 1 route counts are now 25 / 25 / 40 / 3 / 2. The subtitle uses `combatEncounterForStage`, also used by the runtime binding adapter, and reports spawn-entry totals rather than inferring fixed waves. Locked hints and the legacy fallback are preserved.
- RED: the two new production-data tests fail against the old subtitle. GREEN: stage-list tests 16/16 and adjacent cycle tests 6/6 pass. Each new test scopes counts to all five real stage rows, scrolls each count into view and checks hit testing at 1280×720 and 1440×900.
- `flutter analyze --no-pub lib test tool`: no issues. Dart format: zero changes. `git diff --check`: clean. macOS profile build and isolated bundle signature verification pass. Initial unscoped `flutter analyze --no-pub` included the separate `tools/phase0minus_probe` project and failed with unresolved dependencies; the subsequent successful command matches CI scope. Build warnings from the existing audio plugin remain.
- Native production-root smoke: isolated bundle `com.pen.wuxia.acceptance20260905`, third apprentice save. At 1280×720, Black Wind Ridge shows 40 and Ferry shows 2 with no clipping. Dragging the window edge by +160/+180 exercises the larger layout requested as 1440×900; CUA encodes that capture at 1229×768. Exact logical 1440×900 dimensions are independently covered by widget tests; do not describe the resized JPEG as a raw 1440×900 capture.
- Original bundle `com.pen.wuxia.wuxiaIdle` was not launched. All original Documents file hashes are unchanged and the three save hashes match the prior frozen acceptance record. The isolated candidate was closed after smoke verification.
- Focused diff review: no data, combat, save, routing, or control changes; repository encounter lookup returns null for legacy routes, preserving their existing wave/count fallback. No deleted assertions. Only the subtitle consumer, its regression coverage and this recovery record changed.
- Evidence: `/Users/a10506/Documents/Codex/2026-09-05/wuxia-stage-preview-runtime/README.md` (logs, screenshots, candidate hashes, original-save postflight). Work took about 13 minutes (17:21–17:34) of the 45-minute wall-clock budget; formal gate delta 0, integration rework 0.
- This is a local READY prerequisite, not a new M2 victory or human acceptance result. New-candidate remote CI/main integration remain pending; the previous main CI success does not certify this patch. Next step is review/integration authorization, followed by Black Wind Ridge verification against the unified candidate. Black Wind Ridge victory, real human experience, and the older first-CI-failure root cause remain open.

## Authorized integration — 2026-09-05

- User explicitly authorized merging/pushing candidate `363a972c` and continuing Black Wind Ridge evidence after CI; this supersedes the earlier local-only boundary.
- Clean main/origin baseline remained `40f60bde`. Focused diff review found only the existing subtitle consumer, two production-data viewport regressions and the recovery record; no rules/data/control/save changes or assertion removals. No-ff merge is `9e63ecbff8cc76ac5c5903d8ced77b8e24c7315e`.
- Exact merge full regression: **6035/6035 PASS**, exit 0, zero error blocks, 18:52:41–18:59:59 CST (7m18s), under the shared exclusive lock; only the owned lock was released. Analyze `lib test tool`: 0 issues. Format `lib test docs`: 1646 files, 0 changed.
- Existing isolated profile AOT hash rechecked; candidate and merge `lib/data/assets/macos/pubspec` trees match. The integrated menu visibly shows Black Wind Ridge 40 and Ferry 2, automatic combat is off, original saves are hash-recorded and isolated Documents have a fresh backup.
- Governance synchronization follows this tested merge without changing production/tests. The final pushed SHA and its exact CI, plus subsequent Black Wind Ridge results, will be recorded in `/Users/a10506/Documents/Codex/2026-09-05/wuxia-stage-preview-integration/README.md`. Do not infer new-candidate CI or victory from earlier reports. Formal M0–M9 stays 1/10.
