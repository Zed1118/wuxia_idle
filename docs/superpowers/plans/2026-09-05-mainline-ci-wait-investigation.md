# Mainline CI wait investigation

## Current continuation — 2026-09-13

- User goal: proceed in the reported order through CI recovery, native old-save verification, controlled integration, performance and the remaining Phase 2 gates. This first delivery unit fixes both concrete failures from CI `34732200859` at `198ec8641a81ce45110fcc997ba000b54fcd7688`; the older scope/budget statements below describe the September 5 run only.
- Current branch: `codex/p2-player-flow-20260910`. `main` and `origin/main` remain `342d1927529b8306582431be597ef1975bd69deb`. Preserve the four existing user files from the external `baseline.json`; candidate validation does not authorize discarding them or substituting for formal clean/human/Windows acceptance.
- Native failure: Linux rejects the disposable bad-file probe with `MdbxError (-30793): MDBX_INVALID` before the existing missing-SaveData classification. The precise native-open signature now becomes `UnreadableSaveException`, retaining its cause and stack. Other Isar/setup/read/version errors retain their original type. No migration, schema, value or original-save write changes.
- Mainline failure: CI reported a missing `Phase0aMainlineBattleHost` at the pre-`try` lookup, followed by a ten-minute timeout. A one-second settings-load delay in the real event loop reproduced that missing-host assertion on the unchanged 120-pump wait. A five-second wall-clock wait and unconditional tree disposal now preserve the real StageListScreen/participant/Isar/host path and original bot/identity assertions.
- Cleanup counterexample: removing tree disposal made the new timeout-cleanup test fail on the remaining ProviderScope, then remain running for over 60 seconds. Only that controlled runner was terminated at 71 seconds and the exact original bytes restored. Its signal exit code was zero despite failed assertions and shutdown errors; it is a stopped RED experiment, never a passing test. This supports the failure mechanism, but does not prove which I/O operation was slow in the remote run.
- Independent review caught a scheduling race in the initial 300ms-delay/30ms-timeout cleanup fixture. The final fixture uses a Completer that remains blocked until tree disposal, so CPU load cannot accidentally satisfy the expected timeout. The separate one-second delayed successful-entry test remains intact.
- First candidate `76144e458b581e0b994f1b1d90e56e9e58202976`: focused 9/9 files and 57 PASS; locked full coverage regression 921/921 files and 6574 PASS, zero failure/skip/missing files, unchanged sources (15:42:24–15:58:15 CST). Analyze zero issues, format 1705/0 changed, coverage 49122/56873 (86.37%, minimum 81.25%), test-contract gate PASS. Candidate pushed; main unchanged.
- Exact-SHA CI `34746548165` completed with failure: 921/921 unique files, 6573 PASS / 1 FAIL, zero skip. Both original broken areas passed on Linux. The remaining failure is `phase0a_defend_feedback_test.dart` expecting the old battle page gone 400ms after the retry dialog appears. All other test shards and macOS build passed; coverage aggregation was skipped. Full logs and all four shard manifests/results are retained externally.
- Follow-up RED: the unchanged escort test passes locally (7/7). Letting its actual battle route fully enter before cargo destruction reliably reproduces the exact missing-removal assertion (0 PASS / 1 FAIL). Flutter 3.41.5 widget tests use Android's default predictive transition: 450ms both ways. Dialog appearance does not mean the old route has finished its reverse transition. The test now waits with a five-second wall-clock bound for the old page/host to leave the tree, and for `runStageFlow` to finish after returning; all original cargo, participant, retry and persisted-state assertions remain. No production changes in this follow-up.
- Follow-up verification: the complete mainline presentation scope passes 24/24 files and 204 tests, zero failure/skip/missing files, unchanged source hashes; whole analyze zero issues and format 1705/0 changed. Independent diff review found no weakened assertion or new unawaited timer. Since this follow-up changes only the test and this checkpoint, reuse the completed `76144e458` local full suite for its unchanged production code and run a new exact-SHA full Linux CI for the changed test candidate. Do not relabel the earlier local full suite as run at the later SHA.
- Exact-SHA follow-up CI: `1939dafcda836383d62f8d4830e84cd530f5dbcf`, run `34747336113`, completed **success**: four Linux shards cover 921 unique files / 6574 PASS, zero failure/skip; analyze, format, macOS Debug build and coverage gate all pass. Replaying the repository ratchet on the downloaded merged artifact gives 49130/56873 (86.39%, minimum 81.25%). Main remains `342d1927529b8306582431be597ef1975bd69deb`.
- Native verification started only after that successful CI. The old-main 0.49.0 app created a real new save through production menus in its isolated container; a cold 26-collection export confirms 0.49.0 and unchanged input bytes. Candidate production code is identical between the built `76144e458` app and `1939dafcd`; only the escort test/checkpoint differ. The candidate has **not** completed loading/migrating that input. The standalone Debug VM lacks compilation service for the planned live reader, and exact-path/unique-ID CUA selection repeatedly failed; these are unresolved verification-tool failures, not a migration PASS.
- **Native incident / STOPPED**: after exact-path app selection failed, the main agent incorrectly used CUA display-name fallback `wuxia_idle`. This launched the historical Profile package under `.codex/worktrees/1454`, using production bundle `com.pen.wuxia.wuxiaIdle`. Production slot 1/2/3 binary hashes changed. The wrong process and all task-owned game processes were stopped; the user was immediately informed. No further app launch, migration, restore, or original-file overwrite is authorized by this checkpoint. Originals and unaffected humanfix/desktop backups have cold incident copies in the external evidence directory.
- Incident evidence so far: all three preserved production copies export 26 collections with saveVersion 0.40.0. Slot 1 has an independently located **exact pre-incident SHA** backup; full current-schema collection/field comparison gives zero differences. Slots 2/3 lack an exact pre-incident backup so far. All three incident files are byte-identical to historical September 5 opened audit copies, which supports a schema-open explanation but is not by itself proof of every pre-incident field being preserved. Do not claim all original saves were unchanged or all three are proven lossless.
- Additional offline evidence: opening an internal copy of the exact pre-incident slot 1 with the historical 1454 schemas reproduces the incident file byte-for-byte (20 changed bytes; source unchanged, no app/business migration). Slot 1 full logical comparison remains zero differences. The same byte-change precondition does not hold for slots 2/3; no recovery candidate was written. A read-only supplementary search across game containers/user temporary directories found no exact slot 2/3 backup among 743 size-matching files.
- User resumed with “按推荐执行”: preserve all three production saves at their incident-contained state without rollback; repair exact app identity/launch isolation, then verify migration/reopen/full-field comparison using copies only. Slot 2/3 historical uncertainty remains recorded and does not indefinitely block copy-based candidate work. The incident STOP above describes the earlier state and is superseded only for this explicit scope. Never use display-name fallback, start the production bundle, or overwrite originals. Continue to candidate integration preparation/performance only after the native copy gate passes; main merge/push remains separately authorized. Formal M0–M9 remains 1/10. Current detailed incident status: `/Users/a10506/Documents/Codex/2026-09-13/p2-ci-recovery/native-incident/report.md`.
- Resumed native launch: the task-owned copy now has unique bundle ID `com.pen.wuxia.migration1939probe20260913`, bundle/display name `WuxiaMigration1939`, and the original executable name `wuxia_idle`. CUA selected the full app path successfully. Before selecting the real 0.49 slot, the process executable, isolated container file, input hash and all seven protected-file hashes were checked. The user-visible startup, recap dismissal and normal exit were exercised; process open-file evidence identified only the dedicated container. Protected originals remained at their incident-contained hashes.
- Native gate is still open: the first cold export shows 0.49→0.50 migration, three equipment catalog backfills and actual passive rewards (16 EXP / 1 stone), but the SaveData reward totals and settlement anchor were overwritten. `sweepReadinessLastRecoveredAt` predates the inventory reward timestamp by 83.076ms, consistent with the sweep readiness reader persisting a stale whole SaveData row after the passive transaction. Seven progressive unlock baseline receipts are also absent; that startup-chain issue is under investigation, not presumed harmless.
- Sweep repair RED→GREEN: a real native Isar test delays only acquisition of the readiness write transaction, then runs the production passive settlement or production sweep spend. Unchanged production code fails both assertions (expected cumulative EXP 16, actual 0; expected readiness 55, actual 60), without a timeout. Moving the readiness read/normalize/conditional write into one transaction passes both cases plus all four existing service tests. The passive case closes/reopens the database, checks cumulative totals, both reward remainders and timestamps, then verifies same-time settlement cannot award again. No schema, saveVersion, numerical rule or original save is changed. The earlier test compile error is retained separately and is not the behavioral RED.
- Startup-chain RED→GREEN: the monthly coordinator awaited `ref.read(currentSectProvider.future)` and `activeSectEventsProvider.future` while MainMenu had no UI listener for either stream. Riverpod pauses the unobserved stream before its first native result; `Future.wait` therefore never reaches progressive unlock observation. Two true Isar/coordinator tests reproduce bounded five-second timeouts, and an unoverridden real MainMenu test reproduces the absent seven receipts after dismissing the real passive recap. The monthly command now queries current Isar snapshots directly, preserving lazy initialization, pending-event filtering/sort and monthly computations. Missing-sect creation rechecks inside its transaction; a separate queued-creation RED proves why this must preserve a concurrently created sect. Final service 3/3 PASS; the real-menu test also passes after independent review strengthened its restart check: each receipt updatedAt must advance beyond the cold database value read after close/open and before mounting the second menu. First-observed identities/times remain fixed and rewards do not duplicate.
- Focused final verification: 54/54 files, 352 PASS, zero failure/skip/missing files, unchanged source hashes (22:33:47–22:34:33 CST). Whole analyze zero issues, formatting 1708 files / zero changes. The subsequent single-test restart-assertion strengthening passes 1/1 plus focused analyze; production code is unchanged. Freeze this candidate and run its shared-lock full suite and exact-SHA remote CI in parallel before native copy verification. Candidate branch push/CI follows the existing authorized scope; main remains untouched. These tests do not by themselves close the native migration or formal M0–M9 gate.
- Evidence: `/Users/a10506/Documents/Codex/2026-09-13/p2-ci-recovery/`; resumed native evidence is under `resume-native/` (`049-selection-preflight.json`, `049-selected-open-files.log`, `049-after-first-exit/`, `049-first-diff.json`, `sweep-concurrent-behavior-red.log`, `sweep-concurrent-green.log`). The failed native output is preserved; do not reuse it as a clean migration input or label the earlier CI as verification of this new fix.

## Historical September 5 run

- Authorization: initial local investigation was followed by explicit user approval to review, commit and integrate this patch, then execute frozen-version Black Wind Ridge desktop acceptance (2026-09-05 voice session).
- Branch: `codex/mainline-ci-wait-investigation-20260905`, baseline `58ae40bb`.
- Scope: test-wait hardening review and main integration, followed by M2 three-row desktop evidence on one unchanged production package. One integration full suite and push-triggered CI are authorized as necessary integration verification; no CI rerun/cancellation, production rule changes, or original-save writes.
- Budget: 30 minutes; one test-reliability gate. Formal M0–M9 remains 1/10.
- Acceptance: preserve real StageListScreen → stage flow → Isar → battle host and exact participant assertions; use controlled delayed I/O to demonstrate the wait failure and correction; verify cleanup and adjacent tests; analyze changed scope.
- Steps: inspect entry/cleanup; establish targeted baseline; controlled-delay experiment; smallest justified correction; verify and record limitations.
- Current checkpoint: reviewed candidate `e5a6029253bd15c57ffa84cf5510e25445dd6a1d` passed locked full regression 6033/6033, exit 0, zero error blocks, 16:27:36–16:39:42 CST (12m06s wall clock). Whole analyze 0 issues, format 1645/0 changed, test-contract gate PASS (expect 1 removed/1 added, cases 1 removed/2 added, 2 ledger entries). Production/test trees frozen; only this governance closeout follows. Ready for authorized no-ff integration; exact merge CI remains separate. Formal M0–M9 remains 1/10.

## Findings and evidence

- Remote failure: run `33950577057`, attempt 1, test job `101264449295`, missing `Phase0aMainlineBattleHost` plus ten-minute timeout. Attempt 2 passed. Four-document diff does not exonerate pre-existing production races or prove identical runtime environments.
- Local baseline: original file 4/4 PASS. Fresh-worktree offline dependency resolution and code generation completed before testing.
- Controlled counterexample: real-time one-second delay before the existing repository runtime-binding loader completes; the old 120-pump wait failed to find `Phase0aBattleScreen`, 0 PASS / 1 FAIL, and exited normally. This demonstrates the wait algorithm's false-negative susceptibility in the host's loading phase, **not** reproduction of the remote missing-host failure or its ten-minute hang. A database-lock experiment was stopped after blocking and excluded as root-cause evidence.
- Correction: use a five-second wall-clock deadline with real event-loop waits, retain exact participant and real production-loader assertions, preserve ordinary and delayed entry cases, and always unmount the widget tree even if host lookup or pop cleanup fails. Pop settling is separately bounded. No production code, numerical rules, saves, or workflow changes.
- Final verification: current file 6/6, adjacent all-mode consistency 6/6, stage-entry flow 10/10; targeted analyzer 0 issues; format and diff-check pass. The 6 tests include an always-missing-widget timeout case. No full suite or GUI/Windows acceptance rerun.
- Evidence directory: `/Users/a10506/Documents/Codex/2026-09-05/mainline-ci-wait/`; retained baseline, controlled RED, final, adjacent, entry-flow and analyze logs. RED file is `wuxia-mainline-ci-red-real-20260905.log`.
- Remaining boundary: remote root cause remains open; local patch is not a CI stability sign-off. Original report-only restrictions were superseded only for the explicitly authorized integration and isolated desktop acceptance. Never use this test fix to promote a milestone.

## Authorized integration and M2 desktop continuation

- Single primary gate: `P2-M2-HUMAN-G2-ACCEPTANCE`, formal 0/1; overall 1/10. Test integration is a prerequisite, not milestone progress.
- Observable budget: first checkpoint after 60 minutes of this continuation; only ongoing CI wait may extend without new implementation.
- Desktop denominator: the existing three M2 rows (stage_01_03 continuous controls; stage_01_05 Boss response/failure retry/victory; return/continue/state preservation). Each begins NOT_RUN for this frozen version. AI evidence cannot replace natural-person control-feel, audio, or Windows sign-off.
- Integration sequence: exact diff review and test-contract gate; committed candidate full regression under shared exclusive lock, analyzer and format; no-ff merge into unchanged clean main; push and inspect exact-SHA CI. Do not mutate the candidate during the suite.
- Desktop sequence: verify existing frozen package production equivalence and AOT, use isolated application/save copy, capture native dimensions and evidence files, execute manual controls from production menus, record results and remaining gaps. No unrelated weapons/tower content implementation.
- Reviewed-delay fixture starts its timer in the real I/O zone at loader invocation. Old-wait mutation is RED 0/1; exact restoration is GREEN 6/6. This strengthens the experiment without claiming the remote missing-host failure was reproduced.
- Full evidence: `build/mainline_ci_integration_20260905/full.log` and `full-result.json`; runtime logs remain ignored. Prior isolated targeted groups are not added to the full total.
- Frozen desktop package: `135041291672034f2f9504180ed40625fc30c0c4`, production trees unchanged through candidate. AOT SHA256 `7530fd2c4f4aa9539c8009d081df83a9a1280d51df34974ee8303dae31cf313a`, archive `ac87b5dcf615c82cac9a3511757f1603689a4638c36c89d30bfa039ff265657f`. Reuse signed isolated app `build/phase2-main-integration-review-20260905.lvEAmg/wuxia_main_review.app`, bundle `com.pen.wuxia.acceptance20260905`; signature verification passed. Isolated bundle identity differs from archive, so not distribution-signing acceptance.
- Desktop evidence and final exact-merge CI closeout are delivered under `/Users/a10506/Documents/Codex/2026-09-05/wuxia-m2-frozen-acceptance/`; `preflight.json` records original-save hashes, and `isolated-save-before/` is the isolated cold backup. Do not replace historical report rows with future results.
