# G2 两项阻断修复计划

## 目标

在当前 `origin/main@3e589284d17d87dd0515cce2e80b198af157a922` 上关闭两项已确认的 G2 阻断：

1. 胜利后点击下一关并处理机缘时，结果浮层不得阻断后续“继续”交互。
2. `stage_01_04` 真实战斗不得显示 `ch1_s04_blade_01` 等内部敌人 ID。

不启动 M3/M4，不改 schema/saveVersion、数值、存档结构、checkpoint 语义或其他玩法。修复完成不等同于 G2 正式签字。

## 分支

- 分支：`codex/p2-g2-blockers-20260830`
- worktree：`/Users/a10506/Desktop/Projects/挂机武侠-g2-blockers-20260830`

## 验收标准

- [x] 生产接线：真实 `enterNextStage`/待处理机缘链等待不可点击结果浮层结束后再继续，不以孤立 dialog 测试代替。
- [x] 生产接线：`stage_01_04` 敌人血条/标签读取生产显示名，不回退为内部 ID。
- [x] targeted：两项均有回归测试；精确移除对应修复后测试必须变红，并精确还原。
- [x] adjacent：运行 encounter/mainline stage flow 与 Phase 0A 战斗表现相关相邻测试。
- [x] UI/UX：常规桌面视口至少覆盖 `1280x720` 与 `1440x900`；交互测试验证鼠标点击可达，避免只证明内容存在。
- [x] 红线影响：不触及数值硬红线、三系锁死、在线=离线、反主流清单；不在 Dart 散写中文或数值，不改 YAML。
- [ ] Gate：`dart format .`、`flutter analyze`、风险匹配测试与项目 Gate 通过；如触发 `test_deletions`，按契约迁移登记处理。
- [ ] 集成态：全部改动提交，tip 以 `[READY]` 标记，worktree clean；未经用户另行授权不合并、不 push main。

## 任务切片

1. 追踪两项生产消费者与现有测试，先补能复现的 RED。
2. 串行修复机缘浮层时序并验证。
3. 串行修复敌人显示名解析并验证。
4. 执行双向破坏证红、定向/相邻测试、格式化、分析与 Gate。
5. 提交并更新本恢复点，交用户真人复验。

## 当前恢复点

- 状态：READY 候选（Gate 待执行）
- 最后完成：两项生产修复、双向破坏证红、相邻回归、静态检查、整仓格式检查和带锁全量均完成。
- 下一步：生成 `[READY]` tip 与 receipt，执行项目 Gate；Gate 通过后交用户在真实桌面流程复验。
- 已跑验证：机缘组合回归 `1/1`、生产 encounter factory `6/6`、相邻测试共 `85/85`，另有宗门招募 `8/8`，合计 `92/92`；移除机缘 await 和强制退化敌人显示名两向变异均各产生精确 `1` 个失败，精确反向补丁还原后恢复全绿；语义审计 `3/3`；`flutter analyze --no-pub lib test` 无问题；整仓格式化 `1648` 文件、`0` 处变化；带锁全量 `11:17 +5710: All tests passed!`，`[E]` 错误块 `0`。
- 阻塞项：无。
