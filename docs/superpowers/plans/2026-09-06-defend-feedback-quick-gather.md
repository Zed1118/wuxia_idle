# 守阵反馈与 Q 直接施放

- 目标：解决本次试玩中守阵目标不明、敌人攻击难以看见及失败时序疑问；Q 在按键时的鼠标位置直接施放。
- 基线：`c890208e5` clean；分支 `codex/p2-defend-feedback-quick-gather-20260906`。
- 生产路径：战斗屏键盘/鼠标 → 既有 PlayerCommand/adapter；守阵事件 → VFX/目标提示；真实主线 Host 终局回调。
- 边界：不改敌人目标策略、攻击数值、胜负条件、奖励、存档格式；不操作正在试玩的存档。当前反馈只授权这条修复路径，发布/主线集成按已有同范围授权核对。

## 验收

1. 鼠标定位后按 Q，下一次领域步进直接产生该位置的 GatherStarted，无第二次点击；冷却、暂停、失焦及持续普攻不回退。
2. 真实 stage_02_01 攻击守护物产生可见指向/受击反馈，目标名称、耐久及失败原因可理解；不改伤害与胜负判定公式；修复冷却敌人占用出手名额造成的其他敌人饥饿。
3. 核对从目标被毁到终局/结算的真实时序，只修复可复现的延迟，不凭体感修改规则。
4. 相关定向回归、analyze、1280×720/1440×900 原生目检；批末按项目要求完整回归。核对真实生产消费、红线、输入语义和残留风险后冻结候选。

## 当前恢复点

- 状态：本地候选验证完成，随本次文档提交冻结 READY；正式验收仍开放。
- 已实现：Q 按键快照直接施放；AI 冷却单位不再抢占出手名额；镖货 YAML 提示、秒制倒计时、攻击方向/目标受击反馈；真实 Host 将目标损毁原因同步带到重试正文。
- 验证：Q 四文件 64/64、AI 首轮 80/80、真实护送 Host 6/6；analyze 无问题。目标归零与失败回调同 tick，未复现固定 5 秒延迟。
- 全量首轮发现：3 个旧夹具在冷却内索取攻击、1 个新增参数默认值违反源码约定，已对齐真实冷却并改为显式传入 deltaSeconds；保留失败日志，不计全量通过。
- 原生：1280×720 可见镖货/秒数/受击方向，Q 后直接进入冷却；1440×900 同样可读，原生双视口目检完成。该检查为隔离观察角色，不替代合法角色难度验收。
- 存档：用户本轮已在旧版取得镖局护送胜利；获准重启后返回地图并正常退出，三个试玩槽已冷备份。新版已构建并按既有 bundle 更新，原生产档未操作。
- 剩余：本次候选的真人复测、既有主线集成评审与远端 CI；本任务不宣称这些已通过。
- 证据目录：`/Users/a10506/Documents/Codex/2026-09-06/wuxia-defend-quick-gather/`。

- 更新：四个首轮失败已修复，AI 扩展定向 124/124、全树静态分析通过；首轮旧候选全量主动中止，退出 0 不能记 PASS。将对以下本地检查点进行最终全量。

- 完整第二轮：`92a45117f` 跑完 6113 PASS / 1 FAIL（10m17s）。唯一剩余 Ch9 迁移夹具以默认学徒挑战二流，恢复三名敌人攻击后 83 拍败；与 Ch10 一致显式使用关卡境界后 32 拍胜，数值/胜利断言未改，Ch8/9/10 18/18 PASS。生产源码与正常试玩包未变化，将只以测试夹具修正后的冻结树再确认全量。

- 最终收口：`46136226b0e13cf38b51e8feb7045df704d467ae` 全量 6114/6114 PASS；生产源码与已签名正常试玩包一致。两个原生视口和正常导航副本核验完成，更新程序前核对三槽与冷备份哈希一致。完整审计见 `docs/audit/phase2_defend_quick_gather_2026-09-06.md`；main/远端和正式 M0–M9 状态不变。
- 重开结果：正常新版已显示启动页，用户可点击“轻触继续”并选槽 2；程序备份和最新进度均保留。CUA 坐标控制在当前窗口返回 noWindowsAvailable，故不声称已替用户选槽或进入战斗。
