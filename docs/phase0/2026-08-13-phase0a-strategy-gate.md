# Phase 0A 10-seed 策略 Gate 记录

日期：2026-08-13  
状态：`FAIL`（恢复点，不是通过证明）

## 结果

固定 seeds `20260813–20260822`，使用真实三波、固定 `1/60s` 步长：

- 弱策略（不移动、持续普攻、Q/R 可用即放）：严格口径失败 `9/10`，达到 `≥8/10`；
- 基准策略（移动、身法、Q→R、精英破招）：胜利 `7/10`，未达到 `≥8/10`；
- 综合策略 Gate：`FAIL`。

弱策略中 seed `20260817` 在 W2 而不是 W3 失败，按冻结口径不计入“W3 失败或超时”，没有放宽统计。

## 判断

无脑站桩已经被有效压制；当前阻点是基准策略的生存稳定性。失败局集中在 W3，受击累计仍偏高。下一轮只允许验证以下两种解释：

1. 基准代理尚未充分表达“普攻后摇后半身法取消、危险预判、精英破招”的熟练操作；
2. W1/W2 的不可恢复损耗使 W3 容错低于目标。

在真人内部 smoke 前不改战斗数值。若真人同样普遍在精英首次破招前死亡，再进入一次受控数值/攻击节奏回调；不得直接降低 Gate。

