# Phase 0A 10-seed 策略 Gate 记录

日期：2026-08-13  
状态：`PASS`

## 结果

固定 seeds `20260813–20260822`，使用真实三波、固定 `1/60s` 步长：

- 弱策略（不移动、持续普攻、Q/R 可用即放）：严格口径失败 `9/10`，达到 `≥8/10`；
- 基准策略首版（只按最近敌人拉扯）：胜利 `7/10`，未达到 `≥8/10`；
- 基准策略修订版（群体威胁向量、侧向拉扯、重击 commit 身法）：胜利 `10/10`；
- 综合策略 Gate：`PASS`。

弱策略中 seed `20260817` 在 W2 而不是 W3 失败，按冻结口径不计入“W3 失败或超时”，没有放宽统计。

## 判断

无脑站桩已被有效压制。基准首版失败局集中在 W3；在不修改 HP、伤害、攻击名额、冷却和波次的前提下，只改熟练操作代理后达到 10/10，证明首版失败来自代理没有表达群体拉扯，而不是必须下调战斗强度。

策略 Gate 只证明“无脑策略失败、具备身法/聚怪/清场/破招和群体拉扯的代理可稳定通过”，不能代替真人爽感、可读性和操作意愿 Gate。
