# P4.1 1.1 Q6A encounter recruit B1-B3 全闭环 closeout · 2026-05-26

> 体量 ≤80 行 · Mac+Opus xhigh 主对话 ~1.5-2h 累计(B1 ~30min + B2 ~30min + B3 ~30min)
> 范围:Q6A spec 拍板 + 主仓 commit + worktree `feat/p4_1_q6a_encounter_recruit` 起 + B1+B2+B3 一波 + closeout
> 起点:`28e4475` overnight closeout · 终点:branch `f0ba6a0` + B3 commit

## TL;DR

承接 overnight 5h 挂机会话 → 用户拍板 Q6A spec Q1-Q10 默认全 OK → 主仓 commit `98ec94c` push main → 起 sibling worktree `~/Desktop/挂机武侠.q6a` → Phase 0 六维 grep verify spec 锚点 → B1 schema+yaml + B2 wire+UI + B3 R5 测族 一波闭环。**3 commit on feat branch · 13 file + R5 测族 · 0 P0/P1 阻塞 · 1484→1492 测全过 / 0 analyze**。1.0 release readiness ~93%(P4.1 1.1 第一项实装 ✅)。

## 1. B1-B3 流水

| Batch | 内容 | commit | 实测 vs spec 估 |
|---|---|---|---|
| 主仓 spec commit | Q6A spec Q1-Q10 拍板 + commit + push main | `98ec94c` main | — |
| Phase 0 grep | 六维 verify + S1 view 沿例 3 file | (无 commit) | ~10min |
| B1 schema+yaml | encounter_def + sect_candidate_def + sect_candidates.yaml + 3 encounters + 3 events + game_repository load/红线 | `038393e` feat | ~30min vs 1.5-2h(0.33×) |
| B2 wire+UI | encounter_hook sect 分支 + _handleSectRecruit helper + confirm dialog + UiStrings + debug picker 分支 | `f0ba6a0` feat | ~30min vs 2-2.5h(0.20×) |
| B3 R5 测族 | sect_recruit_test 8 测(production 3 + e2e 1 + cap 1 + schema 红线 3)+ closeout + PROGRESS + CLAUDE | (本 commit) | ~30min vs 1.5-2.5h(0.20-0.30×) |
| **总** | **B1+B2+B3 一波闭环** | **3 commit feat · 1 commit main** | **~1.5-2h 主对话** |

## 2. 关键决策(自主)

| # | 决策 | 体例锚 / 影响 |
|---|---|---|
| 1 | spec deviation:NPC 不入 `SaveData.recruitedDiscipleIds`(spec §3 提沿 P1.1 体例)| sect NPC 不属师徒 inactive 池语义 · sect_screen listMembers 显示足够 · 1.2 character_panel 集成时回头 |
| 2 | markTriggered 延后路径分两段 wire(spec §3 R2 修)| sect 类 :76 跳过 mark · sect 类 outcomeId != accept_recruit → mark + banner / accept_recruit → 走 _handleSectRecruit + success 后才 mark |
| 3 | encounter_debug_picker.dart `markTriggered` 也加 sect 跳过分支 | debug 路径不走全套招收 wire · 让 Codex VC 可反复测同 encounter dialog UI |
| 4 | Sect lazy-init inline 不复用 `_defaultSect` private 函数 | sect_providers.dart `_defaultSect:45-53` 是 file-private 不可 import · hook 内 inline 7 行 Sect 字段构造 + 注释指向沿例 |
| 5 | bamboo NPC attribute total 25→24(agility 8→7)| §5.3 红线守 · attributeProfile.total ∈ [16, 24] |
| 6 | encounter_yaml_test baseline 54→57 | memory `feedback_nightshift_verify_count_baseline` baseline+delta 算式实战 · +3 sect_recruit delta |
| 7 | confirm dialog 紧凑布局(无 portrait)| AlertDialog 体例不显大 portrait · name + school chip + 4 属性 + lore preview 够紧凑 · 沿 RecruitmentDialog._onAccept :113-136 体例 |
| 8 | B3 测族 8 测 一文件综合(production load + e2e + schema 红线 broken loader)| 沿 sect_member_service_test setUp + seed helper + game_repository_test brokenLoader inject 两套体例 |

## 3. 速度精度锚点(实测)

| 阶段 | 估时 | 实测 | 精度 |
|---|---|---|---|
| Phase 0 grep + S1 view | ~10-15min | ~10min | 0.7-1.0× |
| B1 schema+yaml | 1.5-2h xhigh | ~30min | 0.25-0.33× |
| B2 wire+UI | 2-2.5h xhigh | ~30min | 0.20-0.25× |
| B3 R5 测族 | 1.5-2.5h xhigh | ~30min | 0.20-0.30× |
| **B1+B2+B3 总** | **5-7h xhigh** | **~1.5-2h** | **0.25-0.30×**(主对话锚 memory `feedback_opus_xhigh_interactive_duration` 历史最低 0.30× 重复印证)|

## 4. 不变量 + 挂账 + 下波

- **不变量沿用**:§5.4 红线(NPC tier=xiangYang/changLianGong 沿 P1.1 体例 · 装备攻击 ≤ 2000)· §5.3 三系锁守(`_enforceSectCandidateRedLines`)· §5.1 反主流 · §6 公式不动 · numbers.yaml 不动 · Isar schema 0.13.0 不动 · `SectMemberService` 0 改 · `RecruitmentService` 0 改 · `founder_buff_service` 0 改(留 P-C founder_buff 跨派系扩 spec 1.2)· GDD §12.2 v1.16 主体不动
- **spec deviation**(decision #1):NPC 不入 SaveData.recruitedDiscipleIds · sect_screen 成员 Tab 通过 listMembers 显示 NPC ·  character_panel 当前不显 sect NPC inactive 池(1.2 wire 时再加)
- **1.2 挂账**:① character_panel 集成 `SectMemberService.listMembers(playerSectId)` inactive 池显示 ② Q6 B stage_boss 招降(1.1+ 另起 spec)③ Q6 C 主动招收 NPC list UI(1.1+ 另起 spec)④ candidateRefs: List<String> rng pick(1.2 升 spec §0 Q9 备注)⑤ events 文案扩 ~10-20 条(P4.1 1.1 挂账 member 招收 narrative 续)⑥ founder_buff per-character 跨派系扩(P-C spec working tree · 等用户拍板)
- **下波候选**:A 启 founder_buff cross_sect spec 拍板 Q1-Q6 + B1-B3 一波(~3-5h 主对话 ~30-50min)/ B 收尾 1.0 release ~93% · 切其他子系统 / C Pen Codex 视觉验收新 sect_recruit encounter UI

---

**B1+B2+B3 一波闭环 ✅** · 3 commit feat branch · 1492 测全过 / 0 analyze · spec 已 ship `feat/p4_1_q6a_encounter_recruit` · 主仓 main 已含 Q6A spec(`98ec94c`)· branch 待 user 决定 PR/merge / 继续 founder_buff / 收尾。
