# Session Handoff 文档目录

此目录存放 `/handoff` 和 `/handoff-light` 生成的 session 交接记录。

## 文件命名规则

- 完整交接：`YYYY-MM-DD_HHMM_<主题>.md`
- 轻量交接：`YYYY-MM-DD_HHMM_light_<主题>.md`

## 使用方式

在 Claude Code 中输入 `/handoff` 或 `/handoff-light` 触发自动生成。
