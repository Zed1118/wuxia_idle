# 夜批交接 · 2026-07-30（用户睡眠期间自主推进）

**基线** `09c44486`（main 未动）· **载体** 候选分支 + draft PR · **开工四拍板**：验收线三连 + B3 评估 / 候选分支制 / 派 codex 返修 / 遇卡自主拍板并记录

## 一、四条 draft PR（早上按需人工挑合）

| PR | 分支 | 内容 | 面 |
|---|---|---|---|
| **#101** | `night-lunjian-art` | 修 Ch21 论剑后人立绘两脚不共面（164px → 10px） | 1 图 + 1 段注释 |
| **#102** | `night-b3-eval` | battle-ui B3 评估文档（零生产改动，待拍板） | 1 文档 |
| **#100** | `worktree-night-desktop-semantics` | 桌面语义：6 处过场遮罩加键盘关闭 + 2 处真控件加焦点光标 + 门禁三件套 + 真机键盘走查 | 10 生产文件 + 4 测试 |
| **#103** | `night-closeout` | 本交接 + 全主线目检报告 + PROGRESS | 纯文档 |

**建议合并顺序 #101 → #102 → #103 → #100**（面从小到大）。四条同基线、文件不重叠。

## 二、自主决策清单（逐条可推翻）

| # | 决策 | 依据 | 可推翻性 |
|---|---|---|---|
| 1 | **放弃「standee 双脚共面」第十判据**（原 3A 拍板项） | 先量存量：75 张里 30 张底部高差 >120px，论剑后人 164px 只排第 20；放大排头三张实证全是**前后脚透视深度差**（前脚更近故在画布上更低）与幻境角色下半身消散的假报。该几何量分不出「前脚在前（正确）」与「后脚悬空（错误）」，加了会一次性红掉 30 张 | **高** — 想做可改成「只对新增单人立绘生效」的白名单棘轮，但仍需人工判定 |
| 2 | **`encounter_dialog` 的 DismissLayer 改动回退** | 该弹窗内 `_ConfirmButton` 是 `PlaqueButton(autofocus: true)`，外层再 autofocus 会抢焦点 = 净回归 | 低 |
| 3 | **论剑后人取第二轮出图**（后脚抬跟）而非第一轮（双脚全平） | 第二轮同时满足脚尖共面（10px）与「重心在前脚掌」；第一轮已留底 `round1_keep.png` 可回退 | **中** — 两版都合规，属审美取舍 |
| 4 | **自行做绿边去除**而非再派 codex 一轮 | 两轮产物都带 113 个不透明绿主导像素（chroma-key 残留），线上现图为 0 = 净回归；邻域中位色去边可验证（恰 113 像素变化 / alpha 零改动 / 绿归零），比再派一轮可控 | 低 |
| 5 | **B3 推荐选项 B**（立绘侧自适应融合）而非原方案 D | 实测色温不是主导轴、杠杆在立绘侧不在背景侧 | **高** — 四选项已列全 |
| 6 | **不改 `FocusHighlightStrategy`** | 桌面端钉 `alwaysTraditional` 是一行全局改动、影响所有按钮焦点表现 | 待你定 |
| 7 | **加做全主线 105 关目检**（超出商定的三连范围） | 同一方法当晚刚在 Ch21 逮到真缺陷，且 Ch16/17/18/20/21 都挂着「未真机目检」；时长富余 | 低（纯验收，零改动） |

## 三、逮到的问题

1. **论剑后人两脚不共面** — 已修（#101）
2. **6 处过场遮罩零键盘等价物** — 已修（#100）。发布目标 Windows，键盘玩家跳不过过场
3. **2 处真控件读屏可达但键盘不可达** — 已修（#100）
4. **`autofocus: true` 的确认弹窗看不出回车落点** — **未修，待拍板**。Flutter 默认 `automatic` 策略开局是 touch 模式，`onShowFocusHighlight` 不回调，故自动聚焦的主按钮在玩家按第一个键前没有可见落点。已用 4 条断言钉住行为
5. **送关旧部新白布「动势」未达成** — 未修。白布存在且是画面最亮元素（语义达成），但画成缠杆固定布箍，非派单要求的「被风吹得笔直 / 画面里唯一在动的东西」；枪尖亦基本水平非「略压下」

## 四、白做的一段（记教训）

花约 25 分钟「发现」场景素材里的伪书法 + 伪印章，写到一半才查 `BACKLOG.md`——
**§一#11 早已登记**：2026-07-26 全库已扫、用户看过真机图后拍板「暂不改动」，
条目里明写「下次拍板可直接用上述数据，**无需重扫**」，且区分了
**朱红印章是主动要的母题**（`PUBLISHING_ART_PASS_1_0.md:215`）与多字伪题字。
我的「印章探针」检的是设计意图，不是违规。

**教训：查到异常先翻 `BACKLOG.md`，那是项目的已知问题登记册。**

## 五、验证状态

- **#100**：format 1 changed / analyze EXIT=0 / **全量 4734 pass 0 fail EXIT=0**（基线 4719 + 新增 15）/ 破坏证红 ×2（改坏 splash → 精确报站点；allowlist 塞伪条目 → 精确报失效）
- **#101**：format 0 changed / analyze EXIT=0 / targeted 4 文件逐个 EXIT=0 / 真机战斗屏复验 / 双向破坏证红（换回旧图门禁仍绿=证明它逮不到这类缺陷；截断文件则精确报 Codec 失败=证明门禁是活的）
- **#102 / #103**：零生产改动
- 存档三槽已用开工前备份还原，`cmp` 逐槽一致

## 六、worktree / 分支清理

夜批留 4 个 worktree（均在 `.claude/worktrees/`）：
`night-desktop-semantics` / `night-lunjian-art` / `night-b3-eval` / `night-closeout`。

合完对应 PR 后按既有三验删除（`is-ancestor` / `main..branch` 计数 0 / `branch --merged`），
远端分支另删。`night-b3-eval` 只用于跑截图，其分支即 #102。

## 七、下一步建议

1. **先拍 #100 合不合**（面最大，其余三条都便宜）
2. **B3 四选项拍板**（#102 已把证据与选项列全）
3. `autofocus` 焦点落点：要不要钉 `alwaysTraditional`
4. 塔 / 心魔 / 群战 / 轻功 / 断魂庄的敌立绘真机目检（本夜只覆盖主线 105 关）
