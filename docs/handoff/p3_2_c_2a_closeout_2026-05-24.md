# P3.2.C ②a inner_demon 销账 + PR rebase 修补 closeout

> 日期:2026-05-24 / 模型:Opus 4.7 high / 工时 ~45min
> 主 cwd `/Users/a10506/Desktop/挂机武侠` @ main HEAD `4af9e4f` · PR #3 MERGED `490a136`

---

## TL;DR

诊断会话(上波 3h)留下的 PR #3 用户复盘发现 3 项修补 → 本会话全销账:① PR #3 mergeable=CONFLICTING(漏报)→ rebase 解 + force-with-lease push;② memory `feedback_balance_buff_singledim_no_effect` P3.2.C 推论证伪段未回写 → 补;③ 修法 ②a 边界(production assert)→ 选不动 production,留「修法 ①」一波合解。最终 ②a fix 1 行 commit → squash merge → main · 1269 pass / 0 analyze · 1.0 整体 ~82%。

## 工作流时间线

1. **复盘**(~10min):git/PR 状态 + phase0/handoff doc 实读 → 列 3 项修补 + 2 项低优先注
2. **修补 ①** PR #3 rebase(~10min):`git rebase origin/main` 撞 PROGRESS.md b340371 冲突 → `git checkout --theirs` 取 PR 版 → rebase --continue 后续 commit 干净 → analyze + test pass → `git push --force-with-lease` → PR mergeable UNKNOWN → MERGEABLE
3. **修补 ②** memory 回写(~5min):`feedback_balance_buff_singledim_no_effect` 加「2026-05-24 P3.2.C 诊断 — 上述 P3.2.B 推论部分证伪」段 + 第 0 步排查 characterId sentinel + MEMORY.md L86 索引同步
4. **修法 ②a 执行**(~15min):`inner_demon_r5_redline_test.dart` L135 加 `character.id = -700 - slotIndex;` 1 行 → inner_demon R5 7 关全 49/1/0 → 全量 1269 pass → commit `287f0bc` push
5. **Ship**(~5min):`gh pr merge 3 --squash` → MERGED `490a136` → 主 cwd `git pull --rebase --autostash` 同步 → `git worktree remove` → PROGRESS 顶段更新 → commit `4af9e4f` push

## 关键改动

| 文件 | 改动 | 行数 |
|---|---|---|
| `test/balance/inner_demon_r5_redline_test.dart` | buildOne 加 `character.id = -700 - slotIndex` | +1 |
| `PROGRESS.md` | 顶段「诊断完成 + 修法决策」→「全闭环 ship `490a136` + ②a 销账」 | -2 net(96 → 94) |
| `~/.claude/projects/-Users-a10506/memory/feedback_balance_buff_singledim_no_effect.md` | 加 P3.2.C 证伪段 + 第 0 步排查 | +12 |
| `~/.claude/projects/-Users-a10506/memory/MEMORY.md` | L86 索引描述更新 | 0 net |

**production 0 改** ✅(诊断 doc 修法 ②a「不动 production assert」的承诺)。

## R5.1 实测结果

```
qiMeng   stage_inner_demon_01 (+10%): leftWins=49 rightWins=1 draws=0
ruMen    stage_inner_demon_02 (+12%): leftWins=49 rightWins=1 draws=0
shuLian  stage_inner_demon_03 (+14%): leftWins=49 rightWins=1 draws=0
jingTong stage_inner_demon_04 (+16%): leftWins=49 rightWins=1 draws=0
yuanShu  stage_inner_demon_05 (+18%): leftWins=49 rightWins=1 draws=0
huaJing  stage_inner_demon_06 (+20%): leftWins=49 rightWins=1 draws=0
dengFeng stage_inner_demon_07 (+40%): leftWins=49 rightWins=1 draws=0
```

**消解伪现象**:Batch 2.5.A 写的「+20% vs +40% 同 3/0/47 分布」是 test bug 假象(0 号 carrier 模式),非「单维度 buff 无效」。+40% 修后正常区分(同 49/1/0 因为 mirror cap §5.4 把 attack 锁在 2000 ≤ 玩家)。

## 复盘修补的教训(本批沉淀)

- **PR 状态必须 gh pr view 实测**:【环境快照】只写「等用户审 squash merge」漏掉 mergeable=CONFLICTING,**手工抄状态会漏关键字段**
- **memory 推论被后续诊断证伪时,主动回写「证伪段」+ 索引更新**(不要让 memory 停在已知错的推论上误导未来)
- **复盘清单要把诊断 doc 与执行清单逐字段对照**:doc 写「7 test buildPlayerTeam 加 ..id + production assert」,但执行清单只写「test 一处」,assert 缺失被发现

## 不变量沿用

- 数值红线 §5.4/§5.3/§5.5/§6 公式 0 改
- BattleStrategy 接口 3 method 不动
- production 0 改(Character.id Isar.autoIncrement 路径不变)
- 1269 pass / 1 skip / 0 analyze

## 挂账下波(合并)

- **mass_battle stage_03+ 数值真挂账** + **ch4-6 跨阶 boss 数值挂账** + **production assert 防 sentinel** → 三者合并到「修法 ① 数值大改」一波解(需 xhigh ~3-4h · 涉及 stages.yaml + ch4-6 跨阶 boss enemy hp/atk 平衡 + production `_findById` 加 assert 同 team characterId 唯一)
- 其他 5 test(mass_battle / ch4-6 跨阶 boss R5)维持 bug 假象,production 0 影响

**会话清理建议**:`建议清理` — P3.2 子系统已完整 ship,下波是新子系统(修法 ① 数值大改 / P2.3 飞升 / Pen 验收 / MJ 派单)。
