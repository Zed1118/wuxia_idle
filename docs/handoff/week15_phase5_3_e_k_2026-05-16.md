# Phase 5 #3 第 5 批 — E + K 任务 lib/ui/debug/ + test/services/ 收尾

**日期**:2026-05-16
**模型**:Opus 4.7 high(sonnet 2-3h 预算,opus 接力未降档)
**会话密度**:1 会话 / 2 commit / 14 文件改动(11 rename + 3 modified)/ 26 行 import 改 / 全程零回退
**HEAD**:`14d2352`(已 push origin/main)
**测试**:653/653 + analyze 0 issues(2 次 verify,机械工作一气呵成)

---

## 1. 一句话结论

`lib/ui/debug/` + `test/services/` **两目录消失**。E 任务 3 文件迁 `lib/features/debug/presentation/` 完成 debug feature application + presentation 闭环;K 任务 8 test 文件迁各 `test/features/<X>/application/` 完成 test 树 100% feature-first。Phase 5 #3 重构第 5 批 E + K 双任务一波收口,**lib/ + test/ 树目录结构 100% feature-first 全面闭环**。

---

## 2. 会话密度统计

| 指标 | 值 |
|---|---|
| commit 数 | 2(`9288b99` E / `14d2352` K) |
| 文件 rename | 11(E 3 + K 8) |
| 文件 modified | 3(E:main_menu + phase2_test_menu_test + t17_scenarios) |
| import 改动 | 26 行(E 22 self + 3 consumer + 1 注释;K 0) |
| verify 节点 | 2(E 后 analyze + test;K 后 test) |
| 回退次数 | **0** |
| 用户介入 | 1 次(3 选项 AskUserQuestion 一次性收口 D/E/K 范围拍板) |

---

## 3. 用户拍板:3 范围决策一次性

### 3.1 D 任务「service interface 抽离」

**Reality check 暴露**:`grep -E "abstract class .*Service"` lib/ 全仓 **0 命中**。当前代码没有任何 abstract Service interface,Phase 5 #2 闭关 `_FakeSeclusionService implements SeclusionService` 用的是 Dart「implements 具体类」语法把具体类当 interface 用,功能上够用。

**3 选项**:
- **A(推荐 ⭐)**:跳过 D — 0 abstract Service 但 fake 模式已成功,无现实痛点,抽 interface 是 over-engineering 嫌疑,等真有 fake service 写不出的痛点再按需抽
- B:抽 SeclusionService 1 个示范模板
- C:抽 6 个核心 service(battle_engine / cultivation / dispel / mainline / tower / encounter)

**用户拍板 A**:跳过 D。本批从「D + E + K」缩为「E + K」。

### 3.2 E 任务 lib/shared/ + lib/ui/ 范围

**Reality check 暴露**:lib/shared/ 是**空目录**。lib/ui/ 还有 effects(3 引)/ narrative(3 引)/ theme(33 引)/ strings(28 引)/ main_menu(2 引)5 项。

**3 选项**:
- **A(推荐 ⭐)**:严格只迁 ui/debug/ 3 文件 → features/debug/presentation/,其他 lib/ui/ 完全不动,lib/shared/ 暂留空
- B:ui/debug/ + lib/ui/theme + strings.dart 一并迁 lib/shared/
- C:整 lib/ui/ 归化 lib/shared/(lib/ui/ 消失)

**用户拍板 A**:严格只迁 ui/debug/。其他 lib/ui/ 等真有 cross-feature UI 抽出需求再迁。

### 3.3 K 任务 2 个开放归属

**Reality check 暴露**:8 test 中 6 个归属明确,2 个开放:
- phase2_scenarios_test:P1-P4 调试场景纯数值验收(脱 Isar),归 debug feature?integration?
- skill_usage_persist_test:Isar `@embedded List<SkillUsageEntry>` fixed-length list 反序列化回归,归 core/domain?data/persistence?

**3 选项**:
- **A(推荐 ⭐)**:phase2_scenarios → `test/features/debug/application/`(数值场景验收属调试范畴) + skill_usage_persist → `test/core/domain/`(本质是 domain model 的 Isar 持久化回归)
- B:phase2_scenarios → test/integration/,skill_usage_persist → test/data/persistence/(新建 2 目录)
- C:2 个开放的全留 test/services/

**用户拍板 A**:推荐配置。

---

## 4. 实施步骤

### 4.1 E-1:E 任务 lib/ui/debug/ 3 文件迁(commit `9288b99`)

**目录 + git mv**:
- `mkdir -p lib/features/debug/presentation`
- 3 × `git mv lib/ui/debug/<file>.dart lib/features/debug/presentation/`
- `rmdir lib/ui/debug`(空目录清理,沿用 J 任务沉淀)

**深度调整**:lib/ui/debug/(2 层)→ lib/features/debug/presentation/(3 层),深度规则混合(features/ 内 peer 去 `features/` 段 / 跨 features/ 加深 +1):

| 文件 | self imports 改 | 路径变化 |
|---|---|---|
| battle_test_menu.dart | 7 | features/ peer(battle/domain, battle/presentation 去 features/ 段);core/data/ui 加深 +1 |
| encounter_debug_picker.dart | 8 | features/ peer(encounter/× 4 去 features/ 段);data/ui 加深 +1 |
| phase2_test_menu.dart | 7 | 同 feature 内 application:`../application/phase2_seed_service.dart`;features/ peer 3 处 + data/ui 加深 +1 |

**Consumer 切换**:3 处
- `lib/ui/main_menu.dart` × 2(battle_test_menu + phase2_test_menu import)
- `test/ui/main_menu/phase2_test_menu_test.dart` × 1(absolute import)

**注释路径同步**:1 处
- `test/combat/t17_scenarios_test.dart:12` 注释 `lib/ui/debug/battle_test_menu.dart` → `lib/features/debug/presentation/battle_test_menu.dart`

**Verify**:flutter analyze 0 issues + flutter test 653/653 一气呵成。

**Commit**:`9288b99` 一次性 6 files / 26 ins / 26 del。

### 4.2 K-1:K 任务 test/services/ 8 文件迁(commit `14d2352`)

**目录 + git mv**:
- `mkdir -p test/features/{battle,cultivation,dispel,debug}/application test/core/domain`(部分已存在不影响)
- 8 × `git mv test/services/<file>.dart test/features/<X>/application/`
- `rmdir test/services`

**归属映射**:
| test 文件 | 目标位置 |
|---|---|
| battle_resolution_test.dart | test/features/battle/application/ |
| master_disciple_battle_test.dart | test/features/battle/application/ |
| stage_battle_setup_test.dart | test/features/battle/application/ |
| cultivation_service_test.dart | test/features/cultivation/application/ |
| dispel_persist_test.dart | test/features/dispel/application/ |
| dispel_service_test.dart | test/features/dispel/application/ |
| phase2_scenarios_test.dart | test/features/debug/application/ |
| skill_usage_persist_test.dart | test/core/domain/ |

**Self / Consumer**:0 改动
- test 文件全用 absolute `package:wuxia_idle/...` import,**深度无关**,无 self 改
- test 是叶节点,**0 consumer**,无下游改

**Verify**:flutter test 653/653(analyze 与 K 无关跳过)。

**Commit**:`14d2352` 一次性 8 files / 0 ins / 0 del(纯 rename)。

---

## 5. 数字实测复审(memory feedback_closeout_numbers_grep)

| 数字 | 实测值 | 来源 |
|---|---|---|
| commit 数 | 2 | `git log --oneline aaa0f5a..HEAD` |
| 文件 rename | 11 | E 3 + K 8(`git show --stat 9288b99 14d2352` rename 段) |
| 文件 modified(非 rename) | 3 | E commit:main_menu + phase2_test_menu_test + t17_scenarios |
| E self imports 改 | 22 | 7(battle) + 8(encounter) + 7(phase2) — grep "^import" 各文件 diff |
| E consumer imports 改 | 3 | main_menu × 2 + phase2_test_menu_test × 1 |
| E 注释路径改 | 1 | t17_scenarios_test.dart:12 |
| E 总 import 改 | 26 | 22 + 3 + 1,与 26 ins/26 del 对得上 |
| K imports 改 | 0 | 纯 rename,test 用 absolute import 不需改 |
| 全测 | 653/653 | flutter test 最后行 |
| analyze | 0 issues | flutter analyze 最后行 |
| 回退次数 | 0 | git log 2 commit 直线 |
| lib/ui/ 顶层项数 | 5 | effects/main_menu.dart/narrative/strings.dart/theme,debug/ 消失 |
| test/ 顶层项数 | 6 | combat/core/data/features/ui/widget_test.dart,services/ 消失 |
| features/debug/ 文件数 | 4 | application/ 1(phase2_seed_service)+ presentation/ 3(3 debug UI) |

**brief 修正**:开干前估算「23 行 self imports」实测 22(7+8+7),memory feedback_closeout_numbers_grep 教训复证一次 — 估算时数行数容易差 1,grep 实测才稳。

---

## 6. 下次开局必读

### 6.1 状态快照

- HEAD `14d2352`(E commit `9288b99` + K commit `14d2352` 已 push origin/main)
- tag `v0.5.3-w15-final` 保留(W15 锚点)
- 653/653 + analyze 0 issues
- **lib/ui/debug/ 目录消失**(lib/ui/ 顶层 5 项:effects / main_menu.dart / narrative / strings.dart / theme)
- **test/services/ 目录消失**(test/ 顶层 6 项:combat / core / data / features / ui / widget_test.dart)
- features/debug/ 完整化:application/ 1 文件(phase2_seed_service)+ presentation/ 3 文件(battle_test_menu / encounter_debug_picker / phase2_test_menu)
- §12 待决 2 条不变(#7 流派 extra_effect / #10 师承遗物规则)
- Phase 5 主战场 11/14 不变(本批是边角整理 + test 树规整,不算新 feature 落地)

### 6.2 开局动作

1. 读 PROGRESS.md「当前阶段」+「下一步」+「挂账事项」
2. 读 本 closeout §6 下次开局必读
3. `git pull --rebase --autostash` 看 drift(本会话已 push,正常无)
4. 选读 memory:本批未新增,沿用 J 沉淀(`feedback_riverpod_codegen_provider_split` + `feedback_git_mv_perl_stage`)

### 6.3 下波候选

E + K 完成后剩 3 候选:

| # | 任务 | 模型 | 时长 | 备注 |
|---|---|---|---|---|
| **H** ⭐ | techniqueLearnPoints / internalForcePoints 消费层接入 | **opus** | 2-3h | #30 新维度落 Character/Technique,独立功能,Phase 5 重构告段后的第一个新业务 |
| F | #34 stage drop 视觉验收 Pen 环境改善 | Codex 派单 | 1h | 老挂账,需借鼠标 |
| G | Pen-only T64 test fail 排查 | sonnet | 30min | 老挂账,短平快 |

**Phase 5 #3 重构整体收口完成**(B/C/I/J/E/K 6 任务连环 / 6 commit:`afcfd87`→`14d2352`)。lib/services/ + lib/ui/debug/ + test/services/ 三个旧布局目录已全部消失,lib/ 树 100% feature-first(顶层:core / data / features / providers / shared / ui / utils + main.dart),test 树 100% feature-first。

**推荐起手**:H(opus 2-3h)— #30 新维度业务功能落角色与心法消费层,Phase 5 主战场推进。F/G 是老挂账可与 H 并行(F 需 Codex 派单,G 是 30min 短平快可顺手 H 之前热身)。

### 6.4 硬约束沿用

延续 C/I/J 任务硬约束清单(详 `week15_phase5_3_j_lib_services_cleanup_2026-05-16.md` §6.4)。本批新经验:

- **service interface 抽离不是必须做的事**:Dart `implements 具体类` 已经够 fake mock 用,0 abstract Service 状态下抽 interface 是 over-engineering。等真有 fake service 写不出来的痛点(比如 service 依赖太复杂 fake 不动)再按需抽,不预先大规模做。
- **test 文件迁移最简**:test 用 absolute `package:wuxia_idle/...` import,深度无关,纯 git mv + verify 即可,不需要 perl 改 imports 也不需要全仓 sed,本批 K 任务 8 文件 0 ins / 0 del 是这一规律的极端实例。
- **brief 数字估算 ≠ 实测**:开干前估算 self imports 22 vs 23 容易差 1,要 grep 实测才稳(本批 22 实测纠正 brief 估算 23),memory feedback_closeout_numbers_grep 再复证一次。
