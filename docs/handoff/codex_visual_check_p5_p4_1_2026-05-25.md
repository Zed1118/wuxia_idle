# Codex visual check P5.0 + P4.1 · 2026-05-25

> Status: BLOCKED before build / GUI launch.
> Scope requested: P4.1 §12.2 sect management + P5.0 onboarding production seed, main menu, sect_screen, fresh-start idempotency, battle e2e.

## Summary

Visual validation was not executed because the Pen Windows checkout could not be brought to the required release HEAD `b375e40`.

Local workspace was restored to a clean pre-existing state after failed sync attempts:
- local branch: `main`
- local HEAD restored: `6a6c21a`
- remote HEAD observed: `b375e40`
- final status: clean worktree, `main...origin/main [behind 175]`

No screenshots were captured. No Flutter clean/build/run was attempted, because running visual acceptance on stale `6a6c21a` would not validate P4.1/P5.0 release readiness.

## Required Reading Completed

Read from fetched remote objects where possible:
- `PROGRESS.md` top section at `origin/main`
- `docs/handoff/1_0_release_audit_v2_2026-05-25.md`
- `docs/handoff/session_closeout_2026-05-25_p5_audit_v2_full.md`
- `CLAUDE.md` §5 / §12 at `origin/main`

Key points confirmed:
- audit v2 reports 1484 tests passing and 0 analyze issues
- remaining release blocker is Pen visual validation
- P5.0 onboarding production seed is expected to make fresh launch idempotent and battle e2e safe
- P4.1 sect screen is expected to expose 4 tabs: current events, history, members, territories
- CLAUDE §12.1 currently has no unresolved decision items

## Sync Failure Details

Initial `git pull` and normal fetch attempts hung or timed out. Remote HEAD was confirmed by `git ls-remote origin HEAD` as:

```text
b375e40c9b83ccdb88e8b36b3fec1662232f5ec3 HEAD
```

The local repo had partial-clone/promisor state reintroduced during filtered fetch attempts:

```text
remote.origin.promisor=true
remote.origin.partialclonefilter=blob:none
```

Those local promisor/filter entries were removed. A full `git fetch --refetch --no-tags origin main` was then attempted with SSH and LFS smudge disabled, but it still timed out after 15 minutes while `index-pack` was processing a pack of 7840 objects.

Subsequent targeted object fetches showed the object database was incomplete. Small blobs such as `PROGRESS.md` and the audit docs could be fetched individually, but checkout/reset to `origin/main` failed because many blobs were still missing. The first blocking large blob identified was:

```text
assets/enemies/qingshan_main.png
blob ed448513cc99d202c041a80d427cf8cc36a9d31d
```

Single-object fetch for that blob repeatedly reached:

```text
remote: Enumerating objects: 1, done.
remote: Counting objects: 100% (1/1), done.
git unpack-objects --pack_header=2,1
```

and then hung without materializing the blob.

An attempted sparse checkout excluding that one file still failed because many non-image blobs were also missing, including `PROGRESS.md`, `pubspec.yaml`, multiple `lib/**` files, `data/**` files, and tests. The reset ended with:

```text
fatal: Could not reset index file to revision 'origin/main'.
```

The workspace was then restored with:

```text
git sparse-checkout disable
git reset --hard HEAD
```

and failed-reset untracked files generated by the attempted checkout were removed. Final status returned to clean.

## Build / Visual Result

Not run.

Reason: required source state `b375e40` was not available in the working tree. Building or screenshotting local `6a6c21a` would miss the P4.1/P5.0 changes under review.

## Recommendation

Mac side should re-seed or repair the Pen checkout before rerunning visual validation. The fastest likely path is the known Pen partial-clone recovery sequence, but with a successful full object refetch before reset:

```text
git config --unset-all remote.origin.promisor
git config --unset-all remote.origin.partialclonefilter
git fetch --refetch origin
git reset --hard origin/main
```

If GitHub SSH object transfer continues to hang on this machine, clone a fresh full repository outside the damaged partial object database, then rerun the visual pass from `b375e40`.

---

# Resume Success · 2026-05-26

> Status: PASS after Mac-side reverse tar pipe repaired the Pen checkout.
> Validation target: `b375e40c9b83ccdb88e8b36b3fec1662232f5ec3`.

## Resume Summary

The visual validation was rerun successfully after the repository was restored to the requested HEAD and the missing blobs were present locally.

Build and launch path:
- `flutter clean` completed. Flutter emitted a non-fatal tag fetch EOF message, but returned exit code 0 and removed `build/` plus `.dart_tool/`.
- `dart run build_runner build --delete-conflicting-outputs` completed, writing 82 generated outputs.
- `flutter build windows --debug` completed and produced `build\windows\x64\runner\Debug\wuxia_idle.exe`.
- Scheduled-task GUI launch failed with access denied, as expected on this Pen session.
- Fallback `Start-Process .\build\windows\x64\runner\Debug\wuxia_idle.exe` launched the app successfully.

Important runtime note:
- The prompt requested deleting `%LOCALAPPDATA%\com.example.wuxia_idle\`.
- The app code currently opens Isar under `getApplicationDocumentsDirectory()`, which resolves here to `C:\Users\Administrator\Documents\wuxia_save_slot1.isar`.
- I first observed that deleting only `%LOCALAPPDATA%` left prior stage progress in place, then stopped the app and cleaned both the requested LocalAppData folder and the actual Documents Isar file before capturing the final 01-08 evidence set.

## Screenshot Evidence

Stored under `docs/screenshots/p5_p4_1_visual_check_2026-05-25/`:

| # | File | Evidence |
|---|---|---|
| 01 | `01_fresh_launch_clean_isar.png` | Clean Isar launch reaches `江湖见闻` with `直入江湖`, no crash / no seed error. |
| 02 | `02_main_menu_top_seeded.png` | Main menu opens after production seed; upper entry set visible. |
| 03 | `03_main_menu_lower_sect_visible.png` | Lower main menu entries visible, including `门派事务`; locked states render normally. |
| 04 | `04_sect_four_tabs_current_events.png` | `门派事务` screen loads with 4 tabs: 当前事件 / 历史记录 / 成员 / 领地. |
| 05 | `05_sect_members_tab_clean_seed.png` | Members tab loads and shows clean-seed empty state without layout breakage. |
| 06 | `06_sect_territories_tab_clean_seed.png` | Territories tab loads territory cards and `占领` actions. |
| 07 | `07_mainline_chapters_clean_seed.png` | Mainline chapter screen opens from clean seeded state; chapter locks render correctly. |
| 08 | `08_battle_e2e_clean_seed.png` | Mainline battle e2e runs and resolves to victory, proving P5.0 seed supports battle setup. |

## Visual Findings

PASS:
- Fresh launch no longer crashes on missing production seed.
- Main menu renders normally and exposes P4.1/P5.0 relevant entries.
- `门派事务` route loads without exception.
- P4.1 sect screen 4-tab structure is present.
- Members and territory views render without overflow at 1280 x 720.
- Territory cards show defense tier, neutral ownership, descriptive text, and action buttons.
- Mainline battle starts from clean seeded state and completes, so the prior `先跑 P1 种子` failure path is visually cleared.

WARN:
- At 1280 x 720, the app window captured a thin strip of desktop at the far right edge in several screenshots. The app content itself is coherent and usable; this appears to be a Pen desktop/window capture framing issue, not an in-app layout failure.
- Clean-seed `成员` tab currently shows `尚无门派成员`. This matches the observed seeded data path in this run; P4.1 member management UI itself loads correctly.

## Final State

No source code or data files were edited for this validation. Build-generated platform registrant changes were reverted after the build. The only intentional outputs are:
- this closeout document
- the screenshot directory above

Pre-existing CRLF-only modified files remain visible in `.nightshift/*.verify.sh` and `tools/pen_screen/pen_screen.sh`, per handoff instruction to ignore them.

