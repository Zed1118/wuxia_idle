# 派单包 C2 · numbers.yaml 零引用 key 接线批（用户 2026-09-19 拍板「按推荐执行」）

- 执行端：codex（`codex exec`，默认模型）
- worktree：`/Users/a10506/Codex/2026-09-19/c2-wiring/wt`（分支 `codex/c2-wiring-20260919`，基线 `36781fbbf`；已预热：libisar.dylib / pub get / build_runner）
- 派单基线（Gate 用）：`base_sha = 36781fbbf`
- 拍板依据：`docs/audit/numbers_unused_keys_round2_2026-09-19.md`（下称「对照单」）§2 的 C 段 `success_formula`、E 段 `bonus_per_event_*`、F 段 `time_range`、H 段 11 叶——四项「接线」。**用户已拍板，不重新论证**。并行的 qoder C1 单在同一基线上删 A/B/D/G 段 key，与本单改动块不相邻；本单**不得触碰**那些段。

## 0. 总则（硬约束）

- **语言**：commit message、代码注释、文档一律简体中文；commit message 中文动宾；改 `numbers.yaml` 结构的 commit 标题前加 `[schema]`。
- **可写范围（白名单，精确路径）**：
  - `data/numbers.yaml`（只动 `equipment.enhancement.success_curve` 末段、`retreat.time_of_day_bonus` 头注、`validation_examples` 内两处注释数字；**不删任何 key、不改任何生效数值**）
  - `lib/data/numbers_config.dart`
  - `lib/features/seclusion/application/seclusion_service.dart`
  - `lib/data/validation/encounter_red_lines_validator.dart`
  - `lib/data/game_repository.dart`（仅当需要把奇遇 attributeDelta 校验挂到 `loadAllDefs` 时）
  - `test/features/equipment/application/enhancement_service_test.dart`
  - `test/data/school_counter_v14_config_test.dart`
  - `test/features/seclusion/application/seclusion_time_of_day_test.dart`（新建）
  - `test/data/encounter_attribute_delta_red_line_test.dart`（新建）
  - `test/combat/damage_calculator_test.dart`
  - `test/support/numbers_fixture_patch.dart`（新建，仅当需要为测试构造带/缺新字段的 NumbersConfig fixture）
  - `docs/superpowers/plans/2026-09-19-codex-c2-wiring.md`（新建，恢复点）
  - `docs/dispatch/reports/2026-09-19_codex_C2_receipt.yaml`（新建，收据）
  - **其余一律禁改**，特别是 `GDD.md`、`PROGRESS.md`、`BACKLOG.md`、`CLAUDE.md`、`AGENTS.md`、`data_schema.md`、`pubspec.yaml`、`lib/shared/strings.dart`、`data/skills.yaml`、`data/encounters.yaml`、`tools/**`、`docs/audit/**`。协调者以 `git diff --name-only` 机器判定：出现白名单外路径整单 FAIL。若确需改白名单外文件（例如某测试 fixture 手写了 `enhancement` 块而缺新字段），**停下打 `[BLOCKED]`** 写明路径与原因，不要自行扩围。
- **git 边界**：只在自己的 worktree 提交；禁 push / merge / rebase / revert；禁碰 `main` 与 `codex/p2-player-flow-20260910`。
- **环境边界**：禁安装任何软件。
- **不碰真实存档**（`~/Library/Containers/com.pen.wuxia.wuxiaIdle/` 只读），不启动游戏 GUI。
- **就绪标记**：最后实质 commit S 带 `[READY]`/`[BLOCKED]` 前缀；收据放独立包装 commit R（`R^ == S`，只含收据），R 消息以同样标记开头。
- **数值零变更**：本单只把「已写死在 Dart 的常量」搬到 yaml 或让 yaml 里已有的值真被读取；每一步接线前后的运行时行为必须逐位相同（用既有测试 + 新增断言证）。
- **TDD + 双向破坏证红**：每目标先写红测再实装；实装 commit 后做两向 mutation（① 删掉新读取逻辑改回常量 → 对应断言必红；② 把 yaml 值临时改错 → 对应断言必红），还原后重跑绿，结果写进收据 `break_red`（四目标各写一组）。

## 1. 目标清单（每目标收口 commit 一次，顺序执行）

### 目标 1：`equipment.enhancement` +20–49 段成功率公式常量入 yaml（`[schema]`）

- 现状：`lib/data/numbers_config.dart:1068-1072 _fallbackFormula` 写死 `0.50 - 0.02 * (targetLevel - 19)`、下限 `0.30`；yaml `:765` 只有 `success_formula` 文案串，`success_rate: null` 触发回退（`:1016-1020`）。
- 做法：在 `equipment.enhancement` 下新增子块（键名自定但须 snake_case，例如 `high_level_success: {base_rate: 0.50, step_per_level: 0.02, anchor_level: 19, floor_rate: 0.30}`），`EnhancementConfig.fromYaml` **必填解析**（缺失 fail-fast 抛错，不给默认值），`_fallbackFormula` 改读这四个字段；`success_formula:` 那一行改为注释（保留公式文案作说明）。
- 测试：`enhancement_service_test.dart` 既有 `successRateFor` 断言（20 / 29 / 49 等）必须原值通过；新增「缺 `high_level_success` 块 → 抛错」与「改 `floor_rate` 为 0.4 → `successRateFor(49)` 变 0.4」两条。

### 目标 2：奇遇 `attributeDelta` ∈ [`bonus_per_event_min`, `bonus_per_event_max`] 加载期红线

- 现状：`character.adventure_attribute_bonus.bonus_per_event_min/max`（1 / 3）零读取；真值是 `data/encounters.yaml` 每条 `attributeBonus` outcome 的 `attributeDelta`（`lib/data/defs/encounter_def.dart:55,81`，实值只有 1 与 2，92 处）。
- 做法：`NumbersConfig` 解析这两个字段（必填、`min ≥ 1`、`min ≤ max`）；在 `lib/data/validation/encounter_red_lines_validator.dart`（或其被 `GameRepository.loadAllDefs` 调用的同层）加一条：任一 `attributeBonus` outcome 的 `attributeDelta` 不在 `[min, max]` → 启动期抛错，错误信息含 encounter id 与 delta。生产数据 92 处必须全过。
- 测试：新建 `test/data/encounter_attribute_delta_red_line_test.dart`：生产数据全过 1 条；合成 fixture `attributeDelta: 5` 抛错 1 条；`attributeDelta: 0` 抛错 1 条。**不得**改 `data/encounters.yaml`。

### 目标 3：`retreat.time_of_day_bonus[].time_range` 真读取

- 现状：`numbers_config.dart:2924` 只读 multiplier/effect/target_attribute/applies_to_school；时段写死 `seclusion_service.dart:797-807`（`_isZiShi: h == 23 || h == 0`、`_isZhengWu: h == 11 || h == 12`）。
- 做法：解析 `time_range: ["HH:MM", "HH:MM"]` 为小时区间（`period: other` 的 `null` 允许），左闭右开、支持跨午夜（`23:00`→`01:00` = h ∈ {23, 0}）；`_isZiShi/_isZhengWu` 改为按 `period` 从 config 取区间判定，删除写死的小时常量；yaml `:1385-1389` 那段「time_range 为固定时辰文档锚 / 不解析」头注改写为「已由 seclusion_service 经 NumbersConfig 读取（2026-09-19）」。**当前 yaml 值不改**，故运行时行为逐位相同。
- 测试：新建 `test/features/seclusion/application/seclusion_time_of_day_test.dart`：边界 22:59 / 23:00 / 00:59 / 01:00 / 10:59 / 11:00 / 12:59 / 13:00 八点；`time_range` 缺失或格式错 → 抛错；`school_counter_v14_config_test.dart` 既有断言原值通过。

### 目标 4：五战例测试数据化（test-only）

- 现状：`test/combat/damage_calculator_test.dart:36-75` 五战例手写 ctx 与期望值，`numbers.yaml validation_examples` 只在注释里被提及；`cultivation_multiplier` / `school_counter`（×5）与 `example_c.attacker.realm_diff_modifier` 共 11 叶零读取。
- 做法：测试改为从 `GameRepository.instance.numbers.raw['validation_examples']`（`numbers_config.dart:291` 保留 raw）逐例读取数值字段（`internal_force` / `equipment_attack` / `skill_multiplier` / `cultivation_multiplier` / `school_counter` / `critical` / `defense_rate` / `realm_diff_modifier`）构造 ctx，境界仍可按现测手写；期望值保留现测已验证的数字（826 / 4879 / 1995 / …），**不得**为凑 yaml 注释改期望值。`lib/` 零改动。
- 允许的 yaml 注释订正：`validation_examples` 内 `calculated_damage` / `expected_outcome` 注释里与测试实证不符的两处数字（B 4889→4879、C 1972→1995）改正；其他文案不动。
- 验收：`python3 tools/audit/numbers_key_usage.py --baseline 36781fbbf --format json` 中这 11 叶 `verdict` 不再是 `零引用`（预期变「仅测试消费」）。

## 2. 验证（全部完成后）

- `flutter analyze --no-pub lib test tool` 0 issue；`dart format --output=none --set-exit-if-changed lib test docs` 0 changed（写完 dart 必 `dart format`）。
- targeted 逐文件确认 `All tests passed!`：上述 4 目标涉及的 6 个测试文件 + `test/data/`。
- 全量 `flutter test --no-pub`，收据写 reporter 末行原文与 `[E]` 块数。
- 计数：`numbers_key_usage.py --baseline 36781fbbf` 零引用叶数 = **41**（59 − 18；C1 单未合并时的独立基线口径），把 JSON 存 `/Users/a10506/Codex/2026-09-19/c2-wiring/usage_after.json`。不等则逐叶解释，不能解释就 `[BLOCKED]`。

## 3. 禁止的修法

- 禁止改任何生效数值（0.50/0.02/19/0.30、1/3、四个时刻、五战例数字）。
- 禁止给新字段默认值兜底（必填 fail-fast，否则 yaml 缺字段时又回到「写死」）。
- 禁止用 `numbers.raw` 在 `lib/` 里读配置（`raw` 只给测试用；`lib/` 走强类型字段）。
- 禁止在测试里手设字段绕开生产路径（memory 判据：「破坏那行生产代码，这条断言必然红吗」）。
- 禁止删 A/B/D/G 段任何 key（那是 C1 单）。

## 4. 收据与恢复点

- 按 `~/.claude/skills/afk/scripts/receipt.schema.md` 产 `docs/dispatch/reports/2026-09-19_codex_C2_receipt.yaml`：`changed_files` 列全部实际改动；三条 last_line 真跑原文（不得 NOT_RUN）；`break_red` 四组两向结果（失败测试名 + 失败条数）。
- 恢复点 `docs/superpowers/plans/2026-09-19-codex-c2-wiring.md`：状态 / 最后完成 / 下一步 / 已跑验证 / 阻塞项。

## 5. [BLOCKED] 出口条件

- 需要改白名单外文件（含测试 fixture 手写 numbers 块缺新字段）。
- 生产 `data/encounters.yaml` 有 `attributeDelta` 越出 [1,3]（说明数据与 GDD §4.1 冲突，需用户拍板，不得改数据也不得放宽区间）。
- 任一目标两向证红有一向不红（说明断言无牙），修不好就停。

## 6. 验收方式（协调者怎么查）

- `gate.sh <worktree> 36781fbbf <S> --whitelist-from <本派单包> --wrap-tip <R>`。
- 协调者复跑：6 个 targeted 测试文件逐个绿；`git grep -n -E 'h == 23|h == 11|0\.50 - 0\.02' -- lib` 为 0 命中；`numbers_key_usage.py` 对撞 41；抽两向 mutation 各复做一次。
