# 派单包 E · 黄金文件搬出 ignore 目录 + 审计脚本已删段空登记清理（用户 2026-09-20 拍板「2做 6做」）

- 执行端：codex（`codex exec`，默认模型）
- worktree：`/Users/a10506/Codex/2026-09-20/golden-relocate/wt`（分支 `codex/golden-relocate-20260920`，基线 `b99cfa72d`；已预热：libisar.dylib / pub get / build_runner）
- 派单基线（Gate 用）：`base_sha = b99cfa72d`
- 背景：D 单（`docs/dispatch/reports/2026-09-19_afk_report.md` 决策 1）查明 `test/tools/output/` 下 6 个跟踪文件是**黄金文件**（三条 diagnostic 测试读它们比对），而该目录被 `.gitignore:89` 忽略，「清产物」时极易误删（今日已实录一次假绿）。拍板：搬到不被 ignore 的 `test/tools/golden/`。另顺手清 `tools/audit/numbers_key_usage.py` 对已删段 `combat.final_damage_formula.` 的 3 处空登记。**用户已拍板，不重新论证**。

## 0. 总则（硬约束）

- **语言**：commit message、代码注释、文档一律简体中文；commit message 中文动宾。
- **可写范围（白名单，精确路径）**：
  - `test/tools/output/phase0a_idle_island_parity_diagnostic.csv`
  - `test/tools/output/phase0a_idle_island_parity_diagnostic.md`
  - `test/tools/output/phase0a_full_content_balance_diagnostic.csv`
  - `test/tools/output/phase0a_full_content_balance_diagnostic.md`
  - `test/tools/output/phase0a_ch1_real_skill_profile_2026-08-20.csv`
  - `test/tools/output/phase0a_ch1_real_skill_profile_2026-08-20.md`
  - `test/tools/golden/phase0a_idle_island_parity_diagnostic.csv`（`git mv` 产生）
  - `test/tools/golden/phase0a_idle_island_parity_diagnostic.md`（同上）
  - `test/tools/golden/phase0a_full_content_balance_diagnostic.csv`（同上）
  - `test/tools/golden/phase0a_full_content_balance_diagnostic.md`（同上）
  - `test/tools/golden/phase0a_ch1_real_skill_profile_2026-08-20.csv`（同上）
  - `test/tools/golden/phase0a_ch1_real_skill_profile_2026-08-20.md`（同上）
  - `test/tools/phase0a_idle_island_parity_diagnostic_test.dart`（只改 `:17-18` 两个路径常量与头注里的路径文字）
  - `test/tools/phase0a_full_content_balance_diagnostic_test.dart`（只改 `:22-24` 路径常量与头注路径文字）
  - `test/tools/phase0a_ch1_real_skill_profile_diagnostic_test.dart`（只改 `:24-27` 路径常量与 `:3` 等头注路径文字）
  - `docs/audit/phase0a_ch1_real_skill_profile_2026-08-20.md`（只改对旧路径的引用串）
  - `docs/audit/phase0a_ch1_starter_power_skill_profile_2026-08-21.md`（同上）
  - `tools/audit/numbers_key_usage.py`（只删 `:34` 那条登记元组、`:334` 与 `:418` 元组里的 `"combat.final_damage_formula."` 一项；**不改判零口径、不删其他检查**）
  - `docs/superpowers/plans/2026-09-20-codex-e-golden-relocate.md`（新建，恢复点）
  - `docs/dispatch/reports/2026-09-20_codex_E_receipt.yaml`（新建，收据）
  - **其余一律禁改**，特别是 `.gitignore`、`data/numbers.yaml`、`GDD.md`、`PROGRESS.md`、`BACKLOG.md`、`CLAUDE.md`、`AGENTS.md`、`pubspec.yaml`、`lib/**`、`lib/shared/strings.dart`、`tools/audit/numbers_unused_keys_review.py`（B2 冻结只读）、其他 `test/**`。协调者以 `git diff --name-only` 机器判定：出现白名单外路径整单 FAIL。若确需改白名单外文件，**停下打 `[BLOCKED]`** 写明路径与原因。
- **git 边界**：只在自己的 worktree 提交；禁 push / merge / rebase / revert / amend；禁碰 `main` 与 `codex/p2-player-flow-20260910`。
- **环境边界**：禁安装任何软件。不碰真实存档，不启动游戏 GUI。
- **就绪标记**：最后实质 commit S 带 `[READY]`/`[BLOCKED]` 前缀；收据放独立包装 commit R（`R^ == S`，只含收据 + 恢复点），R 消息以同样标记开头。
- **黄金文件内容一字不改**：只 `git mv`，`git diff -M --stat` 必须显示 6 个 100% rename。
- **测试断言零变更**：三条测试只改路径常量与注释；比对逻辑、`UPDATE…=1` 刷新开关、表头断言全部原样。

## 1. 目标清单

### 目标 1：6 个黄金文件 `git mv` 到 `test/tools/golden/` 并改三条测试的路径常量

- `git mv test/tools/output/<x> test/tools/golden/<x>` × 6（文件名不变）。
- 三条测试的 `_csvPath` / `_mdPath` 常量改为 `test/tools/golden/…`；头注里「产出:test/tools/output/…」之类文字同步。注意 `phase0a_ch1_real_skill_profile_diagnostic_test.dart:126` 已有 `csvFile.parent.createSync(recursive: true)`，其余两条若在 `UPDATE…=1` 写入路径上没有 `createSync`，**允许**加一行 `File(_csvPath).parent.createSync(recursive: true)`（仅写入分支内，不改比对分支）。
- 先在干净状态单跑三条确认绿（基线绿），改完再单跑三条确认绿；然后做**一次证红**：临时把 `test/tools/golden/phase0a_idle_island_parity_diagnostic.csv` 改一个字节 → `phase0a_idle_island_parity_diagnostic_test` 必红（证明比对仍在生产路径上），`git checkout` 还原后重跑绿；结果写进收据 `break_red`（一组即可，方向写 `force_degenerate_value`）。
- `docs/audit/` 两份文档里的旧路径串改为新路径（只改路径，不改其他文字）。
- 验收：`git ls-files test/tools/output` 为空；`git ls-files test/tools/golden | wc -l` = 6；`git check-ignore test/tools/golden/x.csv` 无输出（未被 ignore）。

### 目标 2：`tools/audit/numbers_key_usage.py` 清 3 处 `combat.final_damage_formula.` 空登记

- 该段已于 C1 单（2026-09-19）删除，登记项现为空指向。删 `:34` 整条元组、`:334` 与 `:418` 元组里的该项（保留其他项与元组结构）。
- 验收：`python3 tools/audit/numbers_key_usage.py --baseline b99cfa72d --format json > /Users/a10506/Codex/2026-09-20/golden-relocate/usage_after.json` 退出 0，且 `verdict == 零引用` 叶数 = **14**、`unused_marked=true` 叶数 = **14**（与 D 单前后一致；不等则逐叶解释，不能解释就 `[BLOCKED]`）。`git grep -n final_damage_formula -- tools` 为 0 命中。

## 2. 验证（全部完成后）

- `flutter analyze --no-pub lib test tool` 0 issue；`NO_COLOR=1 dart format --output=none --set-exit-if-changed .` 0 changed（收据 `format_last_line` 必须用这条全仓命令的原文）。
- targeted 逐文件确认 `All tests passed!`：上述三条 diagnostic 测试 + `test/tools/asset_audit_test.dart`。
- 全量 `flutter test --no-pub`（**前台跑**），收据写 reporter 末行原文与 `[E]` 块数。

## 3. 禁止的修法

- 禁止改黄金文件内容或用 `UPDATE…=1` 重新生成后提交（rename 必须 100%）。
- 禁止放宽三条测试的比对（改成 `existsSync` 之类）。
- 禁止改 `.gitignore`；禁止把 `test/tools/output/` 目录本身删掉（其他测试仍往里写）。
- 禁止改 `numbers_key_usage.py` 的判零口径或 baseline 常量。

## 4. 收据与恢复点

- 按 `~/.claude/skills/afk/scripts/receipt.schema.md` 产 `docs/dispatch/reports/2026-09-20_codex_E_receipt.yaml`：`base_sha` = `b99cfa72d` 全 40 位、`head_sha` = S 全 40 位、`changed_files` = `git diff --name-only b99cfa72d..S` 全部（含 12 个 rename 路径）、三条 last_line 真跑原文、`error_block_count`、`break_red` 一组。
- 恢复点 `docs/superpowers/plans/2026-09-20-codex-e-golden-relocate.md`：状态 / 最后完成 / 下一步 / 已跑验证 / 阻塞项。

## 5. [BLOCKED] 出口条件

- 三条测试在基线（改动前）单跑就红。
- 证红不红（比对已不在生产路径）。
- 扫描器计数不是 14/14 且无法逐叶解释。
- 需要改白名单外文件。

## 6. 验收方式（协调者怎么查）

- `gate.sh <worktree> b99cfa72d <S> --whitelist-from <本派单包> --wrap-tip <R>`（rename 的 `-` 行会落进 `test_deletions`，协调者按 `git diff -M --stat` 6 个 100% rename 核实后视为豁免并在合并消息注明）。
- 协调者复跑：三条 diagnostic 测试绿；`git ls-files test/tools/output` 空；`numbers_key_usage.py` 对撞 14/14；抽做一次黄金文件改字节证红。
