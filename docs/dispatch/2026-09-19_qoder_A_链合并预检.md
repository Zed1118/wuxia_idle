# 派单包 A · 链 → main 合并预检（只读证据单）

- 执行端：qoderclicn（Qwen3.8-Max）
- worktree：`/Users/a10506/Qoder/2026-09-19/a-merge-precheck/wt`（分支 `qoder/a-merge-precheck-20260919`，基线 `5d9bbdeaf` = `codex/p2-player-flow-20260910` tip；已预热 libisar.dylib + pub get + build_runner）
- 派单基线（Gate 用）：`base_sha = 5d9bbdeaf`

## 0. 总则（硬约束）

- **语言**：commit message、文档一律简体中文；commit message 中文动宾（如「产出链合并预检证据单」）。
- **可写范围（白名单，精确路径）**：
  - `docs/audit/chain_merge_precheck_2026-09-19.md`（新建，本单主产出）
  - `docs/superpowers/plans/2026-09-19-qoder-a-merge-precheck.md`（新建，恢复点）
  - `docs/dispatch/reports/2026-09-19_qoder_A_receipt.yaml`（新建，收据）
  - **其余一律禁改**，特别是 `lib/**`、`test/**`、`data/**`、`tool/**`、`tools/**`、`pubspec.yaml`、`pubspec.lock`、`analysis_options.yaml`、`.github/**`、`PROGRESS.md`、`BACKLOG.md`、`GDD.md`、`CLAUDE.md`、`AGENTS.md`、`data_schema.md`、`lib/shared/strings.dart`、`data/numbers.yaml`。本单是只读证据单：**发现问题只登记，不修**。协调者以 `git diff --name-only` 机器判定：出现白名单外路径整单 FAIL。
- **git 边界**：只在自己的 worktree 提交；禁 push / merge / rebase / revert / checkout 其他分支；禁碰 `main` 与 `codex/p2-player-flow-20260910`。
- **环境边界**：禁安装任何软件（brew / pip / npm / flutter channel / pub global 均禁）；禁改本机配置；缺工具就写「未能判定 + 原因」。
- **不碰真实存档**（`~/Library/Containers/com.pen.wuxia.wuxiaIdle/`），不启动游戏 GUI（`flutter build macos --debug --no-pub` 允许，`flutter run` 禁）。
- **就绪标记**：最后实质 commit S 的消息带 `[READY]`（有阻塞项带 `[BLOCKED]`）；收据放独立包装 commit R（`R^ == S`，只含收据），R 消息也带 `[READY]`/`[BLOCKED]`（收据自引用口径见 §4）。
- **审计纪律**：每条结论带可机器复核的证据（命令原文 + 退出码 + 关键输出行 + 墙钟起止 `date +%T`）；禁止转抄 PROGRESS / 历史报告的数字，一律本次实测；不确定就写「未能判定」+ 原因，不猜。**从测试 reporter 的 `mm:ss` 不得推断小时，墙钟以 `date` 起止为准。**

## 1. 背景（一句话）

`codex/p2-player-flow-20260910`（`5d9bbdeaf`）领先 `origin/main`（`342d19275`）101 commit / 336 文件，`git merge-tree --write-tree origin/main 5d9bbdeaf` 无冲突可快进；合并前需要一份在**当前 tip** 上实测的 §8.2 合并 Gate 证据单，供用户拍板合并。合并动作不在本单。

## 2. 目标清单（按顺序，每目标收口 commit 一次）

### 目标 1：CI 同套命令本地镜像（按 `.github/workflows/ci.yml` 顺序）

在 worktree 根依次执行并记录每条的命令原文、退出码、最后 3 行输出、墙钟起止：

1. `flutter pub get --enforce-lockfile`
2. `dart run build_runner build --delete-conflicting-outputs`
3. `dart format --output=none --set-exit-if-changed lib test docs`
4. `flutter analyze --no-pub lib test tool`
5. 全量测试：`flutter test --no-pub 2>&1 | tee /Users/a10506/Qoder/2026-09-19/a-merge-precheck/full_test.log`（并发默认；预算 10–20 min；**不得**加 `--exclude-tags`、`-j1`、路径过滤或 `--name`）。记录最后一个非空 reporter 行、`grep -c '^\[E\]' full_test.log`、退出码。
6. `flutter build macos --debug --no-pub`（退出码 + 最后 3 行）。
7. 覆盖率：CI 门槛由 `tool/coverage_ratchet.dart` 定义，先读该文件写明阈值来源与当前 ratchet 值；若本地能用 `flutter test --no-pub --coverage` 后跑 `dart run tool/coverage_ratchet.dart` 就跑并记录，跑不了写「未能判定 + 原因」（不得为了跑它装 lcov 等工具）。

### 目标 2：§8.2 合并 Gate ⓐ–ⓔ 逐项核（范围 `origin/main..5d9bbdeaf`）

每项给命令 + 命中数 + 命中清单（file:line，超过 30 条只列前 30 并给总数）：

- ⓐ 中文文案 / 数值常量散写进 Dart：对 `git diff --name-only origin/main..HEAD -- 'lib/*.dart'` 的**新增行**扫 `[一-鿿]`，豁免 `lib/shared/strings.dart`、`lib/**/enum_localizations.dart`、`lib/**/battle_log.dart`、注释行（`^\s*///?`）与 `throw`/`ArgumentError`/`StateError` 表达式内的字符串（CLAUDE §5.6 已列合法 sink）；同时列新增行里的裸数值字面量（`= \d{3,}` 且非 `const`/测试/注释/索引），逐条判「疑似散写」或「合法（说明理由）」。
- ⓑ 高频路径 debug 日志：新增行中的 `debugPrint(`、`print(`、`log(`，标出所在函数是否为 `build(`、`paint(`、reducer tick、`didUpdateWidget` 等高频路径。
- ⓒ 误提交：`git diff --name-only origin/main..HEAD` 里是否有 `*.g.dart`、`*.log`、截图/capture 目录、`coverage/`、`build/`、`.dart_tool/`、`test/tools/output/` 产物、临时文档；`git ls-files` 是否新增了体积 > 1 MB 的文件（`git diff --stat` + `git cat-file -s`）。
- ⓓ commit message 中文动宾：`git log --format='%h %s' origin/main..HEAD` 全部 101 条逐条判定，列出不含中文或非动宾的 commit（已知一条历史豁免见 memory：SHA 被证据引用不改写历史——只登记不建议改写）。
- ⓔ format：目标 1 第 3 步结果。

### 目标 3：与 main 的差异面摘要（供用户读）

- `git diff --stat origin/main..HEAD` 按顶层目录（lib/ test/ data/ docs/ tools/ tool/ 其他）汇总文件数与增删行。
- 列出触及「红线层」的文件：`data/numbers.yaml`、`data_schema.md`、`lib/data/validation/**`、`lib/data/numbers_config.dart`、`GDD.md`、`pubspec.yaml`、`pubspec.lock`，每个给 `git log --oneline origin/main..HEAD -- <file>` 的 commit 列表。
- `saveVersion` / Isar schema 是否变化：`git diff origin/main..HEAD -- lib/data/isar_setup.dart lib/data/models/ | grep -n 'saveVersion\|@collection\|@Collection'` 的命中；并读当前 `saveVersion` 常量值。

### 目标 4：证据单 + 结论

写 `docs/audit/chain_merge_precheck_2026-09-19.md`（≤ 150 行），结构：基线与命令环境 / 目标 1 表 / 目标 2 ⓐ–ⓔ 表 / 目标 3 摘要 / **结论**只允许三种之一：`可合并（证据齐）` / `可合并但需知悉（列条目）` / `不可合并（列阻断项）`。结论不得用「基本」「大致」等模糊词。

## 3. 禁止的修法

- 禁止为了让某项变绿而改任何生产/测试/配置文件（本单零修复）。
- 禁止用 `--exclude`、tag 过滤、缩小路径让全量「通过」；禁止只跑一次失败就重跑到绿——若出现失败，原样记录失败测试名与输出，并单独再跑一次该文件（`flutter test --no-pub <file>`）判定是否 flaky，两次结果都写进证据单。
- 禁止把 reporter 的 `mm:ss` 当小时；禁止转抄历史「6971 PASS」等数字。

## 4. 收据与恢复点

- 按 `~/.claude/skills/afk/scripts/receipt.schema.md` 产 `docs/dispatch/reports/2026-09-19_qoder_A_receipt.yaml`。本单 `changed_files` 只有 docs → 属「纯文档审计单」，但**本单本身就跑了 full test / analyze / format**，所以三条 last_line 必须填真实输出行（不得写 `NOT_RUN`）；`error_block_count` 取 `grep -c '^\[E\]' full_test.log`。`audit_verification` 按 schema 的固定命令计算。
- **收据自引用口径**：收据的 `head_sha` 写最后一个实质 commit S（含证据单 + 恢复点，消息带 `[READY]`/`[BLOCKED]`）；收据本身单独提交为 R（`R^ == S`，只含收据文件，消息带同样标记）。写完收据后不得再改实质文件；若改了，重出收据再打新 R。
- 恢复点文件 `docs/superpowers/plans/2026-09-19-qoder-a-merge-precheck.md`：目标 / 分支 / 验收标准 / 当前恢复点（状态 / 最后完成 / 下一步 / 已跑验证 / 阻塞项）。

## 5. [BLOCKED] 出口条件

- 全量测试出现非 flaky 失败（同文件单跑复现）→ 记录后继续做完目标 2/3，结论写「不可合并」，tip 打 `[BLOCKED]`。
- 预热缺失（`.g.dart` 不存在 / `libisar.dylib` dlopen 失败）→ 先按 `flutter pub get` + `dart run build_runner build --delete-conflicting-outputs` 补一次；仍失败打 `[BLOCKED]` 并附错误原文。
- 任何步骤需要装软件 → 不装，写「未能判定」，继续其余目标。

## 6. 验收方式（协调者怎么查）

- `gate.sh <worktree> 5d9bbdeaf <S> --whitelist-from <本派单包> --wrap-tip <R>`：scope / wrap_commit / commit_msg / test_deletions / worktree_clean / analyze / format / receipt_crosscheck 全 PASS；协调者另在独立冷 worktree 复跑一次全量并与收据 `full_test_last_line` 对撞。
- 证据单里每个数字抽 3 个复算（ⓓ 的 commit 计数、ⓒ 的产物计数、目标 3 的红线层文件 commit 列表）。
