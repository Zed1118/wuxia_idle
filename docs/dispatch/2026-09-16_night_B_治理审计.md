# 夜批单 B · 治理与配置审计目标序列（2026-09-16 夜 · Codex 执行 · 审计单）

> 本单自包含，**只读代码、只写 `docs/audit/` 与 `tools/audit/`**。协调者（Claude）按「验收方式」独立复跑数字，不采信自报。
> 基线：`codex/p2-player-flow-20260910` = `c307b3ffcb155af58d4efb9179e36453297b1360`。你的分支 `codex/night-b-governance-20260916` 从该 SHA 建出，worktree 已由协调者建好。本单不需要 build/测试环境；若某目标要跑 dart 脚本，先 `flutter pub get`。

## 0. 总则（硬约束）

- **语言**：commit message、文档一律简体中文；commit message 中文动宾（如「分类登记簿 167 条 ready_reviewed 任务」）。
- **可写范围（白名单）**：`docs/audit/*.md`（本单新建的文件）、`tools/audit/*.py`（新建）。**其余一律禁改**，特别是：`docs/dispatch/phase0a_overhaul/task_registry.yaml`、`decision_registry.yaml`、`data/`、`lib/`、`test/`、`PROGRESS.md`、`BACKLOG.md`、`GDD.md`、`CLAUDE.md`、`AGENTS.md`、`lib/shared/strings.dart`、`pubspec.yaml`、`docs/audit/` 已有文件。协调者以 `git diff --name-only` 机器判定：出现白名单外路径整单 FAIL。
- **git 边界**：只在自己的 worktree 提交；禁 push / merge / rebase / revert；禁碰 `main` 与 `codex/p2-player-flow-20260910`。
- **不碰真实存档**，不启动游戏 GUI。
- **就绪标记**：每目标收口 tip 打 `[READY]`；最终 tip `[READY]` 或 `[BLOCKED]`。
- **时间围栏**：每目标收口后 `date`，过 **2026-09-17 06:00 CST** 不开新目标。
- **审计纪律**：每一条结论都要带可机器复核的证据（commit SHA / `git merge-base --is-ancestor` 结果 / grep 命令与命中数 / file:line）；禁止转抄旧文档的数字，一律本次实测；不确定就写「未能判定」+ 原因，不猜。
- **禁止的修法**：不得为凑「分类完成率」把判不清的条目硬归类；不得修改登记簿来让分类自洽；不得删除或改写既有 audit 文档。

## 1. 目标 B-1 · 登记簿 `ready_reviewed` 任务三分类（审查报告 D1）

**现状**：`docs/dispatch/phase0a_overhaul/task_registry.yaml`（约 8,400 行、约 200 个任务）中 `status: ready_reviewed` 约 167 条（**你要重新数**），其中混着已进入候选链/main 的、被后续批次替代的、以及真正待合的，无法一眼区分欠债。

**期望终态**：`docs/audit/task_registry_ready_reviewed_triage_2026-09-17.md`：
1. 头部：统计命令原文 + 实测 `ready_reviewed` 总数 + 三类计数（已集成 / 已过时 / 待合 / 未能判定）。
2. 逐条表：任务 id / 里程碑 / 分支或 commit 线索（登记簿里 `branch`、`commit`、`evidence` 之类字段实际叫什么以文件为准）/ 判定 / 证据（已集成 → 给出 `git merge-base --is-ancestor <sha> origin/codex/p2-player-flow-20260910` 或对 `main` 的结果，或 `git log --all --grep` 命中的 commit；已过时 → 指出替代它的任务 id 或 commit；待合 → 本地/远端分支名与 tip SHA，并说明 tip 是否 `[READY]`）。
3. 尾部「建议」：哪些条目建议登记簿状态改为 `integrated_*` / `superseded`（**只建议，不改登记簿**）。
4. 可用 `tools/audit/task_registry_triage.py` 辅助（可选），脚本必须可重跑得到同一张表。

**验收方式**：协调者复跑统计命令，总数与表行数相等；随机抽 10 行按证据列复核 `is-ancestor`/`git log`，10/10 成立；出现白名单外改动即 FAIL。

## 2. 目标 B-2 · `data/numbers.yaml` 零引用 key 清点（审查报告 C2 的清点半边）

**现状**：审查报告称 `numbers.yaml` 约 460 个叶子 key 中约 74 个在 `lib/` 零引用（含标 UNUSED 两个月的 `tower.daily_attempts` / `refresh_at`、`leaderboard.sync_to_supabase`、`combat.final_damage_formula.apply_*`、`validation_examples` 段）。这些数字来自只读快照，**未复跑**。

**期望终态**：
1. `tools/audit/numbers_key_usage.py`：解析 `data/numbers.yaml` 全部叶子 key 路径，对每个 key 的**末段名**与**完整路径片段**在 `lib/` 里做引用检索（注意 `numbers_config.dart` 的 `fromYaml` 是按 `yaml['snake_key']` 字符串取值，要按字符串字面量搜，不是按 Dart 标识符），输出 JSON：key / 叶子值 / lib 命中文件与次数 / test 命中次数 / 判定（生产消费 / 仅测试消费 / 零引用 / 疑似间接消费需人判）。
2. `docs/audit/numbers_yaml_unused_keys_2026-09-17.md`：运行命令原文、总 key 数、各判定计数、零引用逐条表（key / 值 / 所在段的注释是否已标 UNUSED / 建议：删除候选 / 保留理由 / 待拍板）。**不删任何 key**（删配置字段是 🔴 用户拍板项）。
3. 与 A 单交叉：A 单可能产出 `docs/audit/numbers_config_fallback_residue_2026-09-17.md`（yaml 里不存在但代码有兜底的 key）；本单反向（yaml 有、代码没用）。两表互不覆盖，不要去读写 A 单的文件。

**验收方式**：协调者重跑脚本，JSON 判定计数与文档一致；随机抽 8 个「零引用」key 手工 `grep -rn` 复核 8/8 为零命中；抽 3 个「生产消费」key 复核命中真实存在。

## 3. 目标 B-3 · 21 条未合入文档类分支的价值评估

**现状**：`docs/audit/worktree_ledger_2026-09-16.md` 的「③ 孤立 · 仅文档/证据/工具」表列出的分支（清理后仍存 21 条，见该文件末节「执行记录」），各自 1–9 个独有 commit，内容是 08-23～09-13 的审计/提案/工具/证据，从未进入候选链。用户明早要拍板「哪些 cherry-pick 进 `docs/audit`、哪些打归档标签后删、哪些直接删」。

**期望终态**：`docs/audit/orphan_doc_branches_triage_2026-09-17.md`，逐分支：
1. 分支名 / tip SHA / 独有 commit 数（`git rev-list --count origin/codex/p2-player-flow-20260910..<branch>`）/ 独有文件清单（`git diff --name-only <merge-base>..<branch>`）。
2. **结论是否仍成立**：把该分支文档的核心结论（如「假绿 N 处」「死字段 M 个」「CI 健康度 X」）对照链 tip 现状逐条核一遍——链上是否已修/已过时/仍成立；给证据（grep / 文件是否仍存在 / 后续 commit）。
3. 工具类（`p2-mutation-probe`、`p2-e2-playtest-capture-pipeline` 等）：工具在链 tip 上能否直接运行（只做只读 dry-run，不跑真机/GUI）。
4. 建议三选一：**cherry-pick 进链**（给出 cherry-pick 的 commit 列表与预期冲突文件）/ **打 `archive/<分支名>` 标签后删** / **直接删**；每条一句话理由。
5. 尾部汇总表按建议分组。

**验收方式**：协调者抽 5 条，按证据列复核结论；分支数与台账一致；出现白名单外改动 FAIL。

## 4. 目标 B-4（弹性尾）· M2 blocker 文本漂移核对

**现状**：审查报告 §2 指出登记簿 M2 的 blocker `first_stage_and_blackwind_playability_requires_human_strategy_validation_or_new_approved_tuning` 仍是 09-05 口径，而 09-06 后首关已有真路径首胜守卫、09-15/16 有连续链与容错变体证据（见 PROGRESS 顶段与 `/Users/a10506/Documents/Codex/2026-09-15/p2-onboarding-chain/batch2/summary.md`，只读）。

**期望终态**：追加到 B-1 文档末尾的附录「M 门 blocker 文本与实况对照」：逐个 M0–M9 门列出登记簿 blocker 原文、链 tip 实况（带证据）、是否漂移、建议替换文案。**只建议，不改登记簿。**

## 5. [BLOCKED] 出口

- 登记簿字段结构与预期不符、无法机器定位任务 → 先写清结构再做，做不了打 `[BLOCKED]` 说明缺什么。
- 任何目标需要改白名单外文件才能完成 → `[BLOCKED]`。

## 6. 收工与收据

1. 每目标收口写 `docs/superpowers/plans/2026-09-16-night-b-governance.md` 恢复点（状态 / 最后完成 / 下一步 / 已跑验证 / 阻塞）。
2. 收工前按 `/Users/a10506/.claude/skills/afk/scripts/receipt.schema.md` 生成**审计单**收据 `docs/dispatch/reports/2026-09-16_night_B_receipt.yaml`（`break_red` 留空 + `audit_verification` 块，`patch_sha256` 用 schema 里那条固定命令实算）并提交。审计单仍要填 `analyze_last_line` / `format_last_line` / `full_test_last_line`：本单不改 dart，可在链 tip 干净 worktree 跑一次取原文，或如实写明未跑并在 summary 说明（协调者会用 `--skip-full`）。
3. 最终 tip `[READY]`/`[BLOCKED]`，worktree 干净。
4. 摘要写到 `/Users/a10506/Documents/Codex/2026-09-16/night-B/summary.md`。
