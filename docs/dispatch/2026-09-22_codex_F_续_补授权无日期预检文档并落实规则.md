# 派单包 F-续 · 补授权无日期预检文档一处引用串 + 落实带日期归档规则（接 codex 2026-09-22 10:12 [BLOCKED]）

- 执行端：codex；worktree `/Users/a10506/Codex/2026-09-22/doclink-dated-archival/wt`（分支 `codex/doclink-dated-archival-20260922`，当前 tip `0b3b4d905`，其下 S=`7d403d76f` 只含阻塞台账与恢复点）
- 原派单包：`docs/dispatch/2026-09-22_codex_F_死链带日期文档归档规则与活文档残余处置.md`（**全部硬约束、禁止修法、目标 1–4 沿用**，本包只做两处修正 + 重出收据）
- 协调者认账：原包「220 条带日期」算错，实为 **219**；差额 `docs/phase0/route-c-external-gate-preflight.md:13 → data/app.so` 的来源文件无日期，你按第 0/5 节停线正确。规则本身不变（basename 短横日期）。

## 修正一：白名单追加一条（本包白名单为完整版，Gate 以本包为准）

- **可写范围（白名单，精确路径）**：
  - `tools/doc_link_scan.py`
  - `tools/test_doc_link_scan.py`
  - `tools/test_doc_link_scan_gitfixture.py`
  - `tools/README.md`
  - `docs/ROADMAP_1_0.md`
  - `docs/PUBLISHING_ART_PASS_1_0.md`
  - `docs/RELEASE_CHECKLIST_1_0.md`
  - `docs/phase0/route-c-external-gate-preflight.md`（**新增；只改 `:13` 一行**的 `` `data/app.so` ``：它是 Windows Profile AOT 构建载荷的路径，不是仓库文件；去反引号改纯文本 `data/app.so（构建产物路径，不入库）`，同行 `wuxia_idle` 等其他反引号串不动）
  - `docs/audit/doclink_residue_disposition_2026-09-22.md`
  - `docs/superpowers/plans/2026-09-22-codex-f-doclink-dated-archival.md`
  - `docs/dispatch/reports/2026-09-22_codex_F_receipt.yaml`
  - **其余一律禁改**（原包禁改清单全部沿用）。

## 修正二：验收数字

- 规则落实 + 目标 3 五条 + 修正一后：`python3 tools/doc_link_scan.py` **dead = 0、归档类 = 1801**（1582+219）、存活 9784 / ignored 781 / md 1832（含你已入库的台账与恢复点）。其他与原包同。

## 顺序

1. 目标 1（规则接入 `tools/doc_link_scan.py:612` 分类点，`ARCHIVAL_DATED_NAME` 常量 + 注释）。
2. 目标 2（`test_doc_link_scan.py` 7 例 → 26；gitfixture 1 例 → 21）。
3. 目标 3 五条 + 修正一共六处改文（去反引号纯文本 + 证据注）。
4. 目标 4：**重写**你已写的 `docs/audit/doclink_residue_disposition_2026-09-22.md`（状态改 `RESIDUE_CLOSED`，规则一段 + 改前/改后摘要原文 + 六条处置表 + §3 留痕保留 + 删掉 §4 阻塞出口；≤60 行）；`tools/README.md:13/:14` 按原包。
5. 三条 python 命令原文写进恢复点「已跑验证」，恢复点状态改 READY。
6. 以上做成最后实质 commit **S'**（`[READY]` 开头，中文动宾）。
7. **commit 后证红**：摘掉 `ARCHIVAL_DATED_NAME` 判据 → 样例 1/2/3 + gitfixture 新例必红，还原后重跑绿；写收据 `break_red` 一组（`remove_implementation`）。
8. 重写 `docs/dispatch/reports/2026-09-22_codex_F_receipt.yaml`：`base_sha` 仍 `8d8ae91951950d0fa8113e689491061da8047783`、`head_sha` = S' 全 40 位、`changed_files` = `git -c diff.renames=false diff --name-only 8d8ae9195..S'` 全部（会含台账/恢复点/README 等）、三条 last_line `NOT_RUN`、`error_block_count: 0`、`break_red` 步骤 7。单独包装 commit **R'**（`R'^ == S'`，只含收据 + 恢复点），`[READY]` 开头。
9. 结束 `git status` 干净；禁 push / merge / rebase / revert / amend。

## [BLOCKED] 出口（沿用原包，另加）

- 落实后 dead ≠ 0 或归档 ≠ 1801 且 `--rows` diff 解释不了。

## 验收方式（协调者）

- `gate.sh <wt> 8d8ae9195 <S'> --whitelist-from <本包> --wrap-tip <R'> --skip-full`。
- 复跑扫描器 0 / 1801；两层 26 / 21；自做一次证红。
