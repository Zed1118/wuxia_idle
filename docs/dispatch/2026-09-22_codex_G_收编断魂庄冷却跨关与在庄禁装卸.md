# 派单包 G · 收编 m4 半成品：断魂庄招式冷却按秒跨关保留 + 在庄禁装卸（用户 2026-09-22 拍板「两条都要」）

- 执行端：codex（`codex exec`，默认模型）
- worktree：`/Users/a10506/Codex/2026-09-22/gauntlet-cooldown-checkpoint/wt`（分支 `codex/gauntlet-cooldown-checkpoint-20260922`，基线 `5e032cb8a`；已预热：libisar.dylib / pub get / build_runner）
- 派单基线（Gate 用）：`base_sha = 5e032cb8a`
- 输入（只读，在 worktree 外）：`/Users/a10506/Codex/2026-09-22/gauntlet-cooldown-checkpoint/input/m4.patch`（sha256 前 16 位 `2dce6a195d7ff0a7`，m4 worktree 2026-09-14 未提交改动，20 个跟踪文件）+ `input/untracked/` 下 4 个新文件（`test/data/gauntlet_cooldown_compatibility_test.dart` 237 行 / `test/features/boss_gauntlet/gauntlet_skill_loadout_occupancy_test.dart` 216 行 / `test/features/boss_gauntlet/phase0a_gauntlet_cooldown_checkpoint_test.dart` 316 行 / `test/fixtures/legacy_gauntlet_cooldown.dart` 56 行）
- 合同：`docs/spec/2026-09-22-gauntlet-cooldown-checkpoint-design.md`（`APPROVED`，§2 S1/S2 两条语义、§3 schema、§6 验收、§7 不做——**全部照办，不重新论证**）。基线已含 `UiStrings.gauntletSkillLoadoutOccupied`（协调者预置，`lib/shared/strings.dart:4409`），patch 里 `strings.dart` 那个 hunk **必须剔除**。

## 0. 总则（硬约束）

- **语言**：commit message、代码注释、文档一律简体中文；commit message 中文动宾；含 schema 变更的 commit 标题前加 `[schema]`。
- **可写范围（白名单，精确路径）**：
  - `build.yaml`（只加 `test/fixtures/legacy_gauntlet_cooldown.dart` 一行排除）
  - `lib/data/isar_setup.dart`（只做 `saveVersion 0.50.0 → 0.51.0`：`:251-252` 注释与常量、迁移链末尾新增「段 21（0.51.0 断魂庄冷却检查点）」——只写版本戳与注释，不回填、不推断）
  - `lib/features/activity/domain/activity_member_snapshot.dart`
  - `lib/features/battle/application/phase0a/phase0a_stage_content_mapper.dart`
  - `lib/features/boss_gauntlet/application/gauntlet_controller.dart`
  - `lib/features/boss_gauntlet/application/gauntlet_providers.dart`
  - `lib/features/boss_gauntlet/application/gauntlet_service.dart`
  - `lib/features/boss_gauntlet/application/phase0a_gauntlet_stage_runner.dart`
  - `lib/features/cangjingge/presentation/cangjingge_screen.dart`
  - `lib/features/character_panel/presentation/encounter_skill_section.dart`
  - `lib/features/cultivation/application/skill_loadout_service.dart`
  - `lib/features/encounter/application/encounter_service.dart`
  - `lib/shared/battle_shared/combatant_snapshot.dart`
  - `test/features/activity/activity_member_snapshot_test.dart`
  - `test/features/battle/application/phase0a/phase0a_stage_content_mapper_test.dart`
  - `test/features/boss_gauntlet/gauntlet_live_headless_parity_test.dart`
  - `test/features/boss_gauntlet/gauntlet_providers_test.dart`
  - `test/shared/battle_shared/combatant_snapshot_test.dart`
  - `test/support/phase0a_production_headless_benchmark.dart`
  - `test/data/gauntlet_cooldown_compatibility_test.dart`（新建，来自 input）
  - `test/features/boss_gauntlet/gauntlet_skill_loadout_occupancy_test.dart`（新建，来自 input）
  - `test/features/boss_gauntlet/phase0a_gauntlet_cooldown_checkpoint_test.dart`（新建，来自 input）
  - `test/fixtures/legacy_gauntlet_cooldown.dart`（新建，来自 input）
  - `test/data/save_migration_version_gate_test.dart`（只改 `'0.50.0'` → `'0.51.0'` 的「当前版本」断言）
  - `test/data/player_yield_migration_test.dart`（同上，只改断言「迁移后版本 = 当前版本」的行；夹具起始版本不动）
  - `test/data/isar_slot_open_error_test.dart`（同上，`supportedVersion`）
  - `test/data/sect_member_count_repair_test.dart`、`test/data/tower_personal_record_migration_test.dart`、`test/data/expedition_milestone_record_migration_test.dart`、`test/data/gauntlet_run_serial_migration_test.dart`、`test/data/progressive_unlock_migration_test.dart`、`test/data/mainline_settlement_journal_migration_test.dart`、`test/features/tower/presentation/tower_historical_tombstone_settlement_test.dart`（同上规则：**只**改「迁移后应等于当前版本」的断言；断言历史夹具版本 / 中间版本的一律不动；每处改动在恢复点列 `file:line` 与理由）
  - `docs/superpowers/plans/2026-09-22-codex-g-gauntlet-cooldown-checkpoint.md`（新建，恢复点）
  - `docs/dispatch/reports/2026-09-22_codex_G_receipt.yaml`（新建，收据）
  - **其余一律禁改**，特别是 `lib/shared/strings.dart`（已预置）、`test/tools/compare_phase0a_headless_baseline_test.dart`（patch 里该 hunk 与主题无关，**剔除**）、`data/numbers.yaml`、`GDD.md`、`PROGRESS.md`、`BACKLOG.md`、`CLAUDE.md`、`AGENTS.md`、`pubspec.yaml`、`.gitignore`、任何 `*.g.dart`（gitignored，不提交）。协调者以 `git diff --name-only` 机器判定：出现白名单外路径整单 FAIL。若确需改白名单外文件，**停下打 `[BLOCKED]`** 写明路径与原因。
- **git 边界**：只在自己的 worktree 提交；禁 push / merge / rebase / revert / amend；禁碰 `main` 与 `codex/p2-player-flow-20260910`；禁碰 `~/Documents/Codex/**`（原 m4 worktree 只读，已拷出）。
- **环境边界**：禁安装任何软件；不碰真实存档（`~/Library/Containers/com.pen.wuxia.wuxiaIdle/` 只读），不启动游戏 GUI。
- **就绪标记**：最后实质 commit S 带 `[READY]`/`[BLOCKED]` 前缀；收据放独立包装 commit R（`R^ == S`，只含收据 + 恢复点），R 消息以同样标记开头。

## 1. 步骤

1. **落 patch**：`git apply --3way --exclude=lib/shared/strings.dart --exclude=test/tools/compare_phase0a_headless_baseline_test.dart input/m4.patch`；再把 `input/untracked/` 4 个文件按原相对路径拷进 worktree。`git status` 必须恰好列出白名单里那 19 个 patch 文件 + 4 个新文件（`.g.dart` 不算）。任何 3-way 冲突：先读上下文再手解，在恢复点记 `file:line` 与解法；解不了打 `[BLOCKED]`。
2. `dart run build_runner build --delete-conflicting-outputs`（`ActivityMemberSnapshot` 加了 3 个 Isar 字段，`.g.dart` 必须重生成；生成物不提交）。
3. **读懂再动**：对照 spec §2/§3/§4 逐条核 patch 实现——S1：`GauntletController` 关末把各槽剩余秒数写 `phase0aCooldownsRecorded/Keys/Seconds`，`CombatantSnapshot.openingSlotCooldownSeconds` → `Phase0aStageContentMapper._skillSlots` 按槽恢复，非法槽位/负数 fail-fast；headless `Phase0aGauntletStageRunner` 同路径。S2：`SkillLoadoutService`/`EncounterService` 写事务内读 `CharacterOccupancyService`，在庄返回 `SlotEquipOccupied`/`EquipOccupied`、卸招抛 `EncounterSkillOccupiedError`；藏经阁/奇遇招式区灰显 + `UiStrings.gauntletSkillLoadoutOccupied`。patch 若与 spec 不符，以 spec 为准改 patch，改动记恢复点。
4. **RED 证据**：patch 自带的 3 条新测试在**只加测试、不加实现**的状态下必红——做法：先只拷 4 个新文件（不 apply lib 部分）跑这 3 条，记录失败计数原文；再 apply 全量。（若 3-way 顺序不便，允许反向：apply 全量后 `git stash push -u -m g-red -- lib/` 不行——**禁用 stash**；改用「先 apply `--include='test/*'` 与 `--include=build.yaml`，跑红，再 apply 其余」。）
5. **saveVersion**：`lib/data/isar_setup.dart` 常量 `0.51.0`、`:251` 前加一行 `// 0.51.0 断魂庄冷却检查点：ActivityMemberSnapshot +phase0aCooldownsRecorded/Keys/Seconds，旧档缺字段读 false/空。`，迁移链末尾新增段 21 仅做版本戳（照段 15–20 体例）。同步白名单里各测试的「当前版本」断言。
6. 定向逐文件 `flutter test --no-pub <file>` 确认 `All tests passed!`：4 个新测试 + patch 改的 6 个测试文件 + 白名单里 10 个 saveVersion 测试文件 + `test/data/truth_source_guard_test.dart` + `test/data/pubspec_asset_declaration_test.dart`。
7. `flutter analyze --no-pub lib test tool` 0 issue；`NO_COLOR=1 dart format --output=none --set-exit-if-changed .` 0 changed（先 `dart format` 改过的文件）。
8. 全量 `flutter test --no-pub 2>&1 | tee /Users/a10506/Codex/2026-09-22/gauntlet-cooldown-checkpoint/full_test.log`（**前台跑**，预算 5–15 分钟），收据写 reporter 末行原文与 `[E]` 块数（必须 0）。
9. 做实质 commit S（`[READY][schema] 收编断魂庄冷却跨关检查点与在庄禁装卸并升 saveVersion 0.51.0`）。
10. **commit 后破坏证红三向**（spec §6.2）：① 摘掉 `GauntletController` 关末写检查点 → `phase0a_gauntlet_cooldown_checkpoint_test` 必红；② `phase0aCooldownsRecorded` 写死 `false` → 恢复断言红、兼容夹具测仍绿；③ 摘掉 `SkillLoadoutService` 的占用读 → `gauntlet_skill_loadout_occupancy_test` 必红。每向 `git checkout -- <file>` 还原并重跑绿。三组写收据 `break_red`（方向 `remove_implementation` / `force_degenerate_value` / `remove_implementation`）。
11. 收据 `docs/dispatch/reports/2026-09-22_codex_G_receipt.yaml`（`base_sha` = `5e032cb8a` 全 40 位、`head_sha` = S 全 40 位、`changed_files` = `git -c diff.renames=false diff --name-only 5e032cb8a..S` 全部、三条 last_line 真跑原文、`error_block_count: 0`、`break_red` 三组）+ 恢复点 → 包装 commit R（`[READY]` 开头）。恢复点必含：生产接线证据（入口/消费方 `file:line`）、RED 计数原文、冲突解法、saveVersion 断言改动清单、红线影响（spec §5）、残留风险（含「真机一次」未做）。

## 2. 禁止的修法

- 禁止把冷却秒数换算回回合或改 `turns × 0.55s` 物化口径；禁止改任何 YAML/数值。
- 禁止让 live 与 headless 走不同检查点路径（parity 测试必须断言两者关末冷却快照逐槽相等）。
- 禁止在旧档迁移里推断/回填历史冷却；旧档 = 未记录。
- 禁止用 `existsSync`/放宽断言换绿；禁止删既有测试行（`test_deletions` 会计数，除字面量替换外须为 0）。
- 禁止把 S2 做成只 UI 灰显不拦 service（写事务内必须拒）。
- 禁 `git stash`。

## 3. [BLOCKED] 出口条件

- 3-way 冲突解不了；RED 不红（新测试在无实现时就绿）；某向证红不红。
- 全量出现 `[E]` 且与本单无关且解释不了。
- 需要改白名单外文件（含发现 spec 与生产事实冲突）。

## 4. 验收方式（协调者怎么查）

- `gate.sh <worktree> 5e032cb8a <S> --whitelist-from <本派单包> --wrap-tip <R>`（代码单，全量必跑）。
- 协调者复跑：4 个新测试 + parity 测试绿；抽做一向证红；`git grep -n "'0.50.0'" test lib` 只剩历史夹具/中间版本；`.g.dart` 未入库。
