# 派单包 G-续 · 补 2 个版本断言文件白名单 + 夹具生成口径订正 + 落实收编（接 codex 2026-09-22 10:25 [BLOCKED]）

- 执行端：codex；worktree `/Users/a10506/Codex/2026-09-22/gauntlet-cooldown-checkpoint/wt`（分支 `codex/gauntlet-cooldown-checkpoint-20260922`，当前 tip `000672178`，其下 S=`992da9bf1` 只含阻塞恢复点）
- 原派单包：`docs/dispatch/2026-09-22_codex_G_收编断魂庄冷却跨关与在庄禁装卸.md`（**硬约束、步骤、禁止修法、[BLOCKED] 出口、验收沿用**，本包只订正四点 + 给完整白名单）
- 合同不变：`docs/spec/2026-09-22-gauntlet-cooldown-checkpoint-design.md`（`APPROVED`）
- 协调者认账：① 原包版本断言文件清单漏了 2 个（协调者 grep 被 `| head` 截断）；② `build.yaml:10-19` 是 `generate_for.include` 列表，原包/合同写「排除」是错的——夹具**加进 include**（patch 原样即对），`.g.dart` 由 build_runner 生成、不入库；③ 原包「19+4」应为「18+4」，以白名单为准。你的静态复核 4（S2 缺 UI 灰显）与 3（parity 测试 74 行删除）两点采纳为硬要求，见下。

## 订正一：完整白名单（Gate 以本包为准）

- **可写范围（白名单，精确路径）**：
  - `build.yaml`（只在 `generate_for.include` 列表加 `test/fixtures/legacy_gauntlet_cooldown.dart` 一行）
  - `lib/data/isar_setup.dart`
  - `lib/features/activity/domain/activity_member_snapshot.dart`
  - `lib/features/battle/application/phase0a/phase0a_stage_content_mapper.dart`
  - `lib/features/boss_gauntlet/application/gauntlet_controller.dart`
  - `lib/features/boss_gauntlet/application/gauntlet_providers.dart`
  - `lib/features/boss_gauntlet/application/gauntlet_service.dart`
  - `lib/features/boss_gauntlet/application/phase0a_gauntlet_stage_runner.dart`
  - `lib/features/cangjingge/presentation/cangjingge_screen.dart`
  - `lib/features/character_panel/presentation/encounter_skill_section.dart`
  - `lib/features/cultivation/application/skill_loadout_service.dart`
  - `lib/features/encounter/application/encounter_service.dart`
  - `lib/shared/battle_shared/combatant_snapshot.dart`
  - `test/features/activity/activity_member_snapshot_test.dart`
  - `test/features/battle/application/phase0a/phase0a_stage_content_mapper_test.dart`
  - `test/features/boss_gauntlet/gauntlet_live_headless_parity_test.dart`
  - `test/features/boss_gauntlet/gauntlet_providers_test.dart`
  - `test/shared/battle_shared/combatant_snapshot_test.dart`
  - `test/support/phase0a_production_headless_benchmark.dart`
  - `test/data/gauntlet_cooldown_compatibility_test.dart`
  - `test/features/boss_gauntlet/gauntlet_skill_loadout_occupancy_test.dart`
  - `test/features/boss_gauntlet/phase0a_gauntlet_cooldown_checkpoint_test.dart`
  - `test/fixtures/legacy_gauntlet_cooldown.dart`
  - `test/data/save_migration_version_gate_test.dart`
  - `test/data/player_yield_migration_test.dart`
  - `test/data/isar_slot_open_error_test.dart`
  - `test/data/sect_member_count_repair_test.dart`
  - `test/data/tower_personal_record_migration_test.dart`
  - `test/data/expedition_milestone_record_migration_test.dart`
  - `test/data/gauntlet_run_serial_migration_test.dart`
  - `test/data/progressive_unlock_migration_test.dart`
  - `test/data/mainline_settlement_journal_migration_test.dart`
  - `test/features/tower/presentation/tower_historical_tombstone_settlement_test.dart`
  - `test/data/isar_missing_field_migration_test.dart`（**新增**；只改 `:63` 当前版本字面量）
  - `test/data/reward_claim_receipt_migration_test.dart`（**新增**；只改 `:131/:185/:257` 当前版本字面量，夹具起始版本不动）
  - `docs/superpowers/plans/2026-09-22-codex-g-gauntlet-cooldown-checkpoint.md`
  - `docs/dispatch/reports/2026-09-22_codex_G_receipt.yaml`
  - **其余一律禁改**（原包禁改清单全部沿用：`lib/shared/strings.dart`、`test/tools/compare_phase0a_headless_baseline_test.dart`、`data/numbers.yaml`、`GDD.md`、`PROGRESS.md`、`BACKLOG.md`、`CLAUDE.md`、`AGENTS.md`、`pubspec.yaml`、`.gitignore`、任何 `*.g.dart`）。版本断言规则不变：**只**改「迁移后应等于当前版本」的断言，历史夹具/中间版本一律不动，每处在恢复点列 `file:line` + 理由。若全量再暴露第三个漏列文件：**允许**改它（同规则），但必须在恢复点单列「白名单外追加」并给 `file:line` + 全量红原文——协调者 Gate 时按证据豁免；除版本字面量外的任何白名单外改动仍 `[BLOCKED]`。

## 订正二：夹具生成口径

- `build.yaml` `generate_for.include` **加入** `test/fixtures/legacy_gauntlet_cooldown.dart`（patch 原 hunk 正确，照落）；`build_runner` 生成 `legacy_gauntlet_cooldown.g.dart`，gitignored 不提交。合同 §3 那句「排除其代码生成」由协调者收账时订正。

## 硬要求三：parity 测试零删除

- `gauntlet_live_headless_parity_test.dart` 既有测试行**一行不删**（含原循环与原断言）；逐槽冷却 parity 用**新增**测试用例 / 辅助函数 / 独立恢复场景补。patch 该文件 hunk 只取其新增部分，74 行删除不落。Gate `test_deletions` 对本单必须为 0（字面量替换除外）。

## 硬要求四：S2 UI 灰显

- 合同 §2 S2 = 服务写事务内拒 **+** UI 灰显 + 提示。patch 只做了拒后提示，须补：`cangjingge_screen.dart:349/:619/:683` 与 `encounter_skill_section.dart:90-98` 消费既有 `activeGauntletProvider`（`gauntlet_providers.dart:36-43`）按**实际成员** `characterId` 匹配（不是「有任何庄局就全灰」），命中则装/卸控件 disabled（`onPressed == null`）+ 显示 `UiStrings.gauntletSkillLoadoutOccupied`；provider loading/error 时 fail closed（禁用）。`applyAutoFill` 保持不拦（首关装配依赖），手动装卸由服务守卫兜底。补 widget 测试：`gauntlet_skill_loadout_occupancy_test.dart` 内加两个界面各一例「在庄成员灰显 + 文案出现 / 非在庄成员可用」（1280×720 视口）。

## 其余

- 新增英文注释一律改简体中文（只动新增行，不为翻译删旧行）。
- 步骤顺序、RED 证据（先 `--include` 测试部分跑红）、saveVersion 段 21、逐文件定向、analyze/format、前台全量、`[READY][schema]` 实质 commit S'、三向证红、收据（`base_sha` 仍 `5e032cb8a8de76069ea382f615e92aa70a2fec9d`，`head_sha` = S'）与包装 R'——全部按原包 §1 步骤 1–11 执行。恢复点在你现有文件上续写（状态改 READY，保留静态复核段作证据）。
- 禁 `git stash`；禁 push / merge / rebase / revert / amend。

## 验收方式（协调者）

- `gate.sh <wt> 5e032cb8a <S'> --whitelist-from <本包> --wrap-tip <R'>`（全量必跑）。
- 复跑 4 个新测试 + parity + 两界面灰显测试绿；抽一向证红；`git grep -n "'0.50.0'" test lib` 只剩历史夹具/中间版本；`.g.dart` 未入库；`git diff 5e032cb8a..S' -- test/features/boss_gauntlet/gauntlet_live_headless_parity_test.dart | grep -c '^-[^-]'` = 0。
