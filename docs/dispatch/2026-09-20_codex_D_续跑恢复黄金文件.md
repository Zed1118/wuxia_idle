# 派单包 D-续 · 恢复 6 个黄金文件跟踪并重出收据（接 Gate 2026-09-20 09:41 FAIL）

- 执行端：codex；worktree `/Users/a10506/Codex/2026-09-20/cap-and-untrack/wt`（分支 `codex/cap-and-untrack-20260920`，当前 tip `b81e62613`）
- 原派单包：`docs/dispatch/2026-09-20_codex_D_强化上限读realms表与清出跟踪产物.md`（**全部硬约束、白名单、禁止修法沿用**，本包只做修正 + 重出收据）
- Gate 事实：在**干净临时 checkout** 上全量 `08:39 +7027 -3`，三条红全是 `PathNotFoundException: Cannot open file, path = 'test/tools/output/…'`：
  - `test/tools/phase0a_idle_island_parity_diagnostic_test.dart:106` 逐字节比对已提交 CSV
  - `test/tools/phase0a_full_content_balance_diagnostic_test.dart:188` 读已提交 CSV 表头
  - `test/tools/phase0a_ch1_real_skill_profile_diagnostic_test.dart:130` 断言已提交文件存在并校验表头
  - 根因：这三对文件是**黄金文件**（测试读它们），不是产物；你的 worktree 里 `git rm --cached` 后磁盘文件仍在，所以本地全量绿——这是假绿，干净 checkout / CI 必红。

## 只做这几步（顺序）

1. 恢复 6 个黄金文件的跟踪（内容按基线原样，不改一字）：
   `git checkout 67dca7567 -- test/tools/output/phase0a_idle_island_parity_diagnostic.csv test/tools/output/phase0a_idle_island_parity_diagnostic.md test/tools/output/phase0a_full_content_balance_diagnostic.csv test/tools/output/phase0a_full_content_balance_diagnostic.md test/tools/output/phase0a_ch1_real_skill_profile_2026-08-20.csv test/tools/output/phase0a_ch1_real_skill_profile_2026-08-20.md`
   然后 `git ls-files test/tools/output` 必须恰好列出这 6 个；其余 5 个产物保持不跟踪、G2 记录保持在 `docs/audit/`。**不改任何 `_test.dart`，不改 `.gitignore`**。
2. 单跑三条：`flutter test --no-pub test/tools/phase0a_idle_island_parity_diagnostic_test.dart`、`…phase0a_full_content_balance_diagnostic_test.dart`、`…phase0a_ch1_real_skill_profile_diagnostic_test.dart`，逐文件确认 `All tests passed!`。
3. 恢复点 `docs/superpowers/plans/2026-09-20-codex-d-cap-and-untrack.md` 追加「Gate 打回 → 6 个黄金文件恢复跟踪」一段，把上面三条单跑结果写进「已跑验证」。
4. **前台**跑全量 `flutter test --no-pub 2>&1 | tee /Users/a10506/Codex/2026-09-20/full_test_2.log`；analyze `flutter analyze --no-pub lib test tool`；format **必须用** `NO_COLOR=1 dart format --output=none --set-exit-if-changed .`（上次收据用的是子集 1764 files，与 Gate 的 1866 files 对撞失败）。
5. 步骤 1 + 3 做成最后实质 commit **S'**（消息以 `[READY]` 开头，中文动宾，例如 `[READY] 恢复三对黄金文件跟踪并补充恢复点`）。
6. 重写 `docs/dispatch/reports/2026-09-20_codex_D_receipt.yaml`：`base_sha` 仍 = `cddf20b4ae4979b9e4544638f1f9fd09cdbeac5b`（S0）、`head_sha` = S' 全 40 位、`changed_files` = `git diff --name-only cddf20b4a..S'` 全部（会含 6 个恢复的黄金文件）、三条 last_line 用步骤 4 真跑原文、`break_red` 保留原两组。单独提交为包装 commit **R'**（`R'^ == S'`，只含收据），消息以 `[READY]` 开头。
7. 结束时 `git status` 干净；禁 push / merge / rebase / revert / amend。
