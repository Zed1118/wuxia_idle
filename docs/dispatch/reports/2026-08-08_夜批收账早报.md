# 2026-08-08 夜批收账早报(8h 挂机 · 已批准 5 单全关闭)

> 开工 00:36 · 生产收口 ~02:10 · preflight PASS 占锁 · allow-merge=yes / allow-push=yes / 代拍档=🟢自决+🟡可做不可合

## 一、当前完成

| 项 | 结果 | 判定 |
|---|---|---|
| **R 稀有度收口** | 四问全按拍板实装;移除奇遇重算改出生锁死;招募两处候选卡加资质展示 | ✅ 已合并 push |
| **C2b 桃花岛美术** | D 类 2 张 2448×1224 出仓 + C 类 2 张补自测 + 逐图建议判定 | ✅ 报告已合;**图待你定谳** |
| **P4 审计脚本入仓** | 9 脚本 + `run_all.py` 入 `tools/audit/`,五计数可一键复跑 | ✅ 已合并 push |
| **P5 PROGRESS 瘦身** | 110 行 79623B → 100 行 59307B,压缩 11 条旧条目 | ✅ 已合并 push |
| **P6 死链标注验证** | precision 95.0% / recall 100% / 一致率 88/90,查出两处系统性假阳 | ✅ 报告已合 |

**验证证据(全部本会话实测,非转抄)**

| 验证 | 值 |
|---|---|
| `flutter analyze --no-pub` | **No issues found** |
| 全量 `flutter test --no-pub` | **4910 pass / 0 fail** exit 0(=基线 4903 + 新增 7 逐值吻合) |
| CI 对 `0e43ec79` | **success**,headSha 与本地 rev-parse 逐字符一致 |
| 破坏证红(出生锁死红线) | 植入重算 → 实际值 `tianCai` 精确命中,其余 31 条不受影响,还原复绿 |
| merge-tree 复算 | 每个分支各零冲突 |
| 删分支前三验 | is-ancestor / `main..branch`=0 / `branch --merged` 全过,且全部先 push 远端留备份 |

## 二、代拍决策清单(逐条标级,红级为零)

### 🟢 绿级(我自决,已执行)

1. 把 main 未 push 的纯文档 commit 推上去,并把两个在途分支 push 到远端备份 —— `worktree-claude-rarity` 里那个真 bug 修复原本**全世界只有本地一份**。
2. P6 只 cherry-pick 报告 commit,**丢弃 pi 越界改 `PROGRESS.md` 的那个 commit**(PROGRESS 是执行端禁区,且我同时在改同一文件)。根因是**我的派单包漏写了这条禁区**,不是 pi 的错。
3. cherry-pick 把 `[READY]` 前缀带进了 main 历史 → 趁未 push amend 成中文动宾(§11)。
4. PROGRESS 压缩哪 11 条、各压到多短(全文留在 git 历史)。
5. 修我自己踩的 Gate ⓐ:全角括号散写进 widget → 收进 `UiStrings.rarityTotalParen`。
6. `numbers.yaml` 那段注释块已 drift(仍在描述修复前的「标签未派生」状态)→ 改写为拍板后实况。
7. 删 `pi/p6` 分支前先 push 远端(原本要 force-delete,不可回溯)。
8. 补跑资质 chip 的 1280×720 视觉 smoke —— §8.2 要求 UI 改动必做,我先前漏了。

### 🟡 黄级(做了方案 + 验证,**未合并**,等你拍)

1. **扫描器两处假阳的修复**:代码写好并全验证,放在 `docs/dispatch/reports/2026-08-08_scanner_fp_fix_proposal.md`(含可 `git apply` 的完整补丁)。**未合并**。
2. **否决了 pi 提的修法**:pi 建议「保留尾斜杠」,我实测证明它**既不必要也不充分**(只把工作树漂移从 17 降到 8,残余全是裸写的 `build`);改用查询侧补斜杠后漂移归零,且加回「保留尾斜杠」两地结果逐值不变=零贡献。补丁因此从 3 处收敛到 2 处。

### 🔴 红级(一律没碰,留给你)

- C2b 四张图的单图 PASS/FAIL 定谳与是否采用
- `tools/README.md` 里扫描器「可试用·非终审事实源」定位是否升级
- 资质档位的视觉档位化表达(色阶/印章),现在是临时版 chip

## 三、要你看的两件事

### 1. C2b 桃花岛四张图(证据在素材目录)

`~/Desktop/Projects/挂机武侠素材/桃花岛美术候选_20260807/`

- `_复检_三图对照_现役vs候选.png` —— 现役 / v1 / v2 同宽对照
- `_复检_建筑区放大_查文字污染.png` —— 三处建筑原生分辨率放大

**我的复检结论(建议,非定谳)**:构图同构成立、左右是真加宽非拉伸、**零文字污染**(三处建筑逐个看过无匾额假汉字)、零禁令项。色调实测比现役**略暗**(平均亮度 160.6/159.8 vs 168.6)、墨占比更高(16.1% vs 12.3%)、对比度持平 —— 我第一眼觉得「候选偏淡」被量测推翻,那是岛体占比变小造成的错觉。

**一项未能独立验证**:执行端自曝「原生 1774×887 等比放大到 2448」。我试了频谱法,**自校验失败**(已知 1456 上采样的对照组反而高于已知 1774 的对照组,该指标不随源分辨率单调)→ 如实标为未验证。若属实,有效细节宽约 1774,高于现役 1456(+22%)但短于规格要求的 2448。

### 2. 资质档位 UI(玩家可见,已进 main)

角色档案页 + 宗门招募确认弹窗 + 招募弹窗,三处显示「资质 绝世(24)」式 chip,沿用同款属性 chip 样式。视觉档位化留你拍。

## 四、决策菜单

**1. 扫描器假阳修复(P7)**
- **A(推荐)** 合并补丁 —— 已全验证:死链 940→909、工作树漂移 17→0、既有 10 类样例仍 10/10、总数守恒
- B 先不合,等 P8(引真实 git fixture 的测试)做完一起合
- C 不修,维持现状并在 `tools/README.md` 注明两处已知假阳

**2. C2b 四张图**
- A 采用 v1 接线(与现役构图最接近,便于直接对比)
- B 采用 v2
- C 都不采用,现役图保持不动
- D 先不定,下次真机看完整场景再说

**3. 资质档位视觉**
- A 保持临时版 chip 不再动
- B 开一个视觉档位化小批(色阶/印章按六档区分)
- C 先真机看一局再定

**4. 下一批**
- A P8 扫描器真实 git fixture 测试(qoderclicn/kimi,60-90min)
- B P9 `/afk` 工作流 v2 三处门禁缺口(Claude,30-45min)
- C P2 真机录屏验收管线(需真机位)
- D P3 checklist E 段 reconcile(捎带)

## 四点五、**没做成的一件事(如实报)**:资质 UI 的 1280×720 视觉 smoke

§8.2 要求「UI/UX 任务 widget test 外必顾常规桌面视口」,我合了玩家可见的资质 chip 却漏了这条,
发现后补跑 —— **两轮都失败,未获得截图**:

| 轮次 | 命令 | 结果 |
|---|---|---|
| 1 | `visual_capture.sh --route recruitment_dialog --resolutions 1280x720` | ❌ `xcrun: error: unable to find utility "xcodebuild"` —— 我自己给命令带了 `DEVELOPER_DIR=/Library/Developer/CommandLineTools`(那是给 git 绕 Xcode license 用的),顶掉了完整 Xcode |
| 2 | 去掉 `DEVELOPER_DIR` 重试 | app **构建成功**(`✓ Built ...wuxia_idle.app`),但截图阶段 `could not create image from rect`,PNG 未生成 |
| 3 | 加 `--all-spaces` 重试 | 同样 `could not create image from rect` |

按视觉验收 SOP「两轮不过标 [BLOCKED]」,不再硬试。症状与 memory
`feedback_flutter_macos_drive_screenshot` 记的「窗口恢复到主屏外负坐标」同型,
但本会话未做进一步定位(属 P2 真机录屏管线范畴)。

**影响**:资质 chip 的真机观感**无截图证据**。它已进 main(analyze 0 + 全量 4910/0 + CI 绿覆盖),
逻辑与布局有测试守,但「在 1280×720 下好不好看/会不会挤」没验过。
建议你真机开一局招募界面看一眼,或把它并进下次真机目检批。

> 顺带:`DEVELOPER_DIR` 这个坑值得记 —— 全局约定用它绕 git 的 Xcode license,
> 但**任何 `flutter build macos` 都不能带它**,否则 xcodebuild 找不到。

**5. 老存档角色资质(收尾自查新发现,BACKLOG 一#15,🔴)** —— 这是你今夜「出生锁死」拍板的直接后遗症:
加载时不重算 → 08-08 之前存档里的角色**永久停留旧写死值「标准」**,改派生只对新建角色生效。
(反讽:改之前那版跟随总点数漂,反而会在玩家吃一次奇遇后自愈。)
- A 不迁移 —— 老档接受显示「标准」,新档正确
- B 一次性迁移按**当前**总点数重算 —— 最简,但对吃过奇遇的角色略高估
- C 一次性迁移按**出生**点数重算(`attributes.total − attributeBonusFromAdventure`)—— 语义最准,
  但该字段近期才写活,更老的存档里可能为 0 而实际吃过奇遇,那部分仍略高估

⚠ 以上为读码推断(`.rarity =` 全仓只有 `character.dart:220` 一处、在 `Character.create` 内),
**本会话无老档样本、未做真实存档验证**,动手前请先拿一个真档实测。

## 五、工作流 v2 首跑发现(已入池 P9)

| # | 缺口 | 后果 |
|---|---|---|
| 1 | preflight `blocked_decisions` 只认分支 tip 的 `[BLOCKED]` 前缀 | 两个卡你拍板的分支没打标记 → 门禁报「待拍板=无」,与实况脱节 |
| 2 | `approved_tasks` 用池文本子串匹配 | 在途分支收口类工作无法表达;硬塞短 ID 会被 `README` 之类误匹配 |
| 3 | `doctor --json` 不透出 `dispatch_template` | skill 文档说「按 doctor --json 给出的 dispatch_template 发单」,实际得读 `executors.json` |

另:WIP 计数把**已合并但分支还在**的算作 RUNNING,以及协调者自己的 worktree 分支也占一格,导致名额虚高。

## 六、按 v2 规矩提前收官的说明

已批准 5 单在 ~1.5h 内全部关闭。v2 明确把目标从「跑满时长」改成「多少任务经过独立复核并真正关闭」,并删掉了 v1 的「墙钟利用率 ≥90%」「池空必补」,所以**不自行补活**。剩余时间用于加深已交付项的验证(扫描器修复实验、C2b 内容复检、P4 工作树无关性实证)与收口,而不是开新单。池里 P2/P3/P8/P9 都在,等你选。
