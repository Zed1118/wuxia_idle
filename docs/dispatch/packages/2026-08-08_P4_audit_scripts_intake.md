# 派单包 · P4 Q2/A1 审计脚本入仓补齐可复现性(2026-08-08 夜批)

> 执行端:qoderclicn · 分支 `qoder/p4-audit-scripts-0808`
> **本单不重做审计、不改审计结论**。结论已于 2026-08-07 夜抽验 3 条全中并合入 main。
> **本单唯一目标:让报告里的五个计数能被任何人一条命令复跑出来。**

## 〇、背景(自包含,不要求你读别的文档)

本仓是 Flutter 写实武侠挂机游戏。2026-08-07 做过两份配置审计:

- **Q2**「配置声明 vs 生产硬编码」→ 报告 `docs/dispatch/reports/2026-08-07_Q2_config_bypass.md`
- **A1**「死字段/never-written 字段」→ 报告 `docs/dispatch/reports/2026-08-07_A1_dead_fields.md`

两份报告的结论是可信的(已抽验),**问题出在过程**:跑数用的 python 脚本写在 `/tmp/` 下,
从未入仓。于是报告里的关键计数**没人能复跑验证**,`/tmp` 一清就永久失去可复现性。

## 一、要救的脚本在哪(两个位置,`/tmp` 优先,失效用备份)

**主位置**(可能已被系统清理,先查):
```
/tmp/q2/leaf_extract.py
/tmp/q2/field_usage.py
/tmp/a1_audit/extract_fields.py
/tmp/a1_audit/verify_fields.py
/tmp/a1_audit/refs.py
/tmp/a1_audit/classify.py
/tmp/a1_audit/classify_owned.py
```

**备份位置**(2026-08-08 01:1x 协调者已复制一份,内容与上面逐字节一致):
```
/Users/a10506/.claude/jobs/1dedbe6d/tmp/p4_backup/q2/
/Users/a10506/.claude/jobs/1dedbe6d/tmp/p4_backup/a1_audit/
```

> ⚠ 备份目录随 job 清理会消失,**本单结束前必须把脚本提交进仓库**,否则等于没做。

## 二、必须能复跑出来的五个计数(验收锚点)

| # | 计数 | 出处 | 报告原文 |
|---|---|---|---|
| 1 | **8** 处背离(confirmed) | Q2 | 「实锤 8 处背离」/ 主表「背离(confirmed) 8 字段 / 8 处」 |
| 2 | **7** 处部分背离 | Q2 | 「+ 7 处部分背离」 |
| 3 | **21** 处「已解析零 caller」休眠配置 | Q2 | 「另有 21 处"已解析零 caller"的休眠配置」 |
| 4 | **44** | A1 | 见 `2026-08-07_A1_dead_fields.md`,自己去报告里定位这个数的定义 |
| 5 | **14** | A1 | 同上 |

**验收判据**:入仓后跑你写的入口,输出里这五个数**逐个对得上报告**。
对不上 → 不许改报告去迁就脚本,也不许改脚本去凑数,**如实写进交付报告的「差异」一节并说明原因**
(合理原因例:main 已前进导致真实计数变了 —— 那就把新值和差异原因都写清楚)。

## 三、本单要做的四件事

### 1. 脚本入仓
- 落 `tools/audit/`(新建目录);文件名保持可读,可适度重命名(如 `q2_leaf_extract.py`)
- **只用标准库 + `git ls-files`/`git check-ignore`**,与 `tools/doc_link_scan.py` 同体例。
  不要引入第三方依赖(本仓 tools/ 无 requirements)
- 脚本里如有写死的绝对路径(`/tmp/...`、`/Users/a10506/...`),改成**从仓库根相对定位**,
  使其在任何 worktree 里跑结果一致

### 2. 统一入口
- 提供一个入口能一把跑完并打印五个计数,例如 `python3 tools/audit/run_all.py`
- 输出要有清晰的「计数名 = 值」行,便于人肉比对与未来 diff
- 支持 `--json` 更好(非硬要求)

### 3. numbers.yaml 行号 drift 订正
- Q2 报告里引用的 `numbers.yaml` 行号已漂:报告写 `:1917`,2026-08-07 实测已变 `:1954`
  (N1 批插注释所致)。⚠ 2026-08-08 本仓又有新提交动过 numbers.yaml 注释,
  **行号可能再次漂移,你必须现查,不要抄这里的 1954**
- 把 Q2 与 A1 两份报告里**所有** `numbers.yaml:<行号>` 形式的引用重新定位并订正
- 订正方式:按字段名/路径正向 grep 定位,**不要**按旧行号反查

### 4. 交付报告
写 `docs/dispatch/reports/2026-08-08_P4_audit_scripts.md`,含:
- 入仓了哪些脚本、各自职责一句话
- 五个计数的**实跑输出粘贴**(命令 + 输出),与报告值的比对结论
- 订正了几处行号,列出「旧 → 新」对照
- 未解决问题 / 建议

## 四、边界与红线

- **不改任何 `lib/` 生产代码、不改 `data/*.yaml` 的值**(订正报告里的行号引用不算改 yaml)
- 不重做审计、不推翻结论。发现结论可疑 → **写进报告的「存疑」节**,不要自行改结论
- 不要 push、不要 merge、不要碰 main、不要 revert。只在你自己的 worktree 里提交
- commit message 必须**中文动宾**(本项目硬规矩;英文 conventional 前缀属违规)
- **不要动任何 .dart 文件**

## 五、[BLOCKED] 出口

以下情况停下,tip commit 打成 `[BLOCKED] ...` 并在报告里写清:

- `/tmp` 与备份目录**都**没了脚本 → 停,不要凭报告反向重写脚本(那是重做审计不是入仓)
- 五个计数里有 ≥3 个对不上且原因不明 → 停下报告,不要硬调

## 六、交付信号

做完:工作区干净,分支 tip commit message 以 **`[READY]`** 开头 + 中文动宾摘要。
例:`[READY] 入仓 Q2/A1 审计脚本并订正行号 drift`
