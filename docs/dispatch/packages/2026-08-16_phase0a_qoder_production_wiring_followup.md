# Phase 0A 生产接线审计收窄返修（Qoder）

你的 `02f4d010` 审计路径和计数基本可复核，但 §6 把已有用户拍板/项目红线误列为四项未决，且接线期“并存旧 3v3 交互”会重现用户已明确否定的方向。请只改两份交付文档，新增返修 commit，不 amend/rebase。

## 已确定约束（不再待拍）

1. 引擎：`CLAUDE.md:86/372` 已锁纯 Flutter，Flame 不进根应用。
2. 战斗形态：用户已明确“后续换了战斗模式，不再测 3v3”，终态是 Phase 0A 单角色水墨 ARPG 替换玩家可见的旧 3v3 交互，不做两套玩家可见模式并存。GDD/CLAUDE 的 3v3 描述是待同步 drift，不是让旧模式继续留在产品的依据。
3. 数值：probe 固定数字不直迁；生产只消费根应用公式/YAML/红线 validator。
4. 在线=离线：实时手动与无头自动必须共用同一确定性战斗模拟/结算核；手动与 AI 只是输入适配器，禁止另造简化胜负或保留旧 3v3 作隐藏无头规则。

## 必改

- 重写报告 §4 接线路线：先建纯 Dart 确定性 simulation core，再接手动/AI input adapter、纯 Flutter 表现、最后切生产入口和清旧交互；回退依赖小切片 commit，不依赖产品内并存模式。
- 重写 §6：上述四项改为“已确定边界”。仅保留确实无法由现有拍板/红线推导的实施细节；若无，写“无新增待拍”。
- 计划恢复点同步改为审计收窄完成。
- 只改原两文档，`git diff --check`，tip 以 `[READY]` 开头，worktree 干净。其余禁区沿用原派单。
