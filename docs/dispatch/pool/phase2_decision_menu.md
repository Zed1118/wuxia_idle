# N13 · 二阶段拍板菜单（按解锁力降序）

> 基线：`f1e645f0`。N7 清账已就绪：`/Users/a10506/Desktop/Projects/挂机武侠-p2-mstone/docs/audit/g0_decision_reconciliation_20260826.md`。本菜单采信其“签字证据 / 查无实据”，不从代码实况反推签字。
> 读法：“直接/传递/总计”单位均为 N2 断言组；传递只算除本节点外没有其他未满足 S/G 前置的行。每个红行本身是独立 S 签字单元，不会因上游已签而被暗中代签。

## 1. 依赖分类与判据

| 前置依赖项 | 类 | 逐项判据 |
|---|---|---|
| `C01` 冻结秒映射/退役边界 | S | 会确定数值映射、schema 权威和旧字段退役，需明示签字。 |
| `C01` 重新拍板装配方案数量 | S | 方案 L371–374 的双方案与已否边界直接冲突，不能用工程完成代替。 |
| `C01` 重新拍板归来报告口径 | S | 方案 L387–393 的统一报告与保留独立摘要边界冲突，需人类裁决。 |
| `N2 #18` 攻击令牌口径 | S | 方案 L617/L1025/L1534 对“2–4”指总数还是近战数存在歧义。 |
| `C01` 精确解锁章点/持久化边界 | S | 改变玩家可见门槛和存档状态，方案 L30 明列为 `PROPOSED`。 |
| `C01` schema/迁移边界 | S | 决定新六槽、旧槽、武器形制和冷却字段如何持久化，不是纯机械前置。 |
| `G2 试玩通过` | G | 方案 L1033–1037 要求真人主观与双视口证据；当前记录已是 `8/8 PASS`。 |
| `C02/C03/C07` 合同关闭 | E | 已签规则的领域实现与测试闭环，不再产生产品取舍。 |
| `A03/A05` 合同关闭 | E | 角色占用与资格合同的工程完成。 |
| `A01` 合同关闭 | E | 实际参与者快照的生产接线。 |
| `G1` 合同通过 | E | typed def/schema/API/ID/迁移的机器 Gate，不是主观签字。 |
| `T01–T07` 目标合同关闭 | E | 七模板目标原语完成和测试通过。 |
| `A03/A07/A08` 合同关闭 | E | 占用、单远征与离线时间线的任务闭环。 |
| `M-TOWER production policy Gate` | E | 另一条塔模式任务的生产 Gate。 |
| `C13` 合同关闭 | E | 失败策略、session 和伤势幂等的工程闭环。 |
| `E01–E04` 合同关闭 | E | AI/spawn/token/入口四个领域前置完成。 |
| `E05–E10` 生态子 Gate | E | 对应生态包的定义、资产和回归闭环。 |
| `C10` 表现 feed 合同关闭 | E | 领域事件到 VFX/SFX/HUD 的生产映射完成。 |
| `T12` 105/49 迁移完成 | E | 内容包迁移和 validator 通过，不需新的主观决策。 |
| `A04` 合同冻结 | E | 装备/主修锁定合同已定后的工程前置。 |
| `无` | 非节点 | 没有入边；若该行为红，其本身就是 S 签字单元。 |

G2 节点为 `0/103/103`，毛可达集是 N2 #8/#15/#20–#23/#28–#32/#34；它已有 `test/tools/output/phase2_g2_stage_01_03_acceptance_record.md:4,14-21` 的 `8/8 PASS`，所以不再出现为早上待拍项。

## 2. 待拍菜单

| 决策 ID | 一句话问题 | 当前状态（现取） | 可选项（仅方案原文） | 拍板需要看什么 | 直接/传递/总计 | 解锁的 §20 任务 ID |
|---|---|---|---|---|---:|---|
| 方案未编 ID | 六类生态在各章和塔层究竟如何固定分配？ | `PROPOSED`（方案 L30；目标表 L653–664） | 方案未列选项，需用户先定方向 | `data/combat/manifest/stage_assignments.yaml`；完整逐关 current→target 表尚不存在，需先产出 | 7/13/20 | 直接 `T12`（#30）；传递 `C02+C03+C04+C05+C06+C10+E02+E03+T01–T07+T11+A01+A03+A04`（#34） |
| 方案未编 ID | 战斗信息层级、伤害组数、屏外方向上限和战后统计定到什么值？ | `TUNING`（方案 L31；候选值 L759–806） | 方案未列选项，需用户先定方向 | 双视口高密度截图/golden 和可读性统计尚不存在，需先产出 | 13/0/13 | `C10+P01+P02+P03`（#33） |
| 方案未编 ID | 归来时保留各活动独立摘要，还是重开统一归来报告？ | `查无实据`（§0.2 L38–53 无对应 ID/状态；方案只写单一目标 L387–393） | 方案未列选项，需用户先定方向 | `lib/features/seclusion/presentation/offline_recap_gate.dart`、`lib/features/expedition/presentation/expedition_recap_screen.dart`；统一/独立对照稿尚不存在 | 8/4/12 | 直接 `A08`（#14）；传递 `M-EXP`（#23） |
| 方案未编 ID | 武器形制、六槽、旧槽和秒冷却按什么 schema/迁移边界一次冻结？ | `查无实据`（§0.2 L38–53 无对应 ID/状态；只有目标 L929–953） | 方案未列选项，需用户先定方向 | `lib/core/domain/character.dart`、`lib/data/defs/skill_def.dart`、`lib/data/defs/equipment_def.dart`；完整旧档迁移 fixture 尚不存在 | 9/0/9 | `C08+C11+A11`（#36） |
| 方案未编 ID | 各模式总敌量、同时活跃数和补兵阈值定为多少？ | `TUNING`（方案 L31；区间 L599–612；未定值 L1533–1535） | 方案未列选项，需用户先定方向 | `docs/spec/phase2_combat_core_tuning_candidates_20260826.md`；全模式模拟和双视口画面尚不存在 | 8/0/8 | `E02+E03`（#28） |
| 方案未编 ID | 每角色继续一套持久装配，还是重开亲战/差遣两套方案？ | `查无实据`（§0.2 L38–53 无对应 ID/状态；方案只写双方案 L371–374） | 方案未列选项，需用户先定方向 | `docs/spec/rejected_task_registry.md:87`、`lib/core/domain/character.dart`；单套/双套存档影响表尚不存在 | 5/0/5 | `A02+A11`（#12） |
| 方案未编 ID | 旧 `cooldownTurns` 怎样映射成秒，何时允许退役旧字段？ | `TUNING`（方案 L31；方向 L132–133；迁移边界 L935–936） | 方案未列选项，需用户先定方向 | `test/features/battle/application/phase0a/phase0a_cooldown_seconds_authority_test.dart`；全技能映射表和旧档报告尚不存在 | 4/0/4 | `C11`（#6） |
| 方案未编 ID | 地点的精确开放章点、hidden/heard/open 和一次蜡封怎样持久化？ | `PROPOSED`（方案 L30；候选时点 L569–580） | 方案未列选项，需用户先定方向 | 每模式 current→target 迁移表尚不存在，需先产出 | 4/0/4 | `U11`（#27） |
| 方案未编 ID | “2–4 攻击令牌”指全部令牌，还是只指近战令牌？ | `TUNING`（方案 L31；互相冲突的原文 L617/L1025/L1534） | 方案未列选项，需用户先定方向 | `data/combat/encounters/black_wind_ridge.yaml`、`docs/dispatch/phase2_wiring/T1_token_budget_realign.md`；统一口径证据尚不存在 | 1/1/2 | 直接 `E03`（#18）；传递 `E03`（#17） |
| 方案未编 ID | 哪些调参值真有用户原始签字，哪些只是候选值？ | `TUNING`（方案 L31；N12 L48 记录来源链冲突） | 方案未列选项，需用户先定方向 | `docs/dispatch/phase2_wiring/T1_token_budget_realign.md`与 N7 的原始会话锚点；须先产出逐值来源表 | 2/0/2 | `C01`（#19） |
| 方案未编 ID | 已解锁后再掉的首领残页转成什么，转换量是多少？ | `TUNING`（方案 L31；转换目标 L536） | `领悟点` / `已有强化材料`（方案 L536）；方案未列转换量选项 | `lib/features/cultivation/domain/skill_unlock_service.dart`；总账守恒模拟尚不存在，需先产出 | 1/0/1 | `C14+U09`（#26） |

待拍 11 个问题直接覆盖 `62` 组，不重复的传递集是 #17 的 `1` 组、#23 的 `4` 组和 #34 的 `13` 组，合计可达 `80` 组。其余红行是 N7 已证明得到方案级推进授权的 `FROZEN`/目标合同，或等待 E 类工程前置，不再伪装成新的产品选项。

## 3. 零成本签字核对

| 决策 ID | 当前状态与证据 | 实况/成本 | 计数位置 |
|---|---|---|---|
| `COMBAT-SCOPE-01` | `FROZEN`（方案 L40；N7 L15 有 `S:249`） | 六几何已在生产域定义，按现状冻结无返工成本；不由实现反推签字 | 绿行 #1，1 组 |
| `COMBAT-WAVE-CD-01` | `FROZEN`（N7 L16 有 `S:249`） | 换波保留冷却已是现状，按现状冻结无返工成本 | 绿行 #2，1 组 |
| `MAINLINE-PARTICIPANT-01` | `FROZEN`（方案 L43；N7 L18 有 `S:249`） | 无效掌门指针已 fail closed，按现状冻结无返工成本 | 绿行 #3，1 组 |
| `INNER-DEMON-CULTIVATION-01` | `FROZEN`（方案 L49 原为 `PROPOSED`；N7 L24 有 `S:11053,S:11071`） | 生产已不扣主修，这里依赖原始签字证据，不依赖“代码已这样” | 绿行 #4，1 组 |

这 4 组已在绿色 25 组中，不重复加入待拍或 G2 解锁数。

## 4. 守恒和早上勾选顺序

- 全部 S/G 节点的“总解锁集”去重后是 `210` 组，`210 ≤ 236`。G2 的 103 组和各红行自身重叠，不另加。
- 差额 `236 - 210 = 26` 组：池文件 L21–28 的 7 条绿任务合计 `25` 组，L25 的黄任务 `A05` 为 `1` 组。
- 按上表已算总解锁力的前三个勾选顺序是：生态分配 `20` 组，战斗信息层级 `13` 组，归来报告 `12` 组。此顺序只表示解锁数，不表示任何选项判断。
