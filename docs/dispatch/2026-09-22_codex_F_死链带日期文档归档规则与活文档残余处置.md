# 派单包 F · 死链扫描器「带日期文档=历史快照」归档规则 + 活文档残余死链处置（用户 2026-09-21 拍板 P13 (b)）

- 执行端：codex（`codex exec`，默认模型）
- worktree：`/Users/a10506/Codex/2026-09-22/doclink-dated-archival/wt`（分支 `codex/doclink-dated-archival-20260922`，基线 `8d8ae9195`；纯 python/markdown 单，**不预热、不跑 flutter**）
- 派单基线（Gate 用）：`base_sha = 8d8ae9195`
- 背景：P13（`docs/dispatch/pool/README.md`）登记的死链残余口径已过期。协调者 2026-09-21 在 `8d8ae9195` 实测 `python3 tools/doc_link_scan.py`：md 1830 / 存活 9784 / ignored 781 / 归档类 1582 / **dead 225**。225 条按来源文件：`docs/spec` 205、`docs/phase0` 8、`docs/art` 7、`docs/ROADMAP_1_0.md` 2、`docs/PUBLISHING_ART_PASS_1_0.md` 2、`docs/RELEASE_CHECKLIST_1_0.md` 1。**关键事实**：前 220 条全部写在**文件名含 `YYYY-MM-DD` 日期**的历史 spec/plan/report/prompt 文档里（如 `docs/spec/2026-06-18-phase5-mainline2-tempo-firstclear-plan.md`、`docs/phase0/p3_1_lightfoot_phase0_2026-05-23.md`、`docs/art/inner_demon_enemy_mj_prompts_2026-05-24.md`），目标多为路线 C（`ca548a3a7`）删除的旧 3v3 `lib/features`/`test/features` 文件——与 2026-08-18 拍板的五目录归档类同一性质（引用指向写作当时的仓库状态，不是文档失修）；`docs/spec` 下**无日期**的活文档（`rejected_task_registry.md`/`playability_phase2_backlog.md`/`BATTLE_ENEMY_STANDEE_COVERAGE.md`/`dispatch_templates/*`/`phase2_*_candidates_20260826.md`）死链为 0。剩 5 条写在活文档里，须真处置。用户拍板 (b)「扩归档类」；协调者把判据定为**文件名带日期**而非整目录 `docs/spec`，以免活 spec 未来的死链被藏。**不重新论证**。

## 0. 总则（硬约束）

- **语言**：commit message、代码注释、文档一律简体中文；commit message 中文动宾。
- **可写范围（白名单，精确路径）**：
  - `tools/doc_link_scan.py`
  - `tools/test_doc_link_scan.py`
  - `tools/test_doc_link_scan_gitfixture.py`
  - `tools/README.md`（只改 `:13` 单元格末尾追加一句归档规则说明 + `:14` 单元格里的用例计数）
  - `docs/ROADMAP_1_0.md`（只改 `:47` 与 `:165` 两处引用串）
  - `docs/PUBLISHING_ART_PASS_1_0.md`（只改 `:53` 与 `:248` 两处引用串）
  - `docs/RELEASE_CHECKLIST_1_0.md`（只改 `:110` 一处引用串）
  - `docs/audit/doclink_residue_disposition_2026-09-22.md`（新建，台账，≤60 行）
  - `docs/superpowers/plans/2026-09-22-codex-f-doclink-dated-archival.md`（新建，恢复点）
  - `docs/dispatch/reports/2026-09-22_codex_F_receipt.yaml`（新建，收据）
  - **其余一律禁改**，特别是 `docs/spec/**`、`docs/phase0/**`、`docs/art/**`（带日期文档的死链由规则分类，**禁止逐条改文**）、`docs/dispatch/pool/README.md`（P13 条目由协调者收账时改）、`data/numbers.yaml`、`GDD.md`、`PROGRESS.md`、`BACKLOG.md`、`CLAUDE.md`、`AGENTS.md`、`pubspec.yaml`、`lib/**`、`lib/shared/strings.dart`、`test/**`、`.gitignore`、`tools/audit/**`。协调者以 `git diff --name-only` 机器判定：出现白名单外路径整单 FAIL。若确需改白名单外文件，**停下打 `[BLOCKED]`** 写明路径与原因。
- **git 边界**：只在自己的 worktree 提交；禁 push / merge / rebase / revert / amend；禁碰 `main` 与 `codex/p2-player-flow-20260910`。
- **环境边界**：禁安装任何软件；**不跑 `flutter`/`dart`**（白名单不含任何 Dart/Flutter 文件）；不碰真实存档，不启动游戏 GUI。
- **就绪标记**：最后实质 commit S 带 `[READY]`/`[BLOCKED]` 前缀；收据放独立包装 commit R（`R^ == S`，只含收据 + 恢复点），R 消息以同样标记开头。

## 1. 目标清单

### 目标 1：`tools/doc_link_scan.py` 新增「带日期文件名 ⇒ 归档类」判据

- 现状：`tools/doc_link_scan.py:74` `ARCHIVAL_DIRS` 五目录；`:612` `elif _is_under_any_dir(ref["file"], ARCHIVAL_DIRS):` 是唯一归档分类点。
- 新规则（与目录规则**并列**，任一命中即归档类）：引用**所在文件**（`ref["file"]`，不是目标）的 **basename** 含 `\d{4}-\d{2}-\d{2}`（带短横的日期，前缀 / 中缀 / 后缀均算）⇒ 归档类。八位无横日期（如 `_20260826.md`）**不算**（这批是「候选」类活文档）；目录名里的日期不算（只看 basename）。实现为模块级常量 `ARCHIVAL_DATED_NAME = re.compile(r"\d{4}-\d{2}-\d{2}")` + 一个小函数，判据写进常量旁注释（一句话：带日期文件名=历史快照，引用指向写作当时的仓库状态；2026-09-21 用户拍板扩类）。
- `--archival` 明细输出**不改列结构**；`--json` 的 key 不增不减（下游 diff 依赖）。
- 验收（在 worktree 根跑）：`python3 tools/doc_link_scan.py` 摘要 **dead = 0**、归档类 = **1802**（1582+220）、存活 9784 / ignored 781 / md 1830 不变；把摘要原文贴进恢复点。任一数字不符 → 逐条解释（`--rows` diff），解释不了就 `[BLOCKED]`。

### 目标 2：两层测试补固定样例（防复发红线）

- `tools/test_doc_link_scan.py` 在 `:137` 「归档类单列」段末追加（沿用 `scan_fixture(..., source=...)` 体例，断言 `dead`/`archival` 计数与 `archival_rows` 目标）：
  1. `docs/spec/2026-06-18-x-plan.md`（日期前缀）内死引用 → archival 1 / dead 0；
  2. `docs/phase0/p3_1_lightfoot_phase0_2026-05-23.md`（日期后缀）→ 同上；
  3. `docs/spec/full_review_2026-07-02_followup_backlog.md`（日期中缀）→ 同上；
  4. `docs/spec/rejected_task_registry.md`（无日期，同目录）→ dead 1 / archival 0（**活 spec 不被藏**）；
  5. `docs/spec/phase2_combat_core_tuning_candidates_20260826.md`（八位无横）→ dead 1 / archival 0；
  6. `docs/2026-01-01/ROADMAP.md`（日期只在目录名）→ dead 1 / archival 0；
  7. 带日期文档里的**存活**引用仍计 alive（`tracked_targets` 给到）→ alive 1 / archival 0（规则只改死引用的去向，不动存活判定）。
- `tools/test_doc_link_scan_gitfixture.py`：真临时 git 仓加 1 例——仓里放一篇 `docs/spec/2026-06-18-x-plan.md` 引用不存在的 `lib/gone.dart`，整仓跑 `scan` 后 `dead == 0` 且 `archival == 1`（钉住规则在真实 `git ls-files` 路径上生效，不只在 mock 层）。
- 两层各自 `python3 tools/<文件>.py` 跑绿；把「Ran N tests」原文贴进恢复点（基线 19 / 20，改后应 26 / 21；不等则解释）。
- **证红（commit 后做）**：临时把 `ARCHIVAL_DATED_NAME` 判据从分类点摘掉（`remove_implementation`）→ 样例 1/2/3 与 gitfixture 新例必红；`git checkout` 还原后重跑绿。结果写收据 `break_red`（一组，方向 `remove_implementation`）。

### 目标 3：5 条活文档死链真处置（协调者已查 git 史，照办；不改其他文字）

| 文件:行 | 引用 | 事实 | 处置（改写该处文字，去反引号改纯文本 + 注明证据） |
|---|---|---|---|
| `docs/ROADMAP_1_0.md:47` | `test/tools/balance_simulator_test.dart` | 随路线 C 旧 3v3 删除（`ca548a3a7`） | 改为 `test/tools/balance_simulator_test.dart（已随路线 C ca548a3a7 移除）` 纯文本，无反引号 |
| `docs/ROADMAP_1_0.md:165` | `test/balance/battle_strategy_e2e_test.dart` | 同上 | 同上写法 |
| `docs/PUBLISHING_ART_PASS_1_0.md:53` | `assets/techniques/` | 心法 7 阶 cover 曾入库，`d3cde5a33` 清理零引用后目录已不存在 | 该句陈述已过期：改写为「assets/techniques/（心法图目录，7 张零引用 cover 已于 d3cde5a33 清理，目录现不存在）」纯文本；`assets/scenes/` 部分不动 |
| `docs/PUBLISHING_ART_PASS_1_0.md:248` | `assets/techniques/` | 同上 | 「目录存在」陈述已假：改写为「assets/techniques/：目录已于 d3cde5a33 随零引用 cover 清理移除，心法图尚未形成体系」纯文本 |
| `docs/RELEASE_CHECKLIST_1_0.md:110` | `docs/handoff/r3_visual_check_screenshots/` | 从未跟踪（视觉证据图不入库） | 改为 `docs/handoff/r3_visual_check_screenshots/（证据图不入库，从未跟踪）` 纯文本，同行其余两个反引号引用不动 |

- 原理：扫描器只提取**反引号内**与 md 链接内的路径（`tools/doc_link_scan.py:202` `_BACKTICK_PATH`），活文档里这 5 处所指对象已不存在，正确修法是让文档**不再以路径形式宣称它存在**，而不是加注后仍留反引号（08-18 台账里 `xxx.dart(已移除)` 那种自注是「接受为残余」，仍计 dead）。改完跑扫描器确认这 5 条从 dead 消失；每处只动本行、不改同行其他引用与语义。

### 目标 4：台账 `docs/audit/doclink_residue_disposition_2026-09-22.md`（≤60 行）

- 体例照 `docs/audit/doclink_residue_disposition_2026-08-18.md`。内容：① 规则变更一段（判据、动机、改前/改后四分类数字原文、影响的 220 条按来源目录计数）；② 目标 3 五条处置表（含 commit 证据）；③ 归档类里三组值得留痕的引用各一句话——`assets/audio/voice`、`assets/audio/sfx/battleDeath.mp3`、`assets/audio/LICENSES`（3 条，计划素材类，与 `docs/audio_asset_generation_guide.md` 文件级排除同构）；`assets/images/inner_demon/enemy_*.png` ×7（`docs/art/…_2026-05-24.md`，`git log --all -- 'assets/images/inner_demon'` 查是否曾入库，一句话写明）；`docs/phase0` 8 条（旧 3v3 文件 ×6 / `tools/phase0minus_probe/test/host_human_session_macos_test.dart` ×1 / `data/app.so` ×1 构建产物路径）。
- `tools/README.md:13` 单元格末尾追加一句（2026-09-22 带日期规则 + 数字 dead 225→0 / 归档 1582→1802）；`:14` 用例计数改为实测值。

## 2. 验证（全部完成后）

- `python3 tools/doc_link_scan.py` 摘要 dead = 0（原文贴恢复点与台账）。
- `python3 tools/test_doc_link_scan.py` 与 `python3 tools/test_doc_link_scan_gitfixture.py` 全绿，「Ran N tests」原文。
- `git diff --stat 8d8ae9195..S` 不含白名单外路径。
- 收据三条 `*_last_line` 写 `NOT_RUN`（本单不跑 flutter，Gate 自跑裁定）。

## 3. 禁止的修法

- 禁止把 `docs/spec` / `docs/phase0` / `docs/art` 整目录加进 `ARCHIVAL_DIRS`（会藏活 spec 的未来死链；判据必须是文件名日期）。
- 禁止把判据放在目标路径上（判据是「引用写在哪个文件里」，与既有五目录规则同向）。
- 禁止改 `EXCLUDE_FILES` / `EXCLUDE_DIRS` / 清洗正则来让 dead 归零。
- 禁止逐条改带日期文档的正文（220 条由规则分类）。
- 禁止删或放宽既有 39 例测试。

## 4. 收据与恢复点

- 按 `~/.claude/skills/afk/scripts/receipt.schema.md` 产 `docs/dispatch/reports/2026-09-22_codex_F_receipt.yaml`：`base_sha` = `8d8ae9195` 全 40 位、`head_sha` = S 全 40 位、`changed_files` = `git -c diff.renames=false diff --name-only 8d8ae9195..S` 全部、三条 last_line 写 `NOT_RUN`、`error_block_count: 0`、`break_red` 一组（`remove_implementation`）。
- 恢复点 `docs/superpowers/plans/2026-09-22-codex-f-doclink-dated-archival.md`：状态 / 最后完成 / 下一步 / 已跑验证（三条命令原文）/ 阻塞项。

## 5. [BLOCKED] 出口条件

- 基线（改动前）两层测试就红，或基线扫描 dead ≠ 225。
- 规则实装后 dead ≠ 0 或归档 ≠ 1802 且无法逐条解释。
- 目标 3 加注后仍计 dead 且 08-18 台账体例也解不掉。
- 证红不红。
- 需要改白名单外文件。

## 6. 验收方式（协调者怎么查）

- `gate.sh <worktree> 8d8ae9195 <S> --whitelist-from <本派单包> --wrap-tip <R> --skip-full`（审计单：零 `lib/`/`test/`/`data/` 改动）。
- 协调者复跑：扫描器 dead = 0 / 归档 1802；两层测试 26 / 21；自做一次 `remove_implementation` 证红；抽查 5 条活文档改行只动引用串。
