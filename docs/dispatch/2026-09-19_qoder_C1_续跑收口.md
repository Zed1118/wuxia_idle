# 派单包 C1-续 · 删除批收口（接 2026-09-19 22:47 中断）

- 执行端：qoderclicn；worktree `/Users/a10506/Qoder/2026-09-19/c1-delete-keys/wt`（分支 `qoder/c1-delete-keys-20260919`）
- 原派单包：`docs/dispatch/2026-09-19_qoder_C1_零引用key删除批.md`（**全部硬约束、白名单、禁止修法沿用**，本包只补收口步骤）
- 中断事实：目标 1–3 已 commit（`93dd15206` / `88c9606ea` / `b6f460140`），恢复点骨架未跟踪、全量测试被 harness 600s 等待上限掐断（log `../full_test.log` 停在 `12:41 +2737 -1`，红条 = `test/features/activity/durable_activity_automation_service_test.dart (setUpAll)`）。

## 只做这几步（顺序）

1. **不要再改 `data/numbers.yaml` / `tools/`**；先 `git status` 确认只有恢复点未跟踪。
2. 先单跑那条红：`flutter test --no-pub test/features/activity/durable_activity_automation_service_test.dart`。绿 → 记为「争抢期 setUpAll 超时，单跑绿」；红 → 贴原文、打 `[BLOCKED]`（删 key 不应影响它，红即说明有隐藏消费或环境问题，不得自行修）。
3. **前台**跑全量：`flutter test --no-pub 2>&1 | tee /Users/a10506/Qoder/2026-09-19/c1-delete-keys/full_test_2.log`，**不要放后台**（harness 600s 会掐）。取 reporter 末行原文与 `grep -c '^\[E\]'`。
4. 填恢复点两处占位（`FULL_TEST_PENDING` / `ERROR_BLOCKS_PENDING`）、把步骤 2 的结果写进「已跑验证」，与恢复点一起做最后实质 commit **S**，消息以 `[READY]` 开头（全量绿）或 `[BLOCKED]` 开头。
5. 按 `~/.claude/skills/afk/scripts/receipt.schema.md` 写 `docs/dispatch/reports/2026-09-19_qoder_C1_receipt.yaml`：`base_sha` = `36781fbbf` 全 40 位、`head_sha` = S 全 40 位、`changed_files` = `git diff --name-only 36781fbbf..S` 全部、三条 last_line 真跑原文、`error_block_count`、`break_red` 留空、`audit_verification` 按 schema 固定命令算 `diff_check_exit` / `patch_sha256`。单独提交为包装 commit **R**（`R^ == S`，只含收据），消息同样以 `[READY]`/`[BLOCKED]` 开头。
6. 结束时 `git status` 必须干净；禁 push / merge / rebase / revert。
