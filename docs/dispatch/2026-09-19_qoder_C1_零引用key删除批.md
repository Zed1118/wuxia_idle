# 派单包 C1 · numbers.yaml 零引用 key 删除批（用户 2026-09-19 拍板「按推荐执行」）

- 执行端：qoderclicn（Qwen3.8-Max）
- worktree：`/Users/a10506/Qoder/2026-09-19/c1-delete-keys/wt`（分支 `qoder/c1-delete-keys-20260919`，基线 `36781fbbf`；已预热：libisar.dylib / pub get / build_runner）
- 派单基线（Gate 用）：`base_sha = 36781fbbf`
- 拍板依据：`docs/audit/numbers_unused_keys_round2_2026-09-19.md`（下称「对照单」）§2 的 A / B / D / G 段「删」+ C 段两叶「留（头注令牌）」。**用户已拍板，本单不再重新论证留删**。

## 0. 总则（硬约束）

- **语言**：commit message、文档一律简体中文；commit message 中文动宾；删配置字段的 commit 标题前加 `[schema]`，改 GDD 的 commit 标题前加 `[GDD]`。
- **可写范围（白名单，精确路径）**：
  - `data/numbers.yaml`（只删对照单点名的 26 叶及其专属头注；给 2 叶改头注令牌；**不改任何数值、不动其他 key、不加新 key**）
  - `data/skills.yaml`（仅 `:8-12` 头注里对 `reference_multipliers` 的引用文字改写，不动任何招式数据）
  - `GDD.md`（仅 §5.3 若仍存在分阶参考区间表/数字则同步为「全局 ≤8,000 单线」口径；无则不改）
  - `tools/audit/numbers_key_usage.py`（只删除对已删 key 的登记项：`:39` `skills.reference_multipliers.` 组、`:346` / `:450` 终端名单里的 `last_updated` / `skill_multiplier_added` / `apply_cultivation_multiplier` / `apply_school_counter`；**不改判零口径、不删其他检查**；若某登记项删除后脚本自检失败，写明原因并回退该项）
  - `docs/superpowers/plans/2026-09-19-qoder-c1-delete-keys.md`（新建，恢复点）
  - `docs/dispatch/reports/2026-09-19_qoder_C1_receipt.yaml`（新建，收据）
  - **其余一律禁改**，特别是 `lib/**`、`test/**`、`tools/audit/numbers_unused_keys_review.py`（B2 冻结工具，只读）、`docs/audit/**`、`PROGRESS.md`、`BACKLOG.md`、`CLAUDE.md`、`AGENTS.md`、`data_schema.md`、`pubspec.yaml`、`lib/shared/strings.dart`。协调者以 `git diff --name-only` 机器判定：出现白名单外路径整单 FAIL。
- **git 边界**：只在自己的 worktree 提交；禁 push / merge / rebase / revert；禁碰 `main` 与 `codex/p2-player-flow-20260910`。
- **环境边界**：禁安装任何软件（含 brew / pub global）。
- **不碰真实存档**（`~/Library/Containers/com.pen.wuxia.wuxiaIdle/` 只读），不启动游戏 GUI。
- **就绪标记**：最后实质 commit S 带 `[READY]`/`[BLOCKED]` 前缀；收据放独立包装 commit R（`R^ == S`，只含收据），R 消息以同样标记开头。
- **收据自引用口径**：`head_sha` = S；收据本身只在 R 里。

## 1. 背景（一句话）

对照单实测 `numbers.yaml` 59 个零引用叶，用户拍板：A meta / B combat 伪开关 / D reference_multipliers / G inheritance 三叶共 **26 叶删除**；C 段 `max_level_formula` 与 `new_owner_retention` 留但头注令牌改为扫描器认的 `UNUSED(`。

## 2. 目标清单（每目标收口 commit 一次）

### 目标 1：删 26 叶（`[schema]`）

按对照单 §2 逐叶删除（行号以 `36781fbbf` 的 `data/numbers.yaml` 为准，动手前用 `grep -n` 重定位）：
- A：`meta.last_updated`（1 叶）。
- B：`combat.damage_formula.skill_multiplier_added`（1 叶）+ 其专属 `UNUSED` 头注行；`combat.final_damage_formula` 整块（5 叶）+ 其两行 `UNUSED`/`⚠` 头注；**保留** `# --- 最终伤害公式（GDD §5.4）---` 与其下一行公式说明注释。
- D：`skills.reference_multipliers.power_skill.tier_*_range` 4 条 + `ultimate.tier_*_range` 4 条（16 叶）+ 该段 `UNUSED` 头注两行；**保留** `normal_attack` 与 `joint_skill` 子块（它们不在删单里）。
- G：`inheritance.unlock_rules.can_pass_legacy_at`、`inheritance.heritage_items.auto_buff_internal_force_max`、`inheritance.heritage_items.resonance_retention`（3 叶）+ 各自专属 `UNUSED` 头注行；`can_pass_legacy_at` 行尾那段 Ch19–21 史料注释一并删除（活配置在 `ascension.unlock_triggers.required_realm`，头注 `:1523-1526` 已说明，删后在 `unlock_rules:` 上方留一行 `# can_pass_legacy_at 已于 2026-09-19 删除:飞升门槛唯一事实源 = ascension.unlock_triggers.required_realm`）。
- 删除后 `python3 -c "import yaml;yaml.safe_load(open('data/numbers.yaml'))"` 必须通过（系统 python3 若无 yaml 模块，改用 `dart run` 加载测试，见目标 4）。

### 目标 2：C 段两叶头注令牌（不删）

- `equipment.enhancement.max_level_formula` 上一行加：`# UNUSED(2026-09-19 二轮·头注): max_level_formula 为公式名文档锚,上限规则写死 enhancement_service._enhanceLevelCap(min(49, absoluteLevel))。`
- `equipment.resonance.new_owner_retention` 现有两行 `⚠️ 未消费…` 头注：首行改为以 `# UNUSED(2026-06-16 审计 M7·头注):` 开头，其余原文保留。
- **不动** `success_formula`（C2 单接线）。

### 目标 3：文档与工具同步

- `data/skills.yaml:8-12` 头注若引用 `numbers.yaml skills.reference_multipliers`，改写为指向 `combat.red_lines.skill_power_multiplier_max` 全局单线（只改注释）。
- `GDD.md` §5.3：`grep -n -E "1,?000.{0,6}1,?800|2,?500.{0,6}3,?500|6,?500.{0,6}8,?000" GDD.md`，命中即同步为全局 ≤8,000 单线口径并单独 `[GDD]` commit；零命中则在恢复点记「GDD §5.3 无分阶区间，无需同步」。
- `tools/audit/numbers_key_usage.py`：删对已删 key 的登记项（见 §0 白名单说明）；`python3 tools/audit/numbers_key_usage.py --baseline 36781fbbf --format json > /Users/a10506/Qoder/2026-09-19/c1-delete-keys/usage_after.json` 退出 0。

### 目标 4：验证

- 计数守恒（硬指标）：`usage_after.json` 里 `verdict == 零引用` 叶数 = **33**（59 − 26），`unused_marked=true` 叶数 = **32**（56 − 26 + 2）；不等则逐叶列差异，不能解释就 `[BLOCKED]`。
- `flutter analyze --no-pub lib test tool` 0 issue；`dart format --output=none --set-exit-if-changed lib test docs` 0 changed。
- targeted：`flutter test --no-pub test/data/ test/combat/damage_calculator_test.dart test/features/equipment/ test/features/inheritance/ test/features/ascension/`（逐目录确认 `All tests passed!`）。
- 全量：`flutter test --no-pub`，收据 `full_test_last_line` 写 reporter 末行原文、`error_block_count` 写 `[E]` 块数。
- 破坏证红：本单为配置删除，无新增断言，收据 `break_red` 留空、改填 `audit_verification`（按 schema 固定命令算 `diff_check_exit` 与 `patch_sha256`）。

## 3. 禁止的修法

- 禁止用「注释掉」代替删除（叶必须消失，扫描器按 yaml 解析）。
- 禁止顺手删任何不在 26 叶清单里的 key，包括同段其他 `UNUSED` 叶（E/F/H 段由 C2 单接线）。
- 禁止改任何数值、禁止改 `numbers_unused_keys_review.py`、禁止放宽 `numbers_key_usage.py` 判零口径。
- 禁止改 `lib/` / `test/`：若删 key 导致任何测试红，说明该 key 并非零引用——停下、`[BLOCKED]`、把红测原文写进恢复点。

## 4. 收据与恢复点

- 按 `~/.claude/skills/afk/scripts/receipt.schema.md` 产 `docs/dispatch/reports/2026-09-19_qoder_C1_receipt.yaml`；`changed_files` 只列本单实际改动；三条 last_line 写真跑原文（本单白名单含 `data/`，**不得写 NOT_RUN**）。
- 恢复点 `docs/superpowers/plans/2026-09-19-qoder-c1-delete-keys.md`：状态 / 最后完成 / 下一步 / 已跑验证 / 阻塞项。

## 5. [BLOCKED] 出口条件

- 删 key 后任何 `flutter test` 红（说明有隐藏消费）。
- 计数不是 33 / 32 且无法逐叶解释。
- `numbers_key_usage.py` 删登记项后自检失败且无法回退到通过。

## 6. 验收方式（协调者怎么查）

- `gate.sh <worktree> 36781fbbf <S> --whitelist-from <本派单包> --wrap-tip <R>`。
- 协调者复跑 `numbers_key_usage.py --baseline 36781fbbf` 对撞 33 / 32；`git diff 36781fbbf..<S> -- data/numbers.yaml | grep '^+' | grep -v '^+++'` 只允许出现头注/指针注释行，**不允许任何 `key: value` 新增行**；`grep -c` 抽检 5 个已删 key 在 `data/ lib/ test/ tools/` 为 0。
