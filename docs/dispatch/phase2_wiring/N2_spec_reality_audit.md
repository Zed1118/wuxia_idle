# 批 N2:二阶段方案 ↔ 生产实况一致性对账(2026-08-26)

分支 `codex/p2-spec-reality-audit-20260826`,基线 **`0378df73`**(= main),worktree
`/Users/a10506/Desktop/Projects/挂机武侠-p2-spec-audit`。**只读审计单:不改任何生产代码或测试。**

## 为什么做这件事

今天用同样的方法**只对过一条**,就抓到一个已进 main 的实质偏差:
生产 `stage_01_03` 攻击令牌总量 6,而方案 `/Users/a10506/Desktop/二阶段优化方案.md:1025` 写的是「攻击令牌 2–4」;
该偏差还被 `test/data/phase2/ch1_production_catalog_test.dart:187-190` 反向钉成断言,
并在 `test/data/phase2/ch1_candidate_combat_catalog_test.dart` 里配了一句**查无实据的「用户拍板」注释**。

方案全文 1500+ 行、拍板点数十个,**只对过一条**。本单把这件事做完。

## 任务

逐条把方案里**可验证的硬断言**对到生产实况上,产出偏差清单。

「可验证的硬断言」= 数值区间 / 数量上限 / 明确的行为规定 / 明确的「必须 / 不得」句 / 字段与配置名。
纯方向性描述(如「提升打击感」)**不在本单范围**,不要硬凑。

对每一条:
1. 抄**方案原文** + 精确行号(现读,**不要凭印象**——今天有执行端把 `:1526` 写错、实际在 `:31`/`:1540` 的实录);
2. 现 grep 生产实况(`lib/` / `data/` / `test/`),给 `file:line`;
3. 判定:`一致` / `偏差` / `未实装` / `无法判定`;
4. 偏差项标严重度:**S1** 已进 main 且玩家可见 / **S2** 已进 main 不可见 / **S3** 仅在分支 / **S4** 文档间互相矛盾。

## 产出

`docs/audit/phase2_spec_reality_audit_20260826.md`(**≤150 行**),含:
- 头部写明**审计基线 sha** `0378df73` 与方案文件的 mtime/行数(并发环境下工作区是移动靶,禁止基于工作区状态下结论);
- 覆盖率:方案里被判定为「可验证硬断言」的总条数 / 已对账条数;**若未覆盖全部,明写漏了哪些章节及原因**;
- 偏差清单表:方案行号 + 原文摘录 + 生产 `file:line` + 判定 + 严重度;
- 「一致」的条目**只给计数与章节分布**,不要逐条铺开(控体量)。

## 硬约束

- **只读**:除上述一个 `.md` 外,不得新增/修改任何文件。`git diff --name-only` 应只有它。
- 禁区:`data/numbers.yaml` / `GDD.md` / `PROGRESS.md` / `lib/shared/strings.dart` / `pubspec.yaml`。
- 禁 push / 禁 merge / 禁碰 main / 禁 revert。commit 中文动宾,tip `[READY]`/`[BLOCKED]`,工作区干净。
- **不得自行开实装单、不得顺手修任何偏差**。偏差入清单等用户拍板 —— 这是本单与实装单的分界线。
- **不得把「查不到」写成「一致」**。查不到就写 `无法判定` 并说明查了什么。
- 引用一律现 grep 带 `file:line`;数字实测禁估算。

## [BLOCKED] 出口条件

- 方案文件不可读或与本单给的路径不符;
- 某条断言的判定需要你改代码才能确认(那属实装,不在本单);
- 覆盖不完但你判断继续做会牺牲准确率 —— 宁可少覆盖也不要糊判定,**把已完成部分交出来并写明覆盖率**。

## 协调者怎么验收

我会**随机抽 3 条**自己复核方案行号与生产 `file:line`——今天执行端审计 6 条里 3 条引用有问题,所以这是重点。
出现代码/测试改动 = 直接打回。把「查不到」写成「一致」= 直接打回。
清单里出现没有 `file:line` 的判定 = 直接打回。
