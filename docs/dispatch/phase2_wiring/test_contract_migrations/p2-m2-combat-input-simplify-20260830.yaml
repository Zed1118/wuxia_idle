schema_version: 1
ticket: p2-m2-combat-input-simplify-20260830
base_sha: ef6a6802dcc61bf4cbf2bb9a9463010e5004192c
head_sha: b653852e28607129b52fa485d1869857c17072c5
summary: 玩家普攻从直刺、横扫、进步斩三段链迁移为无攻击位移的单段远程掌风，玩家防御仍仅保留 Space 闪避，并消费 macOS 移动键重复事件；旧断言锁定的正是本单要删除的玩家产品行为。

migrations:
  - deleted: test('steady guard emits shield outside burst and dodge inside burst', () {
    replacement: steady guard no longer emits shield or parry and keeps dodge for charge
    reason: bot 不得继续使用人类玩家已移除的护盾和化解；新用例锁定常态不出防御、蓄力攻击仍可闪避。
  - deleted: expect(safe.defenseAction, Phase0aDefenseAction.shield);
    replacement: expect(safe.defenseAction, isNull);
    reason: 常态自动护盾是被删除的产品行为，替代断言明确要求 bot 不发动主动防御。
  - deleted: expect(burst.defenseAction, Phase0aDefenseAction.dodge);
    replacement: expect(burst.defenseAction, Phase0aDefenseAction.dodge);
    reason: 该断言仅因用例标题与前置断言迁移而重写，新契约仍原样锁定蓄力攻击时闪避。

  - deleted: testWidgets('正式战斗进步斩插值期间 camera 每帧不超过普通移动一拍', (tester) async {
    replacement: 正式战斗进步斩期间角色在屏幕上前冲而非被镜头钉住
    reason: 旧用例只限制镜头单帧增量，仍允许镜头完全追平角色；新用例直接断言角色屏幕前冲和背景低位移。
  - deleted: expect((current - previous).distance, greaterThan(0));
    replacement: expect(totalForwardPixels, greaterThan(0));
    reason: 旧断言强制进步斩期间镜头每帧都动，正会制造拉扯；新断言改为角色屏幕脚点真实前进。

  - deleted: testWidgets('E 从真实战斗屏入口进入 shield 并显示护盾反馈', (tester) async {
    replacement: $label 从真实战斗屏入口不再启动防御
    reason: E 护盾已从玩家产品输入删除，新用例断言无事件、无位移且无任何防御状态。
  - deleted: testWidgets('F 从真实战斗屏入口进入 parry 并点亮化解反馈', (tester) async {
    replacement: $label 从真实战斗屏入口不再启动防御
    reason: F 化解已从玩家产品输入删除，新用例断言无事件、无位移且无任何防御状态。
  - deleted: testWidgets('Z 从真实战斗屏入口进入 dodge 并显示位移反馈', (tester) async {
    replacement: $label 从真实战斗屏入口不再启动防御
    reason: Z 备用闪避入口已删除，新用例锁定它不产生任何 defense started 事件。
  - deleted: expect(started.action, Phase0aDefenseAction.shield);
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 护盾不再能从生产键盘启动，替代断言要求对应按键的防御事件集为空。
  - deleted: expect(started.windowTicks, harness.tuning.shieldDurationTicks);
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 玩家键盘已无护盾窗口，新契约直接要求不产生防御开始事件。
  - deleted: expect(started.shieldAbsorption, harness.tuning.shieldAbsorption);
    replacement: expect(harness.controller.state.player.shieldRemaining, 0);
    reason: 不再验收吸收量，而是验收按键后护盾状态仍为零。
  - deleted: expect(started.action, Phase0aDefenseAction.parry);
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 化解不再能从生产键盘启动，替代断言要求对应按键的防御事件集为空。
  - deleted: expect(started.windowTicks, harness.tuning.parryWindowTicks);
    replacement: expect(harness.controller.state.player.parryTicksRemaining, 0);
    reason: 玩家键盘已无化解窗口，新契约要求按键后 parry ticks 仍为零。
  - deleted: expect(started.action, Phase0aDefenseAction.dodge);
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 该删除行属于旧 Z 闪避用例；新 Z 契约是不得产生防御事件，Space 的闪避行为由独立用例保留。
  - deleted: expect(started.fromPosition, before);
    replacement: expect(harness.controller.state.player.position, before.position);
    reason: 旧 Z 用例验证闪避起点，新 Z 禁用契约要求玩家位置完全不变。
  - deleted: expect(started.toPosition, after);
    replacement: expect(harness.controller.state.player.position, before.position);
    reason: 旧 Z 用例验证闪避终点，新 Z 禁用契约要求玩家位置完全不变。
  - deleted: expect(started.windowTicks, harness.tuning.dodgeIframeTicks);
    replacement: expect(harness.controller.state.player.dodgeTicksRemaining, 0);
    reason: 旧 Z 用例验收无敌帧，新 Z 禁用契约要求 dodge ticks 仍为零。
  - deleted: expect((after - before).length, harness.tuning.dodgeDistance);
    replacement: expect(harness.controller.state.player.position, before.position);
    reason: 旧 Z 用例验收闪避距离，新 Z 禁用契约要求零位移。

  - deleted: testWidgets('$label 从聚焦战斗屏进入同一闪避路径', (tester) async {
    replacement: Space 是聚焦战斗屏唯一闪避入口
    reason: 旧循环同时承认 Z 和 Space，与新产品语义相反；新契约分开验证 Space 成功和 E/F/Z 禁用。
  - deleted: testWidgets('三项防御状态在 $viewport 初始均显示可用', (tester) async {
    replacement: $viewport 只显示 Space 闪避状态
    reason: HUD 产品契约从三项防御迁移为仅 Space 闪避，新用例同时要求 shield/parry 状态节点不存在。
  - deleted: testWidgets('任一防御动作进入共享 CD 后三项分别显示冷却秒数', (tester) async {
    replacement: Space 闪避后只有闪避状态显示冷却
    reason: 守势和化解 HUD 已删除，新用例仅验收闪避冷却并要求另两状态不存在。
  - deleted: expect(status, findsOneWidget);
    replacement: expect(status, findsOneWidget);
    reason: 该断言从三项循环迁移到唯一 dodge status，新用例仍原样保证节点存在。
  - deleted: expect(tester.widget<Text>(status).data, UiStrings.skillReady);
    replacement: expect(tester.widget<Text>(status).data, UiStrings.skillReady);
    reason: 该断言从三项循环迁移到唯一 dodge status，新用例仍原样保证初始可用文案。
  - deleted: expect(tester.getSize(status).width, greaterThan(0));
    replacement: expect(tester.getSize(status).width, greaterThan(0));
    reason: 该断言从三项循环迁移到唯一 dodge status，新用例仍原样保证可见宽度。
  - deleted: expect(tester.getSize(status).height, greaterThan(0));
    replacement: expect(tester.getSize(status).height, greaterThan(0));
    reason: 该断言从三项循环迁移到唯一 dodge status，新用例仍原样保证可见高度。
  - deleted: expect(tester.widget<Text>(defenseStatus(action)).data, expected);
    replacement: expect(tester.widget<Text>(defenseStatus('dodge')).data, expected);
    reason: 共享三项冷却 HUD 已删除，新断言仅锁定唯一 dodge status 的冷却文案。

  - deleted: expect(
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 旧多行防御断言片段属于已删除的 E/F/Z 启动或三项 HUD 契约，新契约以无防御事件为核心替代。

  - deleted: test('生产 player mapping 只在 reducer 接受普攻后按剑形态三段链推进', () {
    replacement: test('生产 player mapping 连续普攻始终无链段且玩家零攻击位移', () {
    reason: 玩家生产路径不再注入三段链，新用例锁定连续普攻均无链段和攻击位移。
  - deleted: expect(mapping.playerAdapter.basicAttackChain, swordBasicAttackChain);
    replacement: expect(mapping.playerAdapter.basicAttackChain, isNull);
    reason: 旧断言强制生产接入三段链，与用户要求的单段远程普攻相反。
  - deleted: expect(flow.state.player.basicAttackSegmentIndex, 1);
    replacement: expect(flow.state.player.basicAttackSegmentIndex, 0);
    reason: 无链段普攻不再推进三段索引，新断言锁定索引保持初始值。
  - deleted: expect(flow.state.player.basicAttackSegmentIndex, 1);
    replacement: expect(flow.state.player.basicAttackSegmentIndex, 0);
    reason: 连续无链段普攻同样不得推进三段索引。
  - deleted: expect(segmentIds, [...swordBasicAttackSegmentIds, 'sword_thrust']);
    replacement: expect(attackSegments, List<BasicAttackSegment?>.filled(4, null));
    reason: 旧断言验收三段循环，新断言验收四次被接受的普攻都没有链段身份。

  - deleted: test('进步斩出手拍不叠加普通移动且下一拍恢复 held movement 语义', () {
    replacement: test('单段远程普攻与 held movement 同拍兼容且不附加攻击位移', () {
    reason: 生产玩家已无进步斩，新用例锁定攻击同拍只有普通移动且下一拍继续移动。
  - deleted: expect(advancing.advanceDistance, 120);
    replacement: expect(attackTick.state.player.basicAttackSegmentIndex, 0);
    reason: 单段远程普攻没有 advance，替代断言锁定无链索引状态。

  - deleted: group('玩家攻击分型:近战墨痕 / 远程掌风', () {
    replacement: group('玩家单段远程普攻', () {
    reason: 玩家不再按近远距离切换两种攻击身份，统一为远程掌风。
  - deleted: testWidgets('真实 debug 生产链遍历直刺、横扫、进步斩三种墨痕', (tester) async {
    replacement: testWidgets('真实 debug 生产链连续命中始终无链段且攻击不附加位移', (tester) async {
    reason: debug 玩家路径与生产语义同步，不再遍历三段链。
  - deleted: expect(seen, swordBasicAttackSegmentIds.toSet());
    replacement: expect(hitCount, 4);
    reason: 旧断言要求看到三种链段，新断言只要求连续四次单段远程命中完成。
  - deleted: testWidgets('整局进攻中两种 VFX 均出现且同拍互斥', (tester) async {
    replacement: testWidgets('连续四次玩家命中只出现远程掌风，不再出现近战墨痕', (tester) async {
    reason: 玩家攻击表现从近战墨痕与掌风双分型迁移为唯一掌风。
  - deleted: expect(find.byKey(actionKey('player')), findsOneWidget);
    replacement: expect(find.byKey(actionKey('player')), findsOneWidget);
    reason: 该断言因所属用例迁移而重写，仍保留玩家动作可见契约。
  - deleted: expect(find.byKey(meleeSlashKey), findsOneWidget);
    replacement: expect(find.byKey(meleeSlashKey), findsNothing);
    reason: 单段远程普攻任何命中都不得生成近战墨痕。
  - deleted: expect(find.byKey(palmTrailKey), findsNothing);
    replacement: expect(find.byKey(palmTrailKey), findsOneWidget);
    reason: 玩家每次命中均统一生成远程掌风轨迹。
  - deleted: expect(trailSeen, isTrue, reason: 'fixture 必含一次玩家远距命中');
    replacement: expect(trailSeen, isTrue, reason: 'fixture 必含玩家远距命中');
    reason: 用例从双分型迁移为统一掌风，仍保留至少一次掌风命中的夹具守卫。
  - deleted: expect(slashSeen, isTrue, reason: 'fixture 必含一次玩家近距命中');
    replacement: expect(playerHitCount, greaterThanOrEqualTo(4));
    reason: 近距墨痕产品语义已删除，替代断言保证连续命中样本充足。

  - deleted: test('玩家近程命中产生双弧墨痕,远程命中产生掌风且互斥', () {
    replacement: test('玩家统一远程普攻在近距与远距命中都只产生掌风', () {
    reason: 近距与远距不再切换表现，两者均须生成掌风且没有墨痕。
  - deleted: expect(trails, hasLength(1));
    replacement: expect(trails, hasLength(2));
    reason: 旧契约只有远距命中生成掌风，新契约要求近距和远距各生成一条。
  - deleted: expect(trails, hasLength(1));
    replacement: expect(trails, hasLength(2));
    reason: critical 分支同步迁移为近距和远距都生成掌风。
  - deleted: expect(trails.single.actorId, 'player');
    replacement: expect(trails.map((entry) => entry.actorId), everyElement('player'));
    reason: 掌风从单条变为两条，替代断言逐条锁定来源均为玩家。
  - deleted: expect(trails.single.targetId, 'far');
    replacement: expect(trails, hasLength(2));
    reason: 掌风不再只有 far 目标，替代断言锁定近远两个样本都产生轨迹。
  - deleted: expect(slashes, hasLength(1));
    replacement: expect(slashes, isEmpty);
    reason: 单段远程普攻不再产生任何近战墨痕。
  - deleted: expect(slashes, hasLength(1));
    replacement: expect(slashes, isEmpty);
    reason: critical 分支同样不得恢复近战墨痕。
  - deleted: expect(slashes.single.actorId, 'player');
    replacement: expect(slashes, isEmpty);
    reason: 近战墨痕集合已为空，不再存在 actor 归属断言。
  - deleted: expect(slashes.single.targetId, 'near');
    replacement: expect(slashes, isEmpty);
    reason: 近距目标也使用掌风，不再产生 slash target。
  - deleted: expect(slashes.single.anchor, const ArenaVector(20, 0));
    replacement: expect(slashes, isEmpty);
    reason: 近战墨痕 anchor 产品语义随墨痕删除。
  - deleted: expect(slash.isCritical, isTrue);
    replacement: expect(trail.isCritical, isTrue);
    reason: 暴击表现契约从近战墨痕迁移到唯一远程掌风。
  - deleted: test('双弧墨痕/掌风坐标 event-first,与同步位置无关', () {
    replacement: test('统一远程掌风坐标 event-first,与同步位置无关', () {
    reason: event-first 坐标契约保留，但唯一输出类型变为远程掌风。
  - deleted: expect(slashes.single.anchor, const ArenaVector(510, 0));
    replacement: expect(slashes, isEmpty);
    reason: 统一远程表现后没有 slash anchor，替代断言明确集合为空。
  - deleted: expect(trails.single.source, const ArenaVector(0, 0));
    replacement: expect(trails[1].source, const ArenaVector(0, 0));
    reason: 掌风样本从一条变为近远两条，远距样本继续验证 event-first source。
  - deleted: expect(trails.single.vfxTarget, ArenaVector(far, 0));
    replacement: expect(trails[1].vfxTarget, ArenaVector(far, 0));
    reason: 远距掌风继续使用事件目标坐标，只调整为两条列表中的远距项。

  - deleted: expect(mixedHost.flow.state.player.position.x, greaterThan(520));
    replacement: expect(mixedHost.flow.state.player.position.x, 511);
    reason: 取消进步斩位移后混合帧只有普通移动 21，替代断言锁定不会因攻击跨过 checkpoint。

  - deleted: expect(
    replacement: expect(hit.basicAttackSegment, isNull);
    reason: 旧三段链多行断言迁移为每次命中均无链段身份。
  - deleted: expect(
    replacement: expect(hit.basicAttackSegment, isNull);
    reason: 旧进步斩位移多行断言迁移为单段远程命中无链段。
  - deleted: expect(
    replacement: expect(hit.basicAttackSegment, isNull);
    reason: 旧双 VFX 多行断言迁移为统一远程命中无链段。
  - deleted: expect(
    replacement: expect(hit.basicAttackSegment, isNull);
    reason: 旧 checkpoint 三段断言迁移为统一远程命中无链段。
  - deleted: expect(
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 旧多行防御断言片段属于已删除的 E/F/Z 启动或三项 HUD 契约，新契约以无防御事件为核心替代。
  - deleted: expect(
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 旧多行防御断言片段属于已删除的 E/F/Z 启动或三项 HUD 契约，新契约以无防御事件为核心替代。
  - deleted: expect(
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 旧多行防御断言片段属于已删除的 E/F/Z 启动或三项 HUD 契约，新契约以无防御事件为核心替代。
  - deleted: expect(
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 旧多行防御断言片段属于已删除的 E/F/Z 启动或三项 HUD 契约，新契约以无防御事件为核心替代。
  - deleted: expect(
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 旧多行防御断言片段属于已删除的 E/F/Z 启动或三项 HUD 契约，新契约以无防御事件为核心替代。
  - deleted: expect(
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 旧多行防御断言片段属于已删除的 E/F/Z 启动或三项 HUD 契约，新契约以无防御事件为核心替代。
  - deleted: expect(
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 旧多行防御断言片段属于已删除的 E/F/Z 启动或三项 HUD 契约，新契约以无防御事件为核心替代。
  - deleted: expect(
    replacement: expect(events.whereType<Phase0aDefenseStarted>(), isEmpty);
    reason: 旧多行防御断言片段属于已删除的 E/F/Z 启动或三项 HUD 契约，新契约以无防御事件为核心替代。
