# 归档记录 · U05 长周期经济模拟分支(2026-08-28)

## 结论

`codex/p2-u05-long-horizon-economy-20260828` @ `3469ad37 [READY] 完成长周期经济模拟`
**归档不合入**。分支保留在本地,不删除,不进入当前候选链。

## 归档理由

1. **不在任何派单包范围内**。B3 派单包(`B3_presentation_cleanup_dual_lane_20260827.md`,冻结于 main `cb6ebfbc`)
   §3 只列 A1/A2/B1/B2/B3 五件活,均为表现层与 CI 口径,零经济模型。本分支是执行端自行开的第六件活。
2. **基线错误**。它基于 `b3a1b1cf`,不是派单包 §0 指定的唯一基线 `6d59c895`。
3. **与已合入分支存在互斥文件**。本分支与 `codex/p2-b1-n14-replay-guard-20260827` 同为 `85ee8483` 的兄弟,
   两边各自持有一份 `test/features/mainline/application/phase2_same_core_reward_evidence_test.dart`
   (本分支 777 行 / n14 分支 792 行),内容已分叉。n14 那份已于 2026-08-28 合入候选链,
   本分支若直接合入会覆盖或冲突。

## 事实核对(2026-08-28 实测)

- 相对自身 base `b3a1b1cf`:`+1614` 行,4 个 commit。
- `lib/` 零改动;禁区五文件零 diff。
- 净增内容 = 420 行经济模型 + 221 行测试 + 文档。
- `-4898` 那个数字是拿它跟候选链对 diff 得到的基线分叉伪像,不是删除量。

## 复活前置条件(缺一不可)

1. 执行端先把本分支的 `phase2_same_core_reward_evidence_test.dart`(777 行)与
   已入链的 n14 版本(792 行)收敛成一份,以入链版本为准。
2. 重新基于当时的候选链 tip 出分支,不再用 `b3a1b1cf`。
3. 走正规派单:先出派单包冻结到 main,再执行,再独立 Gate。

在这三条满足之前,本分支的经济模型内容不进任何合并。
