# 批 T2:攻击令牌在 ≤4 约束下重跑候选(2026-08-26)

分支 `codex/p2-token-candidate-rerun-20260826`,基线 `03275d2c`(= main),worktree
`/Users/a10506/Desktop/Projects/挂机武侠-p2-token-recand`(已预热)。

**本单只产候选与证据,不改生产配置。** 最终选哪组由用户拍板,你**不得**替他选、更不得落地。

## 为什么要重跑

原候选表 `docs/spec/phase2_combat_core_tuning_candidates_20260826.md:59-77` 给了三组:

| | A | B | C |
|---|---:|---:|---:|
| melee/ranged/charge/support | 1/1/1/1 | 2/2/1/1 | 3/1/2/1 |
| 总和 | **4** | **6** | **7** |
| 总授予率 | 17.28% | 27.03% | 32.06% |

但方案 `/Users/a10506/Desktop/二阶段优化方案.md:1025` 对 `stage_01_03` 写的是**攻击令牌 2–4**。
**B 和 C 都出界**,说明当初生成候选时没有把 2–4 这个约束喂进生成器;
A 之所以合规,只因为它是「当前生产锚点」(文档 `:61` 原文「A 锚定当前生产 `EC`」),
**不是因为它在 2–4 空间里被优化过**。

同一份表还提示了改进方向:三组里 `support` 恒为 1,授予率却高达 **54.17%**——明显不是瓶颈;
而 `melee` 在 A 的 1 令牌下只有 **13.30%**。所以 ≤4 空间里很可能存在比 `1/1/1/1` 参与感更好的分配。

## 任务

**在总和 ∈[2,4] 的约束下穷举组合,用现有台架跑同口径指标,产出候选表 + 你的推荐。**

- 组合空间很小,**穷举、不要抽样**。四类各自的取值下界由你从生产语义确定
  (例如某类为 0 是否会导致该类敌人完全不行动 —— 这点必须实测确认后写进报告,不要凭猜排除)。
- **口径必须与原表完全一致**:同一台架、同样「300 批 × 每批 12 请求」、同样的指标列
  (平均授予/批、总授予率、平均连续拒绝批数、四类各自授予率)。口径不一致的数字无法与 A/B/C 对照,等于白跑。
- 台架现成:`test/tuning/phase2_combat_core_tuning_candidates_test.dart`。
  **先读懂它现在怎么跑 TOKEN 那一段再动**,不要另起一套模拟。

## 产出

1. `docs/spec/phase2_token_budget_candidates_le4_20260826.md`(新文件,**≤120 行**):
   - 穷举出的全部合规组合及其指标表(与原表同列,便于横向对照);
   - 明确标出 `1/1/1/1`(= 现候选 A)在表中的位置,作为基准行;
   - **你的推荐一行 + 一句理由**,理由必须指向表里的具体数字;
   - 若某些组合被排除(如某类为 0 导致该类敌人不行动),写明**实测依据**,不写「显然」。
2. 台架侧的改动:允许在 `test/tuning/phase2_combat_core_tuning_candidates_test.dart` 增加
   跑穷举所需的用例/辅助函数,但**不得修改或删除既有 A/B/C 的用例与断言**
   (它们是原表数字的复现依据,动了就无法对照)。

## 硬约束

- **禁止改任何生产配置**:`data/combat/encounters/**` 一个字不许动。本单是分析单。
- 禁区:`GDD.md` / `PROGRESS.md` / `data/numbers.yaml` / `lib/shared/strings.dart` / `pubspec.yaml`。
- **不碰 `decision_registry.yaml`**(另有 T1 单在写它,撞车即打回)。
- 不碰姿态相关文件、不碰 `defenseBreak`。
- 禁 push / 禁 merge / 禁碰 main / 禁 revert;commit message 中文动宾;tip 前缀 `[READY]`/`[BLOCKED]`;工作区干净。
- 数字**一律实测**,禁估算、禁从原表外推。引用代码现 grep 带 `file:line`。

## 必须跑的验证

1. **口径自证**:先用你的新代码复现原表 A/B/C 三行的数字。**复现不出来就说明口径不一致,立刻 `[BLOCKED]`**,
   不要拿一套对不上的数字去和 A/B/C 比。复现结果贴进报告。
2. **全量**:`flutter test --no-pub 2>&1 | tee /tmp/t2_full.log`,贴 reporter 末行 +
   `grep -c '^\[E\]' /tmp/t2_full.log`(期望 0)。退出码 0 不作数。
3. `flutter analyze --no-pub lib test` → 0 issue;`dart format .` → 0 changed。

## [BLOCKED] 出口条件

- 复现不出原表 A/B/C 的数字(口径对不上);
- 穷举需要你发明新的指标定义或新的模拟参数;
- 发现台架本身对 TOKEN 的模拟与生产真实令牌消费路径不一致 —— 这是真问题,停下报告,别自己修。

## 协调者怎么验收

我会挑表里 2–3 行自己复跑核数。原表 A/B/C 用例被改 / 生产配置被动 / registry 被动 = 直接打回。
报告里出现没有实测依据的排除理由 = 直接打回。
