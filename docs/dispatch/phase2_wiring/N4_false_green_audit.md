# 批 N4:全仓「测试绕开生产路径」审计(2026-08-26)

分支 `codex/p2-n4-falsegreen-20260826`,基线 `6ab44026`,worktree `/Users/a10506/Desktop/Projects/挂机武侠-p2-n4-falsegreen`。
**只读审计单:不改任何代码或测试。**

## 背景

本项目已多次踩「测试绿但没测到生产路径」。今天一天之内又撞到两次:
① 姿态标签的隐藏断言若只写单向,把渲染门写死 `false` 也能绿;
② 生产攻击令牌的越界值被 `test/data/phase2/ch1_production_catalog_test.dart:187-190` 反向**钉成断言**,
改回合规值反而会打红测试。

## 任务

扫全仓 `test/`,找出**断言可能不经过生产路径**的用例,产可疑清单。

已知高发形态(不限于这些,发现新形态请标注):
1. **手设字段**:直接构造领域对象/快照并手填字段,绕过生产 factory / mapper / assembler;
2. **手动改队列或状态**:直接往内部集合 push/pop,而非走生产入口;
3. **手算几何/数值**:测试里自己算一遍期望值,算法与生产实现是同一份逻辑的复制;
4. **常量比自己**:断言 `SomeConst.x == SomeConst.x` 式的自反比较,或断言值来自被测代码自身导出的常量;
5. **读残留态**:依赖上一个用例遗留的状态;
6. **反向钉死偏差**:测试把一个**已知不合规**的生产值断言成期望值(如上面 ②)。

**判据句(逐条问)**:「把这条断言对应的那行生产代码破坏掉,这条断言**必然**红吗?」
不必然红 = 可疑。**你不需要真去破坏验证**(那属实装),只做静态判读并说明理由。

## 产出

`docs/audit/phase2_test_false_green_audit_20260826.md`(**≤150 行**):
- 头部写审计基线 sha `6ab44026` + 扫描的 `test/` 文件总数(实测);
- 可疑清单表:`file:line` + 形态编号 + 一句话理由 + 严重度(**S1** 守着生产红线却可能失效 / **S2** 普通功能测 / **S3** 风格问题);
- **按严重度排序,S1 在前**;
- 尾部给「扫了多少、命中多少」的计数。

## 硬约束

- **只读**:除上述一个 `.md` 外不得新增/修改任何文件。
- 禁区:`data/numbers.yaml` / `GDD.md` / `PROGRESS.md` / `lib/shared/strings.dart` / `pubspec.yaml`。
- 禁 push / 禁 merge / 禁碰 main / 禁 revert。commit 中文动宾,tip `[READY]`/`[BLOCKED]`,工作区干净。
- **不得顺手修任何可疑项**,清单交出来等拍板。
- 引用一律现 grep 带 `file:line`。**不得把「没细看」写成「没问题」**。
- 宁可少报也不要糊报:**每一条都要能让协调者按 `file:line` 打开就看懂为什么可疑**。

## [BLOCKED] 出口

- `test/` 规模大到无法在单次内扫完 → **交出已扫部分并写明覆盖率**,不要糊判定。

## 协调者怎么验收

随机抽 3 条按 `file:line` 打开核对,理由站不住即打回。出现代码/测试改动 = 直接打回。
