# 夜批单 A · 结构整改目标序列（2026-09-16 夜 · Codex 执行 · 代码单）

> 本单自包含。执行端只读本文件与仓库；协调者（Claude）按本文件「验收方式」独立复测，数字一律自己复跑，不采信自报。
> 基线：`codex/p2-player-flow-20260910` = `c307b3ffcb155af58d4efb9179e36453297b1360`（以下简称链 tip）。你的分支 `codex/night-a-structural-20260916` 从该 SHA 建出，worktree 已由协调者建好并预热（`libisar.dylib` / `pub get` / `build_runner`）。
> 若 worktree 缺 `*.g.dart` 或 `libisar.dylib`，先 `dart run build_runner build --delete-conflicting-outputs`，缺 dylib 从 `/Users/a10506/Desktop/Projects/挂机武侠/libisar.dylib` 拷到 worktree 根；这是环境前置，不是代码回归。

## 0. 总则（每条都是硬约束）

- **语言**：commit message、文档、注释一律简体中文；commit message 用中文动宾（如「收口 numbers_config 数值兜底为 fail-fast」），禁英文 conventional 前缀。
- **禁区（不得改动，改了整单作废）**：`data/numbers.yaml`、`data/*.yaml` 任何文件、`GDD.md`、`CLAUDE.md`、`AGENTS.md`、`PROGRESS.md`、`BACKLOG.md`、`lib/shared/strings.dart`、`pubspec.yaml`、`pubspec.lock`、`.github/`、`docs/dispatch/phase0a_overhaul/*.yaml`、`test/` 下**删除**任何测试或断言、`.gitignore`。
- **git 边界**：只在自己的 worktree 提交；**禁 push、禁 merge、禁 rebase、禁 revert 他人提交、禁碰 `main` 与 `codex/p2-player-flow-20260910`**。
- **产品边界**：零数值变化、零玩家可见 UI 变化、零 schema/saveVersion 变化、零战斗规则变化。本单全部目标都是「行为保持」的结构整改；任何目标若做不到行为保持，走 §5 [BLOCKED] 出口。
- **不碰真实存档**：`~/Library/Containers/com.pen.wuxia.wuxiaIdle/` 只读都不要读；不启动游戏 GUI；测试用 `flutter test`。
- **就绪标记**：每完成一个目标，最后一个 commit 的消息以 `[READY]` 开头（如 `[READY] 收口 numbers_config 兜底并补 fail-fast 守卫`）。开始下一目标后 tip 自然变回普通 commit，最终收工 tip 必须是 `[READY]` 或 `[BLOCKED]`。
- **时间围栏**：每个目标收口后执行 `date`，若已过 **2026-09-17 06:00 CST**，不再开始新目标，直接进 §6 收工。
- **禁止的修法**：① 把 `?? <字面量>` 换成 `?? kSomeConst` 或搬进另一文件——这是绕检测不是收口；② 为让测试过而弱化/删除断言、加 `skip`、放宽期望值；③ 在 `data/numbers.yaml` 补 key（禁区）；④ 用 `// ignore:` 压 analyzer；⑤ 把 `DateTime.now()` 改成 `clock.now()` 但 clock 仍是不可注入的全局单例。

## 1. 目标 A-1 · `numbers_config.dart` 数值兜底改 fail-fast（C2 收窄版）

**现状（链 tip 实测口径，你要重新实测）**：`lib/data/numbers_config.dart` 约 3,800 行、70 个手写 `fromYaml`，含大量 `?? <数字字面量>` 兜底，包括红线本身（`playerHpMax ?? 20000`、`bossHpMax ?? 60000`、`skillPowerMultiplierMax ?? 8000`、`damageReadabilityMax ?? 1000000`）与公式常量（`realmLevelFactor ?? 156`）；`lib/data/defs/stage_def.dart` 有 `stageBossRecruitProb ?? 0.40` 重复一份。yaml 删 key 时静默回落 Dart 常量，违反 CLAUDE.md §5.6/§9「不硬编码数值」「yaml 不得无校验静默接受」。同仓 Phase 2 新代码（`phase0a_weapon_mapping_config.dart`、combat catalog loader）已是「no defaults, fail closed」体例。

**期望终态**：
1. `numbers_config.dart` 与 `stage_def.dart` 中所有**数值字面量兜底**（`?? -?[0-9]`、`?? 0.x`、`?? true/false` 用于数值开关的）改为缺 key 即抛出带 key 路径的明确异常（沿用仓库既有 fail-closed 体例与异常类型，先 grep `phase0a_weapon_mapping_config.dart` 看它怎么抛，保持一致）。
2. **先做 `red_lines` 段**，红线段完成即单独 commit 一次，再铺开其余段。
3. 新增守卫测试（放 `test/data/`）：以生产 `data/numbers.yaml` 为底，逐个删除 `red_lines` 段每一个 key 生成副本加载，断言每次都抛且异常信息含该 key；另抽 ≥10 个非红线 key 做同样断言。
4. 生产 yaml **一个字节不改**；完整套件全绿证明生产 yaml 已含全部 key。
5. 若某个 `??` 对应的 key 在 `data/numbers.yaml` 里**本来就不存在**（即目前生产靠兜底在跑）：**不得补 key、不得改 yaml**，把它原样保留并登记到 `docs/audit/numbers_config_fallback_residue_2026-09-17.md`（key 路径 / 兜底值 / 消费者 file:line / 建议），这是需要用户拍板的 🔴 项。

**验收方式（协调者会这样查）**：
- `grep -cE '\?\?\s*-?[0-9]' lib/data/numbers_config.dart lib/data/defs/stage_def.dart` 之和 = 残留登记表行数（理想为 0）。
- 新守卫测试文件单跑 `All tests passed!`；破坏证红：临时把任一 red_lines 字段改回 `?? 20000` 兜底，守卫测试必红。
- `flutter analyze --no-pub lib test tool` 0 issue；`dart format --output=none --set-exit-if-changed .` 0 changed；完整 `flutter test --no-pub` 全绿。

## 2. 目标 A-2 · 结算逻辑从 presentation 迁到 application（C1 收窄版）

**现状**：`lib/features/mainline/presentation/stage_entry_flow.dart` ≈3,000 行，内含 `applyVictoryResolution`（掉落/成长/共鸣/声望/Isar writeTxn，约 560 行）与 `applyParticipantDefeatResolution`；`lib/features/tower/presentation/tower_entry_flow.dart` 含 `applyTowerVictorySettlement` / `applyTowerCombatResolution`。**application 层反向 import presentation**：`lib/features/activity/application/durable_activity_automation_coordinator.dart`、`lib/features/sweep/application/sweep_settlement.dart` 从 `stage_entry_flow.dart` 导入结算函数；每个 headless 门都靠 `WidgetRef? ref` + `dependencies?` 双模参数硬拆。

**期望终态**：
1. 上述四个结算函数（含其私有 helper）搬到 `lib/features/mainline/application/` 与 `lib/features/tower/application/` 下的新文件；presentation 里只留薄包装（拿 `WidgetRef` 解 provider 后调 application 函数）。
2. `activity/application`、`sweep/application` 改为 import 新的 application 文件；`grep -rn "presentation/" lib/features/*/application/` 中与这四个函数相关的反向依赖归零（其余无关的反向 import 只登记不动）。
3. 行为零变：搬迁是移动+改 import，不重写逻辑；`*ForTest` 参数只在搬迁范围内顺手收口，**不做全仓清理**。
4. 既有测试跟随改 import 即可通过，**不得删除或弱化任何断言**；若某测试因搬迁必须改结构，新旧断言一一对应写进 commit message。
5. 分两个 commit：主线一份、塔一份，各自可独立评审。

**验收方式**：
- `grep -rln "stage_entry_flow.dart\|tower_entry_flow.dart" lib/features/*/application/` = 0。
- `git diff --stat` 里 presentation 两文件净减行 ≈ application 新文件净增行（允许包装层几十行差）。
- 完整套件全绿；破坏证红（remove_implementation 向）：临时删掉搬迁后 `applyVictoryResolution` 里的掉落写入分支，对应主线结算测试必红；（force_degenerate 向）临时把塔胜利结算的层数推进写死为 0，塔进度测试必红。两向都要记录失败数并完整还原。

## 3. 目标 A-3 · 离线结算/回放路径的时钟与随机源收口（C5 收窄版）

**现状**：`lib/core/application/system_clock_provider.dart` 自注「只切 sect」，消费者 3 文件；lib 内约 140 处裸 `DateTime.now()`，其中 `stage_entry_flow.dart`（A-2 后已搬到 application）约 24 处、`phase2_seed_service.dart` ~18、`game_event_service.dart` ~10、`gauntlet_service.dart` ~8；另有直接 `Random(` 构造绕开 `rngProvider` 的点（先 grep 实测）。

**期望终态（只推到影响离线结算/回放确定性的写路径，不做 143 处全量统一）**：
1. 范围：A-2 搬出的四个结算函数、`passive_idle` 积分、`seclusion` 结算三处写路径里的 `DateTime.now()` 改走既有 `system_clock_provider`（或其等价注入口，先读它的现有接口，不新造第二个时钟抽象）；同范围内 `Random(` 改走 `rngProvider`。
2. 其余文件的 `DateTime.now()`/`Random(` 只列清单进 `docs/audit/clock_rng_residue_2026-09-17.md`（file:line / 是否影响离线结算 / 建议），不动。
3. 新增确定性测试：同一输入 + 注入固定时钟与固定 seed，两次结算结果逐字段相同；换 seed 或换时钟则至少一个字段不同（防止「注入了但没用上」的假绿）。

**验收方式**：`grep -c 'DateTime.now()' <范围内文件>` 与 `grep -c 'Random(' <范围内文件>` 均为 0（或等于登记表内明确理由的例外数）；确定性测试单跑绿；破坏证红：临时把注入时钟换回 `DateTime.now()`，确定性测试必红。

## 4. 目标 A-4（弹性尾）· `reducePhase0aTick` 事件流 golden 守卫（C4 前置，只加测试）

**现状**：`lib/features/battle/domain/phase0a/phase0a_combat_reducer.dart` 的 `reducePhase0aTick` 约 1,900 行单函数、7 个嵌套闭包；未来按阶段拆分需要先有「行为零变」的 golden 守卫。

**期望终态（只加测试与 fixture，不改 lib）**：
1. `test/features/battle/domain/phase0a/reducer_event_stream_golden_test.dart`：≥3 个场景（主线 `stage_01_01` catalog 编排、塔第 1 层、一个带脆弱窗口/护法结界的机制型 Boss 场景），固定 seed，headless 跑固定拍数，把逐拍事件序列（事件类型 + 关键字段）序列化后与 `test/fixtures/golden/phase0a_reducer/*.json` 比对。
2. 提供环境变量或 `--dart-define` 的 regen 开关，默认关闭；golden 文件提交进仓。
3. 场景构造必须走生产装配口（`Phase0aProductionFlowAssembler` 或既有 headless runner），不得手搓 state。

**验收方式**：测试单跑绿两次（确定性）；破坏证红：临时改 reducer 里任一伤害结算或事件发射顺序，golden 测试必红；还原后绿。

## 5. [BLOCKED] 出口（遇到即停，不硬做）

- 发现某个兜底 key 在生产 yaml 不存在（A-1 第 5 条）→ 登记，继续其他 key；若红线段有此情况，红线段单独 [BLOCKED]。
- A-2 搬迁中发现结算函数依赖 `BuildContext` 做业务判断（不只是取 provider）→ 停在该函数，登记 file:line 与依赖内容，tip 打 `[BLOCKED]`。
- 任何目标需要改 `data/*.yaml`、`strings.dart`、schema、saveVersion 才能完成 → `[BLOCKED]` 并写明缺什么。
- 完整套件出现与本单改动无关的失败 → 先在链 tip 干净 worktree 复现一次；能复现则登记为既有回归、继续；不能复现则是你的改动引入的，必须修。

## 6. 收工与收据

1. 每个目标：targeted 测试 + `flutter analyze --no-pub lib test tool` + `dart format` + 一次完整 `flutter test --no-pub`（并发，本机热缓存约 8–17 分钟；把原始输出存到 `/Users/a10506/Documents/Codex/2026-09-16/night-A/logs/<目标>_full_test.log`）。
2. 每个目标收口写 `docs/superpowers/plans/2026-09-16-night-a-structural.md` 的「恢复点」：状态 / 最后完成 / 下一步 / 已跑验证（贴 reporter 末行原文）/ 阻塞。
3. 收工前在最终 tip 上按 `/Users/a10506/.claude/skills/afk/scripts/receipt.schema.md` 生成 `docs/dispatch/reports/2026-09-16_night_A_receipt.yaml` 并提交（代码单：`break_red` 两向必填，用 A-2 或 A-1 的破坏证红实测数）。收据只能描述真正执行过的命令。
4. 最终 tip 打 `[READY]`（或 `[BLOCKED]`），worktree `git status` 干净。
5. 把最终摘要（各目标状态 / 关键数字 / 残留登记表路径 / 代拍清单）写到 `/Users/a10506/Documents/Codex/2026-09-16/night-A/summary.md`。
