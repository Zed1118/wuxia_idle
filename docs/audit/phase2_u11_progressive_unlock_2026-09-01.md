# Phase 2 U11 渐进解锁与一次性蜡封审计

日期：2026-09-01

基线：`77a40e62134098a667850f54fbe5d49f4686fd4c`

候选分支：`codex/p2-m6-u11-progressive-unlock-20260901`

## 当前结论

U11 固定工程门已形成候选 `0/1 → 1/1`：七项生产能力共用 `hidden / heard / open` 投影；既有门槛零变化；`ProgressiveUnlockReceipt` 只记录每槽每入口的最高观察态、首次开放时间与一次性蜡封确认。旧档和新档首次观察建立安静基线，不补弹已开放历史；后续真实开放在未确认前可跨重启恢复，确认后不重复弹出或形成永久红点。

这不是 M6、Phase 2 或真人验收完成声明。蜡封排版、文案可读性和真实解锁返回主菜单体验继续挂账。

## 生产接线

- 主菜单启动先等待离线回顾、门派月结、远征结算和江湖远行解锁四类既有 writer，再观察三态，避免同帧读取旧快照。
- 主菜单保持挂载时继续监听主线进度、存档、active roster 与角色境界 provider；状态签名真实变化才串行观察，避免重复弹框。
- 江湖地图的轻功/守城由共享投影派生 `heard/open`；百草岭/断魂庄由共享 `hidden/open` 状态决定是否渲染。
- 九霄塔、门人调度保持新档开放；心魔继续读取现有首节点最低境界，不新增章节阈值。
- `saveVersion` 升为 `0.43.0`，新增 collection 对旧档为空；迁移不推测历史解锁时点、不写待确认蜡封。

## 真红与破坏证红

- 初始 RED：领域/receipt/service/migration、生产投影、蜡封 UI/启动顺序共 6 个测试文件因合同不存在真实失败。
- 删除非开放到开放时的 `openedAt` 写入：目标用例 `1/1` 失败，报待确认入口缺少开放时间。
- 取消首次已开放入口的安静确认基线：目标用例 `1/1` 失败，错误产生 2 条历史待确认题签。
- 取消等待四类启动 writer：目标用例 `1/1` 失败，观察在江湖远行写入前提前发生。
- 取消主菜单保持挂载时的 provider 刷新观察：目标用例 `1/1` 失败，观察次数停在 1 而预期为 2。
- 强制渲染 `hidden` 百草岭：目标用例 `1/1` 失败，实际找到“百草岭”入口。
- 移除 `ProgressiveUnlockReceiptSchema` 注册：迁移用例 `1/1` 失败，Isar 报缺失 TypeSchema。

破坏过程中发现旧“未解锁时隐藏百草岭”测试使用默认 600 高视口，错误入口即使渲染也可能尚未进入 ListView 构建区，形成恒真风险；已改为 1280×1400 后重新破坏并确认真实变红。没有删除测试，因此不需要测试契约迁移登记。

## 已完成验证

- U11 核心恢复：`23/23`。
- main menu + jianghu map + schema/migration/save-management 受影响集合：`299/299`。
- `flutter analyze lib test`：`0 issue`。
- `dart format .`：`1698 files / 0 changed`。
- 持锁 full suite：`5820/5820`，exit `0`，`[E]=0`，末行
  `05:51 +5820: All tests passed!`，墙钟 `353s`。
- 总 Gate、合并态与精确 SHA CI：仍须在 READY tip 后执行，不在本文提前冒充通过。

## 不变项

未修改奖励、概率、经济、玩家数值、技能、战斗规则、YAML、TUNING、现有 gate 或模式入口语义；未进入 M3/M7。
