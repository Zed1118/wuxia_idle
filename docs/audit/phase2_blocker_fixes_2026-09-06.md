# 试玩阻塞修复 · 2026-09-06

本批响应用户“修复”，基线 `57889e115` clean，统一代码候选 `57f95bc7f38f49faec9ef577f19bc161af22e743`。固定范围为暗器 Bot 停滞、原生音频并发悬挂/内存增长、减少闪光无效三项；正式 M0–M9 仍 **1/10，仅 M1 关闭**。代码、回归、原生包与备份纳入同一交付链；最终集成 SHA、完整回归、CI 和程序更新结果由证据目录 `delivery.json` 记录。

## 已实现与直接证据

| 缺陷 | 根因与生产修改 | 验证 |
| --- | --- | --- |
| 暗器近身停滞 | Bot 为纠正朝向在射程内仍折返，窄角攻击落空；射程内停止移动，显式瞄准，经同一 InputAdapter/reducer 结算 | 窄角近身/重合、五武器边缘回归；合法冷副本经 EquipmentService 装备五武器×三战术共 15 局，均进入终局。原暗器 seekGap 300 秒/5 杀仍 ongoing → 30.8 秒/40 杀 victory；其他组合有正常战败，未据此改数值 |
| 原生音频悬挂 | 同池槽的 stop/prepare 并发覆盖 AVPlayerItem 状态观察；固定槽互斥、满池丢弃过时请求，BGM 串行合并。SFX 使用 stop release mode，防旧完成回调释放新 item；关闭无消费者的逐帧位置查询；复用同素材、异步归零，避开 stop 的同步 currentTime | 真实 AudioPlayer + 平台通道 mock 的并发 RED/GREEN；后端 15 项，音频目录 35/35。真实原生带声回读 settings，短测无 continuation 告警；长测见下 |
| 减少闪光无效 | 设置已持久化但战斗未消费；四类生产宿主读取 gameplaySettingsProvider，传给战斗表现；抑制立绘闪白，保留气血反馈 | 真实主线 Boss 命中，在 1280×720/1440×900 比较开关两组领域状态，动态打开开关立即取消已有闪白；命中、血量和位置一致 |

没有改动数值、敌人、射程、奖励、经济、解锁、schema/saveVersion、依赖或插件源代码。手动输入、键鼠持续攻击和技能仍沿原链。SFX 并发上限维持原 5 槽，不制造无界音频队列。

## 原生与性能边界

同一第二版 AOT：`3831b5b203faee6861526753c1e360a0d44eb96ae4530a3170c70cc155cfafac`。独立 bundle/container；DPR 2、12 秒预热、60/600 秒采样、30 秒冷却。真实窗口截图、FrameTiming、GC、RSS、音频运行时字段和日志均保留。24-active debug 密度是压力夹具，不代表合法角色难度或自然人验收。

短测 1280×720 带声 p99 62.979ms、静音 36.061ms，1440×900 带声 91.681ms，均未过 16.6ms 帧门。音频第一版的 1280 p99 136.322ms 在源复用后改善，但不能把改善记作 M4 PASS。第一版 1440 轮次与 2 秒原生线程采样及定向编译重叠，只作诊断；其数据不参与性能验收。没有修改帧门、减少敌人数或静音冒充带声。

剩余帧耗同时包含音频、渲染和密度夹具反复重建的开销；当前没有逐项分配归因。密度夹具会使用生产 Bot，修复 Bot 后结束/重建频率可能改变，因此与修复前全包的帧数变化不能单独归因于音频。两视口首轮均红，未机械重复成 3×2 的“全绿”。M4 性能、真人听感、物理 Windows 键鼠/音频/GPU，以及 72h 稳定性仍开放。

## 最终验证与集成

- 针对性：第一候选 106/106；第二版音频 35/35，analyze 0 issue。首轮持锁全量 6091 PASS / 1 FAIL（918.18 秒）：旧边界夹具把敌人放在射程内却要求 Bot 继续移动，已将目标放到射程外，保留边界断言，修正后相关 20/20。最终集成全量与 CI 结果记录于同证据目录 `delivery.json`；不把这次旧夹具失败当作生产缺陷或隐藏掉。
- 原生带声十分钟：48,651 帧，RSS 285.0→320.2 MiB、峰值 328.1 MiB、原生告警 0；退出 0、GC 完整，内存门 PASS。p99 total 79.971ms，帧门仍 FAIL。此前带声十分钟 283→2477 MiB、2874 条告警；同类缺陷复现已闭合，不等于 72h 证明。
- Windows：[34014731726](https://github.com/Zed1118/wuxia_idle/actions/runs/34014731726)，精确代码 `57f95bc7f`，build + 原生窗口启动 PASS，观察 16.07 秒；不代签实际键鼠/听感。
- 原始证据：`/Users/a10506/Documents/Codex/2026-09-06/wuxia-blocker-fixes/`；生产原存档三文件前后哈希一致。用户已关闭 humanfix 试玩，现有三个存档和设置均已冷备份，更新程序前后核对哈希，保留试玩进度。探针仅使用冷副本、临时 Isar 和新签名隔离包。
- 成本：13:20 CST 开始，以真实墙钟计；90 分钟检查点。最终耗时见 `delivery.json`；本批一次旧边界夹具适配，生产代码没有回退，原始失败日志保留。
