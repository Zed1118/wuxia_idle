# 全真机视觉巡检 · bug 报告(2026-07-06 夜间批 M)

> 用户 00:05 追加:真机覆盖每个页面/环节,视觉找 bug,重点「字体不清晰」+「按钮逻辑不联通」。
> **方法**:①静态套件 `visual_capture.sh --suite full` 真机截 **66 路由** @1280×720(retina 2x)→5 个子代理分批逐张目检;②真机 `flutter run -d macos` + CGEvent 点击巡导航连通性。
> 证据帧:`build/visual_acceptance/night_sweep/`(gitignored,目检后可删)。**本报告只定位不修**(改色需设计判定确切 token,逐屏验,不夜间盲改)。
> **静态截图能定对比度/截断/占位/布局,不能定运行时动态**(defId 泄漏/进度条绑定/无返回入口=标「待运行时复现」)。

## 总览

- 静态目检 66 帧:**P1 ×10 · P2 ×28 · P3 ×27**(去重前);按根因归并后见下。
- 连通性巡检(真机点击):存档槽→主菜单→资源总览→返回、主线(闭关中 gating 弹窗正确拦截「此刻不宜出战」)全部**连通正常**;未发现死入口(下方「待运行时复现」项除外)。
- **两类点名问题都命中且高发**:字体不清晰=「两套色板混用」(memory `feedback_paper_vs_dark_text_color_palette` 已记的已知高发类)在 **11 屏**复现;功能断链线索 6 类。

## A · 字体不清晰(最重 · 按根因归并)

### A1 浅宣纸底 × 淡金/浅灰字 低对比(**用户点名高发类 · 优先修**)
命中屏(P1 加粗):**装备详情右栏数值/tag**、**资源总览库存×N**、**商店分区标题+价格「50两」**、**boss战绩「总伤害43280」**、**battle 破招黄卡技能名/耗内**、材料仓库装备名+境界tag、技能详情熟练度注脚(当前/下阶/再用)、闭关失败弹窗「返回/再战」按钮、心法领悟「取消」按钮、九霄塔势当前层亮印(×2图)、兵器谱分组计数「4/11」。
- 共性:深底色板文字色(淡金/浅灰)误用于浅宣纸 PaperPanel;修正方向=改 `WuxiaUi.ink/ink2/muted`(参照同屏正常的红色价格「125两」/紫色装备名)。**单点修,批量扫**。

### A2 深底 × 深墨/暗红字 低对比
物品仓库/使用页物品名「磨剑石×24」暗金压深底、资源总览分区标题「银两/炼器材料」深灰压深底、主菜单闭关横幅暗红字压深灰(下方要事卡同文案清晰=可作参照)。

### A3 水墨实景图上直书文字无 scrim/描边
闭关地图三卡标题(山林/藏经阁/悬崖瀑布)深字压深景近不可读(悬崖瀑布卡标题+badge=重灾)、关卡 banner 副文案「初出茅庐…」浅灰压浅雾、战斗敌方境界副标题压雾。修正=文字层加半透明 scrim 或描影。

### A4 overlay 遮压文字
蓄力读秒环「3」压住底部技能名「强力·重击」只露残角(battle_boss_frame/scene 同根因)、tap 预览「待发」贴纸压住「单/群」角标。修正=overlay 避让/badge 互斥偏移。

### A5 字号过小 / 截断 / 折行孤字
战斗内力条「内500/500」720p 近不可读(memory `feedback_visual_size_min_resolution`)、秘籍名「开碑手·秘籍…」省略号截断、兵器谱详情「开锋候选技」折成孤字「技」(label 列定宽差1字位)。

## B · 功能断链线索

| # | 屏 | 现象 | 判定 |
|---|---|---|---|
| B1 | 奇遇录列表 | 前两条已际遇标题显示 **raw defId**「an_xiang_xi_zhe / bamboo_listen_rain」 | **待运行时复现**:代码路径真实(`encounter_tab.dart:130` title 空回落 `def.id`),但两事件 yaml 都有 title(暗巷一影/听雨悟剑)→ 运行时 `EncounterEventLoader.load` 对这两 id 返空 title?需真机复现确认是加载失败还是 triggered 集特殊。奇遇录详情页(另一屏)显示正常「暗巷一影」→ 疑列表 provider titles 填充漏这两 id |
| B2 | 商店 / 仓库 / 道具使用 | 商店两商品「缺图」红标+深色占位块;仓库多数装备 icon 近全黑;道具页全物品共用同一铜钱占位图 | **占位泄漏/图标未接线或过暗**·部分待运行时确认是 tint 叠加还是素材缺失 |
| B3 | 离线归来卡 | 正文「离去约8时辰」vs 小结「游历时长:8小时」(8时辰=16小时,换算矛盾) | 静态可判·flavor 文案注入小时数未按时辰换算 |
| B4 | 装备详情 | 「默契」进度条 62%(1240/2000)但条体视觉全空 | **待运行时复现**·疑填充未绑定或填充色=槽底色 |
| B5 | 首胜卷宗 | 「共鸣晋阶:」冒号后无内容悬空标签 | 疑值缺失/换行断链·待运行时 |
| B6 | 关卡列表(stage_list/_cycle) | 章内行程第4、5节点都标「Boss」(每章应只章末1 Boss) | **待人工复核数据**·疑节点标签复用/数据标签错 |
| B7 | 技能库(skill_codex) | 无 AppBar/返回按钮(memory `feedback_flutter_subscreen_appbar_audit` 高发) | **待运行时复现**是否有其它返回途径,若无=导航死路 |

## C · 布局 / 抠图 / 术语(P2-P3)

- **桃花岛主屏(P1·720p)**:7 建筑卡大面积互相叠压重影(打造台压草药园/木工坊压丹房…)——已知「桃花岛场景美术待补」挂账的占位布局在 720p 溢出,散点坐标未做碰撞。
- 塔节点条第30层(当前层)被面板右缘裁半、chapter_list 第2章「未至」徽章裁半、装备图鉴 2 张抠图残留(白底块/直线切割碎片)、encounter/skill 详情面板未限宽右半大片空白。
- 标点半/全角混用(离线残毒/奇遇招/藏卷阁副标题/character_growth 等 ~6处)、术语不统一(悟 vs 悟性、周目 vs 轮回)、名字+身份徽章重复显示(祖师/门派)。

## 建议(留拍板 · 分三档)

1. **安全批量修(推荐先做)**:A1 浅底淡金字→`WuxiaUi.ink/muted` 全扫(单一根因、参照物明确、纯色 token 替换、有 `audit_paper_text_contrast_test` 门禁兜底);标点全角统一。
2. **需逐屏设计判定**:A3 scrim/A4 overlay 避让/A5 内力条字号(按最低分辨率)/桃花岛 720p 布局/塔节点定位到当前层。
3. **需运行时先复现再定性**:B1 奇遇 defId 泄漏、B2 图标缺失、B4 默契进度条、B7 技能库返回入口——建议下一会话真机逐个复现(本批静态证不了真伪,不盲修)。
