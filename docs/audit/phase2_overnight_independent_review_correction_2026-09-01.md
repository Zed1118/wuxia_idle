# Phase 2 隔夜独立审查口径校正

日期：2026-09-01

被审查 final tip：`62d6df2aea62d54bb1abf8f730fe8e6cb100f572`

用途：校正同日外部独立审查中已复核的事实边界。本文不替代各批实现审计、Gate 或真人目检，也不重新签署 U11/U12。

## 可采纳结论

1. 九霄塔地点详情仍显示“亲自挑战，不可派遣”，与已接入的已通层持久差遣矛盾，属真实生产 UI 回退。
2. 九霄塔差遣按钮复用百草岭 `expeditionLocationEnter`，动作身份不精确，属真实生产 UI 回退。
3. M5 工程矩阵保持 `38/42`，剩余 `4/42 BLOCKED`，顶层 M5 仍为 `0/1 BLOCKED`；本次文案修复不增加矩阵分子。
4. 同一 SHA 的 GitHub CI 第一次在测试任务接近时限时取消、第二次完整成功，可记录为运行时间余量风险，不构成测试断言失败证据。

## 必须修正的结论

### final Gate

`final Gate UNPROVEN` 的定性不成立。exact final tip 的本机原始 Gate 日志记录 `5840/5840 PASS`、`[E]=0`、analyze/format/receipt crosscheck 全 PASS；receipt 的 `head_sha` 与 final tip 一致。

准确口径应为：**final Gate 已在本机证明；原始日志与执行脚本未纳入仓库，跨机器可移植复核能力有限。** 该问题按证据治理风险跟踪，不冒充产品阻断。

### U11/U12

外部审查明确说明 U11/U12 审查深度较低，因此该报告不能把二者写成独立“接受”。本轮严谨状态为：

- U11：`UNPROVEN`——本报告未完成足够深度的生产消费者、破坏证红和 final-tip Gate 复核。
- U12：`UNPROVEN`——本报告未完成足够深度的生产消费者、破坏证红和 final-tip Gate 复核。

`UNPROVEN` 只约束这份外部审查的证明能力，不自动否定 U11/U12 各自已有实现或原批次证据；若需正式接受，必须另做当前 main 的独立深审。

## 提交与 Gate 范围说明

`62d6df2a` 相对父提交确实回退了塔专用文案和按钮常量，因此 UI 缺陷成立；但 `strings.dart` 在九霄塔持久差遣批次基线到 final tip 的净 diff 中被还原为基线状态，故该批 Gate 的 `forbidden_files PASS` 不是假绿。两者分别是提交级回退事实与批次净差异事实，不能混写。

## 当前处理

- 生产文案和专用按钮已进入独立候选：当前错误实现先取得地点详情 `1` 条、塔层列表 `2` 条 RED，修复后定向 `8/8 + 14/14 PASS`；回退说明与复用百草岭文案的两向 mutation 分别精确 `1`、`2` 条失败并反向还原。是否可合仍服从 final Gate。
- M5 继续保持 `38/42`、顶层 `0/1 BLOCKED`；真人桌面与 Windows 实机目检继续挂账。
