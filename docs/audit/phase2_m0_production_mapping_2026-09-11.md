# M0 时间轴与真气生产映射（2026-09-11）

用户在当前任务审阅三组具体映射后明确回复“批准”。此处记录已生效规则；工程接线已在统一候选完成无界面验证，正式 M0–M9 仍为 1/10（仅 M1）。前置方案及批准基线证据位于 `/Users/a10506/Documents/Codex/2026-09-11/m0-next-integration/m0-mapping-proposal.md`。不改选时间轴 B、真气 C。

## 已批准的生产行为

1. 玩家单段普攻使用 B；起手时冻结瞄准/目标偏好，首效拍按当前位置、存活目标和既有单目标几何结算一次，零攻击位移。空挥也只获得一次普攻气，不视作 failed。松开停止下次尝试，移动不取消在途动作。
2. M3 普攻冷却与 B 时间轴在准入时同时开始，下次出手同时等待二者结束。0.1 秒步长下剑/重兵/软兵/双持/暗器持续普攻间隔为 6/8/7/6/6 拍，首效延迟为 1/2/1/0/0 拍。双持原 5 拍改为 6 拍是已批准变化。
3. 合法且被原有输入优先级接受的 Space、Q/R、数字技能先通过全部校验，才终止在途普攻；取消窗内 cancelled，窗外 interrupted，附加 CD 自终止时与 M3 剩余 CD 取较长者。首效前终止不补效果/真气，首效后不回滚或重发。普通受伤不新增玩家打断；死亡、终局清理在途动作。
4. Q/R、数字技能仍即时首效且独立 CD，真气 reserve/commit 在这一次即时效果完成。没有新建跨拍技能预扣，也没有新建合法玩家 failed 触发；合同的 failed CD 仍保留，不宣称对应新的生产分支。
5. 五武器 C 的普攻回气依次 20/24/22/18/18。数字强力技费用为武器 power + 原始技能费用 −30，大招为武器 ultimate + 原始技能费用 −60。原始 35 气的三条特殊技继续多 5 气。实际 Q/R 绑定 ID 免武器偏移，保留原始 25/50，再与数字技能应用同一既有 QiCycle 减耗。R 仍是 powerSkill；敌人和合击费用保持原路径。
6. C 的 100/40 是基础值，运行态继续使用真实 snapshot 派生气海、开场、心法/紊乱/内容与波间/节点恢复。此前 snapshot 携带但 Phase0A 未消费的回气倍率/减耗，本次按既有 QiCycle 算式及上限实际生效。
7. 每个真实敌人死亡实例独立 +5，同窗口累计实际获得最多 15，不乘普攻回气倍率，溢出不挤占未获得的窗口额度。显式 wave 只在真换波开启下个窗口；SpawnDirector 全 encounter 为一个窗口，补兵、checkpoint、会话 fork 不重置。新遭遇/远征新节点使用新的账本。状态快照包含动作游标、收益/费用 ID、窗口累计，避免重放重复结算。

完整 B/C 数字由 `data/numbers.yaml` 的 `phase0a_arena.weapon_mapping` 集中配置，严格解析拒绝缺少武器或字段；带武器的生产 mapper 必须有完整配置。无武器隔离测试夹具保留原路径。伤害继续使用原 reducer 结算函数与真实 SkillDef，界面技能印、bot、slot、intent 共用派生费用。

## 恢复与验证边界

生产会话内恢复使用不可变 `Phase0aArenaState` 经 `forkWithState`；测试覆盖首效前后继续执行及值相等。持久化对照使用实际临时 Isar 的入场记录关闭/重开和相同 seed 重新推演；项目没有中途战斗序列化 API，本批不声称实现了中途战斗落盘恢复。

验证原始 JSON、源码哈希、完整测试目录与最终结果将保存在 `/Users/a10506/Documents/Codex/2026-09-11/m0-next-integration/`。旧候选全量不算本批通过。测试使用无界面生产 factory/session/reducer/controller 和临时存档，不启动游戏、不操作玩家档。

本批不提交、合 main、push、部署或代签 clean/真人/Windows。塔迁层仍 0/49，M4 仍开放。2026-09-04 决策包与 2026-08-26 差异文档保留为历史；其中关于时间轴/真气仍未接线的旧快照，以本条和当次最终验证记录核对。

## 完整回归中的真实流程检查

首轮全量执行 917/917 文件，6525 PASS、2 个主线诊断用例失败，0 跳过/遗漏，运行中源码未变；原始记录保留为 `implementation-full-first*`。两例使用真实新档、固定种子与按键驱动，期待的胜利未发生，不是超时或下一关路由异常。

实际事件显示旧 Boss 脚本在第 1 拍 R 取消了重兵普攻，首效被推迟到第 11 拍；改为首效后再发送到期的 R/数字1 请求后，第 3 拍普攻首效、第 4 拍 R，最终获胜且原有参与者和两条结算断言全过。主线脚本采用同样的到期请求等待首效规则，保留原有移动、即时 Space、防御优先级、seed、新档属性和实际敌人，随后真实获胜、重载进度并进入 stage_01_02。探索过的停步/射程/聚怪/绕墙策略没有进入最终候选。

这两处只适配测试中的模拟玩家按键，不改生产平衡，不移除原胜利/结算/导航断言；新增真实动作与战斗结果诊断。先前推测的首效前移动并非单独判错依据，批准规则允许移动；最终没有以停止移动或改变射程作为解决方案。该自动操作记录不能代签自然人体验或全局平衡。

## 最终统一候选验证

- `flutter analyze`：0 问题；29 个本批 Dart 文件格式检查零变化，`git diff --check` 通过。
- 最终定向：37/37 文件、342 PASS，0 失败/跳过/遗漏，运行中源码未变。
- 最终完整：917/917 文件、6527 PASS，0 失败/跳过/遗漏，全程持共享锁，运行中源码未变。真实起止为 `2026-09-11T23:07:55.830007+08:00` 至 `2026-09-11T23:16:16.680665+08:00`。
- 定向与全量源文件哈希一致。首轮完整源码到最终完整源码只改变前述两份诊断测试，生产代码保持相同；最终报告落盘后的治理文档单独解析并核对链接。
- 双持跨两次真实换波保持 6 拍；普攻首效/回气、Q/R/数字费用、已有预留余额拒绝、击杀窗口、在途 fork、临时 Isar 准入重开/同 seed 重放及既有塔/槽隔离均进入最终相关回归。

交付文件：`/Users/a10506/Documents/Codex/2026-09-11/m0-next-integration/implementation-validation.json`、`implementation-candidate-manifest.json`、`implementation-m0.patch` 和 `implementation-unified-candidate.patch`。候选树与补丁 SHA 在外部清单记录，避免文档自引用哈希。未提交或合 main；工作树包含原有用户修改，不能宣称 clean 或正式 M0 关闭。
