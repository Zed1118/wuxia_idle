# Session 交接 - 视觉收口二期合并与肖像裁切修复

**时间：** 2026-07-26 16:59
**项目：** 挂机武侠
**分支：** `main`（未 push·领先 origin 37）
**最后 commit：** `6fbc732b`

## 本次完成

详 PROGRESS.md 顶段两条 2026-07-26 条目（区间 `d5005794..6fbc732b`·11 commit）。PROGRESS 没展开的：

- **交集文件要验「合并后的树」而非两侧 diff**：phase2 与 main 都改 `damage_popup.dart`，是相邻非重叠 hunk。merge-tree 报 0 冲突不等于语义对，用 `git show <mergetree-oid>:<file>` 逐项 grep 才算验过（PR #79 的 parse-back 零复活、旧硬编码零残留）。
- **抽查挖出的是空头交付、不是回归**：肖像焦点 no-op 在合并前后渲染完全相同（`WuxiaImage` 默认本就 center），坏的是 codex 的声称与测试口径。区分这两者决定了「该不该拦合并 / 修不修 / 算谁的账」。
- **一次自我证伪**：飘字图册里克制升降没标记，我一度判为死字段漏渲，查到 `damage_popup_test.dart:105` 有显式 `findsNothing` 断言才确认是既定设计。已把依据写进图册标签与代码注释，防下个人重复误报。

## 当前状态

1.0 长线打磨期。codex 全局视觉收口 phase1 + phase2 已全部合入 main，且各经一轮 Claude 端真机抽查；phase2 抽查挖出的肖像裁切 no-op 已当场按 TDD 修复。main 树净、analyze 0、全量 4711/0（均本会话主 checkout 实测），**37 commit 未 push**。

## 进行中的工作

- **Ch17「沙海纵深」未起**：宗师段 spec §8 前瞻已定向（cap 38→40→42 / 末 Boss vulnerability 0.20 教学 / 真解 Ch17 新写），但**章级细化未起**。开工前需拍板：先出章级 spec（沿 Ch14/15/16 惯例）还是沿用段级前瞻直接实装。另 `idle_horizon` s1 45.6 / 下沿 45 已贴线，Ch17 扩缺口必破须重校。
- **PR #83**：OPEN + draft + CI 全绿 + 零冲突，其 worktree `.claude/worktrees/session-handoff-0726` 被另一 session 持锁，本会话既无权 merge PR 也无法移除该 worktree。

## 已知问题

- **`build/visual_acceptance/` 已 493M**（gitignored·纯本地盘）：本会话新增 39.6M（`phase2_spotcheck_20260726` 33M + `portrait_fix_20260726` 6.6M，含修前/修后对照图）**留置**，是这两批唯一可回溯的视觉证据；往批约 428M **连挂第三轮未决**，本轮升级问用户（根卷可用 1.2Ti，非空间压力，纯「证据还要不要留」的判断）。
- **PR #83 连挂第二轮**，非我方能闭环，需用户执行 `gh pr ready 83 && gh pr merge 83`。
- codex phase2 自报的 8 route × 双视口 16/16 截图与 phase1 一样写在仓库外，**不可回溯**。
- Windows 三档缩放 / 正式 CJK 字体 / 9 名候选透明站姿 / 29 组历史近似详情图 / 伪文字规则，五项仍未清零。

## 重要决策

- **合并走临时 integration worktree 再 FF 回 main**，因为用户禁用 `reset --hard`/`checkout --`；worktree 让回退等于删目录，主 checkout 全程不进半合并态。
- **不用 EnterWorktree 建树**：它按 `baseRef=fresh` 基于 origin/main，会丢掉本地未 push 的 30+ commit。改 `git worktree add <path> main` 手工建，再用 `EnterWorktree({path})` 切进去过 bg 写守卫（守卫认工具不认手工树）。
- **肖像修复直接落地不等二次确认**：修前画框留黑柱本身就像渲染 bug，修后是无歧义改善；且本地未 push，`git revert f3e05056` 一条命令可退。

## 下一步建议

1. **Ch17「沙海纵深」（推荐）** — 唯一能推主线的实质任务；开工先拍板 spec 路线。需 xhigh 专会话。
2. 合并 PR #83 — CI 全绿零冲突，一步清掉连挂两轮的债。
3. `build/visual_acceptance/` 往批 428M 存废 — 连挂三轮，一句话决定。
4. 29 组历史详情近似图分四批清零（29→21→13→6→0），复查文档 §P2-01 已给顺序。

## 踩坑提醒

- **`flutter test` 批传显式路径静默漏跑第三次复现，且出新形态**：`ui_terminology_test` 只播报首个 test 即被丢弃（批计 15 = 另两文件 6+9 已占满），仍报 `All tests passed!` + EXIT=0，单跑实为 9 pass。**日志里 grep 到文件名不足以证明它跑完了**。详 memory `feedback_flutter_test_batch_silent_skip`（本会话已更新第三形态）。
- **只断言「参数传到 widget」的测试挡不住渲染层 no-op**，且 `Container(alignment:)` 会给子 widget 松约束致 `BoxFit.cover` 无溢出可裁。新 memory `feedback_flutter_loose_constraints_fit_noop` 已记全（含 widget test 里 `MaterialApp(home:)` 会被路由紧约束撑成全屏、量画框须包 `Center` 的坑）。
