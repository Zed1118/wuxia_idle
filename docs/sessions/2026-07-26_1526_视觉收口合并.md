# Session 交接 - 全局视觉收口合并与真机抽查

**时间：** 2026-07-26 15:26
**项目：** 挂机武侠
**分支：** `main`（未 push·领先 origin 25）
**最后 commit：** `c3a914d8`

## 本次完成

详 PROGRESS.md 顶段 2026-07-26 两条（合并批 + 抽查修正批，区间 `78da778d..c3a914d8`）。PROGRESS 没展开的：

- **codex 预检基于旧 main 必须重跑**：其 merge-tree 用的是 `2942af60`，实际 main 已到 `78da778d`。按新 base 重跑仍 0 冲突、交集仍 2 文件，但这一步不能省——预检结论有保质期。
- **交集文件要验「合并后」而非「两侧 diff 各自看着对」**：真正的风险是 main 删的 `criticalDamagePopup` 被分支侧旧代码复活。用 merge-tree 产出的树 `98430ca8` 逐 grep 取证（复活 0 次、悬空引用 0 次）才算验过。
- **跨侧门禁收紧是隐形风险**：分支自测用的是宽松基线 allowlist（旧版），合并后生效的是 main 的严格版（仅 2 条）。分支侧全绿不代表合并后全绿，必须合并后重跑门禁。

## 当前状态

1.0 长线打磨期。codex 全局视觉收口 phase1（111 文件）已合 main 并经真机抽查修正，phase1 worktree/分支已清（释放 1.3G）。main 树净、analyze 0、全量 4701/0，**未 push**。

## 进行中的工作

- **codex phase2 在途且已就绪**：`codex/global-visual-polish-phase2`（worktree `.worktrees/global-visual-polish-phase2`·tip `04172dec` **`[READY]`**·树干净·ahead main 6·**merge-tree vs main 冲突 0**）。基于 phase1 tip 而非合并后 main，故不含本会话三项修复——合并后需复验那三处还在。
- **PR #83（上轮交接记录）CI 现已全绿**（macos-build pass / test pass 23m33s），draft 状态待 `gh pr ready` + merge。

## 已知问题

- **`build/visual_acceptance/` 已 453M**（gitignored·纯本地盘）：本会话新增 25M（`postmerge_spotcheck_20260726` 21M + `postfix_recheck_20260726` 4.1M）**留置**，理由=本批视觉判读的唯一可回溯证据，且在主 checkout 不随 worktree 清理蒸发；往批 8 目录约 428M **连续第二轮挂账未决**，已升级问用户。
- **Windows 100%/125%/150% 缩放 / 跨平台正式字体方案 / 9 名可出战候选缺专用透明站姿** 三项未清零（本会话只确认墨影降级真机风格一致，不等于补图完成）。
- 视觉抽查为 **10/125 route 抽样**、单视口 1280×720；`settings_panel` 因内容放得下未出现滚动条，「68% 高度提前滚」未验到（需 `settings_panel_bottom`）。
- codex phase1 的 250/250 route 截图证据写在 `/tmp`，已不可回溯。

## 重要决策

- **合并后补做真机视觉抽查**，因为工程门禁（analyze/test/边界检查）证不了「看着对」，而本批核心交付恰恰是视觉。抽查挖出 3 项既有瑕疵，均穿过了本批自建门禁。
- **修复主动扩到 7 个生产站点**（术语混用连带扫荡屏、整数时长连带闭关三处），因为只修截图里看到的那一处，等于重犯本批「立了规矩自己漏执行」的错误。
- **不 push**：用户显式交代「不推送远端除非另行明确要求」，/handoff 的 push 步骤按用户指令让位。

## 下一步建议

1. **评审合并 codex phase2（推荐）** — 已 `[READY]`、冲突 0、同域续作；放着会像 phase1 一样持续落后于 main。
2. 合并 PR #83 — CI 已全绿，一步操作即可清掉上轮遗留债与其 worktree。
3. Ch17「沙海纵深」实装批 — 唯一能推主线的实质任务，需 xhigh 专会话。
4. 真人试玩半小时 — 一次解锁 BACKLOG §一#4/#5/#6 三项数值拍板。
5. `build/visual_acceptance/` 往批 428M 存废拍板 — 已连挂两轮。

## 踩坑提醒

- **`flutter test` 批传显式文件路径会静默漏跑**（本会话两次复现：9 传 5 跑、9 传 8 跑），仍报 `All tests passed!` + `EXIT=0` = 假绿。验收必须逐文件确认出现次数，漏的单跑取证。详 memory `feedback_flutter_test_batch_silent_skip`。
- **幂等守卫别用松散关键词**：本会话 PROGRESS 订正脚本用 `if "phase2" in src and "已清" in src` 判幂等，而 PROGRESS 本就含这两词 → 误判跳过、静默没改。守卫要用**新串本身**判定。
- **宣纸面禁用深底色板**：`wuxia_tokens.dart:14` 明写「浅底文字勿用 `WuxiaColors.*`」，`_Tag` 违反后叠 0.4 灰显即不可读。详 memory `feedback_paper_vs_dark_text_color_palette`。
