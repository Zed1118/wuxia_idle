# Session 交接 - Phase A1 持久化实装

**时间：** 2026-07-15 20:34
**项目：** 挂机武侠
**分支：** main
**最后 commit：** `9f4a025b`（本 handoff 记录/PROGRESS 提交后 HEAD 前进一格）

## 本次完成

详 PROGRESS.md 顶段 2026-07-15 A1 条目（A1 commit 区间 `d745aa59..9f4a025b`，前置 docs merge `221f48b8`）。
- 装备副本编排批1-3 文档 merge 入 main（`221f48b8`，纯文档，本会话开局合入）
- Phase A1 持久化基础 + 占用契约实装（严格 TDD·7 任务·6 commit），冻结 6 类契约供 A2/B/C/装备批依赖

## 当前状态

江湖远行（百草岭远征 / 断魂庄爬 Boss）持久化地基已落：两会话 collection + SaveData 永久进度字段 + 跨活动占用查询服务，saveVer 0.36→0.37。仅数据层，UI/编排/结算未接。main ahead origin 未 push（push 是用户的活）。

## 进行中的工作

- 无进行中本会话分支。A1 worktree 已合并回 main。
- Codex `codex/battle-ui-stage` 活跃并行（非本会话产物，**勿动**）。

## 已知问题

- docs worktree/分支 `equip-baicao-orchestration` 已 merge 入 main、未删除（留用户拍板清理；本轮首次挂账，下轮若仍在则强制处置）。
- D1/D2/D3 三项数值决策（联合经济产出 / 掉落 / 凭证消耗）待探针出数据后拍，不阻塞 A2。

## 重要决策

- A1 冻结 6 类契约（SaveData 6 字段 / ActivityMemberSnapshot @embedded / Expedition+Gauntlet collection / GauntletPhase.awaitingRewardChoice / 托管三列表 / ActivityOccupancy DTO `reservedEquipmentIds: Set<int>`），下游批**不得改名改签名**。
- 实装期订正计划 3 处（@embedded 必带 `part` 指令 / saveVer 版本断言实为 3 处非 1 / 占用测 Character 须设全 7 必填 late 字段），已回写 plan 恢复点。

## 下一步建议

1. **Phase A2（推荐）** — 发布上限 10→17 / 断魂帖 / 配置校验；依赖 A1 冻结契约，地基已就绪可直接接，建议 xhigh。
2. 联合经济探针 — 出 D1/D2/D3 数据供用户拍板。
3. B ∥ C 并行 — 远征编排 / 爬 Boss 会话循环，各依赖 A1 契约。
4. 装备批 — 需先拆 `enhancement_service.dart:97` neverDegrade throw + 加 `ItemType.ticket` 穷尽 switch（推翻「永不掉级」）。

## 踩坑提醒

- bg 写守卫会话中途 cd 进主 checkout 后，Write/Edit 连 worktree 路径也被拦；主 checkout 文档改动走 Bash（python `assert count==1` / heredoc / job tmp `cp`）——已入 memory `feedback_bg_enterworktree_home_cwd_fallback`（cwd 漂移翻转守卫段）。
- schema 批从 worktree merge 回主 checkout 后 `.g.dart` gitignored 不随 merge，必先 `build_runner` 再测（本会话已跑，A1 targeted 绿）——memory `feedback_wuxia_pen_build_runner`。
