# Session 交接 - H3审查红线修复

**时间：** 2026-05-29 12:31
**项目：** 挂机武侠
**分支：** main
**最后 commit：** ac88056

## 本次完成

- **H3 后期挑战 audit**(`0cd5c81`):4 并行子 agent Phase 0 grep(Ch4-6 主线/心魔/群战+轻功/飞升)→ `docs/handoff/h3_lategame_audit_2026-05-29.md`(~105 行)。结论:后期路径**远比中期健康**(大量 🟢:难度曲线+红线+叙事接线全成熟,Ch4-6 35 narrative 0 dangling),查出唯一 🔴 A2。
- **A2 多代飞升循环断裂修复**(`20d7273`):`performAscend` 真传位漏写 `save.founderCharacterId=promotedDiscipleId` → gen2「祖师不在出战阵容」永久 blocked。主控 grep 复核铁证(R5.6/8/10 测试用手动 setup 掩盖)+ 红绿修复(1 行 production + 删测试 setup 暴露真实闸门 + R5.6 防回退断言)。
- **外部 code-review 6 findings 核验**:逐条 grep 实测,**全成立**(P2-a 部分)。给出修复顺序(P1-b/P1-a 红线 → P2-c 公式统一 → P2-a/b 健壮性 → P3 文档 → 根因A)。
- **P1-b 内力/血量红线源头 clamp**(`ac88056`):`derived_stats` 的 `internalForceMaxWithLineage`(:250)+ `maxHp`(:122)末端不 clamp → founder buff(玩家自享 +5%)+ 师承遗物乘法可破 15000/20000 红线(实测内力 18900)。两处 clamp + 改造"狡辩测试"(原手算 18900 + 注释自称'实际不破')为真调 derived_stats 断言 clamp + 加 maxHp clamp 测试。
- **PROGRESS 更新**(`14c3dd2`):记 H3 + A2,删 P2.1 旧详条保持净增长≈0。

## 当前状态

1.0 release readiness ~96% 维持。H3 后期 audit + A2 唯一 🔴 修复收口。外部 review 修复批**起步**(P1-b 完成,1535 测 / 0 analyze)。后期系统经审查证实成熟,主要待办转向 review 剩余 5 条 + 根因A 挂机循环重平衡。

## 进行中的工作

- **外部 review 修复批顺序 2-6 未启**:P1-a auto-swap 锁死 / P2-c 公式统一 / P2-a 奇遇招式兜底 + P2-b 敌人缩放 / P3 文档 drift。
- **根因A 挂机循环重平衡未启**(方向已对齐):idle battleCount 喂共鸣度 + 闭关产修炼度 + 闭关 EXP 提到接近战斗 + insightPoints/learnPoints 轻量兑换 sink。需先清 review 前置(P1-b ✅ / P2-c)再动。

## 已知问题

- **P1-a 飞升 auto-swap 绕过三系锁死**(§5.3 红线):`ascend_service.dart:248` 直写 equipped 槽位无 canEquip,武圣神物可自动装到低境界徒弟。
- **P2-c 战斗公式双路径分叉**:`DamageCalculator` vs `default_ground_strategy._calculateInBattle`(+attackPowerMultiplier),违 §6 公式集中。是根因A balance 验证的前置。
- **P2-a 奇遇招式 YAML 吞异常**:`game_repository` encounter_skills 空集合是红线 noop(无兜底,真静默失效);encounters/synergies 有 ≥N 兜底。
- **P2-b 敌人属性硬编码**:`stage_battle_setup.dart:282` maxInternalForce:1000 / crit·evade 0.05 固定不随境界缩放。
- **P3 文档 drift**:numbers.yaml:68 内力系数实际 0.5,GDD:337/CLAUDE.md:180 仍写 ×0.7;AGENTS.md Demo 口径过时。
- **红线值散落 hardcode**:15000/20000 在 derived_stats/stage_battle_setup/inner_demon/game_repository 4 处 hardcode,P2 统一到 numbers.yaml 候选。

## 重要决策

- **决定外部 review 重排了根因A 的前置**:P1-b 内力 clamp + P2-c 公式统一必须在根因A 之前——根因A 会推高内力放大 P1-b 破线,且 balance 验证需先有单一公式真相源,否则建立在流沙上。
- **决定 P1-b maxHp 同源一并修**:review 只点内力,但 maxHp 同样不 clamp(founder buff/心法相生可破 20000),留一半红线洞不合理。
- **决定 clamp 放 derived_stats 源头**(非 battle_state 入口):所有 caller(战斗+UI)都守红线;沿 stage_battle_setup 既有 hardcode 体例不动 numbers.yaml(避免 schema 变更),后续 P2 统一时再提配置。
- **根因A 方向三选择已定**:共鸣度+修炼度都喂 / 闭关 EXP 提到接近战斗 / insightPoints 轻量兑换 sink(不破 §7.2 学心法 Phase 5+)。

## 下一步建议

1. **(推荐)P1-a auto-swap 锁死**(opus high ~1-2h):飞升 context 刚热。auto-swap 前加 canEquip 境界校验,不够阶只转 owner 不上身(留背包);改 ascend_service_test:433 固化断言 + R5 加不够阶徒弟红线测。
2. **P2-c 公式统一**(opus xhigh ~半天):根因A balance 前置。先诊断 _calculateInBattle 为何分叉(BattleCharacter vs Character 类型差异?),收敛到单一真相源。
3. **P2-a/P2-b 健壮性 + P3 文档 drift**(opus high ~2h):奇遇招式红线兜底 + 敌人属性 numbers.yaml 缩放 + GDD/CLAUDE.md §6 血量系数 0.7→0.5 同步 + AGENTS.md 阶段口径。
4. **根因A 挂机循环重平衡**(opus xhigh ~6-8h):前置(P1-b✅ + P2-c)清完后动,接 balance_simulator 真路径验证。
5. **红线值统一到 numbers.yaml**(opus high ~1h):消除 4 处 hardcode 15000/20000 重复,单一真相源。

## 踩坑提醒

- **bg session worktree 守围**:cwd 虽是 git 仓库,但 bg session 任何 Edit/Write(含 doc-only)走 EnterWorktree → 工作树内改 → `git push origin HEAD:main`(FF)→ 主 `git merge --ff-only <branch>` → ExitWorktree remove(commit 已并入 main 时 discard_changes:true 安全)。
- **fresh worktree 必跑 build_runner**(`.g.dart` gitignored,否则 analyze/test 报一堆 undefined)。
- **audit/review 断言必 grep 复核再信**:本 session 两次验证此纪律 —— A2 被 R5.6/8/10 测试手动 `save.founderCharacterId=N` setup 掩盖;P1-b 被 `founder_buff_service_test:74` 注释「真正 clamp 在 CharacterDerivedStats」狡辩掩盖(实际 derived_stats 根本没 clamp)。**测试 setup / 注释可能掩盖 production 缺失**。
- **DerivedStats 类名是 `CharacterDerivedStats`**(derived_stats.dart),纯函数测试在 `test/combat/derived_stats_test.dart`(不在 test/features/battle/domain/)。
- **纯函数测试构造 Character 要设 `..attributes = Attributes()`**(late field,不设访问报 LateInitializationError);`Equipment()..isLineageHeritage=true` / `..baseHealth=N` 字段写可用。
