# Session 交接 - 断魂庄 Phase C C1

**时间：** 2026-07-16 18:47
**项目：** 挂机武侠（Flutter Desktop 写实武侠挂机）
**分支：** feat/baicao-duanhun-phase-b（worktree `baicao-duanhun-phase-a2`·**未 push·未合 main**）
**最后 commit：** `cb4cf3ee`

## 本次完成

详 PROGRESS.md 顶段「2026-07-16 江湖远行 Phase C 断魂庄批(进行中)」（本会话 3 commit：`2000fd55`/`17d14e5e`/`cb4cf3ee`＝2 代码 + 1 进度）。
- 执 executing-plans + 严格 TDD 跑通 Phase C plan（10 task）的 **C1 组纯域半**：C1.1 qi_drain 效果 / C1.2 关次边界白名单快照。

## 当前状态

江湖远行 Phase C 断魂庄开工，完成 C1 组纯域两件（`QiDrainEffect` + `snapshotAfterStage`）。C1.3（qi_drain 引擎接线＋敌队＋编排）及 C2 全组未开工。分支全未 push。

## 进行中的工作

- 无进行中切片（C1.1/C1.2 全绿已提交）。C1.3 未开工，**三子切片落点写全在 plan `2026-07-15-baicao-duanhun-phase-c-gauntlet.md` 当前恢复点**。

## 已知问题

- 探针产物 `test/tools/output/joint_economy_probe_2026-07-16.md`（gitignored·batch3 留置·可重生）。
- codex worktree `.worktrees/battle-ui-stage`（tip `f29ccef5`·活跃写）——**非本会话债·勿碰**（已续传 · 用户/codex 侧）。
- PROGRESS 现 **106 行**（超 100 目标 6 行）：batch3 与 Phase C 两条同日「江湖远行」条目，Phase C closeout 时合并压缩下修（未中途删以免误伤历史）。

## 重要决策

- **停在 C1 纯域半**：C1.3 qi_drain 要接进 1020 行确定性战斗引擎 `_resolveAction`，深上下文硬塞风险高 → 留新会话满上下文＋battle-determinism harness 认真做（打磨期质量优先）。
- **cooldown 存 A1 快照**：design §5.5 要冷却跨关继承，`ActivityMemberSnapshot` 加平行列 `skillCooldownKeys/Turns`（可加性·无 saveVer 迁移），plan 已授权「冷却单列存 A1 快照」。

## 下一步建议

1. **（推荐）C1.3 qi_drain 引擎接线**（opus·xhigh）：`SkillDef.qiDrainPct` + `_resolveAction` 蓄力完成（`forcedSkill!=null` 未破招）施 `QiDrainEffect`；破招/承伤窗口复用既有 charge-break-vulnerability。风险点先做。
2. C1.3 敌队 3 队 `EnemyDef`（机制走 bossPhases/guardianWard/vulnerability 既有字段）+ `advance()`/runner。
3. C2 组（入场扣帖/托管/恢复/奖励三选一/UI）——大，多切片。

## 踩坑提醒

- **执行既有 plan 的内嵌测代码前必 critical-review**：C1.1 plan 测对同一调用写了 70 与 58 两个矛盾期望，design §5.2「30% 最大真气」为准=58；别照抄 plan 测。
- **`snapshotAfterStage` 需 `before` 入参**：plan 原单参签名会丢 `reserved*` 占用冻结（战斗 `finalState` 不含 reserved）。
- **push 是用户的活**：分支全未 push，勿 push / 勿合 main / 勿 force。
