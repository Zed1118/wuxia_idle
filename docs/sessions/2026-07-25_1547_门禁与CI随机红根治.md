# Session 交接 - 中文门禁合入 + CI 随机红根治 + §一#12 拍板

**时间：** 2026-07-25 15:47
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 9194c2c8

## 本次完成

详 PROGRESS.md 顶段 2026-07-25「工程门禁 + CI 随机红根治三批」条（`1e37f1fc..9194c2c8`）。增量：PR #74 门禁 `ce4e9239` / #75 RNG 根因 `b638af06` / #76 §一#12 拍板 D `9194c2c8`；#75 走 TDD（先写「注入必中 rng → 掉 2 件」wiring 测看红，再改生产 2 行）；三 worktree + 4 分支全清。

## 当前状态

主线 16 章 80 关 + 全套美术在 main，known_missing 清零。工程侧本轮收口：门禁挡新增中文散写，折磨三个 PR 的 CI 随机红根因已除。allowlist 存量 26 条待清。宗师段 1/3 章，Ch17 未开工。

## 进行中的工作

无半成品。三批均已合并、CI 双绿、载体清完。

## 已知问题

- **§一#8 未整项关闭**：余约 8 处生产裸 `DefaultRng()`（tower×2 / gauntlet_reward / recruitment / disciple_join / milestone_equipment / onboarding / founder_creation）；另 `stage_entry_flow.dart:233` 是 `dart:math Random()` 签名，换 provider 需另立注入点。
- Ch16 11 张真 PNG **17M 待 webp 清账**；立绘未真机战斗屏目检。
- **留置**：`build/dispatch/` 432K（gitignored·ch14/15/16 派单书+日志）为历次美术批过程证据，刻意保留非债；本会话未新增产物。

## 重要决策

- **§一#12 取 D（不设第 4 集中 sink）**：原定性经现查证伪——`damage_calculator` 5 条拼进 `formulaBreakdown`，该字段全仓零 lib 消费方（只被测试读），是**调试串**非玩家可见文案；列第 4 sink 等于给该文件未来真正的 UI 文案发免死金牌。改 ASCII 出表即可。
- **`damage_popup` 正则不迁 UiStrings**：它解析的是 `UiStrings.criticalDamagePopup` 自己拼的串，中文=模板复制品，真问题是改文案会静默失配，治本是消除 parse-back（§二#4）。
- **#75 取根因修不取测试侧修**：关彩头/改 `>=1` 会松掉「必掉铁剑」语义，且随机源不可注入的病根仍在。

## 下一步建议

1. **webp 清账小批（推荐）** — Ch16 11 图 17M，面小闭环快，`convert_assets_webp.py` 幂等。
2. 中文存量 26 条清理（§二#3）— A/B 类迁 UiStrings 为主，C 类占位可留。
3. `damage_popup` parse-back（§二#4）— 修完顺带销 allowlist 最后一条 E 类。
4. Ch17「沙海纵深」实装批 — xhigh 专会话，idle_horizon s1 贴线必破须重校。

## 踩坑提醒

- **`gh pr merge` 对 draft PR 会失败**（`still a draft`），且接管道时 `echo EXIT=$?` 取的是管道尾码，把失败读成成功。先 `gh pr ready <n>`，合完查 `gh pr view <n> --json state,mergeCommit`。已补进 memory `feedback_ci_closure_watch_and_format_pitfalls`。
- **单次 CI 绿不证明 flake 已除**（修前单跑本就 93.6% 绿）。真凭据 = 破坏证红精确复现 `Expected 2/Actual 1` + 连跑 15 次全绿。
- memory `feedback_wuxia_sweep_rare_bonus_flaky_drop_count` 已改写为「已根因修，再见同型红按新增回归查」，别照旧条重跑了事。
