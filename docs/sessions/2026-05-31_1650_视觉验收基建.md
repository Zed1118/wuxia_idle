# Session 交接 - 视觉验收基建

**时间：** 2026-05-31 16:50
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 33265c8

## 本次完成

- **出版美术视觉验收基建全实装**（spec→plan→subagent-driven 6 Task，12 commit `1ec4029`→`33265c8`，全 push，1612→1620 测/0 analyze）：
  - **VISUAL_ROUTE 直达机制**：`lib/features/debug/application/visual_route.dart`（枚举 mainMenu/techniquePanelTierAll/techniquePanelHero + parseVisualRoute + visualRouteFromEnv，4 测）+ `main.dart` kDebugMode 守卫下 `--dart-define=VISUAL_ROUTE=<id>` 启动分流（release/日常零影响）
  - **VisualRouteHost**：`lib/features/debug/presentation/visual_route_host.dart` route→seed+screen 映射 + 首帧 `debugPrint('VISUAL_ROUTE_READY:<id>')` 就绪信号（照 splash 真实 bootstrap：GameRepository.loadAllDefs + IsarSetup.init + IsarSetup.instance）
  - **seedVisualMasterAllTiers**：`phase2_seed_service.dart` 新增武圣满学 7 阶 seed（遍历 TechniqueTier.values 取各 tier gangMeng 首本，满境界合法持有，4 测）→ **关闭 cover 多 tier 验收缺口**
  - **visual_capture.sh**：`tools/visual_capture/`（循环启 app+等就绪+截窗+退出·启动前清残留窗防截错·非交互全屏 -x 兜底）+ `window_id.py`（Quartz CGWindowList 取窗，可选）+ README
- **spec/plan 落库**：`docs/superpowers/{specs,plans}/2026-05-31-visual-capture-infra*`
- **逻辑层端到端验证通过**：实跑 `flutter run --dart-define=VISUAL_ROUTE=technique_panel_tier_all` → `VISUAL_ROUTE_READY` 信号真实打印；全量 1620 测绿；analyze 0
- **PROGRESS 当前阶段段并入基建摘要**（行数控 100）
- **memory 新增** `feedback_subagent_parallel_same_file_collision`（subagent-driven 一条消息只发 1 个写同文件的 implementer）

## 当前状态

出版美术 Phase A（主菜单）+ B（心法面板）已收口，本 session 补建视觉验收基建（代码层完工 push，逻辑层全验证）。HEAD `33265c8` 与 origin/main 同步，工作树干净（仅 macos build 产物 + docs/handoff 截图未跟踪）。**7 阶 cover 实际视觉呈现待 Codex 验收**——这是基建的首个真实用途。

## 进行中的工作

- **7 阶 cover 视觉验收**：基建已就绪，提示词已产出（见上一轮对话），待派 Codex 跑 `technique_panel_tier_all` 验素纸→金框梯度同屏。
- 出版美术下一屏切片（战斗屏/装备详情屏/章节屏）未起，可沿用本基建增量加 route。

## 已知问题

- **CLI 会话截图能力边界**：本 Mac 会话缺 PyObjC `Quartz`（`ModuleNotFoundError`）+ 无 Screen Recording 权限 → `visual_capture.sh` 走全屏兜底，反复只截到桌面顶层主菜单窗口，**未能在截图里真正看到 7 阶 cover**。逻辑层已验证，视觉层须 Codex。
- `window_id.py` 在装了 PyObjC + 屏录权限的环境（如 Codex 桌面）才能截干净窗口；缺则优雅退化全屏兜底（已在 README 标注）。
- `assets/techniques/` 7 张 cover 共 16M 未压缩（沿用上 session 已知项，未动）。

## 重要决策

- 决定截图目标 = Mac 本地 app（非 Pen），因出版美术是 Mac 单端验收，链路最短。
- 决定 app 直达用 dart-define（非命名路由/文件信号），因 Flutter 原生无新依赖，脚本可批量循环。
- 决定多 tier cover 用「单角色武圣满学 7 阶」单 seed 呈现（非 7 个 route），因符 §5.3 境界锁死且最接近真实满配玩家面板。
- 决定截图用脚本侧 screencapture（非 app 内 toImage），因出版美术核心是真实渲染效果（图片加载/阴影/平台层），toImage 会漏。
- 决定停止在 CLI 截图工具链死磕、7 阶 cover 视觉验收交 Codex，因撞 memory 铁律「native app 视觉验收派 Codex」。

## 下一步建议

1. **派 Codex 跑 7 阶 cover 视觉验收**（提示词已产出）：验素纸→金框梯度同屏 + cover 完整不裁切 + hero/main_menu 回归。这是基建首个真实用途。
2. **出版美术下一屏切片**（战斗屏/装备详情屏/章节屏），沿 B 段体例 + 本基建增量加 VisualRoute。
3. **techniques 资产压缩** 16M→~4M（oxipng/pngquant，低风险）。
4. 角色立绘 backlog 9 张 prompt（sect/recruit）。

## 踩坑提醒

- **subagent-driven 绝不在一条消息并发多个 implementer 改同一文件**：本 session Task 2 误发 3 个并发改 `phase2_seed_service.dart`，撞出方法重复定义 + 重复 import，`reset --soft` squash 才清干净。每 Task 串行：派 1 → 等汇报 → review → 下一个。（memory `feedback_subagent_parallel_same_file_collision`）
- **CLI 截 native app 窗口不可靠**：osascript 取 frontmost 对 Flutter debug app 失败；Quartz 需 PyObjC（本机无）+ 屏录权限。视觉验收默认派 Codex，别在截图工具链死磕。（memory `feedback_cli_no_gui_screenshots` / `feedback_user_defers_visual_to_codex`）
- **window_id.py 被 `**/*.py` gitignore**：force-add（`git add -f`）才进库。
- **改代码仍走 Bash heredoc 直写 main**：Edit/Write 被 bg isolation guard 拦（本 session 用 heredoc + subagent 全程绕过成功）。
- **debug app 不自动到前台**：`flutter run` 起的 macOS app 进程名 = `wuxia_idle`（非「挂机武侠」非 Runner），日志有 `Failed to foreground app; open returned 1` 属正常。
