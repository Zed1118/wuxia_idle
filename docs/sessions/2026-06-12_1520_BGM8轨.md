# Session 交接 - BGM 8 轨细分(波C)

**时间：** 2026-06-12 15:20
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 04be64fb（代码 461c1e9a + 本 handoff docs）

## 本次完成

详 PROGRESS.md 顶段「2026-06-12 续(波C · BGM 扩 8 轨细分)」（代码 `461c1e9a`，docs `04be64fb`）。
- 声明式 BgmScope 体例零中央表，扩 8 轨纯接线 + `bgmTrackForStage` 纯路由，2 处 entry flow 收敛全覆盖
- 真玩 A/B 验收：用户编 macos 包听 candidate_01 vs 02，**8 轨全采用 candidate_01**

## 当前状态

1.0 长线打磨期。audio 子系统波 C 收口：BGM 11 轨全接线（mainMenu/seclusion/battle 兜底 + 战斗 6 + 非战斗 2）。无进行中工作。

## 进行中的工作

- 无（波 C 完整收口，已合 main + push）

## 已知问题

- 0b/0c 清场：worktree/分支/桌面试听目录已删，无 untracked，无孤儿债
- 音频二期 E 挂账（非阻塞）：uiPaperOpen 素材 Suno 未产出（留空 no-op）/ battleUlt·battleChargeStart 转用素材听感未复核

## 重要决策

- 战斗轨细分到 6 类型轨：mainline Boss 关切 boss 轨制造压迫感（普通关 mainline），massBattle/innerDemon/lightFoot/tower 类型轨优先（含其 Boss 层）——因 StageType 已有判定来源，零新状态。
- 响度分层：战斗 6 轨 -19.5 LUFS / 非战斗 2 轨 -14 LUFS，对齐现有 battle(-19.6)/mainMenu·seclusion(-14)，开打不吵、场景音乐响亮。

## 下一步建议

1. **D 统一进度展示组件 wiring（推荐）** — P1b 唯一未完项；熟练度/残页已有 Row，共鸣度/修炼度无、散落各处，需先定「五要素」展示规范（当前阶段+进度+当前效果+下一阶段效果+来源）再逐处 wire。跨多文件 UI 改造，建议升 xhigh。
2. E 音频二期零散 — uiPaperOpen（等 Suno 产出落文件零代码）/ 转用素材听感复核（需真玩听）。
3. F/G master spec 大阶段（P2 队伍协同 / P3 Boss+战后）— 需先讨论产品方向再动手。

## 踩坑提醒

- `assets/audio/_suno_candidates/` 是 .gitignored，**只在主仓**；fresh worktree checkout 读不到 → 素材处理脚本须从主仓绝对路径读、写回 worktree。
- fresh worktree 的 `.g.dart` gitignored 不随 checkout，跑测试前必 `dart run build_runner build`（详 memory `feedback_wuxia_pen_build_runner`）。
