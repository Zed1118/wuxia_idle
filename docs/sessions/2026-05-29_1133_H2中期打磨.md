# Session 交接 - H2 中期打磨

**时间：** 2026-05-29 11:33
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 7790b74

## 本次完成

- **H1-Q1 小套餐实装**(`a497044`):G1 `mainMenuTitle '挂机武侠 · 调试主菜单' → '挂机武侠'`(P0 ship blocker 清)+ G5 标题 style 24→28/w600/letterSpacing 4 · 顺手清 8 个往期遗留工作树(全已并入 main + clean)
- **H2 中期玩法深度 audit**(`b882907`):4 并行子 agent Phase 0 grep(装备/心法/师徒+共鸣/闭关+章节+升阶)→ `docs/handoff/h2_midgame_audit_2026-05-29.md`(~135 行)· 6 条 load-bearing 断言 grep 实测核验
- **H2 小套餐(接线 polish)实装**(`7790b74` · TDD · 0 数值改 0 schema 改):C1 章节翻篇过场 + C2 升阶大境界仪式 + E2 effective 实战值可见 + S3 死字段清理 + R2 经核验已实装(不加冗余)
- 本会话 4 commit 全 push origin/main · 1520→1534 测全过 · 0 analyze

## 当前状态

1.0 release readiness 维持 ~96%。H 段推进:H1 上手 audit + 小套餐(P0 清)+ H2 中期 audit + 小套餐(接线 polish)全收口。游戏中期"已做 backend 但没接线"的高光时刻(章节翻篇/升阶仪式/effective 数值)已接上。根因A(挂机循环脱节)是已诊断但未修的最大中期硬伤,待中套餐。

## 进行中的工作

- **H2 中套餐(根因A 挂机循环重平衡)未启**:idle 喂共鸣度/修炼度 + 闭关 EXP 重平衡 + insightPoints/learnPoints 消费 sink · 需数值决策 + balance_simulator 验证 · 建议先定方向再动手
- **H2 小套餐 defer 项**:C1 Boss victory 自动翻篇仪式(需 seen-flag 持久化)/ E2 换装 delta vs 已装备(需 equipped-item provider plumbing)/ R2 共鸣度首次 tutorial 引导(G2 类 onboarding)
- **H 段 Batch 3-6 未启**:H3 后期 audit / H4 D4 卡点联动 / H5 UX 微调 / H6 文案 polish
- **F1 Steam 注册搁置**(ship 前 1-2 月)

## 已知问题

- **根因A 挂机循环脱节**(audit B 段诊断):idle/闭关 0 喂共鸣度(seclusion 无 battleCount)· 闭关 EXP 性价比远低于战斗 · insightPoints/techniqueLearnPoints 死钱包无消费 sink · 人剑合一 500 战中期触不到
- **学新心法 UI 0 caller**(§7.2 标 Phase 5+):中期玩家无法主动学新心法 · 心法获取环节缺位(scoped,非 bug)
- **师徒中期错配**:收徒门槛一流(Ch4)vs 中期 Ch2-3 二流 · 收徒后 inactive 池无出阵/培养/产出
- balance_simulator PoC 数据偏极端(29/30 关秒杀)· 仅方向性诊断 · 真 tune 需接 Isar 真路径

## 重要决策

- **决定 H2 走小套餐先行**:接线 polish 全是已产 backend、0 数值改 0 风险、ROI 最高;根因A 重平衡需数值决策单独批
- **决定 C1 不动战斗 victory 流**:章节翻篇做 chapter_list「卷」入口(可达+可重入阅读),Boss 自动触发仪式需 seen-flag 持久化留后续
- **决定 E2 只做 effective 可见、delta defer**:换装 delta 需 equipped-item async plumbing(无现成 provider),effective 可见已解 audit 主诉
- **决定 R2 不动**:victory dialog ResonanceUpgradeBanner 早在 P1.1 候选 3-a 实装,don't build what exists(audit agent 误判)

## 下一步建议

1. **(推荐)H2 中套餐 — 根因A 挂机循环重平衡**(opus xhigh ~6-8h · 先讨论方向再动手):idle 喂共鸣度/修炼度 + 闭关 EXP 重平衡 + insightPoints/learnPoints 消费 sink · 接 balance_simulator 验证
2. **H3 后期挑战 audit**(opus xhigh ~3h):Ch4-6 主线 + 心魔 + 群战 + 轻功 + 飞升 全玩家路径流畅度 + 难度曲线
3. **H6 文案 polish**(opus high ~3h):全 narratives + Ch1-3 events 古风一致性
4. **H2 小套餐 defer 项收尾**(opus high ~2h):C1 Boss 自动仪式 / E2 换装 delta(二选一或都做)
5. **D3 Isar IO 压测**(opus high ~2h):纯工程稳定性

## 踩坑提醒

- **bg session 隔离守围**:本项目 cwd 非 git 仓库(项目在 ~/Desktop/Projects/挂机武侠),任何 Edit/Write 触发 worktree 守围 → 必须 `EnterWorktree` → 工作树内改 → `git push origin HEAD:main`(FF)→ 主 checkout `git -C <root> merge --ff-only <branch>` → `ExitWorktree remove`。doc-only 改也要走这套
- **fresh worktree 必跑 build_runner**(memory `feedback_wuxia_pen_build_runner`):`.g.dart` gitignored,新工作树 analyze/test 会报一堆 undefined provider/getter,先 `dart run build_runner build --delete-conflicting-outputs`
- **DerivedStats 类名是 `CharacterDerivedStats`**:effective 数值方法在此类,不是 `DerivedStats`
- **audit 断言必 grep 实测再信**(memory `feedback_audit_report_phase0_verify`):R2 被 audit agent 误判为"未实装"(自标需确认 caller),实际 P1.1 候选 3-a 早做了 → 实装前 verify 省冗余代码
- **chapter prologue/epilogue 此前是 dead content**:C1 已接 loadChapter,但 Boss victory 自动翻篇仪式仍未接(需 seen-flag)
- **改 widget 加 icon 前先看现有测 icon 计数**:chapter_list_screen_test 锁 `Icons.lock`/`check_circle` 数,加「卷」入口用 `Icons.auto_stories` 避撞
