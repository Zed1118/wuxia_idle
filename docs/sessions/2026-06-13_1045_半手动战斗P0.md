# Session 交接 - 半手动战斗 P0 步骤1-3a

**时间：** 2026-06-13 10:45
**项目：** 挂机武侠
**分支：** main（worktree-p0-seed-determinism ff 并入 + 删除）
**最后 commit：** 7c6cc710（+ 本 handoff doc commit）

## 本次完成

半手动战斗 master spec（`docs/superpowers/specs/2026-06-13-semi-manual-battle-seed-replay-cycle-design.md`）P0 无 schema 域基础三步，commit 区间 `906f3a11..7c6cc710`：
- **步骤1 确定性 seed 链**（`bbca4e99`）：startBattle 接 `{int? seed}` + notifier 持单 `Random(seed)` + advance 全程 seeded。红线测先红（advance 不传 rng→暴击发散）后绿。
- **步骤2 操作记录**（同 commit）：`BattleReplayOp` 域类型 + notifier in-memory `recordedOps`，锚点=`state.tick`。
- **步骤3a 手动选目标**（`7c6cc710`）：`BattleState.pendingTargets` + requestUltimate 全链加可选 `{int? targetId}` + `BattleAI.decide` 消费（目标存活时优先）。

## 当前状态

P0 单关闭环的**域层地基已就位**（seed 确定性 + 操作记录 + 手动目标，全确定性可重放）。尚无单步 UI、无重放执行、无落盘。战斗内核其余路径不变。2011 测 / 1 skip / analyze 0。

## 进行中的工作

P0 剩余 5 步（spec §五）：
- **3b 逐 actor 单步推进**：advance/tick 改逐 actor 让出的状态机。确定性关键——rng 消费顺序须与自动/重放完全一致（intra-tick actor 队列）。
- **3c battle_screen UI**：autoStart=false + 「下一步」按钮 + 单体技目标 picker（需 Codex 视觉验收）。
- **4 重放执行**：同 seed 重建 + 相同锚点回放 recordedOps（地基已齐）。
- **5 解锁+落盘** 🔴 schema 0.19.0 单点收口（BattleReplayRecord + cycleIndex + 迁移）。改前单独对齐。
- **6 GDD §5.5** 改前必 ask。

## 已知问题

- `fix/treasure-glow-tune`（=main 同点）仍占主 checkout：Codex 金光验收 PASS（无回收、无代码改），其 untracked `visual_capture_*` 截图目录留置（用户指示保留不动）。建议下次主 checkout 切回 main 后删该分支。
- `fix/ui-polish-sweep`（已合并 stale）本轮已删。

## 重要决策

- `?? Random()` 全接口硬化（rng 必填，级联 mass/lightfoot/pvp + battle_engine）**缓做**，因超 P0「单关地面战」范围、会动到不在 P0 的战斗形态；P0 内确定性已由红线测结构性守住。→ backlog。
- 锚点用 `state.tick`（非 actionLog.length）：引擎时间计数器，粒度最细，advance 已跟踪。
- §八#3 一步=一 actor（非整 round）；§八#4 单体技可选目标、群体技自动（当前引擎全单体，后者为前瞻）。

## 下一步建议

1. **（推荐）步骤3b 逐 actor 单步推进**：确定性关键状态机，宜新会话满上下文 TDD 专注做，是 3c/4 的前置。
2. 步骤4 重放执行：地基已齐（seed+ops+目标全确定性），可与 3b 并行设计。
3. 步骤5 schema 0.19.0：高风险单点收口，改前对齐。

## 踩坑提醒

- 红线测必须走 **BattleNotifier.advance** 路径（经 ProviderContainer + 永久 listener 防 autoDispose），不是 strategy.tick——strategy 层早确定性，bug 在 notifier。
- 种子场景必须真有 rng 分歧（criticalRate>0）才能证伪不确定性，否则空过（已加 >10 action 防空过断言）。
- requestUltimate 接口加 `{int? targetId}` 后，test 中 `implements BattleStrategy` 的 stub（如 pvp_service_test `_StubStrategy`）须同步签名，否则编译失败。
