# Session 交接 - 测试节奏收口 + clamp 迁 numbers.yaml

**时间：** 2026-07-03 16:10
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 48509165（= origin/main·现跑 ls-remote 核实同步）

## 本次完成

详 PROGRESS.md 顶段 2026-07-03「测试节奏收口 + clamp 迁 numbers.yaml」条（commit 区间 `97f7ccfb..48509165`，6 commit 全 push）。
- 起因=用户反馈「测试太频繁费时」；先量化再定策，非拍脑袋。

## 当前状态

1.0 长线打磨期。工程外围继续收：测试提速（-j1→并发 3.8×）已对每个后续任务持续生效；clamp 迁 config 收尾了 2026-05-29 未迁干净的红线 hardcode。无在途代码。

## 进行中的工作

- 无在途代码。剩余为下波候选（见下）。

## 已知问题

- CLAUDE v1.29 把「全量默认并发」写进 §8.2；`full_review_2026-07-02_followup_backlog.md:24` 的 CI 锚点仍写「-j1 ~10min」，做批次4 CI 时改用默认并发（~2.5min）。
- 两 strategy 类 doc 注释（light_foot:26/mass_battle:34,174）仍写字面「clamp(0.0, 0.95)」，非误（0.95 仍是生产值），未改属留置。

## 重要决策

- clamp cap 用**一个共享 `combined_rate_cap`** 而非 handoff 说的 3 个字段：3 类 cap 值同为 0.95、语义同（率的 100% 天花板），red_lines 块本就是 DRY 单一真相源，3 个重复字段反引入它要消除的重复。
- 全量默认改并发：每测试文件独立 isolate + 独立 createTemp 目录本就隔离，`-j1` 是没验证过的谨慎默认；实测零新 flake。

## 下一步建议

1.（推荐）死文档归档 docs/_archive：先证伪 content_guide.md（wuxia-content/check-redlines skill 依赖）/AGENTS.md（codex）/data_schema.md 引用再动，禁盲归档。改动明确、无拍板依赖。
2. README 重写：需先拍板内容深度（当前仅默认 Flutter 模板）。
3. 批次4 CI / 批次5 battle_screen 拆分：均需先拍板排期。

## 踩坑提醒

- **handoff 给的行号/数量会 drift**：本次 clamp「3处/×4」实为 7 处（Phase 0 grep 现数），凭 handoff 转抄会漏改。处置散落 hardcode 前必全仓 grep 可执行点（排除注释）。
- 迁 numbers.yaml 新字段走 memory `feedback_yaml_config_unused_field`（配而不用要么注释要么砍）；本次 combined_rate_cap 7 点真消费，非死配置。
- 测试节奏铁律见 memory `feedback_test_cadence_no_blind_full`（跨项目）。
