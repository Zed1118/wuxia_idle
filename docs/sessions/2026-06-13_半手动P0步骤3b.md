# Session 交接 - 半手动战斗 P0 步骤3b 逐 actor 单步推进

**时间：** 2026-06-13
**项目：** 挂机武侠
**分支：** main（worktree-p0-step3b-stepone ff 并入 + 删除）
**基线：** 834b2b60

## 本次完成

P0 步骤3b「逐 actor 单步推进」确定性关键状态机（spec §五 P0 步骤3b · session 推荐项）。

**用户拍板（2026-06-13）**：
1. intra-tick actor 队列入 `BattleState.actorQueue`（瞬态、不落盘；重放靠同 seed + 同 stepOne 序列重建）。
2. `tick()` 重构为「边界 stepOne + 循环 drain」，`stepOne` 成唯一 actor 结算真相源。
3. tick 边界（AP 填充、无人行动）单独成一步——玩家可在该 tick 正确锚点布置指令。

**实装**：
- `BattleStrategy.stepOne`：接口加默认实现 = 退化整 `tick()`（非地面形态/stub 零改）。
- `DefaultGroundStrategy.stepOne` override 真逐 actor：队列空=推进 AP/CD + 排序填队列（tick++，不结算、不耗 rng）；队列非空=弹队首结算（死亡跳过 / 敌灭清队列，均不耗 rng；否则 `_resolveAction` 唯一耗 rng）。`tick()` 改为边界 + drain。
- `BattleNotifier.step()`：3c 单步 UI 驱动入口，复用本场单一 seeded `_rng`。
- 4 处 strategy `implements`→`extends`（light_foot/mass_battle/pvp + 测试 stub）继承默认 stepOne。

## 当前状态

确定性地基：rng 只在 `_resolveAction` 消费，拆 actor 循环不改消费顺序。analyze 0 / 全量 2011→**2014 测** / 1 skip（tick 重构零回归）。

## TDD 红线（battle_step_one_test.dart · 3 测）

1. 边界步填 actorQueue + tick++ 且不结算（actionLog 不增长）；随后一步恰好出队一个 actor。
2. **同 seed：stepOne-drain 跑完 ≡ tick-drain 跑完**（actionLog + 胜负 + 双队终态血量逐字节全等）。
3. 同 seed：notifier `step()` 逐步 ≡ `advance()` 整 tick。

## 下一步建议

1. **步骤3c 单步 UI**：battle_screen autoStart=false + 「下一步」按钮调 `notifier.step()` + 单体技目标 picker（需 Codex 视觉验收）。边界步在 UI 上是「本回合行动顺序：甲乙丙」展示，玩家布置指令后逐步走。
2. **步骤4 重放执行**：同 seed 重建 + 相同锚点回放 recordedOps（地基已齐，可与 3c 并行）。
3. **步骤5 解锁+落盘** 🔴 schema 0.19.0 单点收口，改前对齐。

## 踩坑提醒

- `tick()` 入参约定为 tick 边界态（actorQueue 空）——所有整 tick 调用方都满足；mid-tick 态传 tick() 会「续完当前 tick」而非「起新 tick」（实际无此调用）。
- `BattleStrategy` 本是带 const 构造的抽象基类（doc 原文），本意被 `extends`；加默认方法后 `implements` 的类不继承默认实现，故 4 处改 `extends`。
- 等价红线场景须 criticalRate>0 才有 rng 分歧可证伪（沿 battle_seed_determinism_test 体例 + >10 action 防空过）。
