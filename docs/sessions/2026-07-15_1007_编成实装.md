# Session 交接 - 编成实装

**时间：** 2026-07-15（bg 会话，跨 07-14 深夜）
**项目：** 挂机武侠
**分支：** main（产出在 PR #39 分支 `feat/team-lineup-screen`，worktree `team-lineup-impl`）
**最后 commit：** main `5bbefc7c`（含用户 07-15 百草岭设计 doc）/ PR 分支 `e36eb834`

## 本次完成

详 PR #39 body + 分支内 PROGRESS 顶段「2026-07-15 出战编成屏实装批」条（合并后随 PR 落 main）。PROGRESS 未展开的增量：
- PR #38 审合前置：CI test 红定位为 online_presence R2 计时 flaky（纯文档 PR 零交集、重跑即绿），双绿后真 merge `21921dc1`；spec worktree/分支已清。
- 实装期逮到 spec §2 两缺口（已落 spec 订正块）：①战斗态拒无信号→路由结构性保证；②加入无主修弟子=下场战斗必抛（编成→组队 wiring 回归测抓到）→ noMainTechnique 校验+「未修主修」标+研习引导。
- 顺修：零心法态整屏空态藏研习入口（sect/boss 招收弟子首学路径此前不可达）。

## 当前状态

1.0 长线打磨期。§十三：#1✅ #2✅ #4 实装完成待审合（PR #39 draft，CI test 跑中）；剩 #3 Windows / #5 真人试玩。用户今晨新增百草岭远征+断魂庄三关设计 doc（`docs/superpowers/specs/2026-07-15-baicao-expedition-duanhun-gauntlet-design.md`，状态=对话已确认待书面复核，Lv100→170，远征玩法直接消费编成/换人系统）。

## 进行中的工作

- PR #39 等用户审（重点：LineupService 校验矩阵语义 + spec §2 两处实装订正认可与否）；CI test job 跑中（macos-build 已绿），新会话 `gh pr checks 39` 自查。

## 已知问题

- worktree `team-lineup-impl`（分支 `feat/team-lineup-screen`）留置：内容已全 push 进 PR #39，合并后 `git worktree remove` + 删本地分支（plan 恢复点有步骤）。
- **PR #39 合并后主 checkout 必跑 `dart run build_runner build`**：lineup_providers.g.dart gitignored 不随 merge 走，不跑则 main 编译失败（memory `feedback_wuxia_pen_build_runner`）。
- 战报「击杀」标注口径查证：已续传 2 轮（07-14 起），20-30min 小任务，下轮处置或弃。

## 重要决策

- 决定服务层不做战斗态校验，因为全仓无战斗进行时持久信号（roster 进场快照/结算传参），编成屏与 BattleScreen 路由互斥已结构性排他——如实订正 spec 而非造伪校验。
- 决定「加入者须已修主修」进校验矩阵（spec 设计期缺口），因为 stage_battle_setup 硬性要求主修在库；与择路流「研习立为主修」互补成环：招收→学首门→上场。
- 决定编成屏 widget 测走真 Isar 生产链路不用 fixture override，因为 PR #37 竞态教训=fixture 挂载恰绕过生产接线。

## 下一步建议

1. （推荐）审合 PR #39 → Codex 目检 visual_route `team_lineup` → 清 worktree/分支——百草岭远征直接消费编成系统，先收口地基。
2. 百草岭+断魂庄 spec 书面复核拍板 → 实装计划（大批次，建议 xhigh）。
3. §十三 #3 Windows 实机验收（1-2h，D 段用户人工）。
4. 战报「击杀」口径查证（20-30min，连挂两轮）。

## 踩坑提醒

- testWidgets×真 Isar：seed/交互写在 fake-async zone 挂死（pumpAndSettle 每处超时 10min），必须 runAsync 包真 async + runAsync+pump 交替轮询；SnackBar 断言后 pump(5s) 冲 timer——详 memory `feedback_widget_test_pump_until_found_settle`（本批 team_lineup_screen_test 是最新实锚）。
- `dart format --set-exit-if-changed 2>&1 | tail` 管道吞退出码，&&链会假绿——退出码敏感命令别接管道。
- EnterWorktree 在 worktree 内切新分支后，自动建的 `worktree-*` 空壳分支要随手删（本次 `worktree-team-lineup-impl` 已删）。
