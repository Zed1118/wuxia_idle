# Session 交接 · 2026-06-23 续48 · F2 掉落 preview 首通门控

> 第五阶段·掉落优化 子系统 B 核心命中。承接续47 掉落审计 F2。
> brainstorm(方向 2 问拍板)→ Phase 0 锚点核 → TDD(红→绿) → 全面修。
> 合 worktree branch `4772c5be`，merge main 待 push。opus high。

## 做了什么

**Bug**：主线 3 本秘籍 `item_scroll_*`（dropChance=1.0）运行时逐 defId 首通门控（`shouldSkipScrollDrop`），但 preview 整表硬编码 `isFirstClearGated:false` → 被错归「常可得」，实为「首通必得·重打不补」。根因=preview 的 gated 是**整表单布尔**，表达不了同表内秘籍门控、装备不门控。

**用户拍板 2 项**：① 范围=**全面修**（抽 canonical 谓词，不止 preview 层）② 主线 footer 文案=「秘籍首通必得，重打不补」。

**改动**（11 文件）：
- `DropRumorTable.fromDropTable` 表级 `bool isFirstClearGated` → `FirstClearGating` 枚举（`scrollOnly` 主线逐条 / `wholeChannel` 爬塔整渠道），逐条判 gated。
- 抽 `isTechniqueScrollDefId(defId)` 谓词入 `core/domain/enums.dart`，runtime `shouldSkipScrollDrop` + `enums.fromDefId` + preview **三方共用**，消除 `item_scroll_` 前缀散写 3 处 drift。
- dialog footer 按 gating 选串：爬塔=塔层串 / 主线含秘籍=新串 / 否则无（`hasFirstClearGatedEntry` 驱动）。
- 2 call site：stage_list→scrollOnly / tower_floor→wholeChannel。`UiStrings.lootMainlineScrollFirstClearFooter` 新增。
- `bucketOf`（逐条布尔）不变；爬塔行为零变；runtime 掉落行为零变（纯去重）。

## 验证

- analyze **0**（全量）
- 全量测试 **2848+1skip**（基线 2839 **+9** 新测：谓词 3 + F2 桶 4 + dialog footer 2）零回归
- worktree fresh checkout：pub get + build_runner（108 outputs）+ 拷主仓 libisar.dylib

## 下一步候选（审计溢出 backlog）

| 优先 | 项 | 风险 | 备注 |
|---|---|---|---|
| 次 | F5/F7/F8 配置卫生批 | 低 | 死字段清理 + dropTable 引用校验器 + shop §5.7 守门 |
| 边界 | F3 stage_04_05 宝物护甲越阶+概率偏高 | 低 | 降 ≤0.30 或换 +1 阶重器，需拍板 |
| 边界 | F4 终局塔层奖励含水分 | 中 | towers.yaml 无 shenWu + 低阶回掉，设计向需拍板 |
| 需真机 | 战斗节奏 A+C 真机校值 | — | 续45 遗留，flutter run -d macos |
