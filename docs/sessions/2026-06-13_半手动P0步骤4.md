# Session 交接 - 半手动战斗 P0 步骤4 重放执行 + 文档治理

**时间：** 2026-06-13
**项目：** 挂机武侠
**分支：** main（worktree-p0-step4-replay ff 并入 + push）
**基线 → HEAD：** fa729bcc → 5119f891

## 本次完成

### A. 文档治理（阶段性审查报告采纳）
审查报告 4 项 Phase 0 核实**全属实**：
- **P1 AGENTS.md 漂移**（采纳，已修 `24142f48`）：整文件停 v1.1/5月29日，比报告
  更严重（还引用退役的 DeepSeek/Windows 端）。**用户拍板瘦身成 stub**：304→27 行，
  指向 CLAUDE.md（操作层）/GDD（设计层）单一真相源，根治双文档漂移。
- **P1 半手动 seed 未传真实入口**（属实，非回归）：stage/tower entry flow + save_data
  待步骤4/5 收口 → 本会话做步骤4。
- **P2 stress 等 tracked 产物**（属实）：还原工作树；治本（gitignore + rm --cached）
  涉 git 治理，挂账待拍板。
- **P2 CodeGraph 未 init**（属实）：`.codegraph/` 不存在，挂账可选 init。

### B. 步骤4 重放执行（spec §五 P0#4）
`BattleNotifier.replay(left, right, {seed, ops, maxSteps})`：
- startBattle(seed) 重建单一 seeded rng → `step()` 逐步推进（与步骤3c 录制同粒度，
  每整数 tick 落点）→ `state.tick == op.anchor` 时回放 `requestUltimate(targetId)`。
- `_resolveReplaySkill`：op.skillId 在该 actor 当前 availableSkills 解析；找不到跳过
  （防脏数据不中断）。maxSteps 20000 兜底防 near-immunity 死循环。
- 复用 requestUltimate → 相同锚点重新派生 recordedOps 与原 ops 全等（幂等可溯）。
- **0 改战斗内核/确定性**（rng 消费顺序不动）。

## 当前状态

analyze 0 / 全量 2022→**2025 测** / 1 skip。还原 test/tools/output 产物（不提交）。

## TDD 红线（battle_replay_execution_test.dart · 3 测）

① 手动 step 通关(记 seed+ops，crit 0.5 防空过)→ 同 seed+ops `replay` →
actionLog + 胜负全等 ② replay 后 recordedOps 重新派生 = 原 ops ③ 空 ops replay =
纯 advance 自动(同 seed)全等。

## 下一步建议

1. **步骤5 解锁+落盘** 🔴 schema 0.19.0（**改前必对齐**）：
   - `BattleReplayRecord`（battleKey + seed + ops 序列化）新 Isar collection。
   - 进度模型加 cycleIndex + 「已手动通关」判定；saveVersion 0.18→0.19 + 迁移分支。
   - 真实入口 stage_entry_flow(:325/336/345) + tower_entry_flow(:638) 传 seed。
   - 自动刷解锁判定（通关后出现「自动战斗」开关）。
   - **GDD §5.5「在线=离线」措辞调整需用户单独 ask**（spec §四草案）。
2. **步骤3c Codex 视觉验收**（派单已就绪）。
3. 挂账：stress gitignore 策略 / CodeGraph init（均待拍板）。

## 踩坑提醒

- replay 必须 step() 驱动（非 advance）：advance 跳 tick 会错过 op.anchor 落点。
- 录制锚点 = state.tick；step() 每整数 tick 都落点，replay 同粒度才忠实。
- 每跑测试 test/tools/output/*.md 被刷新，worktree commit 前必 `git checkout --`。
