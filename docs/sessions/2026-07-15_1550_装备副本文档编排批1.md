# Session 交接 - 装备副本文档编排批1

**时间：** 2026-07-15 15:50
**项目：** 挂机武侠
**分支：** `docs/equip-baicao-orchestration`（worktree，基于 main `3afe2717`；未 push、未 merge 回 main）
**最后 commit：** worktree `9e415fe6`（主 checkout main 仍 `3afe2717`，全程未碰）

## 本次完成

文档编排批1（3 修订 + companion 去向）。PROGRESS 顶段未更（纯文档未实装，留实装批）。
- **装备 design**：§8 掉级下限 +10→**+17**（补完整状态演算）；§2.1/§13/§14/§15.1 四处隐蔽 `+10 底线` 残留清零；§4 补 Q7（占用角色计入 roster）；状态→已批准待实施；AGENTS 同步。
- **baicao design**：Q2 会话托管 §5.1 / Q3 永久进度存 SaveData §8.3 / Q4 `awaitingRewardChoice` §8.3+§9.2 / Q5 reserved DTO §8.1 / Q6 命名装备保护 §6.2。
- **装备 plan**：删 `current:10/target:20` 伪造测试→真实 +17 下限测试；floor 10→17；助炼资格 +`reservedEquipmentIds`（Phase A 前默认空集，装备批可独立）；Task10 +AGENTS。
- **companion**：状态→已审查 + 文末 §9 去向表（P0 六项并入 / P1+矩阵 保留后续 / §3.3 显式不采纳新 collection）。

## 当前状态

1.0 打磨期。装备助炼+百草岭/断魂庄文档编排：批1 done，批2/3 未启动。批2/3 依赖的共享接口已冻结（occupancy/reserved DTO、SaveData 永久进度、awaitingRewardChoice、schema 0.37、发布上限 10→17）。

## 进行中的工作

- 批2 = Phase A1/A2 计划生成（CharacterOccupancyService、SaveData 字段、schema 0.37、上限10→17、溢出探针、断魂帖/托管），依赖批1（已冻结），需 `superpowers:writing-plans`。
- 批3 = B1/B2/C1/C2 + 联合经济探针 + 审查报告，依赖批2 接口冻结。

## 已知问题

- worktree `docs/equip-baicao-orchestration`（`9e415fe6`）保留供批2/3 续用，未 merge/未 push（本轮首建，续传0轮）。用户取批1：主 checkout `git merge docs/equip-baicao-orchestration`（FF）。
- 主 checkout main ahead origin 6（用户5+上轮handoff未push）/behind 2（#41）+ 2 untracked（用户/codex 战斗台面设计 md+png，留置）。push/rebase 是用户的活。

## 重要决策

- Q1 选 +17：保持「仅+20起掉级」订正恢复底线，不新增持久化状态让 +10 可达（改动最小、守克制）。
- Q3 不采纳 companion 推荐的新建 JianghuJourneyProgress collection，改 SaveData 加字段（贴合 activeCharacterIds/grantedMilestoneEquipmentIds 单一真相源模式）。
- session记录+批1 同进 worktree 分支、不动主 checkout main（main 含用户未push work，隔离交付）。

## 下一步建议

1. **（推荐）批2 Phase A1/A2 计划生成** — 接口已冻结、opus xhigh、需 writing-plans。1.5-2h。
2. 批3 B1/B2/C1/C2 + 联合探针 + 审查报告 — 依赖批2。
3. 用户先 review 批1 两决策（+17 / 不采纳 collection）再展开批2/3。

## 踩坑提醒

- 批2/3 计划接口须对真实代码核（memory `feedback_opus_audit_self_grep_lib`）：SaveData 仿 `save_data.dart:92 grantedMilestoneEquipmentIds`、`isar_setup.dart:147` 迁移段体例、`items.yaml` 副本内效果字段。
- 实装批推翻永不掉级：必先拆 `enhancement_service.dart:97` 的 `neverDegrade` throw（否则配置一改即崩）。
- session记录在 worktree 分支不在 main：新会话从主 checkout 看不到批1，需先 merge 或进 worktree 读。
