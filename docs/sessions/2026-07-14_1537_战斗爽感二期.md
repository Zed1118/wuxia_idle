# Session 交接 - 战斗爽感二期

**时间：** 2026-07-14 15:30（bg 会话）
**项目：** 挂机武侠
**分支：** main（记录所在）；工作分支 `feat/combat-feel-phase2`（draft PR #37）
**最后 commit：** main `e727322d`（本 handoff commit 在其后）；分支 `9ae4c6a5`

## 本次完成

详分支 PROGRESS 顶段「2026-07-14 战斗爽感二期实装批」+ plan `docs/superpowers/plans/2026-07-14-combat-feel-phase2.md`（含 Phase 0.5 探针证据表；均在 PR #37 内，commit 区间 `af66ea93..9ae4c6a5`）。PROGRESS 未展开的增量：
- 拍板链：切片 plan 4 项用户「按推荐」全过 → 实装；尾段用户追加拍板 **SFX 素材挂账**（backlog §七承载，borrowed 维持）。
- T2 诊断结论：首通技能占比由出手次数（≈战斗时长）主导，单发/时机/CD/气四轴探针全证伪；详 memory `feedback_wuxia_skill_share_lever_fight_length`。
- 展示帧题字三态截图（1280×720/1440×900）我已目检 PASS；图在 worktree `build/visual_acceptance/`（gitignored，随 worktree 清理消失）。

## 当前状态

1.0 长线打磨期。§十三 #2 代码侧全部收口进 draft PR #37（4 commit 全 push，分支 worktree 实测全绿）；main 本会话零代码改动（e727322d=origin/main）。CI run `29314383959` 进行中（前一 run 被 docs push 顶掉 cancelled，非失败）。

## 进行中的工作

- T4 真机手感终确认（用户环节，本批唯一剩余）→ 手感 OK + CI 绿后合并 PR #37 并勾账 backlog §十三 #2。

## 已知问题

- worktree `combat-feel-phase2` + 分支留置：PR #37 在途，合并后按 §8.2 清理。
- tempo 新基线报告（47.1%）在 worktree gitignored 输出目录未入库（工具输出约定不入 git），数字已固化进 plan 证据表。
- PR #36 三条非阻塞观察仍开放（无主修自动立主修/learnTechniqueAsMain 未消费/心法 UI 视觉 smoke），建议并入 #4 出战编成批。

## 重要决策

- 决定用首通血线（readable_first_clear 2.4→3.0/1.35→1.5）达成占比 47.1%，因为四轴探针证伪其余杠杆，且 07-09「勿硬抬 HP」的观感顾虑已由 T1 展示帧承接。
- 决定撤除试加的 `auto_skill_cooldown_cap_turns`（代码+key+测整片），因为 A/B 双跑全等=死配置（守「yaml 不留未消费键」）。
- 决定展示帧快进态「消费不呈现」，因为复刷/扫荡不该叠慢镜，且规则统一最简。

## 下一步建议

1. （推荐）T4 真机手感 + merge PR #37：本批唯一剩余环节，收口 §十三 #2。
2. §十三 #3 Windows 端实机验收：workflow 已 pin windows-2022，取 artifact 人工 D 段。
3. §十三 #4 出战编成 UI 范围拍板（30min 讨论，可并入 PR #36 观察①）。
4. §十三 #5 真人试玩（需用户安排场次）。

## 踩坑提醒

- bg 会话 cwd=home 时 EnterWorktree 必报错；手动 `git -C` 建 worktree + Bash cd 前缀，Write/Edit 在 worktree 不被写守卫拦——memory `feedback_bg_enterworktree_home_cwd_fallback`。
- 占比类 balance 调参先探针证伪再动手（本批四轴全证伪只剩时长）——memory `feedback_wuxia_skill_share_lever_fight_length`。
- 诊断测内建 ratchet（`_assertExperienceRatchet`，stage_01_05 动作行 ≥6）会拦时机类改动，改 readable 层前先看它。
- merge PR #37 后主 checkout 必 `dart run build_runner build` 再全量（`.g.dart` 不随 merge，memory `feedback_wuxia_pen_build_runner`）。
- `dart format --set-exit-if-changed | tail` 会被管道吞退出码，判定看输出里 "N changed" 字样别看 $?。
