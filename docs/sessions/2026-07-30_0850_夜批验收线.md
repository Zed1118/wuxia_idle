# Session 交接 - 夜批验收线（桌面语义 / 立绘修复 / B3 评估 / 153 关目检）

**时间：** 2026-07-30 08:50
**项目：** 挂机武侠
**分支：** main（未动）+ 4 条候选分支
**最后 commit：** `09c44486`（main）；分支 tip `954d6520` / `e17444a2` / `05bf3e8b` / `d5ec09dc`

## 本次完成

详 PROGRESS.md 顶两段（「夜批」+「Ch20/Ch21 十张立绘真机目检」，在 `night-closeout` `d5ec09dc`，未合 main）。增量：

- 产出全在 **4 条 draft PR**，main 一行未改（用户拍板「候选分支制」）。建议合并序 #101→#102→#103→#100（面从小到大）；四条文件集不相交、`merge-tree` 全 EXIT=0、CI 全绿。
- 加做两项超商定范围（时长富余，并入已有 PR 不新增分支）：非主线 48 关目检、F2 证伪。
- 清场：`build/` 陈旧 dill **5.5G 已删**（7.2G→1.7G）；遗留 worktree `ch21-dropfix` 三验后删除。

## 当前状态

1.0 打磨期验收线批收口。不动内容/数值，只做验收、缺陷修复与评估。**真机战斗屏目检首次全覆盖 153 关**（主线 105 + 非主线 48）。下一波前置 = 合并这 4 条 PR，不合则后续都建在旧基线上。

## 进行中的工作

- 4 条 draft PR 待 review/合并（#100 桌面语义面最大：10 生产 + 4 测试）。
- **5 项待拍板**（详 `docs/handoff/night_batch_closeout_2026-07-30.md` §八）：PR 合并序 / B3 四选项 / autofocus 焦点策略 / C5 行囊三格 / 送关旧部白布动势。

## 已知问题

- **4 worktree + 4 分支留置**：它们**就是这 4 条 PR 的载体**，合并前不能删。首轮挂账，合完按三验删（handoff §六）。
- `build/dispatch/ch21_lunjian_rework_20260730/` 4.1M 留置（gitignored）：派单书配方 + **`round1_keep.png`**（第二轮抬跟若判过头，这是回退件）。
- **未修**：送关旧部白布动势（语义达成、动势缺失）；autofocus 按钮焦点落点不可见；C5 行囊三格展示假游戏状态（后两项待拍板）。
- **未覆盖**：断魂庄 / 远征敌立绘（无 audit 路由）；动效瞬间帧（F2 配方已给未成批取证）。
- 711M 原始截图随 job 清；17 份复核载体已转存 `build/visual_acceptance/night_batch_2026-07-30/`（35M）。

## 重要决策

- **否决用户拍板过的 3A（加「双脚共面」判据）**：先量存量发现 75 张里 30 张高差 >120px、论剑后人只排第 20，放大实证全是**前后脚透视深度差**（前脚更近故在画布上更低=正确表达）与幻境角色消散的假报——该几何量分不出「正确/错误」，加了会一次性红掉 30 张。
- **B3 推荐选项 B 而非原方案 D**：暖度差中位仅 5.4（`stage_01_05` 只 -0.3）、主导轴是明度（中位 -48.5），且杠杆在立绘侧（`battleStandeeFusionMatrix` 等三常量）不在背景侧，调它**零触 90 关背景美术**——原否决理由「波及 90 关」前提不成立。
- **论剑后人取第二轮**（后脚抬跟）而非第一轮（双脚全平）：第二轮同时满足脚尖共面与「重心在前脚掌」；第一轮已留底可回退。
- **绿边自己修不再派 codex**：邻域中位色去边是确定性操作、可逐项验证（恰 113 像素变化 / alpha 零改动 / 绿归零），比再派一轮可控。

## 下一步建议

1. **合并 4 条 PR + 清 worktree（推荐）** —— 不合则后续都在旧基线上；CI 已全绿、冲突已复算为零。
2. B3 选项拍板；若选 B，实装前须补量塔/心魔暗场景样本定融合下限。
3. C5 行囊三格处置（换中性空槽 / 隐藏 / 维持）。
4. F2 补「战斗中/特效帧」证据图（配方 `--wait ≈ 60 + 4×N`）。
5. 断魂庄 / 远征敌立绘目检（需先加 audit 路由）。

## 踩坑提醒

三条已入 memory，此处只留指针：

- `feedback_measure_before_adding_gate_criterion` —— 加门禁判据前先量存量（本次量完 75 张才发现不具判别力）
- `feedback_check_backlog_before_investigating` —— 查异常先翻 BACKLOG（白做 25min 重扫 §一#11；朱红印章是主动要的母题不是违规）
- `feedback_desktop_keyboard_reachability_audit` —— 判「键盘不可达」须查同屏其他可达控件；默认 automatic 策略下 autofocus 不画焦点环；`parseString` 未解析 AST 下 `Foo(...)` 是 `MethodInvocation` 不是 `InstanceCreationExpression`，只认后者会一个站点都扫不到

另：`visual_capture.sh` 的窗口 id 走 **stderr** 的 `BEST=`，grep stdout 永远拿不到 id。
