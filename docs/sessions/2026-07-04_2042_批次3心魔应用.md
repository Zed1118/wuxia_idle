# Session 交接 - 终局机制型 Boss 批次3 心魔应用

**时间：** 2026-07-04 20:42
**项目：** 挂机武侠
**分支：** main
**最后 commit：** ead9c395

## 本次完成

详 PROGRESS.md 顶段「批次3 心魔应用」条（PR #22 squash `ead9c395`·CI ubuntu 全量绿·ls-remote 核实）。本会话 = brainstorm→spec→plan→6 Task Subagent-Driven TDD→合并收尾一条龙。
- 终局机制型 Boss 特性**全 5 批完结**（批次1底座/2爬塔/3心魔/4周目/5红线）。
- 模块1 镜像脆弱窗口（05/06/07 承伤乘子 0.12/0.10/0.08 + 心魔蓄力技开窗）；模块2 07 survive winCondition（撑 20 tick 或击败任一即胜）。

## 当前状态

1.0 长线打磨期。终局机制型 Boss 特性收官。无进行中切片。main = origin/main = `ead9c395`，工作树干净，全 push。

## 进行中的工作

- 无。批次3 已闭环合并。

## 已知问题

- **07 击败通道对装备良好 on-level 玩家占主导**（~6-11 tick 窗口斩杀），survive 现实中是欠配/龟缩/兜底路径（accepted design，记入 PROGRESS）。
- **镜像 first-mover 结构优势未改**：`_mirror` 缩放 HP/内力/攻击但不缩放 speed → mirror speed==player speed → `_actorOrder` tie-break `teamSide asc` 让玩家整队每回合先手。触及所有关卡的核心引擎 tie-break，本批 scope-out。

## 重要决策

- 07 首版 survive-only 诊断证实装饰性（first-mover 主导→玩家必胜），用户拍板 B：给 07 也加脆弱窗口→窗口 gate 击杀使 survive 成真实第二通道。
- 镜像注入采**原子**（vuln+蓄力技齐备才注）非计划的「无条件注 vuln」——防「vuln 无蓄力技=永久免疫」footgun（破 inner_demon_r5_redline R5.1）。
- winCondition 承载走**方案B 进 BattleState**（strategy 逐 tick 边界判），非上层判（runToEnd 已定 result 无法回溯）。

## 下一步建议

1. **无强依赖后续**——终局机制型 Boss 特性完结。（推荐）视觉验收待真机（心魔 05/06/07 蓄力题字 + 减伤反馈 + 07 survive 表现层；本批复用批次1/2 现成减伤/蓄力题字，可能零表现层新增，确认零新增则免）。
2. 若要 07 survive 成主流体验（非兜底）：窗口调紧向 0.05 + 提高 N（难度曲线决策，需拍板）。
3. 镜像 first-mover 结构优势修复（mirror speed 劣势/tie-break 调整，触及所有关卡·大 scope·需拍板是否要）。

## 踩坑提醒

- **诊断 A/B 对照臂 `copyWith(vulnerabilityMult: null)` 是 no-op**（`?? this` 保原值），对照臂须显式 `vulnerabilityMult: 1.0` 才是「全额承伤」——否则假「A≡B 机制 no-op」读数。已内联注释。
- **心魔敌是运行时镜像玩家队**（`buildMirrorEnemyTeam`），非 yaml 固定 EnemyDef——批次1/2 的 vulnerability via EnemyDef 路径对心魔不存在，注入点在镜像 BattleCharacter（memory `feedback_living_doc_state_drift`：spec §6 原表格前提失效）。
- **确定性战斗诊断走 notifier.advance/ProviderContainer 永久 listener + seed**，非 strategy.tick（memory `feedback_battle_determinism_test_via_notifier`）；诊断产生真实败率须**不归零 crit/evasion**（归零→first-mover 必胜 no-op）。
