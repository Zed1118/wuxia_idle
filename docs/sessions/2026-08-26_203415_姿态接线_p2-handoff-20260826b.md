# Session 交接 - 二阶段姿态接线

**时间：** 2026-08-26 20:34
**项目：** 挂机武侠
**分支：** main（写入走协调 worktree `coordinator/p2-handoff-20260826b`）
**最后 commit：** b0644ce5

## 本次完成

详 PROGRESS.md 顶段「二阶段结果仪表盘（2026-08-26 夜批收账后）」（commit 区间 `6a0c2945..b0644ce5`）。

- W0 接线差异分析复核后合入 main：POSTURE 39 文件/240 命中、TIMELINE 62/377、QI 71/379，三组计数逐项复算吻合（非采信执行端自报）。
- 用户拍板三条：**1B** 姿态统一累计（旧「打蓄力 Boss 即破」规则删除）、**2A** TIMELINE 只作用于玩家普攻、**3A** QI 以兵器档为基准并保留 `skills.yaml` 相对偏移。
- 派单三包冻结：W0（已合）、D 破防技可达崩溃（`[BLOCKED]`）、P1 姿态接线（`[READY]` 但复核不过）。

## 当前状态

main 停在纯文档态——本会话对 main 的改动 **0 行 `lib/`**。两条在途分支未合。P1 是二阶段战斗核心第一条真接线，语义复核通过但全量测试红，须返修后才谈合并。

## 进行中的工作

- `codex/p2-posture-wiring-20260826` @ `2c8015d9`：统一姿态模型确已落地（单一事实源、第二处窗口事实已删、未造数、禁区未碰），但全量 `5612 +/ 4 -`，**不可合**，须返 codex 返修。
- `codex/p2-defense-break-reachability-20260826` @ `39ae8f83`：仅审计 + 复现测（零 `lib/` 改动），我复跑 1/1 通过；处置待用户拍 #5。
- 我对 P1 的**破坏证红未做**（scratch worktree `挂机武侠-p2-break` 已预热待用）。
- 分支 vs main 的 Ch1 诊断 `wins/defeats` 未对比（codex 只报 `maxDamage=4419`，与 main 实测一致）。

## 已知问题

- **P1 四条全量失败（本会话实测定位，均为 P1 引入的真回归，非 flaky）**：
  1. `phase0a_source_contract_test.dart:89` —— `phase0a_combat_reducer.dart` 出现数值默认值 `"= 0,"`，违反「数值必须由调用方显式传入」红线；
  2/3. `phase0a_battle_screen_test.dart:175` 双视口（1280×720 / 1440×900）—— 普通满血敌人 `wave1_blade` 常驻姓名「山贼刀客」被渲染，违反首屏威胁去噪 HUD；
  4. 同文件「键盘 J 普攻」用例 —— 血条 key `phase0a_hp_wave1_archer` 应消退未消退。
  2/3/4 同一根因：新增 `_BossStatusTag` 姿态计数把普通敌人的姓名/血条一并拉了出来。
- **`flutter test` 退出码是 0，掩盖了这 4 条失败**；codex 全程未跑全量，其 `[READY]` 不含全量证据。
- worktree 债 169 条本轮未清（协调锁与在途分支仍在用，清理需独立批次）——**已续传第 2 轮，下轮强制处置**。
- 协调锁 `~/.claude/locks/coordinator__...挂机武侠.json` 持有者是本 session `799898d4`、pid 已死，属同 session 陈锁，下轮可安全回收。

## 重要决策

- 决定**不合 P1**，因为四条失败中三条是玩家可见 UI 回归、一条是红线契约违规，任一都过不了 §8.2 Gate。
- 决定把姿态五字段冻结块（`data/numbers.yaml`）留在分支 `b98b363c` 不单独合 main，因为解析与消费方必须同 commit 落地，否则 main 会出现「配置存在但无消费方」的脱节态。

## 下一步建议

1. **（推荐）把 P1 连同四条失败清单返给 codex 返修** —— 根因已定位到 `file:line`，返修范围小、不需重设计。
2. 用户拍 **#4 真人试玩门槛**（A 合 main 前须过试玩 / B 豁免 / C 连已合的 TOKEN 一并回滚）。
3. 用户拍 **#5 破防技崩溃处置**（A 并入 POSTURE 批按姿态伤害处理 / B 独立止血批 / C 砍字段）。
4. 先定义 M0–M9 权重再继续 —— 整条 TUNE-* 接线不推动 `1/10` 这个权威分母。

## 踩坑提醒

- `flutter test` 报 `-N` 时**退出码仍是 0**，且 reporter 把累计 `-N` 打在「刚跑完的那条」旁边而非失败那条 —— 必须 `tee` 全量 log 再抓 `[E]` 块定位；`| tail` 会毁掉失败详情（memory `feedback_multi_anchor_test_actual_attribution`）。
- 执行端自报「独立 RED 5/5 + 逐文件 39/39 全绿」可以句句为真而全量仍红 —— **逐文件绿 ≠ 全量绿**，跨文件的源码契约测与 UI 去噪测只在全量里才被触发。
