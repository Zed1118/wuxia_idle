# Session 交接 - 半手动战斗 P0 步骤3c 单步战斗 UI

**时间：** 2026-06-13
**项目：** 挂机武侠
**分支：** main（worktree-p0-step3c-manual-ui ff 并入 + push）
**基线 → HEAD：** ae5e79c8 → fa729bcc

## 本次完成

P0 步骤3c「单步战斗 UI」（spec §五 P0#3）。地基(3b stepOne/step + actorQueue +
requestUltimate targetId)已齐，本步**纯 UI 接线，0 改战斗内核/确定性**。

**用户拍板交互流（2026-06-13）**：
- **A 强制停顿**：每点「下一步」= 一次 `BattleNotifier.step()`（一步一 actor）。
- **B 立即弹**：点单体技即弹目标 picker 选敌。
- **C 临时入口**：`manualStep` flag 注入；正式自动/手动开关留步骤5 接落盘判定。

**实装**（battle_screen.dart + 3 debug/visual + strings）：
- `BattleScreen.manualStep`：单步模式不启自动 Timer（gate `!manualStep`）。
- `_NextStepButton`：底部快进位换实心「下一步」→ `step()`。
- `_ActorOrderBar`：tick 边界填队列后顶部显「本回合行动顺序」（队首高亮，
  读 `state.actorQueue`，队列空=tick 边界则隐）。
- 目标 picker：`_TargetPickerDialog` 列存活敌人（右队 isAlive）→
  `requestUltimate(targetId)`；自动模式不弹、直接下发（现有行为不变）。
- C 入口：`ScenarioLauncher` 加 `manualStep`+`seed`（startBattle 传 seed）/
  `VisualRoute.battleManualStep`（复用 scenarioChargeBreak 满配主控 + seed=3）。
- 3 UiStrings：battleNextStep / battleActorOrder / battleTargetPickerTitle。

## 当前状态

analyze 0 / 全量 2014→**2022 测** / 1 skip。还原了 balance 产物
`stress_2026-06-02.md`（测试跑时重写，不提交）。

## TDD 红线（battle_manual_step_ui_test.dart · 8 测）

入口显隐(manualStep true/false) / 点「下一步」调一次 step() / 行动顺序条显隐
(actorQueue 非空/空) / picker 列存活敌人(死亡 13 不在) / 选目标带 targetId 下发
/ 自动模式不弹 picker 直接下发(targetId=null)。

## 下一步建议

1. **Codex 视觉验收**：派单 `docs/_archive/codex_dispatch/codex_dispatch_manual_step_ui_2026-06-13.md`，
   `VISUAL_ROUTE=battle_manual_step`（下一步推进 / 行动顺序条 / 点单体技弹 picker）。
2. **步骤4 重放执行**：同 seed 重建 + 相同锚点回放 recordedOps（地基齐）。
3. **步骤5 解锁+落盘** 🔴 schema 0.19.0 单点收口，改前对齐 + GDD §5.5 ask。

## 踩坑提醒

- `_onSkillCommand` 改 async（await picker 后用 ref，先 `!mounted` 守卫）。
- 单步模式「下一步」战斗结束后仍显但 step() 内部 `isFinished` 守卫，点击 no-op。
- 行动顺序条仅 manualStep 显（自动模式不挂，零影响现有战斗屏）。
