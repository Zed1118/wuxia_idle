# Session 交接 - M2 被动离线挂机 + P1b 进度 wiring

**时间：** 2026-06-15
**项目：** 挂机武侠
**分支：** main
**最后 commit：** c48bfcb1

## 本次完成

详 PROGRESS.md 续12（M2 范围 B `fa58e5bc..212b572c`）+ 续13（P1b `d8112b29..c48bfcb1`）。
- M2 范围 B 通用被动离线挂机：brainstorming 9 决策→spec→plan→7 Task TDD subagent-driven→Task4+final review→合 main。
- P1b：technique_panel 修炼度 tile 接 StageProgressRow，四系统进度展示收口（残页保留方块）。

## 当前状态

1.0 长线打磨期。M2 被动 + P1b 均合 main + push（origin c48bfcb1）。
全量 2231 测 + 1 skip 零回归 / analyze 0（主 checkout 实测）。工作树干净、无孤儿 worktree/分支。

## 进行中的工作

- 无。两任务均闭环合并。

## 已知问题

- 视觉验收挂账（bg 测不到 GUI，攒一轮真机/Codex）：M2 被动卡+lifecycle 写入实效、
  L3 题字/横幅配色、P1b StageProgressRow 嵌入 technique tile 协调。
- 续9 心魔入口锁（闭关期间禁入心魔）待用户拍板范围。

## 重要决策

- M2 闭关与被动互斥（不叠加）+ 旧档首启不回溯（lastOnlineAt==createdAt 守卫）——
  防双倍产出 + 老玩家不吃巨额回溯产出。
- P1b 残页保留方块不接 StageProgressRow——离散收集 X/N 片语义比连续进度条直观（用户拍板）。

## 下一步建议

1. （推荐）playability backlog 无依赖打磨项（P3 战报诊断规则等）——不卡拍板可立即动手。
2. 攒一轮视觉验收（M2/L3/P1b）——手动（我出截图清单）或派 Codex。
3. founder_buff 跨派系扩（P4.1 1.1，需先拍 B1-B3 范围）。

## 踩坑提醒

- fresh worktree 缺全部 .g.dart（gitignored）→ 进 worktree 先 build_runner，否则
  analyze 报 1484 假 error；schema 批 merge 回主 checkout 也要重跑（memory feedback_wuxia_pen_build_runner）。
- 升 saveVersion 必 break 全仓 hardcode 旧版本断言——grep 全仓同步，别只改 scoped
  （本 session implementer 漏 2 文件，回归才暴露）。
- testWidgets 内 Isar writeTxn 撞 "Cannot add event while adding stream"→用
  tester.runAsync + 直接 await 目标函数（捕获 context/ref，不靠 button tap）。
