# Session 交接 - 半手动战斗 P0 步骤3c/4/5 收官 + 文档治理

**时间：** 2026-06-13
**项目：** 挂机武侠
**分支：** main
**最后 commit：** c15ddb88

## 本次完成

详 PROGRESS.md 顶段 3 条（步骤3c `86f395f4` / 步骤4+文档治理 `5119f891`·`24142f48` /
步骤5 `c15ddb88`·`ce62fdf2`）。各步交互体例在 `docs/sessions/2026-06-13_半手动P0步骤{3c,4,5}.md`
展开（步骤5 含审查报告采纳记录）。

## 当前状态

半手动战斗 P0 **地基全收口**：手动单步 UI（3c）→ 操作录制 → 重放执行（4）→ seed+ops
落盘 + saveVersion 0.19.0（5）。确定性闭环地基齐，**未接真实战斗入口/自动开关 UI**。

## 进行中的工作

- 步骤5 全闭环：真实入口 stage/tower 接 manualStep 首通 + 传 seed + 通关写 record →
  通关后「自动战斗」开关 UI → 开自动走 `BattleNotifier.replay`。需 Codex 视觉 + 真玩。

## 已知问题

- 步骤3c 单步 UI 待 Codex 视觉验收（派单在库；验收包用 `./tool/build_acceptance.sh`
  从 main 重建，VISUAL_ROUTE=battle_manual_step）。
- test/tools/output/*.md balance 产物 tracked 每跑测试刷新（治本 gitignore+rm --cached 待拍板；
  当前 commit 前 `git checkout --` 还原。续传 1 轮）。
- CodeGraph 未 init（可选 `codegraph init`，续传 1 轮）。

## 重要决策

- GDD line 81「在线=离线」加注不改原文（采纳 spec §四草案）：自动刷=已通关关产出仍在线=离线、
  手动=一次性内容门槛非留存——半手动方向需澄清红线。
- cycleIndex 不入 progress 模型（YAGNI，周目 P1 接；cycle 已在 battleKey）——schema 改动最小化。
- AGENTS.md 瘦身 stub 指向 CLAUDE.md（审查报告 P1）——双大文档漂移单一真相源根治。

## 下一步建议

1.（推荐）步骤5 全闭环——半手动核心循环唯一缺口，地基齐；改所有战斗入口首通手动是大 UX 变更，
   需 Codex 视觉+真玩拍板，建议新会话专做。
2. 步骤3c Codex 视觉验收——派单就绪，1 命令重建验收包即可闭环。
3. P1 周目进化（江湖记招/问鼎轮回）——cycleIndex 此时接入 progress。

## 踩坑提醒

- Isar schema 升版：`git grep "'0.X.0'" test/` 全仓找断言版本的测试逐个改（本次 3 处）。
  collection 访问器 `isar.xxxs` 在 .g.dart part，测试须 import domain 文件。详 [[feedback_isar_pitfalls]] §3/§8。
- schema 批 merge 回主 checkout 必先 `dart run build_runner build` 再测（.g.dart gitignored）。
  本次已重生成 + 全量 2036 实测绿。
