# Session 交接 - Lv100 里程碑实装（cap 10→17 + 触发接线）

**时间：** 2026-07-16 12:01
**项目：** 挂机武侠（Flutter Desktop 写实武侠挂机）
**分支：** feat/baicao-duanhun-phase-b（worktree `baicao-duanhun-phase-a2`·**未 push·未合 main**）
**最后 commit：** `f4366e0c`

## 本次完成

详 PROGRESS.md 顶段「2026-07-16 江湖远行 Lv100 里程碑批」（本会话 +1 commit `f4366e0c`）。
- 严格 TDD 四切片：mount_deferred 豁免红线⑦ → cap 10→17 → Lv100 触发接线 → 全量验证。
- Phase 0 外自查捕获 4 处 plan 未名的 cap 耦合站点并 reconcile（详 memory `feedback_wuxia_release_cap_raise_reconcile`）。

## 当前状态

江湖远行 Phase B 全线 + Lv100 里程碑（#2）收口。发布上限已二流化（Lv170），Lv100 永久解锁江湖远行入口已接线。分支 8 commit 全未 push。

## 进行中的工作

- 无进行中切片，#2 四切片全绿完成。
- batch3 联合经济探针 + Phase C 断魂庄未启动。

## 已知问题

- **live 主菜单江湖远行入口目检未做**（残留 nice-to-have）：gate seam 已 Isar 测 + 镜像已验 `maybeSettleExpedition`，未驱动真机目视。
- **2 招正式挂载待 batch3/Phase C**：千钧坠岳/烛影摇红 mount_deferred 豁免中，挂载时删标记=发布。
- codex worktree `battle-ui-stage`（tip `d6cf4d1d`·活跃写）勿碰——非本会话债。

## 重要决策

- **触发线钉 abs 层10=Lv100，不随 cap 走**：Lv100 是江湖远行内容起点，cap=17 是成长上界；`kJianghuJourneyUnlockAbsoluteLevel=10`。
- **单 gate 挂载点而非每 advancement caller 接线**：flag 永久且只 gate 菜单入口，`maybeUnlockJianghuJourney` 挂 MainMenuStartupGate post-frame 一处覆盖 fresh-reach + 旧档补触发，避 8 处 caller 侵入接线。
- **mount_deferred 配置化豁免而非红线硬编码 id 名单**：删标记=发布、可留痕。

## 下一步建议

1. **（推荐）你 push feat/baicao-duanhun-phase-b**——8 commit 全未 push，push 是你的活。
2. batch3 联合经济探针（敌队/伤势深度曲线 + Lv100→170 节奏 D2·涉 schema/saveVer）。
3. Phase C 断魂庄（`BossGauntletConfig` 骨架已在·2 招正式挂载点候选）。
4. 可选：seed ≥Lv100 角色真机目检江湖远行入口出现。

## 踩坑提醒

- **抬发布上限 reconcile 清单**：见 memory `feedback_wuxia_release_cap_raise_reconcile`——挂载完备测补 `!mountDeferred` / R5.3 停旧cap 改 cap-agnostic / stale 文案 / 触发线≠cap。cap 会再抬（17→…），下次必复用。
- **红线⑦ mainlineDrop 检查先于 fragment 抛**：cap 抬高一次只暴露一个孤儿；测 opt-in 豁免须选 fragment 招验才是 clean RED（选 mainlineDrop 会假绿）。
