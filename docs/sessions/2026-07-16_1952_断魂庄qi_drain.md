# Session 交接 - 断魂庄 C1.3.1 qi_drain 引擎接线

**时间：** 2026-07-16 19:52
**项目：** 挂机武侠（Flutter Desktop 写实武侠挂机）
**分支：** feat/baicao-duanhun-phase-b（worktree `baicao-duanhun-phase-a2`·**未 push·未合 main**）
**最后 commit：** `e001c164`

## 本次完成

详 PROGRESS.md 顶段「2026-07-16 江湖远行 Phase C 断魂庄批」③ C1.3.1（代码 `2116db97` + 进度 `e001c164`）。
- 升 xhigh，执 executing-plans + 严格 TDD（红绿双验）做 C1.3 第一子切片 qi_drain 引擎接线（风险点·先做）。

## 当前状态

Phase C 断魂庄 C1 组：C1.1/C1.2 纯域 + **C1.3.1 引擎接线 ✅**。C1.3.2（敌队 EnemyDef）/C1.3.3（runner+advance）/C2 全组未开工。分支全未 push。

## 进行中的工作

- 无进行中切片（C1.3.1 全绿已提交）。C1.3.2/C1.3.3 落点写全在 plan `2026-07-15-baicao-duanhun-phase-c-gauntlet.md` 当前恢复点。

## 已知问题

- codex worktree `.worktrees/battle-ui-stage`（tip `f29ccef5`·活跃写）——非本会话债·勿碰（续传 ≥2 轮·用户/codex 侧）。
- PROGRESS 现 **106 行**（超 100 目标 6 行）：batch3 与 Phase C 两条同日「江湖远行」条目，Phase C closeout 时合并压缩下修（未中途删以免误伤历史）。
- 探针产物 `test/tools/output/joint_economy_probe_2026-07-16.md`（gitignored·batch3 留置·可重生）。

## 重要决策

- **测 harness 选型**：plan 建议 notifier.advance；memory `feedback_battle_determinism_test_via_notifier` 的 notifier 路径针对**种子分歧**测，本 wiring 测非此类 → 走 `p0_charge_break` 同款 strategy 直驱（手搭 mid-charge 态 + `Random(seed)`），更贴切、同样确定性。
- **schema fail-fast 而非只靠 QiDrainEffect 运行期 assert**：加 `game_repository` load 期 `qiDrainPct∈[0,0.5]` 守卫，越界配置启动即抛（守 §9·§5.4），不留战斗中崩点。
- **停在 C1.3.1**：引擎子系统收口，C1.3.2 转敌队内容/数据子系统，沿「slice 边界停、下个硬活满上下文做」打磨期节奏。

## 下一步建议

1. **（推荐）C1.3.2 敌队 3 队 `EnemyDef`**（opus·xhigh）：苏无咎/石镇岳/闻九针，机制全走 bossPhases/guardianWard/vulnerability 既有字段 + 数值占位 `TODO(batch3-probe)`；合成参照 `expedition_combat_runner._synthesizeEnemies`→`StageBattleSetup.buildEnemyTeam`。
2. C1.3.3 `gauntlet_battle_runner` + `GauntletController.advance()`（沿 `ExpeditionBattleRunner`·seed=`stableSeed(saveId,gauntletRunId,stage)`）。
3. C2 组（入场扣帖/托管/恢复/奖励三选一/UI），大·多切片；C2.4 奖励前需用户拍板 2 孤儿招挂载。

## 踩坑提醒

- **C1.3.2 加锁脉针招进 skills.yaml → 撞 `test/data/skill_qi_redline_test.dart:57` `hasLength(246)` 硬计数**，同步 bump（并复查其他 skill 计数/内容完备测）。
- `_resolveAction` qi_drain 注入位置：targetAfters 写回后、`next` 组装前，对 `oppSide` 存活成员施效改 `left/right`；**勿在 rng 消费点前插**（破确定性）。
- 执行既有 plan 内嵌测前必 critical-review（C1.1 曾有 70/58 矛盾期望）。
- **push 是用户的活**：分支全未 push，勿 push / 勿合 main / 勿 force。
