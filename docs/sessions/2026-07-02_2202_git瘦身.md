# Session 交接 - .git filter-repo 瘦身

**时间：** 2026-07-02 晚
**项目：** 挂机武侠
**分支：** main
**最后 commit：** ea96b211（= origin/main 现跑 ls-remote 核实同步）

## 本次完成

详 PROGRESS.md 顶段「2026-07-02 `.git` filter-repo 瘦身」条（commit 区间 `caba2422..ea96b211`，全历史重写 + 1 chore commit，全 push）。
- `.git` **2.5G→658M（减 74%）**：安全 gc 清不可达 2.5G→1.1G（零重写）→ filter-repo 剥 docs 图片 500M + 删 121 codex 垃圾 refs → 658M。
- 692 docs `.md` 全保留 / docs 图片历史残留 0 / 415 lib dart 零改动。

## 当前状态

1.0 长线打磨期。批次 2 剩余项①（.git 瘦身）已闭环 push。核心质量优，工程外围继续收。无在途代码。

## 进行中的工作

- 无在途代码。

## 已知问题

- **GitHub 远端磁盘体积非即时缩**：force push 已顶掉旧 ref，旧对象变不可达，GitHub 服务端 gc 择期回收（不可控）。fresh clone 体积立刻小。
- `.git` 全备份留 job tmp（`git_backup_wuxia`，2.5G）：job 删除时自动清；确认无碍后可提前删。

## 重要决策

- 决定分两杠杆（安全 gc + filter-repo）而非一把 filter-repo，因 gc 免费拿走一半（2.5→1.1）且零风险，只有剩下的可达废图才需 force push。
- 决定只剥 docs 图片、不顺带剥旧 PNG 资产（用户拍板），因后者按 blob-id 操作更复杂、收益小（~145M）。

## 下一步建议

1. （推荐）零引用资产清理 `tier_*` 7 张——需先拍板「接线 or 删」，纯资产处置低风险。
2. audio `_suno_candidates` 75M 处置（归档/删，5-15min）。
3. 批次 3 死代码/死文案（66 篇文案「接线 vs 归档」拍板）。
4. P2 零散批（中文散写/clamp 进 yaml/README，无拍板依赖可直接做）。

## 踩坑提醒

- filter-repo × codex refs 坑已沉淀 memory `feedback_git_filter_repo_codex_refs_pin`：先安全 gc；`refs/codex/*` turn-diff 检查点钉活已剥 blob 必须先删再 gc；force push 全新对象上传慢用后台；远端 sha 一律 ls-remote 现跑核实。
