# Session 交接 - 拖招验收闭环 + 红线语义收口

**时间：** 2026-06-14
**项目：** 挂机武侠
**分支：** redline-semantics-twolayer（已 ff 合 main）
**最后 commit：** 1674b099

## 本次完成

详 PROGRESS.md 顶段「续3 红线语义收口」+「战斗交互重做全线闭环」两条（commit `882a1c11..1674b099`）。
- 战斗交互线收尾合 main：拖招验收(Codex aoe PASS / 长按拖鼠标合成无法触发→真玩) + 新 `battle_drag_live` route + 真玩长按确认 + ff 合 main。
- 外部审查 P0/P1/P2 核实+收口：红线分两层 + 软红线测(真实派生路径) + 3 处文档漂移修。

## 当前状态

origin/main `1674b099`，1.0 长线打磨期。战斗交互重做 + 红线语义收口两条线均已合 main 闭环。无半成品。

## 进行中的工作

- 无半成品代码。后续为新方向（见下波候选）。

## 已知问题

- **P1 balance 证据偏旧**（外部审查未处理项）：reviewer 建议用满强化生产派生值刷新 balance simulator ceiling，未做，留下波。
- `docs/acceptance_screenshots/` untracked = Codex 验收截图，throwaway 政策不入库（留置，本地可删）。
- **主 checkout 停在旧 `db61dd16`**：需 `git checkout main && git pull && build_runner build`（.g.dart gitignored，battle-drag 线含 selected_cycle_provider.g.dart + saveVersion 0.22/0.23）后才能测/跑。
- worktree `battle-drag-cycle-chapter`(分支 redline-semantics-twolayer) 已合 main，/clear 后可 `git worktree remove` + 删分支（已续传 1 轮）。

## 重要决策

- **红线分两层**（用户拍板）：硬红线=配置基础表值(装备攻≤2000 等 schema 拦截)；软红线=极值满 build 实战可见值(普攻不进十万·实测 5.8万/暴击8.7万·绝对不进百万)。因 8000 硬界与满强化现实分裂(实测击穿 4-7×)，p1a_redline_test 早自承「设计指南」。
- 拖招真玩只确认长按可用即放行，引导线/光晕等表现层细节留后续微调（核心手势链通即足）。

## 下一步建议

1.（推荐）balance simulator 刷新：用满强化派生值跑 ceiling，看极值 build 在周目进化/敌人 scale 下到底失衡多少——理由：外部审查唯一未处理项，红线分两层后需重测平衡现状。
2. 拖招表现层微调：引导线色/hover 高亮/蓄势光晕/快进间隔（Codex 截图闭环 + 真玩）。
3. 清理 worktree + 同步主 checkout（build_runner）。

## 踩坑提醒

- `ScenarioLauncher` 透传 `allowPlayerIntervention` 是本次新补——debug route 进真战斗默认挂机自动(allowPlayerIntervention=false)拖招层不挂，验拖招必走 `battle_drag_live` 或翻关卡「允许拖招」toggle。
- 写满强化红线测必走真实 `CharacterDerivedStats.effectiveEquipmentAttack` 派生路径，不能用扁平 equipmentAttack 值（否则系统性漏测强化×共鸣×开锋三层乘子，详 `test/balance/full_build_damage_redline_test.dart`）。
- AI `_pickSkill` 自动放 ready 的 powerSkill(burst)，ultimate 类只走 pending 手动——做「拖慢」战斗 sandbox 时主控只配普攻+ultimate。
