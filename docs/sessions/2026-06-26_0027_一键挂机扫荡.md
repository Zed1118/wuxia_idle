# Session 交接 - 一键挂机扫荡

**时间：** 2026-06-26 00:00
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 837299fe

## 本次完成

详 PROGRESS.md 顶段「2026-06-26 一键挂机扫荡实装」（commit `ff54bb45..837299fe`，已 push）。
- 用户拍板新增需求（非原第六阶段战斗轴）：扫荡按钮做明显 + 快速连播可中途停 + 战败停在该关 + 照常掉落（塔按现规则仅残页）。
- 架构：纯逻辑（门槛/recap/控制器）TDD → 结算复用既有 victory 路径提公开 → SweepScreen 逐关托管真战斗 → 双入口按钮。

## 当前状态

1.0 长线打磨期。一键扫荡已合 main 并 push，未实玩（headless 跑不了真实战斗渲染）。

## 进行中的工作

- 无半截工作。扫荡功能闭环合入。

## 已知问题

- 扫荡 happy-path 未端到端实测（连播手感/掉落真入账/recap 总账/按钮醒目度）—— 待用户 `flutter run -d macos` 副屏目检。结算复用既有已测路径，纯逻辑 TDD，但「逐关循环+前进+关间过场」wiring 只在真战斗里走得到。

## 重要决策

- 塔扫荡「按现规则」（用户拍板）：重打仅掉技能残页，不掉装备/银两/经验——守 §5.1 防刷塔，不破红线。主线重打照常掉（既有 farming 规则）。
- 按钮未解锁时**隐藏**（非灰显）：守 §5.7 + 修了 stage_list widget 测的 viewport 挤出回归（灰版占高度把靠后关卡挤出测试视口）。

## 下一步建议

1. **形与势 16 项战斗轴**（推荐）：原第六阶段，框架已确认（⭐#1/2/4/11/12/16）未动工，回主线战斗深度。
2. 用户实玩扫荡后反馈调整（手感/加速速度/recap 展示）。
3. 桃花岛二期类目 / 场景化主屏（待美术）。

## 踩坑提醒

- **新加 ListView 头部 widget（按钮/卡片）会挤出靠后 item**：stage_list/tower widget 测在受限视口下 ListView 懒渲染，加任何占高度的头部元素都可能让靠后关卡不渲染 → 既有计数/findText 测回归。未解锁隐藏（SizedBox.shrink）或扩 viewport。
- **轻量 widget 测读 numbersConfigProvider 崩**（GameRepository 未初始化）：SweepScreen 把 config 读取移进真正渲染 BattleScreen 的分支，recap/preparing 分支不读 → 战败 recap 路径可在无 GameRepository 下测。
- **结算复用走「提公开」非「抽取」**：`_applyVictoryResolution`→`applyVictoryResolution` 仅改可见性，主 victory 路径零行为改，规避抽取大函数的回归风险。
