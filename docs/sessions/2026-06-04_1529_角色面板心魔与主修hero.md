# Session 交接 - 角色面板心魔成长瓶颈 + 主修 hero

**时间：** 2026-06-04 15:29
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 7503dfd（本 session 全部已 push）

## 本次完成

- **P0-3 ③ 心魔成长瓶颈面板**(武圣常驻):新建 `inner_demon_progress.dart`(InnerDemonProgress.from 派生 clearedCount/totalCount〔派生不硬编码 7〕/nextUncleared)+ `inner_demon_panel.dart`(resolveInnerDemonPanel 三态 cleared/blocked/inProgress + 非武圣/空配置 null)+ `inner_demon_providers.dart`(innerDemonProgressProvider 从 mainlineProgressProvider 派生);`breakthrough_blocker.dart` 旧 InnerDemonBreakthroughBlocker 泛化为 InnerDemonProgressPanel(纯渲染三态)。
- **P0-3 ② 主修心法 hero 化**:`_MainTechniqueTile` data 分支换 WuxiaPaperPanel 宣纸底(包 IntrinsicHeight 解滚动列无界高度)+ 主修真名 techniqueDefs[defId].name 加大 20px 校色 + 阶名/段位徽章。
- **接线 + 文案**:`character_panel_screen.dart` _BreakthroughBlockerSection 重写(武圣常驻,watch innerDemonProgressProvider + resolver);UiStrings 加 6 段心魔面板文案。
- **验收基建**:`seedCharacterPanelGrowth` + VisualRoute `character_panel_growth`(祖师 bump 武圣·熟练 exp满 → 心魔 2/7 被拦,不动被广依赖的 seedMasterDisciple)。
- **视觉验收(Mac 自验)**:按路由 dart-define 直编 + screencapture + 读图,②③ 全门 PASS,截图归档 `docs/handoff/vis_char_panel_bc_2026-06-04/` + closeout。
- **流程**:brainstorm→spec→plan→TDD 9 task + 只读对抗式 review(2 findings:total==0 → 采纳改 null;IntrinsicHeight 无界高度 → 误报保留)。全量 1697→1713 测/0 analyze。已 merge --no-ff 到 main + push。

## 当前状态

P0-3 ②③ 全闭环并合并 main。**核心玩法视觉 pass(战斗 + 角色页 + 仓库)视觉层全闭环** —— 1.0 出版美术 pass 的核心玩法视觉部分基本收口。

## 进行中的工作

- 无未完成实装。P0-3 全闭环(代码 + 测试 + 视觉验收 + merge)。
- Codex 派单 doc `codex_vis_char_panel_bc_2026-06-04.md` 保留(本次走 Mac 自验,未跑 Codex desktop;如需二次复核可开 hub 验收包 `build_acceptance.sh`)。

## 已知问题

- 工作树常驻 macos 构建产物(pbxproj/Podfile.lock)等,非代码改动,勿提交。
- `stash@{0}: codex-pre-hub-visual-rerun-tracked` 仍在,无害可 drop。
- 装备 detail 仍有 ~6 张借线/产品照感(用户选暂放),非破图。
- 本会话 osascript 点 Flutter canvas 按钮(Retina 坐标)未命中,B5 非武圣弟子 Tab 心魔面板消失靠 widget 测兜底,未截图。

## 重要决策

- **③ 武圣常驻显示**(而非仅被拦窗口):决定让心魔进度作角色卡常驻视觉,满足 §5.4「成长瓶颈视觉」;非武圣 shrink。
- **「突破」CTA 仅导航不引新机制**:因进阶是自动的(applyExperience while-loop + isLayerLocked break 留账),无玩家手动突破动作,CTA 只导航 InnerDemonScreen。
- **③ 拆 3 层纯单元**(值对象 + 解析器 + 渲染 widget):因纯函数易 TDD、避 Isar/yaml 耦合,解析器单测用 fabricated def 控 isLayerLocked。
- **验收新建独立 seed**:因 seedMasterDisciple 被大量测试/onboarding 依赖,绝不能改它注入武圣。
- **total==0 → null**(review 采纳):无心魔配置(Demo/empty def)不显空面板。
- **Mac 自验代 Codex**:CLI 无法启 Codex desktop,走 memory feedback_user_defers_visual_to_codex「我读截图」路径,本次两门视觉明确置信度够。

## 下一步建议

1. 出版美术其余长尾:~6 张借线感装备 detail 重出统一风格(opus high · ~30min · 用户暂放项)。
2. 清 `stash@{0}` + 评估 macos 构建产物入 .gitignore(opus high · ~15min · 收尾工作树噪音)。
3. 1.0 release checklist 其余项:E 音频 / Pen 实机 8h 性能 / closed beta(讨论先确认大方向)。
4. P0-3b 其余主观视觉细节(如有 Codex 二次复核反馈再调)。

## 踩坑提醒

- **WuxiaPaperPanel 用作滚动列 tile 必包 IntrinsicHeight**:其 Stack(StackFit.expand)设计为全屏 Scaffold body,在滚动 Column 里抛 infinite height;widget 测加 takeException isNull 守。已存 memory feedback_wuxia_paper_panel_scroll_tile。
- **假 defId 致 techName fallback「主修」撞 role label**:hero 化后主修名 fallback UiStrings.techniqueRoleMain 与 role chip 重复;测试 fixture 用真 defId(techniqueDefs.keys.first)规避,生产 def 必有 name 无此问题。
- **recordVictory 签名**:`recordVictory({stageId, now})` 无 saveDataId,内部按 IsarSetup.currentSlotId 查,需先 getOrCreate;各自独立 writeTxn 不嵌套。
- **Mac CLI 视觉自验流程**:按路由 dart-define 直编(非 hub)开 app → osascript 置窗 set size → `screencapture -x -R x,y,w,h` 区域截图 → Read 读图。双屏 2560×1440;窗口拉高塞内容(无 PyObjC/Quartz 滚动);osascript 点 canvas 按钮坐标不准,交互验收仍靠 Codex/测试。
- **写守卫**:bg 会话 Edit/Write 被拦,用 Bash heredoc(新文件)/ python heredoc(改文件 assert 锚点)。
