# Session 交接 - 远征目检与 Lv100 里程碑规划

**时间：** 2026-07-16 10:45
**项目：** 挂机武侠（Flutter Desktop 写实武侠挂机）
**分支：** feat/baicao-duanhun-phase-b（worktree `baicao-duanhun-phase-a2`·**未 push·未合 main**）
**最后 commit：** `f546bd97`

## 本次完成

详 PROGRESS.md 顶段 2026-07-16 Phase B 条目「① 目检已完成」段（commit 区间 `8ef0d5f8..f546bd97`·3 commit）。
- 候选 #1 目检：expedition_overview/active @1280×720/1440×900 四图全 PASS + async-config-race 真机复验通过。
- 附带修**验收 seed 幂等缺陷**（visual_capture 多分辨率复用同磁盘库·`seedTeamLineup` 漏清 expeditionRuns）——已出 memory `feedback_visual_capture_seed_idempotency`。
- 候选 #2 Phase 0 摸透 + 用户拍板 + plan 固化（**未实装**）。

## 当前状态

Phase B 全线收口 + 候选 #1 目检完成。候选 #2（Lv100 里程碑 cap17）Phase 0 摸透、用户拍板选项 A + 升 xhigh、plan 落盘，**未动实装**，等 xhigh 新会话执行切片 1。

## 进行中的工作

- **#2 三实装切片未开始**：切片1 红线⑦豁免（`SkillDef.mountDeferred`）→ 切片2 cap 10→17 → 切片3 Lv100→jianghuJourneyUnlocked 触发接线 → 切片4 全量验证。恢复点 = plan `docs/superpowers/plans/2026-07-16-baicao-lv100-milestone-cap17.md`「下一步：切片 1」。

## 已知问题

- **Lv100 触发未接线**（本批只规划未实装·plan 切片3·设计 companion:79「任一角色首达 Lv100 永久解锁」）。
- codex worktree `battle-ui-stage`（tip `acdd1854`·活跃写）勿碰——非本会话债。

## 重要决策

- **#2 拍板选项 A**（提 cap→17 + 豁免 2 招）+ 升 xhigh（用户 2026-07-16）。因二流内容（远征真掉落/断魂庄）未做、2 招正式挂载点不存在，先豁免让 cap 落地、里程碑现达成。
- **豁免机制 = `SkillDef.mountDeferred` 配置化**，非红线层硬编码 id 名单（配置化+留痕，挂载时删标记=发布）。

## 下一步建议

1. **（推荐）xhigh 新会话执行 #2 plan 切片1-4**——plan 已固化零重复，keystone 里程碑落地。
2. batch3 联合经济探针（敌队/伤势深度曲线 + Lv100→170 节奏 D2 + 「重要战斗」result 富化·涉 schema）。
3. Phase C 断魂庄（`BossGauntletConfig` 骨架已在·填敌队/机制·2 招正式挂载点候选）。

## 踩坑提醒

- **验收 seed 幂等**：见 memory `feedback_visual_capture_seed_idempotency`（Phase C 断魂庄会话态 visual_route 会重蹈）。
- **红线⑦只扫 stage/tower 不扫远征节点**（`game_repository.dart:1919`）——2 招不能靠挂远征满足红线，故走豁免机制。
- **push 是用户的活**：phase-b 全未 push，勿 push/合 main/force。
