# Session 交接 - 百草岭远征 B2.4 交互层

**时间：** 2026-07-16 09:07
**项目：** 挂机武侠（Flutter Desktop 写实武侠挂机）
**分支：** feat/baicao-duanhun-phase-b（worktree `baicao-duanhun-phase-a2`·**未 push·未合 main**）
**最后 commit：** `784d8383`

## 本次完成

详 PROGRESS.md 顶段 2026-07-16 Phase B 条目（本会话 commit 区间 `07c91bab..784d8383`，7 commit·严格 TDD·9 新测）。
- Phase B **全线收口**：B2.4 总览屏两态 + 派遣候选 provider + settle-on-open + 主导航入口(gate) + 2 visual_route。

## 当前状态

百草岭远征 `expedition` feature **代码+build 完成**（后端 + provider + 结算页 + 总览屏交互层）。1.0 长线打磨期，Phase B keystone 交付；剩视觉目检 + Lv100 里程碑（用户拍板）+ batch3 数值探针。

## 进行中的工作

- **Codex 目检未做**：`expedition_overview`/`expedition_active` 两屏 @1280×720/1440×900（`tools/visual_capture/visual_capture.sh --route <id>`），真机复验 async-config-race + 水墨观感。本会话按「Codex 目检派单」设计未自跑（bg 不擅自抢屏启 GUI）。

## 已知问题

- **Lv100→解锁触发未接线**（有意）：`SaveData.jianghuJourneyUnlocked` gate 已正确消费，但触发器未写——与「发布上限 10→17」耦合为同一 Lv100 里程碑（design §3.1/§9），后者是 A2 Task 1 被 block 项（2 孤儿招破 loadAllDefs·待用户拍 cap）。故入口生产存档暂不出现，只能经 visual_route 目检屏本身。
- **codex worktree `battle-ui-stage`**（`22b78c98`·tip 较交接时 `c2be9b1e` 已变=活跃写）勿碰。孤儿 `equip-baicao-orchestration` 本会话已 `git worktree remove`（clean + 分支已 merge main·非破坏）。

## 重要决策

- **未先建 action controller**：随屏共设，避 speculative abstraction（`feedback_avoid_over_engineer_abstraction`）。
- **候选 provider 走单条 Isar 查询**（非组合 lineup provider）：`isFounder==false && isAlive` 即 active 门人∪替补，解耦 + 无 GameRepository 依赖 + 可轻量测（`lineupReserve` 的 absoluteLevel 排序拉 GameRepository 依赖会崩轻量 provider 测）。
- **config 经 `expeditionConfigProvider` watch**：非构造期定死单例，结构性避 async-config-race（总览是 ConsumerWidget 每 build 重 watch config）。

## 下一步建议

1. **（推荐）Codex 目检两屏**——keystone 收尾唯一剩项，本环境已装全 Xcode 26.4.1 可直跑 visual_capture；async-config-race 真机复验一次（memory 硬要求）。
2. Lv100 解锁触发 + 发布上限 10→17 里程碑——需先拍 2 孤儿招 phasing（A2 遗留待决）。
3. batch3 探针：敌队/伤势深度曲线数值定案 + 「重要战斗」result 富化（涉 schema/saveVersion）。

## 踩坑提醒

- **riverpod `ref.watch` 必须 await 前同步调用**：候选 provider 首版在 await 后 watch 另一 provider → 「Ref used after disposed」（`feedback_riverpod_closure_ref_disposed`）。
- **`Override` 类型不由 flutter_riverpod 3.x 公开导出**：widget 测别写 `List<Override> overrides` 参数，改内联 ProviderScope（overrides 类型由 ProviderScope 推断）。
- **本环境已装全 Xcode 26.4.1**：交接旧口径「仅 CommandLineTools·xcodebuild 缺」已过时，macOS build / 真机目检本会话可跑。
