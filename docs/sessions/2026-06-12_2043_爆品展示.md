# Session 交接 - 爆品展示内容化 + tagline + 停留

**时间：** 2026-06-12
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 4fbce11a

## 本次完成

详 PROGRESS.md 顶段两条（爆品展示内容化 `6966005f..4fbce11a` + E 音效落位 `c7e711d3..cd85728a`）。
- 会话从 E 音效素材回收起步 → 长成爆品展示三痛点大波。
- 音效：Codex 去 Suno 生成 28 变体，我裁切归一落位 uiPaperOpen+reward 合 main。
- 爆品展示：印章落款重设计 + 属性 + 35 tagline + 停留，A+B 批 xhigh。

## 当前状态

1.0 长线打磨期。爆品展示 A+B 批收口（视觉用户认可）。C 时序重排留下波。

## 进行中的工作

- 无（A+B 完整）。C 时序重排（爆品取代「勝」结算 overlay）下波单独 spec。

## 已知问题

- E backlog：battleHit/Crit v3 那 12 变体为源档归档（现状满意未动）；realmAdvance 2-4s 等挂 backlog（依赖素材/真玩）。
- 桌面 `挂机武侠_suno_sfx_20260612/_review_processed/`：uiPaperOpen/reward 备选 + 源 28 变体留置（用户可能换候选，可随时删）。
- prototype HTML demo（job tmp，throwaway）server 已停，job 删除时自动清理。

## 重要决策

- tagline 走加载层红线（tier≥重器必填）而非独立测：符合「schema 校验拦下」原则，连 test fixture 都拦（baike `_buildEquipmentYaml` fixture 补占位 tagline 过校验）。
- 停留交互：动画播完不自动消失、轻触继续，治真玩「1秒闪没」——比延长时长更可控。

## 下一步建议

1. （推荐）C 时序重排：爆品取代「勝」结算 overlay 成战斗结束第一高潮。spec 已注明，跨 battle_screen/mainline/tower，xhigh。
2. E 音频 backlog：realmAdvance 2-4s 等素材/真玩回收。

## 踩坑提醒

- 中文路径 cd codepoint 失败 + python heredoc 中文 Non-UTF-8：详 memory [[feedback_chinese_path_shell_pitfalls]]（开局派生 proj 缓存 / 中文用 .py 文件不用 heredoc）。
- fresh worktree 必 `dart run build_runner build`（.g.dart gitignored）；EquipmentDef.tagline 非 Isar 不需重生。
- 视觉验收临时塞爆品改 stages.yaml 测完 `git checkout --` 还原，定向 add 不扫 macos/ build 产物 + test/tools/output throwaway。
