# Session 交接 - 门派事件 + 声望衰减接通 game loop(B1)

**时间：** 2026-06-24
**项目：** 挂机武侠
**分支：** worktree-b1-sect-event-loop → 合 main
**模式：** 用户离线 1h 自主推进(brainstorm→spec→TDD 实装→验测→合并)

## 本次完成

全系统审计 B 组「系统建好未接 game loop」第二项 B1。门派事件死链三段(生产 0 caller)接通：
- `MonthlyTickCoordinator.tick()` / `SectEventService.checkAndTrigger()` / `SectReputationDecayService.computeDecay()` 全部接进真实日历月锚 game loop。

**两决策(用户拍板)**：① 真实日历月锚(复用 offline-recap app-open 体例·守 §5.5)② 仅 tournament(mission/crisis 叙事留底 1.1+)。

**实装**：
- `Sect.lastTickAt`(新 nullable 字段·月度 tick 锚·防同日重触发·Isar nullable 向后兼容不需 saveVer bump)
- `SectMonthlyTickService`(新纯函数·组合 checkAndTrigger + computeDecay + resolve(expired)·过期扫描每次跑/月度 pass ≥1 月跑/decay 按 idle 月数累扣)
- `sectMonthlyTickServiceProvider` + `monthlyTickCoordinatorProvider`(注册 1 callback·keepAlive Provider ref 安全捕获)+ `_runSectMonthlyTick`(writeTxn 落库)+ `maybeRunSectMonthlyTick`(gate)
- HomeFeed 首帧与 `maybeShowOfflineRecap` 并列触发
- numbers.yaml `narrative_ids` 池(tournament_01..05)+ `SectTournamentDef.narrativeIds` 解析
- sect_screen dev-gated(`kDebugMode`)立即触发按钮 `debugSpawnSectEvent`

零 saveVer / 零战斗数值变更 / 0 碰 §5.4 红线。

## 当前状态

1.0 长线打磨期。审计 B 组：A1+B2+B1 已闭环，剩 B3。C/D/E 整批待拍板。
analyze 0 / 全量 2900+1skip (baseline 2884 +16 新测·0 回归·worktree 实测)。

## 重要决策

- 真实日历月锚：游戏唯一时钟=真实 DateTime(§5.5),月度 tick 在 app-open 锚算 `floor(elapsedDays/30)` 月,多月 catch-up clamp ≤ active_events_max=3。
- 过期扫描复用 `resolve(expired)`：DRY·但 resolve 会重置 `lastEventAt=now` → 过期与新触发同 tick 互斥(过期=一次事件活动·语义自洽)。
- 应战仍 Random 50/50(Demo 既有挂账·真战斗 wire 不在 B1 scope)。

## 下一步建议

1. **B3 江湖恩怨注释止血 pending-1.1**(sonnet·~20min)：与 D3 npcId 同源,本期硬接逆设计。
2. C1-3 设计冲突拍板(商店标价/奇遇校验/招式倍率 drift)。
3. D 死字段 8 项 / E 散写中文 卫生批。

## 踩坑提醒

- `rngProvider` 给的是项目自定义 `Rng`(nextInt/nextDouble/pick),非 `dart:math Random`;`checkAndTrigger`/service 用 `Random` → 生产 wiring 用 `Random()`(同 SectEventDialog),确定性测在 service 层。
- `resolve(expired)` 重置 `lastEventAt` 引发过期↔触发同 tick cooldown 互斥——写 e2e 测时别把过期+新触发塞同场景断言(改纯触发场景)。
- fresh worktree env prep:pub get + cp 主仓 libisar.dylib + build_runner,否则 Isar 测编译/dlopen 失败。
