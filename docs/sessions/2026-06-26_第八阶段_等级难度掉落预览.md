# Session 交接 - 第八阶段:角色等级 + 推荐境界 + 掉落悬停预览 + 战斗反馈

**时间：** 2026-06-26（用户睡觉期间自主推进）
**项目：** 挂机武侠 · 分支 worktree-level-difficulty-lootpreview（待合 main）
**spec：** `docs/spec/2026-06-26-level-difficulty-lootpreview-design.md`

## 本次完成（用户拍板 A+B+C+D 必做 · 全 TDD）

- **A 角色等级 Lv**（新成长轴·全局连续·小幅有界）：`Character +level/levelExp`（saveVer 0.30→0.31·全仓断言同步）；`LevelService` 升级纯逻辑 + `LevelConfig`（numbers.yaml `level` 段·容缺省）；`derived_stats` maxHp/内力/速度注入 `(level-1)×per_level` 平直加成（clamp 硬守 §5.4）；**5 EXP 源并行喂**（主线/塔/闭关/离线/经验丹）；角色面板 Lv chip + 经验条。
- **B 推荐境界难度**：`StageDifficultyAssessor` 纯函数（境界差档→碾压/适中/偏高/送死·对齐 §5.5）。数据复用 `StageDef/TowerFloorDef.requiredRealm`（**本就现成**，无新字段）。
- **C 掉落悬停预览**：`StagePreviewContent`（推荐境界+难度徽章+复用既有「掉落传闻」`LootRumorContent`）+ `StagePreviewHoverCard`（OverlayPortal 悬停·**出流不占列表高度**守 viewport 回归）。铺**主线选关屏 + 爬塔层**（保留 info 图标点击 dialog 作 fallback）。
- **D 战斗反馈**：victory dialog 显角色「晋 Lv N」（`LevelUpSummary`「修为精进」banner·与境界突破并列）。

## 实玩反馈续轮（用户副屏目检后当场提）

- **Lv 哨兵 bug 修**（真机抓到，见踩坑）：旧档 Character level 读回 int64 哨兵 → 启动期 `repairCharacterLevels` 幂等回填 + 读层钳制。
- **掉落预览浮层 polish**：字体放大加深（推荐境界 15/墨·桶标签/物品 14）+ 底部翻向上弹 + 限高滚动（防屏幕底裁切）。
- **E 稀有彩头**（用户拍板：低等级副本也爆高阶·低概率不为零）：全局机制 `selectRareBonusTier`（跨 1-2 阶·各档独立 roll 取最高·封顶神物·阶梯递减）+ `rare_bonus_drop` 配置（+1阶 5%/+2阶 1.5%·**初值待真机校**）+ `DropService.rollRareBonus` 并入主线/副本 victory（自动持久+金光仪式）+ 塔首通（守 §5.1）+ 预览金色提示。守 §5.3 高阶可拿不可装。测 +8。全量 **3100+1skip**。

## 红线守

Lv 不破 §5.4（hp/内力 clamp 兜底·extremum 测扩满 build+L100）/ 不破 §5.3 三系锁死（Lv 不解锁高阶装备心法）；掉落不显概率%（§2.1·复用既有桶）；表现层不写 BattleState。

## 验证（本会话主 checkout 实测）

- analyze **0**；全量 **3090 +1skip · 0 回归**（基线 3051+1skip·**+39** 测：level_service 7 / level_derived_stats 5 / maxhp_extremum +14 / stage_difficulty 6 / stage_preview 4 / panel +1 / level_up_summary 2）。
- **未实玩**：悬停浮层手感 / 难度徽章配色 / Lv chip 经验条 / 「晋」banner 待用户 `flutter run -d macos` 副屏目检。

## 我自主拍的 balance 初值（待真机校）

numbers.yaml `level`：max_level 100 / exp_to_next_base 120 / per_level 40 / hp_per_level 15 / if_per_level 8 / speed_per_level 1（保守·同伤势系统先例）。Lv 曲线快慢、加成幅度需真玩校。

## 流程取舍（坦白）

用户睡前批准设计后即睡，**brainstorming 的 spec-review gate 无法执行**（用户不可达）；按授权「开干」直接 spec→TDD 实装。晨起若方向不对即调。

## 下一步建议

1. 用户实玩反馈调 balance（Lv 曲线/加成/难度配色/浮层手感）。
2. D 余 2 候选（题字命中分级 / 掉落金光分桶）未做，可补。
3. 形与势 16 项战斗轴（旧框架·清单已不可考，若回需重新 brainstorm）。

## 踩坑提醒

- **「形与势 16 项」清单仓库/git/memory 全查无**：原是 2026-06-25 实时对话反推，只有简写 `#1/2/4/11/12/16` 进了 spec，内容从未存盘 → 不可考，已废弃改做新点子。
- 悬停浮层用 OverlayPortal **出流不占高度** → 不挤出 ListView 靠后 item（这是选 hover 而非占位卡片的关键·守 feedback_listview_widget_test_viewport）。
- saveVer bump 同步：test/ 三段式 `0.30.0` 仅作 saveVersion 断言（`0.30` 两段式 multiplier 不动），perl 替换。
- **⚠️ Isar 不应用 Dart 字段默认值（真机目检抓到的真 bug）**：新增非空 int 字段（`level`）旧档读回是 **int64 哨兵 -9.2e18**，非 Dart 初始值 `1`。「纯 bump 无迁移」对新增**非空 int** 字段是错的。修：启动期幂等 `IsarSetup.repairCharacterLevels`（版本门迁移升版后不再跑 → 必须用启动安全网而非仅迁移块）+ derived_stats/chip 读层钳制双保险。全量测试用工厂建角色 level=1 故**没抓到** → 加非空字段必须真机/旧档目检。
