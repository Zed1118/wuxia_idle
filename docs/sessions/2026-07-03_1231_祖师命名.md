# Session 交接 - 祖师/门派命名(+死文档归档+README)

**时间：** 2026-07-03 12:35
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 3a69b60d(= origin/main·ls-remote 核实同步)

## 本次完成

详 PROGRESS.md 顶段 3 条(commit 区间 `a91bdb29..3a69b60d`，全 push)：
- **死文档归档**(`5097668b`)：Phase 0 逐个证伪后归档 12 退役 md，保留 content_guide/data_schema/AGENTS 三活基础设施。
- **README 重写**(`411dcf51`)：默认 Flutter 模板→183 行完整文档。
- **祖师/门派命名**(merge `3a69b60d`·9 commit)：brainstorm→spec→plan→subagent-driven TDD→审查→视觉验收→合并。

## 当前状态

1.0 长线打磨期。祖师创建流程本波补齐最后一块(命名)后**整体收官**(6/7 早已实装)。工程外围(测试提速/clamp迁移/死文档/README)基本清完，剩余候选多为需拍板的设计级/内容级项。

## 进行中的工作

- 无在途代码。worktree/分支已清。

## 已知问题

- `drop_table_reference_redline_test` 是已知隔离型 flaky(单跑绿/某些全量组合下报错)，v1.29 已记录；本波全量 3598 绿时它通过。非回归。

## 重要决策

- 祖师命名不改 Isar schema/不 bump saveVer：名字进已有字段 `Character.name`/`SaveData.sectName`(原只默认值填充)。
- sectName 回退用三级兜底 `creation?.sectName ?? save.sectName ?? default`：新档自定义→既有保留(护 R5.7)→默认。

## 下一步建议

1.（推荐）战斗节奏真机校值(E1)——低成本立竿见影，action_interval 等初值已 merge，真机看手感调定。
2. CI 搭建(批次4)——需拍板要不要；给长线项目上质量兜底。
3. 终局 Boss 偏软(C1)——设计级，需单开设计会换杠杆。
4. 死文案/壳组件处置——需拍板归档 vs 待接入。

## 踩坑提醒

- **flutter test 红了别信 tail**：compact reporter 的 `-1` 一路挂后续通过行，tail 的「失败文件」常是假象。grep 首个 `-1:` 才是真失败。本波误判 drop_table 实为 onboarding R5.7 回归。详 memory `feedback_flutter_test_minus1_carryforward`。
- **逐 task 测试会漏邻居文件回归**：我改 A 文件、A 的测试绿，但破坏的是 B 文件(onboarding_service_test R5.7)，批末全量才逮到。跨切面改动(碰存档写路径)必跑全量。
