# Session 交接 - 第五阶段主线二 2.3 即放时序 + 2.5 首通门控

**时间：** 2026-06-18
**项目：** 挂机武侠
**分支：** main（已并 + 已 push）
**最后 commit：** 51240bdb

## 本次完成

详 PROGRESS.md 顶段续23（commit 区间 `9c57c97e..51240bdb`，13 commit 全 push）。
- brainstorm→spec→plan→subagent-driven TDD（5 task，每 task implementer + spec + 质量两阶段 review + 最终 opus 整体 review）。
- 2.3 引擎级真插队·预支（借 AP=1000 复用 `_resolveAction` 出手归零）；2.5 首通强制 interactive。
- 最终 opus review READY TO MERGE，红线 §5.4/§5.5 双守住；视觉验收 battle_drag_live 真机自截 UI 完整 PASS。

## 当前状态

第五阶段主体闭环：主线一✅ + aoe✅ + 主线二✅(2.3/2.5) + 主线三✅。1.0 长线打磨期。

## 进行中的工作

- 无未完成模块（主线二全闭环并 main）。

## 已知问题

- 2.4 打击感表现层（hit-stop/闪白/题字/镜头震）未做，纯表现层 high 档，可选不阻塞。
- 视觉验收：拖招即时出手纯手感单帧不可截（无 cliclick 不合成手势），由 intervene+确定性测+opus review 端到端锁死；本会话截图在 job tmp 临时态不入库。

## 重要决策

- 2.3 选引擎级真插队（非表现层即时）+ 预支 AP 归零：净出手频率近不变，非数值杠杆，守 §5.4。
- 2.5「自动推进」是 spec loose wording（实测无跨关自动链）→ 落地为首通强制 interactive；BattleReplayRecord 已删（saveVer0.23）→ 2.3 前置失效、风险由 xhigh-落盘降为引擎时序。

## 下一步建议

1. 第五阶段收尾盘点 / 下一主线拍板（推荐）— opus high，纯讨论，主体已闭环。
2. 2.4 打击感表现层 — opus high，走 actionLog 边沿不写 BattleState，可选。
3. 主线二真机手感复验（拖招即时出手 + 首通门控）— Codex 交互派单，非阻塞。

## 踩坑提醒

- bg 会话移除自身 worktree 后 cwd 悬空：每条 Bash 需 cd 前缀，否则 git 吐 "Needed a single revision" 假错（详 memory `feedback_bg_session_remove_own_worktree`）。
- fresh worktree 跑测前必拷 libisar.dylib + 跑 build_runner（.g.dart gitignored），本会话已处理。
- `interveneNow` 须在 tick 边界（actorQueue 空）调用，勿从 step-paused 路径调（否则二次出手致负 AP）—— 已在代码注释锁不变量。
