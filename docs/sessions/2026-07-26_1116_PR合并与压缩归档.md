# Session 交接 - 两 PR 合并 + PROGRESS 压缩归档

**时间：** 2026-07-26 11:16
**项目：** 挂机武侠
**分支：** `worktree-session-handoff-0726`（本记录待 PR；代码分支已全清）
**最后 commit：** `78da778d`（main·与 origin 同步）

## 本次完成

详 PROGRESS.md 顶段 2026-07-26 条（区间 `8c9b5ef4..78da778d`）。PROGRESS 没展开的：

- **PR #81 的「CI 绿」是上一个 commit 的**：上轮交接写「PR #81 CI success」，实为 `5e027673` 那次；tip `0734d6e2`（纯 docs commit）的 run 当时仍在跑。交接引用 CI 状态须钉到 commit，不能只写「PR 绿」。
- **契约测没跟着 targeted 跑**：八域 targeted 735/0 的日志里 grep 不到 `rng_provider_wiring_contract_test`（只出现 `test/shared/audio|widgets`），故单独复跑取证 3/0。**不因为「目录传了」就默认文件跑过**。
- **压缩用带断言的脚本而非手改**：8 条 startswith + 7 条空行断言锁行号，漂移即抛错——超长单行 Markdown 手工 Edit 极易错删相邻条目。

## 当前状态

1.0 长线打磨期。main `78da778d` 树净、与 origin 同步，**远端只余 main 一个分支**。BACKLOG §一#8（生产随机源收口）本轮销账，§一 余 5 条开启（#3/#4/#5/#6/#11），其中 **#4/#5/#6 全卡在「待真人试玩数据」**。PROGRESS 98→**90 行**，留出约 5 批余量。

## 进行中的工作

- **无自有在途分支**（两 PR 已合、worktree 与远端分支均已清）。
- **他方在途**：`codex/global-visual-polish`（worktree `.worktrees/global-visual-polish`·tip `fb5a8851`·**tip 无 `[READY]`** → 按 CLAUDE §8.3 判 WIP，全程未碰）。已连续多轮无 `[READY]`，需用户催 codex 打标记才能进 §8.2 评审。

## 已知问题

- **`build/visual_acceptance/` 累积 429M**（gitignored·零仓库/包体影响，纯本地磁盘）：其中 `zh_inscription_ruling_2026-07-25/`（28M·6 图）**本轮继续留置**（§一#11 未拍板，下次零成本重启需要）；另 7 个目录 401M 是往批遗留（`battle_ui_v2_85_sample` 111M / `ch78_mass_battle_rotation_fixed` 128M / `kimi_battle_smoke_20260718` 89M 等），**未擅自删**——400M 视觉证据的存废该由用户定，见「下一步」#4。
- 视觉目检债续挂：飘字分段排版 / 23 处 UiStrings 等值搬迁 / webp q80 观感 / Ch16 立绘真机战斗屏，均未真机逐屏目检。
- `MilestoneEquipmentGrantService` 两构造点拿不到 `ref`，生产走构造默认值（可注入但未强制走 provider）。
- `dart:math Random()` 签名约 15 处未动（含 `stage_entry_flow.dart:233`），换 provider 需另立注入点。

## 重要决策

- **合并前必等 CI 且下钻 job 级**：本轮 PR #82 等了 22 分钟 test job，绿后才合。run 级 conclusion 可能假红（memory 有实例），判绿判红都取 job 级。
- **ExitWorktree 报「Discarded 1 commit」按误报处理**：前提是三验全过（is-ancestor EXIT=0 / `main..分支` 0 commit / `branch --merged` 列出）。删完又复查一次 commit 仍在 main，零丢失。

## 下一步建议

1. **Ch17「沙海纵深」实装批（推荐）** — spec §8 前瞻已定向，是当前唯一能推进主线的实质任务；须开 xhigh 专会话。
2. 真人试玩半小时 — 只有用户能做，一次解锁 §一#4/#5/#6 三项数值拍板。
3. 视觉目检债清账 — 四项积压，需独占屏幕，且与 codex 抢 app。
4. `build/visual_acceptance/` 401M 往批证据存废拍板 — 一句话决定，删完省 400M 本地盘。
5. 催 codex 给 `codex/global-visual-polish` 打 `[READY]` — 否则该分支永远进不了评审。

## 踩坑提醒

- **内联 `until [ ... ]; do ... done` 在本会话 shell 直接 `parse error near 'until'`**（与 memory `feedback_chinese_path_shell_pitfalls` 记的 `for`/`while [ ]` 同类）。解法：写 `.sh` 脚本文件用 `bash script.sh` 跑，脚本内部的 while 正常。**后台任务因此被报 "failed with exit code 1"，实为 shell 解析失败非 CI 红**——再次印证「不信任务级状态码，读输出取真值」。
- **worktree 与本地分支会被 harness 在会话结束时自动回收**：上轮交接把 `rng-provider-closeout` 列为「留置项·合并后才可清」，本轮开局它已消失（`git worktree list` 两分钟内前后不一致）。**远端分支与 PR 不受影响**，但交接里写「worktree 留置」是不可靠承诺，要留东西必须 push 到远端。
- **targeted 传目录 ≠ 目录下文件都跑到**：见「本次完成」第 2 条，验收单个测试文件请单跑取证。
