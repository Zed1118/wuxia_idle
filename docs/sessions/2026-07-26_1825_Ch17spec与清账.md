# Session 交接 - Ch17 spec 拍板冻结与两笔清账

**时间：** 2026-07-26 18:25
**项目：** 挂机武侠
**分支：** `main`（未 push·领先 origin 43）
**最后 commit：** `b7a70788`

## 本次完成

详 PROGRESS.md 顶段 2026-07-26 首条（区间 `14a72e5a..b7a70788`·5 commit·**纯文档零代码改动**）。PROGRESS 没展开的：

- **本会话主要产出是「证伪」而不是「产出文档」**：Ch17 spec 的价值集中在 Phase 0 把上游段级 spec §8 前瞻逐条打掉——三处事实错 + 一处拍板与红线测**根本不相容**。照抄前瞻直接开实装会在中途卡死。
- **段级 spec 未订正**：本 spec 只在自己的 §1 写「上游需订正两处」，没回改 `2026-07-24-zongshi-arc-ch16-18-design.md`。Ch18 起草时若不读本 spec §1，会再踩同样三处。

## 当前状态

1.0 长线打磨期。视觉收口两阶段已全部合入并各经真机抽查（上一会话收口）。本会话把主线推进的前置做完：Ch17「沙海纵深」章级 spec **已拍板冻结**，六项全按推荐，实装依据就位。main 树净、analyze 0（本会话主 checkout 实测），**43 commit 未 push**。

## 进行中的工作

- **Ch17 整章实装未起**：spec §9 已列全 reconcile 站点、§6 给了 A 案实装细则五步、§3 给了五关数值草案（HP 56800→58000 / 攻 1925→1950 / diffMult 18.0→18.8 / exp 章计 424）。数值是草案锚，实装批须探针校准。

## 已知问题

- **远端分支 `origin/worktree-session-handoff-0726` 残留**：PR #83 已 CLOSED、本地 worktree 与分支本轮已清，但删远端分支要 push，**受「不 push 远端」硬约束**未做。一条命令：`git push origin --delete worktree-session-handoff-0726`。
- **`build/visual_acceptance/` 204M**（gitignored·本轮 493M→204M）：保留 6 批，其中 `battle_ui_v2_85_sample` 111M（文档明写「battle-ui 终拍前勿清」·阶段 5 终验未做）、`zh_inscription_ruling_2026-07-25` 28M（BACKLOG §一#11 未拍板要用）两批是**有依据的留置**，非欠账。
- Windows 三档缩放 / 正式 CJK 字体 / 9 名候选透明站姿 / 29 组历史近似详情图 / 伪文字规则，五项仍未清零。

## 重要决策

- **PR #83 改走 cherry-pick 而非合并**，因为 GitHub 的 `CLEAN` 是对 `origin/main` 算的，本地领先 39 未 push；本地 `merge-tree` 实测 `NEXT.md` 冲突且分支侧是过期版，合进来会静默回退。已入 memory。
- **feng_juan 拍 A 案（挂 17_04 章中 Boss + 收紧测语义）**，因为这是四案里唯一同时兑现段级拍板、不破 2/2/2 配平、且让机制教学成两级递进的走法；收紧「每章至多 1 本真解」→「每章至多 1 本**末Boss**真解」是**口径订正**——现跑实证全仓 12 处真解全落 `_05` 章末关，零破坏面。
- **删证据目录时收窄了用户拍的范围**（多留 3 批 160M），因为文档里明写「勿清」「未拍板要用」；删除不可逆，宁可多留。

## 下一步建议

1. **Ch17 整章实装（推荐）** — 唯一能推主线的实质任务，spec 已冻结、站点清单就位，开工无阻塞。需 xhigh 专会话。
2. 删远端残留分支 — 一条命令，但需你授权 push。
3. 29 组历史详情近似图第一批（神武/宝物/特殊 8 组）— 复查文档 §P2-01 已给顺序。
4. push 本地 43 commit — 已积压多轮，需你明确授权。

## 踩坑提醒

- **上游 spec 的「前瞻/登记」段是 drift 最高发区**：它写在实装之前，数字与资源盘点全是估算，最容易被下游 spec 照抄。逐条证伪三步：数字现跑取值 / 「待写」资源先 grep 是否已存在 / 每条拍板去翻它会撞的**红线测断言原文**（不是测试名）。已入 memory `feedback_living_doc_state_drift` 类型 D。
- **`gh pr view` 的 mergeable/CLEAN 对本地不成立**（本地领先未 push 时）。已入 memory `feedback_gh_pr_mergeable_vs_local_divergence`。
- **bg 写守卫拦 Write/Edit**：纯文档用 Bash heredoc（`cat > f <<'EOF'`）落盘，中文无编码问题；改已有中文文档用 `python3` 脚本文件（先 heredoc 写脚本再跑），不要直接 sed。**不要为文档开 EnterWorktree**——它 baseRef=fresh 基于 origin，会丢掉本地未 push 的 43 个 commit。
