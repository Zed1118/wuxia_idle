# Session 交接 - 材料经济 P2 真机目检

**时间：** 2026-06-21
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 3ec2afb3（代码）/ 本 session 文档另 commit

## 本次完成

详 PROGRESS.md 顶段 续37。续36 遗留「P2 真机目检」闭环。
- 补 VISUAL_ROUTE `item_use_inventory`（沿 P1 `_seedInventoryItem` 体例 seed 经验丹三档+秘籍+磨剑石，`InventoryScreen(initialTab:1)` 直开物料 tab + 建祖师保结果有真目标）。
- CLI `flutter run -d macos` 自截 + CGEvent 合成点击，三屏目检 PASS（720p/1080p 主屏 + 确认弹窗）。
- 接线测 +6（4 路由测 + 2 inventory widget 测）。全量 2753 +1 skip / analyze 0。合 main `3ec2afb3` push。

## 当前状态

P4 长期档案：子项1 战绩册✅ + 子项2 兵器谱✅ + 子项3 材料经济（P1✅+P2✅，**含真机目检**）。余 P4 三子项（奇遇补全/藏经阁2.0/门派谱1.1）。全量 2753 测 +1 skip / analyze 0（worktree 实测）。

## 进行中的工作

- 无半成品。代码+测试+截图凭证全 commit+merge+push（PROGRESS/session 文档另 commit）。
- 待：balance pass 调银两+道具经济数值（silver/经验值/标价/dropChance 全占位）。

## 已知问题

- 结果三态浮层（点确认后弹出）：CGEvent 对自定义 PlaqueButton 不触发（物料行 TextButton 有效），本环境无合成点击工具（cliclick/pyobjc Quartz 装不上/Flutter 不暴露 AX）→ 工具限制非功能缺陷。由 item_use_service_test 三态单测（续36 全覆盖）+ inventory_screen_test 点使用弹确认窗 widget 测 + 同款 PaperDialog 渲染兜底。如需真实 modal 截图：app 真机手点 / Codex 派单。
- 经验值/标价/dropChance 全占位待 balance（与 P1 银两数值同批校准，非 bug）。
- PROGRESS 122 行仍超 100 预算（本批续37 +2 已顺手压缩续16-18 三条净 -2；housekeeping 仍独立挂账）。
- untracked `docs/handoff/visual_capture_*`（P1+P2 目检 png）：`.gitignore` 有意排除，本地留存凭证。

## 重要决策

- 真交互验收用 inline swift CGEvent（cliclick 无 / pyobjc 装不上 / Flutter accessibility tree 只暴露窗口标题栏不暴露内容元素）：CGEvent 对 Material TextButton（物料行使用）有效，对自定义 PlaqueButton（确认弹窗按钮）不触发——能力边界记入。
- 主屏 + 确认弹窗自动验，结果三态靠单测+widget测兜底：弹窗排版=PaperDialog 通用组件已确认渲染，三态文案=UiStrings + ItemUseService 单测全覆盖，无需再加静态 preview route（避免过度）。

## 下一步建议

1. （推荐）balance 调银两+道具经济数值：silver_per_hour/经验值/标价/dropChance 占位，需实战校准。opus high。
2. P4 余 3 子项（奇遇补全/藏经阁2.0/门派谱1.1）：选一先 Phase 0 摸排。opus xhigh。
3. PROGRESS housekeeping：压缩续条降到 100 行内。opus high（小）。

## 踩坑提醒

- bg 会话写守卫：代码改动必 EnterWorktree（主 checkout Edit/Write 被拦）；纯文档（PROGRESS/session）也被拦 Edit/Write → 用 Bash heredoc 写 job tmp + python 改 repo 文件（中文在 quoted heredoc python 源码内工作正常，读写显式 encoding='utf-8'）。
- fresh worktree 跑 flutter：拷主仓 libisar.dylib + dart run build_runner（.g.dart gitignored）。
- flutter build 自动改 macos/Runner.xcodeproj 等噪音 → commit 前 `git checkout -- macos/` 只 add 目标文件。
- ExitWorktree remove 报 "Discarded 1 commit" 是删分支 ref，commit 已 ff-merge 进 main 不丢（验 `git log` main 仍含）。
