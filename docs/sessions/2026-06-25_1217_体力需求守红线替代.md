# Session 交接 - 体力系统需求 → 守红线写实替代(战斗张力循环)

**时间：** 2026-06-25
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 8931127e

## 本次完成

详 PROGRESS.md 顶段两条（体力→守红线替代设计 + ③ 双层伤势实装 + ①②③ 收口）。本 session 21 commits（`790e575a..8931127e`，全 push）。
- 跨多主题：审计 C/D/E 收口 + 健康度复盘报告 + 11 招接线 + PVP/stale 订正 + **Windows 真机验收(SSH 自驱)** + 体力需求 brainstorm→spec→plan→subagent-driven 实装。
- 体力需求**守红线改写**：不做体力，三套替代（① 内力弹药已满足/② 境界门槛中后期成立/③ 双层伤势新建）。

## 当前状态

1.0 长线打磨期。全系统审计 A-E 全闭环 + 健康报告 #1-#5 全收口/降级 + 「体力→战斗张力循环」①②③ 全落地。Windows 发布目标平台已实测验证（test 逐位同 Mac + release exe + 启动不崩）。**无腐烂技术债**（analyze 0 / 全量 2931+1skip）。

## 进行中的工作

- 无半截工作。所有 worktree 已 ExitWorktree remove，无孤儿分支。

## 已知问题（全是登记决策·非债·勿误判为遗漏）

- **伤势 balance 数值待真机校**：保守初值（疗养 8h/减内力 15%/攻击 ×0.85/惨胜阈值 25%）。
- 塔 Boss 战败不触发重伤（拍板不接，避越败越弱螺旋）+ Ch1 末宽松（接受）。详 spec §9。
- Windows `F:\wuxia_verify`（含 release exe）留置供用户实机看，不需可删。

## 重要决策

- 体力直撞 §5.1 反主流红线第一项 → 不做体力，守红线写实替代（内力弹药/境界门槛/双层伤势），因体力是手游留存机制与项目灵魂对立。
- 双层 per-角色伤势：带伤永远能打（debuff 非闸）+ 疗养真实时间（无加速）+ 不留存焦虑——红线铁律焊死。

## 下一步建议

1. **伤势 balance 真机校（推荐）**：Windows 在线时 SSH 自驱跑 balance_simulator 扫参 / 或用户实机感受。
2. 其他打磨方向 / 新需求，待用户指定。

## 踩坑提醒

- **saveVer bump 必全仓同步硬编码断言**：本次 0.28→0.29，scoped 测（isar_setup_test）只覆盖 1 文件，全量才暴露 6 处漏（sect/save_migration_021/lineage/passive_idle）。改 saveVer 后必 `grep -rn '0\.28\.0' test/`（禁 head 截断）。memory `feedback_version_bump_test_assert_sync`。
- **subagent-driven 必先预热 worktree**（pub get + cp libisar.dylib + build_runner + 冒烟），否则裸 worktree 0 .g.dart 子代理 test 编译即败。memory `feedback_subagent_driven_fresh_worktree_env_prep`。
- **Mac→Windows SSH**：scp 须加 `-O`（默认 SFTP 在该 Windows sshd 不通）；GitHub clone 大传输 SSL eof，改 LAN 直传。key 在 `~/.ssh/wuxia_win_acceptance`，Windows IP 192.168.1.244 administrator。
- schema 批 merge 回主 checkout 必 `dart run build_runner build`（.g.dart gitignored）。
