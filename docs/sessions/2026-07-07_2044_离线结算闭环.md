# Session 交接 - 离线结算闭环(体检批1)

**时间：** 2026-07-07 20:45
**项目：** 挂机武侠
**分支：** main
**最后 commit：** d6b5c875（本会话 8 commits：7 分支 + 1 merge，全 push，CI run 28866305456 绿）

## 本次完成

详 PROGRESS.md 顶段 2026-07-07「体检修复批1:离线结算闭环」条目（commit 区间 `e8f99d28..d6b5c875`）。
- 用户拍板方案 B（聚焦结算+前台心跳+统一基准写入 controller）后 brainstorm→spec→plan→inline TDD 5 任务一波完成。
- PROGRESS 顺带治理回 99 行（A1 三批压缩为一行归档、07-05 工具链诊断折进 C 批次条目尾注）。

## 当前状态

1.0 长线打磨期。main 全绿（主 checkout analyze 0 + seclusion targeted 168；全量 3733 pass/1 skip/0 fail 在 merge 前 worktree 同内容树实测，CI 于 d6b5c875 复证）。体检候选表批 1 已销，批 2/3/4 及两项拍板未动。

## 进行中的工作

- 无在途 worktree/分支。体检修复候选表剩 6 批未开工（推荐序 3→拍板→2→4）。

## 已知问题

- 真机失焦→聚焦冒烟未做（widget/单测已覆盖逻辑层，建议下次 playtest 顺带看离线归来数值）。
- 闭关互斥下超 cap 的闭关外时段被动不补 = by design 未动，要改属收益语义需拍板。
- P1-14 AOE 记账语义、P1-9 敌 ultimate 进 AI 仍待用户拍板（批 4 前置）。

## 重要决策

- 心跳间隔 60s 留代码常量不进 numbers.yaml：纯防御工程参数（只影响双吃上界 ≤60s），不影响任何收益数值；若未来要调平衡再迁。
- 聚焦结算不弹卡：可能发生在任意屏（含战斗中），弹卡打断爽感；与既有「已静默入包不弹卡」语义一致，卡仍只在启动 gate 弹。
- gate 范围 A（active 闭关）也 markStartupSettleDone 起心跳：修「收功后 stale 基准双吃」边角，闭关窗口本身仍归闭关结算（互斥不变）。

## 下一步建议

1. **（推荐）体检批 3：invalidate 速修**（P0-5 门控三件套 + P1-6/8/11，~2h）——同根问题一把收，新手可见收益大，sect_providers 有 watchLazy 先例。
2. 拍板 P1-14 AOE 记账语义 + P1-9 敌 ultimate 进 AI（~15min 讨论，后者改 Boss 难度需复测）。
3. 体检批 2：战斗 wiring（群战波次/轻功地形进 tick 路径，xhigh，改动面大单独一波）。
4. 体检批 4：战斗小修（P0-4 interveneNow 边界 + P1-12 iconPath 哨兵；P1-9 拍板后并入）。

## 踩坑提醒

- AppLifecycleListener 有状态机断言：测试里 `handleAppLifecycleStateChanged` 直接 resumed→hidden 会被 assert 拦，必须走 resumed→inactive→hidden 合法链（`online_presence_lifecycle_hook_test.dart`）。
- Isar collection accessor（`isar.inventoryItems` 等）是 domain 模型文件生成的 extension，测试文件必须 import 对应 `core/domain/*.dart` 否则报 undefined getter。
- 心跳类时序测试用「短间隔 override（40ms）+ 真实 delayed」而非 fake_async（Isar 真 I/O 在 FakeAsync 下不推进）；本会话连跑 3 次稳定。
- EnterWorktree 的 ExitWorktree(remove) 对「已 merge 的 7 commits」也会要求 discard_changes 确认——先 `git log main..分支 | wc -l` 证 0 再删，零丢失。
