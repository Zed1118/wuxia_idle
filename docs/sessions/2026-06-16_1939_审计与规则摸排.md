# Session 交接 - 审计修复与规则层摸排

**时间：** 2026-06-16
**项目：** 挂机武侠
**分支：** main
**最后 commit：** dcdacf81

## 本次完成

详 PROGRESS.md 顶段 续15（全功能真审计+修复）+ 续16（规则层全域摸排+修复），commit 区间 `f4a80c60..dcdacf81`。
- **元教训**：上会话(工具输出污染)幻觉了「45 项审计+已push」,本会话证伪→重跑真审计→并把防幻觉纪律**回灌**到 /handoff(0d 交付物实证)+ 全局 CLAUDE.md(防幻觉统一守则)+ 各 skill。
- 项目侧改动全 push main;**全局侧(~/.claude memory/skill/CLAUDE)未版本化,本地即时生效**。

## 当前状态

1.0 长线打磨期。代码红线 5 维基本全清(audit 0 Critical/1 High 已修)；规则基础设施(memory/CLAUDE/skill)已摸排修复。全量 2247 测 +1 skip / analyze 0。

## 进行中的工作

- 无活跃进行项；续15/续16 两批均已整批闭环合 main。

## 已知问题

- **M6 心魔失败惩罚未实装**(backlog 待拍板)：GDD §6 设计了「内力×0.85/主修×0.9+余毒」但代码从未 wire,已在 GDD §6 加纠偏注。想做单开一轮 xhigh。
- **~/.claude 全局改动未版本化**(留置)：memory/skill/全局 CLAUDE 是本地配置、非本 repo、用户未要求 push,故无远端备份。属有意留置,非债。
- 可选低优先：视觉验收 5 条 memory 可合并 / 全局 CLAUDE 的 CodeGraph 段(MCP 已自动注入,留着无害)。

## 重要决策

- **EnumL10n + battle_log 认定合法集中 sink**（§5.6 v1.20 正名）：集中维护、带类型安全,迁 UiStrings 反丢 switch 穷尽检查 + 双头维护,不划算。
- **M6 先核查不实装**：GDD 自称「全收尾」但代码零 wire,先证伪再决定,避免白做。
- **check-redlines 重写而非退役**：改运行时读 GDD §5.4 + 跑 16 红线测,变可信。

## 下一步建议

1. **M6 心魔失败惩罚实装**（推荐）：唯一实质挂账,涉战斗结算+存档语义,xhigh + 补测。
2. 视觉验收类 memory 合并 / CodeGraph 段清理：低优先,纯优化。
3. 用户指定的新方向。

## 踩坑提醒

- **渲染层/工具输出可能伪造 sha 与「已push」**：报交付物前必 `git show --stat`/`git ls-files` 现场实证（本会话 0d 规则即因此而生）。
- **fresh worktree**：缺 `.g.dart` 先 `dart run build_runner build`；libisar.dylib 可能截断,从主仓拷（见 memory `feedback_wuxia_pen_build_runner` / `feedback_fresh_worktree_libisar_dylib`）。
- **长寿文档行号会 drift**：CLAUDE §12.2 已去钉行号改符号引用,新增引用别钉瞬时行号。
