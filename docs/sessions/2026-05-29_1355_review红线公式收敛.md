# Session 交接 - 外部 review P1-a 三系锁死 + P2-c 公式收敛

**时间：** 2026-05-29 13:55
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 1a4ee10

## 本次完成

- **P1-a 飞升 auto_swap 三系锁死修复**(`559455f` · TDD)：`performAscend` 副作用 4 auto_swap 直写 `disciple.equipped{Slot}Id` 无 canEquip → 武圣神物可自动装到低境界徒弟,破 §5.3。加 `Equipment.isEquippableAtRealm(RealmTier)` 域规则(`tier.index ≤ realm.index`)+ auto_swap 上身前守卫(不够阶只转 owner 入背包不上身)+ R5.11 红线测 2 测 + R5.6/R5.7/R5.10 收装徒弟 boost 够阶(原断言建立在违规行为上)。
- **P2-c 战斗公式双路径收敛**(`f719172` · 重构保形)：诊断发现 **production 只跑 `_calculateInBattle`,`DamageCalculator` 实为测试专用参考实现**,公式数学复制两份违 §6。抽 `DamageCalculator.calculateResolved(primitives)` 为唯一真相源,`calculate`(Character 路径)/ `_calculateInBattle`(BattleCharacter 路径)双双变薄 adapter;3 处口径差异(IF 满/当前 · defenseRate 境界base/缓存含相生 · attackPowerMult 1.0/烘焙)收为显式参数。删 ~100 行重复数学 + P2-c 聚焦测 2。
- **PROGRESS 更新** 2 次(P1-a + P2-c 条目 · 测试数 1535→1539 · 顶段对齐)。
- 全程 TDD/保形纪律:每步红绿验证 + 全量套件回归。

## 当前状态

1.0 release readiness ~96% 维持。外部 code-review 6 findings 修复批推进中:P1-b(上次 session)+ P1-a + P2-c 已收口。后期系统经 H3 audit 证实成熟。1539 测全过 / 0 analyze,主分支与 origin 同步。

## 进行中的工作

- **外部 review 修复批剩 3 项未启**:P2-a 奇遇招式红线兜底 / P2-b 敌人属性 numbers.yaml 缩放 / P3 文档 drift(§6 血量系数 0.7→0.5 + AGENTS.md 口径)。
- **根因A 挂机循环重平衡未启**:**前置 P1-b✅ + P2-c✅ 已全清,可动**。方向已对齐(idle 喂共鸣度+修炼度 / 闭关 EXP 提到接近战斗 / insightPoints 轻量兑换 sink),但改 numbers.yaml 数值规则层,开工前需先讨论数值方向(硬约束 ask)。

## 已知问题

- **P2-a 奇遇招式 YAML 吞异常**:`game_repository` encounter_skills 空集合是红线 noop(无兜底,真静默失效);encounters/synergies 有 ≥N 兜底。
- **P2-b 敌人属性硬编码**:`stage_battle_setup.dart:282` maxInternalForce:1000 / crit·evade 0.05 固定不随境界缩放。
- **P3 文档 drift**:numbers.yaml:68 内力系数实际 0.5,GDD:337/CLAUDE.md:180 仍写 ×0.7;AGENTS.md Demo 口径过时。
- **红线值散落 hardcode**:15000/20000 在 derived_stats/stage_battle_setup/inner_demon/game_repository 4 处 hardcode,P2 统一到 numbers.yaml 候选。

## 重要决策

- **决定 P2-c 收敛方案 = 抽核心留 DamageCalculator(非新建 formulas.dart)**:`calculateResolved` 纯 primitives 核心放 DamageCalculator(既有公式家),两路径变 adapter。因 §6 点名的 `lib/core/combat/formulas.dart` 从未创建,DamageCalculator 是 de-facto 公式家,留它改动面最小、风险最低。
- **决定 P2-c 保持 3 处口径差异为显式参数而非强行统一**:IF/defenseRate/attackPowerMult 是 Character vs BattleCharacter 的真实语义差异,收为 adapter 显式传参,公式数学单一,差异不再隐式 drift。
- **决定 P1-a 不够阶 disciple 只转 owner 不上身(留背包)**:§5.3 师承遗物不例外,owner 转移(入背包)始终发生,equip 槽位仅 canEquip 时写,等够阶再装。

## 下一步建议

1. **(推荐)根因A 挂机循环重平衡**(opus xhigh ~6-8h):前置已全清,是 1.0 核心体验债。**开工前先讨论数值方向 + 升 xhigh**,接 `tools/balance_simulator.dart` 真路径验证(现 P2-c 后 simulator 应改打 _calculateInBattle 真公式)。改 numbers.yaml 前 ask。
2. **P2-a/P2-b + P3 文档 drift**(opus high ~2h):奇遇招式红线兜底 + 敌人属性 numbers.yaml 缩放 + GDD/CLAUDE.md §6 血量系数 0.7→0.5 同步 + AGENTS.md 阶段口径。改 GDD/CLAUDE 前 ask。
3. **红线值统一到 numbers.yaml**(opus high ~1h):消除 4 处 hardcode 15000/20000 重复,单一真相源。
4. **balance_simulator 改打真公式**(opus high ~1h):P2-c 后 simulator 若仍走 DamageCalculator.calculate,验的是非战斗路径(虽现已同核心,但 atkPowerMult/相生口径不同)→ 接 _calculateInBattle 才反映真实战斗。可与根因A 合并。

## 踩坑提醒

- **bg session worktree 守围**:cwd 是 git 仓库,但 bg session 任何 Edit/Write(含 doc-only)走 EnterWorktree → 工作树内改 → `git push origin HEAD:main`(FF)→ 主 `git merge --ff-only <branch>` → ExitWorktree remove(commit 已并入 main 时 discard_changes:true 安全)。fresh worktree 必跑 `dart run build_runner build`(`.g.dart` gitignored)。
- **audit/review 断言必 grep 复核再信**:P2-c 诊断发现 `DamageCalculator` production 零 caller(grep 全 lib 只有类定义 + 一句注释)—— 若只看类存在就以为它在战斗中跑,会误判。production caller 面必 grep 实测。
- **重构改公式核心:rng 消费顺序必保持**(闪避 roll → 暴击 roll),否则 `Random(seed)` 复现测试(damage_calculator_test 用 Random(99)/Random(42))断言炸。`calculateResolved` 内闪避先 roll、暴击后 roll,与原两实现一致。
- **breakdown 字符串测试用 `contains` + 数 ` * ` 出现次数**(damage_calculator_test:314 期望 5 段):统一 breakdown 用条件式 `atkPowerMult != 1.0 ? ' * X' : ''`,`calculate` 传 1.0 时仍 5 段保形,in-battle 烘焙 !=1.0 时 6 段。
- **P1-a:加 canEquip 守卫会破 R5.6/R5.7/R5.10 既有 auto_swap 断言**(seed 徒弟 erLiu/sanLiu < 祖师 liQi 装备阶)——那些断言原本建立在违规行为上,需 boost 收装徒弟够阶(真实玩法:传位前练上去),非改产品逻辑。
