# Session 交接 - 断魂庄战斗驱动全链 wiring 收官

**时间：** 2026-07-17 17:28
**项目：** 挂机武侠
**分支：** feat/baicao-duanhun-phase-b（worktree·**未 push**）
**最后 commit：** f2348156

## 本次完成

详 PROGRESS.md 顶段「2026-07-17 断魂庄 C2 组末环·战斗驱动全链 wiring 完成」条（commit 区间 `81675ea7..f2348156`）。
- Task 2-8 全绿：reward/defeat 两屏 + entry_flow 编排器 + loadout 接线 + 主菜单入口 + 2 visual_route。断魂庄端到端可玩。
- 全仓 analyze 0 / 全量 test **4282 pass 0 fail** / macOS build ✓（本会话 worktree 实测·代码只在此 worktree）。

## 当前状态

断魂庄 C2 组前后端全闭环、端到端可玩（主菜单→装载→逐关现场战斗[BattleScreen]→整备→通关三选一/战败结算→离庄）。1.0 长线打磨期。phase-b 领先 main 分叉点 12 commit·未 push。

## 进行中的工作

- 无进行中切片。wiring 8 Task 全收口。

## 已知问题

- **reward/defeat 两新屏视觉目检延后**：水墨观感/色板真机 visual_capture（4 屏 @1280×720/1440×900）待白天/Codex。GUI 驱动 + handoff 已标「需白天真机验收」。验收包 `build_acceptance.sh` 已就绪（@dae00576），两 route 已入 `VisualRoute.values` 验收套件。屏均有 widget 测保无溢出+文案，仅美学目检延后。
- **Task5 装载 wiring 集成测撤下**：真-Isar widget 测因 enter 后 provider invalidate × Isar 流 tearDown 竞态 flaky（非接线 bug），已删不留脆弱测；接线由 flow 4 端到端 + `gauntlet_enter_test` + 现有 loadout 5 测佐证。
- **两 codex worktree 用户指定保留（续传·非我方债）**：`battle-ui-stage`（Codex 在写·勿碰）+ `battle-enemy-coverage-continuation`（已合 main·待用户拍清理）。

## 重要决策

- Task5 直接接线（去 `launchFlowForTest` seam）而非留 flaky 测 + dead seam：因真-Isar 装载 widget 测多次踩坑（off-screen tap / ensureVisible 撞 async 死锁 / pop 动画时序 / 流竞态），判定非接线 bug 后撤，避免脆弱测污染套件。

## 下一步建议

1. **视觉目检 4 屏（推荐）** — reward/defeat 新屏水墨/色板真机验收，唯一延后项，白天/Codex 派单。
2. **push** — main 39 + phase-b 12 commit（push 是用户的活）。
3. 复核 wave_b 配平红线排除副本奖励招 + 拍 2 孤儿招（千钧坠岳/烛影摇红）挂载去向。

## 踩坑提醒

- **真-Isar widget 测勿用 pumpAndSettle / ensureVisible**：撞真 async（Isar writeTxn / 内部 pumpAndSettle）死锁；走 runAsync+pumpUntilFound 配方（team_lineup 体例）。单 `pump(大时长)` 不带 runAsync 也会撞 enter 后 provider 重解析的真 async 死锁——settle 用 runAsync+pump 交替。
- **入庄按钮在滚动折叠线下**（720p 视口 y≈788）：widget 测点它需高视口（如 1280×1400）而非 ensureVisible（后者内部 pumpAndSettle 撞死锁）。
- 已写入 memory `feedback_widget_test_pump_until_found_settle`（配方复用）。
