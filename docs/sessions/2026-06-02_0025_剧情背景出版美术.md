# Session 交接 - 剧情背景出版美术

**时间：** 2026-06-02
**项目：** 挂机武侠（Flutter Desktop · Riverpod · Isar）
**分支：** main
**最后 commit：** 见 push 后 HEAD

## 本次完成

- **B2 Codex 视觉验收闭环**（Mac 本地 Codex）：大招题字 + Boss 金边,8 门 7P1W PASS,多模态亲验达标,WARN 仅截图时机非视觉缺陷。派单/closeout/3 截图归档 `docs/handoff/codex_*_battle_b2_2026-06-01*`。
- **剧情阅读屏出版美术基建全闭环并 merge main**（7 commit,brainstorm→spec→plan→subagent-driven 6 task TDD）：`stageNarrativePath` helper + `narrativeSceneScrim`(50%) token + `NarrativeSceneBackground`(仿 B1) + `NarrativeReaderScreen.backgroundImagePath` 可选参数 + 正文墨底浮层(alpha 0.55) + `stage_entry_flow` 三处 wire + `narrative_scene` 验收路由。缺图 errorBuilder 兜底,1667 测/1 skip/0 analyze。code review 1 Important fix(Scaffold 无条件 solid 底对齐 battle_screen 防 Windows 首帧闪)。
- **30 条 MJ prompt 交付 + 30 张图筛选归位**：`docs/handoff/mj_prompts_narrative_scene_2026-06-01.md`(主线 30 关 per-stage,16:9 留暗区)。用户出图后,从 autojourney 306 图中 4 选 1 多模态筛出 30 张 → `assets/scenes/narrative_stage_XX_XX.png`(oxipng 无损压缩 55→50MB)。
- **下载文件夹整理**：`~/Downloads/autojourney/批量美术素材/` 分档为 `已采用/`(30) `未采用/剧情背景落选/`(90) `未采用/其他批次/`(185,角色立绘/心法 tier 封面/卷轴 banner 等别功能批,仅归档非否决)。
- spec/plan：`docs/superpowers/{specs,plans}/2026-06-01-narrative-reader-publishing-art*`。

## 当前状态

剧情阅读屏出版美术代码基建 + 30 张专属背景图全部落地主仓,游戏内三章六回主线剧情阅读已可显专属水墨背景 + scrim + 正文墨底浮层。剩 Codex 真机视觉验收(图已就位,可验真实题材对位 + 观感)。出版美术 pass 战斗屏(A+B1+B2)+ 剧情屏均已收口。

## 进行中的工作

- 30 张背景图在 `assets/scenes/` 已 oxipng 无损压缩,**已随本次 commit 入库**。
- Codex 视觉验收 `narrative_scene` 路由:未做(图已就位,可随时验)。

## 已知问题

- 30 张图 oxipng 无损后仍 50MB(水墨渐变图无损天然大)。pngquant 有损可更小但渐变 banding 风险,需 Codex 同步验收,留作可选跟进(做则一次性替换避免 git 双版本膨胀)。
- B2 验收基建 backlog:`battle_scene`/`battle_boss_frame` 等 ScenarioLauncher 路由 READY 后自动结算过快,严格"战斗中"帧抓不稳;后续若复验战斗中态,值得加"READY 前冻结自动结算"开关。
- 支线(爬塔/飞升/招降/心魔)剧情仍走纯色底兜底(本波只主线,by-design)。

## 重要决策

- 决定剧情背景走「**复用思路但新出 MJ 专属图**」+ per-stage 粒度(30 张),因辨识度最高、用户已出图。
- 决定接线用方案 A(调用方传 `backgroundImagePath` + 通用屏可选参数),因最小侵入、沿 `topBanner`/`chapterCoverPath`/`BattleSceneBackground` 三既有体例。
- 决定本次只 oxipng 无损不 pngquant,因有损对渐变图有 banding 风险且未经 Codex 验,避免不可逆质损。
- 决定其他批次 185 图仅归档不删,因属别功能(角色/心法/banner),非本任务否决对象。
- 选片准则:题材识别度 > 真 16:9 > 水墨克制低饱和(绛红/暖调按章定弧:Ch4 暖、Ch5 转冷) > 中下留白利正文浮层。

## 下一步建议

1. **Codex 视觉验收 `narrative_scene` 路由**(Mac 本地,沿 `feedback_codex_visual_acceptance_mac`):`flutter run -d macos --dart-define=VISUAL_ROUTE=narrative_scene`,验背景题材对位 + scrim 深浅 + 正文墨底浮层可读性。可扩展验收路由覆盖多 stage 抽样。
2. **(可选)pngquant 有损压缩 30 图 + Codex 验近无损** → 一次性替换(50MB→~10-15MB),避免 git 双版本。
3. **其他屏出版美术**(背包/强化/奇遇等)或 **战斗场景长尾 8 biome 专属图**(出版美术 pass 续铺)。
4. push 后确认 origin 同步(本次含早前未 push 的 8 commit + 本次)。

## 踩坑提醒

- **autojourney 下载夹混多批**:筛剧情图时 306 图里含 185 张别功能批(角色/心法 tier/banner/旧场景),靠英文 prompt 开头(A_/The_/An_/Setting_/Crossing_/Outside_/Revisiting_)+ 排除 `depicting_a_*`(角色)/`tier_`(心法)区分。映射脚本见 `$CLAUDE_JOB_DIR/tmp/narr_map.json` 逻辑(已随 job 销毁,需重建按 30 prompt 开头子串匹配)。
- **Read 不支持 glob**:取 MJ 文件名必须先 `ls` 拿 uuid 全名再 Read。
- **改 narrative_reader_screen 必跑全量 `flutter test`**:T16 集成测在 root `test/widget_test.dart`,scoped 漏。
- **fresh worktree** 跑 Isar 测前 `cp 主仓/libisar.dylib`(2187KB) + `dart run build_runner build`(.g.dart gitignored)。
- **ExitWorktree remove 报「Discarded N commits」正常**:FF 合并后 commit SHA 已在 main,删的只是分支 ref。
