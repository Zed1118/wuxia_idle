# Session 交接 - 随机源收口 + 两 PR 合并 + 场景素材调查

**时间：** 2026-07-26 00:52
**项目：** 挂机武侠
**分支：** `worktree-rng-provider-closeout`（PR #81 draft·**未合并**）
**最后 commit：** `5e027673`（本批 2 commit：`56f5c35c` 代码 + `5e027673` docs，均已 push）

## 本次完成

① PR #79/#80 合并进 main（merge `32401583`/`8c9b5ef4`，两条 CI 各 success）；② §一#11 场景素材题字调查（用户拍板「先不改动」，条目保持开启）；③ §一#8 随机源收口（PR #81）。详 PROGRESS.md 顶段 2026-07-26 条（区间 `8c9b5ef4..5e027673`）。PROGRESS 没展开的：

- **没硬凑「全走 provider」**：`MilestoneEquipmentGrantService` 两构造点在无 `ref` 的层，强推需把 Riverpod 铺进 application 层。选「可注入 + 如实标风险」而非假装全清。
- **§一#11 真问题是「字是真是假」不是「有没有字」**：G5.1 只禁 AI 假汉字，印章却是美术方向主动要的母题（`PUBLISHING_ART_PASS_1_0.md:215` + MJ 模板 `T03.md:77`），两类须分开判。

## 当前状态

1.0 长线打磨期。main `8c9b5ef4` 树净、与 origin 同步。BACKLOG §一 余 6 条开启（#3/#4/#5/#6/#7/#11），其中 **#4/#5/#6 全卡在「待真人试玩数据」**——用户玩半小时可一次解锁三项。主线 16 章 80 关、cap 38、美术全补齐不变。

## 进行中的工作

- **PR #81（draft·MERGEABLE·CLEAN·CI success）等用户授权合并**。bg 会话禁 merge/推 main。
- **他方在途**：`codex/global-visual-polish`（tip `5f9ad2cc`·本会话期间持续推进·**工作区脏=仍在写**·tip 无 `[READY]`）。全程未碰，与 PR #81 零文件重叠。

## 已知问题

- **PROGRESS 已 98/100 行**，下批开工前必须先压缩归档，否则新条目加不进去。
- **6 张证据图留置**（0b）：`build/visual_acceptance/zh_inscription_ruling_2026-07-25/`，gitignored 随 checkout 存续。§一#11 未拍板下次要用，故不删；入库会进包体故不入库。
- **worktree/分支留置**（0c·第 1 轮）：`rng-provider-closeout` 承载 PR #81，删分支会废掉 PR。合并后才可清（清前跑 `feedback_exit_worktree_merged_branch_warning` 三验；ExitWorktree 会误报「Discarded N commit」，须先验 commit 从 main 可达）。
- `dart:math Random()` 签名约 15 处未动（含 `stage_entry_flow.dart:233`），换 provider 需另立注入点。

## 重要决策

- **§一#8 按「调用点能否拿到 ref」分两层修**，而非统一强推 provider——service 是纯类无 `ref`，强推会把 Riverpod 依赖铺进 application 层违背分层；构造注入让测试能塞确定性随机源，这才是本项真目的。
- **§一#11 只调查不改动**（用户拍板）。用户提出「不知道游戏画面长什么样没法判断」，补真机合成图后仍选暂缓；数据已落 BACKLOG，下次零成本重启。

## 下一步建议

1. **合并 PR #81（推荐）** — CI 绿 + 双证红闭环 + 零数值改动，风险面最小；不合则 §一#8 销账停在分支上。
2. **压缩 PROGRESS 归档**（~15min）— 98/100 已贴顶，任何后续批次都被卡住。
3. **Ch17「沙海纵深」实装批**（xhigh 专会话·~3.5-4h）— `idle_horizon` s1 45.6/下沿 45 贴线必破须重校。
4. **真人试玩半小时** — 一次解锁 §一#4/#5/#6 三项数值拍板，只有用户能做。
5. battle-ui-v2 阶段 5 全模式终验 — 需独占 app 与屏幕，当前被 codex 占用。

## 踩坑提醒

- **后台任务 exit code 可能来自命令链末尾的 `grep -c`**：零匹配返回 1，把全绿 `flutter test` 报成 "failed with exit code 1"。真值取命令自己 echo 的 `FULL_EXIT=0`，别信任务级 status。本会话中招一次。
- **GitHub run 级 conclusion 可能假红**：main `fbd567df` 标 failure，但 run_attempt=1、2 job 全 success、无一 step 失败。判 CI 红必须下钻 job/step 级。
- **`for` / `while [ ]` 复合语句在本会话 shell 解析失败**（`parse error near 'for'`），改逐条或用 `while ! grep -q ...; do sleep N; done`。
- **`cd` 跨 Bash 调用保留**：`cd` 到 tmp 后 `EnterWorktree` 报 "not in a git repository"。切目录后要么切回，要么全程绝对路径。
