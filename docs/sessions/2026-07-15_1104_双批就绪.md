# Session 交接 - 双批就绪

**时间：** 2026-07-15（同日第二轮 handoff，续 1007_编成实装）
**项目：** 挂机武侠
**分支：** main `71505270`（=origin/main）；产出在 PR #39 `cc128c84` / PR #40 `7e08c436` 两分支
**最后 commit：** main `71505270`

## 本次完成

前段详 1007_编成实装.md 及其补记；本轮增量：
- PR #39 CI 收口：换人 e2e 本地绿/CI 红竞态 → 修为「对每个 UI 终态各自轮询」（`cc128c84`）→ CI 双绿（test 16m18s/macos 8m1s）。A/B 实证：旧 tip 两跑两红、修复后即绿。坑已沉 memory `feedback_widget_test_pump_until_found_settle`（2026-07-15 补条）。
- 百草岭/断魂庄设计评审 → 用户采纳五项拍板+检查点降档 → spec 修订落 PR #40（draft，纯文档 +43/−14，分支 `docs/baicao-spec-revision`）。修订记录见 doc 文首七条索引。

## 当前状态

两个 draft PR 就绪待审：#39 出战编成屏（CI 双绿，随时可合）、#40 spec 修订（审点=§3.1 连跳兜底量级 + §5.6 检查点降档）。#39 是 #40 实施切片的前置。主 checkout 纯 docs，analyze 0（2026-07-15 实测）。

## 进行中的工作

- 无未完成实装；全部收敛为两 PR 待用户审合。

## 已知问题

- worktree ×2 留置：`team-lineup-impl`（#39）、`baicao-spec-revision`（#40），各自合并后 remove+删本地分支（续传第 1 轮，理由=PR 未合）。
- **#39 合并后主 checkout 必跑 `dart run build_runner build`**（.g.dart gitignored，不跑则编译失败）。
- 战报「击杀」标注口径查证：**已续传 3 轮，升级请用户拍板**——下轮塞 20-30min 处置，或明示放弃销账。

## 重要决策

- 决定 CI 竞态修「测试侧终态轮询」而非加大 settle 轮数，因为固定轮数在更慢跑器上仍会碎，轮询目标本身才是确定性同步点。
- 决定 spec 修订以七处增量落原 doc（不重写），因为原框架经评审无结构问题，增量+锚点索引最利终审。

## 下一步建议

1. （推荐）审合 PR #39 + PR #40 → 合并后收尾三件（build_runner / 清两 worktree / Codex 目检 `team_lineup` 路由）。
2. Phase A 百草岭前置批（占用服务+cap 10→17+吞吐探针+schema 0.37，spec §14，建议 xhigh）。
3. §十三 #3 Windows 实机验收（1-2h，D 段人工）。
4. 击杀口径查证（升级拍板：处置或弃）。

## 踩坑提醒

- CI 慢跑器 vs 真 Isar widget 测：详 memory `feedback_widget_test_pump_until_found_settle` 2026-07-15 补条（终态各自轮询 + snackbar 断言紧跟自身轮询）。
- `gh pr checks --watch` 退出码不可信（exit 0 时实际红过一次），终态必须重新 `gh pr checks` 现查。
- 并发组会取消 PR 的 merge-ref run：main 侧 push 或手动 re-run 都可能顶掉在途 run，红/取消先看 annotations 是否「higher priority waiting request」，别当真失败修。
