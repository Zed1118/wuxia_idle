# Session 交接 - M2 离线归来卡 + L1 显示设置

**时间：** 2026-06-15
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 50bada7f

## 本次完成

详 PROGRESS.md 顶段 续10 + 续11（commit 区间 `58ed0005..50bada7f`）。
- L3 闭关非阻塞 + 2 清理（上轮 worktree）gate review 后合 main
- M2 离线「归来」卡范围 A（13 测 TDD）+ L1 显示设置（10 测 TDD），均全 TDD
- 两轮 Codex/用户验收 + 两 fail 修复，UX 审查滚动表 H/M/L 全清

## 当前状态

1.0 长线打磨期。UX 审查滚动表 18 项全部收口清零。M2/L1 实装+验收全闭环，
main 与 origin 同步、工作树干净、无孤儿 worktree/分支。

## 进行中的工作

- 无。M2 范围 A + L1 已闭环；下波为新方向。

## 已知问题

- M2 范围 B（真通用被动离线挂机 + 累计总额）触红线 §5.5，需单独立项 +
  数值平衡复评，4 个待拍板点见 `docs/spec/2026-06-15-m2-offline-recap-design.md` §3。
- L1 F11 在 macOS 被系统「显示桌面」占用不可用，已改主用 Alt+Enter（用户实测 pass），
  F11 保留给 Windows。

## 重要决策

- L1 显示设置走 SharedPreferences 不进 Isar：窗口模式/分辨率是端机本地偏好，
  不该跨设备同步，绕开 saveVer 迁移（用户拍板）。
- M2 拆范围 A（安全可见化已发生闭关产出，0 红线）vs 范围 B（新被动经济，触红线单独立项）。
- WindowController 抽象隔离 platform channel，使 L1 编排逻辑可 TDD（窗口副作用用 fake）。

## 下一步建议

1. （推荐）等用户指定新方向——UX 滚动表已清零，无强依赖待办，方向应由用户拍板。
2. M2 范围 B 立项（需先拍板 4 点 + 数值平衡审）——红线相邻，非轻量。
3. 可玩性二期 backlog（`docs/spec/playability_phase2_backlog.md`）滚动挑项。

## 踩坑提醒

- macOS 全屏快捷键避开 F11（被系统占），用 Alt+Enter——详 memory
  `feedback_macos_f11_fullscreen_shortcut`。
- 视觉验收两工作流（手动:我出截图路径用户拍/自动:派 Codex）——详 memory
  `feedback_visual_acceptance_two_workflows`，发起验收前先建议模式。
