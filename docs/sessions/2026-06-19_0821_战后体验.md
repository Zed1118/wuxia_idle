# Session 交接 - 第七阶段 批一 战后体验

**时间：** 2026-06-19 (端午)
**项目：** 挂机武侠
**分支：** main（已 ff 合并 + 已 push）
**最后 commit：** f2a183ca

## 本次完成

详 PROGRESS.md 顶段续26（commit 区间 `8093fd35..f2a183ca`，spec+plan+6 task+收尾全 push）。
- 全程 brainstorm→spec→plan→**6 task subagent-driven TDD**（每 task spec+quality 两阶段 review + 最终 opus 整体 READY TO MERGE）。
- PROGRESS 没展开的：① 最终 review 抓到爬塔 gate 用 `floor.isBoss` 含小Boss，收窄到 `bossKind==major`（对齐用户拍板「大Boss」10/20/30）② 合并验证时**主 checkout 暴露 2 个既有节日测 flake**——今日端午(`numbers.yaml:1084`)撞 `main_menu_test` 非 hermetic 断言，与本功能无关，一并 hermetic 化修复。

## 当前状态

第七阶段（P2/P3 续作）批一·战后体验全闭环并 push，1.0 长线打磨期。英雄镜头(Boss首胜立绘切入) + 珍稀掉落利器首次细化落地，红线全守。批二/批三未起。

## 进行中的工作

- 无未完成模块（批一 6 task 全闭环并 main）。

## 已知问题

- **`computeFirstAcquisitionTiers` 无独立 isar 单测**（低风险：逻辑 5 行 + 文档化 + 135 mainline/tower e2e 覆盖集成路径）。
- **`project_recon.md` 未跟踪**（Codex 调研稿 737 行，会话开始即在，非本 session 产物）→ 留置,待用户定是否入库。
- 远程 stale 分支 `origin/feat/phase5-battle-experience` / `origin/redline-semantics-twolayer` 留置（远程债,本地分支已全清）。

## 重要决策

- 英雄镜头=立绘切入（复用现成 founder/弟子 png 无需新美术）；触发仅 Boss 首胜（主线章末 + 爬塔 **major** 大Boss）；出镜=本场最高输出。
- 珍稀掉落细化只做装备 tier（利器首次/重器每次）；**技能书/残页展示 defer 批二**（走独立 skill-drop hook，真解源自章末Boss首胜与 Boss 掉落改造同批）。

## 下一步建议

1. **真机手感目检**（推荐）— `flutter run -d macos` 打章末/大Boss 首胜验英雄镜头立绘切入/题字/光晕/时长/跳过；运动效果单帧截不出，逻辑层已测，手感需真机。
2. 批二·Boss 机制标准 — 多阶段/弱点抗性/协同窗口 Boss + 技能书珍稀展示（defer 项）。
3. 批三·队伍成长 — 渐进解锁/编成 UI/二弟子控制（需 LineageRole 子枚举）+ 复盘队伍跳转。

## 踩坑提醒

- **节日/节气测非 hermetic 是日期 flake 高发区**：本项目 `todayFestivalProvider` 读真实 `DateTime.now()`，逢配置节日（端午/夏至等）时「非节日」断言挂。修法=`todayFestivalProvider`/`festivalServiceProvider` override 为 null 解耦墙钟。沿 memory `feedback_flutter_ci_local_green_red_divergence`。
- **ff-merge 后必在主 checkout 实测**：worktree 跑测的日期 ≠ 合并日，本次正是 worktree(6-18)绿、主 checkout(6-19 端午)红，暴露日期 flake。worktree 绿 ≠ main 绿。
- fresh worktree 需先拷 `libisar.dylib` + `dart run build_runner build` 才能跑测（dylib 截断/`.g.dart` gitignored）。
