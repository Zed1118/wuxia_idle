# Session 交接 - 打磨批合并与 codex 复刻评审

**时间：** 2026-07-31 16:01
**项目：** 挂机武侠
**分支：** main
**最后 commit：** ef443e4d

## 本次完成

详 PROGRESS.md 顶段 2026-07-30「用户拍板『按推荐执行』批」（commit 区间 `09c44486..ef443e4d`）。
- 夜批四 PR（#100-#103）+ 我方三 PR（#104 B3 / #105 C5+焦点 / #106 F2）**全合**，四侧清空
- codex 战斗样板复刻批评审：R1 **82** → 派单返修一轮 → R2 **98/100**（分支仍未合）

## 当前状态

主线内容与美术已闭环，处 1.0 打磨期；main 全绿且与 origin 同步。唯一在途是 codex 复刻分支，等合并决策。

## 进行中的工作

- `codex/battle-ui-sample-replica`：20 commit / tip `[READY]` / worktree 干净 / **纯本地未推未合**。
  我已独立验证：全量 4779/0 · analyze 0 · format 0 changed · 三视口原生截图零 overflow ·
  四条硬约束（B3 文件 / 数值规则层 / allowlist / 生产 previewPouchItems）逐条未违反。差最后一步：推分支开 PR 或直接合。

## 已知问题

- codex worktree 留置：**非孤儿**，是待合交付物，本轮首次挂账
- 复刻批未拿的 2 分：顶栏 tooltip/键盘走查无直证；「渲染器帧 vs 原生窗口截图」局限只写在 plan 末尾未前置
- B3 登记值精度 ±9；`cliffwaterfall` 仅 boss 用法取样带可能被立绘右缘侵入（走基线档，当前不影响行为）
- 断魂庄 / 百草岭远征仍无 audit 路由（跨多轮挂账）；评审证据与派单书留置
  `build/dispatch/battle_ui_replica_review_20260731/`（1.3M，gitignored，随 checkout 存续）

## 重要决策

- **B3 主杠杆取 opacity 而非按 style 钉系数**：`out=op×立绘+(1-op)×背景` 物理自限，暗底掺不进亮色；
  实测暗系 style 本就落在明度下沿之下，一条下沿即兑现暗场景保护，省掉 5 个凭空系数
- **行囊 previewItems 只给 debug 路由、生产钉空**：兼顾样板保真与「不谎报玩家持有道具」红线
- **代 codex commit**：沙箱挡住写主仓 `.git`，四项已做完，代提交比重跑一轮长单便宜

## 下一步建议

1. **（推荐）推 codex 分支开 PR → CI 复跑 → 合并** — 已本地验全绿，只差 CI 独立背书，是唯一在途交付物
2. 复刻批剩余 2 分：顶栏 Tab/tooltip 走查取证 + 交付摘要前置方法学局限
3. 断魂庄 / 远征补 audit 路由（解跨轮挂账，同时补齐视觉验收覆盖面）
4. B3 观感真人拍方向；要调只动 `battleStandeeFusionOpacityAtFull` / 下沿 / 上沿三常量，门禁测守边界

## 踩坑提醒

- **派 codex 到 worktree 必须 `--add-dir` 覆盖主仓 `.git`**（元数据在主仓 `.git/worktrees/` 下，
  只给 `-s workspace-write` 会让它活干完却提不了 commit）→ memory `feedback_codex_worktree_dispatch_sandbox`
- **codex 自证据只拍样板黄金尺寸会掩盖缺陷**（冷却签遮字在 1672 最不明显、720p 最严重），
  派视觉单必钉「720p 复拍」→ memory `feedback_golden_frame_hides_viewport_defect`
- **只扫 tip `[READY]` 会误判就绪**：R2 做完时 tip 仍是 R1 陈标记、靠脏工作树兜住；§8.3 判就绪须两条同时看
