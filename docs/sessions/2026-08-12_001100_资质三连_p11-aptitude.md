# Session 交接 - 资质三连实装(P11)

**时间：** 2026-08-12 00:11
**项目：** 挂机武侠
**分支：** `feat/aptitude-birth-rarity`(**未合 main**,PR #119 OPEN/MERGEABLE)
**最后 commit：** `be029505`

## 本次完成

详 PROGRESS.md 顶段「2026-08-11 P11 资质三连实装」条目(commit 区间 `c25a29fa..be029505`,3 commit)。

PROGRESS 没展开的两条:

- **真实存档验证已补上**(#15 原文自标「未做真实存档验证(本会话无老档样本),以上为读码推断」):拿真实 `0.33.0` 老档副本跑迁移,祖师 21 点 / 奇遇加点 0 / 存的档位 `biaoZhun` → 迁移后 `ziYou`,「档位被订正 1/1」,数值字段零改动断言同过。**#15 描述的 bug 在真实存档里确实存在,推断成立**。探针是一次性的,已删,不入库。
- **K2 extension 棘轮第一次真拦到人**,处置走完整审计再更新白名单(K1 报告附录 A 新开「增量登记」段),不是改数字了事。

## 当前状态

P11 两个子项(一#15 旧档回填 / 一#16 chip 括注)已实装、验证、推送,**卡在你合 PR #119**。第三项「视觉档位化」按拍板并入试玩局,已单独立为 BACKLOG 一#19。
下一波原计划(本会话未做完):① 修 BACKLOG 二#12 视觉路由存档污染 ② 备真机决策局 ③ P10 扫描范围收敛。

## 进行中的工作

- **备真机决策局(2A)**:已完成 = macOS debug 构建产出(`build/macos/Build/Products/Debug/wuxia_idle.app`)+ 摸清 133 条视觉路由且**运行时参数 `--visual-route=<id>` 可选路由**(不必重编译)+ 逐项映射到路由。**未做** = 决策局清单文档、启动脚本。
- **P10 扫描范围收敛(3B)**:用户已拍板取 (b)「单列一类」,**一行未动**。
- 修 BACKLOG 二#12:发现并登记,**未修**。

## 已知问题

- **⚠ 你的存档 slot 1 已被顶到 `0.39.0`(本会话事故)**:我跑视觉路由烟测时触发,根因见 BACKLOG 二#12。**数据零损**(逐角色探针确认),但**main 分支构建现在打不开 slot 1**(版本高于程序版本会抛 `UnsupportedSaveVersionException`)。**合掉 PR #119 即恢复**。slot 2/3 未受影响。
- 备局清单未产出前,**别照着「跑视觉路由」的思路自己去验收**——每跑一条就再污染一次生产存档(二#12 未修)。
- 遗留 worktree/分支:`.claude/worktrees/p11-aptitude` + `feat/aptitude-birth-rarity` 故意保留(PR 未合)。远端 4 个备份分支仍挂 P12 待拍,**已续传 3 轮**,本轮仍未处置(纯拍板项,非我可代决)。
- 本会话产物:debug 构建(gitignored)、worktree 内 `libisar.dylib`(gitignored)、job 临时目录下的存档副本(随 job 清理)——均无需入库。

## 重要决策

- **出生点数算法走 extension 而非 `Character` 类内 getter**,因为 isar_community 会把类内公开 getter 生成成持久化属性(实证 `Attributes.total` → `attributes.g.dart` `PropertySchema(id:4)`),加进 `Character` 就是白给的 schema churn 且落盘值永不回读。
- **迁移按出生点数而非当前总点数**,因为祖师出生 21-22 吃满生涯 cap +5 后当前 26-27,越出 `rarity_distribution` 上界会被钳到 `jueShi`,正是出生锁死要避免的档位失真。
- **修 visual route 存档污染算进「备局」范围不算扩范围**,因为备局交付本身就是让用户去跑 8 条视觉路由,不修等于递一颗雷。

## 下一步建议

1. **(推荐)合 PR #119** —— 一条命令,同时解掉 slot 1 打不开的问题;它还带着上一会话未 push 的交接 commit `c25a29fa`
2. 修 BACKLOG 二#12 视觉路由存档隔离 —— 备局的前置,不修则备局不可安全交付
3. 备真机决策局 —— 一局解 8 项积压决策(工程面 9.5/10 vs 流程面 4.5/10,瓶颈在决策)
4. P10 扫描范围收敛 —— 范围已拍板 (b) 单列一类,可直接实装

## 踩坑提醒

- **跑 debug 构建的视觉路由会开并迁移真实存档**——根因与实证见 BACKLOG 二#12,修之前别随手跑。
- **管道吃掉退出码**:`flutter test | tail -30` 报 exit 0 实为 `tail` 的码,真实 1 fail 且失败详情被截掉,只能整轮重跑(白丢 7 分钟)。要退出码就别接管道(memory `reference_anti_hallucination` 阈值 3)。
- **isar_community 把类内公开 getter 当持久化属性生成**——给 Isar collection 加派生值用 extension,加完跑 build_runner 看 `*.g.dart` 有没有多出属性来自证。
