# Session 交接 - night-ui 合并 + 视觉验收修复

**时间：** 2026-06-30 上午
**项目：** 挂机武侠
**分支：** main
**最后 commit：** e3b899a7

## 本次完成

详 PROGRESS.md 顶段 3 条（commit 区间 `55b70f46..e3b899a7`）：
- night-ui 4 批次评审→合入 main（b1 foundation/b2 main-flow/b3 system-pages/b4 qa-safety）
- 视觉验收 P1+5 P2 布局+sect fixture+encounter 仪式浮层（cf168e55）
- 视觉验收 batch2：battle HUD+P3+3 动态 route+清死串（f9257906）

增量（PROGRESS 没展开）：
- bg 写守卫下 **EnterWorktree 实测可用**（cwd 虽非 git，工具仍成功建 worktree+切入），authoring 走隔离 worktree+subagent implementer，merge 走主 checkout `git -C`。fresh worktree 必 pub get+cp libisar.dylib+build_runner 预热。

## 当前状态

1.0 长线打磨期。night-ui UI 专业化（首批 4）+ codex 视觉验收 backlog（P1×1/P2×8/P3×2+3 动态 route+死串）本端全闭环合入 main 并 push、同步。

## 进行中的工作

- 无（本端）。等 codex 视觉验收套件重跑复核全 59 路由×全分辨率无回归 + 我未逐张截图的布局页（zangjuange/inventory/taohua/character_panel/shop）观感。

## 已知问题

- **codex 视觉验收 WIP 留 main 未提交**（7 文件：`docs/handoff/visual_acceptance_2026-06-30/` + 增强截图工具 visual_capture.sh 等）——codex 产物非本会话创建，留给 codex 收口，我全程没动。
- **codex night-ui b1-b4 4 worktree/分支保留**（已全合入 main 的源分支，非本会话创建）——0c 留置，首次 handoff 续传，建议下轮或 codex 确认后 `git worktree remove`+`branch -d`。

## 重要决策

- encounter banner 真问题=生产 SnackBar 仪式感不足（非 fixture 限宽假象），升级居中仪式浮层 `showGeneralDialog`+放大装帧（守爽感主旋律）。
- sect_screen_npc=fixture 问题非 UI bug（默认 tab 停空事件态），SectScreen 加 `initialTabIndex`。

## 下一步建议

1.（推荐）codex 重验反馈处理——下一步天然在 codex，它挑出新问题再发我。
2. 清 codex b1-b4 worktree/分支（低优先，需 codex/用户确认）。

## 踩坑提醒

- **visual_capture 残留 app 进程**：新版去掉 kill stale，连截多 route 会截到上一个窗口——每 route 前 `pkill -f VISUAL_ROUTE` + `pkill -f Build/Products/Debug/wuxia_idle.app`。
- worktree 旧版 `visual_capture.sh` 不支持 `--ready-timeout`（codex 增强未提交进 main）——从主仓拷 codex 工作版用，完后 `git checkout --` 还原不进提交。
- 1280 截图比例易误判「居中约束生效与否」，看 1920 更准（skill_codex 虚惊一场）。
