# Session 交接 - invalidate 速修（体检批3）

**时间：** 2026-07-07 21:30
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 23a7c301（本会话：批3 分支 commit 8232ff5a + merge 23a7c301，全 push；CI run 28869711180 运行中）

## 本次完成

详 PROGRESS.md 顶段 2026-07-07「体检修复批3:invalidate 速修」条目。承批1，按体检候选表推荐序开批3。

- **P0-5**（三门控 provider 全仓 0 invalidate）+ **P1-6**（扫荡漏角色 family）+ **P1-11**（疗伤失效错角色）一把收。
- 新增共享 helper `lib/features/battle/application/post_combat_invalidation.dart`：`invalidateAfterCombatSettlement(void Function(ProviderOrFamily))`，集中角色 family + 主菜单门控 + 银两。三结算路径（stage/tower/sweep）统一调，消灭复制粘贴 + 各自遗漏。
- 疗伤两处（post_battle_healing_panel / injury_status_view）改失效角色全 family（疗伤目标是全队最伤者非固定角色）。
- **P1-8 证伪 = 误诊**：tower roll(228)→落库(246)→invalidate(305) 时序正确，真实缺陷是缺门控/银两 invalidate，已随 helper 修。

## 当前状态

1.0 长线打磨期。main HEAD 23a7c301，与 origin 同步。批3 已合。**已验证（worktree 实测）**：analyze lib/test **0**；新 helper 3 测绿；targeted 435 绿；全量 **3736 pass/1 skip/0 fail 无失败行**（基线 3733+3）。CI run 28869711180 待确认（收尾时若未绿需 gh run view 复查）。

## 进行中的工作

- 无在途 worktree/分支（本批 worktree 待清）。体检候选表批 1、3 已销，剩批 2/4/5/6/7。

## 已知问题 / 遗留

- **P1-11 疗伤 invalidate 行无自动回归测**：widget 内方法 + Isar widget test 死锁风险，靠代码审查 + 现有 formatter/service 测兜底（诚实标注，非阻塞）。
- **闭关（主动 seclusion）产银两门控刷新**：P2 级同根缺口（seclusion_service 落 item_silver 无 invalidate silverBalance/shopUnlocked），未纳本批。offline_passive（离线被动）核实只产磨剑石不涉门控，已确认无需改。
- 真机冒烟未做：首获银两/装备/杀 Boss 后主菜单三入口即时解锁，建议下次 playtest 顺带目检。

## 重要决策

- helper 用 `void Function(ProviderOrFamily)` 参数（生产 `ref.invalidate` / 测试 `container.invalidate`），无 WidgetRef 耦合、可 ProviderContainer 单测。
- defeat 分支也调 helper：defeat 不产资源但 invalidate 门控无害（重算得同值），换取单一事实源。
- online_presence 离线被动**只更注释不扩门控**：核实产出仅磨剑石，补门控会是死代码（不硬编未核实路径）。

## 下一步建议

1. 拍板 P1-14 AOE 记账语义 + P1-9 敌 ultimate 进 AI（~15min，批 4 前置；后者改 Boss 难度需复测）。
2. 体检批 2：战斗 wiring（群战波次/轻功地形进 tick 路径，opus xhigh，改动面大单独一波，必加「生产 tick 路径 wiring」维度 grep）。
3. 体检批 4：战斗小修（P0-4 interveneNow 边界 + P1-12 iconPath 哨兵；P1-9 拍板后并入）。

## 踩坑提醒

- `ProviderOrFamily` 类型未从 `flutter_riverpod` 主 barrel 导出，须 `import 'package:flutter_riverpod/misc.dart' show ProviderOrFamily`。
- 本会话中途 Bash 工具短暂返回 stale/污染输出（所有命令返回同一 git status + "git 代理清理未生效"），git commit/log 全被 block；重试后自愈。commit 前务必 `git log -1` 核实真实落地，别信被污染的回显。
- 删私有 helper 后 stage/tower 的 character_providers/inventory_providers import 变 unused，analyze 当 radar 抓到后清理。
