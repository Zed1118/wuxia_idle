# Session 交接 - 桃花岛 balance + 爬塔/战斗/结算 UI 修复

**时间：** 2026-06-25 21:35
**项目：** 挂机武侠
**分支：** main
**最后 commit：** bbf72d36

## 本次完成

详 PROGRESS.md 顶段两条（commit 区间 `27a9740f..bbf72d36`，均已 push）：
- `35a17014` 桃花岛 balance 第一刀（cap 对齐 72h·确定性探针校）
- `bbf72d36` 爬塔/战斗/结算 5 项实玩反馈 UI 修（用户副屏目检确认）

## 当前状态

1.0 长线打磨期。桃花岛 cap 自洽收口（产速保持温和补充未动）。爬塔/战斗/结算
UI 5 处实玩瑕疵已修。无腐烂技术债，无孤儿 worktree/分支。

## 进行中的工作

- 无半截工作。两批均合 main 并 push，worktree 已清理。

## 已知问题（全是登记决策·非债）

- 桃花岛配方比率/升级成本仍占位，待后续 balance。
- GUI 手感/中文渲染仅用户副屏瞥过；本环境 headless 多显示器+全屏 Space 无法自截游戏窗口（screencapture 抓不到非活动 Space），视觉验收靠用户。
- #2 剧情空白本次按「短段落上下留白→靠上对齐」修；若指「某段内容为空」仍待用户截图定位（已问，用户选前者）。

## 重要决策

- 桃花岛 cap 对齐 72h 而非降 cap_hours：兑现 offline=online 承诺（用户拍板）。产速不动，定位温和补充。
- 战斗站位 #3：弹道坐标 `_slotFrac` 分母从硬编码 3 改实际人数 n，与 `_TeamColumn` 视觉同步（抽 `slotVerticalFraction` 纯函数，避免两处漂移）。
- 结算印章移除：胜利另有 VictorySealFlash 仪式承担印章高光，overlay 内小印章冗余且渲染弱。

## 下一步建议

1. 桃花岛配方比率/升级成本 balance（推荐）：唯一登记 pending，需用户定升级成本相对银两收入的节奏。
2. 第六阶段战斗轴（流派的形与势 #1/2/4/11/12/16 挂账）。
3. 桃花岛二期类目（木工坊/矿洞/灵泉·闭关离线归口）。
4. 桃花岛场景化主屏（待美术）。

## 踩坑提醒

- **flutter build macos 会改 `macos/Runner.xcodeproj/project.pbxproj` + xcworkspace**，提交前 `git checkout --` 撤销，勿混入 commit。
- **多显示器+全屏**：游戏窗口常落第二显示器/独立 Space，`screencapture -x` 只抓主屏活动 Space → 抓不到。靠用户副屏目检，别在自截上耗时。
- 视觉改动合 main 前先让用户副屏目检确认，再 ff merge（本次体例）。
