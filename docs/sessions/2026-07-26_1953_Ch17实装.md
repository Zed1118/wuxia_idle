# Session 交接 - Ch17「沙海纵深」整章实装

**时间：** 2026-07-26 19:53
**项目：** 挂机武侠
**分支：** `main`（已 push·与 origin 同步）
**最后 commit：** `327413b3`

## 本次完成

详 PROGRESS.md 顶段 2026-07-26「Ch17 整章实装」条（区间 `fef32d4a..327413b3`·4 commit·含 `--no-ff` 合并 `754c96b6`）。PROGRESS 没展开的：

- **spec 冻结 ≠ 实装无阻**：Ch17 spec 是前一天刚做完 Phase 0 的，仍漏了 2 个站点（`skill_count_contract:28` 的 genericIds 断言、`battle_standee_asset_role_test` 整个门禁）。**spec §9 的站点清单要当「起点」不当「全集」**，真正的全集只有全量测试知道。
- **本次唯一的真失败是靠「抓第一个 `-1`」找到的**：compact reporter 的 `-1` 会一路挂在后续通过行上，日志尾部显示的文件名是假象（第 1934 行才是真身，尾部指向的是另一个文件）。
- 用户拍板 2A 修的 `skill_tie_ma_bing_he` 缺 `huaJing`，是 Ch16 批 `5441ea3d` 写入截断——**现有测试无一覆盖 proficiency 三层完备性**，故静默了两天。

## 当前状态

1.0 长线打磨期。主线 **17 章 85 关**，宗师段 2/3 章完成（Ch18 未起）。cap 40（宗师·圆熟）。机制型 Boss 首次进入主线。main 与 origin 同步，工作树净。

## 进行中的工作

- **Ch17 美术 11 图未出**：清单已在 `test/fixtures/known_missing_assets.txt` 登记（5 敌立绘 + 封面 + 5 叙事背景），需 codex image_gen 专批。出图后两件收尾：按 alpha 包围盒实测补 `character_avatar.dart` 脚底 fraction（当前走 default 0.95）、删 allowlist 表项（一删门禁自动恢复把关）。
- **Ch18「天地之远」spec 未起草**：段级 spec §8 已定向（cap 40→42 封顶·yang_guan 收编·章中+末 0.12 全机制）。

## 已知问题

- **残留远端分支 `origin/worktree-session-handoff-0726`**（已续传 2 轮）：本轮已逐字节验证可安全删——`2268157f` 内容与 main 的 cherry-pick 版完全一致（两侧 4656 字节），`70406327` 只是 NEXT.md 旧 11:16 版。**已向用户报请授权，未获回复即未动**（删远端分支属对外操作）。命令：`git push origin --delete worktree-session-handoff-0726`。
- **6 个僵死 flutter_tester 进程**（PID 57838 等）：启动于 2026-07-18 23:24、已挂 7 天 20 小时、PPID=1、引用的 `.worktrees/kimi-stage-entry-tests` 路径早已不存在。非本会话产物，未擅自 kill，仅记录。
- 17_03 用 `biome: temple`（spec 冻结值）但枚举语义是「寺院经阁」，与「沙埋古城」略不贴；实测 `encounters.yaml` 零 biome 门控，故当前**零机制影响**。

## 重要决策

- **17_04 定 lingQiao 而非 spec §3 表写的 gangMeng**，因为它的 `chargeSkillId` 是灵巧招「风卷流沙」（自述「身法散作流沙」），且现存 18 个 Boss 蓄力技流派与敌 school **无一例外全部一致**；改后反而更贴段级 spec 的「Ch17 灵巧主题」。
- **wave_b 语义收紧用「单链结构派生章末关」而非锚 `_05` 字面**，因为字面锚是瞬时事实、结构才是约束语义；另补「章中 Boss 也至多 1 本」保住原强度，避免收紧变放水。
- **standee 门禁复用 asset_audit 的同一份 allowlist 而非另开豁免**，因为出图后 guard 2 会强制清表项，门禁自动恢复——双向棘轮，挂不了账。

## 下一步建议

1. **Ch17 美术 11 图 codex image_gen 专批（推荐）** — 唯一能闭合本章的收尾，出图后能同时清 allowlist + 补脚底 fraction，让 standee 门禁恢复全量把关。
2. 删残留远端分支 — 一条命令，已验证零风险，需你授权。
3. Ch18「天地之远」spec 起草 — 宗师段收官章，段级 §8 已定向；**起草前必读 Ch17 spec §1**（那里记了上游段级 spec 的三处事实错，不读会再踩）。
4. 29 组历史详情近似图第一批 — 复查文档 §P2-01 已给顺序。

## 踩坑提醒

- **`flutter test` compact reporter 的 `-1` 带向后续**：日志尾部的文件名不是失败者，必须 `grep -n -m1 -- "-1:"` 抓第一个。已在 memory `feedback_flutter_test_minus1_carryforward`。
- **加章 reconcile 两个新站点**已补进 memory `feedback_wuxia_add_mainline_chapter_reconcile`：立绘 standee 注册表 + `skill_count_contract` 的 genericIds 断言。**推论已一并写入：章批与美术批分两次的项目，凡「读真实资产」的新门禁都要先想清中间态**。
- **`git diff <ref>:<中文路径>` 比对会因八进制转义假报「不存在」**，改用 `git show ref:path` 取字节数/内容比对。
