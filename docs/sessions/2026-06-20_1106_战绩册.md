# Session 交接 - P4 战绩册 + 外审 3 bug

**时间：** 2026-06-20
**项目：** 挂机武侠
**分支：** worktree-p4-battle-record（未合 main · 已 push origin）
**最后 commit：** 7eb910ec（push 后以现跑为准）

## 本次完成

- **拜入 overlay VISUAL_ROUTE**（`disciple_join_ceremony`）已合 main 并 push（commit `faa0bdd0`，main 上）。
- **P4 战绩册全 12 task 实装**（brainstorm→spec→plan→subagent-driven TDD，commit 区间 `ad309623..b28658ca` + `3b6fad18`，**全在 branch `worktree-p4-battle-record`，未合 main**）：BossMemory collection(saveVer 0.26)/service(首胜建·重打累加·幂等)/老档回填骨架/主线+塔 victory 接线(best-effort)/主屏(剩影占位)/详情屏(pre-record 降级)/主菜单 gated 入口/2 VISUAL_ROUTE/红线测。最终 opus review **READY TO MERGE**。
- **外审揪出 batch3(已在 main)两 bug + 我自查出 P4 一 bug**，三处同病根（写 Isar 后 UI 不刷新 / 配置无红线）。**只修完 P2**（commit `7eb910ec`：`enforceLineageOnboardingRedLines` + 7 测）。

## 当前状态

P4 战绩册本体完成、未合 main。三 fix 中 **P2 已完成**，**P1 + P4-entry 未修**。spec/plan：`docs/spec/2026-06-19-p4-battle-record-{design,plan}.md`。

## 进行中 / 未完成（下个会话首要）

- **P4-entry（未修）**：T10 主菜单入口门控 `ref.watch(bossMemoryCountProvider)`，但 T6/T7 victory hook 写 BossMemory 后**没 invalidate** → 首杀后入口要重启才现。修：`stage_entry_flow.dart` + `tower_entry_flow.dart` 的 `runBossMemoryHookAfterVictory(...)` 调用后加 `ref.invalidate(bossMemoryCountProvider); ref.invalidate(bossMemoryListProvider);`（hook 在生产 else 块内，ref 可达）。
- **P1（未修）**：`disciple_join_hook.dart:18` 收了 `ref` 没用；拜入弟子写 `activeCharacterIds` 后无人 invalidate（全 lib grep `invalidate(activeCharacterIds`=0）→ 主菜单/师徒页旧队伍。修：hook 内 join 成功(joined!=null)后 `ref.invalidate(activeCharacterIdsProvider)`（lineageInfo 依赖它会级联）。
- 两处都**必写「不重启即刷新」测**（ProviderContainer 读旧→写→invalidate→读新），别只测落库（见 memory）。
- 全做完 + 全量绿 → 合 main + push。

## 已知问题

- 全量 test 观测到 ~1/15 transient（最初 1 次，之后 14 次连绿）；最终 review 深查判**非 P4 引入**，疑既有 `tower_entry_flow` leaderboardSync teardown 噪音。下个会话全量跑若复现，往 P4 外查。
- 孤儿 worktree：本 `worktree-p4-battle-record` 保留（работа 未合，故意留）；无其它孤儿。

## 重要决策

- 三 fix 并入当前分支一起合：P4-entry 未修前**不合 main**，避免 ship 已知 bug；P1/P2 虽属 batch3 但代码在本 worktree（inherited），一并改一并合最干净。

## 下一步建议

1. **（推荐）修 P4-entry + P1 + 各测 → 全量绿 → 合 main**：同病根小改，闭合信任修复批。
2. 真机目检战绩册（VISUAL_ROUTE battle_record / boss_memory_detail，需先合或在 worktree 跑 macOS）。

## 踩坑提醒

- **subagent「全 pass」+ 外审 OK 只覆盖被测维度**：provider 刷新 / 配置健壮没写测就整条链全绿地漏（本批两 bug 即此）。fix 必写能抓到该洞的测 + 自己跑全量，别转抄 subagent 报告。详 memory `feedback_ui_refresh_after_isar_write`。
- bg 会话 worktree 内 git commit 偶发未执行（本会话 P2 首次 commit 静默没落，git status 才发现）→ commit 后必 `git rev-parse HEAD` + `git status -s` 现场核。
- MasterDef 属性类型是 `AttributeProfile` 非 `MasterAttributeProfile`（写测踩过）。
