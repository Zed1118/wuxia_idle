# Session 交接 - 扫描器收口 + 资质三连 Phase 0

**时间：** 2026-08-11 15:36　**项目：** 挂机武侠　**分支：** main　**最后 commit：** `9311f1d6`

## 本次完成

详 PROGRESS.md 顶段「2026-08-08 夜批(8h 挂机)」条目——本 session 产出分两段挂在其中:**【同日上午续】之后的正文**(P8/P9,08-08)与末尾**【2026-08-11 续】**段(P7/底账/Phase 0)。commit 区间 `6cb6bc5c..9311f1d6`。三条 highlight:

- **本 session 跨 08-08→08-11 两天**,我一度把 08-11 做的事全标成 08-08,已按 commit 实际日期订正 10 处(pool/BACKLOG/tools README/测试文件),PROGRESS 续段头写明了这件事。
- **P7 的漂移是两地自测出来的,不是采信提案**:fresh worktree `dead=956/ignored=575` vs 主 checkout `dead=939/ignored=592`,修复后两地逐值相同 → 17 归零。
- **资质三连 Phase 0 推翻了 BACKLOG 一#15 自己写的前提**(见「重要决策」)。

## 当前状态

main `9311f1d6`,与 origin 同步、工作树干净、worktree 与本地分支只剩 main。滚动池待发 4 条(P2/P10/P11/P12),BACKLOG 一区 7 项待拍板。**无在途实装**。

## 进行中的工作

- **资质三连(P11)实装未开**,Phase 0 已完(结论见 PROGRESS 续段 ⓓ)。用户已升 xhigh 并拍板:一#15 取 (c) 按出生点数重算 / 一#16 取 (b) 改显出生点数 / 视觉档位化并入试玩局。
- 还差:saveVersion `0.38.0`→`0.39.0` + 迁移段、chip 改传出生点数、红线测 + 破坏证红、format + analyze + **全量**(改 saveVersion 属跨切面)。

## 已知问题

- **远端 4 个遗留备份分支**未清:`codex/taohua-art-0807`、`qoder/p4-audit-scripts-0808` 实测 `main..分支`=**0 条独有**可安全删;`pi/p6-link-label-0808`=2、`worktree-claude-rarity`=3 是 rebase 前旧 SHA。**已续传 2 轮,本轮升级处置** → 登记滚动池 P12 交用户拍。
- `doc_link_scan.py:59` `EXCLUDE_DIRS` 定义后全仓零消费,与「939 条 dead 里 600 条来自 `docs/handoff/` 归档文档」合成滚动池 P10,**未修**。
- 临时文件全在 `$CLAUDE_JOB_DIR/tmp`,不在任何仓库,随 job 清理。

## 重要决策

- **资质档位仍用存储字段 + 一次性迁移,不改 getter**,尽管 Phase 0 证明出生点数总能精确还原(`total − attributeBonusFromAdventure`)。理由:① `character.dart:71` 有 08-08 明确记录「不可改写成 getter」;② **将来若新增非奇遇的属性来源(丹药/传承),getter 会静默漂移,迁移后的存储值不会**——后者才是决定性的。
- **P8 的 4 条 `expectedFailure` 在 P7 落地时立即转正式断言**:那套装饰器的设计目的就是以 unexpected success 逼人来删,留着就变成新的假绿。
- **不擅自删远端 4 分支**:其中 2 个 git 可断言已包含,另 2 个「内容已在 main」是推断而非 git 可证的关系,删除权交用户。

## 下一步建议

1. **（推荐）资质三连实装(P11)** —— Phase 0 已完,且是唯一玩家可见的现存缺陷
2. 拍 P10 扫描范围收敛的范围 —— 唯一能把文档信噪比 4/10 从根上抬起来的活
3. 拍 P12 远端分支 / BACKLOG 一#17 C2b 四图 / 一#18 README 定位
4. 安排一次试玩局 —— 一次解锁 BACKLOG 里 6 项卡「没人真玩过」的条目

## 踩坑提醒

- **`git grep -E` 是 POSIX ERE,不认 `\s`**——用 `\s` 查属性写入站点返回空,险些当成「不存在」;改 `[[:space:]]` 并用纯字符串交叉验证(memory `feedback_negative_grep_not_proof_of_absence`)。
- **管道后 `$?` 是末端命令的码**,`python3 test.py | tail` 取到的是 `tail` 的码,本会话掩盖过一次真实 exit 1。要退出码就别接管道。
- **写守卫按会话 cwd 判不按目标路径**:cwd 留在主 checkout 时连写 worktree 也被拦,先 `cd` 进 worktree(memory `feedback_bg_session_write_guard_subagent_dev`)。
- **python heredoc 带中文须加 `# -*- coding: utf-8 -*-`**,否则报 Non-UTF-8 且**静默不落盘**(memory `feedback_chinese_path_shell_pitfalls`)。
