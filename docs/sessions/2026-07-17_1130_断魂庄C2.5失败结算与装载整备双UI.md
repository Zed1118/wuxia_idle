# Session 交接 - 断魂庄 C2.5 失败结算 + 装载/整备双 UI + 2 visual_route

**时间：** 2026-07-17（承 C2.3+C2.4 会话）
**分支：** feat/baicao-duanhun-phase-b（worktree `.claude/worktrees/baicao-duanhun-phase-a2`·**未 push**）
**本会话 commit：** `43e98625`（失败结算）→ `27e6ac2f`（装载屏+provider）→ `98eaf026`（整备屏）→ `424b757e`（2 visual_route+seed）→ `dbc92c90`（PROGRESS/plan 收尾）

## 本次完成（承 plan Task C2.5·严格 TDD·C2 组最后一片）

1. **失败结算 §6.3**（`43e98625`）：`GauntletService.settleDefeat`（战败/认输离庄统一入口·单 writeTxn）——只发已击败精英经验给全体参战（含倒下者·层锁·`stages.take(currentStage-1)` 中 elite 计数·inBattle/interlude 两态统一）+ 按战末快照结轻/重伤（倒下者重伤·存活轻伤·镜像 expedition recall·不扣永久内力）+ 返还托管（已用不返）+ 关会话；装备/秘籍/领悟点/最终奖励全失·不记 clearedGauntletIds·幂等 no-op·awaitingRewardChoice 抛错。**[schema]** `BossGauntletConfig.eliteRewardExp`（可加占位 50≈first_clear 30% 的 1/6·真百分比拆分随 batch3-probe）。
2. **装载屏 §7.1**（`27e6ac2f`）：`gauntlet_providers.dart` 5 provider（镜像 expedition·nullable propagation + config watch 避竞态·+ `GauntletService.activeRun()` public）+ `gauntlet_loadout_screen`（帖库存/三关 Boss+推荐境界[真 config]/择人1-3/补给装载步进/持帖入庄）。
3. **整备屏 §7.2**（`98eaf026`）：`gauntletInterludeView` provider（成员名 Character 查表+生命/真气/阵亡/冷却+托管补给余量·非 interlude 返 null）+ `gauntlet_interlude_screen`（三成员 tile+补给使用+底部固定动作栏「继续/认输」恒可见[一屏不依赖滚动]·疗伤择目标框·认输先弹确认再 settleDefeat）。
4. **2 visual_route §12.4**（`424b757e`）：gauntlet_loadout/gauntlet_interlude 路由+host 分派+seed；seedTeamLineup 补清 `bossGauntletRuns`（幂等·`feedback_visual_capture_seed_idempotency`）。

## 已验证（本会话 worktree 实测）

- 严格 TDD **22 新测**（config2+failure9 / loadout5 / interlude4 / seed 冒烟2）。
- 全仓 `flutter analyze --no-pub` **0**；全量 `flutter test --no-pub` **4260 pass/0 fail**（4238→+22·改共享 schema/strings/seed 故跑全量·零回归）。

## 已知风险 / 延后（document-only·非偷懒）

- **UI 像素/水墨观感待白天真机 Codex 目检**（本片钉信息架构 + 2 route·plan line 178·非阻塞）：2 visual_route 已可孤立目检两屏。
- **战斗驱动全链导航 wiring 属后续切片**：loadout enter→逐关战斗（BattleScreen）→interlude→reward + 主菜单断魂庄入口。C2.5 只钉两屏 UI，未接战斗链与主菜单入口。
- **C2.4d 命名装备保护 + 断魂庄首破典故 延后**（依赖 Equipment 实例级 schema+saveVer bump·bg 夜跑不冒迁移风险）。
- 2 孤儿招（千钧坠岳/烛影摇红）仍未挂载；wave_b 配平红线自主判断待复核（承前）。

## 下一步建议

1. **战斗驱动全链导航 wiring**（loadout→战斗→interlude→reward + 主菜单断魂庄入口·opus xhigh·需真机验收）——C2 组前端最后一环，接线后断魂庄可端到端玩。
2. Codex 真机目检 gauntlet_loadout/gauntlet_interlude 两 visual_route（水墨观感/无溢出/文案）。
3. 复核 wave_b 配平红线 + 拍 2 孤儿招挂载去向（承前待办）。

## 踩坑提醒

- `settleDefeat` 精英经验用 additive `eliteRewardExp`（未改 C2.4 chooseReward 幂等路径·真百分比拆分 defer batch3-probe）。
- widget 测：滚动视口折叠线下的按钮 tap 前须 `ensureVisible`（否则裁剪区落空）；`gauntletServiceProvider` 在无 Isar 测中自然为 null → 屏内 `service==null` 旁路（认输确认框须在 service 检查**前**弹，否则测里不显）。
- seed 幂等：seedTeamLineup 不清 inventory → loadout seed 库存用**设定**非累加；bossGauntletRuns 须随 seedTeamLineup 清（多分辨率复跑）。
- PROGRESS.md 已达 100 行上限，下会话新增条目前先归档超期的 C2.1/C2.2 slice 条。
