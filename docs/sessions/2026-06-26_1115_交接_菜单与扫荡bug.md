# Session 交接 - 主菜单/启动 UX + 一键扫荡 hang bug

**时间：** 2026-06-26 11:15
**项目：** 挂机武侠
**分支：** worktree-level-difficulty-lootpreview（== origin/main，ff 同步）
**最后 commit：** 4ab7f85e

## 本次完成

详 PROGRESS.md 顶段「2026-06-26 第八阶段」（commit `b4ab7b02..4ab7f85e`，全 push）。
- 第八阶段 A-E 全实装（角色等级 Lv / 推荐境界难度 / 掉落悬停预览 / victory「晋 Lv」/ 稀有彩头）+ 实玩反馈轮（Lv 哨兵 bug 修 + 浮层字体/位置 polish）。
- 真机目检抓 1 真 bug 已修（Isar 不应用 Dart 默认值 → 旧档 level 哨兵）。

## 当前状态

1.0 长线打磨期。第八阶段功能闭环合 main。用户实玩中，发现 3 个待处理 UX/bug（下方）。

## 进行中的工作（移交新会话·均未动手）

1. **主菜单按钮大小不齐**（`lib/features/main_menu/presentation/main_menu.dart`）：左/中列按钮带缩略图+描述（高），右列下半（结缘楼/兵器谱/江湖商店/江湖见闻录/设置）纯文字紧凑（矮）。已有等高 helper（line 65-91 IntrinsicHeight+stretch·**仅同行等高，跨列不齐**）。修：统一所有菜单按钮高度/样式。
2. **启动跳过「江湖见闻」页**：流程 = SplashScreen → `HomeFeedScreen`（=江湖见闻 feed·`lib/features/home_feed/`）→「快速领取」→ MainMenu。用户要：过场→直接 MainMenu。修：`splash_screen.dart` `_go()`（line 80-87）push `MainMenu()` 替 `HomeFeedScreen()`（+import）。**注**：跳过后开局不再显离线摘要 feed（离线奖励本身 OfflinePassiveService 加载时已应用·不受影响）；feed 事件会留 unread 堆积，需评估是否别处入口。
3. **⚠️ 一键扫荡 hang bug（优先·功能阻塞）**：主线一键扫荡进入后卡「连播中 1/5」无限转圈，**停止按钮无效**。`lib/features/sweep/`（SweepScreen + SweepController）。上会话该功能「未实玩」，happy-path 浮现。战斗渲染卡在 preparing/loading 不前进，停止信号未打断 await/loading。新会话需：真机复现 → 查 SweepController 状态机 + SweepScreen 逐关托管战斗（强制 auto + `startFastForward`）→ 定位卡点。**疑点**：本会话改了 `battle_resolution.resolve`（加 E 稀有彩头 2 可选注入 + Lv wiring）——虽是 optional 参数理论不影响，但需排查扫荡战斗结算路径是否受波及。

## 已知问题

- 本地 main checkout 落后 origin（停在 61675c20）+ 被 `macos/*.pbxproj`/`xcworkspacedata` 旧构建产物噪音（非本会话产生·prior flutter build 残留）阻塞 ff。origin/main 是准的。新会话在主 checkout：`git checkout -- macos/ && git pull --rebase`。
- worktree `worktree-level-difficulty-lootpreview` 留存（已 ff-push 到 origin/main·可删或复用）。

## 重要决策

- 稀有彩头用全局机制（非逐关改掉落表）：DRY + 可拿不可装由 §5.3 境界锁兜底（用户拍板·见 PROGRESS）。

## 下一步建议

见下方新会话 prompt【下波候选】。扫荡 hang bug 优先（功能阻塞）。

## 踩坑提醒

- **工具调用链路曾被上下文污染**：本会话末出现把 `<invoke name="Bash">` 当文本打印（未真执行）的故障。新会话若复现 → /clear 或重启 Claude Code。
- Isar 加非空字段必带启动期回填/迁移（不应用 Dart 默认值·见 PROGRESS「真机 bug」+ memory `feedback_isar_pitfalls`）。
- 改代码首选 EnterWorktree（bg 写守卫）；macOS 构建/运行勿加 `DEVELOPER_DIR=`。
