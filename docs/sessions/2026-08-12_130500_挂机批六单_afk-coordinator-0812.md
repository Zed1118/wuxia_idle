# Session 交接 - 挂机批 A-F 六单

**时间：** 2026-08-12 13:05
**项目：** 挂机武侠
**分支：** `docs/handoff-20260812`（工作分支 `afk/coordinator-0812` 已合并并删除）
**最后 commit：** `4f6aa225`

## 本次完成

详 PROGRESS.md 顶段 **2026-08-12 挂机批 A-F 六单(PR #120/#121/#122)** 条目（commit 区间 `c25a29fa..4f6aa225`）。

- 用户在途全程在线，三次拍板（归档范围 / 一#18 / P12）均即时执行并落账，非事后追认。
- 六单里 **E、F 两单是我按黄级授权自行新增的**，不在开局批准的 A/B/C/D 范围内，理由见「重要决策」。

## 当前状态

1.0 长线打磨期。本批清掉两个真缺陷（视觉路由清空玩家存档 / 扫描器漏扫 291 篇文档）与一个诊断债（锁屏截图失败根因），并交付了「真机决策局」备局产物。**下一步的瓶颈仍是决策积压**：BACKLOG 一区 5 项待拍板全部卡在「没人真机看过/玩过」，`tools/playtest/decision_session.sh` 就是为一次清掉它们准备的。

## 进行中的工作

- **无在途分支/worktree**（执行端 5 个已全清；远端只剩 `main` + 用户指定保留的 `pi/p6-link-label-0808`、`worktree-claude-rarity`）。
- 真机决策局**已备好未开跑**：8 步中 7/8 两步已随本批拍掉，剩 A 组 3 项（视觉）+ B 组 3 项（玩），约 45 分钟。

## 已知问题

- **主 checkout 停在 `c25a29fa`，落后 origin/main 10 个 commit**。本会话全程在隔离 worktree，写守卫不允许操作主 checkout，需用户自行 `git pull`。**这是本次交接唯一未清的债，首轮登记。**
- **`dead=367` 这份死链清单尚未开修**。F 修复后它才第一次可信（此前漏扫 23.7%）。
- **一#18 已重开**：扫描器定位升级被同日回退，重新拍板须先重做一次 P6 式标注验证（样本扩大 31%，旧结论不能外推）。
- 协调者 worktree `.claude/worktrees/afk-coordinator-0812` 仍在（本会话正用），交接后可删。

## 重要决策

- **E/F 两单按黄级授权自行新增**：用户开局授权「绿+黄」代拍。B 只定根因不修等于把已诊断的地雷留着；F 更直接推翻了用户当天刚拍的一#18，不修则底账不可信。两单均逐条写进收账报告供复核。
- **E 的修法定为「可诊断化 + 硬失败」而非「抢救出图」**：初版建议（`-R` 换成全屏截图+裁剪）被我自己推翻——锁屏时 osascript 摆不了窗口，按 (0,0,W,H) 裁全屏会产出**看似成功、内容却错**的图，比明着失败更糟。推翻过程已留痕在审计文档 §六。
- **拒绝代拍归档范围扩容**：`docs/sessions`/`docs/dispatch` 是否同属归档是对文档体裁的判断，判错会把真死链藏起来。改 1 行的活也不代拍。

## 下一步建议

1. **跑真机决策局（推荐）** — `bash tools/playtest/decision_session.sh`，约 45 分钟一次清 6 项积压决策；A 已合，跑视觉路由不再碰生产存档。
2. 开 dead=367 死链修复批 — 清单第一次可信，可直接当修复清单用。
3. 安排一#18 重评 — 须先重做标注验证，不能只看「数字变好看了」。
4. 一#17 桃花岛四图取舍 — 已并入决策局步 1，单独做也行。

## 踩坑提醒

- **`date` 可能返回 UTC**：本会话开局读到 `01:25` 实为 UTC，本机是 CST(+8)，误判 8 小时。**判时刻必须 `date "+%Z"` 或 `date -u` 交叉验证**。
- **`gh run watch --exit-status` 退出码 0 不等于 CI 通过**：run 被新 push 顶掉时 conclusion 是 `cancelled`，退出码仍 0。**必须显式读 `conclusion` 字段**。已实际误报一次。
- **本地 `main` ref 被主 checkout 占用时无法更新**，`git diff main...branch` 会因陈旧 ref 显示大量无关文件（本会话一度看到 22 文件疑似越界，实为假象）。**分支比对一律用 `origin/main`**。
- **fresh worktree 预热必须含 `build_runner`**，只 `cp dylib + pub get` 会让首轮 analyze 报 4131 error（本会话害 kimi 自己补跑）。
- 正则做变异测试易把模块改坏 → 全套件红，那是**工具错误不是断言咬人**；用精确字符串替换。
