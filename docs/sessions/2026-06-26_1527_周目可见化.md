# Session 交接 - 周目可见化 + 扫荡/掉落 UX

**时间：** 2026-06-26 ~15:30
**项目：** 挂机武侠
**分支：** main（== origin/main，已同步）
**最后 commit：** 45f171ce

## 本次完成

详 PROGRESS.md 顶段「2026-06-26 续·周目可见化 5 改」（commit `b48e0629..45f171ce`，5 commits 全 push）。
- 用户实玩逐轮反馈驱动：扫荡周目标签 → 选择响应式+关卡按周目状态 → keepAlive 修选择丢失 → 删逐关战斗方式 chip+悬停卡右弹 → 悬停卡 IgnorePointer 修卡顿。
- 上会话三处修复（扫荡 hang/按钮等高/启动直达）本会话开局已确认 OK。

## 当前状态

1.0 长线打磨期。周目（NG+）系统的可见性/交互这次系统性补齐，用户确认「周目选择问题没了」。掉落悬停卡刚改完 IgnorePointer 待用户验丝滑度。

## 进行中的工作

- 本会话最后一改（悬停卡 IgnorePointer，`45f171ce`）debug app 已重启，用户尚未回报丝滑度验证结果。

## 已知问题

- 二周目实际差异小（敌人仅 +6%三围+御体词条，掉落与一周目完全相同）——用户拍板「先不动平衡」，已记 `docs/spec/playability_phase2_backlog.md` §十一待拍板。
- 群战/心魔/轻功 3 副本屏仍有逐关「战斗方式」chip（用户只点名主线+爬塔删，未动）；要否统一删待用户定。
- PROGRESS.md 114 行（>100），多为已压缩归档行，留作独立清理任务。
- 所有改动未真机视觉目检（headless 测已锁逻辑），debug app 跑起待用户确认手感。

## 重要决策

- 保持 §5.7 门槛（扫第 N 周目须先手工通该周目全部关卡）——用户拍板，灰显态只是把「为何不能扫」讲清楚，不放宽。
- 周目选择改 keepAlive 而非落盘——选定周目是会话级 UI 偏好，App 重启回默认（最高已通周目）合理。

## 下一步建议

1. （推荐）用户验收悬停卡丝滑度 + 周目全套视觉，确认后收口；不满意则换固定右侧预览面板方案。
2. 副本屏逐关战斗方式 chip 是否统一删（小改，待用户定）。
3. 周目平衡（backlog §十一）：加大二周目敌人难度/掉落回报——需 balance 调+真机校，用户拍板后做。

## 踩坑提醒

- 改 provider 保活性（autoDispose↔keepAlive）后，主 checkout 必重跑 `dart run build_runner build`（`.g.dart` gitignored 不随 merge 走），否则保活不生效。见 memory `feedback_wuxia_pen_build_runner`。
- 悬停浮层若向下盖住后续列表项且自身吃鼠标，会致「移到下一项切不动+边界卡顿」——浮层用 IgnorePointer 纯展示（见 `stage_preview_card.dart`）。
