# Session 交接 - 夜批收账

**时间：** 2026-09-17 20:05
**项目：** 挂机武侠
**分支：** codex/p2-player-flow-20260910（主 checkout；main `342d19275` 全程未动）
**最后 commit：** 29ddf974a

## 本次完成

详 PROGRESS.md 顶段「2026-09-17 当前候选」与「在途 worktree 台账」（commit 区间 `c307b3ffc..29ddf974a`）。
- 夜批 A（结构整改 4 目标）/ B（治理审计 4 目标）经独立 gate 后 `--no-ff` 入链；早报与拍板执行记录见 `docs/dispatch/reports/2026-09-17_night_report.md`。
- 兜底残留收口 `00f450cbe` 入链：`diff_3_or_more.attacker` 显式 1.0 + fail-fast；招降概率缺省统一读 numbers；数值零变化。
- 分支治理：29 条孤立/历史分支打 `archive/codex/*` 标签后删（28 标签在 origin）+ 64 条已进链分支删；worktree 38→14、本地分支 110→14。

## 当前状态

链 tip = 主 checkout HEAD = origin `29ddf974a`，脏文件仅 4 个用户文件。正式 M0–M9 仍 1/10；无在途执行端任务。

## 进行中的工作

- 无。夜批与收尾三项全部收账。

## 已知问题

- 6 个 ③ 代码类 worktree（`m2-d01` / `m5-r02` / `defense-break-posture` / `posture-wiring` / `b1-layer-sort-comment` / `defend-feedback-quick-gather`）09-16 台账判「可删」，已连续两轮挂账未拍板 → 本轮升级进 NEXT 候选。
- B-2 的 202 个 numbers.yaml 零引用 key 待「复核单」（5C），未开单。
- 证据目录留置：`~/Documents/Codex/2026-09-16/night-{A,B}/`、`2026-09-17/{residue,main-dirty-0915}/`（早报与收据引用）；A 的 3.8G SDK 副本已删。`2026-09-17/{bang,banw-g,gei-w,qin}` 非本会话产物未动。
- `flutter analyze` 全仓会报 `tools/phase0minus_probe/` 独立子包 1892 条（未 pub get），gate 口径为 `lib test`。
- gate `receipt_crosscheck` 要求 sha 加双引号，Codex 两次裸写触发 schema 错；下次派单包附 receipt 样例。

## 重要决策

- 决定 7A 的 8 条无 worktree 历史分支「归档标签后删」而非直删，因为现算各含 1–3 个独有补丁、三验不过。
- 决定 09-15 桌面 Codex 未提交治理笔记丢弃（补丁留底 `main-dirty-0915/governance-0915.patch` sha `045f743d…`），因为其「NOT_INTEGRATED」状态已被链上 PROGRESS/plan 后续记录取代。
- 决定夜批提前收官（授权 8h、执行 ~2h）不补派，因为 READY 待复核已达 WIP 上限（memory `feedback_wip_limit_over_executor_utilization`）。

## 下一步建议

1. （推荐）拍板 6 个 ③ 代码类 worktree 处置（归档标签后删 / 保留），已两轮挂账。
2. 开「B-2 零引用 key 复核单」：逐 key 查动态路径消费，产删除/头注清单，不改 yaml。
3. 集中真人试玩（01_01 + 黑风岭）报告，用户主导。
4. 主仓 `tools/phase0minus_probe` 子包 pub get 或从 analyze 范围排除，消除全仓 analyze 噪声。

## 踩坑提醒

- 协调者分支 `worktree-review-followup-20260912` 与链分叉（8 vs 30+ commit），不能在其上合并后推链；须从链 tip 开临时 worktree 集成（memory `feedback_codex_batch_merge_via_integration_worktree`）。
- `git branch -d` 按当前分支判「已合并」，对链上已合的分支会误拒；先 `merge-base --is-ancestor <b> <链 tip>` 验证再 `-D`。
- worktree 隔离守卫拒绝 `git -C`/heredoc/复合命令；写成 python 脚本文件用 subprocess 调 git 可过（memory `feedback_bg_session_write_guard_subagent_dev`）。
- `codex exec resume` 不继承 `--add-dir`，需 bypass-sandbox 时先 `chflags uchg` 锁真档 + sha256 前后核对（memory `feedback_codex_worktree_dispatch_sandbox`）。
