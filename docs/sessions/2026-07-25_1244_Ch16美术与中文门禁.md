# Session 交接 - Ch16 美术 11 图 + 中文散写门禁

**时间：** 2026-07-25 12:44
**项目：** 挂机武侠
**分支：** main（门禁批在 `zh-literal-gate`，PR #74 未合）
**最后 commit：** 34fa6a2e（PR #73 merge·已 push）

## 本次完成

详 PROGRESS.md 顶段 2026-07-25 Ch16 美术条（PR #72 `27b6d96d` + PR #73 `34fa6a2e`）。
PROGRESS 未展开的增量：门禁批独立成 PR #74（`455c3066`·未合）= AST 扫描器 + 双 guard
+ 31 条 allowlist 基线；派单书/返修单/双端日志/验证脚本留底 `build/dispatch/ch16_art_20260725/`（148K）。

## 当前状态

主线 16 章 80 关 + 全套美术已在 main，known_missing **全表清零**（Ch7 起首次）。宗师段 1/3 章落地。中文散写门禁已实装待合，合入后 CI 起挡新增硬编码文案。

## 进行中的工作

- **PR #74 中文门禁**：已推、待用户审后合并；合入后全量 4654→4656。
- allowlist 存量 31 条有意冻结为基线未清理（BACKLOG §二#3，附 A-E 五类判读）。

## 已知问题

- **CI 约 5-6.5% 无故变红**：稀有彩头（cycle=1 时 5%+1.5%）在固定掉落外额外发装备，打翻
  `sweep_settlement_test` 的 `equipmentDrops==1`；注入源 `stage_entry_flow.dart:826` 裸
  `DefaultRng()`（生产 inline new，测试 override 不到）。BACKLOG §一#8 已升级为实证代价项，详 memory `feedback_wuxia_sweep_rare_bonus_flaky_drop_count`。
- 11 张新 PNG 共 17M 真 PNG，待 webp 清账（沿 #63/#66/#70）；立绘未真机战斗屏目检。
- **留置**：worktree `ch16-art` + 分支 `zh-literal-gate` 保留（PR #74 未合，非孤儿）；留底目录为 gitignored，14M 预览图已清（可由 `verify_ch16_art.py` 重生）。

## 重要决策

- **门禁走 AST 不走行级正则**：诊断串大量多行拼接，行级扫描 312 行误报，AST 后降到 31；
  analyzer 9.0.0 已是 dev_dependency，零新增依赖。allowlist key 用「路径+片段」不用行号防漂移。
- **存量冻结不清**：门禁价值在于当天挡住新增，存量清理另开小批。
- **接关人返修**：撞脸源于我派单写「站姿像一个模子里出来的」，codex 连服色甲式一起复制；叙事要的是姿态呼应，不是素材层面两个角色分不出来。

## 下一步建议

1. **修 CI 随机红根因（推荐）** — BACKLOG §一#8，面小价值高，每个 PR 都被它浪费 22min CI。
2. 合 PR #74 + 拍板 §一#12（战报格式化片段是否列第 4 集中 sink）。
3. webp 清账小批（17M）。4. Ch17「沙海纵深」实装批（xhigh 专会话）。

## 踩坑提醒

- **codex 出图「N 图落盘 ≠ 完成」**：它会自我返修，等 bg 退出再接线。其自报 fraction 与 PIL 复测逐值吻合（差恒 1/1536 = 包围盒 inclusive 口径差），可作交叉验证。
- **analyzer 9.0.0 的 `NamedType` 是 `Token name` 不是 `name2`**，写 AST 工具前现查 pub-cache 源码。
- 立绘白边自检必须合成到**真实战斗底图**（memory `feedback_visual_check_real_target_bg`）。
