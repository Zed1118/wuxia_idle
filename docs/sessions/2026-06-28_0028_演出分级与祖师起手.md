# Session 交接 - 战斗演出分级 + 祖师起手回归学徒

**时间：** 2026-06-28 早
**项目：** 挂机武侠
**分支：** main
**最后 commit：** ec466fc1

## 本次完成

详 PROGRESS.md 顶段两条 2026-06-27（🌱 祖师起手回归学徒新手 / 🎬 战斗命中演出分级）。
- 演出分级：brainstorm(视觉伴侣)→spec→plan→subagent-driven 5 任务实装→merge。题字分级+命中特写镜头，纯表现层零 schema。
- 祖师起手：真机试玩发现新档祖师过强→诊断为 Demo fixture 漂移→用户拍 B 降学徒空手+放宽 T55→实装→27 测经 debug seed 解耦修复→merge。
- 两功能分别独立 worktree 实装，合并到含对方的 main 无冲突（改不同文件）。

## 当前状态

1.0 长线打磨期。两功能均已合并 local main（ahead origin/main 10，**本 handoff push**）。均**未真机目检手感**（演出峰值演出/祖师空手通关性）。

## 进行中的工作

- 无进行中切片。两功能代码+测试闭环。

## 已知问题

- **真机校值待跑**（spec 已挂账）：① 演出绝对值(题字字号/辉光/特写缩放/脉冲ms)+节奏ms ② 内力当平衡问题修(提消耗/降预算) ③ **祖师空手学徒能否过 stage_01_01**（不可过则早期难度需下调）。三项均 `flutter run -d macos` 边玩边调。
- 弟子(大/二)起手境界仍 Demo 高值——终局拜入匹配是另一平衡点，本次范围外单列。
- 早先 session doc `2026-06-27_2038_*` 有本会话补的「真机目检补记」(spec B 多存档目检)，随本 handoff 一并提交。

## 重要决策

- 祖师降学徒因 GDD Ch1-3 本意学徒→一流，Demo 高境界 fixture(week4_d)理由(展示队伍/避飞升锚点)已随弟子后移+飞升实装失效。
- 放宽 T55 因 GDD §6.1 师承遗物是「师父留给徒弟」传递机制非起手种子，飞升任选库存不依赖起手。
- debug seed 显式复现进阶祖师而非改测试断言：保测试意图、零断言改动。

## 下一步建议

1. （推荐）真机目检 `flutter run -d macos`：祖师空手过 01_01 + 演出峰值演出 + 多存档新档流程——纯代码测覆盖不到手感。
2. 三项真机校值（演出绝对值 / 内力平衡 / 早期难度）边玩边调 numbers.yaml。
3. 用户指定下一方向。

## 踩坑提醒

- **bg worktree baseRef=fresh 基于 origin/main 非 local main**：本会话两 worktree 都缺对方改动，merge 是真合并（幸改不同文件无冲突）。未 push 时尤其注意（memory `feedback_bg_worktree_baseref_fresh_diverge`）。
- **改 masters.yaml 祖师起手 → 27 个 battle/balance 测连锁**：debug seed `Phase2SeedService.seedMasterDisciple` 用 startingEquipmentIds 装备祖师，清空后 `equippedWeaponId!` 全崩。修法=seed 显式复现，非逐个改断言。
- git/worktree 操作对用户别做吓人的 rm（memory `feedback_git_worktree_explain_before_scary_ops`）。
