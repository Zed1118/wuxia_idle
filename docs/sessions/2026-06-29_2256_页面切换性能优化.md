# Session 交接 - 页面切换性能优化

**时间：** 2026-06-29 22:56
**项目：** 挂机武侠
**分支：** main
**最后 commit：** bb1d5031

## 本次完成

详 PROGRESS.md 顶段「2026-06-29 页面切换性能优化」（commit 区间 `282066c7..bb1d5031`，本 session 4 commit 全 push）。
- 起点是先做祖师创建页真机目检（PASS，数值核红线通过），用户随后提性能卡顿 → 转性能优化主线。
- 诊断走 /diagnose 纪律：profile 帧探针实测定位，非猜。

## 当前状态

1.0 长线打磨期。页面切换性能优化已闭环并 push。main 与 origin 同步，主 checkout 干净。

## 进行中的工作

- 无。性能优化主线已交付完成。

## 已知问题

- 兵器谱真机现场帧测量未取到（埋藏卷阁 hub 深层，UI 自动化导航没达）；按结构同款的心法图鉴实测结果（75ms→1帧）外推已修复。
- 全屏/大图主观目检未逐屏跑（cacheWidth 数学保证解码≥显示分辨率，不糊；心法图鉴 banner 已确认清晰）。
- worktree `founder-visual-check`（分支已合 main，冗余）：本 session 末 ExitWorktree 清理。
- `codex/verification-123678` 分支仍在（别会话的，其 2 个 WIP test 改动已按用户指示丢弃，分支未动）。

## 重要决策

- 测量驱动只在 ItemSlot 加 RepaintBoundary：心法图鉴 cacheWidth 单招即 75ms→1 帧（无 inventory 那种 30 重渐变格的持续 overdraw），不无据堆 RepaintBoundary。
- WuxiaImage auto-cacheWidth（量化 64px 桶）对全屏背景零副作用：cacheWidth≥源宽时 allowUpscaling=false 按源宽解码，不升采样不损画质。

## 下一步建议

1. （推荐）依赖维护轮试 riverpod 3.3.2+ —— 验 pausedActiveSubscriptionCount provider 断言是否消失（独立、低风险、与本次无关）。
2. founder_creation.yaml 补 school 唯一性校验（审查留的非阻塞健壮性项，~15min）。
3. 全屏/大图屏逐一真机目检 + 兵器谱 hub 现场补测（需 flutter run -d macos）。

## 踩坑提醒

- cacheWidth 使 Image provider 变 ResizeImage → 破 `is AssetImage`/`assetName` 断言（11 测文件）。用 `test/support/image_test_helpers.dart` 的 `assetNameOf` 穿透。详 memory `feedback_flutter_image_cachewidth_perf`。
- 驱动 macOS app：别用 frontmost（致窗口 bounds 漂移），读 CGWindow bounds；返回键 y≈0.05 非 0.03。详 memory `feedback_flutter_macos_drive_screenshot`（本次已更新）。
