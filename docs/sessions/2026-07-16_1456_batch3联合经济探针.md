# Session 交接 - batch3 联合经济探针

**时间：** 2026-07-16 14:56
**项目：** 挂机武侠（Flutter Desktop 写实武侠挂机）
**分支：** feat/baicao-duanhun-phase-b（worktree `baicao-duanhun-phase-a2`·**未 push·未合 main**）
**最后 commit：** `ad317356`

## 本次完成

详 PROGRESS.md 顶段「2026-07-16 江湖远行 batch3 联合经济探针批」（commit `f2611f29..ad317356`·本会话 4 commit）。
- 三 Task 严格 TDD：`joint_economy_model.dart` 八纯函数 → 三档探针+四不变式 ratchet → 拍板中档 wire。
- Lv100→170 totalExp=**15000**（实测）；+49 保底结晶 264 对齐 `enhancement_material_supply`。

## 当前状态

江湖远行 batch3 探针收口。`base_exp_per_battle=170` 已完整 wire（expeditions.yaml + ExpeditionConfig + settle）。Phase C 断魂庄未启动。分支共 13 commit（本会话 +4）全未 push。

## 进行中的工作

- 无进行中切片，三 Task 全绿完成。
- Phase C 断魂庄未启动（占位段待其校准）。

## 已知问题

- 探针产物 `test/tools/output/joint_economy_probe_2026-07-16.md`（gitignored·诊断可重生·留置）。
- 占位段 `gauntletWinRate`/`namedEquipAidContribution`/结晶日流入/herb→补给转化待 Phase C1.3 校准（非阻塞·探针标注在案）。
- 不变式③三路径取舍待 Phase C 补硬 ratchet（本批诚实不硬断言防假绿）。
- codex worktree `battle-ui-stage`（tip `f29ccef5`·活跃写）勿碰——非本会话债。

## 重要决策

- **完整 wire 而非只落初值**：`rewardsForNode` 消费点已存在（占位默认），与 `normalNodeMinutes` 同模式补齐 wire 消除 `TODO(batch3-probe)`，避免 unused yaml 字段。
- **中档 170**：Lv100→170 ~18天专挂上界（真实混合玩法更慢）；快10天易过快撞 cap17，慢30天偏拖。
- **占位段诚实不硬断言**：胜率/助炼 Phase C 前无真值，硬断言=假绿。

## 下一步建议

1. **（推荐）你 push feat/baicao-duanhun-phase-b**——13 commit 全未 push，push 是你的活。
2. Phase C 断魂庄（opus·xhigh·大/多会话）：填敌队/机制 + `gauntletWinRate` C1.3 校准 + 2 招正式挂载点（删 `mount_deferred=` 即发布）。
3. 可选：seed ≥Lv100 角色真机目检江湖远行入口。

## 踩坑提醒

- **base_exp_per_battle 三处一致 170**：`expeditions.yaml` + `ExpeditionConfig` 默认 + `rewardsForNode` 默认；下次调 exp 节奏改 `expeditions.yaml`（wire 已通）。
- **抬 cap reconcile** 沿用 memory `feedback_wuxia_release_cap_raise_reconcile`（本批未抬 cap）。
- **探针留 ratchet**：四不变式 + overflow worstJump≤7 守 Lv100→170 曲线，改经济数值破曲线即红。
- **中文文件名 heredoc 写入会静默失败**（本次踩坑）：session 记录用 Write 工具写，勿用 `cat > 中文名 <<EOF`。
