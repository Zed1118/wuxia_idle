# Session 交接 - UX 审查与缺口收口

**时间：** 2026-06-14
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 09ba2a60

## 本次完成

详 PROGRESS.md 顶段「续7 阶段性 UX 审查 + 7 项缺口收口」（commit 区间 `fa49cf52..09ba2a60`，6 功能 commit + 1 PROGRESS）。
- 审查走 Explore 8 维度 + Claude 红线过滤（剔除本地通知/云存档/FPS/教程弹窗等违 §5.1/§5.7），落盘 `docs/reviews/ux_audit_2026-06-14.md`（滚动维护，缺口状态表）。
- 7 项全纯表现层/状态层；H3 投降是唯一触状态机的（stage+tower 三态 wiring `({won,surrendered})`）。

## 当前状态

1.0 长线打磨期。战斗交互线 + 红线线 + 本波 7 项 UX 缺口全闭环，无半成品。审查报告剩 M2/M4/L1/L3 待办。

## 进行中的工作

- 无。剩余缺口见 `docs/reviews/ux_audit_2026-06-14.md` 滚动表 + 下一步。

## 已知问题

- PROGRESS.md 144 行超 100（CLAUDE.md 标）：本波只加续7 未归档旧条目（删超长中文行风险高）→ 建议单独归档清理（移 6-11/6-12 旧条目到末尾，git 有记录）。**已续传 1 轮**。
- M3 重试仅主线普通关；tower 楼层重试未做（tower 无「普通关」概念，战败仅记统计，无散功）。
- worktree `quit-and-splash-polish` 本 handoff 末 ExitWorktree 清理。

## 重要决策

- 投降语义（用户拍板）：即判负退出 = 普通关无惩罚 / Boss 不散功 / 无掉落 → 不走 onDefeat 散功分支，用三态区分。
- M3 用 loop 重构而非递归：避免重试重播 opening narrative。
- M2 离线汇总用户选「扩数据模型记金币/经验总额」→ 涉 Isar schema 迁移 + Phase 0 离线产出路径诊断，xhigh 单独立项。

## 下一步建议

1.（推荐）M4 属性术语气泡（纯 UI 轻量）— 内力/根骨/共鸣度 hover 释义，§5.7 框架下气泡非弹窗，改动小收益明确，本会话可延续。
2. M2 离线收益汇总（xhigh + 新会话）— 用户已拍板扩数据模型，涉 schema 迁移 + Phase 0，不同子系统。
3. PROGRESS 归档清理（轻量文档）。
4. L1 全屏（依赖 window_manager，引入前拍板）。

## 踩坑提醒

- bg 改代码走 EnterWorktree，但 fresh worktree 缺全部 `.g.dart`（46）+ libisar.dylib → 从主 checkout `rsync .g.dart` + `cp libisar.dylib` 才能 worktree 内 analyze/test（memory `feedback_fresh_worktree_libisar_dylib` / `feedback_wuxia_pen_build_runner`）。
- 改共享组件加 widget（WuxiaTitleBar 加 home 键）撞 InkWell 计数断言 → 改语义化（`byType(WuxiaInkButton)` 而非裸 InkWell，memory `feedback_red_line_test_semantics`）。
- runStageFlow 已有 `outcome`（VictoryResolution），新加 battle 结果用 `battleExit` 避免命名冲突（analyze 抓到）。
