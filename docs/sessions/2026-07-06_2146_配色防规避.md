# Session 交接 - 配色防规避（A1收尾 + Tier1 重命名 + Tier2 PanelSurface）

**时间：** 2026-07-06 (实测)
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 8a486a11

## 本次完成

详 PROGRESS.md 顶段三条（commit 区间 `6192e105..8a486a11`）：
- A1 收尾续（`87c8f465`）：浅底 StageProgressRow 金值统一 jiang（baike/cangjingge）+ 订正 session note 把 character_panel 当浅底的 drift。
- Tier1（`c9cad85a`）：Panel 重命名 `WuxiaPaperPanel→DarkParchmentPanel`/`PaperPanel→LightPaperPanel`，名字直接标底色深浅，消除命名坑。
- Tier2（`3af9c93a`）：`PanelSurface`(InheritedWidget) 三角色 + 两面板 provide + wuxia_ui/ 10 组件迁移 + 废 `StageProgressRow.nextEffectColor`。

## 当前状态

「浅底×同色文字低对比」整类问题**结构性根治**：Tier1 消命名坑 + Tier2 让组件按面板底色自动取文字色，跨底混用不可表达。代码闭环，**视觉实拍未做**。

## 进行中的工作

- 无（Tier2 代码闭环）。

## 已知问题

- **Tier2 视觉最终收口未做**：两面板下各迁移组件的对比度需起游戏实拍确认（非代码 gate）→ 归晚上目检工作流。
- paper_dialog 的 `InputDecorationTheme`(hint/label) 刻意保留硬编（恒浅底、值已正确、下沉重构零收益），非 bug。

## 重要决策

- 「自带固定底的组件」（error_fallback/wuxia_title_bar/paper_dialog/glossary_tip 气泡）读**自身**面板 surface（内层 Builder），不跟外层翻转——否则塞进反色面板即糊。
- plaque_button 判**无需迁移**：文字坐按钮自身 fill 上，非 bg-dependent。
- 当前用 `WuxiaUi.ink` 的组件=当前就在浅底，迁到 surface+light 兜底=现状零变，故无回归风险。

## 下一步建议

1. （推荐）晚上开挂机：Tier2 视觉实拍 + 任务3（tier 金值叠浅底排查）+ 夜批 B/C/D 目检——同类视觉批连做，先问挂多久按时长编排。
2. 新增 wuxia_ui 组件时用 `PanelSurface.of(context)`，别再硬编文字色（memory 已记规则）。

## 踩坑提醒

- 判面板底色看 fillColor 不看名字仍成立；新组件文字色规则见 memory `feedback_paper_vs_dark_text_color_palette`（Tier2 段已更新）。
- bg 会话 python heredoc 写中文文档撞 Non-UTF-8 源码错，需首行 `# coding: utf-8`；纯 `cat` heredoc 无此坑。
