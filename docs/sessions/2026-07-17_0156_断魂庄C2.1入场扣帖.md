# Session 交接 - 断魂庄 C2.1 入场扣帖+补给会话托管

**时间：** 2026-07-17 01:56
**项目：** 挂机武侠
**分支：** feat/baicao-duanhun-phase-b（worktree `.claude/worktrees/baicao-duanhun-phase-a2`·本地·**未 push**）
**最后 commit：** phase-b（本会话 +2：C2.1 代码 `b146e96a` + 本 plan/PROGRESS/session 进度 commit）

## 本次完成

断魂庄 Phase C **C2.1 入场扣帖 + 补给会话托管**（严格 TDD·承 plan `docs/superpowers/plans/2026-07-15-baicao-duanhun-phase-c-gauntlet.md`）。
- 新建 `lib/features/boss_gauntlet/application/gauntlet_service.dart` `GauntletService.enter({List<int> characterIds, Map<String,int> supplies=defId→份数, required int supplyCap})` 单 `writeTxn`。
- **校验**：pre-txn 纯输入(1-3人/去重/补给正数/总份≤supplyCap) + txn 内(save 存在/单 active `bossGauntletRuns.any(saveDataId==save.id)`/occupancy snapshot/逐角色 存在·非祖师·未占用·已修主修) + 断魂帖≥1 + 补给库存足量。
- **副作用**：扣一张 `item_duanhuntie` + 最多 3 份补给移入 `BossGauntletRun` escrow 三列表（`escrowItemDefIds/LoadedQty/UsedQty`·扣普通库存·usedQty=0）+ 建 active 会话（saveDataId/seed=save.id/currentStage=1/inBattle/成员保留 id 快照）。
- 任一校验抛 `StateError` → 单 `writeTxn` 回滚保原子（无帖/补给不足/回滚 3 测证）。
- **无 `now` 入参**：`BossGauntletRun` 无时间字段（stage-by-stage 交互，非离线时间结算，异于远征 `dispatch`）。

## 已验证（本会话 worktree 实测）

- `gauntlet_enter_test` **12/12**（happy path + 11 守卫）。
- `flutter analyze --no-pub` 全仓 **0 issues**（11.4s）。
- boss_gauntlet 全目录 **40/40**（28 旧 + 12 新）。
- **全量 `flutter test --no-pub` 4192 pass / 0 fail**（phase-b 首次全量基线·exit 0·无 -N 失败标记·含 battle-UI 合并后计数）。

## Phase 0 drift 订正（plan 路径过时）

- gauntlet 代码在 `lib/features/boss_gauntlet/`，**非** plan 写的 `lib/features/gauntlet/`。
- occupancy 在 `lib/features/activity/application/character_occupancy_service.dart`，**非** `character/application/`。

## 已知问题 / 待复核

- **范围边界（自主判定·供复核）**：补给「类型合规」（仅疗伤丹/行囊补给可托管）**未在 kernel 强校验**——escrow 机制 item-agnostic，类型由 UI-offer 层约束（C2.5），非 corruption（close 照常返还）。不认可可在 enter 加 ItemDef gauntlet-effect 校验。
- `seed = save.id`（save.id 恒 0·无 run serial·每关混 currentStage 归后续 combat drive 层，不在 enter 做）。
- phase-b 全未 push（含 C2.1 `b146e96a`）；用户口径「push=main」，需要时手动 push phase-b。
- 沿用待拍板：wave_b 配平红线排除副本奖励招（上会话自主判断·供复核）；2 孤儿招（千钧坠岳/烛影摇红）挂载去向（C2.4 前）。

## 下一步建议

1. 断魂庄 **C2.2 用药守恒**（庄内整备页用药消耗 escrow·不碰普通库存·§5.1；疗伤丹恢复 1 存活角色 30% maxHp / 行囊补给恢复全体存活 20% maxQi·仅关次间·不复活倒下者/不清持久伤势；close 时装入−已用原子返还）——Phase 0 待下会话摸（模板 = enter 的 escrow 反向消费 + 整备页状态机）。建议 xhigh。
2. 合并后战斗 UI 真机视觉验收（Codex 视觉流程·点按手感/水墨舞台/立绘）。
3. 复核 wave_b 配平 + 拍 2 孤儿招挂载去向（C2.4 前·人工）。

## 踩坑提醒

- 主 checkout / fresh worktree 引入新代码后先 `dart run build_runner build`（`.g.dart` gitignored）；本 worktree 非 fresh、.g.dart 齐备，C2.1 未触 schema 无需 build_runner。
- 本会话 cwd 是 home（`/Users/a10506`）非 worktree，`EnterWorktree` 报「非 git 仓库」失败——按 memory `feedback_bg_enterworktree_home_cwd_fallback` fallback：`git -C` + `cd` 前缀 + Write/Edit 落 worktree 绝对路径（写守卫不拦 `.claude/worktrees/`）。
