# Session 交接 - sect 立绘 wiring

**时间：** 2026-05-31 23:07
**项目：** 挂机武侠 (Flutter Desktop · Riverpod · Isar)
**分支：** main
**最后 commit：** ebc929a

## 本次完成

- **sect 立绘 portrait wiring 全闭环**(关闭 Codex `codex_visual_compress_portraits` B 段 FAIL「生产 UI 未接 portraitPath」)。
- **schema(方案A)**:`Character.portraitPath` String? 单一真相源 + `Character.create` 透传 + Isar saveVersion 0.14→0.15(`9254da2`);2 个版本断言测对齐 0.15(`883ddc7`)。
- **PortraitFrame 共享 widget**(`lib/shared/widgets/portrait_frame.dart` · `ef959c9`):统一 Container+Image.asset+errorBuilder,消 3 站点重复。
- **立绘写入两路径**(`210da37`):祖师/弟子 ← `MasterDef.portraitPath`(buildMasterCharacter) / sect NPC ← `SectCandidateDef.portraitPath`(runSectRecruitFlow)。
- **3 渲染站点接 PortraitFrame**:成员行 48(`d1bd365`)/ 确认 dialog 96(`58ccf94`)/ 强制招募 debug 列表 40(`0ad3f0d`)。
- **L3 验收基建**(`51aa0a7`):`VISUAL_ROUTE=sect_screen_npc` + `seedSectWithFullNpc`(祖师+6 NPC 满立绘)。
- **修空祖师 bug**(`62ab9d2`):seed 开头加 `_clearAll` 重建带立绘祖师 + legacy 短路回归测。
- **Codex 视觉验收全绿**:A 段(成员行 7 立绘)R1 FAIL→R2 PASS / B 段(dialog 96+debug 列表 40)R1 PASS。closeout 入库(`ebc929a`)。

## 当前状态

sect 立绘子系统代码+视觉双闭环,1627 测/0 analyze,全 push 同步 0/0。1.0 出版美术阶段推进中(主菜单/心法面板/7 阶 cover/9 NPC 立绘均已收口)。

## 进行中的工作

- 无进行中半成品。sect 立绘任务完整交付。

## 已知问题

- **follow-up(非阻塞)**:`recruitment_dialog.dart:268` 收徒师徒流程第 4 个立绘位仍用内联 Container+Image.asset,未迁 `PortraitFrame`(范围外,自带 errorBuilder,不影响功能)。

## 重要决策

- 决定**方案A(Character 加 portraitPath)** 而非方案B(存 candidateDefId 反查),因 B 同样要升 Isar schema(member row 只拿得到 Character),反而多一层间接 + fallback。
- 决定**全成员统一显**(祖师+弟子+NPC 都显立绘),因用户拍板;祖师立绘走 MasterDef、NPC 走 SectCandidateDef,统一落 Character.portraitPath。
- 决定**抽 PortraitFrame 共享 widget**(spec 内联体例的细化),因 3 新站点本会是 3 份相同代码;只用新代码,不动既有 lineage/recruitment 内联。
- 决定 **seed 用 `_clearAll` 重置**(沿其余 visual seed 体例),因真机 legacy 祖师(0.14 存档无 portraitPath)会让 ensureFoundingMasters 短路致立绘空。

## 下一步建议

1. (低)`recruitment_dialog.dart` 第 4 立绘位迁 `PortraitFrame` 统一收口(纯 cleanup)。
2. (中)其余出版美术 1.0 收尾项(按 `PUBLISHING_ART_PASS_1_0.md` / RELEASE_CHECKLIST 推进)。
3. 用户指定方向。

## 踩坑提醒

- **visual seed 必须先 `_clearAll`**:`seedSectWithFullNpc` 初版漏了,真机已存 legacy 祖师(0.14 存档·portraitPath=null)使 `ensureFoundingMasters` 短路(`if existing>0 return`),祖师立绘永空。单测在空 tempDir 跑故漏 → 回归测要预置 legacy founder 才能复现。
- **VISUAL_ROUTE 是编译期 dart-define + `VISUAL_ROUTE_READY` 走 debugPrint**:给 Codex 的验收 app 必须 `flutter build macos --debug --dart-define=VISUAL_ROUTE=<id>`(release 剥 debugPrint,且 define 编译期烧入)。
- **bg job + worktree**:Edit/Write 被 bg isolation 拦主仓 → 改主仓文件用 Bash(python/heredoc);代码改动进 worktree 用 Edit。
- **.g.dart gitignored**:fresh worktree/checkout 改 Isar schema 后必跑 `build_runner` + 拷 `libisar.dylib`(Isar 单测需要)。
- **Mac 本地 Codex 视觉验收**:本 session 全程 Mac 本地(非 Pen),截图在 `docs/handoff/codex_visual_sect_portrait_2026-05-31/`(PNG 按惯例未入库,仅 closeout.md 入库)。
