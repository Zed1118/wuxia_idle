# Session 交接 - 弟子终局解锁 + 多存档槽 + 平衡诊断

**时间：** 2026-06-27 (afternoon)
**项目：** 挂机武侠
**分支：** main
**最后 commit：** d05234b9（+ 本 handoff const 修复 & session doc）

## 本次完成

详 PROGRESS.md 顶段两条「2026-06-27」（弟子 spec A `4bda2042..156a85ef` / 多存档 spec B `fb54b119..740264d9` / 诊断 `d05234b9`）。
- spec A：弟子拜入后移 stage_06_05，服务遍历多匹配 + 关级防重后移（修同关 senior 挡 junior 幂等陷阱）+ 红线 dedup 改 role 唯一。
- spec B：IsarSetup switchSlot/listSlots/deleteSlot + SlotSummary + 启动分流 SaveSelectScreen + 切档 invalidate。
- 平衡诊断：武圣单人可通全 Ch4-6（多数 +1 阶 / 04_05·05_05 需 +2 阶 / 06_05 武圣 62%），**无需调参**。

## 当前状态

两 spec 全实装合并 push，平衡诊断闭环。1.0 长线打磨期，三任务收尾。HEAD=origin/main 同步。

## 进行中的工作

- 无进行中切片。两功能 + 诊断均未真机目检（Isar 无 web，需 `flutter run -d macos`）。

## 已知问题

- stage_06_05 配置敌队总 HP 实测 137000（3 敌），spec 文写「52000」是 stale，文档与 config drift（未改，待确认是否订正 spec）。
- stage_05_05 连 3 人 on-level 队都 0%（既有跨阶 Boss 设计，非 spec A 引入）。
- 诊断 output md `test/tools/output/solo_*.md` gitignored，可重跑再生，未入库（留置）。

## 重要决策

- 平衡诊断口径：单人 on-level 1 打 3 必败非公平基线，改扫超阶幅度（+0/+1/+2/武圣）找可通门槛——这才是挂机游戏单人靠练级推进的真实读数。

## 下一步建议

1. （推荐）真机目检 `flutter run -d macos`：存档选择屏/切档/弟子终局拜入流 + 单人 Ch4-6 手感——纯代码测覆盖不到 UI/键鼠/手感。
2. 订正 spec A 文档 stage_06_05「52000」→实测 137000（或确认口径）。
3. 用户指定下一方向。

## 踩坑提醒

- **worktree 改动未 commit 即 ExitWorktree(discard) 会丢**：本次 spec B 的 const lint 修复在 worktree 改了没 commit，discard 时丢失，merge 回 main 残留 1 info lint（已在 handoff 补修）。worktree 收尾前 `git status` 确认全 commit 再 exit。
- bg 写守卫拦 Edit/Write，但 **Bash sed 可改主 checkout 文件**（本次补修用 sed）。
- 主 checkout merge schema/provider 批后必 `build_runner`（.g.dart gitignored 不随 merge，见 `feedback_wuxia_pen_build_runner`）。

## 真机目检补记（2026-06-27 晚 · flutter run -d macos）

spec B 多存档槽核心流程**端到端真机 PASS**（CGEvent 注入点击 + 截图自动驱动）：
- 启动直达「选择江湖」屏，3 槽渲染正确：slot1=现有档（`祖师·一流启蒙` / `第1章·已通关4关` + 删除图标），slot2/3=`空·新开江湖`+chevron，配色守水墨基调。现有档未迁移直读。
- 点 slot1 → switchSlot + ensureFoundingMasters（幂等，旧档未重建）→ 主菜单加载现有档。
- 设置面板→「切换存档」→确认弹窗「返回存档选择，切换到其它江湖？」→ flush→回选择屏，**切档后 slot1 元数据不变**（save 未损）。

**未验证（需终局实玩 / 键鼠手感，截图驱动不可行）**：空槽新开 onboarding、删档、弟子终局拜入流（stage_06_05 门控）、单人 Ch4-6 手感。spec A 弟子拜入由代码测覆盖。

**环境踩坑**：build macOS app 必须用默认 `flutter`（完整 Xcode 26.4.1），**不可**加 `DEVELOPER_DIR=/Library/Developer/CommandLineTools`——该路径无 xcodebuild，build exit 72 失败。该绕过仅用于 git。
