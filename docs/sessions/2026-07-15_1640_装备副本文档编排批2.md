# Session 交接 - 装备副本文档编排批2（Phase A1/A2 计划生成）

**时间：** 2026-07-15 16:40
**项目：** 挂机武侠
**分支：** `docs/equip-baicao-orchestration`（worktree；未 push、未 merge 回 main）
**最后 commit：** `a971a921`（批2 计划）

## 本次完成（批2 = Phase A1/A2 计划生成 · 纯文档零代码）

- **Phase A1 计划**（`plans/2026-07-15-baicao-duanhun-phase-a1-persistence.md`，812 行）：SaveData 6 永久进度字段（Q3）/ `ExpeditionRun`+`BossGauntletRun` 两会话 collection + `ActivityMemberSnapshot` embedded / saveVersion 0.36→**0.37** 可加性迁移 / `CharacterOccupancyService`+`ActivityOccupancy` DTO（Q5）。7 任务 TDD。
- **Phase A2 计划**（`plans/2026-07-15-baicao-duanhun-phase-a2-cap-config.md`，670 行）：发布上限绝对层 **10→17** + 测试断言同步 / 存量溢出连跳探针（§3.1）/ 断魂帖 `ItemType.ticket` / 两补给副本内效果字段 / `expeditions.yaml`+`boss_gauntlets.yaml` 加载与 fail-fast 校验骨架。6 任务 TDD。
- companion §9 §4.1 行标「批2 已产」+ 挂两文件名。

## 冻结面（下游依赖，不得改名/改签名）

occupancy/reserved DTO（`reservedEquipmentIds: Set<int>` 对齐装备 plan）· SaveData 永久进度 6 字段 · `GauntletPhase.awaitingRewardChoice` + 托管三列表 · schema 0.37 + 两 collection · 发布上限 17 · 断魂帖/副本内效果字段。**装备批 + Phase B/C 均在此契约上建。**

## 质量口径

纯文档零代码 → 未跑 test/analyze（实装批的事，计划里 test 命令列全）。验证 = 计划每个 file:line 锚点本会话 grep 实测真实代码（save_data:92 / isar_setup:82,161,368 / character:69-112 / item_def:39-58 / enums:337 / numbers:206 / progression_release_cap[1,49] / applyExperience 签名 / game_repository:172,415,512 / inner_force_qi_migration_test:57）。两计划均带冻结产物表 + 写完 vs 源规格自检。

## 进行中：批3（本会话续开）

B1/B2/C1/C2 实施计划 + 联合经济探针（§4.5 扩展 §12.4 单系统为跨系统 + ratchet）+ 审查报告（矛盾/决策/顺序/待拍板）。依赖批2 冻结面（已就位）。B 与 C 相互独立（都只依赖 A）。

## 踩坑提醒（供实装批）

- 实装批 fresh worktree 先 `pub get`+`build_runner`（新 collection/SaveData 字段/embedded 需 codegen）。
- 装备批推翻永不掉级：先拆 `enhancement_service.dart:97` `neverDegrade` throw（否则配置一改即崩）。
- 加 `ItemType.ticket` 必查穷尽 switch（`default` 静默吞，memory `feedback_enum_fromdefid_default_swallow`）。
- 发布上限 bump 已在 A2 Task1 同步测试断言；`inner_force_qi_migration_test:57` 版本断言在 A1 Task5 同步。

## 并行提醒

Codex 战斗 UI 在 `.worktrees/battle-ui-stage`（`codex/battle-ui-stage`）并行；本批文档与其零文件重叠。共享热点 `lib/shared/strings.dart` 由实装批注意（详 `2026-07-15_1610_codex战斗界面环境整备.md`）。

## 环境

main（主 checkout）`39947967` 已收口 origin/main（behind 0）、ahead 8 未 push（push 是用户的活）。Claude 分支 3 会话未 push（批1 `9e415fe6` / 记录 `5c19b7e7` / 批2 `a971a921`）。
