# Session 交接 - 视觉验收工具链诊断

**时间：** 2026-07-05 22:44
**项目：** 挂机武侠
**分支：** main
**最后 commit：** f95925dc

## 本次完成

详 PROGRESS.md 顶段「2026-07-05 视觉验收工具链诊断」条（commit `f95925dc`，纯诊断+文档，零代码改动）。
- 分层探针法定位：CGWindowList 枚举 / screencapture 屏录权限 / Xcode / flutter 逐层验证全正常后才复现，避免把环境问题误判成代码问题。
- 复现即收口：规范调用一次通过，顺带产出 C 批次战斗屏静态帧零回归实证（battle_charge_break 三环+banner+技能栏全渲染 PASS）。

## 当前状态

BattleScreen 拆分三批（B/C/工具链诊断）全部收口合 main 已 push。视觉验收工具链确认可用（07-03 以来零改动、main 实测通过）。C 批次唯一余项=5 条时钟边沿动态交互，只能用户真机 playtest。

## 进行中的工作

无。无在途 worktree/分支。

## 已知问题

- `build/visual_acceptance/diag_20260705/`（4.1M，gitignored）留置：本批静态帧 PASS 的证据帧，用户目检后可删。
- 5 条时钟边沿交互（暂停中 hit-stop/暂停中改速/hit-stop 中切快进/待发中战斗结束/软暂停即时冻结）待用户 playtest，静态帧证不了。

## 重要决策

- 「工具链修复」判定为**无需修复**：故障是调用形态 drift（plan 文档写错命令行），不是代码缺陷，修文档/memory 而非改脚本。
- 未给 visual_capture.sh 加 VISUAL_ROUTE env var fallback 兼容——诊断批不夹带未拍板的接口改动；若用户想要可 5min 加。

## 下一步建议

1. （推荐·用户）真机 playtest 收口 5 条时钟边沿 + 常规手感——C 批次最后余项，工具链已证可用。
2. 材料来源反查（backlog 新功能，需 design-first 拍板，spec 30-60min + impl）。
3. 早期难度 balance 特征化（零风险只读，20-30min，量化验 stage 3 关下调）。

## 踩坑提醒

- **别照抄 plan/handoff 文档里的验收命令行**：C plan Step6 的 `VISUAL_ROUTE=<id> bash tool/visual_capture.sh` 路径+传参双错。合同=`tools/visual_capture/visual_capture.sh --route <id>`（VISUAL_ROUTE 是编译期 dart-define，env var 无效）。详 memory `feedback_flutter_macos_drive_screenshot`（本次已补）。
- `window_id.swift` 返 `BEST=-1` ≠ 权限坏，=app 没在跑；先 pgrep+空 filter 枚举探针分层定位。
- main 曾丢 upstream tracking（疑 filter-repo 副作用），已 `--set-upstream-to=origin/main` 修复；若他处 clone 遇 `git pull` 报 no tracking 同法处理。
