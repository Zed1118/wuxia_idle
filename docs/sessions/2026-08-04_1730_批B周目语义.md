# Session 交接 - 批B周目语义

**时间：** 2026-08-04 17:30
**项目：** 挂机武侠
**分支：** worktree-tower-batch-b（PR #117 draft，未合）
**最后 commit：** `6045b3aa`（format 修复；主体 `075b5346`/`ae0dbe52`/`d9c86af0`）

## 本次完成

详 PROGRESS.md 顶段「2026-08-04 批 B 周目语义修正」条目（分支版，未进 main）。
- 会话前半：PR #115（批 A 主体）独立复核后合入 + #116 账本落账（均 MERGED，main `c1838d41`）
- 会话后半：批 B B1-B6 一次做全 + 净威胁实测 ×5.11 + PR #117 draft
- 三项拍板：周目体例=主线式自由回选 / 高周目奖励本批一并做（×0.25/周目）/ cap 3+margin 1；
  远征追加拍板：无终点故解锁绑深度里程碑（≥20/40）

## 当前状态

塔 49 层（批 A）已全进 main；批 B（4 支线入口境界段推进）完成在 PR #117 draft 上，
CI 首跑 format 门禁红已修复重跑中。批 C（8 张新 Boss 立绘 + 视觉 smoke）未开工。

## 进行中的工作

- PR #117 待 CI 绿 + §8.2 Gate 评审合并（本会话产的批 B 全部工作在此分支）

## 已知问题

- 批 B 残留 3 条见 PR #117 body §5（UI 无新增 widget 测/初值待真机校/cycle3 词条沿用 mainline 表）
- 8 新 Boss 立绘占位（批 A 传递）与塔 49 层+周目 UI 视觉 smoke → 批 C
- worktree 目录名 `tower-batch-a` 挂着 batch-b 分支（历史沿用，不影响功能）

## 重要决策

- 决定远征解锁绑深度里程碑而非「通关」，因摸底证实远征无终点无通关概念（按时间无限推进），
  「通了 N-1 解锁 N」结构上无落点；挂到 20/40 深 = 挂机资格线，体例与其余入口统一
- 决定 `ExpeditionCombat.fight` 的 cycleIndex 参数用 required 而非默认值 1，
  因 runner 文件受「零数字字面量」红线测约束（剥注释后禁裸数字）
- 决定净威胁探针改 calculator 单发交换比，因整场承伤口径被自证零负载否决（满配 2.3 tick
  秒杀敌无出手机会，c1=c2=0 的 ratio=∞ 是假绿）

## 下一步建议

1. **评审合并 PR #117**（推荐）—— CI 绿后按 §8.2 Gate 独立复核合入，批 B 落账；批 C 以它为地基
2. 批 C：8 张新 Boss 立绘（codex image_gen 派单）+ 塔 49 层与支线周目 UI 1280×720 smoke
3. 远征里程碑 [20,40] / 奖励系数 0.25 真机校（非急，初值保守）

## 踩坑提醒

- **脚本/heredoc 批量编辑过的 dart 文件必过 `dart format` 再 commit**——本批 9 文件 CI format
  门禁红（批 A 同款第二次踩），本地 test/analyze 全绿不代表 format 干净
- 断魂庄/远征 fake-combat 轻量测不加载 GameRepository：结算路径读 config 必须 cycle≤1 短路
  （memory `feedback_battle_result_path_config_read_crashes_light_test` 同模式，本批实锤一次）
- RealmTier 枚举序：xueTu/sanLiu/erLiu/yiLiu/jueDing/zongShi/wuSheng——sanLiu+3=jueDing
  非 yiLiu，写门槛断言先数枚举（本批测试写错一次被实现纠正）
