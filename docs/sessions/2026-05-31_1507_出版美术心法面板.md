# Session 交接 - 出版美术心法面板

**时间：** 2026-05-31 15:07
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 6bad7a9

## 本次完成

- **出版美术 Phase B 心法面板全收口 + 主菜单门面 bg**（10 commits `b147b49`→`6bad7a9`，全 push，1612 测/0 analyze）：
  - **B1** 抽 `WuxiaPaperPanel` 宣纸底共用组件（`lib/shared/widgets/wuxia_paper_panel.dart`）+ 应用心法面板 body
  - **B3/B4/B5** 秘籍质感整合（`technique_panel_screen.dart`）：`_SealBadge`(seal_red 印章叠层名)/ink_divider 分隔/tile 半透 + 主修 hero(meditation 打坐+内丹金光)+ `_LayerLadder` 9 层段位阶梯(金徽章三态)
  - **B2** 7 阶 cover 卷轴 banner 落地（`assets/techniques/tier_*.png`，书法卷轴·装帧梯度 素纸→金框）+ cover wire 改 `AspectRatio` 完整呈现
  - **主菜单门面 bg** `assets/ui/mountain_bg.png` 换青墨山门水墨远景（替占位）
- **2 个 Codex WARN 治本**：深色空底（`WuxiaPaperPanel` 加 `StackFit.expand`，真因=Stack shrink-wrap 没撑满 body 露 Scaffold 冷 background）+ seal 印章收敛(48→44+向内收)+ 暖兜底色 `WuxiaColors.paperUnderlay`
- **debug 菜单返回修复**：`BattleTestMenu`(battle_test_menu.dart Phase1 入口)补 AppBar 返回箭头
- **返回按钮全库审计**：grep 全扫 34 个整页 Scaffold，正式页(玩家可见)100% 有返回入口 0 bug，仅 debug BattleTestMenu 缺(已修)
- **3 轮 Codex 视觉验收闭环**(Mac 本地)：r1 6PASS/2WARN → r2 深色空底 FAIL → r3 终 PASS · 截图+closeout 在 `docs/handoff/b_cover_visual_2026-05-31{,_r2,_r3}/`
- **工作流 memory 沉淀** 3 条：MJ 批量 prompt 纯文本空行分隔 / Mac Codex 视觉验收派单规范 / 视觉验收默认派 Codex 用户只拍方向

## 当前状态

出版美术 Phase A(主菜单)+ B(心法面板+cover)全收口，Codex 3 轮验收终 PASS，1612 测/0 analyze，HEAD `6bad7a9` 与 origin/main 同步，工作树干净(仅 docs/handoff 截图+macos build 产物未跟踪)。

## 进行中的工作

- 无半成品（B 段全收口，每步 commit+验收闭环）。
- 出版美术后续屏(战斗屏/装备详情屏/章节屏)未起，沿 B 段体例(WuxiaPaperPanel+卷轴+印章)。

## 已知问题

- `assets/techniques/` 7 张 cover 共 16M(单张 ~2.3M)未压缩，仓库偏大，建议 oxipng/pngquant 瘦身。
- cover 只在 1 个 tier 的角色面板显 1 张(seedRefineInsight 单 tier)，想验全 7 阶梯度需多 tier 的 visual seed(工程基建项)。
- seal 印章 seal_red.png 白底块在深 tile 上略浮(Codex 判可接受，非阻塞)。
- docs/handoff 各轮截图未 commit(跟随项目惯例留本地，Mac 本地不丢)。

## 重要决策

- 决定 cover 走「书法卷轴·装帧梯度」(非武学意象/山水)因贴样图 03 秘籍感，梯度压装帧(素纸→织锦→金框)。
- 决定 cover wire 用 `AspectRatio` 完整呈现(maxWidth 480)因 height60 `BoxFit.cover` 会裁掉梯度所在的卷轴边框。
- 决定深色空底用 `StackFit.expand` 治本(让宣纸铺满 body)因根因是 Stack shrink-wrap 露 Scaffold 冷底，改兜底色无效。
- 决定视觉验收默认派 Codex + 我读截图复核，用户只拍方向不拍像素(用户看不出视觉细节)。
- 决定第一批 cover(无梯度书法 + autojourney 把文件名当 prompt 出杂图)弃用重跑，不将就。

## 下一步建议

1. **工程基建** `VISUAL_ROUTE` + visual seed(含 technique_panel_b 多 tier)+ `visual_capture.sh`(opus xhigh，验收提速+补 cover 多 tier 验收缺口)。
2. **出版美术下一屏切片**(战斗屏/装备详情屏/章节屏，沿 B 段体例)。
3. **techniques 资产压缩** 16M→~4M(无损 oxipng，低风险)。
4. **角色立绘 backlog 9 张** prompt(sect/recruit，需逐个 lore 定制+出图)。

## 踩坑提醒

- **改代码用 Bash python/带引号 heredoc 直写 main**(Edit/Write 被 bg isolation guard 拦)，assert count + git diff + flutter test 核验，git add 显式文件不用 -A。
- **加 AppBar 撞 widget 测**：AppBar title 与 body 标题同文案 → find.text 从 1 变 2，findsOneWidget 挂(battle_test_menu 踩过，AppBar 留空标题避开)。
- **Stack 包可滚动内容默认 shrink-wrap 不撑满**，露 Scaffold 背景 → 必须 `StackFit.expand`；改兜底色治标无效。
- **MJ autojourney 会把"目标文件名"当 prompt 跑**(出楼阁/人物杂图)→ 必须喂完整英文 prompt；文件名截断后仅 UUID 区分，挑图靠看内容辨梯度。
- **视觉验收派 Codex 用已编译 app**(build macos --debug + cp ~/Desktop/visual_builds/wuxia_idle_<sha>.app + pkill 关旧 + 不让 checkout/build)，规范见 `docs/handoff/codex_visual_acceptance_prompt_guide_2026-05-31.md`。
