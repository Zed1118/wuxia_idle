# Session 交接 - 江湖远行 Phase A2 基建批

**时间：** 2026-07-15 22:19
**项目：** 挂机武侠
**分支：** feat/baicao-duanhun-phase-a2（worktree·**未 push/未合 main**）
**最后 commit：** e5e39de7

## 本次完成

详 PROGRESS.md 顶段 2026-07-15 Phase A2 条目（commit 区间 `c14f4b15..3830a80b` + 记录 `e5e39de7`）。
- Tasks 2/3/4/5（cap 无关基建）严格 TDD 落地并 commit；**Task 1（cap 10→17）阻塞待用户拍板**。

## 当前状态

江湖远行 A2 基建落在独立分支：断魂帖 `ItemType.ticket` / 补给副本内效果字段 / 远征·断魂庄配置类+骨架+`loadAllDefs` 校验 / 溢出探针（判定一次性兑现，回填 design §3.1）。均 cap 无关，全绿。cap 抬升本身阻塞。

## 进行中的工作

- **Task 1 发布上限 10→17：阻塞。** cap→17 使 `releaseSkillTierCap` 二流化，触发 `game_repository._enforceSkillSourceRedLines`（波B红线⑦）要求二流招挂载；2 招孤儿（`skill_qian_jun_zhui_yue`/`skill_zhu_ying_yao_hong`）致 `loadAllDefs` 抛错。wave-B 挂载点（stage_03_05 灰衣人/tower f15）被 Codex Lv100 批 re-tier 到学徒，重挂属 B/C 内容工作。待拍 phasing（推荐 defer B/C）。

## 已知问题

- 分支**未 push**：基于本地 main 24 未 push commit，push 会连带上传用户 work（「push 是用户的活」）。留用户处置。
- worktree `equip-baicao-orchestration`（docs·已合 main）**第 2 轮挂账**——A1 handoff 曾说「下轮若仍在则强制处置」，本 bg 会话不擅删用户 worktree，**升级请用户** `git worktree remove`。
- worktree `.worktrees/battle-ui-stage`（codex 活跃·本会话 tip `2063c305`→`1c604bcf`）**勿动**。

## 重要决策

- Task 1 从「一行 yaml」翻为「阻塞待拍板」：releaseSkillTierCap 二流化触发内容完整性红线，非计划预期。溢出探针（T2）与 cap 抬升时机解耦，已独立出「一次性兑现」结论。
- items.yaml 断魂帖用 `type: ticket`（非计划的 miscMaterial）：`ItemDef.type` 即 `ItemType`，单真相源对齐 `fromDefId`。

## 下一步建议

1. **（推荐）** 用户拍 Task 1 phasing → defer B/C 则直接合 Tasks 2-5（全绿基建）。
2. 若坚持现在 cap→17：需内容级重排（re-tier 敌人回二流 + 挂 2 招），属 B/C 范畴。
3. Phase B/C（远征编排 / 爬 Boss 循环）：各依赖 A1+A2 契约，可并行 worktree。

## 踩坑提醒

- 进度/常量 cap bump 前先 grep 红线层（`game_repository._enforce*`）：发布上限这类 cap 会把更高阶内容拉进完整性红线，非一行改（本批实证·波B红线⑦）。
- bg 会话分支须基于本地 HEAD 建 worktree（非 EnterWorktree 默认 origin），否则丢 A1 的 24 未 push commit（memory `feedback_bg_worktree_baseref_fresh_diverge`）。
