# Session 交接 - 终局机制型 Boss 批次4「周目脆弱窗口收窄」

**时间：** 2026-07-04 00:50
**项目：** 挂机武侠
**分支：** worktree `worktree-endgame-boss-batch4-cycle-vuln`（待合 main）
**基线：** main = origin/main = `33de4559`（本 worktree 6 commit `93f4f5b3..63bbb034`）

## 本次完成

终局机制型 Boss 批次4：爬塔机制型 Boss（floor25/30）脆弱窗口随二周目变严，满配 NG+ 加压，一周目手感逐字节不变。详 PROGRESS.md 顶段。

- **方案A（per-cycle 映射，用户拍板）**：`EnemyDef.cycleVulnerability: Map<int,BossVulnerabilityDef>` + resolver `vulnerabilityForCycle(cycleIndex)`（镜像 `bossPhasesForCycle`）+ 跨字段校验（cycleVulnerability 非空⟹必有 base vulnerability）。
- **plumbing 一行**：`stage_battle_setup.dart:519` 灌 vulnerabilityMult 改走 resolver（cycleIndex 已在作用域）。
- **校准定稿（诊断驱动）**：floor25 0.10→0.06 / floor30 0.20→0.12（plan 起点值一次过）。

## 当前状态

Subagent-Driven 5 Task TDD 全绿（每 Task spec+quality 双审，Task3+4 校准原子合并）。analyze lib/ test/ **0** · 全量 `flutter test --no-pub` **3647 pass/1 skip/0 fail**（基线 3629→+18 新测·零回归·worktree 实测）。

## 关键发现（A/B 隔离证明）

诊断 A/B 臂（同 cycle-2 敌队，boss vuln treatment vs copyWith 还 base）显示：floor25 control 臂=6.4 反比 cycle-1 的 6.6 **略快**——即 cycle-2 的 +10%stat/相位提前对满配其实略有利，**全部加压由 vuln 收窄独立扛**（floor25 +1.65 / floor30 +1.4 tick）。A/B 设计避免了「误以为 +10% 在贡献」。

## 已知风险 / 限制

- 满配终局战仅 ~4-8 tick，vuln 杠杆是**温和加压非硬门槛**（符合 §5.4「不膨胀数字」非目标，守软线，不追陡度）。
- 爬塔仅 2 周目（`numbers.yaml max_cycle_tower: 2`），故本批杠杆只作用 cycle 2。若要周目纵深需另议提 max_cycle_tower（独立 scope，未并入本批）。

## 红线合规

减伤方向 · schema 有界 [0.05,1.0] · 非属性 buff · 守 v1.30 §5.4 机制型 Boss 例外（已含「周目进化后」）· **GDD/CLAUDE 无需再改**。

## 下一步

1. **合并**：待用户拍板，按 batch1/2 惯例走 squash PR。批次4 零表现层改动（纯数值/plumbing），视觉验收 N/A。
2. **批次3 心魔**（终局机制型 Boss 剩此一批）：相位开窗 + 终关模块B 限时生存，改 isFinished 回归面大，可 defer，opus xhigh。

## 提交链

```
63bbb034 test: cycle-2 脆弱窗口红线边界断言
e2b64db2 [balance] floor25/30 二周目收窄脆弱窗口 + cycle-2 加压诊断
008cdaf1 feat: vulnerabilityMult 按周目取 resolver + schema 红绿测
18823b78 feat: EnemyDef 加 cycleVulnerability 字段 + vulnerabilityForCycle resolver
ef3a3844 docs: 批次4 实装 plan
93f4f5b3 docs: 批次4 设计 spec
```
