# Session 交接 - 藏经阁2.0 武学收录图鉴(P4 子项6 · 收官)

**时间：** 2026-06-22
**项目：** 挂机武侠
**分支：** worktree-martial-codex → 合 main
**实装 commit 范围：** `14cb6abf`(design)..`f7a1e7f6`(Task9) + PROGRESS/closeout

## 本次完成

详 PROGRESS.md 顶段「续41」。江湖见闻录第5 tab「武学」——205招武学典籍账号级收录图鉴(点亮+剪影藏名+收录进度+详情屏),纯派生展示层。**P4 长期档案全6子项达成**(战绩册/兵器谱/材料经济/门派谱1.1/奇遇录/藏经阁2.0)。

- brainstorm 4 个范围决策拍板(收录205/账号级视角/来源分5组/纯复用description) + 1 个实装中发现的破招点亮口径(按派)
- 10 task subagent-driven TDD,纯函数 task controller 亲验、UI 层派 spec reviewer、整体 final review READY_TO_MERGE

## 当前状态

P4 长期档案 6 子项 **6/6 全闭环**。1.0 长线打磨期。全量 2815测+1skip / analyze 0(worktree 实测)。

## 架构要点(对称 encounter_codex 纯派生)

- `martial_codex_provider.dart`:纯函数(`isMartialCodexSkill` 收录过滤单一真相源 / `martialSourceKindOf`+`labelForMartialGroupKind` 防双份漂移 / `litSkillIds` 三套点亮 / `maxUsesOf` / `groupMartialSkills` 心法小节) + `@riverpod martialCodex`
- 三套点亮口径:心法招→active学过该心法(techDef.skillIds并集) / 稀有招→`unlockedSkillIdSet` / 破招→active队伍含该style角色
- `martial_arts_tab.dart`(第5tab) + `skill_codex_detail_screen.dart`(详情屏纯同步,复用 skill.description 零async)

## 重要决策

- **破招技点亮口径(实装中发现)**:3破招技(破势/截影/拂脉)不走 skillUnlockProgress、不属任何心法,是按 style 默认可装配的 build gate 组件。用户拍板「纳入·按派点亮」(active 队伍含该 style 角色→点亮),新增第三套口径,收录池保持205。
- **收录范围排除轻功18+joint1**:minigame/系统招,非「可修习的武学典籍」。

## 已知问题

- 真机目检未做(非阻塞):`VISUAL_ROUTE=skill_codex flutter run -d macos` + `=skill_codex_detail`,双路由已备(混态前6点亮+其余剪影)。

## 踩坑提醒

- `numbersConfigProvider` 在 `battle_providers.dart` 而非 `numbers_config.dart`(Task4 implementer 核对修正)。
- `SkillSource` 定义在 `skill_def.dart` 自身(不在 enums.dart)。
- 主 checkout merge 后必 `dart run build_runner build`(.g.dart gitignored,martial_codex_provider.g.dart 需重生)。
- 收录数 205 = 心法147+真解6+残页9+破招3(canInterrupt)+奇遇40(encounter_skills.yaml,加载时并入 skillDefs)。

## 下一步建议

1. 真机目检藏经阁2.0(可选,~10min)
2. P4 长期档案全闭环,待用户指定新打磨方向
