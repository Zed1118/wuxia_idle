# Session 交接 - 批次123 清理(资产/死配置/文案归档)

**时间：** 2026-07-03 15:25
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 657444f1（= origin/main·现跑 ls-remote 核实同步）

## 本次完成

详 PROGRESS.md 顶段两条（2026-07-03 batch-123 清理批 + 拍板落地，commit 区间 `a55785ea..657444f1`，6 commit 全 push）。
- 「处理123」批量授权：批次2剩余资产 + 批次3死代码死文案 + P2零散，能自主的全做、需拍板的用户已拍板落地。

## 当前状态

1.0 长线打磨期。批次2/3 清理线基本收尾（零引用资产/死配置/孤儿文案全处置）。工程外围继续收，核心质量优。无在途代码。

## 进行中的工作

- 无在途代码。剩余为下波候选（见下）。

## 已知问题

- numbers.yaml `tower.daily_attempts` 每日5次限制（GDD §8.2 称强制规则）实际未实装，lib/ 零消费——已在死配置头注标注，未擅自加限制功能。
- `_archive/techniques/` 66 篇文案有意留置（Phase 5 武学领悟 UI 迁回，README 有指引）。

## 重要决策

- 3「死文件」(battle_engine/battle_demo/stage_auto_play_control) 决定**不删**：Phase 0 证伪为平衡红线模拟引擎/7+屏测试夹具/未接入功能组件，backlog 误记为「死」（同 tier_* 教训）。盲删摧毁诊断体系。
- 66 文案**移 _archive** 而非删/接线：真孤儿（无加载管线），Phase 5 前接线属提前造轮子违 §5.7，删则 Phase 5 要重写；移 _archive 最保守。
- docs/handoff **不改**：图片治理早由现有 gitignore 完备，Agent 报「2.7G tracked」误导（tracked 全是小文档）。

## 下一步建议

1.（推荐）clamp ×4 迁 numbers.yaml：无拍板依赖、改动明确，`stage_battle_setup:343`/light_foot/mass_battle 三处 0.95 硬编码进 yaml + NumbersConfig 加 3 cap 字段。
2. 死文档归档：先证伪 content_guide.md（skill 依赖）/AGENTS.md（codex）/data_schema.md 引用再动，不可盲归档。
3. README 重写（需拍板内容深度）；setUpAll 252 文件抽 def_loading（11-15h 专项批缓做）。
4. 批次4 CI / 批次5 battle_screen 拆分（原就需拍板）。

## 踩坑提醒

- 零引用资产/「未接线」/「死文件」标注可能是误记：处置前必 `git log -S` 查移除史 + grep 全仓引用 + 看测试是否用作基础设施（memory `feedback_zero_ref_asset_may_be_deprecated` / `feedback_audit_report_phase0_verify`）。本波 3 死文件 + Agent handoff tracked 均误报，subagent 结论逐点 grep 证伪。
- pubspec asset 目录移 `_archive` 段即豁免 `pubspec_asset_declaration_test` 守卫（`dir.split('/').any(seg==_archive)`）。
