# Session 交接 - 江湖远行 B 阶段

**时间：** 2026-07-16 00:20
**项目：** 挂机武侠
**分支：** feat/baicao-duanhun-phase-b（worktree `baicao-duanhun-phase-a2`·未 push·未合 main）
**最后 commit：** 本次 handoff commit（承 `4524598a`）

## 本次完成

详 PROGRESS.md 顶段 2026-07-16 Phase B 条目（代码 commit 区间 `8926be46..5dab9768`，严格 TDD）。
- B1 纯规则/领域 4 任务 + B2.1 `ExpeditionService.dispatch` 入场事务；worktree 本会话实测 analyze **0** / expedition 测族 **23 绿**。
- A2 基建已 FF 合入本地 main `a61df363`（ahead origin 30·未 push——push 是用户的活）。

## 当前状态

百草岭远征（江湖远行 Phase B）领域层 + 派遣入场事务已就位。下一步是本 feature 最难点 B2.2 离线分批幂等结算；UI 尚未接线。

## 进行中的工作

- **B2.2 离线分批幂等结算**：未开工。6 不变式（在线分段==一次性离线 / 幂等 / 批上限 maxNodesPerBatch / 游标守卫 / 战败即停 / 时间回拨）。恢复点见 plan `2026-07-15-baicao-duanhun-phase-b-expedition.md`。
- B2.3 召回/战败返程单事务、B2.4 provider+总览/行记 UI、B.V 批末验证：均未开工。

## 已知问题

- 隔离 flaky `drop_table_reference_redline`（CLAUDE v1.29 已录）：全量并发跑偶发 4045/1+超时，单跑 4/4 绿、非回归。
- `equip-baicao-orchestration` worktree（`25221323`）已验证并入 main、可删；bg 不删用户 worktree，续传给用户手删（第 2 轮挂账，本轮升级问用户）。

## 重要决策

- **run.seed 存新 serial（非 hash）**：`ExpeditionRun` 无 runSerial 字段，B2.2 用 `run.seed` 作 `ExpeditionRules.generateNode` 的 runSerial。
- **runNodeBattle 不内调 resolve**：远征奖励走 `rewardsForNode` 非掉落；resolve 的 Isar 副作用（修炼/伤势）归 B2.2 结算事务在 `finalState` 上按需调。
- **成员 HP/qi 派遣存 0（currentNode==0=fresh）**：B2.2 首战按 `fromCharacter` 满血起、战后写回快照×`recoveryMultiplier` 跨节点继承。

## 下一步建议

1. **B2.2 离线分批幂等结算（推荐）** — 本 feature 最难点，领域层已备齐，趁 B1↔B2 seam 记忆热接线，收益最大。
2. B2.3 召回/战败返程单事务 — 依赖 B2.2 结算路径。
3. B2.4 provider + 总览/行记 UI + B.V 批末验证（macOS build + Codex 视觉）。

## 踩坑提醒

- `.g.dart` gitignored：phase-b 若从别处 fresh checkout / merge，必先 `dart run build_runner build` 再测，否则 Isar 静默丢字段（memory `feedback_wuxia_pen_build_runner`）。
- `ExpeditionRun` **无 runSerial 字段**：B2.2 别找它，用 `run.seed`。
- 派遣快照 `currentHp/Qi=0` 是「未开战」哨兵**不是**真血量：B2.2 首战须 `fromCharacter` 满血起，勿把 0 当残血。
