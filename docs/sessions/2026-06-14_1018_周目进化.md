# Session 交接 - P1 周目进化

**时间：** 2026-06-14
**项目：** 挂机武侠
**分支：** worktree-p1-cycle-evolution（未合 main；main 仍在 G3 45841d36）
**最后 commit：** 7a523a38（分支已 push origin）

## 本次完成

详 PROGRESS.md 顶段 `2026-06-14 P1 周目进化 A-F1 全闭环` 条目（分支 commit `45841d36..7a523a38`，23 commit）。
- subagent-driven 13 task + opus 终审 + 对抗性质量审计（A−）；终审/审计各抓修真 gap（识破死机制 / 凝甲 in-battle e2e 缺失）。
- 红线选项 B 已实装：Boss HP 50000→60000（config 化 + scaledHp clamp + GDD/CLAUDE §5.4 同步）。

## 当前状态

P1 周目进化（主线江湖记招 3 周目 + 爬塔问鼎轮回 2 周目 + cycleIndex schema 0.21.0 + 敌人 +6%/周目 scale + 5 反制词条 + 周目选择 UI）功能全实装、全绿（2169 测/analyze 0）、push 到分支。**未合 main**，唯一剩 Codex 视觉验收一道门。

## 进行中的工作

- Codex 视觉验收：验收包已编（hub @ 7a523a38），派单 `docs/_archive/codex_dispatch/codex_dispatch_cycle_evolution_2026-06-14.md`（route stage_list_cycle / tower_cycle）。PASS 后 `git merge --ff-only` 合 main。

## 已知问题

- **意图性留置**：worktree `p1-cycle-evolution` + 分支 `worktree-p1-cycle-evolution` 保留中（P1 待 Codex 视觉 + merge，非孤儿，merge 后再 ExitWorktree remove）。
- 敌人 attack 在 stage_06 cycle3 peak 3024 / tower floor30 cycle2 2385 超 §5.4「2000」——§5.4 2000 是玩家装备红线非敌人约束，记录待数值层确认是否需敌人 attack 帽（非 bug，不阻塞）。
- 已清理：主 checkout 一个漂移误写的 untracked `test/data/cycle_evolution_config_test.dart`（分支有 committed 版）。

## 重要决策

- 红线选 B（调 60000）非 A（降 stage_06_05 baseHp）：因为西凉霸主 baseHp=52000 是 pre-existing，选 B 不动既有 boss 数值，接受终局膨胀。
- 5 反制词条全复用现有战斗机制（defense/IF/chargeSkill/crit/InternalInjurySlot），不建新战斗子系统 → 确定性零新 rng。

## 下一步建议

1. **（推荐）Codex 视觉验收 P1 → 合 main**：唯一剩余门，验收包+派单就绪，PASS 即 ff-only 合并。
2. P1 合 main 后真玩验收：周目手感（首通手动→自动回放→二周目江湖记招触发流）+ 词条战斗体验。
3. P2 文档2 其余创新点（战败诊断帖 / 本命兵器编年史 / 江湖传闻等，独立 spec）。

## 踩坑提醒

- **subagent cwd 漂移**：昨晚某 subagent 把测试文件误写进主 checkout（非 worktree）→ 残留 untracked。派 subagent 时 prompt 必须强调 "Work from <worktree 绝对路径>"，收尾 handoff 0c 必查主 checkout `git status`。
- **P1 合 main 后主 checkout 必跑 `dart run build_runner build`**：schema 改（0.21.0 新字段）的 `.g.dart` gitignored 不随 merge 走，否则 Isar 静默丢字段（memory `feedback_wuxia_pen_build_runner`）。merge 后全仓 `git grep "'0.20.0'" test/` 应已无（A3 改 0.21.0）。
