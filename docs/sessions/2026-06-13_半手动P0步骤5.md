# Session 交接 - 半手动战斗 P0 步骤5 重放落盘地基

**时间：** 2026-06-13
**项目：** 挂机武侠
**分支：** main（worktree-p0-step5-replay-persist ff 并入 + push）
**基线 → HEAD：** 6c0ae9ee → ce62fdf2
**档位：** xhigh（schema 迁移 + 数据模型，用户批准升档）

## 用户拍板（开工前对齐）

1. **GDD line 81「在线=离线」红线** → 采纳半手动 spec §四草案**加注**（不改原文，旁加澄清）。
2. **步骤5 范围** → 先做 **schema 落盘地基**，不动真实入口/自动开关 UI（大 UX 变更留下一波）。
3. **升 xhigh**。

## 本次完成（spec §五 P0#5 地基部分）

- **`BattleReplayRecord`** 新 Isar @collection（`lib/features/battle/domain/`）：
  `saveDataId` + `@Index battleKey` + `seed` + `opsJson` + `clearedAt`。
  battleKey = `stage#<stageId>#<cycle>` / `tower#<floor>#<cycle>`，**cycle 默认 1**
  （前向兼容 P1 周目，本波不实装周目）。
- **`BattleReplayOp` JSON 序列化**（battle_replay.dart）：toJson/fromJson/
  encodeList/decodeList，targetId 可空保真。
- **`BattleReplayRecordService`**（application/）：record（手动通关写，同 battleKey
  覆盖最新单行）/ isManuallyCleared / find / stageBattleKey / towerBattleKey。
- **isar_setup**：注册 BattleReplayRecordSchema + saveVersion **0.18→0.19.0**
  （新 collection 旧档天然空，无数据迁移动作）。
- **GDD line 81 加注**：自动=刷已手动通关关卡（产出仍在线=离线，不加速）/
  手动=推新内容一次性门槛（非留存机制），反留存初衷不破。

**设计取舍**：cycleIndex **不入** MainlineProgress/TowerProgress（YAGNI，周目 P1
接入；cycle 已编码进 battleKey），schema 改动最小化（仅 1 新 collection）降迁移风险。

## 当前状态

analyze 0 / 全量 2025→**2036 测** / 1 skip。.g.dart + libisar.dylib 均 gitignored
（接收方 fresh checkout 需 `dart run build_runner build` + 拷 dylib）。

## TDD（11 新测）

- 序列化 4（单条 round-trip / targetId null 保真 / 列表保序 / 空列表）。
- service 6（battleKey 构造 / 未通关 false / record 后读出 seed+ops / 同 key 覆盖单行 /
  独立 key（stage/tower/cycle）/ close-reopen 持久化）。
- isar_setup 迁移 1（0.18→0.19 升版 + BattleReplayRecord 天然空）。
- 更新 isar_setup_test（2 处）+ sect_isar_persistence_test（1 处）版本断言 0.18→0.19。

## 下一步：步骤5 全闭环（大 UX 变更）

1. **真实入口接 manualStep 首通**：stage_entry_flow(:325/336/345) + tower_entry_flow(:638)
   首通该关时走 manualStep 单步 + 传 seed；通关后 `BattleReplayRecordService.record`。
2. **「自动战斗」开关 UI**：已手动通关的关（isManuallyCleared 命中）显开关；
   开自动 → `BattleNotifier.replay(seed, ops)` 确定性复刻。
3. 需 **Codex 视觉验收 + 真玩拍板**（每关首通变手动单步是大 UX 转变）。
4. 之后 → **P1 周目进化**（江湖记招 / 问鼎轮回，cycleIndex 此时接入 progress）。

## 挂账（待拍板）

- test/tools/output/*.md 等 balance 产物 tracked，每跑测试刷新（治本 gitignore +
  `git rm --cached`，涉 git 治理待拍板；当前靠 commit 前 `git checkout --` 还原）。
- CodeGraph 未 init（CLAUDE.md 声称有该 MCP，可选 `codegraph init`）。
- 步骤3c 单步 UI 待 Codex 视觉验收（派单 + 预编包就绪）。

## 踩坑提醒

- Isar collection 的 `isar.battleReplayRecords` 访问器在 `*.g.dart` 的 part 里 —
  测试需 import 该 domain 文件才在作用域（否则 “getter not defined”）。
- 版本号升级会打断断言 saveVersion 常量的现有测试（isar_setup_test/sect 持久化），
  改 schema 版本时全仓 grep 该版本字符串同步更新。
