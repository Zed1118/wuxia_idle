# Session 交接 - 爬塔 spec 复核与拍板

**时间：** 2026-08-04 09:02
**项目：** 挂机武侠
**分支：** main（本批工作在 `worktree-tower-extension-spec` / PR #113 draft，**未合**）
**最后 commit：** main `8877560e` · 分支 tip `b03dff15`

## 本次完成

批 D 详见 **PR #113 分支上**的 PROGRESS 顶段「2026-08-03 爬塔扩展 spec xhigh 复核」条目
（commit 区间 `36fe9d80..b03dff15`）。**该条目尚未进 main**，只读 main 的 PROGRESS 看不到本批。

- 复核把问题范围整个改写：不是「塔停在二流」，是除主线与心魔外 **5 个支线入口 48 关**对终局玩家全部近免疫；
  用户质询「推荐有没有为省工作量委曲求全」后自查出 3 条，修正并把该失效模式落成全局规则

## 当前状态

1.0 长线打磨期。爬塔与支线终局适配完成 spec 复核 + 8 项拍板 + 实装 plan（批 D），
批 A/B/C 均未开工。PR #113 draft 待 review/合并。

## 进行中的工作

- **PR #113（draft，未合）**：批 D 全部产物在此分支；worktree `tower-extension-spec` 保留（locked 态）
- 批 A 第一切片 A0（解 `tower_progress_service.dart` 三处硬编码 30）**不阻塞于待拍项**，可直接开

## 已知问题

- **本批 PROGRESS/BACKLOG 更新只在 PR #113 分支**，未进 main —— 下会话只读 main 会漏整批上下文
- spec 154 行超 150 上限 4 行：已压 5 轮，余下均为实测表格与拍板依据，为凑行数删数据不划算
- spec §1.3「净威胁增益 ~2.4×」是**解析推算未跑 balance_simulator**，已在 spec 内标注待批 A 实测

## 重要决策

- **塔取 1:1 锚死 49 层而非起草版 70 层**：因 `floor N ↔ abs N` 是唯一自解释映射、断档结构上
  不可能存在；内容量减半（~38 vs ~80 敌条目）是附带结果，非选它的理由。
- **塔走扩层、其余 4 入口走周目语义修正**：塔是顺序爬的，扩层天然自动分层；境界周目是整体抬，
  对刚通关的三流玩家是差 3 阶硬墙，在塔上机制不自洽；固定关数的 4 个入口反之。
- **全局 CLAUDE.md 加硬规则**（§输出与执行风格首条）：推荐不得为省工作量缩水范围。
  因为 CLAUDE.md §7 早有同义原则我仍违反了 —— 缺的不是原则，是可执行的自检动作。

## 下一步建议

1. **合 PR #113 后开批 A**（推荐）—— 先合并让 PROGRESS/BACKLOG 进 main，再从 A0 解硬编码起步
2. 拍断魂帖里程碑分布 —— 只阻塞批 A 的里程碑部分，A0/A1 不受阻
3. 批 B 周目语义修正 —— 与批 A 文件不重叠，可并行

## 踩坑提醒

- **`tower_progress_service.dart` 三处硬编码 30 不是从 `allFloors` 派生**（`:78` `availableFloor`
  封顶 / `:89` `canChallenge` 上界 / `:176` 周目完成判定）：扩层前不解则新层进不去，且**静默失效不报错**
- **spec / 上游 / 上一版给的形状不是推荐依据，必须自己算一遍**
  → memory `feedback_no_effort_saving_in_recommendations`
- `.codegraph/` 未初始化，本会话结构性查询全部退回 grep；批 A 开工前建议跑 `codegraph init -i`
