# Session 交接 - 战前情报弹窗 opt-in 重做

**时间：** 2026-06-28 14:55
**项目：** 挂机武侠
**分支：** main
**最后 commit：** 5292ca50

## 本次完成

详 PROGRESS.md 顶段 🔍 2026-06-28 战前情报弹窗 opt-in 重做条目（commit 区间 `0e610bc0..5292ca50`，8 commit 全 push）。
- 销睡觉模式 11「战前情报」暂缓挂账：重做成 opt-in（复用 ⓘ 图标）+ 去四重冗余 + onTap 直接进战斗不变。
- 全程 brainstorming→spec→writing-plans→subagent-driven(5 task)→最终 review APPROVED 闭环。

## 当前状态

1.0 长线打磨期。战前情报已合入 main 并 push，无进行中切片。纯表现层，零 schema/saveVer/numbers.yaml。

## 进行中的工作

- 无代码切片进行中。macOS run 仍挂着供真机目检；目检手感反馈待用户给。

## 已知问题

- **真机目检手感未定**：弹窗排版/信息密度/段距/字号绝对值需用户 `flutter run -d macos` 现场看（spec §6），未调。
- **遗留债·第二轮·升级问用户**：16 个 `~/.codex/worktrees/` + 17 个 codex 分支（含 `codex/nightly-2026-06-28-integration`）仍在。11 已重做、其余 15 已合 main，integration 源料已用完，**全部可清**；但属 Codex 域 + 16× 删除（branch -d 对非 ancestor 会拒，整备方式不明），需用户拍板清理方式 → 列候选 #1。已续传 2 轮，下轮强制处置。
- **untracked** `docs/audit/stage_review_2026-06-28.md`：上轮 Codex 审查报告，沿用留置。

## 重要决策

- info 图标复用而非新增第二图标：守睡觉模式 14 信息密度警告，零新增行内元素。
- 「应对」段标题不用「整备」：避与行内整备条（14）重名混淆。
- plan 漏列守旧 loot wiring 测（`stage_row_loot_wiring_test`），verify 期全量跑出 -1 才补；改 UI 交互的 spec 改动清单应含「反查断言旧行为的测试」维度。

## 下一步建议

1. （推荐）清理 16 codex 遗留 worktree/分支（销第二轮债）— sonnet 10-15min，理由：已续传 2 轮、源料全用完、不清则下轮强制。
2. 真机目检微调（若用户看完弹窗有手感反馈）— opus，视反馈量。
3. 红线跟进切片：07 realm_gap priority 降级 + 09 叙事迁 `data/narratives/retreat/` — opus 20-30min。

## 踩坑提醒

- **`flutter run -d macos` 复踩已知坑**：误加 `DEVELOPER_DIR=/Library/Developer/CommandLineTools` → `xcrun: error unable to find xcodebuild` (exit 72)。该 override 仅给 git/analyze 绕 license，macOS 构建/运行须裸跑默认工具链。**memory `feedback_xcode_license_workaround` line 32-37 早有记录**（2026-06-11/06-17 已踩，本次第三次）——开局选读该 memory 可免。
- **改 UI 交互的 spec/plan 必含「反查断言旧行为的测试」维度**：本次只列改动文件，漏了 `stage_row_loot_wiring_test` 断言旧弹窗标题，全量才暴露。
