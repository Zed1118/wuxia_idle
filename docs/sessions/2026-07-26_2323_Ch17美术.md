# Session 交接 - Ch17「沙海纵深」美术 11 图

**时间：** 2026-07-26 23:23
**项目：** 挂机武侠
**分支：** `worktree-ch17-art`（PR #84 draft·未合 main）
**最后 commit：** `b420fc56`

## 本次完成

详 PROGRESS.md 顶段 2026-07-26「Ch17 美术 11 图接线」条（区间 `19f21445..b420fc56`·2 commit）。PROGRESS 没展开的：

- **交接文档说的「两处接线」实测只有一处**：身份项 `character_avatar.dart:1074-1085` 章批已登记好，本批只需补 footFraction 表。开工前按文档准备会多算一步。
- **`battle_standee_asset_role_test:104-151` 的判据比缺图 allowlist 严得多**：除四角 alpha 还硬卡四边不贴画布、人物比 >0.78、脚底容差 0.035。我先写 `gate_precheck.py` 逐条复刻这些判据做预检，才能在返修单里一次说清，而不是接完线再退回。
- **codex 自评在关键项上说了反话**：黑风刀客自评「厚背阔刀识别明确」，实际画的是双刃直身剑。照自报接线就进 main 了。

## 当前状态

1.0 长线打磨期。主线 **17 章 85 关全部美术齐备**，`known_missing_assets` **清零**（Ch16 后再次全表归零），standee 门禁恢复全量把关。Ch17 全章闭环。main 未动（`19f21445`），成果在 PR #84。

## 进行中的工作

- **PR #84 待审待合**（draft·MERGEABLE·14 文件）。合入后 main 才有 Ch17 美术。
- **Ch18「天地之远」spec 未起草**：段级 §8 已定向（cap 40→42 封顶·yang_guan 收编·章中+末 0.12 全机制）。

## 已知问题

- **11 张真 PNG 共 14M 待 webp 清账**（assets 98M→114M），沿 #63/#66/#70/#77 先例。
- **立绘未真机战斗屏目检**（已合成到真 `battle_desert`/`battle_temple`/`battle_frontier` 底图逐张读图托底）。
- **8 个僵死 `flutter_tester`**（PID 57838/57910/57946/57965/57968/57970/72143/72159）：启于 07-18、07-19，PPID=1，引用的 `.worktrees/` 路径已不存在。**上轮交接记 6 个，实测 8 个**。本会话全量耗时 7m33s vs CLAUDE §8.0 记的 ~2.5min，疑其抢核。需授权 kill。
- **残留远端分支 `origin/worktree-session-handoff-0726`（已续传 3 轮·本轮强制升级）**：本会话独立复验——其独有 2 commit 只贡献 2 个文件，`2026-07-26_1116_PR合并与压缩归档.md` 与 main 侧**逐字节同**（各 4656 字节·hash `10f46eb540bc251b`），另一个是**已被 main 取代的过期 NEXT.md**。删除零丢失。**属对外操作，两轮未获回复即未动**。命令：`git push origin --delete worktree-session-handoff-0726`。
- **留置（gitignored·主 checkout）**：`build/dispatch/ch17_art_20260726/`（派单书/返修单/双日志/两复测脚本/hash 快照/10 张解码锚）+ `ch17_art_out/` + `ch17_art_preview/`，共 ~41M。留作返修追溯与下次美术批配方参考，不入库。

## 重要决策

- **返修单定性为「派单锅不是模型锅」**：锚图 `liangzhou_jieguan_ren` 本身就是厚背宽面重刀，是我派单里「不要取其铁甲」让 codex 弃掉了整张锚。改成「只有铁甲不要照，刀形必须照」+ 锁死刃形几何后一次过。**带否定限定词削弱参考锚，会连带丢掉本想保留的特征。**
- **破坏证红选脚底 fraction 而非删表项**：报红信息会打印门禁自己的实测值，借此拿到「门禁独立读盘测得 0.9701」与我 PIL 测的 0.9701 逐位一致——一次操作同时证明门禁有牙 + 两条测量路径口径一致。

## 下一步建议

1. **审并合 PR #84（推荐）** — Ch17 美术只有合入 main 才算真闭环，且 14M 体积债要等合入后才能清。
2. 清 8 个僵死 `flutter_tester` + 删残留远端分支 — 两条命令，均已验零风险，需授权。
3. Ch17 webp 清账小批 — 14M→~1.5M，须在 #84 合入后做。
4. Ch18「天地之远」spec 起草 — **起草前必读 Ch17 spec §1** 的三处上游段级事实错。

## 踩坑提醒

- **锚图喂给 codex 前必须 `sips -s format png` 解码**：库内 `.png` 实为 webp-in-png（本批 10 张锚全是 `RIFF…WEBP`）。已在 memory `reference_codex_image_gen_art_pipeline`。
- **`git show ref:中文路径` 比对要看字节数**：两侧都取不到会得到相同的空哈希，**假报「内容一致」**。本会话验残留分支时首次踩到，靠 `wc -c` 逼出真值（4656/4656）。
- **`flutter test` 传多个文件路径会静默漏跑**：本批 6 个 targeted 全部**逐个单跑**并各自确认 EXIT，不批跑。memory `feedback_flutter_test_batch_silent_skip`。
- **zsh 没有 `$PIPESTATUS`**（是 `$pipestatus`，且 1-indexed）：`cmd | tail; echo $PIPESTATUS[0]` 取到空值，会让「EXIT=0」变成没有证据的断言。本会话两次踩到，改用 `cmd > log; echo $?`。
