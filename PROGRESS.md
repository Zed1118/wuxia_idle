# 挂机武侠 · 开发进度
> Mac 端 Claude Code 维护。会话开始主动读取，任务完成主动更新。
> 总行数控制在 100 行内，超出归档到末尾。
>
> **当前阶段：1.0 长线打磨期（质量优先 · 不设上线时间压力）** — Demo ✅(2026-05) → 1.0 内容周期 ✅(P1-P5+) → 打磨中。阶段一变只改本行；工作原则见 CLAUDE.md §7。

## 当前阶段

> **2026-08-17 Phase 0A Batch 9C 键盘焦点/导航合入(merge `7ca0e33e`,分支 `feat/phase0a-batch9c-keyboard-focus-nav` RP0 冻结 + RP1 实装 + RP2 破坏证红)**:BACKLOG 二#13 ③ 落地,整条销账。① `_SkillSeal` 转 StatefulWidget + `FocusableActionDetector`(PlaqueButton 体例):`onShowFocusHighlight` 驱动 `if (_focused)` 金边环(环宽入 token `focusRingWidth`),Enter/Space 走 ActivateIntent,其余键 ignored 冒泡屏幕;② `_RetryButton` 补同款金边环(9B 只接了激活/光标没接高亮);③ Semantics label = 印字+键位+状态行,内容层 `ExcludeSemantics` 防节点 label 与子 Text 拼双份;④ 新增 `phase0a_focus_nav_test` 8 例:Tab 环游 screen→gather→clear→screen 闭合、终局跳过禁用印直达再战、焦点在印上 W/Q/R 仍冒泡真实驱动(移动/聚怪/清场逐事件断言)、金边环出现/随焦移动/失焦撤掉。**已验证(本会话实测)**:新增测 8、**两处破坏证红精确命中**(删印金边环→4 红;删再战金边环→2 红,均反向补丁还原复绿)、analyze 0、format 门禁净、desktop_semantics 门禁保持 phase0a 零豁免、全量 **5160 pass / 0 fail**(=基线 5152+8 逐值吻合,五段分跑 676+469+3889+124+2;基线另起独立 worktree 同口径复测 5152 逐值吻合)。**过程逮到两个真问题**:① 焦点高亮回调走帧后派发,失焦断言单 `pump()` 吃不到须 `pumpAndSettle`(plaque 失焦测恰用 pumpAndSettle 才绿,复刻实验对照定位);② 全新 worktree 缺 gitignored 生成物(`*.g.dart` 须先 `dart run build_runner build`、根目录 `libisar.dylib` 须拷入),否则批量 loading 级联红——基线复测当场踩中并修。**视觉验收口径**:焦点环是键盘态视觉,静态路由拍不到,以 widget 测断言焦点态金边存在/失焦消失为主,J/Q/R 三条既有路由保非焦点态零回归(未新增路由、未动 20s 保持窗口)。
> **2026-08-17 Phase 0A Batch 9B 终局重试入口 + 胜负 jingle 合入(merge `b3f2942f`,分支 `feat/phase0a-batch9b-retry-outcome-sfx` RP0 计划 + RP1 实装)**:BACKLOG 二#13 ② 落地。① `Phase0aBattleController.restart(newFlow)`:flow/sequencer/VFX 三实例换入重建,state 完全复位(tick/seq/血量/敌编制回初始)、缓存清空、notifyListeners。② 终局封签正下方加「再战」纸签按钮(宣纸底/绛红描边/墨字,tokens 入 `Phase0aPresentationTokens`;**必须放主 Stack**——封签在 IgnorePointer 反馈层收不到手势);点击/Enter 触发 `_retry()`(播 uiTap→await retryFlowBuilder 重载 fixture→restart→清局部表现态),`_retryInFlight` 防双击;`retryFlowBuilder==null` 的静态验收路由不显示按钮、Enter 无效。③ sfx 映射补 `Phase0aBattleVictory→victory.mp3`/`Phase0aBattleDefeat→defeat.mp3`(对齐旧战斗结算 overlay 语义)。**已验证(本会话实测)**:新增测 5(retry 4:controller 复位+点击/Enter/null-builder;sfx widget「victory.mp3 恰好一次」)、**两处破坏证红精确命中**(victory jingle 哑火→sfx 2 红;restart 哑火→retry 2 红,均反向补丁还原复绿,不用 `git checkout --`)、analyze 0、全量 **5152 pass / 0 fail**(=5147+5 逐值吻合,四段分跑 1271+1529+1325+1027)。**过程逮到一个真问题**:裸 GestureDetector 被桌面语义门禁(`desktop_semantics_audit_test`)拦下,按 CLAUDE §8.2 用 `FocusableActionDetector` 补焦点/键盘激活/鼠标光标四义项(非 allowlist 豁免);widget 测里 `_retry` 的 fixture 重载是真实异步 I/O,`pump()` 等不到,须 `tester.runAsync` 包触发动作。**二#13 只剩 ③键盘焦点/导航**。
> **2026-08-17 Phase 0A Batch 9A 战斗音效接线合入(merge `e0353f2c`,分支 `feat/phase0a-batch9a-sfx-wiring` 2 commit)**:BACKLOG 二#13 ① 落地——新战斗屏此前全程静默。新增 `phase0a_sfx.dart` 纯映射(事件→资产路径):普攻命中按出手方 `battleHit_<side>_0` 变体、暴击 battleCrit、isUltimate/ R 清场起手 battleUlt、Q 聚怪起手 battleChargeStart(全库语义最接近的既有资产,用户可否决);接线点=`Phase0aBattleScreen._refresh()` 逐事件 `SoundManager.playSfxPath`,天然继承静音/音量/缺资产 guard。**有意冻结两项**:敌方死亡静默(`battleDeath.mp3` 资产不存在,守「不新增无资产 SfxId」;补资产后翻转 `phase0a_sfx_test` 静默断言即可)、victory/defeat jingle 留 ② 终局批一并拍。**已验证(本会话实测)**:红测 5 例(纯映射 4 + 真 fixture 整屏接线 1,逐步断言「录制路径 == 事件流映射」不依赖具体伤害结果)、**破坏证红精确命中**(接线哑火变异 → widget 测红,还原复绿)、analyze 0、format 门禁全仓 0 changed、全量 **5147 pass / 0 fail**(=5142+5 逐值吻合,四段分跑)。**过程教训**:mutation 后误用 `git checkout --` 还原导致未提交接线被整段撤掉(该文件接线尚未 commit),重接后复绿——未提交改动的变异还原须用反向补丁而非 checkout。
> **2026-08-17 Phase 0A Batch 8B 战斗反馈强化合入(merge `dd019d6d`,分支 `codex/phase0a-batch8b-combat-feedback` 6 commit)**:codex 按冻结计划(`docs/superpowers/plans/2026-08-17-phase0a-batch8b-combat-feedback.md`)交付命中闪白/血条强调、近远程分型(近战双弧墨痕/远程有体积青墨掌风片)、Q 拉拢事件位置快照+双层弯曲墨带轨迹、死亡层级(普通克制墨散/精英双层墨环+绛红点染);数值/规则零触碰,所有时长尺寸线宽入 `Phase0aPresentationTokens`。**接管时现场**:分支未合并,worktree 留有未提交收尾——J/Q/R 三条首拍静态验收路由(`_Phase0aFeedbackPreview` 单拍冻结 + screen 新增 `autoStep`/`feedbackHoldSeconds` 参数)+ 墨带法线收口,代为验证(format/analyze 0/diff --check 净/targeted 67 全绿)后补两 commit 收口。**接管复核逮到一个真问题**:`visualRouteFeedbackHoldSeconds=5s` 小于截图管线 READY 轮询+focus/resize 固有开销(实测 5-8s),默认 `--wait 12` 下三张首拍帧全部落在保持窗口外——拍到的是特效已过期、敌人满血、技能刚进冷却的「空帧」,若采信即假绿;token 放宽至 20s(注释写明原因)后重拍 3 路由×1280×720/1440×900 共 6 帧目检全过:掌风片体积感/双层弯曲墨带指向玩家/墨爆锚定施法者(R 走 `effectRadius` 以 actor 为原点,弓手在半径外满血=reducer 正确行为非 bug)/伤害数字 2049·3830/波次横幅/技能印可用·冷却语言/真气消耗均符合契约。**已验证(本会话实测)**:全量 **5142 pass / 0 fail**(因单次 300s 上限分四段跑:1271+1519+1325+1027,各段 exit 0 均报 All tests passed)、合并态 analyze 0、合并态 targeted 67 复绿。**过程留痕**:本机 flutter 环境将 `pubspec.lock` 镜像 URL 从 `pub.flutter-io.cn` 翻成 `pub.dev` 被 `git add -A` 扫进提交,已 amend 剔除(纯环境噪音,零版本变化)。**首推 CI 两连红已修**:① Batch 1-8A 纯本地合入从未过 CI,带入 4 个未 `dart format` 文件,`bf4002fd` 修复并全仓复验 0 changed;② `phase0b_art_load_evidence_test` 的「frozen build_commit 须为 HEAD 祖先」断言在 CI 浅克隆(默认 fetch-depth:1)下必红,`de0a7c0b` 改为 commit 对象缺失才跳过(完整克隆仍强制校验,本地实测两路径)。**终态 CI `de0a7c0b` success**(macos-build + test 双 job,test job 5141+1=5142 与本地逐值吻合)。**另注**:本次 push 把整条 Phase 0A 本地线(Batch 1-8B,2218 objects/34MB)首次推上 origin。**下一步已解锁**:BACKLOG 二#13 交互收口 ①复用 `shared/audio` 接命中/Q/R/死亡音效(不新增无资产 SfxId)→ ②终局重试入口 → ③键盘焦点/导航。
> **2026-08-17 Phase 0A 生产战斗 Batch 1-8A 合入(`df68efa7`)**:从纯 Dart 几何/事件/reducer 起步，完成角色快照、正式伤害公式适配、波次终局、生产 flow 装配与 debug 可玩表现层；Batch 7 接入正式水墨背景、角色/敌人立绘、全体血条名字、伤害数字、Q/R 技能印与真气/CD 展示；Batch 8A 将掌风、Q 涡旋、R 墨爆、死亡墨散和致死飘字从屏幕中心改为事件发生时的 world 坐标快照，经 `worldToScreen` 精确映射。接管终验补回容量测试并强化为精确 48/160 截断，坐标旧版回退破坏证红 5/5、还原复绿 5/5；相关回归 351/351、analyze 0、probe 隔离 5/5、macOS build 与 1280×720/1440×900 真机 J/Q/R Gate 通过。下一步已解锁 Batch 8B：命中闪白/血条强调、Q 拉拢轨迹、掌风水墨化与死亡反馈层级；音效、重试、键盘导航独立后置。
> **2026-08-12 挂机批 A-F 六单(PR #120/#121/#122)**:用户授权时长/范围/合并推送/绿黄代拍四项后开工。ⓐ **A 视觉路由存档隔离(BACKLOG 二#12)**——`visual_route_host.dart` 裸调 `IsarSetup.init()` 不传 directory,视觉路由直开玩家真实存档;**危害比原登记重一档**:`phase2_seed_service.dart:1052-1058` 的 `_clearAll()` 清 characters/equipments/techniques/inventoryItems/gameEvents 且被 6 个 seed 方法调用,跑一条会 seed 的路由=清空玩家全部业务表。新增 `visualRouteIsarDirectory()`(systemTemp 下固定目录,每次递归清空重建),核实 33 处 seed 调用点全自带 seed、无路由依赖真实存档故隔离零副作用。ⓑ **C 死链扫描归档类单列(池 P10)**——`ARCHIVAL_DIRS={docs/handoff}` 照扫但单列不计 dead(907→322),顺带接上定义后全仓零消费的 `EXCLUDE_DIRS`。ⓒ **B 锁屏截图根因(BACKLOG 二#11)**——协调者两组实验定论:锁屏时 `screencapture -R` **必死**(8 采样完美相关)而窗口截图与全屏截图**照常成功**,`could not create image from rect` 是 fallback 症状非根因;复现成本从「跑完整构建+视觉管线」降到「5 秒零构建」;掩蔽因素=Amphetamine 持 `PreventUserIdleDisplaySleep` 95 小时致屏幕永不自动锁。详 `docs/audit/visual_capture_lock_rect_failure_2026-08-12.md`(含一条**协调者自己推翻的初版修法建议**留痕)。ⓓ **E 锁屏可诊断化**——新增 `lock_state.py`,主路径失败时打 `VISUAL_CAPTURE_DIAG:` 记锁屏态/window_id 实际值/窗口截图 rc,双失败非零硬停不再让 `crop_window_content.py` 抛 PIL traceback;`-R` fallback 保留(未锁仍有效)。ⓔ **D 备真机决策局**——8 项决策清单 `docs/spec/2026-08-12-playtest-decision-session.md` + 分步可中断脚本 `tools/playtest/decision_session.sh`。ⓕ **F 扫描器漏扫修复**——用刚升级的扫描器验自己新文档时挖出:git 对非 ASCII 文件名默认八进制转义加引号,致 `p.startswith("docs/")` 恒假、**291 篇中文名文档从未被扫描(占应扫 23.7%)**,`git_check_ignore` 回显同病;两处改 `-z` 后扫描源 **939→1230**、死链 327→367。**因此一#18「扫描器升级为可引用事实源」当日拍板当日回退**(P6 的 precision 95%/recall 100% 是在漏扫样本上测的,不能外推),条目已重开并写明重拍须先重做标注验证。**另两项拍板已执行**:P12 删两个 `is-ancestor` 正面验证过的零风险远端分支(留两个 rebase 前旧 SHA)、归档范围锁定只含 `docs/handoff`。**验证(协调者独立复跑非采信自报)**:合并后全量 **4920 pass/0 fail**(守恒=上轮 4917+A 新增 3)、analyze 净、A 的 `test/features/debug/` **21/21 文件**逐个单跑、扫描器两套件 14/20、**独立变异 7 次全部精确命中并 sha256 逐字节还原复绿**;D 的清单逐条抽查代码引用全对但**查出 1 处事实错误已订正**(称「BACKLOG 一区无 #19」实则第 14 行就有)。**协调者自身两处失误留痕**:开局 `date` 读到 UTC 误判时刻差 8 小时;一度把 CI 的 `cancelled` 当成 success(`gh run watch --exit-status` 退出码 0 会掩盖取消)。
> **2026-08-11 P11 资质三连实装(分支 `feat/aptitude-birth-rarity`)**:用户升 xhigh 后开工,拍板口径 #15 取(c) 按出生点数重算 / #16 取(b) 改显出生点数 / 视觉档位化并入试玩局。ⓐ **一#15 旧档回填**:saveVersion `0.38.0`→`0.39.0`,`isar_setup` 加迁移段 9,按出生点数(`attributes.total − attributeBonusFromAdventure`)重算全部角色 `Character.rarity`——2026-08-08「出生锁死」后加载期不再重算,而 08-08 之前的创建点写死 `biaoZhun`,那批角色本会**永久**停在错档位(讽刺点:改锁死之前那版反会在吃一次奇遇后自愈)。**不按当前总点数**:祖师出生 21-22 吃满生涯 cap +5 后当前 26-27,越出 `rarity_distribution` 上界会被钳到 `jueShi`(GDD 标 2%),正是出生锁死要避免的失真。段 9 沿段 6 体例在事务内**重读当前行**防被前置段快照覆盖;`GameRepository.isLoaded` 门沿段 3/4 体例,但**已注明与它们的关键差异——本段没有自愈路径,真跳过就是永久停留旧值**(生产不会走到:splash 先 `loadAllDefs`、Isar 由 SaveSelectScreen 选档后才开,顺序实测在案)。ⓑ **一#16 chip 括注改出生点数**(原拼「存储的出生档名 + 当前实时总点数」,锁死后按设计分叉但拼法在暗示对应;新档也中:「资优(27)」而资优区间 21-22)。顺带核过招募候选卡两段同取 `profile.total` 自洽,不存在同源问题,不动。ⓒ **算法 sink 走 extension 不是风格选择**:`attributes.g.dart` 实证 isar_community 把类内公开 getter 生成成**持久化属性**(`Attributes.total` → `PropertySchema(id:4)`),加进 `Character` 就是白给 schema churn 且落盘值永不回读;改 extension 后重跑 build_runner,`character.g.dart` 里 `birthAttributeTotal` **0 命中**、对照组 `r'total'` 在 `attributes.g.dart` **5 命中**。ⓓ **K2 extension 棘轮按设计拦下本批(15→16 变红)——这条 2026-08-07 建的棘轮第一次真拦到人**:按其要求先在 K1 报告附录 A 开「增量登记」段写审计结论(硬编码=无 / 更优归宿两条均劣 / **残留风险=将来新增非奇遇属性来源(丹药/传承/洗点)不同步累加 `attributeBonusFromAdventure` 则式子静默失真**,已写进 extension doc)再更新白名单;顺带把棘轮头注对 K1 报告的 `:142-160` 行号引用改成引「附录 A」(本次编辑恰好推移了那些行号,现成的 drift 样本)。**已验证(本会话实测)**:`flutter analyze --no-pub` **No issues found!**;全量 `flutter test --no-pub` **4917 pass / 0 fail** exit 0(=上轮基线 4910 + 本批新增 7 逐值吻合);**CI 对 `1c6f5af8` conclusion=success**(两 job 全过:`macos-build` 含 format 门禁 + `test` 含带覆盖率跑测与行覆盖率棘轮,headSha 与本地 `rev-parse` 逐字符一致);新增迁移测 5 例 + chip 测 2 例(该 chip 此前**零覆盖**);**3 次变异全部精确命中**——段 9 改按当前总点数 → Actual 正是预测的 `jueShi`、chip 回退 `a.total` → 红、段 9 顺手清零 bonus → 「零数值影响」守卫红(证明该守卫非空转)。**过程教训(已复用防幻觉守则逮到)**:首轮全量写成 `flutter test | tail -30`,harness 报 exit 0 但那是 `tail` 的码,真实是 4916 pass/**1 fail**,且失败详情被 tail 截掉只能整轮重跑——管道后别信退出码,要退出码就别接管道。**BACKLOG 变动**:#15/#16 销账删行;**「资质视觉档位化」此前只寄生在 #16 正文里,两条一销会连带丢掉,已单独立行为一#19** 挂到试玩局(与 #4/#5/#6 同属「卡在没人真玩过」一批)。
> **2026-08-08 下午:P8 扫描器真 git fixture 测试层 + P9 `/afk` 门禁三缺口(merge `b6792e33`)**:上午交接后按只读契约开局,用户点选「先做不用拍板的两项」。① **P8**——既有 `tools/test_doc_link_scan.py` 10 例把 `git_ls_files`/`git_check_ignore` 全 mock 且临时目录无 `git init`(`test_doc_link_scan.py:32-40` 现读实测),而 P6 查出的两个假阳恰活在这层,沿用同体例加样例是假绿;故新建 `tools/test_doc_link_scan_gitfixture.py` 真起临时 git 仓(只替换 `REPO_ROOT`,两个 git 调用原样真跑)共 **15 例**:harness 自证 3(结论在 mock 体例下得不出)+ 当前正确行为回归 5(含两 bug 各自对照组)+ 已知假阳 7。**机制先做隔离实验**:`.gitignore` 写 `build/` 时 `git check-ignore build` 在目录不存在时 rc=1、存在时命中,带斜杠两种都命中(git 2.50.1 实测),Bug A 与 A2 修法同时被独立复现。4 条捕 bug 用例用 `expectedFailure`,**破坏证红三态实测**:当前 main exit 0 `OK (expected failures=4)` / 打上 P7 补丁 exit **1** `FAILED (unexpected successes=4)` / 还原 exit 0 —— 即 P7 一合并套件会自己报错提醒删装饰器。既有 10 例两状态下均 OK(解析层零回归)。顺带订正 `tools/README.md` 对整个工具的「任何 worktree 结果一致」自述(证伪范围仅限 `check-ignore` 一步,`collect_scan_files` 的同义注释确实成立故不动);「可试用·非终审事实源」定位属 🔴 未动。② **P9**(在 `~/.claude`)——缺口① 待拍板只认分支 tip `[BLOCKED]` → 新增 `is_pending_decision`/`pending_decisions` 同时认池内「待拍 / 用户拍板」,分支型仍致命、池内型只提示(否则池里长期挂一条待拍就再也开不了工),真正拦人的是新增致命检查 `tasks_awaiting_decision`(本批任务自己卡拍板);缺口② `approved_tasks` 用整池文本子串匹配(`P4` 命中 `P40`、条目正文里的 `README` 也算合法 ID)→ 改**前导 ID 精确匹配** + `branch:`/`adhoc:` 逃生舱表达非池工作;缺口③ `doctor --json` 补透出 `dispatch_template`(注册表仍是唯一事实源),SKILL.md 同步 —— 此条**反转**了上午那笔「改文档说去 executors.json 找」的处置。afk 套件 19 → **34 例** exit 0,三次变异各自精确命中(4/4/1 红)。**已验证(本会话实测)**:主 checkout 合并后双套件 10/10 + 15/15 exit 0、扫描器冒烟 exit 0(`refs=7442 alive=5911 dead=939 ignored=592`,与 P7 提案基线 7440/5908/940/592 差 2 条引用是交接新增两份 `docs/sessions/*.md` 所致,非行为变化);preflight 端到端 —— 开 P8 PASS、开 P7 因卡拍板 BLOCKED exit 1、`blocked_decisions` 现报「池内待拍板=P7」(原症状是报「无」)。**0 个 `.dart` 改动**(`git diff --name-only main..分支 -- '*.dart'` 实测)故未跑 Flutter 测试,守 §8.0 v1.29。**当时待用户拍板**:P7 补丁合不合 / 资质三连(BACKLOG 一#15 + #16 + 视觉档位化)/ C2b 四图。 **【2026-08-11 续:P7 合入 + 底账同步 + 资质三连 Phase 0】**(本 session 跨 08-08→08-11,上段 P8/P9 在 08-08,本段在 08-11;文档里若干原标 08-08 的条目已按 commit 实际日期订正) ⓐ **P7 假阳修复用户拍板合入**(`ab38b43c`):**协调者独立实测非采信提案数字**——修复前主 checkout `dead=939/ignored=592`、fresh worktree `dead=956/ignored=575`(**漂移 17 当场现形**),修复后同一份代码两地逐值相同 `refs=7442 alive=5929 dead=908 ignored=605`(**漂移 0**),引用总数守恒、alive +18。两处修复各自破坏证红精确命中 2 条红线。同批把 P8 那 4 条 `expectedFailure` 转为正式断言(装饰器设计目的=补丁落地时以 unexpected success 逼人来删,职责已完成),类名 `KnownFalsePositiveTest`→`FixedFalsePositiveTest`。 ⓑ **底账同步**(`9311f1d6`):池销账 **5 条**(P3/P4/P7/P8/P9 **全是做完仍标「待发」**——preflight 刚被修得可信、喂给它的数据却是旧的);按池 header「水位 ≥3」补 P10/P11/P12;**checklist E 段 reconcile**(原 P3)逐条现查——BGM 实装 **11 轨**(原定 3 套)、战斗 SFX 6 类、UI SFX 3 类全在位且有 `lib/shared/audio/` 播放层,**另订正原文两处与实装不符**:「死亡」音 `sfxForAction` 头注明写 v1 不做且无 `battleDeath.mp3`、「7 阶递进」实为 `battleHit_<teamSide>_<slotIndex>` 队伍侧×站位变体,唯一真未完项=配音 0 段;补登记 BACKLOG 一#17 C2b 四图 / 一#18 README 定位(**两项此前任何底账都没有**,只活在早报里,故能挂两轮没人推)。 ⓒ **阶段性评估**(用户要求,评分表):工程执行面 **9.5/10**(analyze 净 / CI 对 HEAD success / 4 次破坏证红全中)、流程面 **4.5/10**(决策积压 7 项 3/10 为最大瓶颈、文档信噪比 4/10)。**两条新发现**:939 条 dead 里 **600 条(64%)来自 `docs/handoff/` 的 404 篇归档文档**(那里的引用本就指向当时状态,当清理清单会做 600 条无意义的活);`doc_link_scan.py:59` 的 `EXCLUDE_DIRS` **定义后全仓零消费**(排除项硬编码在 `collect_scan_files`)——两者合成滚动池 P10。 ⓓ **资质三连 Phase 0 完成、实装未开**(用户已升 xhigh 并拍板 #15 取(c) 按出生点数重算 / #16 取(b) 改显出生点数 / 视觉档位化并入试玩局):**推翻 BACKLOG 一#15 的一条前提**——原写「`attributeBonusFromAdventure` 近期才写活,更老存档可能为 0 而实际吃过奇遇,那部分仍略高估」**不成立**:写活它的 `08d18fb9`(2026-06-23)正文明写旧 bug 是「只累加 `EncounterProgress`,**从不改 `Character.attributes`**」,修复后属性分量与计数器是**同块同 `applied` 一起增**(`encounter_service.dart:354-360` 与 `:366`);全仓核查(POSIX ERE 重跑 + 纯字符串交叉验证,首次用 `\s` 写的正则是假阴性)确认**生产层改 `attributes` 仅此一处** → **`出生总点数 == attributes.total − attributeBonusFromAdventure` 对所有存档精确成立,选项 (c) 无误差**。其余实测:`.rarity =` 在 `lib/` 仅 `character.dart:220` 一处、生产读取仅 `lineage_character_detail_screen.dart:303` 一处、chip 分叉源 `:301-306` 传 `a.total`、模板 `strings.dart:798`、saveVersion `0.38.0`、**启动顺序 splash 先 `loadAllDefs`(`splash_screen.dart:61`)Isar 后开(`save_slot_startup_service.dart:17`)故迁移期 `GameRepository.isLoaded` 恒真**。**架构岔路已决**:不改 getter(① `character.dart:71` 有 08-08 明确记录「不可改写成 getter」;② 将来若新增非奇遇属性来源,getter 会静默漂移而迁移后的存储值不会)。
> **2026-08-08 夜批(8h 挂机):稀有度出生锁死收口 + C2b 桃花岛美术闭环 + PROGRESS 瘦身(merge `4234eb7b`/`535d5cef`/`aea56f2c`)**:开局四项授权 + 稀有度四问一次拍全后开工。**已完成**:① **R 稀有度收口**——用户四问拍板(档位语义=**出生锁死**/概率分布**不适用**静态角色/展示面=档案页+招募候选卡/视觉保持临时版):移除 `EncounterService.rarityResolver` 与奇遇加点后重算,依据 GDD:224「不可重 roll·出生即命运」与设计理由「让投胎本身具有意义」;**原实装的实际后果本会话实测**——祖师属性两条来源都落在资优:`masters.yaml` masters[0] 为 5+7+5+5=**22**,`founder_creation.yaml` `fatePool` 六个命格选项为 `[21,21,21,21,21,22]`(`master_builder.dart:44` 取 `attributeProfile ?? def.attributeProfile`,选命格则用命格);两者吃满生涯 cap +5 后**一律钳到「绝世」**(GDD 标 2%),档位失去区分度。(顺带核过:rarity 与 attributes 取同一个 profile,不存在「属性用命格、档位用祖师」的错配;生产层 rarity 赋值恰 3 处全已派生,其余 6 处在 `lib/features/debug/` 非生产。)新增红线测(总点数 22 加 1 点到 23 跨档,rarity 必须停在资优),**破坏证红精确命中**(植入重算 → 实际值 `tianCai` 正是预测漂移目标档,其余 31 条不受影响)。numbers.yaml 该段注释块**已 drift**(仍在描述修复前的「标签未派生」状态),一并改写为拍板后实况;招募两处候选卡加资质展示;Gate ⓐ 自查逮到自己把全角括号散写进 widget → 收进 `UiStrings.rarityTotalParen`。② **C2b 桃花岛美术闭环**(codex):补 D 类 2 张 2448×1224 扩幅背景候选 + C 类 2 张逐图自测 + 逐图**建议**判定表;协调者独立复核规格(PIL 实测尺寸/比例/md5 与执行端自报逐项一致,C 类 md5 与上一单 MANIFEST 相同证实未重出)。**未能独立验证一项**:报告自曝「原生 1774×887 等比放大到 2448」,我的频谱法**自校验失败**(已知 1456 上采样对照组反而高于已知 1774 对照组,该指标不随源分辨率单调)→ 如实标未验证,有效细节宽约 1774 高于现役 1456 但短于规格 2448,**采用权在用户**。③ **P5 PROGRESS 瘦身**:110 行 79623 字节 → 100 行 57425 字节(−28%),压缩 **11** 条旧条目为摘要(全文见 git 历史),最长单行 5043→4800。④ **P6 死链扫描器标注验证**(pi):90 条分层抽样(dead 40/alive 40/ignored 10,独立 git 三查标注不读扫描器判定)→ **precision 95.0%(38/40)、recall 100%(38/38)、一致率 88/90**,并查出两处系统性假阳。**协调者独立复现两个 bug 全部成立**:**Bug A** 裸目录引用剥尾斜杠后 `check-ignore` 对目录型模式失效——实测主 checkout 有 `build` 目录故无斜杠查询命中,而 fresh worktree 无该目录则**不命中**、带斜杠才命中,即**扫描结果随工作树漂移**,直接推翻 `tools/README.md` 自称的「任何 worktree 结果一致」;**Bug B** `:Symbol`/`:行号+内容` 后缀剥不掉——`data/numbers.yaml:130 combined_rate_cap: 0.95`、`sect_screen.dart:_MemberRow` 被判死链而基底文件实测全部 tracked 存在。结论:作为**分类底账**可升终审(假阳全可枚举、各 1 行可修),作为**修复清单**暂不能(17 条 dead mdlink 仅 1 条真死链)。⑤ **P4 审计脚本入仓**(qoderclicn):Q2/A1 的临时脚本从 `/tmp/` 救回并规范化入 `tools/audit/`(9 个脚本 + `run_all.py` 统一入口),**五个计数从此可一键复跑**;协调者独立复跑 exit 0,并在另一 worktree 跑同一份脚本**逐行结果相同**(ROOT 从脚本自身位置派生,与工作树解耦)。**两处计数偏差同源且合理**——`Q2 背离 8→7` 与 `A1 只写不读 44→43` 都因今夜稀有度实装:B1「六档稀有度概率整段失效」已被修掉、`Character.rarity` 现有 4 处生产读取,**即审计脚本独立检出了本批实装恰好销掉它自己一条结论**。另订正两份报告全部 `numbers.yaml:<行号>` 引用(不止池里记的 `:1917→:1958`,`rarity_distribution` 942→966 等全表重定位;协调者抽验 4 处正向 grep 全部精确命中)。⑥ **补前夜三处盘面缺口**:Q2/A1 审计报告已合入 main(结论抽验 3 条全中);`/afk` 工作流 v2 脚本层落地 `~/.claude/skills/afk/scripts/`(preflight 9 项门禁 + doctor + 19 条测试)+ `~/.claude/automation-playbook/executors.json` 作执行端唯一事实源;`~/.claude` 已纳管。**已验证(本会话实测)**:合并后 analyze **No issues found**;全量 `flutter test --no-pub` **4910 pass / 0 fail** exit 0(=上轮基线 4903 + 新增 7 逐值吻合:rarity 配置测新文件 6 + 出生锁死红线 1);两分支 merge-tree 各零冲突;删分支前三验全过且远端留备份。**工作流 v2 首跑发现三处门禁缺口**(已记账):preflight 的 `blocked_decisions` 只认分支 tip 的 `[BLOCKED]` 前缀,故两个卡用户拍板的分支被漏报为「待拍板=无」;`approved_tasks` 用池文本子串匹配,在途分支收口类工作无法表达;`doctor --json` 不透出 `dispatch_template` 字段(实际在 `executors.json` 里)。**已知风险**:⓪ **出生锁死的后遗症(收尾自查发现,已入 BACKLOG 一#15 待拍)**——全仓 `.rarity =` 只有 `character.dart:220` 一处在 `Character.create` 内,**加载时不重算**,故 08-08 之前存档里的角色永久停留旧写死值 `biaoZhun`(改之前那版跟随总点数漂反而会在吃一次奇遇后自愈)。**二次审计后严重性下调=纯展示零数值影响**:唯一真实读取点是 `lineage_character_detail_screen.dart:303`,两个招募候选卡从 `AttributeProfile` 现算不读存档,`RarityTier` 在战斗/成长/掉落层零消费。本夜零迁移零 saveVersion bump,三处置选项与误差见 BACKLOG(读码推断,未拿真档验证)。**同一 chip 另有新档也中的问题(BACKLOG 一#16)**:chip 拼「存储出生档位 + 当前实时总点数」,锁死后两者按设计分叉但拼法在暗示对应——祖师出生 22(资优)吃满 +5 显示「资质 资优(27)」而资优区间是 21-22,与 defer 的视觉档位化同一决策面;① C2b 四张图与稀有度 UI 均待用户真机目检定谳;② rarity 分支中间 commit 消息仍写「奇遇加点后同步重算」,已被本批反转,读 git 历史时以最终态为准。 **【同日上午续:清夜批遗留】** ⓐ **补齐昨夜欠的 §8.2 视觉 smoke**——资质 chip 1280×720 真机图拿到,渲染与数值均正确(云寒青 6+5+4+3=18→「资质 寻常(18)」、柳拂陲 3+5+7+4=19→「寻常(19)」,单行不溢出),证据存 `~/Desktop/Projects/挂机武侠素材/资质chip视觉验收_20260808/`。ⓑ **推翻昨夜对 capture 失败的误诊**:当时猜「显示器休眠」并据此写了 `caffeinate -u/-d` 修复,今日两条假设**全被证伪**——`pmset -g log` 实测 02:00-02:30 **零条 `Display is turned off`**(全天最早一条是 10:54 排查时我自己造的),且对照组(手动睡屏+**未修版**)照样出图(脚本自身 osascript 置前瞬间唤醒,off→on 同一秒);「后台执行拿不到 GUI session」亦证伪(屏亮时后台跑正常出图)。**修复已 revert 不合**(治的是不存在的病),两条证伪路径连证据写入 BACKLOG 二#11 防重走,根因仍未立(剩锁屏一条存疑)。ⓒ **修 `/afk` skill 派单包缺陷**(昨夜 P6 越界事故根因):禁区从「协调者须知」升级为 **Phase 2「派单包必含清单」5 项**并附事故实录,同时订正 skill 里两处错描述(`dispatch_template` 实在 `executors.json` 不在 `doctor --json`;预热漏做的症状=一片 `.g.dart` 读取失败);skill 19 条测试仍全绿。
> **2026-08-07 夜 收账批:K2 假绿抽查 + L1-D 扫描器根因修(两分支 Gate 全过 · merge `24fcebd7` + `d1a8ab37`)**:①**K2**(kimi 12 commit / 12 测试文件,纯 test/ 不碰生产)逐文件 targeted 复跑 **12/12**(各自 exit 0 且 `All tests passed` 恰出现 1 次,非批量假绿);分支原带 analyze **2 info** —— `skill_drop_result_test` 去 const 触发 `prefer_const_constructors`,但**去 const 本身是正确的假绿修复**(const 规范化致两侧同实例、`identical` 短路字段级 `==`),故补 `ignore` 保留语义而非退回 const → analyze 0;**破坏证红 ×2 独立复现**:`battle_ai` 集火回落血最低 → `coop_chain` 正确性测红而确定性测仍绿(证判别式设计有效,确定性对「恒不集火」同样成立)、`wardMultOf` 恒 1.0 → `guardian_ward` 7 条红含 e2e 135 vs 900,而 `wardMult=1.0` 用例**不红**(证多值参数化必要,单点 0.15 会漏);基线漂移查:main 侧对 `test/`+`lib/` 零改动,结论未失效;全量 **4903·0**。②**L1-D 根因**:主流程取 `ref["raw"]`(mdlink 存的是 `[text](path)` 带方括号)→ 命中 `_RE_TEMPLATE_BRACKET` 判模板占位 → **所有 md 链接静默漏扫**,采集的 `target` 字段全程未用;交付自报「678 死链」正好落在验收区间内,**总数对了一整类功能是死的**。修法=改用 `ref["target"]` 且**正则不放宽**(反引号内真模板占位仍须跳过)+ 补 10 类固定样例测试;Claude 独立复跑数字逐项吻合(引用 7079 / 死链 706 / ignored 572 / `template[]` **49→4**),沙箱破坏证红改回 raw → 恰 4 条红且全是 md 类(反引号/模板/相对路径保持绿,证作用域精确)。**但底账可信度未证**:10 类样例只覆盖解析层且 mock 了 `git_ls_files`/`git_check_ignore`,全仓 precision/recall 未测,`tools/README.md` 已标「可试用·非终审事实源」,标注验证入池 P6。③**C2 美术口径订正**:23/25 已持久化出仓 ≠ 验收完成——清单全条目仍「待选」无逐图 PASS/WARN/FAIL、C 类 2 张缺执行端自测(自测报告原写「0/待开始」与目录实况漂移,已订正为 2/2 并标主观项未判定)、D 类 2 张未产出,**验收未闭环**。④**工作流治理**:`/afk` skill 此前只内联 2/6 端命令、缺的 4 端外置到桌面文档,致 qoderclicn 被 `which qoder` 误判「未安装」(实际在 `~/.local/bin/`)—— 已内联六端矩阵 + 加禁猜条款 + 桌面总账改软链消灭双副本,根因沉淀 memory `feedback_identifier_from_source_not_guessed`(高频硬参数外置到长文档 = 结构性诱导编造)。
> **2026-08-07 挂机批(五端并发)+ 中止复盘(`00469c57..4fa7c854`,26 commit / 4 merge,CI `4fa7c854` success)**:8 单派五端(codex/kimi/pi/qoderclicn/codebuddy),**5 交付 3 未复核 2 中途停**;侦察阶段推翻三个任务前提(C2 背景图已是成品且被测锁尺寸、热区图标位仅 18-21px 塞不下立绘;L1「1092 死链」含 398 条 gitignored 证据索引+29 条 build 产物假阳性+迁移账本自毁风险;新增 Q2「配置声明 vs 生产硬编码」审计维度)。**已完成**:合入 L1-A/B/C 文档路径修复三批(12 处改路径 + 14 文件 spec 修 + 63 文件归档档头)+ N1 numbers.yaml 4 段字段处置注释;新增 `docs/PATH_MIGRATION_MAP.md`(旧→新路径全表,11 条新路径 + 7 条已删目录逐条实测)与 `docs/spec/rarity_wiring_gap_2026-08-07.md`。**已验证(主 checkout 实测)**:analyze 0(4.8s);全量 **4886 pass / 0 fail**(exit 0;= 上轮基线 4886 + main 侧新增 0,新增 6 测在未合的 rarity 分支);CI run 31169857460 对终态 SHA success。**已知风险**:① 三份审计交付(Q2 背离 8 / A1 只写不读 44 / L1-D 扫描器)**数字全未复核、脚本未入仓**,A1 结论已被稀有度分支基线漂移;② **L1-D 缺陷实锤不可合**——md 链接存 `raw="[text](path)"` 而主流程 `:477` 拿 `raw` 清洗,带 `[]` 判模板跳过致整类漏扫;③ 6 个未合分支续传(见 session 记录)。**下批建议**:拍稀有度三问(卡住 `worktree-claude-rarity`,该分支 analyze 0 + 全量 4892/0 + 破坏证红,并含 `Character.rarity` `late` 无默认致 65 处构造点崩的真 bug 修复)→ 修 L1-D 根因 → Q2/A1 脚本化重验。用户外评 67/100,三条教训入 memory(`feedback_negative_grep_not_proof_of_absence` / `feedback_wip_limit_over_executor_utilization` / `feedback_scanner_acceptance_needs_fixture_cases`)。

> **2026-08-07 两批已压缩归档**:夜批收账(C1 题字清查 20 张双验证复检 20/20 + K1 技术债 Random 注入化 15 位点/契约测 5,两分支合 main `c7183303`+`35f2229b`;工作流四大机制入 memory 并封装 /afk)+ 午后五端矩阵首跑(pi PI1 yaml 消费扫描 668 文件/25 疑似 + kimi P1 资源总览接 MaterialSourceSheet,`789d0f4d`+`3e8b94fe`,全量 4886/0;执行端报告归位 `docs/dispatch/reports/` 立体例)。
> **2026-08-06 第八阶段两批收口 已压缩归档**:切片 1-3 协同 Boss 机制 Gate 合入(merge `714e68a0`)+ 切片 4-5 塔 42 碎星拳圣实例校准(`8e689c79`/`727b794d`)——掩护重定向/护法合击落地,探针首轮抓到「仅相位蓄招=机制永不触发」死配置,加 CD 型 chargeSkillId 后三态梯度(满配 20/20·同阶 3/20·跨阶 0/20)一次成立;floor42 长期守卫 3 测;全量 4880/0;CI format 门禁事故修复后 success。详 git 历史。

> **2026-08-05 夜 第八阶段实装批切片 1-3(分支 `worktree-phase8-boss-coop` · commit `e25c3e35`,xhigh)**:承同日 spec 拍板,机制+合击+配置 schema 一次做全(切片 4-5 实例+校准留下会话)。**已完成**:① **§2.1 掩护重定向**——协同 Boss(新开关)蓄力中被破招命中 → 存活护法**血最低者**代吃+强制踉跄(基础窗口/基础减防,零新数值面),Boss 蓄力不断;AI 破招锁定与手动指定(`canManuallyTargetEnemy` 破招例外)两路径在 `_resolveAction` 结算层统一拦截;护法死光回落既有真打断;踉跄→集火链复用 `_pickFocusTargetId` 零新代码;代吃走正常闪避 roll(与既有破招 vs Boss 同口径零特例)。② **§2.2 护法合击**——掩护相位双护法存活且未踉跄、每相位一次:先行动护法把两人**普攻**公式伤害求和一次结算(无独立倍率字段;attackResult 保真主发起者份+`coopStrikeTotalDamage` 合并总伤),目标=对面血最低(公开 `BattleAI.pickLowestHpTargetId` 防口径 drift);partner 拍被消费(AP-1000+`coopStrikeConsumedAtTick` 防同 tick 双花);Boss 两处进蓄力点重置相位标记;表现=合击题字+heavy hit-stop+关键帧评分+战报专句(battle_log 集中层)。③ **§2.3 配置**——`EnemyDef.guardInterceptsInterrupt`(**camelCase 沿 EnemyDef yaml 现状体例,spec 原文 snake_case 系抄 numbers.yaml 体例的失误已修正**);**复用既有 `chargeSkillId` 蓄招机制**(spec `coop_charge_skill_id` 语义与其完全重合,不造重复概念=守概念增量最小化拍板);fromYaml 联结校验 fail-fast(须配 guardianWard+蓄招途径,沿 vulnerability 开窗先例);`_enemyToBattle` 透传。**零配置回归**:开关默认 false,既有 4 护法层(14/21/28/32)零行为变化,零配置路径 rng 消费逐字节不变(两分支均 gate 在开关)。**已验证(本会话实测)**:analyze 0(11.9s);新增测 18=机制 13(`phase8_coop_guard_test`:重定向不变式/集火衔接/死光真打断/手动路径/守恒 2×单发/每相位一次/击破退化/踉跄压制/新相位重置/零配置×2)+校验 5(`enemy_def_guard_intercepts_test` 红绿双验);battle 域 866 pass+data 458 pass;**破坏证红 ×2 各精确命中**(重定向哑火→恰 2 红 guardIntercepted 断言;合击 total ×2 膨胀→恰 1 红 Expected 3600/Actual 7200),各还原复绿;批末全量 **4876 pass / 0 fail**(=基线 4858+18 逐值吻合,首跑即绿)。**已知风险**:① 合击附带效果从简(不施内伤/反震/破招副作用,partner 份产气不结算)=「少于正常两拍」保守方向,校准阶段若压迫感不足再评估;② 表现层题字/hit-stop 为代码接线,真机观感待切片 4 实例落地后一并目检;③ 集火衔接测的目标锚在「代吃者=血最低」下与默认选目标重合,辨别力弱(重定向本体已被 2 红锚死,集火是既有机制自有测试)。**下批建议**:切片 4-5(塔 35-49 段选 1-2 大 Boss 层配置实例+balance_simulator 画像×20 组难度闭环,沿 floor32 校准体例)。

> **2026-08-05 晚 BACKLOG 一区拍板批(commit `043d2ac8`+`4fdb4fb3` 已合入 push,worktree 三验后清)**:一区 8 条逐条过菜单;用户「工作量无关」质询实锤两处省力伪装并修正(#11 撤倾向+清查真范围订正 16-20 张含 narrative_stage 逐张确认;#13 理由从「省一轮回归」改为 isar fork 兼容风险排序),#3 补「本会话谈」选项;后用户拍「全部按推荐执行」。落地:#7A→CLAUDE v1.42 §12.2#5 归档行双倍率订正(retreat 1.3+经验专用 1.65+passive_idle 1.6,numbers.yaml:1236-1237/:1275 现查)销账;#12A→百草岭 9 立绘拍留+`character_avatar.dart` standee 表注记防「零引用悬案」重查(纯注释零行为)销账;#13A→一区移三区#3(再开条件=windows-2022 退役/audioplayers 需求/isar 依赖批合并做);#3→第八阶段设计收口(同日晚 xhigh 讨论):a(掩护-蓄力:Boss 蓄招护法代吃破招+踉跄集火链)+d(护法合击:两公式伤害合并结算零膨胀)融合拍板,范围 spec 落盘 `docs/spec/2026-08-05-phase8-boss-coop-guard-charge-design.md`;b(连环窗口·观战本质错配)/c(敌方集火·纯加压无新意)进 rejected registry;一区#3 销账转四区实装批(预估 1-2 专注会话+xhigh)。#4/#5/#6(真玩手感)+#11(观感终拍)无推荐项继续挂。验证:build_runner 128 outputs+analyze 0(12.9s,worktree 实测);BACKLOG 40 行 ≤80。

> **2026-08-05 webp 压缩批(commit `7af8ab9e`+`846f0127` 已 push)+ N2 远征①A 拍板**:18 张真 PNG 转 webp 省 19.9M——commit A=批 C 塔 Boss 8 张+战斗样板 sample 6 张,commit B=白布+我方三立绘 4 张**独立 commit 保 revert 线**(晚间目检否决时由我代回滚,revert 视觉 commit 会碰二进制冲突);3 张 ≥300KB equipment 评估未翻转、110 张池稳定,`git status` 恰 18 零杂散。验证:alpha 逐像素 Δ=0×18 + rgbMax=255 探针定性透明边缘毛刺(alpha≤46,实心区 ≤51)+ 临时探针 18 测绿+破坏证红闭环(跑完已删)+ analyze 0 + targeted 2/3/21/45 全绿(纯资产批按 v1.29 免全量);worktree 三验删,四侧全清。N2 拍板:①A 维持 0.25(ceil 后整数件已进位;0.5 有中期 exp 效率越位险);②终拍 A 维持 [20,40]——模拟(5 画像×20 组·全程带伤推进)证伪战败墙:一流 ceiling 起全 20/20 零减员,40 深=纯时长墙;欠配一流 5/20 属超纲正常信号(数据 audit 同报告)。N2 全销。**同日晚目检定谳+站位批(`dc74f43a` 已 push)**:白布+三立绘用户真机目检**全过**,两条 revert 线作废、BACKLOG 一#14 销账(二弟子接地偏浮随定谳接受);随批用户拍板我方三人站位均匀分布——`battle_stage_geometry.dart` slot1 锚 x 0.283→0.241(间距 0.173/0.089→0.131/0.131,y/scale 纵深不动),geometry 测试钉值同步,targeted 单文件 8 pass+analyze 0,真机重编复核用户拍「满意」。夜批收账线全清,唯一开放项归零。
> **2026-08-05 夜批收账(评分 94/100)+「全部按推荐执行」批已压缩归档(merge `aa378d71`/`00e2edd4` + `3a82abac`/`4d59726b`)**:① N1 分支合入——capture 窗口错截根修(`window_id.swift --pid` 绑窗 / 杀净确认升级 SIGKILL / `stop_pid` 替裸 kill)+ N2-N5 四报告入 `docs/audit/`(收账时逐份独立复测吻合);② songguan 白布返修**代终拍 7/7 PASS**(枪尖压下实测 6-10° 弱于字面 10-15° 但「可辨压下」成立,已注记),PIL 三方复测与 targeted 33 逐值吻合;③ **4A 远征整数件奖励摘出 round 吞没**(物品/装备改 ceil 使 cycle2 1→2 真增益,连续量维持 round 保 EXP 口径零变),破坏证红精确命中反吞没断言;④ **3A 我方 3 张战斗立绘重出接线**(codex image_gen 一次过,PIL 三验逐值同,冷色相实测 32.1%/23.5%/31.4% 全过 ≥15% 门槛,三层旧画布补偿清零走默认),`revert 4d59726b` 即回旧三张;⑤ 账本批改(BACKLOG 二#8/#9 销行、二#10 证伪墓碑=错拍非 seed 漂移)。批末全量 **4858/0**(=批 C 基线 4854+4A 新增 4 逐值吻合)。**已知风险**:白布+三立绘为代终拍待用户真机定谳(两条 revert 线在案);二弟子后位接地偏浮 ~7 逻辑 px。详 git log。
> **2026-08-04 批 C 8 张新 Boss 立绘 + 塔 49 层/周目 UI 视觉 smoke 已压缩归档(PR #117 `2e52ae1c` + PR #118 merge `6cf4d5c3`)**:8 张立绘 codex image_gen 一次过(塔 18/21/25/28/35/39/42/46),形象逐张取自塔叙事启幕文案,**毒手药叉/无相神尼为全库前两张女性立绘**(库内零女性锚,走文字画风锚);批次尺度归一(bbox 纵跨 1410/fraction 0.963 全批一致,防「矮一截」坑)。接线:towers.yaml 8 处 iconPath + `character_avatar.dart` identity 8 条 + 脚底 fraction 8 条(PIL 逐图现测与出图端自报逐值一致);文件名改语义拼音**不沿旧层号体例**(新层号 25 会与旧剑魔文件撞车)。视觉 smoke 14 张 1280×720 + 周目选择区专属 debug route 2 条补 smoke 盲区。用户终拍 8 立绘 + 16 smoke **全 PASS 放行**;复核会话独立 Gate 后合入(自报「webp 5」实测 2,一处计数 drift 订正),**塔 49 层三批全收口**。新挂账:我方 3 立绘 vs 新 Boss 色调代际差 → BACKLOG 一#14。详 git log + PR #117/#118。
> **2026-08-04 批 B 周目语义修正(4 支线入口境界段推进)已压缩归档(commit `075b5346`/`ae0dbe52`)**:B1 配置 `cycle_evolution.realm_advance`(tiers_per_cycle 3/max_cycle 3/margin 1/reward_bonus 0.25/远征里程碑 [20,40],后四值用户拍板)；B2 三轴消费(effTier 一处计算三处消费:境界内力派生/防御率档/差距修正,入口白名单 {lightFoot,massBattle},主线塔心魔零回归)；B4 断魂庄/远征**从零建周目**(默认值语义自洽故零迁移零 saveVersion bump,旧档通关态读侧派生),奖励乘数 ×(1+0.25×(cycle−1))；B5 `CycleRealmGate` 纯函数(顺序∩cap∩境界门槛),**远征无终点故解锁改绑深度里程碑**；B3 三系锁死经摸底证实掉落基准阶全锚静态配置,结构性满足 §5.3 + 守卫测钉死；B6 **净威胁实测 ×5.11**(敌单发 ×9.51/玩家 ×1.86,坐实 spec 解析 ~2.4 且更强,批 A 挂账②销账)。**过程中自证零负载否决第一版探针**(整场承伤口径 c1=c2=0 假绿,满配 2.3 tick 秒杀敌无出手机会 → 改单发交换比)。全量 **4854/0**(=批 A 基线 4813+新增 41 逐值吻合)+ 破坏证红 6 轮各精确命中。详 git log。
> **2026-08-04 批 A 塔 30→49 层 1:1 锚死重排已压缩归档(PR #115 merge `ce114bac`,承 A0 PR #114 `e36884f9`)**:towers.yaml 全重写(1377→2438 行·49 层·116 敌条目,floor N ↔ abs N 每 tier 7 层,曲线对齐主线同 (tier,layer) 实测中位,敌 atk ≤2000 / Boss HP ≤60000);Boss 位 14 个(tier 中点 minor + 末层 major),旧 6 Boss 机制原样迁移 + 新 8 Boss(立绘占位待批 C);28 篇塔叙事(12 迁 + 16 新写);断魂帖 16/33/49 + saveVersion **0.38.0**(bossKey 重映射防数据孤儿);塔势总览补 postFrame 居中滚动(旧「30 节点落安全区」前提在 49 节点失效);validator Boss 位改公式派生 + requiredRealm 加载期校验。**机制型 Boss 血量-乘子联动校准**(原样迁参数两头失效):floor32 剑魔 55000×0.12 满配 0/20 全败(vuln bootstrapping 门槛随血 ×9)→ 定稿 40000×0.35+浅开窗 0.92;floor49 魔尊 0.20 反向失效(6 tick 速通门槛名存实亡)→ 收窄 0.10。EXP 探针重测 combat 6511→7444、终态 Lv130→134、idle 缺口 2668→1585。全量 **4813/0** + analyze 0 + 三机制诊断全绿 + towers.yaml 红线逐值独立抽查。详 git log + PR #115。
> **2026-08-03 批 D 爬塔扩展 spec xhigh 复核 + 8 项拍板已压缩归档(纯文档零代码,HEAD `36fe9d80`)**:Phase 0 全量重测(禁转抄起草版)与起草版逐值吻合;**起草版四处订正**——断档成因实为「每 tier 只取前 3 layer×5 楼」的系统性取法(照原思路扩层会在每个 tier 边界再生断档)、`0.05` 是输出乘子非承伤乘子、否决「70 层」改 **1:1 锚死 49 层**(内容量减半且断档由 6 归零)、方案 D 前提证伪。**两条改变问题定义的新发现**:(a) 境界差是**阶梯函数**(`derived_stats.dart:40` 查表,武圣对 xueTu→yiLiu 威胁全 0.05 零变化,「每周目抬 1 阶」前几周目完全空转,须抬 ≥3 阶;且 `diff_3_or_more` 兜底 1.0 比 diff_2 的 2.5 还低=曲线非单调);(b) 问题范围由「塔」扩为**5 个支线入口 48 关**,且断魂庄与远征连周目机制都没有。8 项拍板点用户全部按推荐通过。**过程中的自我修正(用户质询触发)**:首版 7 项推荐有 3 条为省工作量委曲求全(「避免范围爆炸」推 backlog / 照抄「70 层」未自算致贵一倍还留 6 断档 / 「够不上一个专批」绕开立绘),已全部修正并落 memory `feedback_no_effort_saving_in_recommendations`。详 spec `docs/spec/2026-08-01-tower-extension-design.md`。
> **2026-08-03 BACKLOG §二#6 前提证伪 + 顶栏键盘走查补证已压缩归档(PR #111 merge `dfafd4d9`)**:净树先证伪再动手——§二#6 两项挂账实测 tooltip 与「渲染器帧」两半条均 stale(`visual_capture.sh:202` 早已原生窗口抓图、4 份终拍日志全含 `window_id:`),Tab 半条是真缺口(顶栏组件从不发 Tab 不断言 focus,唯一那条 Tab+Enter 测作用于案台技能签)→ 补 2 例直证(焦点归属绑死子树,不用弱断言),整条销账。破坏证红 ×2 精确命中(skipTraversal→焦点归属断言;`onPressed ?? () {}`→禁用态断言)。同日续批:`avatar_status_tags` 两处整洁(级联共享 Paint 拆两支笔、死字段 color 砍掉),**像素哈希三点证明零像素变化**(fnv 3760436573 前后相同 + 对照组 4148465193 自证探针有负载);过程中逮到自写假绿——`shouldRepaint` 恒 false 致同型 widget 换参不重绘量到陈旧像素,加「先 pump 空树」后证红成立。关键判断:不能把 spec.color 接进 painter(深底亮金 vs 宣纸底旧金,守 `feedback_paper_vs_dark_text_color_palette`)。详 git log + PR #111。
> **2026-08-02 战斗界面样板还原度 95+ 独立终审 + 合并已压缩归档(merge `c9a7fdc2`)**:用户终拍通过(G/P/F=95)后按 §8.2 做**不采信外部结论**的独立复核。四项视觉:护法结界改旧金干笔题签 + 贴身断毫;高阶血/真气保留精确值仅降上限视觉权重;三张空签改未题字旧签;塔境加强下部接地墨层。全部本会话实跑:analyze 双 0 / targeted 12 文件 217 / 全量分支 **4802** + 合并后 main 再跑 4802 / macos build ✓;**合并后 main tree hash 与分支 tip 逐字节相同**(`e97faf0e`)故推上去的就是审过的树;四项逐像素在终拍截图上坐实不靠报告转述。**已知风险**:`fidelity_manifest.json` 记 `commit: 558885f7` 而实装是 7 分钟后的 `680ea698`——capture 只记 `rev-parse HEAD` 不记脏树标记,manifest 无法自证截图对应哪份代码(当日即实装 tree-hash 修复 `d307f9a2`,schema 2→3 加 `tree`/`dirty`)。**复现项目已知坑**:`flutter test` 传 12 个文件路径时 `battle_layout_metrics_test.dart` 被**静默漏跑却仍报 All tests passed**,验收须逐文件确认出现次数。详 git log。
> **2026-08-01 挂机批已压缩归档(main `b2335f2d`)**:A 合 #110 收尾;B audioplayers 升级证伪(真闸门=Flutter SDK ≥3.44,条目移 §一#13);C/D 主动让出(Codex WIP 报告推翻 R2 口径);E 爬塔扩展 spec 起草(草案,已被 08-03 批 D xhigh 复核取代);3 处 doc drift 登记。详 git log + docs/sessions 2026-08-01。> **2026-08-01 守卫扩覆盖 + 断魂庄 audit 路由两批已压缩归档(PR #109 merge `78817891` / PR #110 merge `afbf52f4`)**:① 规模类文案扩真相源守卫(BACKLOG §二#10 销账),新增 group C 六例 + 「每章五关」断言,期望值全从 `loadAllDefs` 派生。**两处现测偏离上批自写的审查报告**:报告列 11 处实为 **13 处**;断魂庄取 `stages.length` 而非 `supply_cap`(恰都是 3 但无因果)。新增 `scaleToken` 数字边界匹配防子串漏网——实测 `'15 关地形'.contains('5 关')` 与 `'心魔十七关未启'.contains('七关')` **均 True**,纯 contains 两处都假绿。② 断魂庄敌立绘补 audit 路由(§二#5 前半销账):7 张立绘**从未进过任何一轮目检**(敌队不在 `stageDefs` 里,既有 audit 够不着),新增 `battle_audit_gauntlet_01..03`;覆盖测重点是逐关次跑生产场景工厂取 iconPath 求并集比对,漏一张即红。**远征那半前提证伪未照做**(战斗完全 headless 零渲染路径)→ 转 BACKLOG §一#12。破坏证红 ×4/×3 各还原复绿;CI 全量 4786→4792 逐值吻合;合并态 tree 与 CI 所跑 tree 逐字节相同故未无谓重跑。顺带发现:断魂庄是全项目唯一不叠场景美术的战斗但画面不残缺(程序化层全开满强度)。详 git log + PR #109/#110。
> **2026-08-01 账面订正批 + 阶段性全面审查 + 工作区清理已压缩归档(PR #108 merge `42d7e00c`)**:① 章数文案 drift 订正(`mainlineRouteMapSubtitle` 写「十六章」而实测 **21 章 105 关**,玩家可见文案落后 5 章)。**根因是两个缺口叠加**:守卫本就从 yaml 派生章数但只钉 `GDD.md` 不覆盖 `UiStrings`;而引用该常量的既有测试是 `find.text(UiStrings.xxx)`——**引用常量本身不钉字面量,故改章数永远不红**。破坏证红 ×2 还原复绿。② 两份账本对账,订正一处**我上批压缩时照抄的过期挂账**;BACKLOG 销两条 stale。③ 阶段性全面审查(`docs/audit/stage_review_2026-08-01.md`):无 P0,新发现 P1 = 规模类文案「写死数字+无守卫」约 11 处(当前值全对故非活 bug,风险是扩内容静默 drift)。**核对期间两处量测假报当场证伪**(`grep -c 'stageType: innerDemon'` 报 8 实为 7,第 8 个命中是注释行;towers.yaml grep 报 60 实为 30 层,双模式重复计数)。④ 工作区清理 13G→**2.8G**,跟踪文件零丢失、192 处文档截图引用零破坏;探针报「缺失 268」查为 `git ls-files` 对中文名转义所致,已入 memory。全量 **4780/0**。详 git log + PR #108。
> **2026-07-31 codex 战斗样板复刻批合并已压缩归档(PR #107 merge `cdb370e4`)**:20 commit / 40 文件 / +4403−1193。评审 R1 **82** → 派单返修一轮(耗气断言/冷却墨洗/蓄势横幅层级/铭牌墨染)→ R2 **98/100**。合并前 §8.2 Gate 四项本会话实测:ⓐ `lib/` 除集中层外新增中文 0 行 ⓑ 新增 debugPrint 0 处 ⓒ 6 张新 PNG 逐张查得引用且 pubspec 已声明(是美术资产非截图残留)ⓓ 20 条 commit 全中文动宾。**一处省事推理先做了实证再用**:分支 merge-base 与 main tip 之间 3 条 commit 经 `git diff -- lib data test` 实测为空 → 分支代码态==合并态,故分支侧数字即合并态数字(仍照跑一遍复核,未省)。CI 双 job pass;合并态全量 **4779/0**(=合并前 4762+新增 17 逐值吻合);四侧全清。动 worktree 前先定位评审证据在主 checkout 不在 worktree,确认删不会连带毁掉才动手。**仍挂账**:顶栏 Tab/tooltip 键盘走查无直证 + 方法学局限未前置到交付摘要(均已于 08-03 销账)。详 git log + PR #107。
> **2026-07-30 「按推荐执行」批已压缩归档(夜批 #101/#102/#103/#100 + PR #104/#105/#106 全合)**:① 夜批四 PR 按序全合(`62d169d5`/`b1781630`/`1af5efc9`/`6e9f2084`),逐条 merge-tree 复算零冲突且文件零重叠,合并态全量 **4734/0**。② **B3 立绘浮贴选项 B**(PR #104 `ef443e4d`):16 个背景的合成明度登记表(真机 19 张实测)+ 明度→强度斜坡(下沿 125/上沿 195);**自己第一版「原图明度代理合成明度」被自己证伪作废**(Pearson 0.68/Spearman 0.43 秩序错位,根因 cover 裁切+雾/暗角分布);取样带经跨数据集自校验选「紧邻右 x∈[0.74,0.86]」(平均绝对误差 5.9,候选带 18.3/45.5 且后者秩相关 −0.80),精度 ±9;融合物理自限省掉 5 个凭空 style 系数。③ **C5 行囊三格 + 桌面焦点落点**(PR #105 `4302bb16`):行囊 slot 0/1 旧版无条件写死展示**假游戏状态**→ 改空囊印记;桌面钉 `alwaysTraditional`(默认 automatic 致 autofocus 画不出焦点环)。④ **F2 证据补齐 + 订正夜批错配方**(PR #106 `31f1f9d7`):底层定时器实为 2200ms 非「4s/节拍」,**并逮到前 8 拍战场是冻结的**(beat 1 vs 7 立绘带逐像素差 0)→ 改可判读判据。破坏证红 ×4 全精确报站点;B3 真机逐像素定量对照(desert +13.2 单调,**alley/tower 变化像素 0 = 暗场景零回归直证**)。详 git log + PR #104/#105/#106。

> **2026-07-30 夜批四 PR + Ch20/Ch21 十张立绘目检 已压缩归档**(详 `docs/sessions/2026-07-30_0850_夜批验收线.md`):四条 draft PR(#100 桌面语义 / #101 论剑后人立绘修复 / #102 B3 评估 / #103)已由本页 07-30 拍板批全合,当时列的五条已知风险**全部销账**(#100 已合 / `autofocus` 焦点落点已拍板并随 #105 实装 / 四 worktree 已清)。Ch20/Ch21 十张真机战斗屏目检 **10/10 PASS**(题眼逐条现测命中),逮到 1 处门禁测逮不到的规格偏差(论剑后人两脚不共面、alpha 实测高差 **164px**)并当批修掉;同批清 `build/` 陈旧 dill **5.5G**(7.2G→1.7G)。**留作长期教训四条**:(a) 加几何判据前先量存量——「双脚共面」第十判据经实测 75 张 standee 里 **30 张**底部高差 >120px(多为前后脚透视深度差与幻境下半身消散的假报)证无判别力,**留目检不可自动化**;(b) 查到异常先翻 `BACKLOG.md`——伪书法/伪印章白查 25 分钟,§一#11 早已登记且朱红印章是主动要的母题;(c) `parseString` 是未解析 AST,`Foo(...)` 不带 new/const 解析成 MethodInvocation,只认 InstanceCreationExpression 会**一个站点都扫不到**;(d) `battle_standee_asset_role_test` 只验 alpha bbox 底沿,单脚悬空不改底沿故九判据全绿。**仍挂账**:送关旧部白布动势未修;**断魂庄/百草岭远征**敌立绘未纳入目检(该两处无 audit 路由——塔 30/轻功 5/群战 5/心魔专属路由 1 共 **48 关**已于本批目检零缺陷,连主线 105 关共覆盖 **153 关**)。

> **2026-07-29 主线终章闭环 + Ch20 三 PR 收账 已压缩归档**(commit 区间 `adb2df71..212abdd3`,git log + 各 PR body 可溯):Ch21「绝顶交程」终章(PR #96 merge `212abdd3`·cap 47→**49** dengFeng 绝对终点·末 Boss dengFeng·lingQiao·五关 HP 59500<60000 硬线·叙事 13 篇 6673 汉字·21_05 `surviveTicks: 10`·vuln 0.10·cycle2 **0.05**=§5.4 下界)+ Ch20「东入阳关」整章(PR #95·cap 45→47)+ Ch19 美术 11 图接线(PR #94)+ Ch19/Ch20 webp(PR #98 `461ef9f2`)+ Ch21 美术 11 图(PR #97 `5bad3ee6`)。**留作长期经验三条**:(a) **12 项逐条现测而非采信分支自报**——逮到 Ch21 装备掉落显著薄于 Ch19/Ch20(21_01 是全段唯一零装备关、末 Boss 唯一不掉神物),PR 内补齐(`f2c37a1e`·[balance]·零新内容,全复用既有 tier6 宝物与既有概率);(b) **CI `test` job 实为 format+analyze+全量 test --coverage+ratchet,故 CI 绿即全量绿**;(c) 破坏证红把 20_05 cycle 0.06→0.20 后 EXIT=1 精确指向 `stage_20_05`、Expected `<0.12>`/Actual `<0.2>`,还原复跑 6 pass。

> **2026-07-28 三批已压缩归档(武圣段 Ch19-21 spec 九项全 A 拍板 `docs/spec/2026-07-28-wusheng-arc-ch19-21-design.md` / Ch19「旧路照人」整章实装 PR #91 merge `0d5390cf` / Ch19 美术 11 图 #94 + Ch20「东入阳关」整章 #95 + 疗伤 flaky 修 #93 · 难度定调拍板 A+B 落段级 spec §12 `ecdbe4a4`)**:git log + 各 PR body + 两份 spec 可溯,均已合 main;要点=武圣段段构 Ch19-21·cap 42→45→47→49 封顶·Ch21 收官即开飞升·敌招零新增全取 tier7 闲置池·神物 11 件分三章投放;**攻轴封顶后难度定调**=主线不做 undergeared 战败威胁(难度语义走机制参与+时长+周目压力),威胁性内容归塔/心魔/断魂庄,Ch21 末 Boss 配 `surviveTicks` 换威胁轴;`vulnerability` 只减 Boss 受伤不增玩家承伤故「难度 100% 走机制层」只成立一半。**仍在账**:三弟子年龄矛盾已修(五十→六十上下);「破坏证红不能与在跑的全量并行」教训已入 memory。

> **2026-07-28 battle-ui-v2 阶段 5「全模式终验」收官(PR #90 **MERGED** merge `d3ff1212` · 生产改动 commit `57c6b164` · 已 push)**:1.0 打磨期悬置最久的视觉验收项销账,`docs/spec/2026-07-19-battle-ui-v2-85-fidelity-repair-report.md` 与配套 plan 头部状态均已改「已收官」。**已完成**:① **40 张证据截图**——24 图 S1～S12 双视口矩阵 + 12 图生产/特殊模式(`stage_01_03`/`stage_01_05`/塔 Boss 护法结界/轻功/心魔/群战)+ 4 图补充真 3v3;走 `visual_capture.sh --background` **后台抓图不占前台**(交接原判「必须真人独占屏幕」证伪,只 Task 5.4 终拍需真人);② **生产改动 2 处**——护法结界药丸 `WuxiaColors.internalForce`→`bossFrame`(前者是真气语义色 SteelBlue `#4682B4`,属 CLAUDE.md §9 禁的 Material 默认饱和色,挂 boss 头顶在水墨色板外多出高饱和蓝;**真气条继续用 internalForce 不动**——基准图真气条本就青蓝,非偏离;该 token 35 处使用点+Material secondary,改值属超授权重构故只改此调用点)、空技能签 `paper@0.38`/纸纹 `0.08`→`0.55`/`0.18`(旧值在深褐案台底被压成灰褐块,早期关卡 7 槽多空时案台退化成按钮阵列,触阶段 2 Gate 明文红线);③ 补 `avatar_status_tags` 视觉守卫测(该色此前零测试保护)。**已验证(worktree 实测)**:`dart format` 0 changed;`analyze` EXIT=0 **No issues**;targeted `avatar_status_tags` 8 / `battle_command_console` 44 / `battle/presentation` 281 / `guardian_ward_presentation` 14 均 EXIT=0;**全量 4712 pass/0 fail** EXIT=0(5m26s·基线 4711 **+1**=新增守卫测);**破坏证红(commit 后做)**——改回 `internalForce` 精确报 `+7 -1` 定位到该守卫测,`git checkout` 还原后复跑 8 pass 复绿;改后复拍 4 route 零黑图并逐图目检确认(ward 转深金/早期关卡 6 空槽与全空 7 槽均读作宣纸签)。**终验结论 94/100**(目标 ≥85):A 20/20 · B 22/25 · C 23/25 · D 15/15 · E 10/10 · F 4/5,A～E 各 ≥88%,一票否决 6 条通过 5 条(桌面语义未做专项量测但有实现+6 文件 7 处断言覆盖)。与阶段 4 自评 88.5 的差异集中在 A/D/E——本轮补上代码常量(A2)/场景 profile 实现(D3)/对比度代码层精算(E4)三项证据,C(23)、F(4)、B(22.5→22)两次独立评分高度吻合证评分模型可复现。**Phase 0 逮到 3 处文档 drift**:(a) plan Task 5.2 写「正式主线标准 3v3:stage_01_03」,实测 **90 个主线关卡 `enemyTeam` 长度分布 `{1: 90}`——主线根本不存在 3v3**(3v3 只在塔 11 层/群战 5 关/轻功 5 关),改用 `battle_audit_stage_mass_battle_01` 真 stage 替代;(b) 阶段 4 自称的证据目录 `battle_ui_v2_85/final/` 已随 codex worktree 清理蒸发,本轮重生成;(c) 截断的顶栏文本实为 debug route hint(`strings.dart:184`),不进生产。**未取的 6 分(逐项拒绝放宽判据)**:F2 缺 1(所有 visual route 冻结静态帧·`--wait 14/17/20/23` 实测仍停节拍 0·全仓无特效/动作 route,需 CGEvent 逐拍驱动或新建 route);B3 缺 3(`stage_01_05` 冷白背景致暖褐立绘浮贴,差距真实,但修法须改 `_mainlineSceneColorGrade` 全主线统一矩阵,波及 90 关已终拍美术);C5 缺 2(行囊三格是硬编码占位+Semantics 标「待装配」,属未实装功能预留位,改视觉无意义)。**A2 方法论**:图像分析三次均未过基准图自校验(暗缝低谷法/最大连续亮带法/宽度众数法),最终改读 `BattleLayoutTokens` 确定性常量解决——**能读确定性配置时不要从渲染结果反推**。**合并态复验(2026-07-28 主 checkout 实测·非采信分支自报)**:合并前 CI 双 job 全绿(`test`+`macos-build` 均 SUCCESS·headSha `37f3d396`),`gh pr merge --merge --delete-branch` 合入(merge commit `d3ff1212`·07:12:57Z);合并态 `flutter analyze --no-pub` **EXIT=0 · No issues found**(5.3s);合并态**全量 4712 pass/0 fail** EXIT=0(`All tests passed!`·`[E]` 0·失败标记 0·**4m37s**·main 基线 4711 **+1** 恰为新增守卫测)。**清理债零**:远端分支随合并删除(`git ls-remote` 只余 `main`),本地 stale remote-tracking ref 已 `fetch --prune`,worktree 与本地分支在上会话结束时已被 harness 自动回收 → 交接预留的「三验清理」本次不需要跑。**已知风险**:① 首轮评分 71 分系严重低估(其中 4 处为判断错误、2 处为重复扣分),v3 修正至 94,评分表 `SCORE_V2.md`/`SCORE_V3.md` 已逐项留改判溯源,后续复评应以 v3 口径为准;② 桌面语义(semantics/键盘/focus/cursor)未做专项量测;③ B3/C5/F2 三项差距仍在账。**下批建议**:武圣段 spec 起草(cross-tier·xhigh 专会话);三弟子年龄 1 句改;`build/visual_acceptance/battle_ui_v2_85_sample` 111M 已可回收。

> **2026-07-27..26 Ch18「阳关故人」+ Ch17「沙海纵深」六批已压缩归档(Ch18:内容 PR #86 merge `dfeff861` / 美术 #87 `1fe8ddd1` / webp #88 `d760cf99` / lore canon 订正 #89 `719fe4fe`;Ch17:章级 spec+清账 `67bff693`/`c94d2ee9` / 整章实装 --no-ff / 美术 #84 `f2135ca9` / webp #85 `80531d37`)**:git log + 各 PR body + 两章 spec 可溯,均已合 main;宗师段收官 + 第 2 章全链闭环(各 5 关·真解 Ch18 收编阳关无故人 6400 / Ch17 新写平沙落雁·cap 38→40→42 均 within-tier·**机制层首入主线**(17_05 vulnerability 0.20 教学 → 18_04/18_05 0.20/0.12 全机制)·叙事各 13 篇 6583/7370 字·美术各 11 图接线 known_missing 清零·webp 各压至 7.5%/7%)。**仍在账**:两章立绘均未真机战斗屏目检(alpha 定量 Δ=0 + standee 九判据 + 合成真底图三重托底);17_03 `biome: temple` 语义微偏(实测零 biome 门控故零机制影响);18_05 两相位总 tick 比 17_05 单相位更短(压迫感体现在残血 0.4178 而非时长,待真人试玩);Ch18 三弟子年龄弱矛盾已于 2026-07-28 拍板批修掉(五十→六十上下)。

> **2026-07-26 全局视觉收口两阶段四批已压缩归档(phase1 `57f12082`(111 文件 +5349/-958)+抽查修正 `8592154e` / phase2 `885edf01`(33 文件 +1648/-232)+抽查 8 route×双视口 16/16+`PortraitFrame` 松约束 no-op 修复 `f3e05056`)**:git log 可溯,均已合 main;边界=只修溢出/对比度/状态辨识/素材误用/术语一致性,不改玩法·经济·战斗数值;要点=paperSurfaceTheme/darkSurfaceTheme/WuxiaTypography 基础设施 + 正式战斗 portrait fallback 归 0 + 14 张物品 PNG 去棋盘底 + `docs/UI_TERMINOLOGY.md` 立规 + 术语双向排他门禁 + 脚底接触影实心核 + 状态墨拓归属指针 + `InkPageHeader`/`InkSectionLabel`/`InkListCard` 三共享件 + 断魂庄三装备专用详情图 + `asset_framing.dart` 焦点登记 + `equipment_detail_asset_quality_test` 双向棘轮。**仍在账(5)**:① `settings_panel`「68% 高度让滚动区提前滚」**未验到**(需 `settings_panel_bottom` route);② `inventory` route 落装备 tab,物品图棋盘底靠 `shop` 货架**旁证**非直证;③ 暴击色 `#B72218`→`#8A2B21` 非等值迁移(已实拍判读为沉稳绛红,可读);④ 抽查只覆盖 8 route、判读集中 1280×720;⑤ `battle_ui_v2_85_sample` 111M **已删**(2026-07-28 用户授权·整个 `build/visual_acceptance` 375M 一并清,本项销账)。

> **2026-07-26 生产随机源接线收口已压缩归档(PR #81·merge `2942af60`·批 commit `56f5c35c`)**:BACKLOG §一#8 销账——8 处生产 inline `DefaultRng()` 按「调用点能否拿到 ref」分两层清零(UI/flow 层 4 处直接 `ref.read(rngProvider)`;service 层 4 处改构造注入 `Rng`,沿 `MilestoneEquipmentGrantService` 既有注入体例)+ 新增 `rng_provider_wiring_contract_test` 三层契约棘轮 + 双破坏证红;git log + PR #81 body 可溯,已合 main·全量 4661/0·analyze 0。**仍在账**:`dart:math Random()` 签名约 15 处(含 `stage_entry_flow.dart:233`)需另立 Random 类型注入点;`MilestoneEquipmentGrantService` 两构造点拿不到 ref 仍走构造默认值。同批 A2 文档:BACKLOG §一#11 已补全库扫描数据(问题全封闭在 2026-07-02 单批早期 MJ 素材·07-18 后 52 张零问题),条目保持开启。

> **2026-07-24..25 宗师段(Ch16-18)spec 拍板冻结 + Ch16「凉州词」两批已压缩归档(spec `f8d52ae4`·main 直落·BACKLOG §一#9 销账 / 整章实装 PR #71 `a5d6ddba` / 美术 11 图接线 PR #72 `27b6d96d`)**:git log + `docs/spec/2026-07-24-zongshi-arc-ch16-18-design.md`(92 行·段级六项+Ch16 章级六项拍板全文)+ 各 PR body 可溯,均已合 main;宗师段首章全链闭环(5 关 stage_16_01..05·敌招零新增复用失传神功心法 9 门·真解「铁马冰河」·cap 35→38 cross-tier·16_05 相位 unlockSkillIds 主线首用·叙事 13 篇·~30 站点 reconcile·11 图接线+known_missing 清零),当时已知风险(11 图缺图 / 17M 待转码)已由 PR #77 销账。**仍在账**:① idle_horizon **s1 45.6 天/下沿 45 贴线**(Ch17 扩缺口必破须重校);② 16_05 相位配法与 Ch16 立绘真机战斗屏均未目检;③ Ch17/Ch18 章级细化未起(spec §8 前瞻已定向:cap 38→40→42·Ch17 末 Boss vulnerability 0.20 教学·Ch18 章中+末 0.12 全机制·真解 Ch17 新写 / Ch18 收编 `yang_guan`)。

> **2026-07-24 Ch15 三批已压缩归档(整章实装 PR #67 `80439723` / 美术 11 图 PR #68 `854a89ad` / webp 清账 PR #70 `1e7bc261`)**:git log + 各 PR body 可溯,均已合 main;绝顶段收官章全链闭环,当时已知风险(11 图缺图 / 18M 待转码 / idle_horizon 贴线)均已由后续批销账。

> **2026-07-23..24 两批已压缩归档(真相源收口+死配置清理 PR #69 `0091f06e`(GDD v1.24 当前状态块制 + `truth_source_guard_test` 守卫 + 死配置 `boss_internal_force_penalty` 退役 + 排行榜/Boss 红线措辞订正) / webp 清账 PR #66 `2a8e61a1`(Ch14 11 图 18.4M→2.3M 省 16.1M))**:git log + 各 PR body 可溯,均已合 main 并 push;当时已知风险均已由后续批销账。

> **2026-07-23 Ch14/Ch15 spec 起草+拍板 已压缩归档**(`docs/spec/2026-07-23-ch14-shanwai-laike-design.md` 七项拍板全兑现于 PR #64、`docs/spec/2026-07-23-ch15-guanshan-yicheng-design.md` 八项拍板全兑现于 PR #67·git log + spec 文件可溯)。

> **2026-07-23 Ch14 两批已压缩归档(整章实装 PR #64 `8cf1d168`·全量 4652/0 / 美术 11 图接线 PR #65 `79aeea4e`·终判 11/11 PASS)**:git log + 各 PR body 可溯,均已合 main;绝顶段第二章全链闭环(5 关西凉马队·真解十荡十决收编·cap 31→33·叙事 13 篇·全站点 reconcile·kimi 403 中断 Claude 接手历史可溯);当时已知风险(11 图 18M 待转码/随机 fail 未捕获名/kimi 配额)均已由后续批销账或续传。

> **2026-07-22..23 三批收账已压缩归档(日批五 PR #55-#59 终验 4647/0 / kimi 三单 #60-#62+Ch13 美术接线 #61 全量 4651/0 / 清账小批 #63 Ch13 webp 110M→94M+recall 双 invalidate)**:git log + 各 PR body 可溯,均已合 main 并 push;当时已知风险(P1-5.2 schema/散功契约/Ch13 图 webp)均已由后续批销账;PR #55 worktree 首跑 1 例随机 fail 未捕获名续传中。

> **2026-07-22 夜批六 PR 收账(#49-#54 全合 main·全量 4626/0)+ mount_deferred A2+B1 拍定 已压缩归档**:format 收口/P0 断魂庄恢复路由/Ch12 11 立绘接线/Ch13 spec/夜批交接·git log + PR #49-54 可溯。


---
## 已知偏差 / 挂账事项

- **任务储备总账 → 根目录 `BACKLOG.md`**(2026-07-19 建账):待拍板/已解锁可派/依赖锁死/方向级四段,每批收账随 PROGRESS 同步更新;原开放挂账(Riverpod TickerMode 断言)已迁其 §三。
> 已销账条目(#1-#45)详见末尾归档。**P1 阶段全销账 ✅** + **Demo §8.4 14/14 全达标 ✅** + **1.0 ~95% release ready ✅**(A+B+C 全 PASS · 剩 D-G 留 M15-16)。
## 关键约束(每次开局必读)

- 数值硬红线(配置基础表值·schema 拦截):装备基础攻击 ≤2000 / 玩家血 ≤20000 / 内力 ≤15000 / Boss 血 60000+(GDD §5.4)
- 数值软红线(极值满 build 实战可见值·保可读):核心唯一线=不进百万膨胀(普攻真实峰值~13.5万 / 大招~21万,均六位可读)
- 不硬编码数值/文案(走 numbers.yaml / data/narratives, lore, events)
- Riverpod 状态管理;Isar 本地存储;data/ asset 根
- 不动 GDD.md / CLAUDE.md / numbers.yaml / data_schema.md / IDS_REGISTRY.md(数值/规则层 · 改前 ask)
- Mac 端写 lib/、data/(顶层)、test/、文案(v1.8 起 DeepSeek 退役)
## 远程仓库

- GitHub:https://github.com/Zed1118/wuxia_idle · 主分支 main
- 协作:Mac 单端代码+数值+文案;视觉验收 Mac 本地 Codex(Pen Windows AI 工具 2026-06-11 已下线)
## 归档

### 当前阶段旧条目(逆时序 · 从上方迁入;标「已压缩归档」的为摘要,全文见 git 历史)

> **2026-07-20..21 五批已压缩归档(Ch10「中州」`8be841d0` / Ch11「名门之虚」`758a2637`+11 立绘 / Ch12「名下之实」一流三章收官 / kimi 红线区考核首单 `591fb81b` / Ch9 立绘接线 PR #44 `01dae889`+kimi 测试硬化 `7cdd9e23`+4A 死字段清理 `0bc59ed5`)**:git log + PR #44/#45 可溯,均合 main;当时已知风险均已由后续批销账。

> **2026-07-20 Ch9「碛北」二流第 3 章实装(worktree `ch9-qibei` · 3 commit · draft PR · A 案二流收束)**:承 Ch8 spec 冻结,coupled xhigh 批整章一次做完。**已完成**:① `stages.yaml` +stage_09_01..05(erLiu·Boss 位 {4,5}·09_04 隘口守卫 stat 门槛 Boss/09_05 那一位末 Boss·09_03 蜃楼幻敌非 boss 保 45 关全有敌队·章首不跨章 prevStageId);② `skills.yaml` +`skill_chen_sha_yi_jue`(沉沙一诀·tier3 gangMeng·mult3000·末 Boss chargeCounter+dropSkillManual 双用·守≤8000·tier3≤skillTierCap);③ 叙事 13 篇 6546 字(chapter_09 卷首尾+10 段 stage+09_04/09_05 defeat·铜符 motif 沉→烫→凉·师父遗言回响 3 处·「不追」母题蜃楼镜像·章末拐点「符的那头不是答案,是又一个开始」·黑名单14词+现代词 grep 0 命中);④ **~26 reconcile 全站点**(spec §7 记 ~17 + 批末全量逮出 5 处下游级联):count(playtest CSV byte-lock 重生/game_repo mainlineCount 45/narrative/balance/tempo 终章 07→09_05 顺修 drift)+boss 敌 18→20+图鉴 catalog 31→33·主线 25→27+9 章循环([1..9]/bossIdx 9:{4,5})+生产可见性(strings 副标题九章+chapter9Title/Hint+两 switch/main_menu·status<=9/chapter_list _chapters+9)+**Lv 快照逐值实测**(首通 843→1053 Lv57→**63**/全内容 1750→2101 Lv91→**94**·守<Lv100/idle_horizon 缺口 8209→7924)+**skill 计数 3 处**(game_repo/skill_count_contract/skill_qi_redline 248→249)+**wave_b 真解流派配平白名单** +沉沙一诀+enhancement 结晶 633→761+GDD §8.1 章表/招式池同步+known_missing_assets +11 缺图 allowlist。**已验证(worktree 实测)**:analyze **0**;终局全量 `flutter test --no-pub` **4572 pass/0 fail**(基线 4570+2 boss_memory_key 测例);那一位 standard avgHpEndRatio **0.9394** 命中 spec §6 血线带 0.94-0.96。开工两红线守卫逮真错(prevStageId 跨章/3 敌 jingTong 越 cap17,已修)。**已知风险**:Ch9 11 图缺走 errorBuilder allowlist 兜底·待 codex image_gen 出图;末 Boss 真解按 Ch7/8 先例默认加(用户可否决)。**下批建议**:codex 出 Ch9 立绘→kimi/test-hardening 收账→4A 字段清理;memory `feedback_wuxia_add_mainline_chapter_reconcile` 补 5 漏站点(skill 计数三处/真解配平白名单/material supply/idle horizon 级联)。
> **2026-08-04 批 A · A0 解爬塔层数硬编码已压缩归档(PR #114 merge `e36884f9`)**:范围 Phase 0 由 3 处修正为 11 处生产行为点(validator 启动崩 + isFirstClear 静默卡 30 两处比 plan 更硬);`GameRepository.towerMaxFloor` 唯一派生点+注入式 maxFloor;破坏证红 5 轮逐处对应;守卫缺口经非 30 fixture 常驻化;codegraph「未初始化」证伪(worktree 里查所致,索引在主 checkout,V8 OOM 加 NODE_OPTIONS 重建)。全量 4813/0。详 git log + PR #114 body。
> **2026-07-25 四批已压缩归档(工程门禁+CI 随机红根治 PR #74 `ce4e9239`/PR #75 `b638af06` / webp 清账 PR #77 `5f679573` / 中文散写存量清理 PR #78 `176a38b0` / damage_popup parse-back 解耦 PR #79 `32401583`)**:git log + 各 PR body 可溯,均已合 main;要点=中文散写 CI 门禁落地(analyzer 真 AST 扫 `lib/`·双 guard·allowlist 31→26→3→**2**,只余 C 类占位串)+ CI 随机红根因根治(`stage_entry_flow` 稀有彩头随机源绕过 provider·测试 override 不到·即 PR #55/#64/#72 三次「随机 fail」真身·破坏证红+连跑 15 次全绿)+ Ch16 美术 11 图 17M→1.5M(assets 113M→98M)+ 飘字 parse-back 正则连同整句模板删净。**仍在账**:四批共同遗留视觉目检债——飘字分段排版 / 23 处 UiStrings 等值搬迁 / webp q80 观感逐图并排,均未做真机逐屏目检(全量+门禁+widget 测托底)。
> **2026-07-19 七批已压缩归档(三端夜批 battle-ui-v2 阶段4+A案 `42fb2a03` / kimi 测试质量 `cd8e266b` / 断魂庄三选一命名装备 `9d5053d9`+`e7dc29fa` / C1目标1 立绘脚底校准 `a8171afe` / C2目标2 存档安全 + C3 群战墨影递补 `85f173ff`·`d3d6df96` / 1A 闭关经验倍率拆分 `0cc82cd0`+`6b54b3c1`+`4c95d5e5` / C2目标3 远行·断魂庄数值回填 `9be0ee29`+`6e7f3497`)**:git log + 各 commit / PR body 可溯,均已合 main(全量 -j1 **4504 pass / 0 fail**)。要点=自动观战案台七签骨架统一(`colors.dart` 2 个 token 改动波及全 battle 域视觉)+ Isar 原生库下载竞逐 flaky 根治 + 断魂庄三流派命名装备与 3 篇典故([schema] 批)+ **schema 批必跑全量的铁证**(targeted 48/48 全绿,全量逮出 4 处隐藏 reconcile 回归:装备/lore 计数 80→83、开锋武器 36→37、非武器 44→46)+ 群战「首发三敌全灭后后备永不进战位」真 bug 根治(固定 seed 同拍对照)+ 存档前向版本 fail-closed + 闭关经验专用倍率 1.65 破「挂闭关严格劣于关游戏」速率倒挂。**同期三批并入**:K1 equipment_disposal 反向依赖收敛 `1b1eef21` / 快进 spread 同槽双飘字相交根治 `5d0c14a2` / overnight 审查落地 PR #42 `9c6e9e5a`(GameRepository 2402→1137 行 + 反向 import 归零 + strict-casts)。当时已知风险(断魂庄 3 装备 + 16 敌立绘待美术、`colors.dart` token 全 battle 域未逐屏真机复检)均已由后续批销账。
> **2026-07-17..20 六批已压缩归档(立绘图标全批 codex image_gen 16 敌 `fcb726a3` / C2 目标1 热修 `6a441286` / kimi stage_entry_flow 补强 33 测 / codex Ch7/Ch8 美术 22 图终拍 `655dd610`/`ce471fa5` / 门派堂口视觉重构 `de7e4157` / 战斗敌人 79 全身立绘+视觉验收套件 `f2197879` / 窄窗左裁根修 `dcc34339`)**:git log 可溯,均已合 main;69.86MiB 体积债已被 07-19 webp 批清偿(assets 170M→85M)。
> **2026-07-15..18 江湖远行全链 13 批已压缩归档(Phase A1 持久化+编排/A2 基建/远征 Phase B/Lv100 cap17/batch3 经济探针/断魂庄 Phase C+C2.3-C2.5+wiring+4 屏目检/wave_b 复核+孤儿招挂载/Ch7「北望」实装/Ch8 spec)**:全程 `feat/baicao-duanhun-phase-b` 线,git log + PR #43 body + docs/sessions 2026-07-15..18 可溯,4034→4383 测,整线已随 PR #43 合 main(见顶部 Ch8 条)。
> **2026-07-14 六批已压缩归档(心法学习闭环 PR#36/战斗爽感二期 PR#37/出战编成拍板 PR#38/审查修复批/Claude 全量审查/Codex Lv100 cap100 重校)**:git log + PR #36-38 可溯,3897→3987 测,均已合 main。
> **2026-06-29..07-13 十一批已压缩归档(角色四属性职责统一/成长结算单一真相源收敛/境界派生 Lv1-490/外部审查复核与分支收敛/二周目快速开局/内力真气循环+开放式闭关 3807→3849·saveVer 0.35→0.36/项目健康治理 7 批收口·CI format 门禁+lcov+macOS build job/07-10 六批 CI+webp+审查债务/filter-repo 2.5G→658M/UI 专业化+性能+祖师塑形)**:git log + docs/audit + 各 closeout 可溯,1661→3897 测,均已合 main。
