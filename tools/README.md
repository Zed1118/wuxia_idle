# tools/ — 本地开发辅助脚本

> 与 `tool/`(CI 消费的仓库级工具,路径被 workflows 引用)职责区分,见其 README。
> 本目录**不进 CI**,均为本地手动/会话内工具。

| 条目 | 用途 |
|---|---|
| `visual_capture/` | 视觉验收截图工具链(多分辨率 seed/驱动运行中 app 截图) |
| `audit_paper_text_contrast.py` | 宣纸底文字对比度审计 |
| `run_asset_webp_pilot.sh` | webp 转码试点脚本(正式批量转码用 `tool/convert_assets_webp.py`) |
| `pen_screen/` | 历史 Pen Windows 截图遗留(2026-06-11 该路径已下线) |
| `doc_link_scan.py` | docs/ 内部引用死链扫描器:跑 `python3 tools/doc_link_scan.py` 出「存活/ignored/归档类/死链」四分类底账(`--json` 供 diff,`--rows`/`--ignored`/`--archival` 看明细;只用标准库+`git ls-files`/`git check-ignore`;详 `docs/dispatch/reports/2026-08-07_L1D_link_scanner.md`)。**「任何 worktree 结果一致」现已成立**——该自述曾于 2026-08-08 被实测证伪(裸写目录名如 `build` 时 ignored 判定随工作树上该目录是否物理存在而翻转,两地差 17 条),P7 补丁修复后**同一份代码在有/无 `build/` 的两地跑出逐值相同结果**(`refs=7442 alive=5929 dead=908 ignored=605`),由 `test_doc_link_scan_gitfixture.py` 红线守住。**当前定位=可引用事实源(2026-08-18 用户拍板升级,依据见下段)**。**2026-08-12 升级拍板曾因新缺陷暂缓,2026-08-18 重评后定位正式升级**:当时的根因——`git_ls_files()` 逐行读 `git ls-files` 输出,而 git 对**非 ASCII 文件名**默认输出八进制转义并加引号(`"docs/sessions/..._\350\265\204..."`)。后果有二——① 这类文件的记录路径以 `"` 开头,`collect_scan_files` 的 `p.startswith("docs/")` 恒假,**291 篇中文名文档从未进入扫描源(占应扫总数 23.7%)**;② 指向中文名文件的引用因在 `tracked` 集合里匹配不上而被**误判死链**;`git_check_ignore` 回显同样转义,中文名 gitignored 路径被误归死链。**已修复(2026-08-12,派单 F)**:两处同改——`git ls-files -z` 与 `git check-ignore -z --stdin`(NUL 分隔,git 不转义;`check-ignore` 的输入拼接与输出切分两侧同改)。本 worktree 实测修复前→后:扫描源 939→**1230**(+291)、引用 7578→7925(+347)、存活 6059→6279(+220)、ignored 607→686(+79)、归档类 585→593(+8)、死链 327→**367**(+40);`tracked` 条目数不变(4135),变的是字符串内容。防复发:`test_doc_link_scan_gitfixture.py` 新增 `NonAsciiPathTest` 三例(中文名文档进扫描源 / 指向中文名文件的引用判存活 / 中文名路径命中 ignore),把 `-z` 改回换行模式必红。**2026-08-18 重评后定位升级**——F 修复后的新基线上重做 P6 式标注验证(报告 `docs/dispatch/reports/2026-08-18_doclink_scanner_reeval.md`):90 条分层抽样,precision 92.5%(FP 恰 3 条,全枚举=Bug C「已知扩展名后空格段名/§章节」未剥)/ recall 100%(FN=0)。Bug C 已修复合入(清洗规则与 Bug B 冒号截断同体例,红绿测钉住),全池同形态 FP 清零。升级依据至此全部兑现——① 标注验证在无漏扫样本上重做成立;② 系统性假阳(Bug A/B/C)均已修复并有防复发测试;③ 工作树漂移归零(同一份代码两地逐值相同);④ `dead` 经归档扩类后可直接当修复清单。**仍须注意**:`dead` 是「未跟踪且未被 ignore」的机械判定,个别条目仍可能是写法特殊而非真失修,逐条修时按需目检。**归档类单列(2026-08-12 建立,2026-08-18 扩类)**:`ARCHIVAL_DIRS`(现为 `docs/handoff`/`docs/sessions`/`docs/dispatch`/`docs/superpowers`/`docs/audit` 五目录)下的文档照扫,但其内部失效引用单列为「归档类」不计 `dead`——归档文档的引用本就指向写作当时的仓库状态,不是文档失修;实测 907 条死链中 585 条(64.5%)由此分出,`dead` 降至 322。判据是「引用写在哪个文件里」,不是「指向哪个路径」。**范围已拍板扩至五目录(2026-08-18 用户重拍,推翻 08-12「只含 handoff」首拍)**:重评数据显示四新目录(sessions/dispatch/superpowers/audit)内的失效引用均为历史快照性质,扩类后当日基线 dead 373 → 87,剩余 87 条可直接当修复清单(首拍担心的 dispatch 混真错路径经重评标注未见系统性证据)。**文件级排除**(`EXCLUDE_FILES`):`docs/PATH_MIGRATION_MAP.md`(自指历史映射)与 `docs/audio_asset_generation_guide.md`(计划素材清单,与前者同构,2026-08-18 纳入)。扫描源排除项真从 `EXCLUDE_DIRS` 常量派生(此前该常量定义后零消费),按目录边界匹配 |
| `test_doc_link_scan.py` / `test_doc_link_scan_gitfixture.py` | 扫描器的两层测试,均 `python3 tools/<文件>.py` 直接跑(**不进 CI**)。前者 19 例覆盖**解析层**(采集→清洗→跳过→分类,含归档类判据:判据钉在「引用写在哪个文件里」,改坏 `ARCHIVAL_DIRS` 分类必红;2026-08-18 新增 5 例钉住 Bug C 空格/§截断、归档扩四目录与 audio 指南排除),把 `git_ls_files`/`git_check_ignore` mock 掉;后者 20 例真起临时 git 仓覆盖**git 交互层**——P6 查出的两处假阳恰活在这层,mock 体例加样例测不出、加了也是假绿。后者的 `FixedFalsePositiveTest` 现为**防复发红线**:改坏 `doc_link_scan.py` 里 P7 那两处修复(`_RE_STRIP_COLON_SUFFIX` / `check-ignore` 补斜杠变体)必红;`ScanSourceExclusionTest` 钉住 `EXCLUDE_DIRS` 真被消费(退回硬编码必红)与目录边界匹配(裸 startswith 必红);`NonAsciiPathTest`(2026-08-12 派单 F)钉住 `-z` 修复——中文名文档进扫描源、指向中文名文件的引用判存活、中文名路径命中 ignore,把 `ls-files`/`check-ignore` 改回换行模式必红 |
