# =====================================================================
# Flutter / Dart
# =====================================================================
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/

# 生成代码不入库（CI 必须先跑 build_runner，见 phase1_tasks T01）
*.g.dart
*.freezed.dart

# 注：pubspec.lock 应当入库（App 项目锁版本，不再用 *.lock 通配排除）

# =====================================================================
# IDE
# =====================================================================
.claude/

.idea/
.vscode/
*.iml

# =====================================================================
# macOS
# =====================================================================
.DS_Store
# CocoaPods / Xcode 构建产物(macos 仅本地视觉验收用,目标平台是 Windows)
macos/Podfile.lock
macos/Flutter/ephemeral/
**/Pods/
# 注:project.pbxproj / contents.xcworkspacedata 是已跟踪的 Xcode 工程定义,
# 不能 gitignore;其 Pods 集成本地噪音用 `git update-index --skip-worktree` 屏蔽

# =====================================================================
# Windows
# =====================================================================
Thumbs.db
desktop.ini

# =====================================================================
# 项目其它
# =====================================================================
*.log

# Isar.initializeIsarCore(download: true) 在 dart test 环境下载的 native lib
# （Mac 跑 isar_setup_test 时拉到项目根，平台特定，不入库）
libisar.dylib
libisar.so
isar.dll

# Nightshift dispatcher runtime (auto-created)
.nightshift/logs/
.nightshift/status/
.nightshift/SUMMARY.md
.nightshift.bak.*/

# 美术素材(MJ 生成大图 / 素材目录 · 本地保留不进 git · 2 个 tracked 参考图不受影响)
docs/art/

# docs/handoff 视觉验收截图不入库(throwaway · 验收结论已记 PROGRESS/closeout · 2026-06-09)
docs/handoff/**/*.png
docs/handoff/**/*.jpg
docs/handoff/**/*.jpeg
docs/handoff/**/*.pid

# brainstorm visual companion 工作目录(不入库)
.superpowers/

# Suno 音频候选素材(本地保留不进 git · BGM 扩展二期还要用 · 2026-06-11)
assets/audio/_suno_candidates/
.superpowers/

# 平衡/压测/审计工具的再生产物(每跑 test/tools/* 即重写 · 纯输出无基线读回 ·
# 工具自 createSync 目录 · 治本停 tracked 防每跑测试 dirty · 2026-06-13)
test/tools/output/

# CodeGraph 本地代码索引(machine-local · db/config 含本机路径 · 2026-06-13 init)
.codegraph/

# Codex 视觉验收截图(throwaway · 结论已记 closeout/PROGRESS · 本地保留不入库 · 2026-06-13)
docs/**/codex_*_screenshots/

# 自验/视觉截图 throwaway 不入库
docs/screenshots/

# throwaway UI 原型 demo(prototype skill 产物,不入库)
docs/demos/
