# Nightshift T09 · lib/ 目录结构审计

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T09`(branch `nightshift/T09`,从 `main` HEAD `3d24af1` 起)内独立完成本任务。

## 背景

挂机武侠 Flutter Desktop 武侠 idle 游戏。CLAUDE.md §3 规定 `lib/features/<feature>/{domain,application,presentation}` 三层结构。本会话(2026-05-17/18)新加 feature(`tutorial` / `codex` + W16 `festival` + W17 `lineage`)需周期审计是否合规。W17 nightshift T05 已审过 14 feature(`festival` 是当时新加的 15th),本批审 17+ feature 含 tutorial/codex/lineage 新增。

## 目标

产出**唯一**新文件:`docs/handoff/lib_structure_audit_2026-05-19.md`(**≤ 80 行**)

## 操作

1. **列 features/ 树**:
   ```bash
   ls lib/features/
   ```
   每个 feature 检查 `domain/` / `application/` / `presentation/` 是否齐(允许某层空,但需明示)

2. **跨 feature 依赖图**:
   ```bash
   grep -rn "^import.*features/" lib/features/ --include="*.dart" | sort -u
   ```
   提取 features A → features B 引用关系(domain 层不应 import 其他 feature 的 application/presentation,违 DDD)

3. **命名规范 check**:每 feature 内文件名 snake_case 检查 + class UpperCamelCase 检查(grep 反例)

4. **lib/data/ + lib/core/ + lib/shared/ 是否仍合规**:无 features/ 子目录混入,不耦合 features/ 业务

5. 写 `docs/handoff/lib_structure_audit_2026-05-19.md` 4 段:

```markdown
# lib/ 目录结构审计 · 2026-05-19

> Nightshift T09 audit 产出。**只审,不动 lib/**。CLAUDE.md §3 三层规范周期复审。

## §1 lib/features/ 全 feature 三层完整度

| feature | domain/ | application/ | presentation/ | 备注 |
|---|---|---|---|---|
| battle | ✅ | ✅ | ✅ | 核心 |
| codex | ✅/⚠️/❌ | ... | ... | (新增 P1.z) |
| tutorial | ... | ... | ... | (新增 P1.x/P1.y) |
| ... | ... | ... | ... | ... |

总 feature 数:N(目标 17+)

## §2 跨 feature import 依赖图

| from feature | to feature | import path 例 | 是否违 DDD? |
|---|---|---|---|
| main_menu | tutorial | application/tutorial_providers.dart | OK(presentation→application 合规) |
| ... | ... | ... | ... |

(若发现 features/A/domain/ import features/B/application/ → 标记违规)

## §3 命名规范偏差(若有)

| file/class | 偏差 | 建议 |
|---|---|---|
| (任何 snake_case / UpperCamelCase 违规) | ... | ... |
| (若无偏差,本段写「无偏差,全 feature 命名合规」) | — | — |

## §4 总结 + 推荐处置

- 全 feature 数:N
- 三层完整:M
- 跨 feature import 违规:K
- 推荐:① 补缺层 ② 重构违规 ③ 维持(若全合规)

**closeout**:本批审计基线对齐 CLAUDE.md §3。
```

6. 写好后:
```bash
git add docs/handoff/lib_structure_audit_2026-05-19.md
git commit -m "docs(nightshift T09): lib/ 目录结构审计"
```

## 硬约束

- **只新增** `docs/handoff/lib_structure_audit_2026-05-19.md` **一个文件**
- **不动** `lib/` / `data/` / `test/` / 任何其他 docs/ 文件
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `numbers.yaml` / `IDS_REGISTRY.md`
- **数字必 grep 实测**(memory `feedback_closeout_numbers_grep`):feature 数 / import 数 / 三层完整度

## 完成动作

```bash
wc -l docs/handoff/lib_structure_audit_2026-05-19.md  # ≤ 80
git add docs/handoff/lib_structure_audit_2026-05-19.md
git commit -m "docs(nightshift T09): lib/ 目录结构审计"
```

报告一句话「T09 done (audit md N 行, X feature 三层全 ✅, Y 偏差)」。
