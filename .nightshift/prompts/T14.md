# Nightshift T14 · P3.4 sect_event Batch 2.2 service(SectEventService + decay + monthly_tick + R1/R2)

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T14`(branch `nightshift/T14`,从 main HEAD 起)内独立完成本任务。

## 背景

P3.4 sect_event Batch 2.1 schema 已在 main(`efc7604`):`Sect`/`SectEvent` @Collection + composite index `(sectId, triggeredAt)` + 2 enum + `numbers.yaml §14 sect_event:` 段。本 task 落 **Batch 2.2 service**,完整闭合 spec §8 第二行(spec 估 ~2h xhigh / nightshift ~18-25min)。

**关键 spec 锚点**:
- §4 SectEventService 行为(checkAndTrigger 触发链路 + resolve win/loss/expired 联动 + SectReputationDecayService monthly -5)
- §7 R1 触发链路 4 测 + R2 联动 5 测
- 体例参考:`lib/features/inheritance/application/founder_buff_service.dart`(Isar 注入 ctor + Future 方法 + 静态 service)/ `lib/features/inner_demon/application/inner_demon_service.dart`(同 P3 系列 service)

**简化路径(Batch 2.2 落地)**:
- **NumbersConfig 取值**:走 `numbers.raw['sect_event']` dynamic map(不加 SectEventDef 强类型,Phase 4+ 升级时统一做 · 同 T13 体例避撞 numbers_config.dart conflict)
- **monthly_tick 最简体例**:`lib/core/game_loop/monthly_tick.dart` 新建 1 class `MonthlyTickCoordinator`,本 task 只接 1 callsite(SectEventService.checkAndTrigger),Phase 4+ 接 SectReputationDecay tick;**不动现有 game loop / battle tick**,纯独立 monthly infra
- **service pure-ish 体例避 Isar 测撞**:checkAndTrigger 接 `Sect sect / List<SectEvent> activeEvents / RealmTier playerRealm / Random rng` 入参 → 返 `SectEvent?`(触发就 instance / 不触发就 null) · resolve 接 `SectEvent event / SectOutcome outcome / Sect sect` → 返 mutation 后 `(Sect, SectEvent)` tuple · 真持久化(writeTxn)在 caller 端(Phase 4 wire 到 Riverpod 时落)
- **decay 同理**:`SectReputationDecayService.computeDecay(sect: Sect, now: DateTime) → int delta`(纯函数 · 返 reputation 应扣数 · caller 端 mutation)

## 目标

| # | 产出 | 锚点 |
|---|---|---|
| 1 | `lib/features/sect/application/sect_event_service.dart`(新)`checkAndTrigger / resolve` pure-ish service | spec §4 |
| 2 | `lib/features/sect/application/sect_reputation_decay.dart`(新)`computeDecay` 纯函数 | spec §4 末段 decay |
| 3 | `lib/core/game_loop/monthly_tick.dart`(新)`MonthlyTickCoordinator` infra(本 task 只接 1 callsite stub)| spec §4 「关键 callsite」 |
| 4 | `lib/features/sect/domain/sect_outcome.dart`(新,可选)`enum SectOutcome { win, loss, expired }` | resolve 入参体例 |
| 5 | `dart run build_runner build` 重跑 | 应 no-op(Batch 2.2 不加 @Collection) |
| 6 | `test/features/sect/sect_event_service_test.dart`(新)R1 触发链路 4 测 | spec §7 R1 |
| 7 | `test/features/sect/sect_reputation_decay_test.dart`(新)R2 联动 5 测 | spec §7 R2 |
| 8 | commit(message 含 `nightshift T14`) | verify_commit_message |

## 允许修改(verify_path_guard 白名单)

- `lib/features/sect/.+\.dart`
- `lib/core/game_loop/.+\.dart`(新建 monthly_tick.dart)
- `test/features/sect/.+\.dart`
- `pubspec\.lock`
- `lib/.*\.g\.dart`

## 禁止修改

- `lib/features/(?!sect)[^/]+/`(尤其不动 inner_demon / light_foot / mass_battle / battle/strategy)
- `lib/main\.dart`
- `lib/core/(?!game_loop/)`(只新建 game_loop/,不动 core 其他)
- `lib/data/numbers_config\.dart`(用 raw map 取值)
- `data/.*`(0 改 yaml / lore / narrative)
- `test/(?!features/sect/)`
- `pubspec\.yaml` / `.env.*` / `docs/.*`

## Phase 0 · reality check(必跑,3 min)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T14
git log -1 --oneline                                                   # 确认 base 含 efc7604 main
ls lib/features/sect/domain/                                           # 应见 sect.dart / sect_event.dart / *.g.dart
grep -n "^sect_event:" data/numbers.yaml                              # 应见 §14 段
ls lib/core/game_loop/ 2>&1                                            # 应报 No such file(greenfield)
sed -n '/class FounderBuffService/,/^}/p' lib/features/inheritance/application/founder_buff_service.dart | head -30  # 体例参考
grep -n "RealmTier\|RealmLayer" lib/core/domain/enums.dart | head -5  # 看 playerRealm 类型
```

## 操作

### Step 1: SectOutcome enum(可选,`lib/features/sect/domain/sect_outcome.dart` 新建)

```dart
/// resolve 入参体例 · 简化 service signature。
enum SectOutcome { win, loss, expired }
```

> 也可直接在 sect_event_service.dart 顶部定义,看个人偏好。

### Step 2: SectEventService(`lib/features/sect/application/sect_event_service.dart` 新建)

**pure-ish service 体例**(测层 0 Isar):checkAndTrigger 接全部入参 → 返 `SectEvent?` · resolve 接 event + outcome + sect → 返 `(Sect, SectEvent)` mutation 后 tuple · 真持久化在 caller 端。

```dart
import 'dart:math';

import '../../../core/domain/enums.dart';    // RealmTier
import '../../../data/numbers_config.dart';
import '../domain/sect.dart';
import '../domain/sect_event.dart';
import '../domain/sect_outcome.dart';

/// 门派事件服务(1.0 P3.4 §12.1 Batch 2.2)。
///
/// **pure-ish 体例**(避 Isar 实例化测撞 autoIncrement,memory
/// `feedback_isar_autoincrement_test_id_collision`):method 接全部入参,
/// 返 SectEvent? / (Sect, SectEvent) tuple;真持久化在 caller 端
/// (Phase 4 wire 到 Riverpod + writeTxn 时落)。
class SectEventService {
  final NumbersConfig numbers;

  const SectEventService({required this.numbers});

  /// 月度 tick 触发(spec §4 链路):
  /// ① (now - lastEventAt) < cooldown_days → null
  /// ② playerRealm < trigger_realm_min → null
  /// ③ activeEvents.length ≥ active_events_max → null
  /// ④ rand >= trigger_probability → null
  /// 全通过 → 返 new SectEvent(pending)
  SectEvent? checkAndTrigger({
    required Sect sect,
    required List<SectEvent> activeEvents,    // 当前 pending events
    required RealmTier playerRealm,
    required DateTime now,
    required String pickedNarrativeId,        // caller 端随机选 narrative
    required Random rng,
  }) {
    final cfg = (numbers.raw['sect_event'] ?? const <String, dynamic>{}) as Map<String, dynamic>;
    final tournament = (cfg['tournament'] ?? const <String, dynamic>{}) as Map<String, dynamic>;
    final cooldownDays = ((tournament['cooldown_days'] as num?) ?? 30).toInt();
    final triggerProbability = ((tournament['trigger_probability'] as num?) ?? 0.0).toDouble();
    final triggerRealmMinName = (tournament['trigger_realm_min'] as String?) ?? 'yiLiu';
    final triggerRealmMin = RealmTier.values.byName(triggerRealmMinName);
    final activeEventsMax = ((cfg['active_events_max'] as num?) ?? 3).toInt();

    // ① cooldown
    final lastAt = sect.lastEventAt;
    if (lastAt != null) {
      final daysSince = now.difference(lastAt).inDays;
      if (daysSince < cooldownDays) return null;
    }

    // ② 境界
    if (playerRealm.index < triggerRealmMin.index) return null;

    // ③ activeEvents 上限
    if (activeEvents.length >= activeEventsMax) return null;

    // ④ rand
    if (rng.nextDouble() >= triggerProbability) return null;

    // 全通过 → 返 SectEvent
    return SectEvent()
      ..sectId = sect.id
      ..type = SectEventType.tournament
      ..status = SectEventStatus.pending
      ..triggeredAt = now
      ..narrativeId = pickedNarrativeId;
  }

  /// 解决事件(spec §4 链路):
  /// win:     reputation +10 · totalWins +=1 · if %3==0 → sectLevel +=1 (clamp ≤7)
  /// loss:    reputation -5 (clamp ≥0)
  /// expired: reputation -5 + status=expired
  /// 返 (Sect mutation 后, SectEvent mutation 后) tuple
  (Sect, SectEvent) resolve({
    required Sect sect,
    required SectEvent event,
    required SectOutcome outcome,
    required DateTime now,
  }) {
    final cfg = (numbers.raw['sect_event'] ?? const <String, dynamic>{}) as Map<String, dynamic>;
    final repCfg = (cfg['reputation'] ?? const <String, dynamic>{}) as Map<String, dynamic>;
    final levelCfg = (cfg['sect_level'] ?? const <String, dynamic>{}) as Map<String, dynamic>;
    final winDelta = ((repCfg['win_delta'] as num?) ?? 10).toInt();
    final lossDelta = ((repCfg['loss_delta'] as num?) ?? -5).toInt();
    final repMax = ((repCfg['max'] as num?) ?? 100).toInt();
    final repMin = ((repCfg['min'] as num?) ?? 0).toInt();
    final levelMax = ((levelCfg['max'] as num?) ?? 7).toInt();
    final promoteThreshold = ((levelCfg['promote_wins_threshold'] as num?) ?? 3).toInt();

    int newRep = sect.sectReputation;
    int newWins = sect.totalWins;
    int newLevel = sect.sectLevel;
    int? repDelta;
    SectEventStatus newStatus = event.status;

    switch (outcome) {
      case SectOutcome.win:
        newRep = (newRep + winDelta).clamp(repMin, repMax);
        newWins += 1;
        if (newWins > 0 && newWins % promoteThreshold == 0) {
          newLevel = (newLevel + 1).clamp(1, levelMax);
        }
        repDelta = winDelta;
        newStatus = SectEventStatus.resolved;
        break;
      case SectOutcome.loss:
        newRep = (newRep + lossDelta).clamp(repMin, repMax);
        repDelta = lossDelta;
        newStatus = SectEventStatus.resolved;
        break;
      case SectOutcome.expired:
        newRep = (newRep + lossDelta).clamp(repMin, repMax);
        repDelta = lossDelta;
        newStatus = SectEventStatus.expired;
        break;
    }

    // mutate(原 instance 改字段,Dart Isar Collection 体例)
    sect
      ..sectReputation = newRep
      ..totalWins = newWins
      ..sectLevel = newLevel
      ..lastEventAt = now;
    event
      ..status = newStatus
      ..resolvedAt = now
      ..reputationDelta = repDelta;

    return (sect, event);
  }
}
```

> RealmTier index 比较(`playerRealm.index < triggerRealmMin.index`)假设 RealmTier 按七阶顺序声明(xueTu=0 → wuSheng=6),Phase 0 grep 确认。

### Step 3: SectReputationDecayService(`lib/features/sect/application/sect_reputation_decay.dart` 新建)

```dart
import '../../../data/numbers_config.dart';
import '../domain/sect.dart';

/// 门派声望衰减服务(1.0 P3.4 §12.1 Batch 2.2)。
///
/// **pure 函数体例**:computeDecay 接 Sect + now → 返 reputation 应扣数。
/// 真 mutation 在 caller 端(Phase 4 wire 到 Riverpod monthly tick callback)。
class SectReputationDecayService {
  final NumbersConfig numbers;

  const SectReputationDecayService({required this.numbers});

  /// 计算 decay:30 天无 event → -decay_per_month_idle(默认 5)
  /// 返 0 = 不衰减 / 负数 = 应扣 reputation 数
  int computeDecay({required Sect sect, required DateTime now}) {
    final cfg = (numbers.raw['sect_event'] ?? const <String, dynamic>{}) as Map<String, dynamic>;
    final repCfg = (cfg['reputation'] ?? const <String, dynamic>{}) as Map<String, dynamic>;
    final decayAmount = ((repCfg['decay_per_month_idle'] as num?) ?? 5).toInt();

    final lastAt = sect.lastEventAt;
    if (lastAt == null) return 0;     // 没事件不算衰减(新创建 sect)

    final daysSince = now.difference(lastAt).inDays;
    if (daysSince < 30) return 0;     // 未满 30 天不衰减
    return -decayAmount;
  }
}
```

### Step 4: MonthlyTickCoordinator(`lib/core/game_loop/monthly_tick.dart` 新建)

最简 infra:接受 callback list,每月触发一次(stub,Phase 4 真接入 Riverpod / 系统时间锚)。

```dart
/// 月度 tick 协调器(1.0 P3.4 §12.1 Batch 2.2 引入)。
///
/// **最简 infra**:本 Phase 只接 1 callsite stub(SectEventService.checkAndTrigger),
/// Phase 4 wire 到 Riverpod 系统时间锚时落真触发(每月第 1 天 / 用户进 sect screen 时检查)。
///
/// **不动现有 game loop / battle tick**(那是 in-battle 实时 tick,本协调器是
/// 跨月度长周期 tick,完全独立)。
class MonthlyTickCoordinator {
  /// 注册的 tick callback(Phase 4 加 SectEventService + SectReputationDecay 等)。
  final List<MonthlyTickCallback> _callbacks = [];

  void register(MonthlyTickCallback cb) => _callbacks.add(cb);

  /// 触发一次月度 tick(caller 决定时机)。
  /// 顺序保证:按 register 顺序串行调,各 cb 异常不阻塞后续。
  Future<void> tick(DateTime now) async {
    for (final cb in _callbacks) {
      try {
        await cb(now);
      } catch (_) {
        // 单 cb 异常吞掉 · log 留 Phase 4 接 logger 时落
      }
    }
  }

  /// 测试用 · 重置 callback 列表
  void clear() => _callbacks.clear();
}

typedef MonthlyTickCallback = Future<void> Function(DateTime now);
```

### Step 5: R5 测族

**`test/features/sect/sect_event_service_test.dart`** R1 触发链路 4 测:
- R1.1 cooldown 满足 + 境界达标 + activeEvents 未满 + rng 0.0(必触发)→ 返 SectEvent pending
- R1.2 cooldown 未到(lastEventAt 1 天前)→ 返 null
- R1.3 境界不够(playerRealm=xueTu < yiLiu)→ 返 null
- R1.4 activeEvents.length >= max(3)→ 返 null

**`test/features/sect/sect_reputation_decay_test.dart`** R2 联动 5 测:
- R2.1 SectOutcome.win → reputation +10 / totalWins +1 / 第 3 胜 sectLevel +1
- R2.2 SectOutcome.loss → reputation -5(clamp ≥0,基线 0 时不变)
- R2.3 SectOutcome.expired → reputation -5 + status=expired
- R2.4 SectReputationDecayService 30 天 lastEventAt → -5
- R2.5 sectLevel clamp ≤ 7(连胜 21 场 sectLevel 7 后不再升)

> **写测原则**:**0 Isar 实例化**,只 `Sect()..字段=值` unsaved instance + service pure-ish method · `NumbersConfig` 用现成的 yaml 加载或者 mock raw map(`final cfg = NumbersConfig(... raw: {'sect_event': {...}})`)— 实测两路任选,先看 `NumbersConfig` ctor 是否能用 raw-only 模式简化,否则用真 yaml 加载体例。

### Step 6: commit(不 push)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T14
git add lib/features/sect/ lib/core/game_loop/ test/features/sect/
git commit -m "[feat] P3.4 sect_event Batch 2.2 service · SectEventService + decay + monthly_tick · nightshift T14

- lib/features/sect/application/sect_event_service.dart 新建(pure-ish checkAndTrigger + resolve win/loss/expired 联动 reputation/sectLevel)
- lib/features/sect/application/sect_reputation_decay.dart 新建(30 天 idle → -5 纯函数 computeDecay)
- lib/core/game_loop/monthly_tick.dart 新建(MonthlyTickCoordinator infra · Phase 4 wire 真接入)
- lib/features/sect/domain/sect_outcome.dart 新建(enum win/loss/expired)
- test/features/sect/sect_event_service_test.dart R1 触发链路 4 测
- test/features/sect/sect_reputation_decay_test.dart R2 联动 5 测
- NumbersConfig 走 raw map 取值,0 改 numbers_config.dart 强类型化(Phase 4+ 升)
- 真持久化(Isar writeTxn)留 Phase 4 wire Riverpod 时落

详 docs/spec/p3_4_sect_event_spec_2026-05-24.md §4/§7
"
```

## 不要做

- **🚨 严禁 cd / git -C 到 dispatcher 创建的 worktree 之外的任何路径**(v2 A1 红线,memory `feedback_nightshift_v2_first_run_lessons`)
  - 所有 git/edit/cd 必须在 `/Users/a10506/Desktop/wuxia-idle-T14` 内
- 不要改 `lib/data/numbers_config.dart`(用 raw map 取值避撞 conflict)
- 不要写 Isar 实例化测(memory `feedback_isar_autoincrement_test_id_collision`)— service pure-ish 体例正是为避此撞
- 不要在 service 内做 writeTxn(留 Phase 4 wire 时落,本 task service 纯 method)
- 不要写 UI(Batch 2.4 才动 sect_screen / sect_event_dialog / main_menu 入口)
- 不要动 battle/strategy/*(Batch 2.3 才接 DefaultGroundStrategy 战斗联动)
- 不要 git push / git rebase / git reset --hard
- 不要顺手改 GDD.md / CLAUDE.md / numbers.yaml(本 task 0 改 yaml)

## 参考

- spec: `docs/spec/p3_4_sect_event_spec_2026-05-24.md` §4/§7/§8
- service 体例: `lib/features/inheritance/application/founder_buff_service.dart`(Isar 注入但本 task pure-ish 更简) / `lib/features/inner_demon/application/inner_demon_service.dart`
- NumbersConfig raw map 体例: `lib/data/numbers_config.dart` field `raw`(`Map<String, dynamic>`)
- memory: `feedback_isar_autoincrement_test_id_collision`(pure-ish 体例理由) / `feedback_avoid_over_engineer_abstraction`(MonthlyTickCoordinator 不过度抽象)
