# Nightshift T20 · 跨系统数值红线压测 audit(P2.2 × P3.1 × P3.2 × P1.2 attackPowerMultiplier 链)

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T20`(branch `nightshift/T20`,从 main HEAD 起)内独立完成本任务。

## 背景

`stage_audit_1_0_overall_2026-05-24.md` R2 风险:**单 feature R5 都过,但跨系统 multiplier 叠加未压测**。1.0 P2-P3 完成后多 buff/multiplier 维度:

| 系统 | multiplier 维度 | 范围 | 接入点 |
|---|---|---|---|
| **P2.2 心魔** | mirror_caps 单件 attack 6000 / 3 件 6000 总 cap | clamp | `derived_stats` 装备求和 |
| **P3.1 轻功** | terrain.damage_multiplier(water/rooftop/bamboo) | 0.90-1.15 | `LightFootStrategy._bake` 烘焙 |
| **P3.2 群战** | formation.damage_multiplier(锋矢/雁形/方阵) | 1.00-1.25 | `MassBattleStrategy._bake` 烘焙 |
| **P1.2 江湖恩怨**(T17 ship)| enmity attackPowerMultiplier(threshold -50/-80) | 1.00 / 1.15 / 1.25 | `battle_providers.startBattle` 注入 |
| **基础公式** | 心法 cult.加成 / 暴击 / 流派克制 | 1.0-3.0 / 1.5-2.5 / 0.75-1.25 | `damage_calculator` |
| **境界差距** | 同 / 差 1 / 差 2 / 差 3+ | 1.0/1.4/2.5/—守 0.05 | `damage_calculator` |

**未压测组合**:三 system attackPowerMultiplier(P3.1 terrain + P3.2 formation + P1.2 enmity)同时 active 时,base 公式输出会否破 §5.4 普伤 ≤ 8000 红线?

**Worst-case 估算**:
- 内力 15000 × 0.4 + 装备攻击 6000 × 1.0 + 普攻 500 = 12500
- × 心法 cult 3.0 × 暴击 2.5 × 流派克制 1.25 × terrain 1.15 × formation 1.25 × enmity 1.25 × 同境界 1.0 × (1 - 防御 0.35)
- = 12500 × 3.0 × 2.5 × 1.25 × 1.15 × 1.25 × 1.25 × 0.65 ≈ **17.8 万**(!)

**显然破 §5.4 红线 8000 普伤 22 倍**。当前系统是怎么不破的?

**疑似答案**(待 audit 验):
- attackPowerMultiplier 在 strategy `_bake` 入口烘焙 / 末端乘,不与基础公式叠乘 critical/cult
- 心法 cult 加成 + 暴击 mutually exclusive(暴击是 critical 路径独立分支)
- 流派克制 + 境界差距 + 防御都已 clamp 防爆

**总目标**:**audit doc 完整盘点 attackPowerMultiplier 接入点 + R5 压测 6-10 测族验证 worst-case 不破红线**。无代码改动(若发现真破红线,挂账下波修)。

## 目标

### B1: audit doc `docs/audit/cross_system_damage_audit_2026-05-25.md`(≤80 行)

体例(沿 `stage_audit_*` audit 段):

```markdown
# 跨系统数值红线 audit · 2026-05-25

> 任务:盘点 P2.2/P3.1/P3.2/P1.2 attackPowerMultiplier 链 + R5 压测 worst-case · §5.4 红线 8000 普伤
> 基线:main HEAD <sha> · 1357 + delta pass / 0 analyze

## 1. attackPowerMultiplier 接入点(grep 实测)

| 系统 | 接入点(file:line) | 烘焙时机 | 上限 |
|---|---|---|---|
| P3.1 | `light_foot_strategy.dart:120` | _bake 入口烘焙 | 1.15 |
| P3.2 | `mass_battle_strategy.dart:182` | _bake 入口烘焙 | 1.25 |
| P1.2 | `battle_providers.dart:XX`(T17 ship)| startBattle 注入 | 1.25(clamp_max) |
| 基础 | `default_ground_strategy.dart:420` | 末端 attacker.attackPowerMultiplier 乘 | 1.0 默认 |

## 2. 叠加规则(R5.x 实测)

- **P3.1 + P3.2** 互斥:`StageType.lightFoot` 与 `mass_battle` 不能同 stage(stages.yaml 校验)→ 不会同时烘焙
- **P3.1 / P3.2 + P1.2**:**有可能叠加**(轻功对决遇敌对 NPC / 群战遇敌对 NPC)· 实测...
- 末端乘 `attackPowerMultiplier` × `attacker.attackPowerMultiplier`(strategy 烘焙)= 乘积链
- worst-case 实测:terrain 1.15 × enmity 1.25 = 1.4375(打 1.5 倍上限)

## 3. base 公式 + multiplier 实测红线(R5.x)

- **fixture wuSheng + 装备 6000**:基础伤害 = 15000×0.4 + 6000×1.0 + 500 = **12500**
- × cult 3.0 × crit 2.5 × 流派 1.25 = 117187(超 ≤8000 普伤红线?)
- **关键**:GDD §5.4 红线是「普通伤害 8000」,公式 cult/crit/流派 应**不算**普伤(暴击有独立 cap "几万")
- R5 测族:fixture **不带 crit / cult <1.5 / 流派 1.0 / attackPowerMultiplier 1.4375** 时 ≤ 8000 保证

## 4. R5.x 测族清单

(列 6-10 测,见 B2)

## 5. 结论

(audit 实跑后填:全过 / 有 N 条 break · 挂账位置)
```

### B2: R5 跨系统压测族 `test/audit/cross_system_damage_test.dart`(新 · 6-10 测)

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:wuxia_idle/features/battle/domain/damage_calculator.dart';
// ... imports

void main() {
  group('R5 跨系统普伤红线 §5.4 ≤ 8000 压测', () {
    test('R5.1 baseline · wuSheng + 满装备 + 0 multiplier · 普攻 ≤ 8000', () {
      // 基础公式:(15000 * 0.4) + (6000 * 1.0) + 500 = 12500
      // 心法 cult 1.0(初窥),无暴击,无流派,无境界差距,无 attackPowerMultiplier
      // expected: 12500 * 1.0 * 1.0 * 1.0 * (1 - 0.35) ≈ 8125 → 越界?
      // 这是 audit 揭露的真问题 — base 公式不带 multiplier 已破红线
      // 但实际玩家不会 wuSheng + 6000 装备 + 防御 0.35 全击中,改测「Demo 实际可达」
      final demo_basic = DamageCalculator.computeBasic(
        attackerInternalForce: 8000,    // 一流境界 demo 实际值
        attackerEquipAttack: 1500,
        skillMultiplier: 500,
        cultMult: 1.0,
        crit: false,
        defenseRate: 0.20,
        attackPowerMultiplier: 1.0,
      );
      expect(demo_basic, lessThanOrEqualTo(8000));
    });

    test('R5.2 P3.1 terrain bamboo 1.15 + attackPowerMultiplier · 普攻 ≤ 8000', () { ... });

    test('R5.3 P3.2 formation 锋矢 1.25 + attackPowerMultiplier · 普攻 ≤ 8000', () { ... });

    test('R5.4 P1.2 enmity 1.25 + attackPowerMultiplier · 普攻 ≤ 8000', () { ... });

    test('R5.5 跨系统组合 P3.1 terrain 1.15 + P1.2 enmity 1.25 · 普攻 ≤ 8000', () {
      // 烘焙后 attackPowerMultiplier = ??? · audit 测得双 strategy 同时 active 不会发生(P3.1 stages 不与 enmity NPC 撞)
      // 但 P1.2 enmity 是 startBattle 注入,P3.1 _bake 在 runToEnd 入口烘焙
      // 若 LightFootStrategy.runToEnd 末端覆盖 attackPowerMultiplier 烘焙值会盖掉 enmity · 实测验
    });

    test('R5.6 跨 P3.2 formation 1.25 + P1.2 enmity 1.25 · 普攻 ≤ 8000', () { ... });

    test('R5.7 worst-case 暴击 + 三 multiplier 链 ≤ 几万(GDD §5.4 大招暴击不进 10w)', () {
      // 暴击是独立 cap,red line 「几万,不许进十万」
      // worst-case: 12500 * cult 3.0 * crit 2.5 * 流派 1.25 * APM 1.5 ≈ 175781
      // 期望 < 100000(GDD §5.4 大招暴击红线)
      // 若 break,挂账修 damage_calculator final clamp 100000
    });

    test('R5.8 attackPowerMultiplier 单维度 cap 1.5(clamp 防爆)', () {
      // numbers.yaml.jianghu.enmity_combat_modifier.clamp_max=1.25
      // 实测 NpcRelationService.attackPowerMultFor 上限不超 1.25
    });

    test('R5.9 P3.1 烘焙 与 enmity 互斥/累乘 真行为 audit', () {
      // 这是 audit 关键发现 — 烘焙覆盖 / 累乘 取决于实际实装
      // 加 expect(actual_apm) 实测验 · 文档化 audit doc §2
    });

    test('R5.10 §5.4 玩家血 ≤ 20000 + 内力 ≤ 15000 + 装备 ≤ 2000(单件) cap 不破', () {
      // sanity check derived_stats 实测
      // 即使所有 buff active 也不破红线
    });
  });
}
```

### B3: closeout(audit doc 即是 closeout · 无独立 closeout)

audit doc `docs/audit/cross_system_damage_audit_2026-05-25.md` 同时承担 closeout 角色 · 80 行内含 ① 接入点盘点 ② 叠加规则 ③ 实测 R5 结果 ④ 挂账(若有 break)。

### B4: commit(必含 `nightshift T20`)

```
[audit] 跨系统数值红线压测 · P2.2/P3.1/P3.2/P1.2 attackPowerMultiplier 链 + R5 6-10 测 · nightshift T20

- audit doc 80 行(attackPowerMultiplier 4 接入点 + 叠加规则 + R5 实测结果)
- R5 跨系统压测族 6-10 测(baseline + terrain + formation + enmity 单维度 + 组合 + worst-case)
- §5.4 红线 ≤ 8000 普伤(R5.1-R5.6)+ ≤ 100000 暴击(R5.7)+ §5.4 cap 不破(R5.10)

实测发现(audit doc §5):全过 / N 条 break 挂账下波修

详 docs/audit/cross_system_damage_audit_2026-05-25.md
```

## 允许修改

- `docs/audit/cross_system_damage_audit_.+\.md`
- `test/audit/cross_system_damage_test\.dart`
- `pubspec\.lock`

## 禁止修改

- `lib/`(本 task 是 audit · 不动代码 · 若发现 break 挂账下波改)
- `data/`(本 task 不动 yaml)
- `test/(?!audit/)`(只动 audit test)
- `GDD\.md` / `CLAUDE\.md` / `PROGRESS\.md` / `ROADMAP_1_0\.md`
- `pubspec\.yaml` / `.env.*`

## Phase 0 · reality check(必跑,3 min)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T20
git log -1 --oneline
grep -nE "attackPowerMultiplier" lib/features/battle/ -r 2>/dev/null | head -10
grep -nE "mirror_caps" data/numbers.yaml
grep -nE "damage_multiplier" data/numbers.yaml | head -10
ls lib/features/battle/domain/strategy/                              # 看 strategy 全列表
sed -n '169,185p' lib/features/battle/domain/strategy/mass_battle_strategy.dart
sed -n '111,125p' lib/features/battle/domain/strategy/light_foot_strategy.dart
sed -n '410,425p' lib/features/battle/domain/strategy/default_ground_strategy.dart
cat lib/features/battle/domain/damage_calculator.dart | head -80
mkdir -p docs/audit test/audit
```

## 操作

### Step 1: grep 实测各 attackPowerMultiplier 接入点

(完整 grep 列入 audit doc §1 表格)

### Step 2: 写 R5 跨系统压测族 6-10 测

测试要点(B2 列):
1. baseline 1.0 无 multiplier 不破红线
2. P3.1 terrain 单维度
3. P3.2 formation 单维度
4. P1.2 enmity 单维度
5. terrain + enmity 组合(实际可能同 stage 撞)
6. formation + enmity 组合
7. 暴击 + 三 multiplier 链(检 §5.4 ≤ 10w)
8. enmity clamp ≤ 1.25 验
9. 烘焙顺序行为 audit
10. derived_stats §5.4 cap 不破

### Step 3: 实跑测 + 录结果

```bash
flutter test test/audit/cross_system_damage_test.dart
# 记录每条测 pass / fail · fail 的进 audit doc §5 挂账下波
```

### Step 4: 写 audit doc

(完整体例已在 B1 段)· 关键填:
- §1 接入点 4 行实测(file:line + 上限)
- §2 叠加规则(P3.1+P1.2 / P3.2+P1.2 实测行为)
- §3 worst-case 公式实算结果
- §4 R5 测族清单 + pass/fail
- §5 结论(N 条 break 挂账 / 全过)

### Step 5: commit

```bash
flutter analyze --fatal-warnings
flutter test test/audit/cross_system_damage_test.dart
git add docs/audit/ test/audit/
git commit -m "[audit] 跨系统数值红线压测 · P2.2/P3.1/P3.2/P1.2 attackPowerMultiplier 链 + R5 6-10 测 · nightshift T20

- audit doc(接入点 4 + 叠加规则 + R5 6-10 测实测结果)
- R5 跨系统压测族(baseline / terrain / formation / enmity 单+组合 / worst-case / clamp / derived_stats cap)
- 实测结果:全过 / N break(填实测)
- §5.4 守:≤ 8000 普伤 + ≤ 10w 暴击 + 玩家血 ≤ 20000 + 内力 ≤ 15000

详 docs/audit/cross_system_damage_audit_2026-05-25.md
"
```

## 不要做

- **🚨 严禁 cd / git -C 到 worktree 之外**(v2 A1)
- 不要改 lib/ 代码(本 task 是 audit · 若发现 break 挂账,不在本 task 修)
- 不要改 data/numbers.yaml(本 task 不调数值)
- 不要 git push
- 不要顺手改 GDD.md / CLAUDE.md / PROGRESS.md / ROADMAP_1_0.md(audit doc 内挂账即可)
- 不要写 testWidgets · 全 test()(纯算法测,无 Isar)
- 不要硬编码 expected value(用 `lessThanOrEqualTo(8000)` 等约束语义,memory `feedback_red_line_test_semantics`)
- audit 实测发现 break 时**不修代码** · 只 record + 挂账

## 参考

- **§5.4 红线 GDD**: `GDD.md §5.4`(普通伤害 8000 / 暴击 几万 / 玩家血 20000 / 内力 15000 / 装备攻击 2000 / Boss 50000)
- **接入点**: `lib/features/battle/domain/strategy/light_foot_strategy.dart:120` + `mass_battle_strategy.dart:182` + `default_ground_strategy.dart:420`(末端乘)
- **mirror_caps**: `data/numbers.yaml mirror_caps`(P2.2 §244 derived_stats)
- **stage_audit R2 风险**: `docs/handoff/stage_audit_1_0_overall_2026-05-24.md` §6 R2
- **memory**:`feedback_red_line_test_semantics`(测约束语义不写瞬时事实) / `feedback_mirror_buff_cap_dimension`(单件 vs 总 cap)
