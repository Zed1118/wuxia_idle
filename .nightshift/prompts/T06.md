# Nightshift T06 · 今晚 SUMMARY(必跑,最后任务)

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T06`(branch `nightshift/T06`,从 `main` HEAD `4132a13` 起)内独立完成本任务。

## 背景

夜间 T01-T05 已跑完(可能 pass / 可能 skipped),需要为用户早上 review 生成 SUMMARY,聚合 git log + 各 task 状态 + 测试数变化 + follow-up 候选。

## 目标

产出**唯一**新文件(相对当前 worktree 根路径):`.nightshift/SUMMARY.md`

**重要**:T06 worktree(`/Users/a10506/Desktop/wuxia-idle-T06`)基于 nightshift/T06 branch(从 `main` HEAD `4132a13` 起,当时还没有 `.nightshift/` 目录)。**所以你需要 `mkdir -p .nightshift` 然后写入 `.nightshift/SUMMARY.md`**。worktree 内 git 操作会把它纳入 nightshift/T06 分支。

主工程的 `.nightshift/status/` / `.nightshift/logs/` 是 dispatcher 写的运行时文件,读时用绝对路径 `/Users/a10506/Desktop/挂机武侠/.nightshift/status/T0X.status`(主工程,不是 worktree)。

## 操作

1. 跑 `git log --all --oneline | head -30` 拿到所有分支(含 nightshift/T01..T05)commits
2. 跑 `ls /Users/a10506/Desktop/挂机武侠/.nightshift/status/` 拿到所有 status 文件(主工程绝对路径)
3. 读 `/Users/a10506/Desktop/挂机武侠/.nightshift/status/T0[1-5].status`(若存在,主工程绝对路径)拿各 task pass/skipped 状态
4. 跑 `flutter pub get >/dev/null 2>&1 && flutter analyze --fatal-infos 2>&1 | tail -5`
5. 跑 `flutter test 2>&1 | tail -3`(可能慢,接受 2-3 min)
6. **在 worktree 内**:`mkdir -p .nightshift` 后写 `.nightshift/SUMMARY.md`
6. 写 SUMMARY,结构:

```markdown
# Nightshift SUMMARY · 2026-05-17 夜

> Nightshift T06 自动生成。用户早上首读此文件。

## 1. 任务执行状态

| Task | Status | 耗时 | 产出 |
|---|---|---|---|
| T01 #37 永封档 | (completed / skipped + 原因) | (s.finished - s.started) | docs/handoff/wuxia_w17_orphan_events_permanent_archive_2026-05-17.md |
| T02 pattern 审计 | ... | ... | docs/handoff/wuxia_widget_test_pattern_audit_2026-05-17.md |
| T03 死代码 scan | ... | ... | docs/handoff/wuxia_dead_code_scan_2026-05-17.md |
| T04 LineagePanel 边界 test | ... | ... | test/features/character_panel/presentation/lineage_panel_screen_edge_test.dart |
| T05 NavigatorObserver mock doc | ... | ... | docs/handoff/wuxia_navigator_observer_mock_pattern_2026-05-17.md |

(读 status 文件的 status= 行 + finished - started 算耗时)

## 2. Git commits 一览

(贴 `git log --all --oneline --since="22:00" | head -20` 输出)

## 3. 测试 / Analyze 状态

- `flutter analyze --fatal-infos`:(N issues / clean)
- `flutter test`:(N/M tests passing,N 比开夜前的 765 多 X 个)

## 4. 早上 review 清单(动作型)

按 task 类型分:
- **doc 类**(T01/T02/T03/T05):review 内容质量 + 决定是否合并 nightshift/T0X 分支到 main(`git merge nightshift/T0X --no-ff`)
- **test 类**(T04):review test 用例质量 + 决定合并

如果某 task SKIPPED:
- 查 `.nightshift/logs/T0X.log` 看失败原因
- 查 `.nightshift/status/T0X.status` 的 `reason=` 字段

## 5. 已知偏差 / follow-up

(根据 T01-T05 实际状态:若全 pass,标「无偏差」+ 列下一波候选;若有 skipped,标出原因 + 建议处理)

## 6. 分支清单

(`git branch -a | grep nightshift`)

## 7. 启动到结束时间

- dispatcher start: (从 `.nightshift/logs/dispatcher.log` 第一行)
- dispatcher end: (本任务运行时,date)
- 总耗时: ...
```

## 硬约束

- **不动** `lib/` / `test/` / `docs/` 任何已存在文件
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 只产出 `.nightshift/SUMMARY.md` 一个新文件
- 不调用网络

## 完成动作

SUMMARY 写好后:
```bash
git add .nightshift/SUMMARY.md
git commit -m "docs(nightshift T06): 今晚 SUMMARY"
```

**不要 push**。报告一句话「T06 done」。

## 注意

`flutter test` 可能跑 2-3 min,接受;若 timeout 在 dispatcher 50min 内仍能完成。如 flutter test 跑出 error 不影响 SUMMARY 产出 — 把 error 摘要写进 SUMMARY §3 即可。
