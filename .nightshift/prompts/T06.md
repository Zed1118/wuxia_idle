# Nightshift T06 · EquipmentDetailScreen _LoreSection 延续典故渲染 edge test【最高价值】

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T06`(branch `nightshift/T06`,从 `main` HEAD `217ddf7` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。P1 #42 Phase 1 Phase 5(2026-05-17)新增**延续典故 hook**:`GameEventService.recordBossDefeated(warbornEquipment)` / `recordEquipmentObtained(equipment)` 内部追加 `Lore(isPreset=false)` 到装备的 `lores` 列表;`EquipmentDetailScreen._LoreSection`(`lib/features/inventory/presentation/equipment_detail_screen.dart` 486 行)显化:**preset 段在前 + 延续段在后**,延续段用墨青色 `_ContinuedLoreChip` 装饰。

**当前 0 test 覆盖** `_LoreSection`(`grep _LoreSection test/` 只命中 `game_event_service_test.dart` 自己,而该 test 测的是 service 端 lore 追加副作用,**不是 widget 渲染**)。这是本批 nightshift 8 task 里**最高价值 0→1 缺口**。

## 红线:约束语义不写瞬时事实

断言写**段顺序集合自洽 + chip 类型 widget 存在性**:断言「preset 段全部出现在 `_ContinuedLoreChip` 之前」(顺序谓词),不写「第 3 个是延续段」位置硬编码。颜色断言用 `Color` 字段等价 或 `Container.decoration` 类型校验,不写 hex 字面值(墨青色具体值在 lib/ 找 + 用常量引用)。

## 目标

产出**唯一**新文件:`test/features/inventory/presentation/equipment_detail_screen_lore_section_test.dart`

## 操作

1. 读 `lib/features/inventory/presentation/equipment_detail_screen.dart` 486 行找 `_LoreSection` widget:
   - 接 equipment 参数(P1 #42 Phase 5 新增显化)
   - preset 段 vs 延续段 渲染分支
   - `_ContinuedLoreChip` widget 定义(墨青色具体颜色常量)
2. 读 `lib/core/domain/lore.dart` 或类似 Lore model:
   - `isPreset: bool` 字段
   - `Equipment.lores: List<Lore>` 字段
3. 查现有 `test/features/inventory/` 下任何 equipment_detail 相关 test 学 fixture
4. 新增 5 个 testWidgets,group 名「_LoreSection 延续典故渲染」:
   - **A. 仅 preset 段**:fixture equipment 含 3 preset lore 无延续 → pump → 3 段全显 + 0 个 `_ContinuedLoreChip`
   - **B. 仅延续段**:fixture equipment `presetLoreIds: []` + 2 延续 lore(`isPreset=false`)→ 2 个 `_ContinuedLoreChip` + 0 preset 段
   - **C. preset + 延续混排顺序**:fixture 2 preset + 3 延续 → 段顺序谓词:所有 preset 段 widget.y < 所有 `_ContinuedLoreChip` widget.y(用 `tester.getTopLeft(...)` 比较 / 或在 Column 子序集索引验证)
   - **D. 都空**:fixture equipment 全无 lore → `_LoreSection` 显占位或不渲染(以 lib/ 实际为准)
   - **E. `_ContinuedLoreChip` 墨青色**:fixture 1 延续 lore → `_ContinuedLoreChip` 装饰颜色 ≡ `WuxiaColors.<墨青色常量>`(从 lib/shared/theme 等找常量名)
5. **新 test 必须 `flutter test` 全过**。若 `_LoreSection` 是私有类不可外部直接 pump,改为 pump `EquipmentDetailScreen` 完整屏 + find `_ContinuedLoreChip` 类型断言(类型可见性方法:`find.byType` + 内部 widget 用 `find.descendant`)

## 硬约束

- **只新增** `test/features/inventory/presentation/equipment_detail_screen_lore_section_test.dart`
- **不动** `lib/` / `data/` / 其他 test
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`

## 完成动作

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/inventory/presentation/equipment_detail_screen_lore_section_test.dart
git add test/features/inventory/presentation/equipment_detail_screen_lore_section_test.dart
git commit -m "test(nightshift T06): _LoreSection 延续典故渲染 edge(0→1)"
```

报告一句话「T06 done (added N _LoreSection edge cases, 0→1 coverage)」。
