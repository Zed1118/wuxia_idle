# Nightshift T06 · TutorialBannerCard widget test 边界 +5

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T06`(branch `nightshift/T06`,从 `main` HEAD `3d24af1` 起)内独立完成本任务。

## 背景

挂机武侠 Flutter Desktop 武侠 idle 游戏。P1 #42 Phase 2 §10 P1.y(2026-05-18 销账)新落 `TutorialBannerCard` widget(`lib/features/tutorial/presentation/tutorial_banner_card.dart` 108 行),MainMenu 顶部条件渲染 step 6/7/8 hint。当前 `test/features/tutorial/presentation/tutorial_banner_card_test.dart` 100 行有渲染 case + tap 路径覆盖,本 task 补 5 边界 case。

## 红线避坑(memory `feedback_isar_widget_test_deadlock`)

**禁止**写 `onTap → markHintRead → writeTxn` 端到端 testWidgets case(Isar writeTxn 在 testWidgets event loop 死锁 10min)。本批仅补 hintsRead 状态边界 + multi-banner 顺序派生 case,**纯渲染或 mocked tap**,不接真 Isar。

## 目标

改 **唯一**文件:`test/features/tutorial/presentation/tutorial_banner_card_test.dart`(100 → ≥ 130 行,加 5 case)

## 操作

1. 读 `lib/features/tutorial/presentation/tutorial_banner_card.dart` 全文 + `lib/features/tutorial/domain/tutorial_hint_def.dart` 全文 + `lib/shared/strings.dart` 找 step 6/7/8 const
2. 读 `test/features/tutorial/presentation/tutorial_banner_card_test.dart` 当前 case 学体例
3. 加 5 edge case(group 命名「边界」):

   **A. hintsRead 含 [6] 时 step 6 不应显**:这条由 MainMenu 派生 `_firstUnreadHint`,但本 widget 假设父端 filter 过,banner_card 接收的是已 filter 后的 hint;**实际 test 应是测 `_firstUnreadHint` 派生函数**。**先 grep `_firstUnreadHint`** 位置:`lib/features/main_menu/presentation/main_menu.dart`(若在 main_menu 内)→ 本 case 跳过,改为下:**A.** TutorialHintDef.step6 渲染时 icon = `Icons.people_outline`(grep 实际 icon)断言。

   **B. multi-step 派生 firstUnread**:写一个 helper test(group「派生」,**不用 testWidgets,用 test()**),验 `_firstUnreadHint(step=8, hintsRead=[6, 7])` → 返回 step8 hint;`(step=8, hintsRead=[])` → 返回 step6 hint(最早 unread);`(step=7, hintsRead=[6])` → 返回 step7 hint。**注意:若 `_firstUnreadHint` 不是 banner_card_test 文件作用域,改测 `TutorialHintDef.byStep(6/7/8)` 边界 + `TutorialHintDef.byStep(99)` → null fallback**。
   
   **C. TutorialHintDef.byStep 边界**:`byStep(5)` / `byStep(9)` / `byStep(-1)` → null(集合自洽)。
   
   **D. step 7/8 渲染**:沿 step 6 体例,断言 title/body/icon 各自存在(派生从 `TutorialHintDef.step7/step8`)。
   
   **E. 红点 Positioned 可见**:验 `find.byType(Positioned).evaluate().any((e) => ...)` 找到 banner_card 内的红点 Container(grep lib 找红点实际 widget 类型);或断言 step6 卡片 height > 60 默认有 Stack 红点结构。

4. 全 test 跑过 `flutter test test/features/tutorial/presentation/tutorial_banner_card_test.dart`(若实际 widget 与背景描述不符,**以代码实际为准**调整断言,不修 lib/)

## 硬约束

- **只改** `test/features/tutorial/presentation/tutorial_banner_card_test.dart` **一个文件**
- **不动** `lib/` / `data/` / 其他 test/
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `numbers.yaml` / `IDS_REGISTRY.md`
- testWidgets + test 总数 **≥ 8**(原 N + 新 5)
- **不写 writeTxn 端到端 case**(memory `feedback_isar_widget_test_deadlock`)
- 派生函数测试用 `test()` 不用 `testWidgets()`(纯函数 unit)

## 完成动作

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/tutorial/presentation/tutorial_banner_card_test.dart
# 全过后:
git add test/features/tutorial/presentation/tutorial_banner_card_test.dart
git commit -m "test(nightshift T06): TutorialBannerCard 边界 +5 case"
```

报告一句话「T06 done (TutorialBannerCard test +N case, 100 → M 行)」。
