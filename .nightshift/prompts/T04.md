# Nightshift T04 · P3.4 门派事件 Phase 1 spec 起草

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T04`(branch `nightshift/T04`,从 main HEAD 起)内独立完成本任务。

## 背景

P3.4 门派事件 Phase 0 已 done(commit `db2b7d9` 之 PR #5),含 6 维 reality check + Q1-Q5 候选 + P1.2 依赖梳理 + GDD §12.x 升档草案。详 `docs/phase0/p3_4_sect_event_phase0_2026-05-24.md`。

**下一步**:Phase 1 spec doc 起草,沿 P1.2 spec 默认决议版体例(`docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md` 144 行)。Q1-Q5 用 Phase 0 各推荐默认决议(reviewer 实战可用版,用户后续决策可调)。

## 目标

新建 `docs/spec/p3_4_sect_event_spec_2026-05-24.md`(120-160 行),9 节体例(沿 P1.2 spec):

| 节 | 内容 |
|---|---|
| §1 | 范围 IN / OUT(明确 P3.4 cover 什么、留什么给 P5+) |
| §2 | schema(SectEvent Isar @Collection + 字段定义 + composite index) |
| §3 | yaml 数值(numbers.yaml `sect_event.*` 段:触发概率 / 衰减 / 上限) |
| §4 | 行为(SectEventService 触发链路 + sect_reputation 联动 + NpcRelation 联动) |
| §5 | UI(MainMenu / SectScreen 入口 + 事件 dialog 显示) |
| §6 | narrative(data/lore/sect_event/ 体例 + 写 N 条样本占位) |
| §7 | 测试(R1 触发 + R2 联动 + R3 narrative loader + R4 schema 红线) |
| §8 | batch 拆分(Phase 1 spec / Phase 2 schema+yaml / Phase 3 logic / Phase 4 UI / Phase 5 narrative + closeout) |
| §9 | 风险(数值红线 / 触发链路死循环 / sect_reputation 衰减撞 P1.2 reputation 衰减) |

## Q1-Q5 默认决议(沿 Phase 0 推荐,reviewer 实战版)

读 `docs/phase0/p3_4_sect_event_phase0_2026-05-24.md` 找 Q1-Q5 各自候选,挑标"默认/推荐/A 选项"的写到 spec §1。**不要拍 Q1-Q5 真定**(用户决策权留 reviewer),只用作 spec 默认值,文中标注「Q1 默认: ... / 真定见 P3.4 Phase 0 §X」。

## 允许修改

- `docs/spec/p3_4_sect_event_spec_2026-05-24\.md`

## 禁止修改

- `lib/.*`
- `test/.*`
- `data/.*`
- `docs/phase0/.*`(读不改)
- `docs/spec/p1_2_.*`(仅参考)
- `GDD\.md`
- `pubspec.*`

## 操作

### Phase 0 · reality check(5 min)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T04
git log -1 --oneline
ls docs/phase0/p3_4_sect_event_phase0_2026-05-24.md
ls docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md   # 体例参考
wc -l docs/phase0/p3_4_sect_event_phase0_2026-05-24.md  # 应 51
wc -l docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md  # 应 144
# 看 Phase 0 Q1-Q5 候选
cat docs/phase0/p3_4_sect_event_phase0_2026-05-24.md
# 看 P1.2 spec 9 节体例
head -50 docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
```

### Step 1: 起 spec 9 节框架

按上述 §1-§9 顺序写大纲,每节先写 1-2 句先 commit 一版骨架。

### Step 2: 各节填充

- §1 IN/OUT 明确(P3.4 不 cover 战斗机制层,留 P5+);Q1-Q5 默认决议引述
- §2 schema 至少 1 个 @Collection(SectEvent)+ 2-3 字段 + composite index(沿 P1.2 §2 体例)
- §3 yaml 至少 3-5 数值字段(trigger_probability / decay_rate / max_active_events 等)
- §4 行为画触发链路图(arrow chart 文字版)+ 关键 callsite
- §5 UI 列 2-3 widget 入口点
- §6 narrative 写 1 个 sect_event 样本(20-30 行 yaml + 文案)
- §7 R1-R4 测试体例(沿 P1.2 §7)
- §8 5 batch 拆分(对照 P2.2 心魔 Batch 2.1-2.5 节奏)
- §9 风险 3-5 条(数值红线 / 死循环 / 与 P1.2 撞)

### Step 3: 体量校验

```bash
wc -l docs/spec/p3_4_sect_event_spec_2026-05-24.md   # 期望 120-160
```

### Step 4: commit(不 push)

```bash
git add docs/spec/p3_4_sect_event_spec_2026-05-24.md
git commit -m "[spec] P3.4 门派事件 Phase 1 默认决议版 · nightshift T04

- 9 节体例沿 P1.2 spec 144 行
- Q1-Q5 用 Phase 0 推荐默认值(用户决策权留 reviewer)
- 5 batch 拆分对照 P2.2 心魔节奏
"
```

## 不要做

- 不要改 GDD.md(spec doc 是预案,GDD 进 Phase 2/3 才更新)
- 不要动 lib/test/data
- 不要拍 Q1-Q5 真定(标注默认即可,留用户决策)
- 不要写 lore 真文案(只写 1 个样本)
- 不要 git push
- 不要超 160 行(memory feedback_doc_inflation_overnight spec ≤150)
- commit message **必须**含字符串 `nightshift T04`

## 参考

- `docs/phase0/p3_4_sect_event_phase0_2026-05-24.md` Phase 0 51 行
- `docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md` 144 行(体例样本)
- `docs/handoff/p2_x_inner_demon_spec_2026-05-22.md` 心魔 spec(5 batch 拆分参考)
