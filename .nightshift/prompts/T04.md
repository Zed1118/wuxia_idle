# Nightshift T04 · CodexEntryDetail widget test 加固 37→80+

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T04`(branch `nightshift/T04`,从 `main` HEAD `3d24af1` 起)内独立完成本任务。

## 背景

挂机武侠 Flutter Desktop 武侠 idle 游戏。P1 #42 Phase 2 §10 P1.z(2026-05-18 销账)新落 `CodexEntryDetail` widget(`lib/features/codex/presentation/codex_entry_detail.dart`),用 NarrativeReader 体例展示百科条目。当前 `test/features/codex/presentation/codex_entry_detail_test.dart` 仅 **37 行 2 case**(title 渲染 + 多段 paragraphs 渲染),覆盖薄。本 task 补 8-10 边界 case。

## 红线:约束语义不写瞬时事实

参 `feedback_red_line_test_semantics`:测试断言写**白名单 / 集合自洽 / 类型契约**,不写「'段落一', '段落二'」字面计数(用 `paragraphs.length` 派生)。

## 目标

改 **唯一**文件:`test/features/codex/presentation/codex_entry_detail_test.dart`(扩到 ≥ 80 行,2 case → 10+ case)

## 操作

1. 读 `lib/features/codex/presentation/codex_entry_detail.dart` 全文,确认:
   - widget 接口(`CodexEntry entry` 唯一参数)
   - AppBar 标题(title from entry)
   - body 用什么(应为 NarrativeReader 或类似多 page 滚屏)
   - 返回按钮(AppBar.leading 默认 BackButton)
   - 跳过按钮显隐(NarrativeReader.mandatory wire,默认 codex 是 mandatory=false 跳过可见)
2. 读 `lib/features/narrative/presentation/narrative_reader_screen.dart` 知 NarrativeReader page 切换 API
3. 读 `test/features/codex/presentation/codex_entry_detail_test.dart` 当前 2 case 学体例
4. 加 8-10 edge case:
   - **A. 短文 entry(1 段)**:`paragraphs: ['一段']` → 1 段渲染,无 page navigation
   - **B. 中文 entry(5 段)**:`paragraphs.length == 5` 全渲染
   - **C. 长文 entry(8+ 段)**:`paragraphs.length >= 8` 验首段渲染 + 末段可达(若 NarrativeReader 分页)
   - **D. AppBar 返回**:`finder = find.byType(BackButton)` find 1
   - **E. 跳过按钮显隐(mandatory=false)**:`find.text('跳过')` 存在(因 codex entry 默认 mandatory=false)
   - **F. 标题边界(短标题)**:1 字 entry.title `'境'` 完整渲染
   - **G. 标题边界(长标题)**:10+ 字 entry.title 完整渲染或 ellipsis 处理
   - **H. CodexCategory 枚举覆盖**:`CodexCategory.values` 全部 9 值至少各起 1 个 entry 实例 build(types 兼容性)
   - **I. (可选)空 paragraphs fallback**:`paragraphs: []` 不 crash + 渲染 empty hint
   - **J. (可选)scroll**:多段 entry 在小 viewport 可 scroll(`setSurfaceSize(400, 600)` + drag)
5. 全 testWidgets 跑过 `flutter test test/features/codex/presentation/codex_entry_detail_test.dart`(若实际 widget 与背景描述不符,**以代码实际为准**调整断言,不修 lib/)

## 硬约束

- **只改** `test/features/codex/presentation/codex_entry_detail_test.dart` **一个文件**
- **不动** `lib/` / `data/` / 其他 test/
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `numbers.yaml` / `IDS_REGISTRY.md`
- testWidgets 总数 **≥ 10**(原 2 + 新 8+)
- 红线:不写瞬时数字 / 字面值(用 `entry.paragraphs.length` 等派生)

## 完成动作

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/codex/presentation/codex_entry_detail_test.dart
# 全过后:
git add test/features/codex/presentation/codex_entry_detail_test.dart
git commit -m "test(nightshift T04): CodexEntryDetail widget 边界 +8 case"
```

报告一句话「T04 done (CodexEntryDetail testWidgets 2 → N)」。
