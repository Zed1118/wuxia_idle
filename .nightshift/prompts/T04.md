# Nightshift T04 · PROGRESS.md 行数清理 + 归档冲刺

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T04`(branch `nightshift/T04`,从 `main` HEAD `fc25207` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。全局规则:**PROGRESS.md 总行数控制在 100 行内,超出归档到末尾或删除**。当前 PROGRESS.md 约 83 行,逼近 100 红线。W14/W15/W16/W17 详条已迁出但「当前阶段」段最近 4 段(W17 nightshift / W17 候选 E 师徒 panel / W17 候选 B 节日 / W16 节日)行数密集 + 已知偏差段有可压缩条目。

## 红线(本任务专属例外)

**本 task 唯一允许修改 PROGRESS.md** 的 nightshift task。其他 task 严禁动 PROGRESS。本任务**不动** GDD/CLAUDE/numbers/IDS_REGISTRY/data/lib/test。

## 目标

修改 `PROGRESS.md`:压缩行数到 **< 80 行**(留 20+ 行 buffer),保留所有锚点信息,把超长详条迁到 `## 归档` 段或者一句话化。

## 操作

1. 读 `PROGRESS.md` 全文(应 ~83 行,行数若已变更以实际为准)
2. 识别可压缩段:
   - **「当前阶段」段** 最旧的 1-2 段(W16 节日 / W17 候选 B / W17 候选 E)可以**一句话化** → 例「W17 候选 E LineagePanelScreen 全链闭环(commit `9dcfe8a` + Codex 视觉验收 `de7b862`,详 closeout `codex_w17_lineage_panel_visual_check_2026-05-17.md`)」
   - **「已知偏差」段** 已销账条目(已 `~~划掉~~` 的)若已堆 ≥30 个,把全部销账提示「详尾段归档」一句话化,只留**未销账**条目(应只剩 #37 永封档已锁)
   - **归档段** 已有内容**不动**(它本就是归档,再压会失信)
3. **强保留**(不可压缩):
   - 「当前阶段」**最新 1-2 段**(W17 nightshift 6 task + T03 follow-up 删 2 死 provider)— 因为还没归档
   - 「下一步」段
   - 「关键约束」段
   - 「远程仓库」段
4. 写完后 `wc -l PROGRESS.md` 必须 **< 80 行**
5. **不漏锚点**:被压缩的段必须有 git commit hash / closeout 路径 / handoff 路径 / memory id 让后续可追溯

## 写法示例(参考)

**压缩前**(W16 节日段 5 行密集 markdown):
```markdown
**W16 节日 encounter 全链闭环 · framework + DeepSeek 文案 + Mac override + Codex 视觉验收 7 PASS**(2026-05-16):**四段一波完成 GDD §12.4 接口预留 + 内容层首批落地里程碑**。① **W16 framework 0→1**(commit `5ea1f60`)...(此处 5 行 markdown)
```

**压缩后**(1 行):
```markdown
**W16 节日 encounter 全链闭环**(2026-05-16,详 closeout `codex_w16_festival_chip_visual_check_2026-05-16.md`):framework(`5ea1f60`)+ DeepSeek 6 节日文案(`5a3a06c`)+ Mac DEBUG override(`cdee42e`)+ Codex 7 PASS(`9421c55`),759/759。GDD §12.4 节日通道接口+ 内容首批落地里程碑销账。
```

## 硬约束

- **只动** `PROGRESS.md` **一个文件**
- **不动** `lib/` / `test/` / `data/` / `assets/` / `docs/`
- **不动** `GDD.md` / `CLAUDE.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不跑 `flutter pub get` / `dart run build_runner` / `flutter test`(dispatcher 跑 verify)
- 不调用网络

## 完成动作

修改完成后:
```bash
wc -l PROGRESS.md  # 必须 < 80
git add PROGRESS.md
git commit -m "docs(nightshift T04): PROGRESS.md 行数清理压缩到 < 80 行"
```

**不要 push**。
报告一句话「T04 done (PROGRESS.md: X → Y lines)」。
