# Nightshift T04 · BaikeScreen 典故 tab 7 阶分组 + presetLoreIds 段数 edge test

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T04`(branch `nightshift/T04`,从 `main` HEAD `217ddf7` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。P1 #42 Phase 1(2026-05-17)新落 `BaikeScreen`(`lib/features/baike/presentation/baike_screen.dart` 228 行)含 2 tab(见闻 + 典故),**典故 tab 按 7 阶分组装备**(寻常货→像样货→好家伙→利器→重器→宝物→神物,GDD §5.2 锁定),每件装备显化 `presetLoreIds` 段数。

现有 `baike_screen_test.dart` 5 testWidgets:AppBar 标题 + 2 tab + 见闻 tab 空/非空 + 典故 tab 加载/未加载。**未细化**:7 阶分组顺序严格性 / presetLoreIds 段数 0/1/N 三态显化 / 同阶内装备子排序。本任务补 4 个边界,产出独立 `_tier_group_test.dart`。

## 红线:约束语义不写瞬时事实

7 阶顺序断言:用枚举 `EquipmentTier.values` 顺序(白名单)对账 widget 上 chip / heading 顺序,**不写「第 1 个是寻常货」这种瞬时位置硬编码**。段数断言用「presetLoreIds.length 维度等价」非数字。

## 目标

产出**唯一**新文件:`test/features/baike/presentation/baike_screen_tier_group_test.dart`

## 操作

1. 读现有 `test/features/baike/presentation/baike_screen_test.dart` 学 GameRepository override + tab 切换体例
2. 读 `lib/features/baike/presentation/baike_screen.dart` 找典故 tab 的:
   - 7 阶分组方法(按 tier groupBy)
   - 每件装备 widget(presetLoreIds.length 显化)
   - 同阶内装备子排序(id / name / tier 内序号?)
3. 读 `lib/core/domain/realm.dart` 或类似找 `EquipmentTier.values` 枚举顺序
4. 新增 4 个 testWidgets:
   - **A. 7 阶分组顺序严格**:fixture 全 7 阶各 1 装备 + 切典故 tab → 找 7 个 tier heading widget → 顺序 ≡ `EquipmentTier.values`(用 `tester.widgetList<Text>` 提取 + 集合等价校验)
   - **B. presetLoreIds 段数 0 段**:fixture 1 装备 `presetLoreIds: []` → tab → 该装备 widget 显「0 段」或「未录入」(以 lib/ 实际为准)
   - **C. presetLoreIds 段数 1 段**:fixture 1 装备 `presetLoreIds: ['a']` → 显「1 段」
   - **D. presetLoreIds 段数 N 段(混合)**:fixture 3 装备 段数 1/3/5 → 各显对应段数(集合等价)
5. **新 test 必须 `flutter test` 全过**。若同阶子排序有特定规则,加 1 个补充 test

## 硬约束

- **只新增** `test/features/baike/presentation/baike_screen_tier_group_test.dart`
- **不动** `lib/` / `data/` / 其他 test
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`

## 完成动作

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/baike/presentation/baike_screen_tier_group_test.dart
git add test/features/baike/presentation/baike_screen_tier_group_test.dart
git commit -m "test(nightshift T04): BaikeScreen 7 阶分组 + 段数 edge"
```

报告一句话「T04 done (added N tier-group edge cases)」。
