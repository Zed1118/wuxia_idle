# Nightshift T04 · LineagePanelScreen 边界 test 补强

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T04`(branch `nightshift/T04`,从 `main` HEAD `4132a13` 起)内独立完成本任务。

## 背景

W17 候选 E 刚收口的 `LineagePanelScreen`(commit `9dcfe8a`)已有 3 widget test(完整 / 全空 / 仅 founder)。本任务补 3-5 边界用例,提升测试覆盖度。

**已存在**:`test/features/character_panel/presentation/lineage_panel_screen_test.dart`(读它学体例,但**不要改它**)。
**本任务产出**:`test/features/character_panel/presentation/lineage_panel_screen_edge_test.dart`(新文件)。

## 目标

产出**唯一**新文件:`test/features/character_panel/presentation/lineage_panel_screen_edge_test.dart`

## 操作

1. 读现有 3 widget test 学体例:
   - `test/features/character_panel/presentation/lineage_panel_screen_test.dart`(全文)
   - 关键体例:
     - `lineageInfoProvider.overrideWith((ref) async => fixture)` 注入
     - 3 次 `tester.pump()`(不 pumpAndSettle)等 AsyncValue 翻转
     - `mkCharacter` / `mkHeritage` helper(plain Dart 构造)

2. 读 view model 定义看可注入字段:
   - `lib/features/character_panel/application/lineage_info_provider.dart` 中 `LineageInfo` 类(founder / disciples / heritageEquipments)

3. 写 3-5 边界用例,任选(必须有 3 个以上):
   - **大量 heritage**:`heritageEquipments` 长度 10 → 验证「10 件」count 文字 + 10 行 row 渲染(可能触发 SingleChildScrollView 但不要求测 scroll)
   - **founder 但 disciples 含 isFounder=true 异常数据**:fixture 把第 2 个 disciple `isFounder=true`(view model 已经过 provider 过滤,UI 只渲染 `disciples` 列表 → 应正常显 2 弟子 chip,不再渲染为 founder)
   - **heritage 装备 isLineageHeritage=false**:fixture 直接给 `heritageEquipments=[Equipment.create(isLineageHeritage=false)]`(view model 不会过滤,UI 直接显)— **跳过此用例**(测的是 provider 过滤,本 screen 不过滤)
   - **多 disciples**:3 个 disciples(Demo Phase 5+ 路线),验证 3 个 chip 渲染 + 间距
   - **founder + disciples 全在,heritage 空**:验证段标题「师承遗物」+ 空态文案「尚未拥有师承遗物」+ 无「N 件」计数
   - **founder 有 school=null**:`_CharacterChip` 流派色条走 `WuxiaColors.textMuted` 兜底,不抛错

4. 红线遵守 `feedback_red_line_test_semantics`:**约束语义不写瞬时事实**(如「heritage 长度 N → 找到 N 行」而非「找到 10 件磨剑石」)

5. 测试通过 `flutter test test/features/character_panel/presentation/lineage_panel_screen_edge_test.dart` 必须全过

## 硬约束

- **不动** 现有 `test/features/character_panel/presentation/lineage_panel_screen_test.dart` 或任何其他 test 文件
- **不动** `lib/` 任何文件
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- **不引入** 新 import 项目外的 package(只用已有 deps)
- 不调用网络
- 不跑 `dart run build_runner`(本任务不新增 @riverpod;若 verify 失败因 codegen 过期,记 SKIPPED 不强求)

## 完成动作

test 写好后,本地跑一次确认:
```bash
flutter pub get
flutter test test/features/character_panel/presentation/lineage_panel_screen_edge_test.dart
```
全过后:
```bash
git add test/features/character_panel/presentation/lineage_panel_screen_edge_test.dart
git commit -m "test(nightshift T04): LineagePanelScreen 边界用例 +3-5"
```

**不要 push**。报告一句话「T04 done,N 用例全过」(N = 实际写的用例数)。

## 注意

如果撞坑(test 跑不过 / fixture 构造失败 / Character.create / Equipment.create 签名变更),**不要回滚**,把已写的部分提交并报告问题。dispatcher 标 SKIPPED 不影响后续 T05/T06。
