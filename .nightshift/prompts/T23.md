# Nightshift T23 · 6 关键问题 5 小项合批修(SHA / spec 砍 / 字面引用 / 死字段 / 恒等断言)

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T23`(branch `nightshift/T23`,从 main HEAD 起)内独立完成本任务。

## 背景

2026-05-25 nightshift 6h 挂机批 + retry T17b/T19b 全闭环后,审查发现 6 关键问题(详 `docs/handoff/session_closeout_2026-05-25_nightshift_6h_review.md` §5)。本 task 合批修 5 小项;剩 1 大项(T17 EncounterIntegration 真 wire)由 T24 xhigh 处理。

## 目标(5 子修)

### 1. T20 R5.8/R5.9 恒等断言修(`test/audit/cross_system_damage_test.dart:164,177`)

现状:
```dart
expect(specClampMax, lessThanOrEqualTo(1.25),  // line 164
expect(singleStrategyMaxApm, lessThanOrEqualTo(1.15),  // line 177
```

这是**恒等断言**(1.25 ≤ 1.25 永真),违 memory `feedback_red_line_test_semantics`(白名单/集合自洽优于具体数字)。

改:从 `NumbersConfig.fromYaml` 加载真值再断言:
- 找 numbers.yaml 中 P3.1 terrain / P3.2 formation 的 attackPowerMultiplier 上限字段
- test setUp 加载 NumbersConfig,断言用 `lessThanOrEqualTo(numbersConfig.<path>)` 替硬编码

### 2. T18 narrative「听雨剑」字面引用删(`data/lore/pvp/pvp_event_rank_up_yiLiu.yaml`)

现状:rank_up_yiLiu 通用文案中含「听雨剑」字面,违 narrative 抽象统一(rank_up 不应锁特定招式)。

改:把「听雨剑」改为通用表述(「招式」/「秘技」/「剑意」等保留意境)。**不要动** `data/narratives/techniques/insights/ting_yu_jian.yaml`(那是听雨剑真领悟文案,合理)。

### 3. T18 loader 测 `≤6` → `≤4`(对齐 spec)

Phase 0 grep 定位:`grep -rn "lessThanOrEqualTo(6)\|<= 6" test/ | grep -E "narrative|rank_up|lore|pvp"`。把命中的 `≤6` 测断言改 `≤4`。若找不到,grep `test/data/narratives/` `test/features/pvp/` 扩展。

### 4. T17 enemyAttackPowerMult 死字段加注释(`lib/data/numbers_config.dart:1528`)

现状:字段定义在 NumbersConfig 但只有 test 引用一次(`test/features/jianghu/jianghu_r5_test.dart:67`),实战代码 0 caller。

改:`lib/data/numbers_config.dart:1528` 字段定义行**前**加注释:
```dart
/// TODO B3+ 接入:未来 PVP 镜像配置(对手 NPC 攻击倍率)预留;
/// 目前仅 schema 占位,实战代码 0 caller(R5 schema 校验已覆盖)。
final double enemyAttackPowerMult;
```

**不砍字段**(留 P5+ 扩展点)。若 `data/numbers.yaml` 同名段也有(Phase 0 grep 验),同步加注释。

### 5. T21 P4.1 spec 砍 ≤150 行(`docs/spec/p4_1_sect_management_spec_2026-05-25.md`)

现状:166 行超 150 上限(memory `feedback_doc_inflation_overnight`)。

改:压缩 §3 service 段 + §7 R5 段(各砍 5-10 行),保留 Q1-Q8 决议 + 实装清单核心。`wc -l` 验 ≤ 150。

### 6. T22 SHA 错 `4e79722` → `68c816d`(2 文件)

- `docs/handoff/6h_unattended_handoff_2026-05-25.md:21`
- `docs/handoff/stage_audit_1_0_overall_2026-05-25.md:27`

用 `perl -i -pe 's/4e79722/68c816d/g'` 仅这 2 文件。**不要动** `docs/handoff/session_closeout_2026-05-25_nightshift_6h_review.md:63`(那是 review doc 记 SHA 错的史实,保留原样)。

## 允许修改(path_guard 白名单)

- `test/audit/cross_system_damage_test\.dart`
- `data/lore/pvp/pvp_event_rank_up_yiLiu\.yaml`
- `test/data/.+\.dart|test/features/(pvp|inheritance|jianghu)/.+\.dart`(Phase 0 定位后窄化)
- `lib/data/numbers_config\.dart`
- `data/numbers\.yaml`
- `docs/spec/p4_1_sect_management_spec_2026-05-25\.md`
- `docs/handoff/(6h_unattended_handoff|stage_audit_1_0_overall)_2026-05-25\.md`

## 禁止修改

- 任何不在白名单的 lib/ 改动(尤其 features/battle, features/encounter)
- `docs/handoff/session_closeout_2026-05-25_nightshift_6h_review.md`(史实)
- `data/narratives/techniques/insights/ting_yu_jian.yaml`(听雨剑真领悟)
- `pubspec.yaml` / `GDD.md` / `CLAUDE.md` / `data_schema.md` / `IDS_REGISTRY.md`

## Phase 0 · reality check + memory recall(必跑,5min)

**memory grep**(verify spec 起草前必查 · memory `feedback_nightshift_v2_first_run_lessons` A9):
- `feedback_red_line_test_semantics`(R5 恒等断言修法)
- `feedback_nightshift_v2_first_run_lessons` A1-A9(verify 避坑)
- `feedback_doc_inflation_overnight`(spec/doc 体量上限)
- `feedback_edit_chinese_punctuation_pitfall`(中文标点编辑)

**reality check**:
```bash
cd <worktree>
git log -1 --oneline
grep -nE "lessThanOrEqualTo\(1\.(25|15)\)" test/audit/cross_system_damage_test.dart
grep -n "听雨剑" data/lore/pvp/pvp_event_rank_up_yiLiu.yaml
grep -rn "lessThanOrEqualTo(6)\|<= 6" test/ | grep -E "narrative|rank_up|lore|pvp" | head -5
grep -n "enemyAttackPowerMult" lib/data/numbers_config.dart data/numbers.yaml 2>/dev/null
wc -l docs/spec/p4_1_sect_management_spec_2026-05-25.md
grep -n "4e79722" docs/handoff/6h_unattended_handoff_2026-05-25.md docs/handoff/stage_audit_1_0_overall_2026-05-25.md
```

## 操作

### Step 1: R5.8/R5.9 恒等断言修
- Read `test/audit/cross_system_damage_test.dart:140-200`
- 找 NumbersConfig terrain/formation cap 字段(Phase 0 grep `terrainConfig\|formationConfig\|damage_multiplier_max`)
- 改测从 NumbersConfig 加载,断言用 `lessThanOrEqualTo(numbersConfig.<path>)`
- 跑 `flutter test test/audit/cross_system_damage_test.dart` 验

### Step 2: narrative「听雨剑」+ loader 测 ≤4
- Edit `data/lore/pvp/pvp_event_rank_up_yiLiu.yaml` 替「听雨剑」
- 找 loader 测(Phase 0 grep)改 ≤6 → ≤4
- 跑相关测验

### Step 3: enemyAttackPowerMult 注释
- Edit `lib/data/numbers_config.dart:1528` 前加 TODO 注释
- 若 numbers.yaml 同名(Phase 0 grep)同步

### Step 4: T21 spec ≤150
- Read `docs/spec/p4_1_sect_management_spec_2026-05-25.md`
- 压 §3 service + §7 R5(各 5-10 行)
- `wc -l` 验 ≤150

### Step 5: SHA 替
- `perl -i -pe 's/4e79722/68c816d/g' docs/handoff/6h_unattended_handoff_2026-05-25.md docs/handoff/stage_audit_1_0_overall_2026-05-25.md`
- 跑 grep 验 2 文件 0 `4e79722`

### Step 6: 全测 + analyze
- `flutter analyze`(应 0)
- `flutter test test/audit/ test/data/ test/features/jianghu/ test/features/inheritance/`

## 完成后

git commit,commit message 必须含 `nightshift T23` 子串。

## 不要做

- 标准 nightshift 红线(见 `_PROMPT_TEMPLATE.md`)
- 不顺手优化无关代码(memory `feedback_layered_bugs`)
- 不为让 verify 过而降断言

## 参考

- closeout §5:`docs/handoff/session_closeout_2026-05-25_nightshift_6h_review.md`
- memory:`feedback_red_line_test_semantics`, `feedback_doc_inflation_overnight`
