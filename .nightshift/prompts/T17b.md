# Nightshift T17b · P1.2 B3 UI + B4 R5 + closeout 续作(T17 budget exceeded 后 retry)

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T17b`(branch `nightshift/T17b`,从 main HEAD 起)内独立完成本任务。

## 背景

**前批失败上下文**:T17 spec 拆 P1.2 全 4 batch · budget=$8 跑到 B2 用完被截断 · B1+B2 真产出 cherry-pick 到 main(`68c816d` + `ed0f862`)· **B3 UI + B4 R5 + closeout 待续**。

**本 task 续作**(base 是 main,已含 B1+B2):
- B3 UI:ReputationPanelScreen + ReputationTierChip + main_menu 江湖 入口 + UiStrings 12 段 + battle setup attackPowerMultiplier 注入 + main_menu_test 入口数 +1 + reputation_panel_screen_test
- B4 R5:R5.1-5.6 ~10-12 测族 + closeout doc ≤80 行

**budget**:$15(本次 conf 已升 · 不再 exceeded)

**关键 spec 锚点**(全引 spec doc):
- §4 UI(`docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md` §4)
- §5 战斗 attackPowerMultiplier 烘焙(沿 `light_foot_strategy.dart:120` + `mass_battle_strategy.dart:182` 体例)
- §7 R5(5-6 族 ~10-12 测)

## 目标

### B3 UI(~25min · ~12 文件)

| # | 产出 | 锚点 |
|---|---|---|
| B3.1 | `lib/shared/strings.dart` 加 ~12 段(mainMenuJianghu + Hint + reputationPanelTitle + reputationTier* 7 阶 + enmityWarning + panelFriendSection + panelFoeSection) | spec §4 |
| B3.2 | `lib/features/jianghu/presentation/reputation_panel_screen.dart`(新 · 沿 lineage_panel_screen 卡片三段式) | spec §4 |
| B3.3 | `lib/features/jianghu/presentation/widgets/reputation_tier_chip.dart`(新 · 7 阶颜色梯度) | spec §4 |
| B3.4 | `lib/features/main_menu/presentation/main_menu.dart` 加 江湖 _MenuButton(PVP 后 Leaderboard 前)| spec §4 |
| B3.5 | `lib/core/application/battle_providers.dart` startBattle 注入 attackPowerMultiplier(查 NpcRelationService.attackPowerMultFor)| spec §5 |
| B3.6 | `test/features/main_menu/presentation/main_menu_test.dart` 入口 17→18 | UI 测 |
| B3.7 | `test/features/jianghu/reputation_panel_screen_test.dart`(新 R4 3-5 测) | UI 测 |
| B3.8 | commit `[feat] P1.2 B3 UI + battle · nightshift T17b B3` | — |

### B4 R5 + closeout(~20min)

| # | 产出 | 锚点 |
|---|---|---|
| B4.1-4.6 | `test/features/jianghu/jianghu_r5_test.dart`(新 · ~10-12 测) | spec §7 |
| B4.7 | `docs/handoff/p1_2_jianghu_full_closeout_2026-05-25.md` ≤80 行 | doc_inflation |
| B4.8 | commit `[feat] P1.2 B4 R5 + closeout · nightshift T17b B4` | — |

**R5 测族**:
- R5.1 reputation 7 阶 e2e 21 测点 sweep + clamp ±100
- R5.2 enmity 阈值 5 测(level=-49/-50/-51/-80/-81)
- R5.3 §5.4 红线 ≤8000 普伤(wuSheng·dengFeng + enmity 1.25 fixture)
- R5.4 trigger e2e 3 测(stage_boss_kill + rival_kill + encounter delta)
- R5.5 §5.2 七阶 label 锁(reputationTiers 等 xueTu..wuSheng)
- R5.6 P3.4 隔离 schema 断言(Reputation collection name ≠ 'SectReputation')

## 允许修改(verify_path_guard 白名单)

- `lib/features/jianghu/presentation/.+\.dart`
- `lib/features/main_menu/presentation/main_menu\.dart`
- `lib/shared/strings\.dart`
- `lib/core/application/battle_providers\.dart`
- `lib/core/application/battle_providers\.g\.dart`
- `test/features/jianghu/.+\.dart`
- `test/features/main_menu/presentation/main_menu_test\.dart`
- `docs/handoff/p1_2_jianghu_full_closeout_.+\.md`
- `pubspec\.lock`

## 禁止修改

- `lib/features/jianghu/(domain|application)/`(B1+B2 已 ship · 0 改)
- `lib/data/`(B1 已 ship JianghuConfig · 0 改)
- `data/`(B1 已 ship yaml · 0 改)
- `lib/features/(?!jianghu|main_menu)[^/]+/`
- `lib/features/battle/domain/strategy/.*\.dart`(战斗策略不动)
- `lib/main\.dart`
- `GDD\.md` / `CLAUDE\.md` / `ROADMAP_1_0\.md` / `PROGRESS\.md`
- `pubspec\.yaml` / `.env.*`

## Phase 0 · reality check(必跑,3 min)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T17b
git log -1 --oneline                                                  # 应含 ed0f862(B2 service 已在 main)
git log --oneline | grep "nightshift T17"                             # 应见 B1 + B2 commit
ls lib/features/jianghu/                                              # 应见 domain/ + application/(B1+B2 已 ship)
ls lib/features/jianghu/domain/ lib/features/jianghu/application/
grep -nE "class Reputation\b|class NpcRelation\b" lib/features/jianghu/domain/*.dart
grep -nE "class ReputationService|class NpcRelationService" lib/features/jianghu/application/*.dart
grep -nE "class JianghuConfig|jianghu\b" lib/data/numbers_config.dart | head -5
grep -nE "ReputationSchema\|NpcRelationSchema" lib/data/isar_setup.dart
grep -c "_MenuButton" lib/features/main_menu/presentation/main_menu.dart  # 当前入口数(应 17)
sed -n '111,125p' lib/features/battle/domain/strategy/light_foot_strategy.dart  # attackPowerMultiplier 烘焙体例(P3.1.B)
sed -n '169,185p' lib/features/battle/domain/strategy/mass_battle_strategy.dart  # 同体例(P3.2)
find lib/features/inheritance/presentation -name "lineage_panel*"    # 卡片体例参考
flutter pub get && dart run build_runner build                       # 确保 .g.dart 在 worktree 也生成
flutter analyze 2>&1 | tail -3                                       # baseline 应 0 issue
```

## 操作

### B3 Step 1: UiStrings 12 段

`lib/shared/strings.dart` 加(沿 mainMenuPvp/Hint 体例):

```dart
static const String mainMenuJianghu = '江湖恩怨';
static const String mainMenuJianghuHint = '声望 7 阶 + 多门派关系';
static const String reputationPanelTitle = '江湖声望';
static const String reputationTierXueTu = '声名狼藉';
static const String reputationTierSanLiu = '恶名';
static const String reputationTierErLiu = '默默无闻';
static const String reputationTierYiLiu = '薄有微名';
static const String reputationTierJueDing = '侠名初显';
static const String reputationTierZongShi = '声振江湖';
static const String reputationTierWuSheng = '天下闻名';
static const String enmityWarning = '当前有敌对 NPC';
static const String panelFriendSection = '盟友';
static const String panelFoeSection = '敌对';
```

### B3 Step 2: ReputationTierChip widget

新 `lib/features/jianghu/presentation/widgets/reputation_tier_chip.dart`(沿 RankBadge 体例):

```dart
import 'package:flutter/material.dart';

class ReputationTierChip extends StatelessWidget {
  final String tier;     // xueTu..wuSheng
  final int value;
  const ReputationTierChip({super.key, required this.tier, required this.value});

  Color _color() {
    switch (tier) {
      case 'xueTu':    return Colors.grey;
      case 'sanLiu':   return Colors.deepOrange.shade300;
      case 'erLiu':    return Colors.grey.shade400;
      case 'yiLiu':    return Colors.teal.shade300;
      case 'jueDing':  return Colors.amber.shade300;
      case 'zongShi':  return Colors.amber.shade700;
      case 'wuSheng':  return Colors.red.shade700;
      default:         return Colors.grey;
    }
  }

  String _label() {
    switch (tier) {
      case 'xueTu':    return UiStrings.reputationTierXueTu;
      // ... 7 阶
    }
    return '';
  }

  @override
  Widget build(BuildContext context) {
    return Chip(label: Text(_label()), backgroundColor: _color());
  }
}
```

### B3 Step 3: ReputationPanelScreen

新 `lib/features/jianghu/presentation/reputation_panel_screen.dart`(沿 lineage_panel_screen 卡片三段式):

```dart
class ReputationPanelScreen extends ConsumerWidget {
  const ReputationPanelScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final reputations = ref.watch(reputationsForCurrentPlayerProvider);
    return Scaffold(
      appBar: AppBar(title: const Text(UiStrings.reputationPanelTitle)),
      body: reputations.when(
        data: (list) {
          if (list.isEmpty) return const Center(child: Text('暂无声望记录'));
          return ListView(
            children: [
              // ... 顶部主阶 chip + 门派 list + foe/friend 分组
            ],
          );
        },
        loading: () => const Center(child: CircularProgressIndicator()),
        error: (e, _) => Center(child: Text('加载失败:$e')),
      ),
    );
  }
}
```

### B3 Step 4: main_menu 江湖 入口

`lib/features/main_menu/presentation/main_menu.dart`:
- 在 PVP `_MenuButton` 之后、Leaderboard `_MenuButton` 之前插江湖 `_MenuButton`
- 入口数 17→18

### B3 Step 5: battle_providers 注入 attackPowerMultiplier

`lib/core/application/battle_providers.dart` startBattle 入口:

```dart
// 沿 mass_battle_strategy.dart:182 体例(SET 不乘 · battle setup 阶段烘焙)
final npcService = ref.read(npcRelationServiceProvider);
final playerCharId = save.activeCharacterIds.first;   // 沿 founder_buff_service 体例
for (var i = 0; i < rightTeam.length; i++) {
  final mult = await npcService.attackPowerMultFor(playerCharId, rightTeam[i].id);
  if (mult != 1.0) {
    leftTeam[0] = leftTeam[0].copyWith(attackPowerMultiplier: mult);   // SET
    rightTeam[i] = rightTeam[i].copyWith(attackPowerMultiplier: mult); // 双向对等
  }
}
```

**不变量守**:
- 双向对等(spec §2 enemy_attack_power_mult = player_attack_power_mult = 1.15)
- clamp ≤ 1.25(`numbers.jianghu.enmityCombatModifier.clampMax`)
- 0 strategy 改(strategy 已支持 attackPowerMultiplier)

### B3 Step 6/7: main_menu_test 17→18 + reputation_panel_screen_test

main_menu_test:把 `_MenuButton` 数从 17 改 18 + 加江湖 button finder。

reputation_panel_screen_test 3-5 测:
- empty state 显「暂无声望记录」
- 注入 3 门派 reputation 显 3 row
- ReputationTierChip xueTu/yiLiu/wuSheng 三阶颜色 + label
- UiStrings 7 阶 tier 字符串非空

**memory `feedback_listview_widget_test_viewport`**:≥7 行 ListView 用 `tester.binding.setSurfaceSize(Size(800, 2000)) + addTearDown`。

### B3 Step 8: commit B3

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter analyze --fatal-warnings
flutter test test/features/jianghu/ test/features/main_menu/presentation/
git add lib/features/jianghu/presentation/ lib/features/main_menu/ lib/shared/strings.dart lib/core/application/battle_providers.dart test/features/jianghu/ test/features/main_menu/presentation/
git commit -m "[feat] P1.2 B3 UI + battle · ReputationPanelScreen + main_menu + attackPowerMultiplier 注入 · nightshift T17b B3

- ReputationPanelScreen(3 段式卡片 · 沿 lineage_panel_screen 体例)
- ReputationTierChip widget(7 阶颜色梯度)
- main_menu 入口 17→18(江湖 在 PVP 后 Leaderboard 前)
- UiStrings 12 段(mainMenuJianghu + reputationTier* 7 + enmityWarning + panel*)
- battle_providers.startBattle 注入 attackPowerMultiplier(沿 P3.1.B/P3.2 体例 · strategy 0 改)
- R4 widget 测 3-5 + main_menu test 入口 +1

详 docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md §4/§5
"
```

### B4 Step 1-6: R5 测族 ~10-12 测

`test/features/jianghu/jianghu_r5_test.dart`(新):

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:isar_community/isar.dart';

void main() {
  group('R5 P1.2 江湖恩怨+声望 红线', () {
    late Isar isar;
    late ReputationService repService;
    late NpcRelationService npcService;

    setUp(() async {
      // 内存 Isar fixture · NumbersConfig fixture
    });

    tearDown(() async {
      await isar.close(deleteFromDisk: true);
    });

    group('R5.1 reputation 7 阶 e2e + clamp', () {
      // 21 测点 sweep + 边界外 ±150 clamp ±100
    });

    group('R5.2 enmity 阈值', () {
      test('level=-49 attackPowerMultFor 返 1.0', () async { ... });
      test('level=-50 attackPowerMultFor 返 1.15', () async { ... });
      test('level=-51 attackPowerMultFor 返 1.15', () async { ... });
      test('level=-80 attackPowerMultFor 返 1.25', () async { ... });
      test('level=-81 attackPowerMultFor 返 1.25 (clamp_max)', () async { ... });
    });

    group('R5.3 战斗 §5.4 cap 不越', () {
      test('wuSheng·dengFeng + enmity 1.25 fixture · default_ground_strategy 输出 ≤ 8000 普伤红线', () { ... });
    });

    group('R5.4 trigger e2e', () {
      // stage_boss_kill + rival_kill + encounter delta 各 1 测
    });

    group('R5.5 §5.2 七阶 label 锁', () {
      test('numbers.jianghu.reputationTiers 7 阶等 §5.2 七阶', () { ... });
    });

    group('R5.6 P3.4 隔离 schema 断言', () {
      test('Reputation collection name 不与 SectReputation 撞', () {
        expect(ReputationSchema.name, isNot(equals('SectReputation')));
      });
    });
  });
}
```

### B4 Step 7: closeout doc ≤80 行

新 `docs/handoff/p1_2_jianghu_full_closeout_2026-05-25.md`:

```
# P1.2 §12.1+§12.2 江湖恩怨+声望 全 4 batch closeout · nightshift T17 (B1+B2) + T17b (B3+B4)

## TL;DR(≤5 行)
P1.2 全 4 batch 完整闭环 · 跨 2 batch dispatch · main HEAD <sha> · 4 commit · baseline +N test · 0 analyze · 1.0 整体 78→85%。

## Commit timeline
| Batch | SHA | Origin |
|---|---|---|
| B1 schema | 68c816d | T17(2026-05-25 01:05)|
| B2 service | ed0f862 | T17(2026-05-25 01:10)|
| B3 UI+battle | <sha> | T17b |
| B4 R5+closeout | <sha> | T17b |

## R5 测族结果
- R5.1-5.6 ~10-12 测全过 / baseline 1414 + delta ~12

## 工作量复盘
spec 估 ~7h xhigh / T17+T17b nightshift wall 总 ~XX min / ratio 0.XX(原 T17 budget exceeded 是 retry 原因)

## 挂账(下波)
- Q3 C 心法触发 1.1 / Q4 D 援军 stage 1.1 / Q5 C 双轴 1.1
- Phase 5 narrative 江湖 encounter 完整文案 8-12 条留 1.1
- StageDef.npcId 真 NPC schema 实装(本 task 字符串占位)
- attackPowerMultiplier 注入 battle setup edge case audit(可能与 P3.1.B/P3.2 烘焙顺序冲突 · T20 audit 已实测 SET 不冲突 → 无 audit 风险)

## 不变量沿用
- §5.4 红线 ≤8000 普伤(R5.3 + T20 audit 双校验)
- §5.3 七阶锁(R5.5 校验)
- §5.5 在线=离线 · §5.1 反留存
- BattleStrategy / DamageCalculator 0 改 · founder_buff_service 0 改
```

### B4 Step 8: commit B4(verify 必抓 last `nightshift T17b`)

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter analyze --fatal-warnings
flutter test test/features/jianghu/
git add test/features/jianghu/jianghu_r5_test.dart docs/handoff/p1_2_jianghu_full_closeout_2026-05-25.md
git commit -m "[feat] P1.2 B4 R5 + closeout · 10-12 测 + closeout doc · nightshift T17b B4

- R5.1 reputation 7 阶 e2e + clamp 双向(21 测点 sweep)
- R5.2 enmity 阈值 5 测(level=-49/-50/-51/-80/-81)
- R5.3 §5.4 红线 ≤8000 普伤(wuSheng·dengFeng + enmity 1.25 fixture)
- R5.4 trigger 3 测
- R5.5 §5.2 七阶 label 锁
- R5.6 P3.4 隔离 schema 断言
- closeout 80 行 + 挂账 4 项

P1.2 100% 完整闭环 · 1.0 整体 78→85% 跳档。

详 docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
"
```

## 不要做

- **🚨 严禁 cd / git -C 到 worktree 之外**(v2 A1)
- 不要重写 B1+B2 内容(已在 main · 0 schema/service 改动)
- 不要改 BattleStrategy / DamageCalculator
- 不要 git push
- 不要顺手改 GDD.md / CLAUDE.md / PROGRESS.md / ROADMAP_1_0.md
- 不要写 testWidgets + writeTxn(memory `feedback_isar_widget_test_deadlock`)
- 不要破红线 §5.4(R5.3 校验)
- B3 Step 5 battle_providers 注入只在 startBattle 入口烘焙一次(0 strategy 改)

## 参考

- **spec**: `docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md` §4/§5/§7(全引)
- **B1+B2 已 ship code**:
  - `lib/features/jianghu/domain/reputation.dart` + `npc_relation.dart`
  - `lib/features/jianghu/application/reputation_service.dart` + `npc_relation_service.dart` + `jianghu_providers.dart`
  - `lib/data/numbers_config.dart` `JianghuConfig` class
  - `data/numbers.yaml jianghu` 段 + `data/factions.yaml`
- **UI 体例**: `lib/features/inheritance/presentation/lineage_panel_screen.dart`(3 段式卡片)
- **attackPowerMultiplier 烘焙体例**:
  - `lib/features/battle/domain/strategy/light_foot_strategy.dart:120`(P3.1.B SET)
  - `lib/features/battle/domain/strategy/mass_battle_strategy.dart:182`(P3.2 SET)
  - `lib/features/battle/domain/strategy/default_ground_strategy.dart:420`(末端乘)
- **测体例**: `test/features/inheritance/founder_buff_service_test.dart` + 已有 `test/features/jianghu/reputation_service_test.dart`(B2 已 ship · sibling 参考)
- **memory**:`feedback_isar_widget_test_deadlock` / `feedback_listview_widget_test_viewport` / `feedback_red_line_test_semantics`
