# Nightshift T02 · widget test pattern 审计(pumpAndSettle 风险扫描)

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T02`(branch `nightshift/T02`,从 `main` HEAD `4132a13` 起)内独立完成本任务。

## 背景

挂账 #31(已销账,commit `4aa54fa`)真解 = NavigatorObserver mock 套路 — tap push 按钮后**单帧 `tester.pump()` 不 settle**,绕过子屏 build 内 `AsyncValue.loading` + Isar zone 死循环。本任务审计全仓 `pumpAndSettle` 用法,标记还能套用本套路降低死循环风险的潜在点。

## 目标

产出**唯一**新文件:`docs/handoff/wuxia_widget_test_pattern_audit_2026-05-17.md`

## 操作

1. 全仓 grep `pumpAndSettle` 用法:
   ```bash
   grep -rn "pumpAndSettle" test/ --include="*.dart" > /tmp/pumpAndSettle_hits.txt
   wc -l /tmp/pumpAndSettle_hits.txt
   ```
2. 对每处 hit 评估风险类别:
   - **A 低风险**:pump dialog / pump SnackBar / pump 内部 setState(无 push + 无 Isar provider 链)
   - **B 中风险**:pump 后子屏内有 AsyncValue.loading 但已有 fake provider override(W15 Phase 5 #2 Consumer 化套路)
   - **C 高风险候选**:pump 按钮 tap 后 push 新 screen 且子屏 watch Isar provider 链 → 与 #31 同型,可套用 NavigatorObserver mock 改写
3. 读 `test/features/main_menu/presentation/main_menu_test.dart` 末尾的 `_RecordingNavigatorObserver` 实现(commit `4aa54fa` 加),把模式片段抄到 doc 内供参考
4. 写 doc,结构:

```markdown
# Widget Test Pattern 审计 · pumpAndSettle 死循环风险扫描(2026-05-17)

> Nightshift T02 产出。挂账 #31 销账后 NavigatorObserver mock 套路总结 + 全仓 pumpAndSettle 风险扫描 + 推荐替换清单。

## 1. NavigatorObserver Mock 套路(W17 #31 销账 commit `4aa54fa`)

(贴 _RecordingNavigatorObserver 源码 + 用法 + 适用场景,~40 行)

## 2. 全仓 pumpAndSettle 用法扫描

共 N 处 hit(grep `pumpAndSettle` test/ 命中数)。按风险分:
- 低风险 X 处
- 中风险 Y 处
- 高风险候选 Z 处(可套用 NavigatorObserver mock)

### 2.1 低风险(A 类)
| 文件 | 行号 | 用法摘录 | 类别 |
|---|---|---|---|
| ... | ... | dialog 关闭等 | A |

### 2.2 中风险(B 类)
(同表格,但说明已有 fake override 兜底)

### 2.3 高风险候选(C 类,推荐改写)
| 文件 | 行号 | 用法摘录 | 是否 push 按钮 tap | 推荐改写 |
|---|---|---|---|---|

## 3. 推荐替换清单

(列出 0-3 个 C 类候选 + 改写方案 sketch,**不真改代码**,留下次 sprint)

## 4. 后续 follow-up

(若发现 0 高风险候选,正面结论;若发现 N 候选,建议下次起 small task 改写)
```

## 硬约束

- **不动** `lib/` 任何文件
- **不动** `test/` 任何已存在文件(只产出 docs/)
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不跑 `flutter pub get` / `dart run build_runner` / `flutter test`(dispatcher 跑 verify)
- 不调用网络

## 完成动作

doc 写好后:
```bash
git add docs/handoff/wuxia_widget_test_pattern_audit_2026-05-17.md
git commit -m "docs(nightshift T02): widget test pattern 审计 · pumpAndSettle 风险扫描"
```

**不要 push**。报告一句话「T02 done」。
