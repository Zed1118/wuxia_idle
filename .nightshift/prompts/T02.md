# Nightshift T02 · BreakthroughBlocker 集成 character_panel

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T02`(branch `nightshift/T02`,从 main HEAD 起)内独立完成本任务。

## 背景

`InnerDemonBreakthroughBlocker` widget 已存在 `lib/features/inner_demon/presentation/breakthrough_blocker.dart`,P2.2 Batch 2.2/2.5 全闭环 ✅。**唯一遗留**:未集成到 character_panel 拦截 wuSheng 突破。

详 `docs/handoff/p2_x_inner_demon_implementation_closeout_2026-05-22.md` line 86 + `p2_x_inner_demon_final_closeout_2026-05-23.md` line 43:
- 显示条件:`advancement.layersGained=0 + ch.experience >= ch.experienceToNextLayer`
- character_panel 当前 1257 行,集成需 reactive provider + 找合适插入点

## 目标

在 `lib/features/character/presentation/character_panel.dart` 接 BreakthroughBlocker widget,wuSheng 阶心魔关未过时 → 阻拦升层 UI。

具体:
1. 找 character_panel 显示「升层 / 突破」按钮 / 提示的位置(grep `experienceToNextLayer` / `advancement` / `layersGained`)
2. 加 ConsumerWidget 或 Riverpod ref 读 `mainlineProgressProvider` 拿 `clearedStageIds`
3. wuSheng 阶 + 心魔关未过 + `experience >= experienceToNextLayer` → 显示 BreakthroughBlocker
4. 其他阶不变(qiMeng-huaJing 6 阶正常升层)

## 允许修改

- `lib/features/character/presentation/character_panel\.dart`
- `lib/features/inner_demon/presentation/breakthrough_blocker\.dart`(若需补 export)
- `test/features/character/presentation/.*\.dart`(可选,加 widget test 验集成)

## 禁止修改

- `data/.*`
- `lib/features/inner_demon/(application|domain)/.*`(provider/service 不动)
- `lib/features/inner_demon/presentation/inner_demon_screen\.dart`(不动)
- `lib/main\.dart`
- `pubspec.*`
- `\.env.*`

## 操作

### Phase 0 · reality check(5 min)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T02
git log -1 --oneline
wc -l lib/features/character/presentation/character_panel.dart
wc -l lib/features/inner_demon/presentation/breakthrough_blocker.dart
grep -n "experienceToNextLayer\|layersGained\|wuSheng" lib/features/character/presentation/character_panel.dart | head -10
grep -n "ConsumerWidget\|ref\.watch" lib/features/character/presentation/character_panel.dart | head -10
# 看 BreakthroughBlocker 现有签名
head -50 lib/features/inner_demon/presentation/breakthrough_blocker.dart
# 看 mainlineProgressProvider 在哪
grep -rn "mainlineProgressProvider" lib/ | head -5
grep -rn "clearedStageIds" lib/ | head -5
```

### Step 1: 决定插入点

读 character_panel.dart 找升层按钮 / experience 进度条 widget。**优先在已有 widget 旁追加,不重排布局**。

### Step 2: 接 Riverpod

如果 character_panel 已是 ConsumerWidget,直接加 `ref.watch(mainlineProgressProvider)`。如果不是,改成 ConsumerWidget(可能涉及构造改 stateful → stateless,小心 dispose)。

### Step 3: 集成 BreakthroughBlocker

```dart
// 伪代码
if (character.realmTier == RealmTier.wuSheng &&
    !mainlineProgress.clearedStageIds.contains('stage_inner_demon_${layerIdx}') &&
    character.experience >= character.experienceToNextLayer) {
  return InnerDemonBreakthroughBlocker(layerIdx: layerIdx, ...);
}
```

(具体 API 看 BreakthroughBlocker 现签名)

### Step 4: verify build_runner + analyze + test

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter analyze --fatal-warnings
flutter test test/features/character/  # 现有 test 不破
flutter test test/features/inner_demon/  # 心魔相关 test 不破
```

### Step 5: commit(不 push)

```bash
git add -A
git commit -m "[feat] BreakthroughBlocker 集成 character_panel · nightshift T02

- character_panel wuSheng 阶接 mainlineProgressProvider
- 心魔关未过 + 满经验 → 显示 BreakthroughBlocker
- 其他阶 (qiMeng-huaJing) 路径不变
"
```

## 不要做

- 不要 git push
- 不要重写 character_panel 大段(只在升层附近加 if)
- 不要改 BreakthroughBlocker 内部逻辑(只 import 用)
- 不要为通过 verify 删 test / 降断言
- 不要顺手改其他 panel widget
- 不要 git rebase / git reset --hard
- commit message **必须**含字符串 `nightshift T02`

## 参考

- `docs/handoff/p2_x_inner_demon_implementation_closeout_2026-05-22.md` line 86(集成方案 v1.0)
- `docs/handoff/p2_x_inner_demon_final_closeout_2026-05-23.md` line 43(挂账描述)
