# Nightshift T02 · #43 高阶占位 audit

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T02`(branch `nightshift/T02`,从 `main` HEAD `3d24af1` 起)内独立完成本任务。

## 背景

挂机武侠 Flutter Desktop 武侠 idle 游戏。PROGRESS.md 已知挂账 #43:**高阶内容占位**(外部 P1):
- `data/equipment.yaml` line 11 `drop_source_tags` 占位
- `data/towers.yaml` 21-30 层敌人 `skillIds` 暂用 mingjia 阶
- `data/towers.yaml` 21-30 层 `dropTable` 暂用 liqi 阶

本 task 要 audit(只调研,不改),输出 docs/handoff/ markdown 决议 + 推荐方案。

## 目标

产出**唯一**新文件:`docs/handoff/p1_43_higher_tier_placeholders_audit_2026-05-19.md`(**≤ 80 行**)

## 操作

1. 读 `data/equipment.yaml` line 1-50(确认 line 11 `drop_source_tags` 占位现状)
2. 读 `data/towers.yaml` 全文(确认 21-30 层敌人 `skillIds` + `dropTable` 实际值,grep 哪些是 mingjia 阶/liqi 阶占位)
3. 对照 `data/skills/encounter_skills.yaml` + 现有 7 阶装备 yaml(equipment 阶段:寻常货/像样货/好家伙/利器/重器/宝物/神物)确认 21-30 层 expected tier:
   - 21-25 层 expected `tier == zongShi` (宗师阶,对应装备 baoWu/宝物 + 心法 shiChuanShenGong/失传神功)
   - 26-30 层 expected `tier == wuSheng` (武圣阶,对应装备 shenWu/神物 + 心法 chuanShuoShenGong/传说神功)
4. 写 `docs/handoff/p1_43_higher_tier_placeholders_audit_2026-05-19.md` 含 4 段:

```markdown
# P1 #43 高阶内容占位 audit · 2026-05-19

> Nightshift T02 audit 产出。**只调研,不改 yaml**。

## §1 占位现状表

### equipment.yaml drop_source_tags
| 文件 | line | 字段值 | 是否占位 | 期望 |
|---|---|---|---|---|
| ... | ... | ... | ... | ... |

### towers.yaml 21-30 层 skillIds + dropTable
| stage_id | tier (实际) | skillIds (实际) | skillIds (期望) | dropTable (实际) | dropTable (期望) |
|---|---|---|---|---|---|
| ... | ... | ... | ... | ... | ... |

## §2 风险评估

Demo 是否可接受当前占位?— 答案 + 理由(GDD §7 Demo 内容总量表对照,Demo 阶段玩家最多打到主线 sanLiu/erLiu 阶,21-30 层属 P1.1+ 范围)

## §3 推荐补齐方案

| 方案 | 时机 | 工作量 | 风险 |
|---|---|---|---|
| A · P1.1 起手 | 主线 15-20 关后立即 | 中 | 数值红线 |
| B · P2 起手 | 1.0 路线图 P2 | 低 | 推迟 |
| C · 不补,Demo 上线 | 永久占位 | 0 | 数值断档 |

**推荐**:(选一个 + 一句话理由)

## §4 closeout

(2-3 句话:本批 audit 结论 + 后续 task 谁负责落地 + 估时)
```

5. 写好后:
```bash
git add docs/handoff/p1_43_higher_tier_placeholders_audit_2026-05-19.md
git commit -m "docs(nightshift T02): #43 高阶占位 audit"
```

## 硬约束

- **只新增** `docs/handoff/p1_43_higher_tier_placeholders_audit_2026-05-19.md` **一个文件**
- **不动** `lib/` / `data/` / `test/` / 任何其他 docs/ 文件
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `numbers.yaml` / `IDS_REGISTRY.md`
- **数字必 grep 实测**(memory `feedback_closeout_numbers_grep`),不写「~21」「约 30」这种估值
- **占位状态以 yaml 实际为准**,不预设结论

## 完成动作

```bash
wc -l docs/handoff/p1_43_higher_tier_placeholders_audit_2026-05-19.md  # ≤ 80
git add docs/handoff/p1_43_higher_tier_placeholders_audit_2026-05-19.md
git commit -m "docs(nightshift T02): #43 高阶占位 audit"
```

报告一句话「T02 done (audit md N lines)」。
