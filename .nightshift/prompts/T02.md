# Nightshift T02 · equipment id ↔ lore yaml 一致性扫描

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T02`(branch `nightshift/T02`,从 `main` HEAD `fc25207` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。CLAUDE.md §8.1 装备域类似 encounter 域:`data/equipment.yaml` 的 `id` 必须与 `data/lore/<equipment_id>.yaml` 文件名严格一致。W15 #35 DeepSeek 35 件装备 lore 派单全交付(GDD §6.6 75 段达标)。本任务做装备-lore 双向 id 一致性扫描,产出 doc 锁定基线。

## 目标

产出**唯一**新文件:`docs/handoff/wuxia_equipment_lore_id_consistency_2026-05-17.md`

## 操作

1. 扫描 `data/equipment.yaml` 提取 35 个 id(grep `^  - id:` 模式)
2. `ls data/lore/*.yaml` 列出 35 lore yaml 文件名(去 `.yaml`)
3. 双向对账:
   - equipment.yaml id 全部 ∈ lore 文件集 ?
   - lore 文件全部 ∈ equipment.yaml id 集 ?
4. 报告 0 漂移即基线建立;若有漂移列具体 id + 文件名 + 性质(missing lore / missing equipment def / 命名漂移)
5. 写 doc 严格按下面结构:

```markdown
# equipment id ↔ lore yaml 一致性扫描(2026-05-17)

> Nightshift T02 产出。当前基线:equipment.yaml ↔ lore/*.yaml 一致性快照。

## 0. 扫描范围
- 工作树:`/Users/a10506/Desktop/wuxia-idle-T02`(基于 main HEAD `fc25207`)
- 输入 1:`data/equipment.yaml`(35 装备 def)
- 输入 2:`data/lore/*.yaml`(35 lore yaml)

## 1. 统计

| 集合 | 数量 |
|---|---|
| equipment.yaml id | (35) |
| data/lore/*.yaml | (35) |

## 2. 双向对账

### 2.1 equipment.yaml id → lore/ 文件
- 全部命中 / X 个 missing-lore(列出 id)

### 2.2 lore/ 文件 → equipment.yaml id
- 全部命中 / X 个 orphan-lore(列出文件名)

## 3. 7 阶分布抽样校验

| tier | equipment.yaml 件数 | lore yaml 件数 | 匹配 |
|---|---|---|---|
| xunChang(寻常货) | (5) | (5) | (yes/no) |
| xiangYang(像样货) | (5) | (5) | (yes/no) |
| haoJiaHuo(好家伙) | (5) | (5) | (yes/no) |
| liQi(利器) | (5) | (5) | (yes/no) |
| zhongQi(重器) | (5) | (5) | (yes/no) |
| baoWu(宝物) | (5) | (5) | (yes/no) |
| shenWu(神物) | (5) | (5) | (yes/no) |

(7 阶 × 5 件 = 35,W15 #35 已交付。grep `^    tier: xunChang$` 等扫描)

## 4. 结论

(0 漂移 = 基线建立。或列具体漂移 + 建议修复路径)

## 5. 后续维护建议

- W18+ 任何新增装备 def:同步加 `data/lore/<id>.yaml`,id 严格匹配
- 装备永退役场景:`mv data/lore/<id>.yaml data/lore/_archive/` 路径需先在 GDD/CLAUDE 加锚点(目前未约定)
```

## 硬约束

- **不动** `lib/` / `test/` / `data/equipment.yaml` / `data/lore/*.yaml`
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不跑 `flutter pub get` / `dart run build_runner` / `flutter test`(dispatcher 跑 verify)
- 不调用网络,不用 web fetch

## 完成动作

doc 写好后:
```bash
git add docs/handoff/wuxia_equipment_lore_id_consistency_2026-05-17.md
git commit -m "docs(nightshift T02): equipment id ↔ lore yaml 一致性扫描"
```

**不要 push**。
报告一句话「T02 done」。
