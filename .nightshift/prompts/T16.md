# Nightshift T16 · P3.4 sect_event Batch 2.3 战斗联动 + 2.4 UI/narrative + 2.5 R5 + closeout(收尾批)

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T16`(branch `nightshift/T16`,从 main HEAD 起)内独立完成本任务。

## 背景

P3.4 sect_event Batch 2.1 schema(`efc7604`)+ Batch 2.2 service(`8c001b3`)已在 main。本 task 落 **Batch 2.3 战斗联动 + 2.4 UI/narrative + 2.5 R5 + closeout 全收尾**,P3.4 100% 完整闭环(spec §8 第 3+4+5 行 ~4h xhigh / nightshift ~25-35min)。

**关键 spec 锚点**:
- §4 战斗联动(SectEventDialog 应战 → DefaultGroundStrategy → resolve(outcome))
- §5 UI(SectScreen 顶 name/level/reputation + active list + history tab / SectEventDialog 弹 narrative + 应战/拒绝)
- §6 narrative(`data/lore/sect_event/tournament_01..08.yaml`)
- §7 R1-R4 完整测族
- 体例参考:`lib/features/inner_demon/presentation/`(同 1.0 P3 UI)/ `lib/features/light_foot/presentation/`(三态体例,但 Sect 不需 cleared 终态)

## 目标

| # | 产出 | 锚点 |
|---|---|---|
| 1 | `lib/shared/strings.dart` 加 `mainMenuSect` + `mainMenuSectHint` | UiStrings |
| 2 | `lib/features/main_menu/presentation/main_menu.dart` 在 `mainMenuMassBattle` 后、`mainMenuLeaderboard` 前插 Sect `_MenuButton` | spec §5 |
| 3 | `lib/features/sect/presentation/sect_screen.dart`(新)顶 name/level/reputation 进度条 + active list + history tab | spec §5 |
| 4 | `lib/features/sect/presentation/widgets/sect_event_dialog.dart`(新)弹 narrative opening + 「应战」/「拒绝」 → battle → resolve(outcome)| spec §4+§5 |
| 5 | `lib/features/sect/application/sect_providers.dart`(新)Riverpod wire(SectEventServiceProvider 注 NumbersConfig + SectReputationDecayServiceProvider)| Phase 4 wire |
| 6 | `data/lore/sect_event/tournament_01.yaml` + `tournament_02.yaml`(新)2 条 stub | spec §6 样本(完整 5-8 留挂账)|
| 7 | `test/features/main_menu/presentation/main_menu_test.dart` 入口 10→11(Sect)| spec §5 · T15 也加 PVP,cherry-pick 时合并 12 |
| 8 | `test/features/sect/sect_screen_test.dart`(新)R4/R5 widget 测 3-5 个 | spec §7 |
| 9 | `test/features/sect/sect_battle_integration_test.dart`(新)R1 应战 e2e 整链 1-2 测 | spec §7 R1 |
| 10 | `docs/handoff/p3_4_sect_event_full_closeout_2026-05-24.md`(新)≤80 行 | doc_inflation 上限 |
| 11 | commit(message 含 `nightshift T16`)| verify_commit_message |

**Batch 2.4 narrative 体力活挂账**:本 task 只 ship `tournament_01.yaml` + `tournament_02.yaml` 2 条 stub · 完整 5-8 条(春秋赛事/比武大会/门派友谊赛/正邪辩武 等不同主题)留 closeout 挂账下波单独 batch。

## 允许修改(verify_path_guard 白名单)

- `lib/features/sect/.+\.dart`
- `lib/features/main_menu/presentation/main_menu\.dart`
- `lib/shared/strings\.dart`
- `data/lore/sect_event/.+\.yaml`
- `test/features/sect/.+\.dart`
- `test/features/main_menu/presentation/main_menu_test\.dart`
- `docs/handoff/p3_4_sect_event_full_closeout_.+\.md`
- `pubspec\.lock`
- `lib/.*\.g\.dart`

## 禁止修改

- `lib/features/(?!sect|main_menu)[^/]+/`(尤其不动 battle/strategy/* / pvp/(T15 并行))
- `lib/main\.dart` / `lib/core/(?!game_loop/)`
- `lib/data/numbers_config\.dart`(用 raw map)
- `data/(?!lore/sect_event/)`
- `test/(?!features/(sect|main_menu)/)`
- `GDD\.md` / `CLAUDE\.md` / `ROADMAP_1_0\.md` / `PROGRESS\.md`(closeout 内挂账)
- `pubspec\.yaml` / `.env.*`

## Phase 0 · reality check(必跑,3 min)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T16
git log -1 --oneline                                                   # base 含 8c001b3 Batch 2.2 service
ls lib/features/sect/                                                  # 应见 domain/ application/ + Batch 2.2 service
grep -n "mainMenuMassBattle\|mainMenuLeaderboard" lib/shared/strings.dart
sed -n '155,170p' lib/features/main_menu/presentation/main_menu.dart   # MassBattle/Leaderboard 现位
grep -nE "class SectEventService\b|class SectReputationDecayService\b" lib/features/sect/application/*.dart
ls lib/features/sect/domain/                                           # sect.dart / sect_event.dart / sect_outcome.dart
find test/features/main_menu -name "*.dart"
sed -n '1,30p' lib/features/light_foot/presentation/light_foot_screen.dart  # screen 体例参考
grep -rn "DefaultGroundStrategy" lib/features/battle/domain/strategy/default_ground_strategy.dart | head -3
```

## 操作

### Step 1: UiStrings(`lib/shared/strings.dart`)

```dart
static const String mainMenuSect = '门派事务';
static const String mainMenuSectHint = '门派经营 · 比武大会 · 声望积累(一流境界 yiLiu 解锁)';
```

### Step 2: main_menu.dart 加 Sect 入口

在 `mainMenuMassBattle` 的 `_MenuButton` 之后、`mainMenuLeaderboard` 的 `_MenuButton` 之前插入(沿同体例 onPressed → Navigator.push SectScreen)。

> 注:T15 并行也加 PVP 入口在同位置,cherry-pick 时主对话手动 resolve 成「MassBattle → PVP → Sect → Leaderboard」。

### Step 3: SectScreen(`lib/features/sect/presentation/sect_screen.dart` 新建)

`ConsumerStatefulWidget` 体例:
- 顶部:sect_name + sect_level + sect_reputation LinearProgressIndicator(0-100 进度)
- 中部:active SectEvent list(status=pending,红点 + 应战 CTA → 弹 SectEventDialog)
- 底部:history tab(status=resolved/expired)

```dart
class SectScreen extends ConsumerWidget {
  const SectScreen({super.key});
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final sect = ref.watch(currentSectProvider);    // 真名按 sect_providers.dart 实际
    return Scaffold(
      appBar: AppBar(title: const Text('门派事务')),
      body: sect.when(
        data: (s) => _SectBody(sect: s, ref: ref),
        loading: () => const Center(child: CircularProgressIndicator()),
        error: (e, _) => Center(child: Text('加载失败: $e')),
      ),
    );
  }
}
```

### Step 4: SectEventDialog(`lib/features/sect/presentation/widgets/sect_event_dialog.dart`)

弹 narrative.opening + 「应战」/「拒绝」按钮:
- 「应战」→ 起战斗(StageBattleSetup 或直接 BattleEngine 调 DefaultGroundStrategy,enemy 用 mock 同境界 mirror team,沿 inner_demon `buildMirrorEnemyTeam` 体例)→ BattleResult → resolve(SectOutcome.win/loss)→ Riverpod writeTxn 持久化(用 ref.read(sectEventServiceProvider).resolve(...))
- 「拒绝」→ resolve(SectOutcome.loss)(沿 spec §4 拒绝 = reputation -5)

> 注:Phase 4 wire 真持久化(Isar writeTxn)放 sect_providers.dart 内,SectEventService.resolve 返 (Sect, SectEvent) tuple 后,provider 内 writeTxn 落库。

### Step 5: sect_providers Riverpod wire(`lib/features/sect/application/sect_providers.dart`)

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../../data/numbers_config.dart';
import 'sect_event_service.dart';
import 'sect_reputation_decay.dart';

final sectEventServiceProvider = Provider<SectEventService>((ref) {
  final numbers = ref.watch(numbersConfigProvider);
  return SectEventService(numbers: numbers);
});

final sectReputationDecayProvider = Provider<SectReputationDecayService>((ref) {
  final numbers = ref.watch(numbersConfigProvider);
  return SectReputationDecayService(numbers: numbers);
});

// currentSectProvider / activeSectEventsProvider 等 StreamProvider/FutureProvider 读 Isar
```

### Step 6: tournament narrative(`data/lore/sect_event/tournament_01.yaml` + `tournament_02.yaml`)

沿 spec §6 体例 + Tier yiLiu 风格梯度词「沉着 / 肃杀 / 老练」+ 第二人称「你」+ 黑名单同 Ch4-6(无「霸气 / 史诗 / 逆天」):

```yaml
# tournament_01.yaml
id: tournament_01
type: tournament
title: 群英会
opening: |
  江湖传檄,武林大会三日后在五台山开设。
  你接掌门派以来,声名渐起,今受邀赴会,与诸派高手切磋武艺,以扬本派声威。
choices:
  - { text: 应战赴会, outcome: accept }
  - { text: 闭门谢客(reputation -5), outcome: refuse }
victory_text: 群雄退避,你立于擂台中央。本派声威远播,弟子归心。
defeat_text: 败下阵来,心知技艺尚需精进。归山闭关三月,再图江湖。
```

`tournament_02.yaml` 别样主题(eg.「春秋赛事」/「华山论剑」/「门派友谊赛」之一)— Claude 自由发挥沿同体例。

### Step 7: main_menu_test 入口 10→11

参 T15 同处理,cherry-pick 时主对话合并成 12(PVP + Sect 双加)。

### Step 8: sect_screen_test 新建 R4/R5(3-5 测)

```dart
testWidgets('SectScreen 顶 sect_name/level/reputation 进度条显示', ...);
testWidgets('SectEventDialog 弹 opening + 应战/拒绝 button', ...);
test('tournament_01 + tournament_02 schema 合法 + opening 非空 + 黑名单 0', ...);
```

### Step 9: sect_battle_integration_test R1 应战 e2e(1-2 测)

```dart
test('R1.5 应战 → BattleResult win → resolve(win) → reputation +10 + totalWins +1', ...);
// 仅测 service 整链 in-memory,不实例化 Isar(memory autoIncrement 撞)
```

### Step 10: closeout(`docs/handoff/p3_4_sect_event_full_closeout_2026-05-24.md` ≤80 行)

TL;DR ≤5 行 + 改动总览 + R1-R4 测族 + 挂账(tournament 完整 5-8 + sect_screen 真持久化 wire writeTxn 验收 + monthly_tick 真接 Riverpod tick)+ 工作量复盘。

### Step 11: commit(不 push)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T16
git add lib/features/sect/ lib/features/main_menu/ lib/shared/strings.dart data/lore/sect_event/ test/features/sect/ test/features/main_menu/ docs/handoff/p3_4_sect_event_full_closeout_2026-05-24.md
git commit -m "[feat] P3.4 sect_event Batch 2.3 战斗联动 + 2.4 UI/narrative + 2.5 R5 + closeout · nightshift T16

- SectScreen(name/level/reputation 进度条 + active list + history tab)+ SectEventDialog(应战/拒绝 + battle wire DefaultGroundStrategy + resolve(outcome))
- main_menu 入口 10→11 插 Sect(MassBattle 后 Leaderboard 前)+ UiStrings mainMenuSect/Hint
- sect_providers Riverpod wire(SectEventServiceProvider + DecayProvider)
- data/lore/sect_event/tournament_01+02.yaml stub(2 条 · 完整 5-8 留挂账)
- R4/R5 widget 测 3-5 + R1 应战 e2e 整链 1-2 测
- 0 改 lib/features/battle/strategy/*(复用 DefaultGroundStrategy)/ 0 改 numbers_config.dart(raw map)/ 0 改 data/numbers.yaml

详 docs/spec/p3_4_sect_event_spec_2026-05-24.md §4/§5/§6/§7
"
```

## 不要做

- **🚨 严禁 cd / git -C 到 dispatcher 创建的 worktree 之外的任何路径**(v2 A1 红线)
- 不要写完整 5-8 条 tournament narrative(本 task 2 条 stub · 完整留挂账)
- 不要改 lib/features/battle/strategy/*(SectEventDialog 复用 DefaultGroundStrategy)
- 不要给 sect 引入新数值轴(§5.4 红线,tournament 复用 default 不引入)
- 不要写 Isar 实例化测(memory `feedback_isar_autoincrement_test_id_collision`)
- 不要改 `lib/data/numbers_config.dart`(用 raw map)
- 不要 git push / git rebase / git reset --hard
- 不要顺手改 GDD/CLAUDE/ROADMAP/PROGRESS(closeout 内挂账)
- 不要动 `lib/features/pvp/`(T15 并行 task)

## 参考

- spec: `docs/spec/p3_4_sect_event_spec_2026-05-24.md` §4/§5/§6/§7/§8
- UI 体例: `lib/features/light_foot/presentation/` / `lib/features/inner_demon/presentation/`
- main_menu 体例: line 134-200 10 入口
- battle wire 体例: `lib/features/inner_demon/application/inner_demon_service.dart`(buildMirrorEnemyTeam + StageBattleSetup)
- narrative 体例: `data/lore/inner_demon/` / `data/narratives/chapter_04/`
- memory: `feedback_isar_autoincrement_test_id_collision` / `feedback_nightshift_v2_first_run_lessons` A1 / `feedback_listview_widget_test_viewport`
