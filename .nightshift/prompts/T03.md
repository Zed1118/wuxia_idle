# Nightshift T03 · #37 6 orphan event 决议归档

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T03`(branch `nightshift/T03`,从 `main` HEAD `3d24af1` 起)内独立完成本任务。

## 背景

挂机武侠 Flutter Desktop 武侠 idle 游戏。PROGRESS.md 已知挂账 #37:**6 events orphan 剩余可后续挂回**。当前位于 `data/events/_archive/`:
1. `duan_qiao_can_yue.yaml`
2. `gu_chuan_deng_ying.yaml`
3. `huang_cun_yao_ren.yaml`
4. `lao_jing_hui_xiang.yaml`
5. `qing_lou_can_meng.yaml`
6. `yu_zhong_qiao_men.yaml`

PROGRESS line 48 备注:「主题不适配(心境/江湖故事/邪门调子/音律无对应武学),留 _archive/ 不动」。本 task 要逐条复审 → 决议是否挂回 / 永封档,**只输出 markdown 决议表,不动 yaml**。

## 目标

产出**唯一**新文件:`docs/handoff/p1_37_orphan_decree_2026-05-19.md`(**≤ 100 行**)

## 操作

1. 读 6 个 orphan yaml 全文(`data/events/_archive/<id>.yaml`),提取每条:
   - `id`
   - `title`(剧情标题)
   - `opening`(开篇 1-2 句)
   - `choices` + `outcome`(节点结果)
   - 主题归类(心境 / 江湖故事 / 邪门 / 音律 / 其他)
   - biome / weather 隐含线索(可从 title + opening 推)

2. 读 `data/encounters.yaml` 当前 44 个 encounter 列表 + 类型(techniqueInsight / fortuneEvent / festivalRequired),识别**可能挂回的 slot**:
   - 缺哪些 biome × weather 组合
   - 缺哪些 type(基础奇遇 15-25 / 节日 6-10 / 武学领悟 20-30 GDD §8.4 lower bounds 是否仍待补)

3. 写 `docs/handoff/p1_37_orphan_decree_2026-05-19.md` 4 段:

```markdown
# P1 #37 6 orphan event 决议归档 · 2026-05-19

> Nightshift T03 audit 产出。**只决议,不动 encounters.yaml / events/_archive/**。

## §1 6 orphan 主题表

| id | 标题 | 主题归类 | biome 推断 | weather 推断 | 调子 |
|---|---|---|---|---|---|
| duan_qiao_can_yue | (实际 title) | (类) | (推断) | (推断) | (调) |
| gu_chuan_deng_ying | ... | ... | ... | ... | ... |
| huang_cun_yao_ren | ... | ... | ... | ... | ... |
| lao_jing_hui_xiang | ... | ... | ... | ... | ... |
| qing_lou_can_meng | ... | ... | ... | ... | ... |
| yu_zhong_qiao_men | ... | ... | ... | ... | ... |

## §2 encounters.yaml slot 缺口扫描

| type | 当前数 | GDD §8.4 下限 | 缺口 |
|---|---|---|---|
| techniqueInsight | (grep 实测) | 20 | (差/满) |
| fortuneEvent | (grep 实测) | 15 | (差/满) |
| festivalRequired | (grep 实测) | 6 | (差/满) |

## §3 每条决议表

| id | 决议(挂回/永封档) | 理由 | 若挂回:目标 type + biome + weather + outcome 兼容性 |
|---|---|---|---|
| duan_qiao_can_yue | (推荐) | (一句话) | (若挂回:type=fortuneEvent / biome=X / weather=Y / outcome 映射到 encounters.yaml outcomeMapping key) |
| ... | ... | ... | ... |

## §4 closeout

(2-3 句话:本批 audit 结论 + 建议下波是否挂回:估时 + 风险红线核查;若全永封档则说明 #37 永久归档可对挂账列表 strikethrough)

**优先级判定**:Demo §8.4 内容总量表 fortuneEvent 当前数 ≥ 15 → 不强求挂回(Demo 不阻塞);< 15 → 优先挂回 2-3 条主题最近的。
```

4. 写好后:
```bash
git add docs/handoff/p1_37_orphan_decree_2026-05-19.md
git commit -m "docs(nightshift T03): #37 6 orphan event 决议归档"
```

## 硬约束

- **只新增** `docs/handoff/p1_37_orphan_decree_2026-05-19.md` **一个文件**
- **绝对不动** `data/encounters.yaml` / `data/events/_archive/` / 其他 data/
- **不动** `lib/` / `test/` / `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `numbers.yaml` / `IDS_REGISTRY.md`
- **挂回是建议,不落实装**(本 task 仅决议;若用户拍板挂回,下波 task 起 spec 落)
- **数字必 grep 实测**(memory `feedback_closeout_numbers_grep`):encounter 总数 / 各 type 数 / GDD §8.4 下限

## 完成动作

```bash
wc -l docs/handoff/p1_37_orphan_decree_2026-05-19.md  # ≤ 100
git add docs/handoff/p1_37_orphan_decree_2026-05-19.md
git commit -m "docs(nightshift T03): #37 6 orphan event 决议归档"
```

报告一句话「T03 done (decree md N lines, X 挂回 / Y 永封档)」。
