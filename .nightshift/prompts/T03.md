# Nightshift T03 · 死代码 / YAGNI scan 报告

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T03`(branch `nightshift/T03`,从 `main` HEAD `4132a13` 起)内独立完成本任务。

## 背景

Phase 5 W6-S2 引入 nullable propagation 时一刀切声明 N 个 provider,可能有死 provider 残留。`feedback_riverpod_codegen_provider_split` cookbook 给出死 provider 审计与处理决策。本任务跑 0 lib consumer 扫描,生成报告。**不删代码,只报告**。

## 目标

产出**唯一**新文件:`docs/handoff/wuxia_dead_code_scan_2026-05-17.md`

## 操作

1. 扫描候选维度:
   - **A. 死 provider**:`grep -rn "@riverpod" lib/ --include="*.dart" | grep -v ".g.dart"` 拿到所有 @riverpod 定义 → 对每个 provider 名 grep 全仓 lib/ + test/ 引用 → 0 引用 = 死 provider 候选
   - **B. 0 lib consumer service class**:`grep -rE "^class \\w+Service" lib/ --include="*.dart" | grep -v ".g.dart"` 拿 service class 名 → grep 全仓引用 → 0 lib 但 test 有 = service class 0-consumer 候选
   - **C. 未引用 private function/method**:私有方法(`_funcName`)定义点 + 同文件内 grep 引用 → 0 内部引用 = 死 private function
   - **D. extension on entity 类硬编码点**:参 `feedback_extension_hardcode_audit` — `grep -n "^extension.*on " lib/ --include="*.dart"` 看 extension 是否硬编码本应从 yaml 读的字段

2. 与 GDD 锚点交叉验证:
   - B 类 0-consumer service:`grep -n "<Service 名>" GDD.md` 验证是否 GDD 预留(`feedback_riverpod_codegen_provider_split` C 节 J 任务沉淀决策)
   - 有 GDD 锚点 → 保留(标 "预留")
   - 无 GDD 锚点 → 报告候选(标 "需用户确认")

3. 写 doc 结构:

```markdown
# 死代码 / YAGNI scan 报告(2026-05-17)

> Nightshift T03 产出。**只扫描不删代码**,所有候选留用户早上 review。

## 0. 扫描范围

- 工作树:`/Users/a10506/Desktop/wuxia-idle-T03`(基于 main HEAD `4132a13`)
- 全仓 lib/ + test/

## 1. 死 provider 候选(A 类)

(列表 + 0-引用证据;每条注「保留」/「可删」决策建议)

## 2. 0-lib-consumer Service Class(B 类)

(列表 + GDD.md 锚点验证;每条注「GDD §X 预留 → 保留」/「无锚点 → 用户确认」)

## 3. 未引用 private function/method(C 类)

(列表 + 文件内 grep 证据;每条注「可删」/「保留(可能被反射调用)」)

## 4. extension hardcode 嫌疑(D 类)

(列表 + 字段名 + 「应改读 yaml」/ 「合理硬编码」决策建议)

## 5. 总结

- 累计候选 N 个(A=X / B=Y / C=Z / D=W)
- 推荐下次 sprint 处理:N 个高置信度可删 + N 个需用户确认
- 0 候选 = healthy,无 follow-up
```

## 硬约束

- **不动** `lib/` / `test/` 任何代码
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不跑 `flutter pub get` / `dart run build_runner` / `flutter test`(dispatcher 跑 verify)
- 不调用网络
- 候选数量上限 50(grep 命中太多就分类聚合,不要堆 200 行 raw 输出)

## 完成动作

doc 写好后:
```bash
git add docs/handoff/wuxia_dead_code_scan_2026-05-17.md
git commit -m "docs(nightshift T03): 死代码 / YAGNI scan 报告"
```

**不要 push**。报告一句话「T03 done」。
