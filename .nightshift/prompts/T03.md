# Nightshift T03 · HomeFeedProviders markAllFeedRead 边界 test

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T03`(branch `nightshift/T03`,从 `main` HEAD `217ddf7` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。P1 #42 Phase 1(2026-05-17)新落 `markAllFeedRead`(`lib/features/home_feed/application/home_feed_providers.dart` 44 行,**顶级函数**接 `Isar?` 直接,避开 Ref 类型耦合 + provider 闭包持 ref disposed 风险,参 memory `feedback_riverpod_closure_ref_disposed`)。

现有 `home_feed_providers_test.dart` 4 test:gameEventsFeed 空表/排序/limit + markAllFeedRead **基础批量 mark**。**未测 markAllFeedRead 边界**:Isar null / 已全部 isRead=true 再 mark / mark 后 feed 顺序不乱 / occurredAt 单调性受影响?。本任务补这 3 个边界 test,产出独立 `_mark_all_edge_test.dart` 不动现有 test。

## 红线:约束语义不写瞬时事实

断言写**集合自洽 + 副作用语义**:断言「mark 后所有 GameEvent.isRead==true」(集合自洽)而非「mark 影响 3 条」。

## 目标

产出**唯一**新文件:`test/features/home_feed/application/home_feed_providers_mark_all_edge_test.dart`

## 操作

1. 读现有 `test/features/home_feed/application/home_feed_providers_test.dart` 学 Isar fixture / writeTxn 体例 + ProviderContainer 用法
2. 读 `lib/features/home_feed/application/home_feed_providers.dart` 确认 markAllFeedRead 签名 + 实现细节:
   - 接 `Isar?` 还是 `Isar` 必传?
   - 内部如何 query unread → batch mark?
   - 写 txn 边界?
3. 新增 3 个 test:
   - **A. Isar null 容错**:`await markAllFeedRead(null)` 不抛异常,graceful return(若代码签名是 `Isar?` nullable;若必传 `Isar`,改为「空 Isar 实例 + 空 collection」边界)
   - **B. 已全读再 mark idempotent**:fixture 5 个 GameEvent.isRead=true → markAllFeedRead → 全 isRead 仍 true + 无写 txn 副作用(查 Isar log / 或断言 occurredAt/其他字段未被改)
   - **C. mark 后 feed 顺序不乱**:fixture 5 个 GameEvent 不同 occurredAt → markAllFeedRead → 再查 gameEventsFeed → 顺序仍是 occurredAt desc(集合等价校验)
4. 文件结构沿 T01/T02 模板,group 名「markAllFeedRead 边界」
5. **新 test 必须 `flutter test` 全过**

## 硬约束

- **只新增** `test/features/home_feed/application/home_feed_providers_mark_all_edge_test.dart` **一个文件**
- **不动** `lib/` / `data/` / 其他 test
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不调用网络

## 完成动作

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/home_feed/application/home_feed_providers_mark_all_edge_test.dart
git add test/features/home_feed/application/home_feed_providers_mark_all_edge_test.dart
git commit -m "test(nightshift T03): markAllFeedRead 边界 edge"
```

报告一句话「T03 done (added N mark-all edge cases)」。
