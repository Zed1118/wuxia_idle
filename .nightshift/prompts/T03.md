# Nightshift T03 · CharacterPanelScreen 边界用例 +3-5

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T03`(branch `nightshift/T03`,从 `main` HEAD `fc25207` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。`CharacterPanelScreen` 显示当前角色 + tab 切换 + 装备/心法/属性详情。现有 widget test `test/features/character_panel/presentation/character_panel_screen_test.dart`(431 行 主体例)已覆盖主路径;W17 T04 在 `LineagePanelScreen` 同侧加 `lineage_panel_screen_edge_test.dart`(245 行,5 边界用例)沉淀边界测试模板。本任务**完全比照 LineagePanelScreen 边界模板**,为 CharacterPanelScreen 新增 3-5 边界用例,产出独立 `_edge_test.dart` 不动现有 test。

## 红线:约束语义不写瞬时事实

参 `feedback_red_line_test_semantics`(用户全局 memory):测试断言写**白名单 / 集合自洽 / 类型契约**,而非具体数字 / 计数 / 字面值。例:断言「装备 chip 数 = active 装备数」而非「装备 chip 数 = 3」。

## 目标

产出**唯一**新文件:`test/features/character_panel/presentation/character_panel_screen_edge_test.dart`

## 操作

1. 读现有 `test/features/character_panel/presentation/character_panel_screen_test.dart`(431 行)学:
   - import 套路
   - 各 provider override 模式
   - fixture 注入方式
   - pump 节奏
2. 读现有 `test/features/character_panel/presentation/lineage_panel_screen_edge_test.dart`(245 行,W17 T04 产)看边界测试体例
3. 读 `lib/features/character_panel/presentation/character_panel_screen.dart` 看 widget 依赖哪些 provider
4. 选 3-5 个边界用例方向(自选,以下为候选,任选 3+):
   - **A. character 主属性极端值**(全 1 极低 / 全 10 极高 / 总和触上下界 16 和 24)
   - **B. 无装备态**(activeEquipmentIds 全空)断言:不应崩,装备 tab 显示空态
   - **C. 无心法态**(allTechniques 空)断言:心法 tab 显示空态
   - **D. tab 切换序**(默认 tab → 切到装备 → 切到心法 → 切回)
   - **E. school=null 兜底**(若 character model 允许)
   - **F. 装备 tier 与角色 realm 边界**(角色 三流 / 装备 神物)断言:仍能渲染 chip 但 canEquip=false 显示(看现有 widget 是否分支)
5. 写新 test 文件,严格 mirror lineage_panel_screen_edge_test.dart 的结构:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:flutter_test/flutter_test.dart';
   // ... 沿现有 character_panel_screen_test.dart 同款 imports

   void main() {
     // group / testWidgets 块,每个边界用例独立
   }
   ```
6. **每个用例必须能通过 `flutter test` 全过**。若用例设计不能通过,改为可通过的等价用例(verify.sh 会跑 flutter test 限新文件,fail 整 task 算 skip)

## 硬约束

- **只新增** `test/features/character_panel/presentation/character_panel_screen_edge_test.dart` **一个文件**
- **不动现有** test/ 任何文件
- **不动** `lib/` / `data/`(yaml 数值)/ `assets/`
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不调用网络

## 完成动作

test 写好后:
```bash
flutter pub get
dart run build_runner build  # 若现有 character_panel 有 @riverpod 代码生成依赖
flutter test test/features/character_panel/presentation/character_panel_screen_edge_test.dart
# 全过后:
git add test/features/character_panel/presentation/character_panel_screen_edge_test.dart
git commit -m "test(nightshift T03): CharacterPanelScreen 边界用例 +N"
```

**不要 push**。
报告一句话「T03 done (added N edge cases)」。
