# Nightshift T17 · P1.2 §12.1 江湖恩怨 + §12.2 声望 全 4 batch 一次过(单 worktree 累积 commit)

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T17`(branch `nightshift/T17`,从 main HEAD 起)内独立完成本任务。

## 背景

P1.2 spec(`docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md` 148 行 · PR #6 squash merge `4cc649a`)已在 main · Q1-Q5 默认决议草案完整(B 拆批 / 稀疏 NpcRelation / A+B 触发 / A+B 反应 / 七阶映射)。本 task 落 **B1-B4 全 4 batch 一次过**,P1.2 100% 完整闭环(spec §8 估 ~7h xhigh / nightshift ~80-110min)。

**关键 spec 锚点**(全引 spec doc):
- §2 schema:`Reputation` + `NpcRelation` Isar Collection 双新
- §3 service:`ReputationService` + `NpcRelationService` + `EncounterIntegration`
- §4 UI:`ReputationPanelScreen` + HUD chip + UiStrings ~12 段
- §5 战斗:`attackPowerMultiplier` 烘焙(沿 P3.1.B `light_foot_strategy.dart:120` + P3.2 `mass_battle_strategy.dart:182` 体例)
- §6 数据流:`data/factions.yaml` 新 + `stages.yaml` boss `npcId` 字段 + `encounters.yaml` `affects_reputation` 字段
- §7 R5:R5.1-5.6 ~10-12 测族

**4 commit 累积**(单 worktree 内逐 batch commit · 不 push):
- B1 schema commit
- B2 service commit
- B3 UI + battle commit
- B4 R5 + closeout commit

verify 抓 last commit msg 含 `nightshift T17`(每个 commit message 都含 `nightshift T17 B<X>`)。

## 目标

| # | Batch | 产出 | 锚点 |
|---|---|---|---|
| **B1.1** | schema | `lib/features/jianghu/domain/reputation.dart`(新 Isar Collection)| spec §2 |
| **B1.2** | schema | `lib/features/jianghu/domain/npc_relation.dart`(新 Isar Collection)| spec §2 |
| **B1.3** | schema | `data/numbers.yaml` 尾部加 `jianghu:` 段(reputation_tiers 7 阶 + enmity_combat_modifier + triggers)| spec §2 |
| **B1.4** | schema | `data/factions.yaml`(新 · 6-8 门派)| spec §6 |
| **B1.5** | schema | `data/stages.yaml`:5-8 boss entry 加 `npcId` 可选字段 | spec §6 |
| **B1.6** | schema | `data/encounters.yaml`:6-10 encounter 加 `affects_reputation` 可选段 | spec §6 |
| **B1.7** | schema | `lib/data/numbers_config.dart` 扩 `JianghuConfig` 类 + 解析(沿 `HeritageItems` 体例)| spec §6 |
| **B1.8** | schema | `lib/data/stage_def.dart` 加 `npcId` 字段 + `lib/features/encounter/domain/encounter_def.dart` 加 `affectsReputation` 字段 | spec §6 |
| **B1.9** | schema | `lib/data/isar_setup.dart` 注册 `ReputationSchema` + `NpcRelationSchema` | sibling 体例 |
| **B1.10** | schema | build_runner + commit `[feat] P1.2 B1 schema · nightshift T17 B1` | — |
| **B2.1** | service | `lib/features/jianghu/application/reputation_service.dart`(沿 `founder_buff_service.dart` 体例)| spec §3 |
| **B2.2** | service | `lib/features/jianghu/application/npc_relation_service.dart` | spec §3 |
| **B2.3** | service | `lib/features/jianghu/application/jianghu_providers.dart` Riverpod wire | spec §3 |
| **B2.4** | service | `lib/features/encounter/application/encounter_service.dart` 集成 `affects_reputation` 消费(applyDelta hook) | spec §3 |
| **B2.5** | service | 单测 `test/features/jianghu/reputation_service_test.dart`(applyDelta + clamp + tierOf 6-8 测)| 单测 |
| **B2.6** | service | 单测 `test/features/jianghu/npc_relation_service_test.dart`(upsert + enmityAgainst + attackPowerMultFor 5-7 测)| 单测 |
| **B2.7** | service | commit `[feat] P1.2 B2 service · nightshift T17 B2` | — |
| **B3.1** | UI | `lib/shared/strings.dart` 加 12 段(reputationTitle / reputationTierLabels[7] / enmityWarning / panelFriendSection / panelFoeSection / mainMenuJianghu + Hint)| spec §4 |
| **B3.2** | UI | `lib/features/jianghu/presentation/reputation_panel_screen.dart`(沿 `lineage_panel_screen.dart` 卡片三段式)| spec §4 |
| **B3.3** | UI | `lib/features/jianghu/presentation/widgets/reputation_tier_chip.dart`(7 阶颜色梯度)| spec §4 |
| **B3.4** | UI | `lib/features/main_menu/presentation/main_menu.dart` 加 江湖 入口(在 PVP 后、Leaderboard 前)| spec §4 |
| **B3.5** | UI | `lib/core/application/battle_providers.dart` startBattle 时注入 attackPowerMultiplier(查 NpcRelationService.attackPowerMultFor)| spec §5 |
| **B3.6** | UI | `test/features/main_menu/presentation/main_menu_test.dart` 入口 16→17 | UI 测 |
| **B3.7** | UI | `test/features/jianghu/reputation_panel_screen_test.dart`(R4 widget 测 3-5)| UI 测 |
| **B3.8** | UI | commit `[feat] P1.2 B3 UI + battle · nightshift T17 B3` | — |
| **B4.1** | R5 | R5.1 reputation 7 阶 e2e(21 测点 sweep + clamp ±100)| spec §7 |
| **B4.2** | R5 | R5.2 enmity 阈值(level=-49/-50/-51 attackPowerMultFor 返 1.0/1.15/1.15) | spec §7 |
| **B4.3** | R5 | R5.3 战斗 §5.4 cap 不越(普伤 ≤8000 红线 + enmity active fixture) | spec §7 |
| **B4.4** | R5 | R5.4 trigger e2e(stage_boss_kill / encounter delta) | spec §7 |
| **B4.5** | R5 | R5.5 §5.2 七阶 label 锁(7 阶 enum 等 `xueTu..wuSheng`) | spec §7 |
| **B4.6** | R5 | R5.6 P3.4 隔离(Reputation 与 SectReputation Collection name 不等 schema 断言)| spec §7 |
| **B4.7** | closeout | `docs/handoff/p1_2_jianghu_full_closeout_2026-05-25.md`(≤80 行)| doc_inflation |
| **B4.8** | closeout | commit `[feat] P1.2 B4 R5 + closeout · nightshift T17 B4`(verify 必抓 last `nightshift T17`)| — |

## 允许修改(verify_path_guard 白名单)

- `lib/features/jianghu/.+\.dart`
- `lib/features/jianghu/.+\.g\.dart`
- `lib/features/main_menu/presentation/main_menu\.dart`
- `lib/features/encounter/application/encounter_service\.dart`
- `lib/features/encounter/application/encounter_service_providers\.dart`
- `lib/features/encounter/application/encounter_service_providers\.g\.dart`
- `lib/features/encounter/domain/encounter_def\.dart`
- `lib/shared/strings\.dart`
- `lib/data/numbers_config\.dart`
- `lib/data/stage_def\.dart`
- `lib/data/isar_setup\.dart`
- `lib/core/application/battle_providers\.dart`
- `lib/core/application/battle_providers\.g\.dart`
- `data/numbers\.yaml`
- `data/factions\.yaml`
- `data/stages\.yaml`
- `data/encounters\.yaml`
- `test/features/jianghu/.+\.dart`
- `test/features/main_menu/presentation/main_menu_test\.dart`
- `docs/handoff/p1_2_jianghu_full_closeout_.+\.md`
- `pubspec\.lock`

## 禁止修改

- `lib/features/(?!jianghu|main_menu|encounter)[^/]+/`(只动 jianghu/main_menu/encounter 模块)
- `lib/features/battle/domain/strategy/.*\.dart`(战斗策略不动 · attackPowerMultiplier 在 battle_providers setup 时注入,不改 strategy)
- `lib/features/battle/domain/.*\.dart`(BattleState 已有 attackPowerMultiplier 字段 P3.1.B 实装,0 改)
- `lib/main\.dart` / `lib/core/(?!application/battle_providers)`
- `data/lore/` / `data/narratives/` / `data/events/`(文案下波 batch · 本 task 不动)
- `GDD\.md` / `CLAUDE\.md` / `ROADMAP_1_0\.md` / `PROGRESS\.md`(closeout 内挂账即可,本 task 不 sync)
- `pubspec\.yaml` / `.env.*`

## Phase 0 · reality check(必跑,3-5 min · 不可跳)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T17
git log -1 --oneline                                                  # 确认 base = 4cc649a 或更新
ls lib/features/jianghu/ 2>/dev/null || echo "  (空,本 task 全新建)"
ls lib/features/inheritance/application/                              # 看 founder_buff_service.dart 体例
ls lib/features/encounter/application/                                # 看 encounter_service + providers
grep -n "class NumbersConfig\|class HeritageItems\|class MassBattleConfig" lib/data/numbers_config.dart
grep -n "class StageDef\|class EncounterDef" lib/data/stage_def.dart lib/features/encounter/domain/encounter_def.dart
grep -n "ReputationSchema\|NpcRelationSchema" lib/data/isar_setup.dart || echo "  (无 schema 注册,B1.9 新增)"
grep -n "class BattleState\|attackPowerMultiplier" lib/features/battle/domain/battle_state.dart | head -10
grep -n "currentSlotId" lib/data/isar_setup.dart
grep -c "_MenuButton" lib/features/main_menu/presentation/main_menu.dart  # 当前入口数 baseline
find lib/features/inheritance/application -name "*.dart"              # 看 lineage_service.dart 体例
sed -n '169,185p' lib/features/battle/domain/strategy/mass_battle_strategy.dart  # attackPowerMultiplier 烘焙体例(P3.2)
sed -n '111,125p' lib/features/battle/domain/strategy/light_foot_strategy.dart   # 同体例(P3.1.B)
tail -30 data/numbers.yaml                                             # 看 yaml 尾部 inheritance/mass_battle/sect_event/pvp 体例,jianghu 段加在尾部
cat docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md | head -80       # 重读 spec §1-4
```

**Phase 0 输出**:把 baseline `_MenuButton` 数(应是 16,T15+T16 加 PVP+Sect)+ jianghu/ 目录是否存在 + sect_event 段 yaml 尾部位置写到 closeout doc。

## 操作

### === B1 schema(~25min)===

#### B1.1: Reputation Isar Collection

新 `lib/features/jianghu/domain/reputation.dart`:

```dart
import 'package:isar_community/isar.dart';

part 'reputation.g.dart';

/// 玩家对各门派的声望(P1.2 §2 · GDD §12.2)
///
/// - value clamp [-100, +100] 入仓
/// - tier 映射走 ReputationService.tierOf(查 numbers.yaml.jianghu.reputation_tiers)
/// - 多 save 隔离 by playerId(沿 SaveData.playerCharacterId 体例 · Demo 单 save 但 schema 预留)
@collection
@Index(composite: [CompositeIndex('playerId'), CompositeIndex('factionId')], unique: true)
class Reputation {
  Id id = Isar.autoIncrement;
  late int playerId;
  late String factionId;
  @Index() late int value;        // [-100, +100]
  late DateTime updatedAt;
}
```

#### B1.2: NpcRelation Isar Collection

新 `lib/features/jianghu/domain/npc_relation.dart`(稀疏单向):

```dart
import 'package:isar_community/isar.dart';

part 'npc_relation.g.dart';

/// NPC 关系稀疏表(P1.2 §2 · Q2=B 稀疏)
///
/// type 枚举走 string(friend/foe/master/disciple/owed)
/// level [-100, +100],enmity = level ≤ -50
@collection
class NpcRelation {
  Id id = Isar.autoIncrement;
  @Index() late int sourceCharacterId;
  @Index() late int targetCharacterId;
  late String type;
  late int level;
  late DateTime updatedAt;
}
```

#### B1.3: numbers.yaml `jianghu:` 段

在 `data/numbers.yaml` 尾部加(对齐 spec §2):

```yaml
jianghu:
  reputation_tiers:
    - { tier: xueTu,    min: -100, max: -71, label: "声名狼藉" }
    - { tier: sanLiu,   min: -70,  max: -41, label: "恶名" }
    - { tier: erLiu,    min: -40,  max: -11, label: "默默无闻" }
    - { tier: yiLiu,    min: -10,  max:  10, label: "薄有微名" }
    - { tier: jueDing,  min:  11,  max:  40, label: "侠名初显" }
    - { tier: zongShi,  min:  41,  max:  70, label: "声振江湖" }
    - { tier: wuSheng,  min:  71,  max: 100, label: "天下闻名" }
  enmity_combat_modifier:
    threshold: -50
    player_attack_power_mult: 1.15
    enemy_attack_power_mult: 1.15
    severe_threshold: -80
    severe_mult: 1.25
    clamp_max: 1.25
  triggers:
    stage_boss_kill_delta: 5
    stage_boss_kill_rival_delta: 3
    encounter_npc_delta_min: -8
    encounter_npc_delta_max: 8
```

#### B1.4: factions.yaml 新建

新 `data/factions.yaml`(6 门派 Demo · 沿 techniques.yaml 列表体例):

```yaml
factions:
  - id: shaolin
    name: "少林寺"
    alignment: orthodox
    npc_ids: []
  - id: wudang
    name: "武当派"
    alignment: orthodox
    npc_ids: []
  - id: emei
    name: "峨眉派"
    alignment: orthodox
    npc_ids: []
  - id: luLin
    name: "绿林"
    alignment: neutral
    npc_ids: []
  - id: jiaoMen
    name: "邪教"
    alignment: evil
    npc_ids: []
  - id: cijianzhuang
    name: "刺剑庄"
    alignment: evil
    npc_ids: []
```

#### B1.5: stages.yaml boss `npcId` extend

在 Phase 0 grep `data/stages.yaml` 找到 5-8 个有 boss 的 stage(主线 Ch5/Ch6 + 爬塔大 Boss · `stage_05_05`/`stage_06_03`/`stage_06_05`/`tower_10`/`tower_20`/`tower_30` 等),逐 stage 加可选 `npcId` 字段:

```yaml
- id: stage_05_05
  # ... 现有字段
  npc_id: zhao_yima                  # 关联 factions npc_ids
```

**npcId 内容**:对未实装具体 NPC 的 stage 留 `null` 或 string id 占位,实际 NPC schema 留 1.1+(本 task 不动 character 系统)。**只挑 3-5 个 boss stage 加占位 npcId · 不全部加**。

#### B1.6: encounters.yaml `affects_reputation` extend

在 Phase 0 grep `data/encounters.yaml` 找 6-10 个声望相关 encounter(`encounter_fortune_*` 类 / 师门相关 / 行侠仗义类 · 按 trigger.type 筛选),加可选段:

```yaml
- id: encounter_save_villager
  # ... 现有字段
  affects_reputation:
    faction_id: luLin
    delta_min: 3
    delta_max: 7
```

**6-10 个挑 4-6 个加占位即可** · 不全 sweep encounter pool。

#### B1.7: NumbersConfig + JianghuConfig 解析(`lib/data/numbers_config.dart`)

在 `NumbersConfig` class 内沿 `HeritageItems` 体例加:

```dart
class NumbersConfig {
  // ... 现有字段
  final JianghuConfig jianghu;

  NumbersConfig({
    // ... 现有 required
    required this.jianghu,
  });

  factory NumbersConfig.fromYaml(Map<String, dynamic> yaml) {
    return NumbersConfig(
      // ... 现有解析
      jianghu: JianghuConfig.fromYaml(yaml['jianghu'] as Map<String, dynamic>? ?? {}),
    );
  }
}

class JianghuConfig {
  final List<ReputationTierDef> reputationTiers;
  final EnmityCombatModifier enmityCombatModifier;
  final JianghuTriggers triggers;

  JianghuConfig({
    required this.reputationTiers,
    required this.enmityCombatModifier,
    required this.triggers,
  });

  factory JianghuConfig.fromYaml(Map<String, dynamic> y) {
    return JianghuConfig(
      reputationTiers: (y['reputation_tiers'] as List?)
              ?.cast<Map>()
              .map((m) => ReputationTierDef.fromYaml(m.cast()))
              .toList() ??
          const [],
      enmityCombatModifier: EnmityCombatModifier.fromYaml(
          y['enmity_combat_modifier'] as Map<String, dynamic>? ?? {}),
      triggers: JianghuTriggers.fromYaml(y['triggers'] as Map<String, dynamic>? ?? {}),
    );
  }
}

class ReputationTierDef {
  final String tier;
  final int min;
  final int max;
  final String label;
  // 沿 P3 现有 def 体例 fromYaml constructor
}

class EnmityCombatModifier {
  final int threshold;
  final double playerAttackPowerMult;
  final double enemyAttackPowerMult;
  final int severeThreshold;
  final double severeMult;
  final double clampMax;
  // 沿体例
}

class JianghuTriggers {
  final int stageBossKillDelta;
  final int stageBossKillRivalDelta;
  final int encounterNpcDeltaMin;
  final int encounterNpcDeltaMax;
  // 沿体例
}
```

#### B1.8: StageDef + EncounterDef 字段扩展

`lib/data/stage_def.dart` 加 `final String? npcId;` 字段 + fromYaml 解析 `yaml['npc_id'] as String?`。

`lib/features/encounter/domain/encounter_def.dart` 加 `final AffectsReputation? affectsReputation;` 字段 + 新嵌套类 + fromYaml 解析。

#### B1.9: IsarSetup schema 注册

`lib/data/isar_setup.dart` 加:

```dart
import '../features/jianghu/domain/reputation.dart';
import '../features/jianghu/domain/npc_relation.dart';

// 在 Isar.open schemas 列表里加:
schemas: [
  // ... 现有
  ReputationSchema,
  NpcRelationSchema,
],
```

#### B1.10: build_runner + commit

```bash
cd /Users/a10506/Desktop/wuxia-idle-T17
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter analyze --fatal-warnings   # baseline 守 0 issue
git add lib/features/jianghu/ lib/data/ data/numbers.yaml data/factions.yaml data/stages.yaml data/encounters.yaml lib/features/encounter/domain/encounter_def.dart
git commit -m "[feat] P1.2 B1 schema · Reputation/NpcRelation Isar + numbers.yaml jianghu + factions.yaml + stages/encounters extend · nightshift T17 B1

- Reputation Isar Collection(playerId/factionId composite unique + value [-100,+100])
- NpcRelation Isar Collection(稀疏 source/target/type/level)
- numbers.yaml jianghu 段(reputation_tiers 7 阶 + enmity_combat_modifier + triggers)
- factions.yaml 新(6 门派 Demo)
- stages.yaml 5-8 boss 加 npcId 占位
- encounters.yaml 4-6 加 affects_reputation 占位
- JianghuConfig 解析(NumbersConfig.jianghu)+ StageDef.npcId + EncounterDef.affectsReputation
- IsarSetup 注册 ReputationSchema + NpcRelationSchema

详 docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md §2/§6
"
```

### === B2 service(~30min)===

#### B2.1: ReputationService(`lib/features/jianghu/application/reputation_service.dart`)

沿 `lib/features/inheritance/application/founder_buff_service.dart` 体例(Isar 注入 + IsarSetup.currentSlotId · 但本 service 需 writeTxn 不是 read-only):

```dart
import 'package:isar_community/isar.dart';
import '../../../data/isar_setup.dart';
import '../../../data/numbers_config.dart';
import '../domain/reputation.dart';

class ReputationService {
  final Isar isar;
  final NumbersConfig numbers;

  ReputationService(this.isar, this.numbers);

  /// 累积 delta + clamp [-100, +100]
  Future<void> applyDelta(int playerId, String factionId, int delta) async {
    await isar.writeTxn(() async {
      final existing = await isar.reputations
          .filter()
          .playerIdEqualTo(playerId)
          .factionIdEqualTo(factionId)
          .findFirst();
      if (existing == null) {
        final rep = Reputation()
          ..playerId = playerId
          ..factionId = factionId
          ..value = delta.clamp(-100, 100)
          ..updatedAt = DateTime.now();
        await isar.reputations.put(rep);
      } else {
        existing.value = (existing.value + delta).clamp(-100, 100);
        existing.updatedAt = DateTime.now();
        await isar.reputations.put(existing);
      }
    });
  }

  /// 查 tier(numbers.yaml.jianghu.reputation_tiers 7 阶映射)
  String tierOf(int value) {
    for (final t in numbers.jianghu.reputationTiers) {
      if (value >= t.min && value <= t.max) return t.tier;
    }
    return 'yiLiu';  // fallback 中间区间
  }

  Future<List<Reputation>> allFor(int playerId) async {
    return isar.reputations.filter().playerIdEqualTo(playerId).findAll();
  }
}
```

#### B2.2: NpcRelationService(`lib/features/jianghu/application/npc_relation_service.dart`)

```dart
class NpcRelationService {
  final Isar isar;
  final NumbersConfig numbers;

  NpcRelationService(this.isar, this.numbers);

  Future<void> upsert({
    required int sourceCharacterId,
    required int targetCharacterId,
    required String type,
    required int level,
  }) async {
    // 沿 reputation upsert 体例 · 双 index find + writeTxn put
    // ...
  }

  Future<List<NpcRelation>> enmityAgainst(int playerCharacterId) async {
    return isar.npcRelations
        .filter()
        .sourceCharacterIdEqualTo(playerCharacterId)
        .typeEqualTo('foe')
        .levelLessThanOrEqualTo(-50)
        .findAll();
  }

  /// 沿 spec §5 公式:level [-50,-80) → 1.15 · level ≤ -80 → 1.25 · 其余 → 1.0
  Future<double> attackPowerMultFor(int playerCharacterId, int enemyNpcId) async {
    final rel = await isar.npcRelations
        .filter()
        .sourceCharacterIdEqualTo(playerCharacterId)
        .targetCharacterIdEqualTo(enemyNpcId)
        .findFirst();
    if (rel == null) return 1.0;
    final emc = numbers.jianghu.enmityCombatModifier;
    if (rel.level <= emc.severeThreshold) return emc.severeMult;       // ≤ -80
    if (rel.level <= emc.threshold) return emc.playerAttackPowerMult;  // ≤ -50
    return 1.0;
  }
}
```

#### B2.3: Riverpod wire(`lib/features/jianghu/application/jianghu_providers.dart`)

沿 `lib/features/inheritance/application/founder_buff_providers.dart` 体例(riverpod_generator):

```dart
import 'package:riverpod_annotation/riverpod_annotation.dart';
import '../../../data/isar_setup.dart';
import '../../../data/numbers_config.dart';
import 'reputation_service.dart';
import 'npc_relation_service.dart';

part 'jianghu_providers.g.dart';

@riverpod
ReputationService reputationService(ReputationServiceRef ref) {
  final numbers = ref.watch(numbersConfigProvider);   // 真名按 Phase 0 grep
  return ReputationService(IsarSetup.isar, numbers);
}

@riverpod
NpcRelationService npcRelationService(NpcRelationServiceRef ref) {
  final numbers = ref.watch(numbersConfigProvider);
  return NpcRelationService(IsarSetup.isar, numbers);
}

@riverpod
Future<List<Reputation>> reputationsForCurrentPlayer(
    ReputationsForCurrentPlayerRef ref) async {
  // 当前 player = SaveData.activeCharacterIds 中 isFounder=true 角色 · 沿 founder_buff 体例
  final service = ref.watch(reputationServiceProvider);
  // playerId 真接入留下波 P1.2 polish · 本 task fixture playerId = 1
  return service.allFor(1);
}
```

#### B2.4: encounter_service 集成

`lib/features/encounter/application/encounter_service.dart` 加 hook:resolve encounter 时若 `encounter.affectsReputation != null` 调 `ReputationService.applyDelta`。**沿 encounter_service 现有 outcome 分支体例**,不破坏现有签名:

```dart
// 在 resolve / applyOutcome 体例位置
if (encounter.affectsReputation != null) {
  final rep = encounter.affectsReputation!;
  final delta = rng.nextInt(rep.deltaMax - rep.deltaMin + 1) + rep.deltaMin;
  await reputationService.applyDelta(playerId, rep.factionId, delta);
}
```

**注**:encounter_service 体例可能用 `EncounterOutcome` enum + dispatch · 按 Phase 0 grep 实际为准。如果 encounter_service 没有 reputationService 依赖,**最小改**:在 `encounter_service_providers.dart` 加 reputationServiceProvider 依赖。

#### B2.5/B2.6: 单测

`test/features/jianghu/reputation_service_test.dart`(沿 `test/features/inheritance/founder_buff_service_test.dart` 体例,**用 test() 不 testWidgets · memory `feedback_isar_widget_test_deadlock`**)6-8 测:
- applyDelta 新建 reputation
- applyDelta 累积 + clamp 上限 +100
- applyDelta 累积 + clamp 下限 -100
- applyDelta 多门派隔离
- tierOf 7 阶映射 sweep
- tierOf 边界值(-71/-70 边界 / -41/-40 / -11/-10 / 11/10 / 40/41 / 70/71)

`test/features/jianghu/npc_relation_service_test.dart` 5-7 测:
- upsert 新建 / upsert 更新已有
- enmityAgainst 过滤 type=foe + level ≤ -50
- attackPowerMultFor 三档(1.0/1.15/1.25)
- attackPowerMultFor 无 relation 返 1.0

**Isar 测体例**:用 `Isar.openSync` 内存数据库 · NumbersConfig fixture · **memory `feedback_isar_autoincrement_test_id_collision`**:多实例时 `id` 用 explicit assign,不依赖 autoIncrement。

#### B2.7: commit

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter analyze --fatal-warnings
flutter test test/features/jianghu/
git add lib/features/jianghu/ lib/features/encounter/ test/features/jianghu/
git commit -m "[feat] P1.2 B2 service · ReputationService + NpcRelationService + EncounterIntegration · nightshift T17 B2

- ReputationService(applyDelta + clamp + tierOf 7 阶 + allFor)
- NpcRelationService(upsert + enmityAgainst + attackPowerMultFor 三档)
- jianghu_providers riverpod_generator(reputationServiceProvider + npcRelationServiceProvider + reputationsForCurrentPlayerProvider)
- encounter_service 集成 affects_reputation hook(applyDelta on resolve)
- 单测 reputation 6-8 + npc_relation 5-7

详 docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md §3
"
```

### === B3 UI + battle 集成(~25min)===

#### B3.1: UiStrings 12 段

`lib/shared/strings.dart` 加(沿 mainMenuPvp/Hint 体例):

```dart
static const String mainMenuJianghu = '江湖恩怨';
static const String mainMenuJianghuHint = '声望 7 阶 + 多门派关系(主线 Ch3 cleared 解锁)';
static const String reputationPanelTitle = '江湖声望';
static const String reputationTierXueTu = '声名狼藉';
static const String reputationTierSanLiu = '恶名';
static const String reputationTierErLiu = '默默无闻';
static const String reputationTierYiLiu = '薄有微名';
static const String reputationTierJueDing = '侠名初显';
static const String reputationTierZongShi = '声振江湖';
static const String reputationTierWuSheng = '天下闻名';
static const String enmityWarning = '当前有敌对 NPC';
static const String panelFriendSection = '盟友';
static const String panelFoeSection = '敌对';
```

#### B3.2: ReputationPanelScreen

`lib/features/jianghu/presentation/reputation_panel_screen.dart` 卡片三段式(沿 `lineage_panel_screen.dart`):

```dart
class ReputationPanelScreen extends ConsumerWidget {
  const ReputationPanelScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final reputations = ref.watch(reputationsForCurrentPlayerProvider);
    return Scaffold(
      appBar: AppBar(title: const Text(UiStrings.reputationPanelTitle)),
      body: reputations.when(
        data: (list) => ListView(...),  // 3 section: faction list + 主阶 chip + foe/friend section
        loading: () => const Center(child: CircularProgressIndicator()),
        error: (e, _) => Center(child: Text('加载失败:$e')),
      ),
    );
  }
}
```

#### B3.3: ReputationTierChip widget

`lib/features/jianghu/presentation/widgets/reputation_tier_chip.dart`(沿 RankBadgeWidget 体例):

```dart
class ReputationTierChip extends StatelessWidget {
  final String tier;     // xueTu..wuSheng
  final int value;       // 当前 value · 进度条用
  // 7 阶颜色梯度:xueTu 灰 / sanLiu 暗红 / erLiu 浅灰 / yiLiu 青 / jueDing 浅金 / zongShi 金 / wuSheng 朱红
}
```

#### B3.4: main_menu 江湖 入口

`lib/features/main_menu/presentation/main_menu.dart` 在 PVP `_MenuButton` 之后、Leaderboard `_MenuButton` 之前插江湖 `_MenuButton`(沿现有体例)。**入口数从 16 → 17**(T15 加 PVP / T16 加 Sect 各 +1 后基数 16)。

#### B3.5: battle 注入 attackPowerMultiplier

`lib/core/application/battle_providers.dart` startBattle:

```dart
// 沿 mass_battle_strategy.dart:182 体例,但这里在 setup 阶段烘焙(strategy 不动 · 0 改 strategy)
// startBattle 入口 + enemyTeam 列表里 each enemy:
final npcService = ref.read(npcRelationServiceProvider);
final playerCharId = save.playerCharacterId;     // 真接入按 Phase 0 grep · 可能叫 activeCharacterIds.first
for (var i = 0; i < rightTeam.length; i++) {
  final mult = await npcService.attackPowerMultFor(playerCharId, rightTeam[i].id);
  if (mult != 1.0) {
    leftTeam[0] = leftTeam[0].copyWith(attackPowerMultiplier: mult);
    rightTeam[i] = rightTeam[i].copyWith(attackPowerMultiplier: mult);
  }
}
```

**关键不变量**:
- 双向对等(leftTeam[0] 和 rightTeam[i] 都加同 mult · spec §2 决议 enemy_attack_power_mult = player_attack_power_mult = 1.15)
- clamp ≤ 1.25(numbers.yaml.jianghu.enmity_combat_modifier.clamp_max)
- 仅 P3.1.B/P3.2 已实装时的 attackPowerMultiplier · DamageCalculator/strategy 0 改

#### B3.6: main_menu_test 入口 16→17

`test/features/main_menu/presentation/main_menu_test.dart` 现有断言找 `_MenuButton` 数,改 16 → 17 + 加江湖 button finder 测。

#### B3.7: ReputationPanelScreen widget test(R4 3-5 测)

```dart
testWidgets('ReputationPanelScreen 空声望显空 state', ...);
testWidgets('ReputationPanelScreen 注入 3 门派 reputation 显 3 row', ...);
testWidgets('ReputationTierChip xueTu/yiLiu/wuSheng 三阶颜色 + label 显示', ...);
test('UiStrings.reputationTierXueTu...wuSheng 7 段非空', ...);
```

**ListView widget test 必扩 viewport**(memory `feedback_listview_widget_test_viewport`):≥ 7 行 ListView 用 `tester.binding.setSurfaceSize(Size(800, 2000)) + addTearDown`。

#### B3.8: commit

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter analyze --fatal-warnings
flutter test test/features/jianghu/ test/features/main_menu/presentation/
git add lib/features/jianghu/ lib/features/main_menu/ lib/shared/strings.dart lib/core/application/battle_providers.dart test/features/jianghu/ test/features/main_menu/presentation/
git commit -m "[feat] P1.2 B3 UI + battle · ReputationPanelScreen + main_menu 入口 + battle attackPowerMultiplier 注入 · nightshift T17 B3

- ReputationPanelScreen(3 段式卡片 · 沿 lineage_panel_screen 体例)
- ReputationTierChip widget(7 阶颜色梯度)
- main_menu 入口 16→17(江湖 在 PVP 后 Leaderboard 前)
- UiStrings 12 段(mainMenuJianghu + reputationTier* 7 + enmityWarning + panel*)
- battle_providers.startBattle 注入 attackPowerMultiplier(沿 P3.1.B/P3.2 体例 · strategy 0 改)
- R4 widget 测 3-5 + main_menu test 入口数 +1

详 docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md §4/§5
"
```

### === B4 R5 + closeout(~20min)===

#### B4.1-B4.6: R5 红线测族 ~10-12 测

`test/features/jianghu/jianghu_r5_test.dart`(新):

```dart
group('R5.1 reputation 7 阶 e2e + clamp', () {
  // 21 测点 sweep:-100/-71/-70/-41/-40/-11/-10/0/10/11/40/41/70/71/100 + 边界外 (-150 clamp -100 / +150 clamp +100)
});

group('R5.2 enmity 阈值', () {
  test('level=-49 attackPowerMultFor 返 1.0', ...);
  test('level=-50 attackPowerMultFor 返 1.15', ...);
  test('level=-51 attackPowerMultFor 返 1.15', ...);
  test('level=-80 attackPowerMultFor 返 1.25', ...);
  test('level=-81 attackPowerMultFor 返 1.25(clamp_max)', ...);
});

group('R5.3 战斗 §5.4 cap 不越', () {
  test('wuSheng·dengFeng + enmity 1.25 fixture · default_ground_strategy 输出 ≤ 8000 普伤红线', () {
    // fixture: attacker.internalForce = 15000, attacker.attackPowerMultiplier = 1.25,
    // skillMultiplier = 普攻 500, etc
    // DamageCalculator 输出 ≤ 8000 (§5.4)
  });
});

group('R5.4 trigger e2e', () {
  test('stage_boss_kill_delta · ReputationService.applyDelta(player, faction, +5)', ...);
  test('stage_boss_kill_rival_delta · 击杀邪派 boss → orthodox +3', ...);
  test('encounter applyDelta · affects_reputation 段触发', ...);
});

group('R5.5 §5.2 七阶 label 锁', () {
  test('numbers.yaml.jianghu.reputation_tiers 7 阶等 §5.2 七阶 (xueTu..wuSheng)', () {
    final tiers = numbers.jianghu.reputationTiers.map((t) => t.tier).toList();
    expect(tiers, [
      'xueTu', 'sanLiu', 'erLiu', 'yiLiu', 'jueDing', 'zongShi', 'wuSheng',
    ]);
  });
});

group('R5.6 P3.4 隔离 schema 断言', () {
  test('Reputation collection name 与 SectReputation 不冲突(或 SectReputation 不存在则 trivially 过)', () {
    expect(ReputationSchema.name, isNot(equals('SectReputation')));
    // memory feedback_red_line_test_semantics: 写约束语义不写瞬时事实
  });
});
```

#### B4.7: closeout doc ≤80 行

新 `docs/handoff/p1_2_jianghu_full_closeout_2026-05-25.md` 体例(沿 doc_inflation):

```
# P1.2 §12.1 江湖恩怨 + §12.2 声望 全 4 batch closeout · nightshift T17

## TL;DR(≤5 行)
P1.2 全 4 batch nightshift 一次过 · main HEAD <sha> · 4 commit · baseline +N test · 0 analyze · 1.0 整体 75→85%。

## Commit timeline
| Batch | SHA | 内容 |
|---|---|---|
| B1 | <sha> | schema(2 Collection + numbers.yaml jianghu + factions/stages/encounters) |
| B2 | <sha> | service(ReputationService + NpcRelationService + EncounterIntegration) |
| B3 | <sha> | UI + battle(ReputationPanelScreen + main_menu + battle attackPowerMultiplier 注入) |
| B4 | <sha> | R5(10-12 测) + closeout(本文件) |

## R5 测族结果
- R5.1 reputation 7 阶 21 测点 sweep + clamp 双向(2 测)
- R5.2 enmity 阈值 5 测
- R5.3 §5.4 红线 ≤ 8000 普伤(1 测)
- R5.4 trigger 3 测
- R5.5 §5.2 七阶锁(1 测)
- R5.6 P3.4 隔离 schema 断言(1 测)
- 全过 / baseline 1357 + delta 13

## 工作量复盘
spec 估 ~7h xhigh / nightshift wall ~XXmin / ratio 0.XX

## 挂账(下波)
- Q3 C 心法触发 1.1 / Q4 D 援军 stage 1.1 / Q5 C 双轴 1.1
- Phase 5 narrative 集成(江湖 encounter 完整文案 8-12 条 · 本 task ship schema 占位 4-6)
- StageDef.npcId 真 NPC schema 实装(本 task 字符串占位)
- attackPowerMultiplier 注入 battle setup edge case audit(可能与 P3.1.B/P3.2 烘焙顺序冲突 · 需主对话 grep 验)

## 不变量沿用
- GDD §5.4 红线 ≤8000 普伤(R5.3 校验)
- §5.3 七阶锁(R5.5 校验)
- §5.5 在线=离线(reputation 仅靠 trigger 累积 · 0 后台时钟)
- §5.1 反留存(0 每日声望任务)
- BattleStrategy / DamageCalculator 0 改
- founder_buff_service 0 改
```

#### B4.8: commit(必含 `nightshift T17`)

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter analyze --fatal-warnings
flutter test test/features/jianghu/
git add test/features/jianghu/jianghu_r5_test.dart docs/handoff/p1_2_jianghu_full_closeout_2026-05-25.md
git commit -m "[feat] P1.2 B4 R5 + closeout · 10-12 测 + closeout doc · nightshift T17 B4

- R5.1 reputation 7 阶 e2e + clamp 双向(21 测点 sweep)
- R5.2 enmity 阈值 5 测(level=-49/-50/-51/-80/-81)
- R5.3 §5.4 红线 ≤8000 普伤(wuSheng·dengFeng + enmity 1.25 fixture)
- R5.4 trigger 3 测(stage_boss_kill + rival_kill + encounter)
- R5.5 §5.2 七阶锁(reputationTiers 等 xueTu..wuSheng)
- R5.6 P3.4 隔离 schema 断言
- closeout 80 行 + 挂账 4 项(Q3C/Q4D/Q5C 1.1 + Phase 5 narrative + StageDef.npcId 真 NPC + battle setup edge case)

P1.2 100% 完整闭环 · 1.0 整体 75→85% 跳档。

详 docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
"
```

## 不要做

- **🚨 严禁 cd / git -C 到 dispatcher 创建的 worktree 之外的任何路径**(v2 A1 红线)
- 不要在 worktree 之外加 commit(全 4 batch 都在 wuxia-idle-T17)
- 不要 git push / git rebase / git reset --hard
- 不要顺手改 GDD.md / CLAUDE.md / ROADMAP_1_0.md / PROGRESS.md(closeout 内挂账即可,本 task 不 sync · T22 总收尾会处理)
- 不要改 `lib/features/battle/domain/strategy/*.dart`(attackPowerMultiplier 在 battle setup 注入,strategy 已支持 · 0 改 strategy)
- 不要写 testWidgets 调 writeTxn(memory `feedback_isar_widget_test_deadlock`,改 test() 单测)
- 不要硬编码数值/文案(数值走 numbers.yaml · 文案走 strings.dart)
- 不要破红线 §5.4(R5.3 校验)
- 不要动 sect/PVP/inner_demon 等其他 P3 feature 模块(本 task 只 jianghu/main_menu/encounter)
- B3.5 battle_providers 注入只在 startBattle 入口烘焙一次(0 strategy 改)
- Phase 5 narrative 完整 8-12 条挂账下波 batch(本 task 只 ship 4-6 个 encounter 占位 schema · 不写真文案)

## 参考

- **spec**: `docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md` §1-§9(全引)
- **service 体例**: `lib/features/inheritance/application/founder_buff_service.dart`(read-only)+ `lib/features/sect/application/sect_event_service.dart`(writeTxn)
- **provider 体例**: `lib/features/inheritance/application/founder_buff_providers.dart`(riverpod_generator)
- **attackPowerMultiplier 烘焙体例**: `lib/features/battle/domain/strategy/mass_battle_strategy.dart:182`(P3.2)+ `lib/features/battle/domain/strategy/light_foot_strategy.dart:120`(P3.1.B)
- **encounter 集成体例**: `lib/features/encounter/application/encounter_service.dart`(outcome 分支)
- **NumbersConfig 解析体例**: `lib/data/numbers_config.dart` `HeritageItems` class(line 436)+ `MassBattleConfig`
- **UI 体例**: `lib/features/inheritance/presentation/lineage_panel_screen.dart`(3 段式卡片)
- **测体例**: `test/features/inheritance/founder_buff_service_test.dart` + `test/features/sect/sect_event_service_test.dart`
- **memory**:
  - `feedback_isar_autoincrement_test_id_collision`(多实例 id 显式 assign)
  - `feedback_isar_widget_test_deadlock`(testWidgets + writeTxn 死锁 · 改 test())
  - `feedback_listview_widget_test_viewport`(≥7 行 ListView setSurfaceSize)
  - `feedback_red_line_test_semantics`(R5 写约束语义不写瞬时事实)
  - `feedback_nightshift_v2_first_run_lessons` A1-A7(verify 教训)
  - `feedback_phase0_grep_two_axes`(6 维 grep · Phase 0 必跑)
  - `feedback_doc_inflation_overnight`(closeout ≤80)
