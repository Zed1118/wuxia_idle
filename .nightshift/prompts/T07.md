# Nightshift T07 · 师徒系统 Phase 5+ 升级 spec 起草(audit)

你是 Claude Opus 4.7 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T07`(branch `nightshift/T07`,从 `main` HEAD `598015a` 起)内独立完成本任务。

## 背景

挂机武侠 Flutter Desktop。GDD §7.1 师徒传承在 Demo 阶段只做 3 角色(祖师 + 大弟子 + 二弟子,见 `data/masters.yaml`),**飞升 / 收徒孙 / 祖师爷 buff 全部 hold 到 Phase 5+**。规则层在 `numbers.yaml inheritance` 段已落:

```yaml
inheritance:
  founder_ancestor_buff:
    enabled_when_alive: false  # Demo 不实装,1.0+ 再设计
  heritage_items:
    transfer_trigger: ascend_to_wusheng        # 武圣飞升时自动传(GDD §7.1 v1.5)
    multi_disciple_allocation: player_pick      # 玩家选件分配
    stack_across_generations: false             # 不累代叠加
    conflict_slot_resolution: auto_swap         # 自动卸下原装入背包
```

本 task **0 改动 yaml / lib / test**,**只起草** `docs/handoff/phase5_master_disciple_spec_2026-05-20.md`(≤ 80 行),作为 Phase 5+ 师徒升级的方案文档,供 1.0 路线图后期回头看。

## 目标

产出唯一新文件 `docs/handoff/phase5_master_disciple_spec_2026-05-20.md`(60-80 行),含 5 段。

## 操作

### Phase 0 · reality check(5 min)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T07
cat data/masters.yaml | head -60                                   # 3 角色现状
grep -A 30 "inheritance:" data/numbers.yaml | head -50              # 4 规则字段
grep -n "§7.1\|师徒\|飞升" GDD.md | head -10
grep -A 15 "### 7.1 师徒传承" GDD.md
ls lib/features/lineage/ 2>/dev/null                                # 师徒 feature 现状
```

### Phase 1 · 写 spec doc(35-45 min)

写到 `docs/handoff/phase5_master_disciple_spec_2026-05-20.md`,沿如下 5 段结构:

```markdown
# Phase 5+ 师徒系统升级 spec · 2026-05-20

> Nightshift T07 起草。**audit 类 0 改 yaml/lib**,纯方案文档。供 1.0 路线图 Phase 5+(Demo 后)激活时回头看。

## §1 当前状态(Demo 锚定)

- **角色固定 3**:祖师(yiLiu)/ 大弟子(erLiu)/ 二弟子(xueTu)— `data/masters.yaml`
- **飞升机制**:`numbers.yaml inheritance.founder_ancestor_buff.enabled_when_alive: false` Demo 不实装
- **遗物 4 规则字段已锚定**:见 numbers.yaml `inheritance.heritage_items`
- **lib/features/lineage/** Phase 4 已铺基础设施(具体扫描记录)

## §2 Phase 5+ 解锁节奏(GDD §7.1 锚)

| 突破到 | 解锁内容 | 实装位置(Phase 5+ 时点) |
|---|---|---|
| 一流 / 结丹 | 收徒(1 → 2 弟子) | 已 Demo 实装(`MasterRepository.unlockDisciple`) |
| 绝顶 / 化神 | 徒弟收徒孙(2 → 4 角色) | **Phase 5 新增** masters.yaml lineageRole=grand_disciple |
| 飞升渡劫 | 传位大徒弟 + 前任成祖师爷 | **Phase 5+ 新增** AscendService + AncestorBuffService |

## §3 飞升机制设计(Phase 5+ 实装)

3 子段:
- §3.1 触发条件(玩家境界达 wuSheng + 内力满 + 修炼度 9 层圆满 + 主线特殊章节解锁)
- §3.2 自动遗物分配 UI 流程(`AscendInheritanceScreen` 选件界面,沿 v1.5 `multi_disciple_allocation: player_pick`)
- §3.3 数据迁移(`MasterRepository.ascendFounder`:founder → ancestor 角色 + 大徒弟变 founder + 二弟子变大徒弟)

## §4 祖师爷 buff(`founder_ancestor_buff`)激活方案

- §4.1 触发:`enabled_when_alive: false → true`(Phase 5+ 切换 yaml)
- §4.2 buff 维度建议(内力上限 +5% / 修炼度 base +3% / 暴击 +2%,具体 v1.0 平衡时拍)
- §4.3 buff 作用域(所有 active 弟子)
- §4.4 UI 显示(MasterCard 顶部「祖师爷 [名字]」+ 一行 buff 摘要)

## §5 遗物 4 规则代码实装清单

| 规则字段 | 代码层位置 | 实装难度 |
|---|---|---|
| `transfer_trigger: ascend_to_wusheng` | `AscendService.transferHeritage` Phase 5 | 中(联动 AscendInheritanceScreen) |
| `multi_disciple_allocation: player_pick` | `AscendInheritanceScreen` UI | 中(逐件分配交互) |
| `stack_across_generations: false` | `HeritageBuffCalculator.compute` 只取当代 | 低 |
| `conflict_slot_resolution: auto_swap` | `EquipmentService.equipHeritage` 自动卸原装入背包 | 低 |

## §6 closeout

(2-3 句话:本 spec 起草背景 / 实装顺序建议 / 估时锚点)
```

### 严格遵守

- 80 行以内(verify ≤ 80)
- 6 段都齐(§1 / §2 / §3 / §4 / §5 / §6)
- 表格 + 列表混用(避免大段文字墙)
- 引用 GDD § / numbers.yaml 字段时给具体行号 / 锚点
- **数字与现状必 grep 实测**(memory `feedback_closeout_numbers_grep`)
- 不预设 1.0 数值具体值(留「v1.0 平衡时拍」)

### Phase 2 · commit

```bash
git add docs/handoff/phase5_master_disciple_spec_2026-05-20.md
git commit -m "docs(nightshift T07): Phase 5+ 师徒升级 spec 起草"
```

## 硬约束

- **只新增 1 个文件**:`docs/handoff/phase5_master_disciple_spec_2026-05-20.md`
- **不动** `lib/` / `data/` / `test/` / 其他 docs / `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `numbers.yaml`
- **≤ 80 行**(允许 +20% buffer 到 96 行,超 96 必 fail)
- **6 段结构齐**:§1 当前状态 / §2 解锁节奏 / §3 飞升机制 / §4 祖师爷 buff / §5 遗物 4 规则 / §6 closeout
- **commit message 含 `nightshift T07`**

## 完成动作

```bash
wc -l docs/handoff/phase5_master_disciple_spec_2026-05-20.md  # ≤ 80
git log -1 --pretty=%s | grep "nightshift T07"
```
