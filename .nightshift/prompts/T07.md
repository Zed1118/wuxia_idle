# Nightshift T07 · GameEventService #4 lineageInherited 接口 + #9 路由 edge test

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T07`(branch `nightshift/T07`,从 `main` HEAD `217ddf7` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。P1 #42 Phase 1 Phase 2(2026-05-17)实装 `GameEventService`(`lib/features/event/application/game_event_service.dart` 212 行)7 type 写入 API + 6 caller 接入:
- #1 retreatCompleted / #2 adventureTriggered / #3 equipmentObtained / #5 skillEnlightened / #6 realmBreakthrough / #7 resonanceUpgraded / #8 bossDefeated
- **#4 lineageInherited 留接口**(0 caller,Phase 5+ 师徒系统升级时激活)
- **#9 借 #6 lineageRole 路由**:founder 角色走 realmBreakthrough eventType,disciple 走 disciplePromoted eventType

现有 `game_event_service_test.dart` 14 test 覆盖 #1-#3 #5-#8 主路径 + safety 网,**未细化** #4 接口 placeholder + #9 路由分支 + 多场景组合。本任务补 3-5 test,产出独立 `_lineage_routing_edge_test.dart`。

## 红线:约束语义不写瞬时事实

#9 路由断言用 `eventType` 枚举值集合等价(`founder → GameEventType.realmBreakthrough` / `disciple → GameEventType.disciplePromoted`),不写字面字符串。#4 placeholder 断言写「API 可调用 + 调用后返回 sane default(如 null / void)」,不假设具体行为。

## 目标

产出**唯一**新文件:`test/features/event/application/game_event_service_lineage_routing_edge_test.dart`

## 操作

1. 读 `lib/features/event/application/game_event_service.dart` 找:
   - `recordLineageInherited` 或类似 API 签名(#4 placeholder,可能 throw UnimplementedError 或 noop)
   - `recordRealmBreakthrough` 实现 lineageRole 路由分支
2. 读现有 `test/features/event/application/game_event_service_test.dart` 14 test 学 Isar fixture / 验证 GameEvent 写入体例(`isar.gameEvents.where().findFirst()` 等)
3. 新增 3-5 test:
   - **A. #4 lineageInherited 接口 placeholder**:调用 `recordLineageInherited(...)` → 不抛非预期异常 + 若返回 void/null 则验证 + 若写入空 GameEvent 验证字段(以代码实际为准)
   - **B. #9 founder 路由 realmBreakthrough**:fixture character lineageRole=founder + 调 recordRealmBreakthrough → 写入 GameEvent.eventType == realmBreakthrough(已在 #6 test 覆盖,本 test 强化为「founder 路径不命中 disciplePromoted」反向断言)
   - **C. #9 disciple 路由 disciplePromoted**:fixture character lineageRole=disciple + 调 recordRealmBreakthrough → 写入 GameEvent.eventType == disciplePromoted(已在 #6/#9 test 覆盖,本 test 强化为「disciple 路径不命中 realmBreakthrough」反向断言)
   - **D.(选做)路由 lineageRole=null 兜底**:character lineageRole 未设 → 路由到 default(founder?以代码为准)
   - **E.(选做)多次同 character 连续 breakthrough**:验证多 layersGained 累加正确(已在 safety 网 covered,本 test 强化跨调用累积语义)
4. **新 test 必须 `flutter test` 全过**

## 硬约束

- **只新增** `test/features/event/application/game_event_service_lineage_routing_edge_test.dart`
- **不动** `lib/` / `data/` / 其他 test
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`

## 完成动作

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/event/application/game_event_service_lineage_routing_edge_test.dart
git add test/features/event/application/game_event_service_lineage_routing_edge_test.dart
git commit -m "test(nightshift T07): #4 接口 + #9 路由 edge"
```

报告一句话「T07 done (added N lineage-routing edge cases)」。
