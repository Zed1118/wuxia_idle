# Nightshift T01 · HomeFeedScreen 6 档相对时间剩 4 档 edge test

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T01`(branch `nightshift/T01`,从 `main` HEAD `217ddf7` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。P1 #42 Phase 1(2026-05-17)新落 `HomeFeedScreen`(`lib/features/home_feed/presentation/home_feed_screen.dart` 193 行)作为 main.dart home,**含 6 档相对时间格式**:刚才 / N 分钟前 / N 小时前 / 昨天 / N 天前 / 月日。

现有 `test/features/home_feed/presentation/home_feed_screen_test.dart` 仅覆盖 2 档(5 分钟内显「刚才」+ 30 分钟前显「N 分钟前」),剩 4 档无 widget test 覆盖。本任务补这 4 档 edge,产出独立 `_time_format_test.dart` 不动现有 test。

## 红线:约束语义不写瞬时事实

参 `feedback_red_line_test_semantics`(用户全局 memory):测试断言写**白名单 / 集合自洽 / 类型契约**,而非具体数字 / 字面值。例:断言相对时间字符串「N 小时前」格式匹配 `RegExp(r'^\d+ 小时前$')`,不写「3 小时前」。

## 目标

产出**唯一**新文件:`test/features/home_feed/presentation/home_feed_screen_time_format_test.dart`

## 操作

1. 读现有 `test/features/home_feed/presentation/home_feed_screen_test.dart` 学体例(import / provider override / pump / find 模式)
2. 读 `lib/features/home_feed/presentation/home_feed_screen.dart` 找相对时间格式化函数(`_formatRelativeTime` 或类似命名),确认 6 档边界:
   - < 5 分钟:「刚才」
   - 5-60 分钟:「N 分钟前」
   - 1-24 小时:「N 小时前」
   - 24-48 小时:「昨天」
   - 2-7 天:「N 天前」
   - ≥ 7 天:「月日」(如 5月10日)
3. 新增 4 个 testWidgets,每个覆盖一档:
   - **A.「N 小时前」档**:occurredAt = now - 3 hours → 显示符合 `RegExp(r'\d+\s*小时前')`
   - **B.「昨天」档**:occurredAt = now - 26 hours → 显示「昨天」
   - **C.「N 天前」档**:occurredAt = now - 3 days → 显示符合 `RegExp(r'\d+\s*天前')`
   - **D.「月日」档**:occurredAt = now - 10 days → 显示符合 `RegExp(r'\d+月\d+日')`(或项目实际格式)
4. 每个 testWidgets 复用现有 test 的 fixture/override 模式,差异仅在 occurredAt 数值
5. 文件结构:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:flutter_test/flutter_test.dart';
   // ... 沿现有 home_feed_screen_test.dart 同款 imports

   void main() {
     group('HomeFeedScreen 相对时间格式 6 档(剩 4 档)', () {
       testWidgets('N 小时前档', (tester) async { ... });
       testWidgets('昨天档', (tester) async { ... });
       testWidgets('N 天前档', (tester) async { ... });
       testWidgets('月日档', (tester) async { ... });
     });
   }
   ```
6. **新 test 必须 `flutter test` 全过**(verify.sh 会限定跑本文件)。若实际格式与背景描述不符,**以代码实际为准**调整断言,不修 lib/

## 硬约束

- **只新增** `test/features/home_feed/presentation/home_feed_screen_time_format_test.dart` **一个文件**
- **不动现有** test/ 任何文件
- **不动** `lib/` / `data/` / `assets/`
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不调用网络

## 完成动作

test 写好后:
```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/home_feed/presentation/home_feed_screen_time_format_test.dart
# 全过后:
git add test/features/home_feed/presentation/home_feed_screen_time_format_test.dart
git commit -m "test(nightshift T01): HomeFeedScreen 相对时间 4 档 edge"
```

**不要 push**。报告一句话「T01 done (added 4 time-format edge cases)」。
