# Nightshift T01 · #37 events orphan 永久封档(W17 长期挂账冲刺收尾)

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T01`(branch `nightshift/T01`,从 `main` HEAD `4132a13` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠是 Flutter Desktop 武侠 idle 游戏。PROGRESS.md 挂账 #37:6 个 events yaml 在 `data/events/_archive/` 长期无对应武学 / 心境 skill,W17 polish-C 评估「主题不适配,留 archive 永久不动」。本任务把这决议写成永久封档 doc,锁定不再扫不再评。

## 目标

产出**唯一**新文件:`docs/handoff/wuxia_w17_orphan_events_permanent_archive_2026-05-17.md`

## 操作

1. 读 `PROGRESS.md` 找 #37 段(grep `'^- 37\\.'` 或 `'orphan'`),理解现状
2. 读 `data/events/_archive/` 目录下 6 个 yaml 文件:
   - `duan_qiao_can_yue.yaml`
   - `gu_chuan_deng_ying.yaml`
   - `huang_cun_yao_ren.yaml`
   - `qing_lou_can_meng.yaml`
   - `lao_jing_hui_xiang.yaml`
   - `yu_zhong_qiao_men.yaml`
   每个文件读 `opening` 字段(或前 15 行)提炼主题
3. (可选)grep `GDD.md` 找「§8.4」「内容总量」附近主题清单
4. 写 doc,严格按下面结构:

```markdown
# W17 #37 6 events orphan 永久封档(2026-05-17)

> Nightshift T01 产出。决议锁定:6 件 events orphan 留 `data/events/_archive/` **永久不挂回**,不再扫描不再评估。

## 1. 背景

W14-3 派 DeepSeek 23 events 时,因主题与 GDD §8.4 武学 / 心境 skill 池不匹配,6 件被归入 _archive/。W17 polish-C 挂回 2 件(`huang_yuan_yi_zhong → qiu_quan` / `jiang_xin_ye_hua → wu_xia_yi`),余 6 件评估后仍不挂回。

## 2. 6 文件逐文件分析

### 2.1 duan_qiao_can_yue
- **主题**:(从 opening 提炼一句)
- **不挂回理由**:(主题不属于 武学/心境/心法 任何 GDD 锚定类目,挂回反而污染语义)

(其余 5 个同结构,each ~3-5 行)

## 3. 决议

所有 6 件留 `data/events/_archive/`,**永久不挂回**。后续 W18+ 若 GDD §8.4 新加 武学 tier / 心境主题 / 江湖故事类目,可重新评估;在此之前不再扫描。

## 4. PROGRESS 销账建议

(给一段约 80 字的建议文字,可直接抄入 PROGRESS.md 挂账 #37 段替换原文,标注「2026-05-17 W17 长期挂账冲刺永封档收尾」并 ~~划掉~~ 旧条目)
```

## 硬约束

- **不动** `lib/` / `data/encounters.yaml` / `data/events/*.yaml`(非 _archive)/ `data/events/_archive/*.yaml`
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不跑 `flutter pub get` / `dart run build_runner` / `flutter test`(dispatcher 跑 verify)
- 不调用网络,不用 web fetch

## 完成动作

doc 写好后:
```bash
git add docs/handoff/wuxia_w17_orphan_events_permanent_archive_2026-05-17.md
git commit -m "docs(nightshift T01): #37 6 events orphan 永久封档"
```

**不要 push**(dispatcher 不需要远程同步)。
报告一句话「T01 done」。
