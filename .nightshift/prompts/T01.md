# Nightshift T01 · encounter id 一致性扫描

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T01`(branch `nightshift/T01`,从 `main` HEAD `fc25207` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。CLAUDE.md §8.1 强约定:`data/encounters.yaml` 的 `id` 必须与 `data/events/<id>.yaml` 文件名严格一致,加载层强校验(`GameRepository._enforceEncounterRedLines`)。W14-W17 经多次扩容,现状:`encounters.yaml` 40 entry / `data/events/*.yaml` 40 文件 / `data/events/_archive/*.yaml` 6 orphan(W17 #37 永封档已 lock)。本任务做一次 id 一致性扫描,以 doc 形式产出报告,锁定当前对齐状态作基线。

## 目标

产出**唯一**新文件:`docs/handoff/wuxia_encounter_id_consistency_2026-05-17.md`

## 操作

1. 扫描 `data/encounters.yaml` 提取所有 id(grep `^  - id:` 模式),列表化
2. `ls data/events/*.yaml` 列出 40 个 yaml 文件名(去 `.yaml` 后缀作为 file-id 集合)
3. `ls data/events/_archive/*.yaml` 列出 6 orphan 文件名
4. 三集合**双向对账**:
   - encounters.yaml id ∈ events/ 文件集 ? (应 100%)
   - events/ 文件 ∈ encounters.yaml id 集 ? (应 100%)
   - _archive/ 文件 ∉ encounters.yaml id ? (应 100% — orphan 已永封档)
5. 报告 0 漂移即基线建立成功;若有漂移列具体 id + 文件名 + 性质(missing event / missing encounter / orphan-but-still-referenced)
6. 写 doc 严格按下面结构:

```markdown
# encounter id 一致性扫描(2026-05-17)

> Nightshift T01 产出。当前基线:encounters.yaml ↔ events/*.yaml ↔ _archive/ 三集合一致性快照。

## 0. 扫描范围
- 工作树:`/Users/a10506/Desktop/wuxia-idle-T01`(基于 main HEAD `fc25207`)
- 输入 1:`data/encounters.yaml`
- 输入 2:`data/events/*.yaml`
- 输入 3:`data/events/_archive/*.yaml`

## 1. 统计

| 集合 | 数量 |
|---|---|
| encounters.yaml id | (40) |
| data/events/*.yaml | (40) |
| data/events/_archive/*.yaml | (6) |

## 2. 双向对账

### 2.1 encounters.yaml id → events/ 文件
- 全部命中 / X 个 missing(列出)

### 2.2 events/ 文件 → encounters.yaml id
- 全部命中 / X 个 orphan(列出)

### 2.3 _archive/ → encounters.yaml id(应 0)
- X 个 orphan-but-referenced(列出,若有)

## 3. 结论

(0 漂移 = 基线建立。或列具体漂移 + 建议修复路径)

## 4. 后续维护建议

- 任何 W18+ 新增 encounter:同步加 `data/events/<id>.yaml`,id 严格匹配
- 任何 encounter 决议永封档:`mv data/events/<id>.yaml data/events/_archive/` + 从 encounters.yaml 删 entry,本扫描器(`.nightshift/prompts/T01.verify.sh` 风格 grep)定期跑保证 0 漂移
```

## 硬约束

- **不动** `lib/` / `test/` / `data/encounters.yaml` / `data/events/*.yaml` / `data/events/_archive/*.yaml`
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不跑 `flutter pub get` / `dart run build_runner` / `flutter test`(dispatcher 跑 verify)
- 不调用网络,不用 web fetch

## 完成动作

doc 写好后:
```bash
git add docs/handoff/wuxia_encounter_id_consistency_2026-05-17.md
git commit -m "docs(nightshift T01): encounter id 一致性扫描"
```

**不要 push**(dispatcher 不需要远程同步)。
报告一句话「T01 done」。
