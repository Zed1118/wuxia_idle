# Nightshift T01 · P1.2 spec PR #6 reviewer 4 项 minor fix

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T01`(branch `nightshift/T01`,从 main HEAD 起)内独立完成本任务。

## 背景

PR #6 是 P1.2 江湖恩怨 + 声望 spec 草案,reviewer 评 8/10,4 项 minor fix 后 merge。详 `docs/handoff/p1_2_spec_pending_e_fix_handoff_2026-05-24.md`。

**特殊性**:spec doc 在 `feat/p1_2_spec` 分支(远端 origin/feat/p1_2_spec)不在 main。本 task 不走 dispatcher 默认 nightshift/T01 分支,而是先切到 feat/p1_2_spec 分支改完 commit,**不要 push**(留待用户早上人工 push 触发 PR #6 更新)。

## 4 项 fix(全部改 spec doc 内容,无 lib/test/data 改)

文件:`docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md`

| # | 位置 | 当前 | 改成 |
|---|---|---|---|
| 1 | §3 yaml 数值 | `player_attack_power_mult: 1.15` 单一值 + `clamp_max=1.25` 但 trigger 无第 2 档 | 补 `severe_threshold: -80` + `severe_mult: 1.25`(原 25% 永远触不到) |
| 2 | §1 范围 OUT | 「心魔系统(P2.2 已闭)」 | 改「心魔系统(P3.2.C 销账)」(P2.2 是飞升前置,心魔实装在 P3.2.C) |
| 3 | §7 R5.6 | grep 校验作测试 | 改 Dart schema-level 断言(`Reputation.factionId` 类型 ≠ `SectReputation.sectId` Collection/字段分离) |
| 4 | §2 schema | `Reputation.factionId` String 无 composite index | 加 `@Index(composite=[playerId, factionId])` 防同 faction 多行重复 |

## 允许修改

- `docs/spec/p1_2_jianghu_enmity_spec_2026-05-24\.md`

## 禁止修改

- `lib/.*`
- `test/.*`
- `data/.*`
- `pubspec.*`
- `\.env.*`

## 操作

### Phase 0 · reality check(必跑,3 min)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T01
git log -1 --oneline
git fetch origin feat/p1_2_spec
git checkout -B feat/p1_2_spec origin/feat/p1_2_spec
git log -1 --oneline                    # 应是 spec PR #6 head commit
ls docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
grep -n "player_attack_power_mult" docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
grep -n "心魔系统" docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
grep -n "R5.6" docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
grep -n "Reputation.factionId" docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
```

### Step 1: §3 数值补 severe 第 2 档

定位 §3 yaml 段 `player_attack_power_mult: 1.15` 附近,加 2 行:
```yaml
severe_threshold: -80
severe_mult: 1.25
```
(放在 clamp_max 同段,缩进对齐)

### Step 2: §1 范围 OUT 引用 fix

把「心魔系统(P2.2 已闭)」改为「心魔系统(P3.2.C 销账)」。

### Step 3: §7 R5.6 测试方法改写

把 grep 校验改成 Dart schema-level 断言。示例改写:
```dart
test('Reputation.factionId 与 SectReputation.sectId 字段分离', () {
  expect(Reputation.factionId.runtimeType, isNot(SectReputation.sectId.runtimeType));
  // 或 ISAR collection 字段断言
});
```

### Step 4: §2 schema 加 composite index

在 §2 Reputation 段 `factionId: String` 附近加注:
```
@Index(composite: [playerId, factionId])  // 防同 faction 多行重复
```

### Step 5: commit(不 push)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T01
git add docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
git commit -m "[spec] P1.2 reviewer 4 项 minor fix · nightshift T01

- §3 数值阈值梯度补 severe_threshold/severe_mult
- §1 OUT 心魔引用改 P3.2.C
- §7 R5.6 grep → Dart schema 断言
- §2 Reputation 加 composite index
"
```

## 不要做

- 不要 git push(留用户早上人工)
- 不要改 lib/test/data 任何文件
- 不要顺手改其他 spec doc
- 不要 git rebase / git reset --hard
- commit message **必须**含字符串 `nightshift T01`
