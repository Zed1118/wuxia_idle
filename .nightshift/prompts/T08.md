# Nightshift T08 · 死代码 dry-run scan

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T08`(branch `nightshift/T08`,从 `main` HEAD `3d24af1` 起)内独立完成本任务。

## 背景

挂机武侠 Flutter Desktop 武侠 idle 游戏。代码量随 P1 #42 Phase 1+2 持续扩(lib/ 新增 tutorial / codex feature),周期需 dry-run scan 找 orphan provider/widget/service(W17 nightshift 已实战 1 次 — T03 死代码 scan 找到 2 死 provider 销账)。本 task 周期清账,**钉死 dry-run 不删**。

## 目标

产出**唯一**新文件:`docs/handoff/deadcode_scan_2026-05-19.md`(**≤ 100 行**)

## 操作

1. **dart fix dry-run 全仓**:
   ```bash
   cd /Users/a10506/Desktop/wuxia-idle-T08
   flutter pub get
   dart run build_runner build --delete-conflicting-outputs
   dart fix --dry-run > /tmp/dart_fix_dry.log 2>&1
   ```
   提取 unused_import / unused_local_variable / dead_code 等 hint 计数 + top 5 文件

2. **@riverpod provider 0 引用扫描**:
   ```bash
   grep -rn "@riverpod\b" lib/ --include="*.dart" | wc -l   # 总 provider 数
   ```
   对每个 `@riverpod` 上方紧邻的 function/class 名(provider name),grep 在 lib/ 外的 caller 引用数(`grep -rn "<providerName>" lib/ --include="*.dart" | grep -v <provider def file>` ),0 引用 = orphan candidate

3. **service class 0 引用扫描**:
   ```bash
   grep -rn "class \w*Service\b" lib/ --include="*.dart"
   ```
   对每个 service class,grep `new <ServiceName>(` + `<ServiceName>(` constructor pattern 在 lib/ 内的引用数,0 引用 = orphan candidate

4. **widget class 0 引用扫描**:
   ```bash
   grep -rnE "^class \w*Screen extends|^class \w*Page extends" lib/ --include="*.dart"
   ```
   对每个 screen/page class,grep 在 lib/(Navigator.push / MaterialPageRoute / .push)+ test/(builder factory)的引用数,0 引用 = orphan

5. 写 `docs/handoff/deadcode_scan_2026-05-19.md` 4 段:

```markdown
# 死代码 dry-run scan · 2026-05-19

> Nightshift T08 audit 产出。**钉死 dry-run 不删**。

## §1 dart fix --dry-run 摘要

- 总 hint 数: N
- 主要 hint type: (top 5 with count)
- 主要文件: (top 5 with hint count)

## §2 @riverpod provider 0 引用 candidates

| provider name | def file | 引用数 | 推荐处置 |
|---|---|---|---|
| ... | ... | 0 | 删/留(扩 caller) |

## §3 service / widget class 0 引用 candidates

| class | type | def file | 引用数 | 推荐处置 |
|---|---|---|---|---|
| ... | Service/Widget | ... | 0 | 删/留 |

## §4 总结 + 推荐处置(优先级排)

- 总扫描:provider N / service M / widget K
- 0 引用 candidate: provider X / service Y / widget Z
- 推荐处置:
  1. (高优:孤立明确删 candidate)
  2. (中优:需 caller 扩展 / 留作未来 feature 入口 candidate)
  3. (低优:debug/test only widget candidate)

**closeout**:本批仅 scan,删 / 扩 caller 由下波 task 起 spec(类似 W17 T03 follow-up 删 2 死 provider 体例)。
```

6. 写好后:
```bash
git add docs/handoff/deadcode_scan_2026-05-19.md
git commit -m "docs(nightshift T08): 死代码 dry-run scan"
```

## 硬约束

- **只新增** `docs/handoff/deadcode_scan_2026-05-19.md` **一个文件**
- **绝对不删任何代码** — 即使 `dart fix --dry-run` 提示删,也只列不执行
- **不动** `lib/` / `data/` / `test/` / 任何其他 docs/ 文件
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `numbers.yaml` / `IDS_REGISTRY.md`
- **数字必 grep 实测**(memory `feedback_closeout_numbers_grep`)

## 完成动作

```bash
wc -l docs/handoff/deadcode_scan_2026-05-19.md  # ≤ 100
git add docs/handoff/deadcode_scan_2026-05-19.md
git commit -m "docs(nightshift T08): 死代码 dry-run scan"
```

报告一句话「T08 done (scan md N 行, X provider / Y service / Z widget 0-引用 candidates)」。
