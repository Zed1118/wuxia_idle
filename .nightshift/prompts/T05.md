# Nightshift T05 · BaikeScreen MainMenu 11 按钮导航 + 见闻 tab 6 档时间 override edge

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T05`(branch `nightshift/T05`,从 `main` HEAD `217ddf7` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。P1 #42 Phase 1(2026-05-17)新落 `BaikeScreen` 同时 MainMenu 10→11 按钮(新增「江湖见闻录」入口,语义"江湖事"类查阅,师徒后)。新按钮的 widget 导航 0 test 覆盖。**另**:`BaikeScreen` 见闻 tab 在 override 非空 feed 时已有 1 test,但 6 档相对时间格式 override 详化未覆盖。

本任务补 3 个 widget test,产出独立 `_navigation_test.dart` 不动现有 test。

## 红线:约束语义不写瞬时事实

MainMenu 按钮数断言:`find.byType(ElevatedButton)` 等 widget 类型集合大小 ≥ 11(`MainMenu.buttons.length` 维度);新增「江湖见闻录」按钮存在性用文案匹配 + tap → 导航目标 类型校验(`find.byType(BaikeScreen)`),不写"按钮位置 index = 10"。

## 目标

产出**唯一**新文件:`test/features/baike/presentation/baike_screen_navigation_test.dart`

## 操作

1. 读 `lib/main.dart` / MainMenu 所在 widget 文件,确认:
   - MainMenu 是哪个 widget(MainMenuScreen / HomeFeedScreen home 已经替换 main.dart home)
   - 11 按钮列表
   - 「江湖见闻录」按钮 tap 链路(push BaikeScreen)
2. 读现有 MainMenu 测试(如有)学体例;若无独立测试,从 `test/features/baike/presentation/baike_screen_test.dart` 起手
3. 新增 3 个 testWidgets:
   - **A. MainMenu 11 按钮中含「江湖见闻录」**:pump MainMenu → `find.text('江湖见闻录')` 至少 1 实例
   - **B.「江湖见闻录」tap 导航到 BaikeScreen**:tap → `pumpAndSettle` → `find.byType(BaikeScreen)` 存在
   - **C. 见闻 tab 6 档时间 override 详化**:fixture 6 个 GameEvent 各档时间(< 5min / 30min / 3h / 26h / 3d / 10d)→ override 见闻 tab → 6 个文案各档格式正则匹配(`刚才` / `\d+ 分钟前` / `\d+ 小时前` / `昨天` / `\d+ 天前` / `\d+月\d+日`)
4. **新 test 必须 `flutter test` 全过**。若 MainMenu 在 main.dart home 已被 HomeFeedScreen 替换,改为「从 HomeFeedScreen 跳 MainMenu 跳 BaikeScreen」链路 或 直接 pump MainMenuScreen widget

## 硬约束

- **只新增** `test/features/baike/presentation/baike_screen_navigation_test.dart`
- **不动** `lib/` / `data/` / 其他 test
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`

## 完成动作

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/baike/presentation/baike_screen_navigation_test.dart
git add test/features/baike/presentation/baike_screen_navigation_test.dart
git commit -m "test(nightshift T05): BaikeScreen MainMenu 导航 + 时间 override edge"
```

报告一句话「T05 done (added N navigation + time edge cases)」。
