# Nightshift T05 · P3.3 PVP Phase 1 spec 起草

你是 Claude Opus 4.7,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T05`(branch `nightshift/T05`,从 main HEAD 起)内独立完成本任务。

## 背景

P3.3 PVP Phase 0 已 done(commit `b0ffe9a` 之 PR #4),含 6 维 reality check + Q1-Q5 候选 + GDD §12.3 升档草案。详 `docs/phase0/p3_3_pvp_phase0_2026-05-24.md`。

**下一步**:Phase 1 spec doc 起草,沿 P1.2 spec 默认决议版体例。Q1-Q5 用 Phase 0 推荐默认决议。

## 目标

新建 `docs/spec/p3_3_pvp_spec_2026-05-24.md`(120-160 行),9 节体例(沿 P1.2 spec):

| 节 | 内容 |
|---|---|
| §1 | 范围 IN / OUT(明确 P3.3 PVP cover 异步对决 vs 同步对战 / 排行榜) |
| §2 | schema(PvpRecord Isar @Collection + 字段 + composite index) |
| §3 | yaml 数值(numbers.yaml `pvp.*` 段:匹配区间 / 段位 / 数值红线) |
| §4 | 行为(PvpService 匹配 + Strategy 复用现有 BattleStrategy 链 + 离线快照体例) |
| §5 | UI(MainMenu 入口 + PvpScreen + 段位 widget) |
| §6 | narrative(data/lore/pvp/ 体例 + 写 N 条事件文案占位) |
| §7 | 测试(R1 离线快照 + R2 BattleStrategy 复用 + R3 段位计算 + R4 narrative loader) |
| §8 | batch 拆分(Phase 1 spec / Phase 2 schema+yaml / Phase 3 logic / Phase 4 UI / Phase 5 narrative + closeout) |
| §9 | 风险(数值红线 / Strategy 复用撞当前 P3.1.B / 离线快照 fairness) |

## Q1-Q5 默认决议(沿 Phase 0 推荐)

读 `docs/phase0/p3_3_pvp_phase0_2026-05-24.md` 找 Q1-Q5 各候选,挑标"默认/推荐/A 选项"的写到 spec §1,标注「Q1 默认: ... / 真定见 P3.3 Phase 0 §X」。

## 允许修改

- `docs/spec/p3_3_pvp_spec_2026-05-24\.md`

## 禁止修改

- `lib/.*`
- `test/.*`
- `data/.*`
- `docs/phase0/.*`(读不改)
- `docs/spec/(p1_2_|p3_4_).*`(仅参考)
- `GDD\.md`
- `pubspec.*`

## 操作

### Phase 0 · reality check(5 min)

```bash
cd /Users/a10506/Desktop/wuxia-idle-T05
git log -1 --oneline
ls docs/phase0/p3_3_pvp_phase0_2026-05-24.md
ls docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
wc -l docs/phase0/p3_3_pvp_phase0_2026-05-24.md   # 应 41
# 看 Phase 0 Q1-Q5 候选
cat docs/phase0/p3_3_pvp_phase0_2026-05-24.md
# 看 P3.1.B 现有 Strategy 体例(P3.3 要复用)
grep -rn "BattleStrategy\|implements BattleStrategy" lib/features/battle/ | head -10
# 看 P1.2 spec 9 节体例(默认决议引述法)
head -50 docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md
```

### Step 1: 起 spec 9 节框架

按 §1-§9 写大纲。

### Step 2: 各节填充

- §1 IN(异步 PVP / 离线快照 / 段位) OUT(同步实时对战 / 大群战 留 P5+);Q1-Q5 默认引述
- §2 schema:PvpRecord @Collection(matchId / leftSnapshot / rightSnapshot / winnerId / timestamp + composite [playerId, timestamp])
- §3 yaml 至少 3-5 数值字段(段位阈值 / 匹配区间 / 衰减)
- §4 行为:PvpService.match(playerId) → snapshot 双方 → 调 BattleStrategy 链(复用 default_ground / lightfoot / mass_battle)→ 写 PvpRecord
- §5 UI:MainMenu「PVP」入口 + PvpScreen(段位 widget + match button + history list)2-3 widget
- §6 narrative:写 1 个 pvp_event yaml 样本(20-30 行)
- §7 R1-R4:R1 离线快照 e2e / R2 复用 Strategy 不破坏 / R3 段位计算 / R4 narrative loader
- §8 5 batch 拆分(对照 P2.2 / P3.1.B 节奏)
- §9 风险 3-5 条:**数值红线**(伤害 ≤ §5.4 cap) / **Strategy 复用 collision**(P3.1.B 已 wire damage_multiplier,PVP 要 0 改 BattleStrategy) / **离线快照 fairness**(快照时刻 vs 玩家当前状态 drift)

### Step 3: 体量校验

```bash
wc -l docs/spec/p3_3_pvp_spec_2026-05-24.md   # 期望 120-160
```

### Step 4: commit(不 push)

```bash
git add docs/spec/p3_3_pvp_spec_2026-05-24.md
git commit -m "[spec] P3.3 PVP Phase 1 默认决议版 · nightshift T05

- 9 节体例沿 P1.2 spec
- Q1-Q5 用 Phase 0 推荐默认值
- 异步 PVP + 离线快照, 同步 / 大群战留 P5+
- 5 batch 拆分对照 P2.2 / P3.1.B
"
```

## 不要做

- 不要改 GDD.md
- 不要动 lib/test/data
- 不要拍 Q1-Q5 真定
- 不要写 narrative 真文案(只写 1 个样本)
- 不要 git push
- 不要超 160 行(memory feedback_doc_inflation_overnight spec ≤150)
- commit message **必须**含字符串 `nightshift T05`

## 参考

- `docs/phase0/p3_3_pvp_phase0_2026-05-24.md` Phase 0 41 行
- `docs/spec/p1_2_jianghu_enmity_spec_2026-05-24.md` 144 行(体例样本)
- `docs/handoff/p3_1_b_closeout_2026-05-24.md` P3.1.B damage_multiplier(数值红线参考)
