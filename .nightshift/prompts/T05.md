# Nightshift T05 · lib/ 目录结构对照 CLAUDE.md §3 全扫描审计

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T05`(branch `nightshift/T05`,从 `main` HEAD `fc25207` 起)内独立完成本任务。

## 背景(无需展开 lookup)

挂机武侠 Flutter Desktop 武侠 idle 游戏。CLAUDE.md §3 锚定 lib/ 目录结构(`core/{domain,application}/` + `data/{defs/,*.dart,isar_provider}` + `features/<X>/{domain,application,presentation}/` + `shared/{effects,theme,utils,strings.dart}` + `main.dart`)。Phase 5 #3 第 6 批(W15)做了 lib/ 100% finalization 销账(closeout `week15_phase5_3_lib_structure_finalization_2026-05-16.md`),应**0 漂移**。本任务做一次审计扫描,产出 doc 锁定基线状态;若发现新漂移列出具体路径。

## 目标

产出**唯一**新文件:`docs/handoff/wuxia_lib_structure_audit_2026-05-17.md`

## 操作

1. 读 `CLAUDE.md` §3 目录结构期望表(用 grep `## 3. 目录结构`)
2. `find lib/ -type d | sort` 列出所有目录
3. `find lib/ -type f -name "*.dart" -not -name "*.g.dart" | sort` 列出所有源文件
4. 对照 §3 期望检查:
   - `lib/main.dart` 存在 ✓
   - `lib/core/{domain,application}/` 存在
   - `lib/data/{defs/,isar_provider.dart,*.dart}/` 存在
   - `lib/features/<X>/{domain,application,presentation}/` 14 个 feature 各子目录(基于 W15 Phase 5 #3 第 6 批 finalization 销账,14 feature 含 main_menu / narrative)
   - `lib/shared/{effects,theme,utils,strings.dart}/` 存在
   - **不应存在**:`lib/ui/` / `lib/utils/` / `lib/providers/` / `lib/data/models/` / `lib/services/`(全在 Phase 5 #3 各批迁出)
5. 找漂移:
   - 任何 `lib/<top>` 不在期望集
   - 任何 feature 直接持有 `.dart` 文件而非放 `domain/application/presentation/` 三子目录
   - 任何 `.g.dart` 文件不与对应 `.dart` 同目录(应不会发生,build_runner 默认就近)
6. 写 doc 严格按下面结构:

```markdown
# lib/ 目录结构审计 vs CLAUDE.md §3(2026-05-17)

> Nightshift T05 产出。预期 0 漂移(Phase 5 #3 第 6 批 finalization 销账后)。

## 0. 扫描范围
- 工作树:`/Users/a10506/Desktop/wuxia-idle-T05`(基于 main HEAD `fc25207`)
- 输入:`lib/` 全树 + `CLAUDE.md` §3 锚点

## 1. 目录树快照

```
lib/
├── main.dart
├── core/
│   ├── domain/
│   │   └── ...
│   └── application/
│       └── ...
├── data/
│   ├── defs/
│   ├── isar_provider.dart
│   └── ...
├── features/
│   ├── battle/
│   │   ├── domain/
│   │   ├── application/
│   │   └── presentation/
│   ├── ...(列 14 个 feature)
└── shared/
    ├── effects/
    ├── theme/
    ├── utils/
    └── strings.dart
```

## 2. 14 feature 子目录三态完整性

| feature | domain/ | application/ | presentation/ |
|---|---|---|---|
| battle | ✓ | ✓ | ✓ |
| ...(14 行) | | | |

(注:某些 feature 可能不需要全部三层,例如纯 UI feature 可能没有 domain/,记录但不算漂移)

## 3. §3 期望对账

| 期望 | 状态 | 备注 |
|---|---|---|
| `lib/main.dart` 存在 | ✓/✗ |  |
| `lib/core/{domain,application}/` 存在 | ✓/✗ |  |
| `lib/data/` 持基础设施(无 features import) | ✓/✗ |  |
| `lib/features/` 14 个 feature | ✓/✗ | 数量 |
| `lib/shared/` 存在 | ✓/✗ |  |
| **不应存在** `lib/ui/` | ✗ (=正确) |  |
| **不应存在** `lib/utils/` | ✗ (=正确) |  |
| **不应存在** `lib/providers/` | ✗ (=正确) |  |
| **不应存在** `lib/services/` | ✗ (=正确) |  |
| **不应存在** `lib/data/models/` | ✗ (=正确) |  |

## 4. 漂移清单

(若 0 漂移,写一句「无漂移,Phase 5 #3 第 6 批 finalization 状态稳定」即可。若有漂移逐条列具体路径 + 性质)

## 5. 后续维护建议

- W18+ 新增 feature:严格 `lib/features/<new>/{domain,application,presentation}/` 三子目录
- 任何 lib/ 顶层新文件:必须在 `CLAUDE.md` §3 表内或新加锚点
- 本审计扫描器(`.nightshift/prompts/T05.verify.sh` 风格)定期跑保证 0 漂移
```

## 硬约束

- **只产** doc **一个文件**
- **不动** `lib/` / `test/` / `data/` / `assets/`
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `data/numbers.yaml` / `IDS_REGISTRY.md`
- 不跑 `flutter pub get` / `dart run build_runner` / `flutter test`(dispatcher 跑 verify)
- 不调用网络

## 完成动作

doc 写好后:
```bash
git add docs/handoff/wuxia_lib_structure_audit_2026-05-17.md
git commit -m "docs(nightshift T05): lib/ 目录结构对照 CLAUDE.md §3 全扫描审计"
```

**不要 push**。
报告一句话「T05 done (X drifts found)」或「T05 done (0 drifts)」。
