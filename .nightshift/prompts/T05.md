# Nightshift T05 · CodexTab widget test 加固 115→160+

你是 Claude Code 实例,被 nightshift dispatcher 调起,在 git worktree `/Users/a10506/Desktop/wuxia-idle-T05`(branch `nightshift/T05`,从 `main` HEAD `3d24af1` 起)内独立完成本任务。

## 背景

挂机武侠 Flutter Desktop 武侠 idle 游戏。P1 #42 Phase 2 §10 P1.z + P2 扩段(2026-05-18 销账)新落 `CodexTab` widget(`lib/features/codex/presentation/codex_tab.dart`),百科 ListView 渲染 19 条 entry(12 机制 + 7 lore)+ 9 分类 + unlockedCount 用「档数」语义(8 档 1/2/.../8)。

当前 `test/features/codex/presentation/codex_tab_test.dart` 115 行,有基础覆盖。本 task 补 10-15 边界 case 到 160+ 行。

## 红线:约束语义不写瞬时事实

参 `feedback_red_line_test_semantics`:断言用 `CodexIndex.entries.where(...).length` 派生 / `CodexCategory.values` 集合等;不写「12 机制」「7 lore」字面计数(P3+ 扩段必 break)。**实际现有 test 已遵循 dynamic mechanicCount/loreCount 体例**,本批沿用。

## ListView viewport 红线(memory `feedback_listview_widget_test_viewport`)

≥ 8 行 ListView 必扩 viewport:
```dart
await tester.binding.setSurfaceSize(const Size(800, 3000));
addTearDown(() => tester.binding.setSurfaceSize(null));
```
否则尾行 lazy build 漏 → find 报 0(本批 19 行 entry,viewport 必扩)。

## 目标

改 **唯一**文件:`test/features/codex/presentation/codex_tab_test.dart`(115 → ≥ 160 行,base case → 12-15 case)

## 操作

1. 读 `lib/features/codex/presentation/codex_tab.dart` 全文,确认:
   - tab 实际 widget tree(TabBar 2 tab 机制/江湖背景 vs 单 ListView 分段?)
   - 每 entry tile(`_CodexListTile`)的 locked / unlocked 派生(`entry.step ≤ tutorialStep` 解锁)
   - lore 永远 unlocked(`entry.category.isLore` 短路)
   - unlockedCount header「已解锁 N / 8」语义(只数机制档,8 档)
2. 读 `lib/features/codex/domain/codex_index.dart` 找 `CodexIndex.entries` 静态 list + `CodexCategory` 9 值
3. 读 `test/features/codex/presentation/codex_tab_test.dart` 当前 case 学体例 + provider override 模式
4. 加 10-15 edge case 分类:
   - **A. 19 条全 locked**(tutorialStep=0):lore 仍 unlocked,机制全灰显
   - **B. 部分 unlock 矩阵**:tutorialStep=2 / 4 / 8(满)各 1 case,验 unlockedCount 派生
   - **C. CodexCategory 9 分类渲染顺序**:从 `CodexCategory.values` 顺序断言渲染分组顺序
   - **D. ListView viewport setSurfaceSize 800x3000**:尾行 entry 可 find(memory 锚)
   - **E. InkWell push to detail**:tap 某 entry → Navigator push CodexEntryDetail
   - **F. lock_outline 图标**:locked 状态有 `Icons.lock_outline` find
   - **G. 「已解锁 N / 8」计数边界**:unlockedCount=0 / 1 / 8(满)各 1 case,数字派生
   - **H. 「待解锁」文案**:locked entry tile 有项目 `Strings.codexLockedHint` find
   - **I. lore 段顺序**:江湖背景 7 lore 渲染顺序 == `CodexIndex.entries.where(isLore).toList()`
   - **J. tab 切换**(若双 tab):tap 江湖背景 tab → 7 lore 渲染 / tap 机制 tab → 12 机制渲染
   - **K. (可选)空 tutorialStep null fallback**:provider 未 ready 不 crash
5. 全 testWidgets 跑过 `flutter test test/features/codex/presentation/codex_tab_test.dart`(若实际 widget 与背景描述不符,**以代码实际为准**调整断言,不修 lib/)

## 硬约束

- **只改** `test/features/codex/presentation/codex_tab_test.dart` **一个文件**
- **不动** `lib/` / `data/` / 其他 test/
- **不动** `GDD.md` / `CLAUDE.md` / `PROGRESS.md` / `numbers.yaml` / `IDS_REGISTRY.md`
- testWidgets 总数 **≥ 12**(扩到 162 行 + 12 case)
- 红线:不写「12 机制」「7 lore」字面值(用 `mechanicCount` / `loreCount` 派生)
- ListView viewport `setSurfaceSize(800, 3000) + addTearDown` 必须用

## 完成动作

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/codex/presentation/codex_tab_test.dart
# 全过后:
git add test/features/codex/presentation/codex_tab_test.dart
git commit -m "test(nightshift T05): CodexTab widget 边界 +10 case + ListView viewport"
```

报告一句话「T05 done (CodexTab testWidgets N, file ≥ 160 行)」。
