# 中文散写门禁 allowlist(guard 消费·扫描器 test/tools/chinese_literal_audit.dart)。
# 格式:每行 `<相对路径> :: <字面量片段>`;# 开头为注释;空行忽略。**不含行号**(行号会漂移)。
#
# 口径 A(2026-07-25 用户拍板)已在扫描器里自动豁免:注释 / 诊断异常串(throw·assert·
# Error·Exception) / @Deprecated 注解 / 开发者日志(debugPrint·print·log) / debug 域 /
# 三集中 sink(strings.dart·enum_localizations.dart·battle_log.dart)。
# 本表 = 豁免之外的**存量**散写,门禁上线时冻结为基线,只减不增;清理一条删一条
# (guard 2 会拦已清理的残留)。
#
# ── 清账史 ──
# 2026-07-25 建表冻结 31 → §一#12 拍板 D 销 damage_calculator 5(改 ASCII)→ 26
# 2026-07-25 存量清理批(BACKLOG §二#3)销 23 → **余 3**。销账明细:
#   · A 玩家可见 UI 文案 → 迁 UiStrings:synergy_def 6 / asset_fallback「缺图」/
#     equipment_glyph「器」/ item_slot「未达境界」/ tower_entry_flow「· 胜利」
#   · B 种子角色名·副本名 → 迁 UiStrings(大弟子/二弟子/断魂庄复用既有常量,不新增):
#     master_builder 4 / gauntlet_service 2 / gauntlet_providers 1
#   · D game_repository 3 条 label/loc → 改 ASCII 自然出表(沿 §一#12 先例;串只经
#     参数/局部变量流进 throw StateError,是真诊断串)
#   · **建表时归 D 的 encounter_service 3 条经 Phase 0 证伪**:其 reason 由
#     `UiStrings.encounterSkillEquipFailedReason` 呈现给玩家(encounter_skill_section
#     :193 真 return),不是纯诊断串——改 Error 构造只会让门禁闭嘴而玩家照看,
#     故按 A 类迁 UiStrings。
#
# ── 余 3 条的处置 ──
# C 占位兜底串(CLAUDE §5.6 允许开发期占位):narrative_loader「[剧情待补]」/
#   encounter_event_loader「[文案待补]」→ 保留。
# E damage_popup 的 r'^暴击 (\d+) 伤害$' = 对 UiStrings.criticalDamagePopup 输出的
#   **反解析**,中文是模板复制品(改 strings.dart 会静默失配)。真修法是消除 parse-back
#   让上游传结构化数据,已立 BACKLOG §二#4,不按散写迁 UiStrings。

lib/data/encounter_event_loader.dart :: '[文案待补:$id]'
lib/data/narrative_loader.dart :: '[剧情待补：$id]'
lib/features/battle/presentation/damage_popup.dart :: r'^暴击 (\d+) 伤害$'
